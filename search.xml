<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Java 笔试题</title>
    <url>/java/cc356c4a-ea8c-4755-bd96-b982473795f6.html</url>
    <content><![CDATA[<p>这篇文章为本人整理的 Java 方向笔试题，其中包含各种方面内容的笔试题（主要为选择题）及其答案。</p><a id="more"></a><h2 id="异常"><a href="#异常" class="headerlink" title="异常"></a>异常</h2><p><strong>调用下面的方法，得到的返回值是什么？</strong></p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getNum</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">int</span> a = <span class="number">1</span> / <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">2</span>;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">3</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>答案：3</strong></p><p>代码走到第 3 行的时候遇到了一个 MathException，这时第 4 行的代码就不会执行了，代码直接跳转到 catch 语句中，走到第 6 行的时候，异常机制有一个原则：如果在 catch 中遇到了 return 或者异常等能使该函数终止的话那么有finally 就必须先执行完 finally 代码块里面的代码然后再返回值。因此代码又跳到第 8 行，可惜第 8 行是一个 return 语句，那么这个时候方法就结束了，因此第 6 行的返回结果就无法被真正返回。如果 finally 仅仅是处理了一个释放资源的操作，那么该道题最终返回的结果就是 2。因此上面返回值是 3。</p><h2 id="常用-API"><a href="#常用-API" class="headerlink" title="常用 API"></a>常用 API</h2><h3 id="String"><a href="#String" class="headerlink" title="String"></a>String</h3><p><strong>请说出下面程序的输出</strong></p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">StringEqualTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        String s1 = <span class="string">"Programming"</span>;</span><br><span class="line">        String s2 = <span class="keyword">new</span> String(<span class="string">"Programming"</span>);</span><br><span class="line">        String s3 = <span class="string">"Program"</span>;</span><br><span class="line">        String s4 = <span class="string">"ming"</span>;</span><br><span class="line">        String s5 = <span class="string">"Program"</span> + <span class="string">"ming"</span>;</span><br><span class="line">        String s6 = s3 + s4;</span><br><span class="line">        System.out.println(s1 == s2);    <span class="comment">//false</span></span><br><span class="line">        System.out.println(s1 == s5);    <span class="comment">//true</span></span><br><span class="line">        System.out.println(s1 == s6);    <span class="comment">//false</span></span><br><span class="line">        System.out.println(s1 == s6.intern());    <span class="comment">//true</span></span><br><span class="line">        System.out.println(s2 == s2.intern());    <span class="comment">//false</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>解答：</p><ul><li>String 对象的 intern() 方法会得到字符串对象在常量池中对应的版本的引用（如果常量池中有一个字符串与 String 对象的 equals 结果是 true），如果常量池中没有对应的字符串，则该字符串将被添加到常量池中，然后返回常量池中字符串的引用；</li><li>字符串的 <code>+</code> 操作其本质是创建了 StringBuilder 对象进行 append 操作，然后将拼接后的 StringBuilder 对象用 toString 方法处理成 String 对象，这一点可以用 javap -c StringEqualTest.class 命令获得 class 文件对应的 JVM 字节码指令就可以看出来。</li></ul><p><strong>下面程序的运行结果是</strong></p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    String str1 = <span class="string">"hello"</span>;</span><br><span class="line">    String str2 = <span class="string">"he"</span> + <span class="keyword">new</span> String(<span class="string">"llo"</span>);</span><br><span class="line">    String str3 = <span class="string">"he"</span> + <span class="string">"llo"</span>;</span><br><span class="line">    System.err.println(str1 == str2);   <span class="comment">// false</span></span><br><span class="line">    System.err.println(str1 == str3);   <span class="comment">// true</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="Date-相关"><a href="#Date-相关" class="headerlink" title="Date 相关"></a>Date 相关</h3><p><strong>如何取得年月日、小时分钟秒？</strong></p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.time.LocalDateTime;</span><br><span class="line"><span class="keyword">import</span> java.util.Calendar;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DateTimeTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Calendar cal = Calendar.getInstance();</span><br><span class="line">        System.out.println(cal.get(Calendar.YEAR));</span><br><span class="line">        System.out.println(cal.get(Calendar.MONTH)); <span class="comment">// 0 - 11</span></span><br><span class="line">        System.out.println(cal.get(Calendar.DATE));</span><br><span class="line">        System.out.println(cal.get(Calendar.HOUR_OF_DAY));</span><br><span class="line">        System.out.println(cal.get(Calendar.MINUTE));</span><br><span class="line">        System.out.println(cal.get(Calendar.SECOND));</span><br><span class="line">        <span class="comment">// Java 8</span></span><br><span class="line">        LocalDateTime dt = LocalDateTime.now();</span><br><span class="line">        System.out.println(dt.getYear());</span><br><span class="line">        System.out.println(dt.getMonthValue()); <span class="comment">// 1 - 12</span></span><br><span class="line">        System.out.println(dt.getDayOfMonth());</span><br><span class="line">        System.out.println(dt.getHour());</span><br><span class="line">        System.out.println(dt.getMinute());</span><br><span class="line">        System.out.println(dt.getSecond());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>如何取得从 1970 年 1 月 1 日 0 时 0 分 0 秒到现在的毫秒数？</strong></p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GetTime</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"第一种："</span> + Calendar.getInstance().getTimeInMillis());</span><br><span class="line">        System.out.println(<span class="string">"第二种："</span> + System.currentTimeMillis());</span><br><span class="line">        System.out.println(<span class="string">"第三种："</span> + Clock.systemDefaultZone().millis());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>如何取得某月的最后一天？</strong></p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GetLastDay</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SimpleDateFormat format = <span class="keyword">new</span> SimpleDateFormat(<span class="string">"yyyy-MM-dd"</span>);</span><br><span class="line">        <span class="comment">//获取当前月第一天：</span></span><br><span class="line">        Calendar c = Calendar.getInstance();</span><br><span class="line">        c.add(Calendar.MONTH, <span class="number">0</span>);</span><br><span class="line">        c.set(Calendar.DAY_OF_MONTH, <span class="number">1</span>);<span class="comment">//设置为 1 号，当前日期既为本月第一天</span></span><br><span class="line">        String first = format.format(c.getTime());</span><br><span class="line">        System.out.println(<span class="string">"first:"</span> + first);</span><br><span class="line">        <span class="comment">//获取当前月最后一天</span></span><br><span class="line">        Calendar ca = Calendar.getInstance();</span><br><span class="line">        ca.set(Calendar.DAY_OF_MONTH, ca.getActualMaximum(Calendar.DAY_OF_MONTH));</span><br><span class="line">        String last = format.format(ca.getTime());</span><br><span class="line">        System.out.println(<span class="string">"last:"</span> + last);</span><br><span class="line">        <span class="comment">//Java 8</span></span><br><span class="line">        LocalDate today = LocalDate.now();</span><br><span class="line">        <span class="comment">//本月的第一天</span></span><br><span class="line">        LocalDate firstday = LocalDate.of(today.getYear(), today.getMonth(), <span class="number">1</span>);</span><br><span class="line">        <span class="comment">//本月的最后一天</span></span><br><span class="line">        LocalDate lastDay = today.with(TemporalAdjusters.lastDayOfMonth());</span><br><span class="line">        System.out.println(<span class="string">"本月的第一天"</span> + firstday);</span><br><span class="line">        System.out.println(<span class="string">"本月的最后一天"</span> + lastDay);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>运行结果：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">first:2019-04-01</span><br><span class="line">last:2019-04-30</span><br><span class="line">本月的第一天2019-04-01</span><br><span class="line">本月的最后一天2019-04-30</span><br></pre></td></tr></table></figure><p><strong>如何格式化日期？</strong><br>java.text.DataFormat 的子类（如 SimpleDateFormat 类）中的 format(Date) 方法可将日期格式化。Java 8 中可以用 java.time.format.DateTimeFormatter 来格式化时间日期，代码如下所示：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.text.SimpleDateFormat;</span><br><span class="line"><span class="keyword">import</span> java.time.LocalDate;</span><br><span class="line"><span class="keyword">import</span> java.time.format.DateTimeFormatter;</span><br><span class="line"><span class="keyword">import</span> java.util.Date;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DateFormatTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SimpleDateFormat oldFormatter = <span class="keyword">new</span> SimpleDateFormat(<span class="string">"yyyy/MM/dd"</span>);</span><br><span class="line">        Date date1 = <span class="keyword">new</span> Date();</span><br><span class="line">        System.out.println(oldFormatter.format(date1));</span><br><span class="line">        <span class="comment">// Java 8</span></span><br><span class="line">        DateTimeFormatter newFormatter = DateTimeFormatter.ofPattern(<span class="string">"yyyy/MM/dd"</span>);</span><br><span class="line">        LocalDate date2 = LocalDate.now();</span><br><span class="line">        System.out.println(date2.format(newFormatter));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>运行结果：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">2019&#x2F;04&#x2F;22</span><br><span class="line">2019&#x2F;04&#x2F;22</span><br></pre></td></tr></table></figure><p>补充：Java 的时间日期 API 一直以来都是被诟病的东西，为了解决这一问题，Java 8 中引入了新的时间日期 API， 其中包括 LocalDate、LocalTime、LocalDateTime、Clock、Instant 等类，这些的类的设计都使用了不变模式，因此是线程安全的设计。</p><p><strong>打印昨天的当前时刻？</strong></p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.Calendar;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">YesterdayCurrent</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Calendar cal = Calendar.getInstance();</span><br><span class="line">        cal.add(Calendar.DATE, -<span class="number">1</span>);</span><br><span class="line">        System.out.println(cal.getTime());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//java-8</span></span><br><span class="line"><span class="keyword">import</span> java.time.LocalDateTime;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">YesterdayCurrent</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        LocalDateTime today = LocalDateTime.now();</span><br><span class="line">        LocalDateTime yesterday = today.minusDays(<span class="number">1</span>);</span><br><span class="line">        System.out.println(yesterday);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="数据类型"><a href="#数据类型" class="headerlink" title="数据类型"></a>数据类型</h2><p>下面 Integer 类型的数值比较输出的结果为？</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Integer f1 = <span class="number">100</span>, f2 = <span class="number">100</span>, f3 = <span class="number">150</span>, f4 = <span class="number">150</span>;</span><br><span class="line">        System.out.println(f1 == f2);</span><br><span class="line">        System.out.println(f3 == f4);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>解析：<br>简单的说，如果整型字面量的值在 -128 到 127 之间，那么不会 new 新的 Integer 对象，而是直接引用常量池中的 Integer 对象，所以上面的题中 <code>f1==f2</code> 的结果是 true，而 <code>f3==f4</code> 的结果是 false。</p><h2 id="集合"><a href="#集合" class="headerlink" title="集合"></a>集合</h2><h3 id="ArrayList"><a href="#ArrayList" class="headerlink" title="ArrayList"></a>ArrayList</h3><p><strong><code>ArrayList list = new ArrayList(20);</code> 语句中的 list 集合大小扩充了几次（A）</strong><br>A.0<br>B.1<br>C.2<br>D.3</p><h3 id="HashMap-排序"><a href="#HashMap-排序" class="headerlink" title="HashMap 排序"></a>HashMap 排序</h3><p>已知一个 <code>HashMap&lt;Integer，User&gt;</code> 集合， User 有 name（String）和 age（int）属性。请写一个方法实现对HashMap 的排序功能，该方法接收 <code>HashMap&lt;Integer，User&gt;</code> 为形参，返回类型为 <code>HashMap&lt;Integer，User&gt;</code>，要求对 HashMap 中的 User 的 age 倒序进行排序。排序时 key=value 键值对不得拆散。</p><p>注意：要做出这道题必须对集合的体系结构非常的熟悉。HashMap 本身就是不可排序的，但是该题偏偏让 HashMap 排序，那我们就得想在 API 中有没有这样的 Map 结构是有序的，我们不难发现其中 LinkedHashMap 就具有这样的结构，是链表结构有序的，更可喜的是他是 HashMap 的子类，我们返回 <code>LinkedHashMap&lt;Integer，User&gt;</code> 即可，还符合面向接口编程的思想。</p><p>但凡是对集合的操作，我们应该保持一个原则就是能用 JDK 中的 API 就用 JDK 中的 API，比如排序算法我们不应该去用冒泡或者选择，而是首先想到用 Collections 集合工具类。</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.*;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">HashMapTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        HashMap&lt;Integer, User&gt; users = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">        users.put(<span class="number">1</span>, <span class="keyword">new</span> User(<span class="string">"张三"</span>, <span class="number">25</span>));</span><br><span class="line">        users.put(<span class="number">3</span>,<span class="keyword">new</span> User(<span class="string">"李四"</span>,<span class="number">22</span>));</span><br><span class="line">        users.put(<span class="number">2</span>, <span class="keyword">new</span> User(<span class="string">"王五"</span>, <span class="number">28</span>));</span><br><span class="line">        System.out.println(users);</span><br><span class="line">        HashMap&lt;Integer, User&gt; sortHashMap = sortHashMap(users);</span><br><span class="line">        System.out.println(sortHashMap);</span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * 控制台输出内容</span></span><br><span class="line"><span class="comment">         * &#123;1=User [name=张三, age=25], 2=User [name=王五,age=28], 3=User [name=李四, age=22]&#125;</span></span><br><span class="line"><span class="comment">         * &#123;2=User [name=王五, age=28], 1=User [name=张三, age=25], 3=User [name=李四, age=22]&#125;</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> HashMap&lt;Integer, User&gt; <span class="title">sortHashMap</span><span class="params">(HashMap&lt;Integer, User&gt; map)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 首先拿到 map 的键值对集合</span></span><br><span class="line">        Set&lt;Map.Entry&lt;Integer, User&gt;&gt; entrySet = map.entrySet();</span><br><span class="line">        <span class="comment">// 将 set 集合转为 List 集合，为什么，为了使用工具类的排序方法</span></span><br><span class="line">        List&lt;Map.Entry&lt;Integer,User&gt;&gt; list = <span class="keyword">new</span> ArrayList&lt;Map.Entry&lt;Integer, User&gt;&gt;(entrySet);</span><br><span class="line">        <span class="comment">// 使用 Collections 集合工具类对 list 进行排序，排序规则使用匿名内部类来实现</span></span><br><span class="line">        Collections.sort(list, <span class="keyword">new</span> Comparator&lt;Map.Entry&lt;Integer, User&gt;&gt;() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compare</span><span class="params">(Map.Entry&lt;Integer, User&gt; o1, Map.Entry&lt;Integer, User&gt; o2)</span> </span>&#123;</span><br><span class="line">                <span class="comment">//按照要求根据 User 的 age 的倒序进行排</span></span><br><span class="line">                <span class="keyword">return</span> o2.getValue().getAge() - o1.getValue().getAge();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        <span class="comment">//创建一个新的有序的 HashMap 子类的集合</span></span><br><span class="line">        LinkedHashMap&lt;Integer, User&gt; linkedHashMap = <span class="keyword">new</span> LinkedHashMap&lt;Integer, User&gt;();</span><br><span class="line">        <span class="comment">//将 List 中的数据存储在 LinkedHashMap 中</span></span><br><span class="line">        <span class="keyword">for</span> (Map.Entry&lt;Integer,User&gt; entry : list) &#123;</span><br><span class="line">            linkedHashMap.put(entry.getKey(), entry.getValue());</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> linkedHashMap;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">User</span><span class="params">(String name, <span class="keyword">int</span> age)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getAge</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setAge</span><span class="params">(<span class="keyword">int</span> age)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"User&#123;"</span> +</span><br><span class="line">                <span class="string">"name='"</span> + name + <span class="string">'\''</span> +</span><br><span class="line">                <span class="string">", age="</span> + age +</span><br><span class="line">                <span class="string">'&#125;'</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="进程"><a href="#进程" class="headerlink" title="进程"></a>进程</h2><h3 id="进程状态"><a href="#进程状态" class="headerlink" title="进程状态"></a>进程状态</h3><p><strong>下列哪些方法可以使线程从运行状态进入到阻塞状态（BCD）</strong><br>A.notify<br>B.wait<br>C.sleep<br>D.yield</p><h3 id="Thread"><a href="#Thread" class="headerlink" title="Thread"></a>Thread</h3><p><strong>下列关于 Thread 类提供的线程控制的方法中，错误的一项是（A）</strong><br>A.在线程A中执行线程 B 的 join() 方法，则线程 A 等待直到 B 执行完成。<br>B.线程A通过调用 interrupt() 方法来中断其阻塞状态。<br>C.currentThread() 方法返回当前线程的引用。<br>D.若线程A调用方法 isAlive() 返回为 true，则说明 A 正在执行中。</p><h3 id="锁"><a href="#锁" class="headerlink" title="锁"></a>锁</h3><p><strong>关于锁 locks，描述正确的是（A）</strong><br>A.当一个事务在表上防止了共享锁（shared lock），其他事务，能阅读表里的数据<br>B.当一个事务在表上防止了共享锁（shared lock），其他事务，能更新表里的数据<br>C.当一个事务在表上防止了排他锁（exclusive lock），其他事务，能阅读表里的数据<br>D.当一个事务在表上防止了排他锁（exclusive lock），其他事务，能更新表里的数据</p><h2 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h2><p><strong>下列哪两个数据结构，同时具有较高的查找和删除性能？（CD）</strong><br>A.有序数组<br>B.有序链表<br>C.AVL 树<br>D.Hash 表</p><p>分析如下：</p><table><thead><tr><th>数据结构</th><th align="center">查找</th><th align="center">插入</th><th align="center">删除</th><th align="center">遍历</th></tr></thead><tbody><tr><td>数组</td><td align="center">O(N)</td><td align="center">O(1)</td><td align="center">O(N)</td><td align="center">—</td></tr><tr><td>有序数组</td><td align="center">O(logN)</td><td align="center">O(N)</td><td align="center">O(N)</td><td align="center">O(N)</td></tr><tr><td>链表</td><td align="center">O(N)</td><td align="center">O(1)</td><td align="center">O(N)</td><td align="center">—</td></tr><tr><td>有序链表</td><td align="center">O(N)</td><td align="center">O(N)</td><td align="center">O(N)</td><td align="center">O(N)</td></tr><tr><td>二叉树（一般情况）</td><td align="center">O(logN)</td><td align="center">O(logN)</td><td align="center">O(logN)</td><td align="center">O(N)</td></tr><tr><td>二叉树（最坏情况）</td><td align="center">O(N)</td><td align="center">O(N)</td><td align="center">O(N)</td><td align="center">O(N)</td></tr><tr><td>平衡树（AVL）</td><td align="center">O(logN)</td><td align="center">O(logN)</td><td align="center">O(logN)</td><td align="center">O(N)</td></tr><tr><td>哈希表</td><td align="center">O(1)</td><td align="center">O(1)</td><td align="center">O(1)</td><td align="center">—</td></tr></tbody></table><h3 id="二叉树"><a href="#二叉树" class="headerlink" title="二叉树"></a>二叉树</h3><p>7、已知一棵二叉树，如果先序遍历的节点顺序是：ADCEFGHB ，中序遍历是：CDFEGHAB ，则后序遍历结果为：（D）<br>A.CFHGEBDA<br>B.CDFEGHBA<br>C.FGHCDEBA<br>D. CFHGEDBA</p><p>分析：根据二叉树的前序遍历与中序遍历，可得出二叉树为：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/FDD3EE2368A04892BA8B07E83069A613/368" alt=""></p><h3 id="栈"><a href="#栈" class="headerlink" title="栈"></a>栈</h3><p><strong>一个栈的入栈序列是 A，B，C，D，E，则栈的不可能的输出序列是？（C）</strong><br>A．EDCBA<br>B．DECBA<br>C．DCEAB<br>D．ABCDE</p><p><strong>下列关于栈的描述错误的是（B）</strong><br>A.栈是先进后出的线性表<br>B.栈只能顺序存储<br>C.栈具有记忆功能<br>D.对栈的插入和删除操作中，不需要改变栈底指针</p><h2 id="算法"><a href="#算法" class="headerlink" title="算法"></a>算法</h2><h3 id="排序"><a href="#排序" class="headerlink" title="排序"></a>排序</h3><p><strong>下列排序算法中，哪些时间复杂度不会超过 nlogn？（BC）</strong><br>A.快速排序<br>B.堆排序<br>C.归并排序<br>D.冒泡排序</p><p>分析如下：</p><table><thead><tr><th>排序方法</th><th align="center">时间复杂度</th><th align="center">空间复杂度</th><th align="center">稳定性</th></tr></thead><tbody><tr><td>选择排序</td><td align="center">O(N²)</td><td align="center">O(1)</td><td align="center">不稳定</td></tr><tr><td>冒泡排序</td><td align="center">O(N²)</td><td align="center">O(1)</td><td align="center">稳定</td></tr><tr><td>插入排序</td><td align="center">O(N²)</td><td align="center">O(1)</td><td align="center">稳定</td></tr><tr><td>shell 排序</td><td align="center">O(N¹·³)</td><td align="center">O(1)</td><td align="center">不稳定</td></tr><tr><td>快速排序</td><td align="center">O(NlogN)</td><td align="center">O(NlogN)</td><td align="center">不稳定</td></tr><tr><td>归并排序</td><td align="center">O(NlogN)</td><td align="center">O(1)</td><td align="center">稳定</td></tr><tr><td>堆排序</td><td align="center">O(NlogN)</td><td align="center">O(1)</td><td align="center">不稳定</td></tr></tbody></table><p><strong>初始序列为 1 8 6 2 5 4 7 3 一组数采用堆排序，当建堆（小根堆）完毕时，堆所对应的二叉树中序遍历序列为：（A）</strong><br>A. 8 3 2 5 1 6 4 7<br>B. 3 2 8 5 1 4 6 7<br>C. 3 8 2 5 1 6 7 4<br>D. 8 2 3 5 1 4 7 6</p><p>解析：初始化序列：1 8 6 2 5 4 7 3，小根堆就是要求结点的值小于其左右孩子结点的值，左右孩子的大小没有关系，那么小根堆排序之后为：1 2 4 3 5 6 7 8；中序遍历：左根右，故遍历结果为：8 3 2 5 1 6 4 7，故最后选择的结果：A</p><p><strong>写出一个冒泡排序</strong></p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">bubbleSort</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> arr[] = &#123;-<span class="number">5</span>, <span class="number">29</span>, <span class="number">7</span>, <span class="number">10</span>, <span class="number">5</span>, <span class="number">16</span>&#125;;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; arr.length; i++) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; arr.length - i; j++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (arr[j] &lt; arr[j + <span class="number">1</span>]) &#123;</span><br><span class="line">                <span class="keyword">int</span> temp;</span><br><span class="line">                temp = arr[j];</span><br><span class="line">                arr[j] = arr[j + <span class="number">1</span>];</span><br><span class="line">                arr[j + <span class="number">1</span>] = temp;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; arr.length; i++) &#123;</span><br><span class="line">        System.out.print(<span class="string">" "</span> + arr[i] + <span class="string">" "</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="递归"><a href="#递归" class="headerlink" title="递归"></a>递归</h3><p><strong>当 n = 5 时，下列函数的返回值是：（A）</strong></p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">foo</span><span class="params">(<span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (n &lt; <span class="number">2</span>) <span class="keyword">return</span> n;</span><br><span class="line">    <span class="keyword">return</span> foo(n - <span class="number">1</span>) + foo(n - <span class="number">2</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>A．5<br>B．7<br>C．8<br>D．1</p><p><strong>递归函数最终会结束，那么这个函数一定？（B）</strong><br>A.使用了局部变量<br>B.有一个分支不调用自身<br>C.使用了全局变量或者使用了一个或多个参数<br>D.没有循环调用</p><h2 id="Linux"><a href="#Linux" class="headerlink" title="Linux"></a>Linux</h2><p><strong>Linux 系统中，哪些可以用于进程间的通信？（ABCD ）</strong><br>A．Socket<br>B．共享内存<br>C．消息队列<br>D．信号量</p><p>Linux 中，可用于进程间通信的有：</p><ul><li>管道（Pipe）及有名管道（named pipe）：管道可用于具有亲缘关系进程间的通信，有名管道克服了管道没有名字的限制，因此，除具有管道所具有的功能外，它还允许无亲缘关系进程间的通信；</li><li>信号（Signal）：信号是比较复杂的通信方式，用于通知接受进程有某种事件发生，除了用于进程间通信外，进程还可以发送信号给进程本身；linux 除了支持 Unix 早期信号语义函数 sigal 外，还支持语义符合 Posix.1 标准的信号函数 sigaction（实际上，该函数是基于 BSD 的，BSD 为了实现可靠信号机制，又能够统一对外接口，用 sigaction 函数重新实现了 signal 函数）；</li><li>报文（Message）队列（消息队列）：消息队列是消息的链接表，包括 Posix 消息队列 system V 消息队列。有足够权限的进程可以向队列中添加消息，被赋予读权限的进程则可以读走队列中的消息。消息队列克服了信号承载信息量少，管道只能承载无格式字节流以及缓冲区大小受限等缺点。</li><li>共享内存：使得多个进程可以访问同一块内存空间，是最快的可用 IPC 形式。是针对其他通信机制运行效率较低而设计的。往往与其它通信机制，如信号量结合使用，来达到进程间的同步及互斥。</li><li>信号量（semaphore）：主要作为进程间以及同一进程不同线程之间的同步手段。</li><li>套接口（Socket）：更为一般的进程间通信机制，可用于不同机器之间的进程间通信。起初是由 Unix 系统的 BSD分支开发出来的，但现在一般可以移植到其它类 Unix 系统上：Linux 和 System V 的变种都支持套接字。</li></ul><h2 id="数据库"><a href="#数据库" class="headerlink" title="数据库"></a>数据库</h2><h3 id="连接池"><a href="#连接池" class="headerlink" title="连接池"></a>连接池</h3><p><strong>下列关于数据库连接池的说法中哪个是错误的（D）</strong><br>A.服务器启动时会初始建立一定数量的池连接，并一直维持不少于此数目的池连接。<br>B.客户端程序需要连接时，池驱动程序会返回一个使用的池连接并将其使用计数加 1。<br>C.如果当前没有空闲连接，驱动程序就会再新建一定数量的连接，新建连接的数量可以由配置参数决定。<br>D.当使用池连接调用完成后，池驱动程序将此连接标记为空间，其他调用就可以使用这个连接。</p><h3 id="索引"><a href="#索引" class="headerlink" title="索引"></a>索引</h3><p><strong>以下哪句是对索引的错误描述（C）</strong><br>A.选择性差的索引只会降低 DML 语句的执行速度<br>B.选择性强的索引只有被 Access Path 使用到才是有用的索引<br>C.过多的索引只会阻碍性能的提升，而不是加速性能<br>D.在适当的时候将最常用的列放在复合索引的最前面<br>E.索引和表的数据都存储在同一个 Segment 中</p><h3 id="JDBC"><a href="#JDBC" class="headerlink" title="JDBC"></a>JDBC</h3><p><strong>请编写一个jdbc查询任意一个数据库（如 MySQL、Oracle 等），数据库名为test，表名为user，只有一个字段。</strong></p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SqlConnectionUtil</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Connection conn = <span class="keyword">null</span>;</span><br><span class="line">        Statement stmt = <span class="keyword">null</span>;</span><br><span class="line">        ResultSet rs = <span class="keyword">null</span>;</span><br><span class="line">        String sql;</span><br><span class="line">        String url = <span class="string">"jdbc:MySQL://localhost:3306/test?user=root&amp;password=root&amp;useUnicode=true&amp;characterEncoding=UTF8"</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Class.forName(<span class="string">"com.MySQL.jdbc.Driver"</span>);</span><br><span class="line">            conn = DriverManager.getConnection(url);</span><br><span class="line">            stmt = conn.createStatement();</span><br><span class="line">            sql = <span class="string">"select * from user"</span>;</span><br><span class="line">            rs = stmt.executeQuery(sql);</span><br><span class="line">            <span class="keyword">while</span> (rs.next()) &#123;</span><br><span class="line">                String name = rs.getString(<span class="string">"name"</span>);</span><br><span class="line">                System.out.println(<span class="string">"姓名是："</span> + name);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (rs != <span class="keyword">null</span>) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    rs.close();</span><br><span class="line">                &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (stmt != <span class="keyword">null</span>) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    stmt.close();</span><br><span class="line">                &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (conn != <span class="keyword">null</span>) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    conn.close();</span><br><span class="line">                &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="Java-Web"><a href="#Java-Web" class="headerlink" title="Java Web"></a>Java Web</h2><h3 id="Servlet"><a href="#Servlet" class="headerlink" title="Servlet"></a>Servlet</h3><p><strong>有关Servlet的生命周期说法正确的有（CD）</strong><br>A. Servlet 的生命周期由 Servlet 实例控制<br>B. init() 方法在创建完 Servlet 实例后对其进行初始化，传递的参数为实现 ServletContext 接口的对象<br>C. service() 方法响应客户端发出的请求<br>D. destroy() 方法释放 Servlet 实例</p><h3 id="Filter"><a href="#Filter" class="headerlink" title="Filter"></a>Filter</h3><p><strong>编写一个Filter，需要（B）</strong><br>A. 继承 Filter 类<br>B. 实现 Filter 接口<br>C. 继承 HttpFilter 类<br>D. 实现 HttpFilter 接口</p>]]></content>
      <categories>
        <category>题库</category>
      </categories>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis 的基本使用</title>
    <url>/redis/e751063c-d6cf-4faf-8ef7-85598d272c31.html</url>
    <content><![CDATA[<p>Redis 是一个高性能的 key-value 数据库，属于 NoSQL 类型的数据库。本文介绍其基本使用。</p><a id="more"></a><h2 id="Redis-特性"><a href="#Redis-特性" class="headerlink" title="Redis 特性"></a>Redis 特性</h2><p>Redis 与其他 key - value 缓存产品有以下三个特点：</p><ul><li>Redis支持数据的持久化，可以将内存中的数据保存在磁盘中，重启的时候可以再次加载进行使用。</li><li>Redis不仅仅支持简单的key-value类型的数据，同时还提供list，set，zset，hash等数据结构的存储。</li><li>Redis支持数据的备份，即master-slave模式的数据备份。</li></ul><p>Redis <strong>优势</strong>：</p><ul><li>性能极高: Redis能读的速度是110000次/s,写的速度是81000次/s 。</li><li>丰富的数据类型: Redis支持二进制案例的 Strings, Lists, Hashes, Sets 及 Ordered Sets 数据类型操作。</li><li>原子: Redis的所有操作都是原子性的，同时Redis还支持对几个操作全并后的原子性执行。</li><li>丰富的特性: Redis还支持 publish/subscribe, 通知, key 过期等等特性。</li></ul><h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><p>Redis 的安装非常方便，只需要在 <a href="http://download.redis.io/releases/" target="_blank" rel="noopener"><strong>官网</strong></a> 下载压缩包解压然后 make 即可。</p><ul><li><p>step1:下载之后解压<br><code>tar -zxvf redis-3.2.8.tar.gz</code></p></li><li><p>step2:进⼊⽬录<br>直接使用 <code>make</code> 命令进行编译<br>然后使用 <code>make test</code> 命令进行测试 (需要的时间可能比较长)<br>最后使用 <code>make install</code> 命令进行安装 (会安装到/usr/local/bin目录下)</p></li><li><p>step3:安装完成后，我们进入目录 /usr/local/bin 中查看<br>cd /usr/local/bin<br>ls -alh redis*</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">-rwxr-xr-x 1 root root 4.2M Mar 31 01:08 redis-benchmark</span><br><span class="line">-rwxr-xr-x 1 root root 7.8M Mar 31 01:08 redis-check-aof</span><br><span class="line">-rwxr-xr-x 1 root root 7.8M Mar 31 01:08 redis-check-rdb</span><br><span class="line">-rwxr-xr-x 1 root root 4.6M Mar 31 01:08 redis-cli</span><br><span class="line">lrwxrwxrwx 1 root root   12 Mar 31 01:08 redis-sentinel -&gt; redis-server</span><br><span class="line">-rwxr-xr-x 1 root root 7.8M Mar 31 01:08 redis-server</span><br></pre></td></tr></table></figure><p>其中：<br>redis-server:redis服务器<br>redis-cli:redis命令行客户端<br>redis-benchmark:redis性能测试工具<br>redis-check-aof:AOF文件修复工具<br>redis-check-rdb:RDB文件检索工具</p></li><li><p>step4:配置⽂件，移动到/etc/⽬录下<br>配置⽂件⽬录为解压文件中的 redis.conf 文件，将其移动至/etc/redis中</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo cp redis.conf &#x2F;etc&#x2F;redis&#x2F;</span><br></pre></td></tr></table></figure></li></ul><h2 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h2><p>Redis 的配置文件在 /etc/redis/redis.conf 下</p><h3 id="绑定-ip"><a href="#绑定-ip" class="headerlink" title="绑定 ip"></a>绑定 ip</h3><p>如果需要远程访问，可将此⾏注释，或绑定⼀个真实 ip</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bind 127.0.0.1</span><br></pre></td></tr></table></figure><h3 id="端⼝"><a href="#端⼝" class="headerlink" title="端⼝"></a>端⼝</h3><p>端口默认为 6379</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">port 6379</span><br></pre></td></tr></table></figure><h3 id="守护进程"><a href="#守护进程" class="headerlink" title="守护进程"></a>守护进程</h3><p>如果以守护进程运⾏，则不会在命令⾏阻塞，类似于服务。如果以⾮守护进程运⾏，则当前终端被阻塞。</p><p>设置为 yes 表示守护进程，设置为 no 表示⾮守护进程</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">daemonize yes</span><br></pre></td></tr></table></figure><h3 id="数据⽂件"><a href="#数据⽂件" class="headerlink" title="数据⽂件"></a>数据⽂件</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">dbfilename dump.rdb</span><br></pre></td></tr></table></figure><h3 id="数据⽂件存储路径"><a href="#数据⽂件存储路径" class="headerlink" title="数据⽂件存储路径"></a>数据⽂件存储路径</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">dir &#x2F;var&#x2F;lib&#x2F;redis</span><br></pre></td></tr></table></figure><h3 id="⽇志⽂件"><a href="#⽇志⽂件" class="headerlink" title="⽇志⽂件"></a>⽇志⽂件</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">logfile &#x2F;var&#x2F;log&#x2F;redis&#x2F;redis-server.log</span><br></pre></td></tr></table></figure><h3 id="数据库"><a href="#数据库" class="headerlink" title="数据库"></a>数据库</h3><p>默认有 16 个数据库</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">database 16</span><br></pre></td></tr></table></figure><h2 id="启动"><a href="#启动" class="headerlink" title="启动"></a>启动</h2><h3 id="服务器端"><a href="#服务器端" class="headerlink" title="服务器端"></a>服务器端</h3><p>如果是通过包管理安装的话，可以使用<code>service redis start</code>进行启动。如果是通过源代码编译安装的话，则需要使用<code>redis-server</code>命令启动。</p><p>可以使⽤help查看帮助⽂档:<code>redis-server --help</code></p><p>启动命令：<code>redis-server /etc/redis/redis.conf</code></p><p>一般使用这条命令进行启动，可以指定加载的配置文件</p><p>如果要结束这个进程的时候，需要使用<code>kill</code>命令来结束进程:</p><ul><li><code>ps -ef|grep redis</code>：查看 redis 服务器进程 pid</li><li><code>kill -9 pid</code>：杀死 redis 服务器进程</li></ul><h3 id="客户端"><a href="#客户端" class="headerlink" title="客户端"></a>客户端</h3><p>客户端的命令为<code>redis-cli</code></p><p>可以使⽤help查看帮助⽂档:</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis-cli --help</span><br></pre></td></tr></table></figure><p>如果连接本地redis，直接使用<code>redis-cli</code>命令即可</p><p>连接成功后如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux ~# redis-cli </span><br><span class="line">127.0.0.1:6379&gt;</span><br></pre></td></tr></table></figure><p>可以使用<code>ping</code>命令来测试连接：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">127.0.0.1:6379&gt; ping</span><br><span class="line">PONG</span><br></pre></td></tr></table></figure><p>redis默认有16个数据库，连接的时候是连接第一个数据库，可以使用<code>select</code>命令来切换数据库。如：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">127.0.0.1:6379&gt; SELECT 10</span><br><span class="line">OK</span><br><span class="line">127.0.0.1:6379[10]&gt;</span><br></pre></td></tr></table></figure><h2 id="键命令"><a href="#键命令" class="headerlink" title="键命令"></a>键命令</h2><h3 id="查找键"><a href="#查找键" class="headerlink" title="查找键"></a>查找键</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">keys pattern</span><br></pre></td></tr></table></figure><p>查找键的参数⽀持正则表达式</p><ul><li>查看所有键：<code>keys *</code></li><li>查看名称中包含 a 的键：<code>keys &#39;a*&#39;</code></li></ul><h3 id="判断键是否存在"><a href="#判断键是否存在" class="headerlink" title="判断键是否存在"></a>判断键是否存在</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">exists key [key ...]</span><br></pre></td></tr></table></figure><p>如果存在返回1，不存在返回0</p><ul><li>判断键 a1 是否存在：<code>exists a1</code></li></ul><h3 id="查看键对应的的类型"><a href="#查看键对应的的类型" class="headerlink" title="查看键对应的的类型"></a>查看键对应的的类型</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">type key</span><br></pre></td></tr></table></figure><ul><li>查看键 a1 的值类型，为 redis ⽀持的五种类型中的⼀种：<code>type a1</code></li></ul><h3 id="删除键及对应的值"><a href="#删除键及对应的值" class="headerlink" title="删除键及对应的值"></a>删除键及对应的值</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">del key [key ...]</span><br></pre></td></tr></table></figure><ul><li>删除键 a2、a3：<code>del a2 a3</code></li></ul><h3 id="设置过期时间"><a href="#设置过期时间" class="headerlink" title="设置过期时间"></a>设置过期时间</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">expire key [seconds]</span><br></pre></td></tr></table></figure><p>以秒为单位，如果没有指定过期时间则⼀直存在，直到使⽤ DEL 移除</p><ul><li>设置键 ‘a1’ 的过期时间为3秒：<code>expire &#39;a1&#39; 3</code></li></ul><h3 id="查看有效时间"><a href="#查看有效时间" class="headerlink" title="查看有效时间"></a>查看有效时间</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ttl key</span><br></pre></td></tr></table></figure><p>以秒为单位</p><ul><li>查看键 ‘bb’ 的有效时间：<code>ttl bb</code></li></ul><h2 id="string"><a href="#string" class="headerlink" title="string"></a>string</h2><p>字符串类型是 Redis 中<strong>最为基础的数据存储类型</strong>。它在 Redis 中是二进制安全的，这便意味着该类型可以接受任何格式的数据，如 JPEG 图像数据或 Json 对象描述信息等。在 Redis 中 string 类型的 Value 最多可以容纳的数据长度是 512M。</p><h3 id="set"><a href="#set" class="headerlink" title="set"></a>set</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">set key value</span><br></pre></td></tr></table></figure><p>如果设置的键不存在则为添加，如果设置的键已经存在则修改</p><ul><li>设置键为 name 值为 abc 的数据：<code>set name abc</code></li></ul><h3 id="setex"><a href="#setex" class="headerlink" title="setex"></a>setex</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">setex key seconds value</span><br></pre></td></tr></table></figure><p>设置键值及过期时间，以秒为单位</p><ul><li>设置键为 aa 值为 aa 过期时间为 3 秒的数据：<code>setex aa 3 aa</code></li></ul><h3 id="mset"><a href="#mset" class="headerlink" title="mset"></a>mset</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mset key value [key value ...]</span><br></pre></td></tr></table></figure><p>设置多个键值</p><ul><li>设置键为 ‘a1’ 值为 ‘python’、键为 ‘a2’ 值为 ‘java’、键为 ‘a3’ 值为 ‘c’：<code>mset a1 python a2 java a3 c</code></li></ul><h3 id="append"><a href="#append" class="headerlink" title="append"></a>append</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">append key value</span><br></pre></td></tr></table></figure><p>追加值</p><ul><li>向键为 a1 中追加值 ‘haha’：<code>append &#39;a1&#39; &#39;haha&#39;</code></li></ul><h3 id="get"><a href="#get" class="headerlink" title="get"></a>get</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">get key</span><br></pre></td></tr></table></figure><p>获取：根据键获取值，如果不存在此键则返回nil</p><ul><li>获取键 ‘name’ 的值：<code>get &#39;name&#39;</code></li></ul><h3 id="mget"><a href="#mget" class="headerlink" title="mget"></a>mget</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mget key [key ...]</span><br></pre></td></tr></table></figure><p>根据多个键获取多个值</p><ul><li>获取键 a1、a2、a3 的值：<code>mget a1 a2 a3</code></li></ul><h2 id="hash"><a href="#hash" class="headerlink" title="hash"></a>hash</h2><p>hash ⽤于存储对象(结构体)，对象的结构为属性、值。值的类型 <strong>只能为string</strong></p><h3 id="hset"><a href="#hset" class="headerlink" title="hset"></a>hset</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hset key field value</span><br></pre></td></tr></table></figure><p>设置单个属性</p><p><strong>注意：</strong><br>如果使用hset命令的时候出现以下错误：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">MISCONF Redis is configured to save RDB snapshots, but is currently not able to persist on disk. Commands that may modify the data set are disabled. Please check Redis logs for details about the error.</span><br></pre></td></tr></table></figure><p>是由于Redis被配置为保存数据库快照，但它目前不能持久化到硬盘。于是用来修改集合数据的命令不能用。原因：强制关闭 Redis 快照导致不能持久化。</p><p>解决方案：运行 <code>config set stop-writes-on-bgsave-error no</code>命令后即可解决该问题。</p><ul><li>设置键 user 的属性 name 为 asdf：<code>hset user name asdf</code></li></ul><h3 id="hmset"><a href="#hmset" class="headerlink" title="hmset"></a>hmset</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hmset key field value [field value ...]</span><br></pre></td></tr></table></figure><p>设置多个属性</p><ul><li>设置键 u2 的属性 name 为 asdf、属性 age 为 11：<code>hmset u2 name asdf age 11</code></li></ul><h3 id="hkeys"><a href="#hkeys" class="headerlink" title="hkeys"></a>hkeys</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hkeys key</span><br></pre></td></tr></table></figure><p>获取指定键所有的属性</p><ul><li>获取键 u2 的所有属性：<code>hkeys u2</code></li></ul><h3 id="hget"><a href="#hget" class="headerlink" title="hget"></a>hget</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hget key field</span><br></pre></td></tr></table></figure><p>获取键属性</p><ul><li>获取键 u2 属性 ‘name’ 的值：<code>hget u2 &#39;name&#39;</code></li></ul><h3 id="hmget"><a href="#hmget" class="headerlink" title="hmget"></a>hmget</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hmget key field [field ...]</span><br></pre></td></tr></table></figure><p>获取多个属性的值</p><ul><li>获取键 u2 属性 ‘name’、’age 的值：<code>hmget u2 name age</code></li></ul><h3 id="hvals"><a href="#hvals" class="headerlink" title="hvals"></a>hvals</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hvals key</span><br></pre></td></tr></table></figure><p>获取所有属性的值</p><ul><li>获取键 ‘u2’ 所有属性的值：<code>hvals u2</code></li></ul><h3 id="hdel"><a href="#hdel" class="headerlink" title="hdel"></a>hdel</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hdel key field [field ...]</span><br></pre></td></tr></table></figure><p>删除属性，属性对应的值会被⼀起删除</p><ul><li>删除键 ‘u2’ 的属性 ‘age’：<code>hdel u2 age</code></li></ul><p><strong>如果要删除整个 hash 键及值，使⽤ del 命令</strong></p><h2 id="list"><a href="#list" class="headerlink" title="list"></a>list</h2><p>list类型是一种列表类型，列表的元素类型为 <strong>string</strong></p><h3 id="lpush"><a href="#lpush" class="headerlink" title="lpush"></a>lpush</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">lpush key value [value ...]</span><br></pre></td></tr></table></figure><p>在左侧(列表头部)插⼊数据</p><ul><li>从键为 ‘a1’ 的列表左侧加⼊数据 a、 b、c：<code>lpush a1 a b c</code></li></ul><h3 id="rpush"><a href="#rpush" class="headerlink" title="rpush"></a>rpush</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">rpush key value [value ...]</span><br></pre></td></tr></table></figure><p>在右侧(列表尾部)插⼊数据</p><ul><li>从键为 ‘a1’ 的列表右侧加⼊数据 0、1：<code>rpush a1 0 1</code></li></ul><h3 id="linsert"><a href="#linsert" class="headerlink" title="linsert"></a>linsert</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">linsert key BEFORE|AFTER pivot value</span><br></pre></td></tr></table></figure><p>在指定元素的前或后插⼊新元素</p><ul><li>在键为 ‘a1’ 的列表中元素 ‘b’ 前加⼊ ‘3’：<code>linsert a1 before b 3</code></li></ul><h3 id="lrange"><a href="#lrange" class="headerlink" title="lrange"></a>lrange</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">lrange key start stop</span><br></pre></td></tr></table></figure><p>返回列表⾥指定范围内的元素，<code>start</code>、<code>stop</code> 为元素的下标索引。索引从左侧开始，第⼀个元素为 0，索引可以是负数，表示从尾部开始计数，<strong>-1表示最后⼀个元素</strong></p><ul><li>获取键为 ‘a1’ 的列表所有元素：<code>lrange a1 0 -1</code></li></ul><h3 id="lset"><a href="#lset" class="headerlink" title="lset"></a>lset</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">lset key index value</span><br></pre></td></tr></table></figure><p>设置指定索引位置的元素值</p><ul><li>修改键为 ‘a1’ 的列表中下标为 1 的元素值为 ‘z’：<code>lset a 1 z</code></li></ul><h3 id="lrem"><a href="#lrem" class="headerlink" title="lrem"></a>lrem</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">lrem key count value</span><br></pre></td></tr></table></figure><p>删除指定元素。将列表中前 count 次出现的值为 value 的元素移除</p><ul><li>count &gt; 0: 从头往尾移除</li><li>count &lt; 0: 从尾往头移除</li><li>count = 0: 移除所有</li></ul><p>如：</p><ul><li>向列表 ‘a2’ 中加⼊元素 ‘a’、’b’、’a’、’b’、’a’、’b’：<code>lpush a2 a b a b a b</code></li><li>从 ‘a2’ 列表右侧开始删除 2 个 ‘b’：<code>lrem a2 -2 b</code></li><li>查看列表 ‘a2’ 的所有元素：<code>lrange a2 0 -1</code></li></ul><h2 id="set-1"><a href="#set-1" class="headerlink" title="set"></a>set</h2><p>set类型是一种⽆序集合，<strong>元素为string类型</strong>。元素具有唯⼀性，不重复。<strong>注意</strong>：对于集合没有修改操作。</p><h3 id="sadd"><a href="#sadd" class="headerlink" title="sadd"></a>sadd</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sadd key member [member ...]</span><br></pre></td></tr></table></figure><p>添加元素</p><ul><li>向键 ‘a3’ 的集合中添加元素 ‘zhangsan’、’lisi’、’wangwu’：<code>sadd a3 zhangsan sili wangwu</code></li></ul><h3 id="smembers"><a href="#smembers" class="headerlink" title="smembers"></a>smembers</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">smembers key</span><br></pre></td></tr></table></figure><p>返回所有的元素</p><ul><li>获取键 ‘a3’ 的集合中所有元素：<code>smembers a3</code></li></ul><h3 id="srem"><a href="#srem" class="headerlink" title="srem"></a>srem</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">srem key</span><br></pre></td></tr></table></figure><p>删除指定元素</p><ul><li>删除键 ‘a3’ 的集合中元素 ‘wangwu’：<code>srem a3 wangwu</code></li></ul><h2 id="zset"><a href="#zset" class="headerlink" title="zset"></a>zset</h2><p>zset类型是一种有序集合，<strong>其元素为 string 类型</strong>，元素具有唯⼀性，不重复。每个元素都会关联⼀个 double 类型的 score，表示权重，通过权重将元素从⼩到⼤排序。</p><p><strong>注意</strong>：没有修改操作</p><h3 id="zadd"><a href="#zadd" class="headerlink" title="zadd"></a>zadd</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">zadd key score member [score member ...]</span><br></pre></td></tr></table></figure><p>添加元素</p><ul><li>向键 ‘a4’ 的集合中添加元素 ‘lisi’、’wangwu’、’zhaoliu’、’zhangsan’，权重分别为 4、5、6、3：<code>zadd a4 4 lisi 5 wangwu 6 zhaoliu 3 zhangsan</code></li></ul><h3 id="zrange"><a href="#zrange" class="headerlink" title="zrange"></a>zrange</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">zrange key start stop</span><br></pre></td></tr></table></figure><p>返回指定范围内的元素，<code>start</code>、<code>stop</code>为元素的下标索引，索引从左侧开始，第⼀个元素为0。索引可以是负数，表示从尾部开始计数，如 -1 表示最后⼀个元素。</p><ul><li>获取键 ‘a4’ 的集合中所有元素：<code>zrange a4 0 -1</code></li></ul><h3 id="zrangebyscore"><a href="#zrangebyscore" class="headerlink" title="zrangebyscore"></a>zrangebyscore</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">zrangebyscore key min max</span><br></pre></td></tr></table></figure><p>返回 score 值在 min 和 max 之间的成员</p><ul><li>获取键 ‘a4’ 的集合中权限值在 5 和 6 之间的成员：<code>zrangebyscore a4 5 6</code></li></ul><h3 id="zscore"><a href="#zscore" class="headerlink" title="zscore"></a>zscore</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">zscore key member</span><br></pre></td></tr></table></figure><p>返回成员 member 的 score 值</p><ul><li>获取键 ‘a4’ 的集合中元素 ‘zhangsan’ 的权重：<code>zscore a4 zhangsan</code></li></ul><h3 id="zrem"><a href="#zrem" class="headerlink" title="zrem"></a>zrem</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">zrem key member [member ...]</span><br></pre></td></tr></table></figure><p>删除指定元素</p><ul><li>删除集合 ‘a4’ 中元素 ‘zhangsan’：<code>zrem a4 zhangsan</code></li></ul><h3 id="zremrangebyscore"><a href="#zremrangebyscore" class="headerlink" title="zremrangebyscore"></a>zremrangebyscore</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">zremrangebyscore key min max</span><br></pre></td></tr></table></figure><p>删除权重在指定范围的元素</p><ul><li>删除集合 ‘a4’ 中权限在 5、6 之间的元素：<code>zremrangebyscore a4 5 6</code></li></ul>]]></content>
      <categories>
        <category>Database</category>
      </categories>
      <tags>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker 常用命令</title>
    <url>/docker/7cb5b28d-26d4-45d0-9a84-726b6c7fbb81.html</url>
    <content><![CDATA[<p>本文介绍 Docker 的常用命令。</p><a id="more"></a><h2 id="Docker-信息"><a href="#Docker-信息" class="headerlink" title="Docker 信息"></a>Docker 信息</h2><h3 id="查看-Docker-信息"><a href="#查看-Docker-信息" class="headerlink" title="查看 Docker 信息"></a>查看 Docker 信息</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker info</span><br></pre></td></tr></table></figure><br><h3 id="查看-Docker-版本"><a href="#查看-Docker-版本" class="headerlink" title="查看 Docker 版本"></a>查看 Docker 版本</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker version</span><br></pre></td></tr></table></figure><br><h2 id="镜像相关"><a href="#镜像相关" class="headerlink" title="镜像相关"></a>镜像相关</h2><h3 id="构建镜像"><a href="#构建镜像" class="headerlink" title="构建镜像"></a>构建镜像</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker build -t image-name docker-file-location</span><br></pre></td></tr></table></figure><br><h3 id="查看所有镜像"><a href="#查看所有镜像" class="headerlink" title="查看所有镜像"></a>查看所有镜像</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker images</span><br><span class="line">docker image ls</span><br></pre></td></tr></table></figure><br><h3 id="删除一个镜像"><a href="#删除一个镜像" class="headerlink" title="删除一个镜像"></a>删除一个镜像</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker rmi image-name</span><br></pre></td></tr></table></figure><br><h3 id="删除所有镜像"><a href="#删除所有镜像" class="headerlink" title="删除所有镜像"></a>删除所有镜像</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker rmi $(docker images -q)</span><br></pre></td></tr></table></figure><br><h3 id="强制删除所有镜像"><a href="#强制删除所有镜像" class="headerlink" title="强制删除所有镜像"></a>强制删除所有镜像</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker rmi -r $(docker images -q)</span><br></pre></td></tr></table></figure><br><h3 id="删除所有虚悬镜像"><a href="#删除所有虚悬镜像" class="headerlink" title="删除所有虚悬镜像"></a>删除所有虚悬镜像</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker image prune</span><br><span class="line">docker rmi $(docker images -q -f dangling&#x3D;true)</span><br></pre></td></tr></table></figure><br><h3 id="打包本地镜像"><a href="#打包本地镜像" class="headerlink" title="打包本地镜像"></a>打包本地镜像</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker save IMAGEID -o FILE.tar</span><br><span class="line">docker save IMAGEID &gt; FILE.tar</span><br></pre></td></tr></table></figure><br><h3 id="从-tar-文件中加载镜像"><a href="#从-tar-文件中加载镜像" class="headerlink" title="从 tar 文件中加载镜像"></a>从 tar 文件中加载镜像</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker load -i FILE.tar</span><br><span class="line">docker load &lt; FILE.tar</span><br></pre></td></tr></table></figure><br><h2 id="容器相关"><a href="#容器相关" class="headerlink" title="容器相关"></a>容器相关</h2><h3 id="创建并运行容器"><a href="#创建并运行容器" class="headerlink" title="创建并运行容器"></a>创建并运行容器</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run [-d] image-name</span><br></pre></td></tr></table></figure><br><h3 id="查看最近的运行容器"><a href="#查看最近的运行容器" class="headerlink" title="查看最近的运行容器"></a>查看最近的运行容器</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker ps -l</span><br></pre></td></tr></table></figure><br><h3 id="查看所有容器-包括已停止的"><a href="#查看所有容器-包括已停止的" class="headerlink" title="查看所有容器 (包括已停止的)"></a>查看所有容器 (包括已停止的)</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker ps -a</span><br></pre></td></tr></table></figure><br><h3 id="停止运行容器"><a href="#停止运行容器" class="headerlink" title="停止运行容器"></a>停止运行容器</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker stop container_id</span><br></pre></td></tr></table></figure><br><h3 id="删除所有容器"><a href="#删除所有容器" class="headerlink" title="删除所有容器"></a>删除所有容器</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker rm $(docker ps -a -q)</span><br></pre></td></tr></table></figure><br><h3 id="删除所有已停止的容器"><a href="#删除所有已停止的容器" class="headerlink" title="删除所有已停止的容器"></a>删除所有已停止的容器</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker container prune</span><br></pre></td></tr></table></figure><br><h3 id="进入-Docker-容器"><a href="#进入-Docker-容器" class="headerlink" title="进入 Docker 容器"></a>进入 Docker 容器</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker exec -it container-id &#x2F;bin&#x2F;bash</span><br></pre></td></tr></table></figure><br><h3 id="从主机复制文件到容器"><a href="#从主机复制文件到容器" class="headerlink" title="从主机复制文件到容器"></a>从主机复制文件到容器</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker cp host_path containerID:container_path</span><br></pre></td></tr></table></figure><br><h3 id="从容器复制文件到主机"><a href="#从容器复制文件到主机" class="headerlink" title="从容器复制文件到主机"></a>从容器复制文件到主机</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker cp containerID:container_path host_path</span><br></pre></td></tr></table></figure><br><h2 id="数据卷"><a href="#数据卷" class="headerlink" title="数据卷"></a>数据卷</h2><h3 id="查看所有数据卷"><a href="#查看所有数据卷" class="headerlink" title="查看所有数据卷"></a>查看所有数据卷</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker volume ls</span><br></pre></td></tr></table></figure><br><h3 id="删除指定数据卷"><a href="#删除指定数据卷" class="headerlink" title="删除指定数据卷"></a>删除指定数据卷</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker volume rm [volume_name]</span><br></pre></td></tr></table></figure><br><h3 id="删除所有未关联的数据卷"><a href="#删除所有未关联的数据卷" class="headerlink" title="删除所有未关联的数据卷"></a>删除所有未关联的数据卷</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker volume rm $(docker volume ls -qf dangling&#x3D;true)</span><br></pre></td></tr></table></figure><br>]]></content>
      <categories>
        <category>Docker</category>
      </categories>
      <tags>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>Git 分支操作</title>
    <url>/git/ff7a41c7-6cbd-405f-9de5-0478d2be3693.html</url>
    <content><![CDATA[<p>假设你准备开发一个新功能，但是需要两周才能完成。第一周你写了50%的代码，如果立刻提交，由于代码还没写完，不完整的代码库会导致别人不能干活了。如果等代码全部写完再一次提交，又存在丢失每天进度的巨大风险。</p><p>现在有了分支，就不用怕了。你创建了一个属于你自己的分支，别人看不到，还继续在原来的分支上正常工作，而你在自己的分支上干活。想提交就提交，直到开发完毕后，再一次性合并到原来的分支上，这样，既安全，又不影响别人工作。</p><a id="more"></a><h1 id="创建与合并分支"><a href="#创建与合并分支" class="headerlink" title="创建与合并分支"></a>创建与合并分支</h1><p>Git 把我们之前每次提交的版本串成一条时间线，这条时间线就是一个<strong>分支</strong>。截止到目前只有一条时间线，在 Git 里，这个分支叫主分支，即 <strong>master 分支</strong>。</p><p>一开始的时候，master 分支是一条线，Git 用 master 指向最新的提交，再用 HEAD 指向 master，就能确定当前分支，以及当前分支的提交点。每次提交，master 分支都会向前移动一步，这样，随着你不断提交，master 分支的线也越来越长。</p><p>当我们创建新的分支，例如 dev 时，Git 新建了一个指针叫 dev，指向 master 相同的提交，再把 HEAD 指向 dev，就表示当前分支在 dev 上。Git 创建一个分支很快，因为除了增加一个 dev 指针，改变 HEAD 的指向，工作区的文件都没有任何变化。</p><p>不过，从现在开始，对工作区的修改和提交就是针对 dev 分支了，比如新提交一次后，dev 指针往前移动一步，而 master 指针不变。假如我们在 dev 上的工作完成了，就可以把 dev 合并到 master 上。</p><p>Git 怎么合并呢？最简单的方法，就是直接把 master 指向 dev 的当前提交，就完成了合并。Git 合并分支也很快，就改改指针，工作区内容也不变。合并完分支后，甚至可以删除 dev 分支。删除 dev 分支就是把 dev 指针给删掉，删掉后，我们就剩下了一条 master 分支</p><p>我们可以使用 <code>git branch</code> 命令来查看当前有几个分支，并且可以知道我们在哪个分支下工作：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux git_test# git branch </span><br><span class="line">* master</span><br></pre></td></tr></table></figure><p>然后我们可以创建一个新的分支，有两种方法：</p><ul><li><strong><code>git branch NAME</code></strong>：创建一个新的分支</li><li><strong><code>git checkout -b NAME</code></strong>：创建并切换到新分支<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux git_test# git checkout -b dev</span><br><span class="line">Switched to a new branch &#39;dev&#39;</span><br><span class="line">root@Linux git_test# git branch </span><br><span class="line">* dev</span><br><span class="line">  master</span><br></pre></td></tr></table></figure></li></ul><p>之后我们可以进行新的操作并进行提交，如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux git_test# vim code </span><br><span class="line">root@Linux git_test# git add code</span><br><span class="line">root@Linux git_test# git commit -m &#39;dev分支提交&#39;</span><br><span class="line">[dev 7b61bd1] dev分支提交</span><br><span class="line"> 1 file changed, 1 insertion(+)</span><br><span class="line">root@Linux git_test# git log --pretty&#x3D;oneline </span><br><span class="line">7b61bd17ec3abb4a950f0a313ad3f73078b2160b dev分支提交</span><br><span class="line">7318522d370086d9a9c856c0404bb6c4999b893d 版本2</span><br><span class="line">b83de1e3cce9f3c7b903a677906ca0c513f7f086 版本1</span><br></pre></td></tr></table></figure><p>然后使用 <code>git checkout master</code> 切换回 master 分支，可以看到并没有 dev 分支的提交：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux git_test# git checkout master </span><br><span class="line">Switched to branch &#39;master&#39;</span><br><span class="line">root@Linux git_test# git log --pretty&#x3D;oneline </span><br><span class="line">7318522d370086d9a9c856c0404bb6c4999b893d 版本2</span><br><span class="line">b83de1e3cce9f3c7b903a677906ca0c513f7f086 版本1</span><br></pre></td></tr></table></figure><p>之后可以使用 <code>git merge dev</code> 来合并 dev 分支到 master 上：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux git_test# git merge dev </span><br><span class="line">Updating 7318522..7b61bd1</span><br><span class="line">Fast-forward</span><br><span class="line"> code | 1 +</span><br><span class="line"> 1 file changed, 1 insertion(+)</span><br><span class="line">root@Linux git_test# git log --pretty&#x3D;oneline </span><br><span class="line">7b61bd17ec3abb4a950f0a313ad3f73078b2160b dev分支提交</span><br><span class="line">7318522d370086d9a9c856c0404bb6c4999b893d 版本2</span><br><span class="line">b83de1e3cce9f3c7b903a677906ca0c513f7f086 版本1</span><br></pre></td></tr></table></figure><p>注意这里使用的是 <strong>Fast-forward</strong>，Git 告诉我们，这次合并是「快速模式」，也就是直接把 master <strong>指向</strong> dev 的当前提交，所以合并速度非常快。合并完成之后可以删除dev分支了，如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux git_test# git branch -d dev</span><br><span class="line">Deleted branch dev (was 7b61bd1).</span><br><span class="line">root@Linux git_test# git branch </span><br><span class="line">* master</span><br></pre></td></tr></table></figure><br><h1 id="解决冲突"><a href="#解决冲突" class="headerlink" title="解决冲突"></a>解决冲突</h1><p>合并分支往往也不是一帆风顺的。如果在 dev 分支下对某个文件进行修改然后提交，之后切换回 master 分支再对这个文件进行修改并提交，如果 master 想要合并 dev 分支，就会有冲突，如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux git_test# vim code </span><br><span class="line">root@Linux git_test# git add code</span><br><span class="line">root@Linux git_test# git commit -m &#39;dev冲突提交&#39;</span><br><span class="line">[dev 7644bd1] dev冲突提交</span><br><span class="line"> 1 file changed, 1 insertion(+)</span><br><span class="line">root@Linux git_test# git checkout master </span><br><span class="line">Switched to branch &#39;master&#39;</span><br><span class="line">root@Linux git_test# vim code </span><br><span class="line">root@Linux git_test# git add code</span><br><span class="line">root@Linux git_test# git commit -m &#39;master冲突提交&#39;</span><br><span class="line">[master ebc2ba5] master冲突提交</span><br><span class="line"> 1 file changed, 1 insertion(+)</span><br><span class="line">root@Linux git_test# git merge dev</span><br><span class="line">Auto-merging code</span><br><span class="line">CONFLICT (content): Merge conflict in code</span><br><span class="line">Automatic merge failed; fix conflicts and then commit the result.</span><br></pre></td></tr></table></figure><p>这时候就提示有冲突，需要解决冲突，打开 code 文件可以看到：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">This is the first line</span><br><span class="line">This is the second line</span><br><span class="line">This is the third line</span><br><span class="line">&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD</span><br><span class="line">This is the master new line</span><br><span class="line">&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;</span><br><span class="line">This is the dev new line</span><br><span class="line">&gt;&gt;&gt;&gt;&gt;&gt;&gt; dev</span><br></pre></td></tr></table></figure><p>需要将 <code>&lt;&lt;&lt;&lt;&lt;&lt;&lt;</code>、<code>=======</code>、<code>&gt;&gt;&gt;&gt;&gt;&gt;&gt;</code> 这些内容去掉，然后再进行一次提交，如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux git_test# git add code</span><br><span class="line">root@Linux git_test# git commit -m &#39;解决冲突&#39;</span><br><span class="line">[master abe37f9] 解决冲突</span><br><span class="line">root@Linux git_test# git log --pretty&#x3D;oneline --graph </span><br><span class="line">*   abe37f91dcec5ad24b1a4c95f71650b08e8b68c6 解决冲突</span><br><span class="line">|\  </span><br><span class="line">| * 7644bd1e80d1013979aa14c9d9d67d7611196e1c dev冲突提交</span><br><span class="line">* | ebc2ba59de73ccb04c207d6f3748bf1137028b93 master冲突提交</span><br><span class="line">|&#x2F;  </span><br><span class="line">* 7b61bd17ec3abb4a950f0a313ad3f73078b2160b dev分支提交</span><br><span class="line">* 7318522d370086d9a9c856c0404bb6c4999b893d 版本2</span><br><span class="line">* b83de1e3cce9f3c7b903a677906ca0c513f7f086 版本1</span><br></pre></td></tr></table></figure><p>合并完成，可以删除 dev 分支了。</p><p><strong>注意</strong>：这种有冲突的情况往往是发生在<strong>两个分支修改了同一个文件并提交</strong>的时候</p><br><h1 id="Bug-分支"><a href="#Bug-分支" class="headerlink" title="Bug 分支"></a>Bug 分支</h1><p>在 Git 中，每个 Bug 都可以通过一个新的临时分支来修复，修复后，合并分支，然后将临时分支删除。如：当你接到一个修复一个代号 001 的 Bug 的任务时，很自然地，你想创建一个分支 bug-1 来修复它，但是，当前正在 dev 上进行的工作还没有提交。这时候可以使用 <code>git stash</code> 把当前工作现场 “保存” 起来，等以后恢复现场后继续工作：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux git_test2# git checkout -b dev</span><br><span class="line">Switched to a new branch &#39;dev&#39;</span><br><span class="line">root@Linux git_test2# vim code </span><br><span class="line">root@Linux git_test2# git stash </span><br><span class="line">Saved working directory and index state WIP on dev: 89d3e06 删除</span><br><span class="line">HEAD is now at 89d3e06 删除</span><br><span class="line">root@Linux git_test2# git checkout -b bug-1</span><br><span class="line">Switched to a new branch &#39;bug-1&#39;</span><br><span class="line">root@Linux git_test2# vim code </span><br><span class="line">root@Linux git_test2# git add code</span><br><span class="line">root@Linux git_test2# git commit -m &#39;修复bug1&#39;</span><br><span class="line">[bug-1 f2d31b4] 修复bug1</span><br><span class="line"> 1 file changed, 3 deletions(-)</span><br><span class="line">root@Linux git_test2# git checkout master </span><br><span class="line">Switched to branch &#39;master&#39;</span><br><span class="line">root@Linux git_test2# git merge --no-ff -m &#39;修复bug1&#39; bug-1 </span><br><span class="line">Merge made by the &#39;recursive&#39; strategy.</span><br><span class="line"> code | 3 ---</span><br><span class="line"> 1 file changed, 3 deletions(-)</span><br><span class="line">root@Linux git_test2# git branch -d bug-1 </span><br><span class="line">Deleted branch bug-1 (was f2d31b4).</span><br><span class="line">root@Linux git_test2# git log --pretty&#x3D;oneline --graph </span><br><span class="line">*   57eb3a55fa658d21437ea67cfe151ae5a78988c3 修复bug1</span><br><span class="line">|\  </span><br><span class="line">| * f2d31b4ce666471c47a585bf940494c1f5d92cc2 修复bug1</span><br><span class="line">|&#x2F;  </span><br><span class="line">* 7b61bd17ec3abb4a950f0a313ad3f73078b2160b dev分支提交</span><br><span class="line">* 7318522d370086d9a9c856c0404bb6c4999b893d 版本2</span><br><span class="line">* b83de1e3cce9f3c7b903a677906ca0c513f7f086 版本1</span><br></pre></td></tr></table></figure><p>这里就是临时修复了 bug1，然后就使用 <code>git stash pop</code> 来恢复工作现场，之后就可以继续之前的工作了。</p><br>]]></content>
      <categories>
        <category>Software</category>
      </categories>
      <tags>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>Git 基本操作</title>
    <url>/git/cd7c75d2-f24c-41ea-a901-9b93f97b27d5.html</url>
    <content><![CDATA[<p>Git 是一个开源的分布式版本控制系统，用于敏捷高效地处理任何或小或大的项目。与常用的版本控制工具 CVS, Subversion 等不同，它采用了分布式版本库的方式，不需要服务器端软件支持。本文介绍 Git 的一些基本操作。</p><a id="more"></a><h1 id="创建版本库"><a href="#创建版本库" class="headerlink" title="创建版本库"></a>创建版本库</h1><p>在任何一个目录下 (如 <code>git_test</code>) 可以执行 <code>git init</code> 命令，该目录就是一个本地仓库 (工作区)。可以看在目录下会有一个隐藏目录 <code>.git</code>，如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">drwxr-xr-x 8 root root 4096 Mar 29 20:09 .git</span><br></pre></td></tr></table></figure><p>我们也可以使用 <code>git clone</code> 命令下载远程仓库</p><br><h1 id="工作区和版本库"><a href="#工作区和版本库" class="headerlink" title="工作区和版本库"></a>工作区和版本库</h1><p>简单来说，使用过 <code>git init</code> 或者 <code>git clone</code> 命令的目录 (包含 <code>.git</code> 的目录)，就是一个<strong>工作区</strong>。在工作区中有个隐藏的目录<code>.git</code>就是 Git 的<strong>版本库</strong>。</p><p>Git 的版本库里存了很多东西，其中最重要的就是称为 <strong>stage (或者叫 index )</strong> 的<strong>暂存区</strong>，还有 Git 为我们自动创建的第一个<strong>分支 master</strong>，以及指向 master 的一个指针叫 <strong>HEAD</strong>。</p><p>因为我们创建 Git 版本库时，Git 自动为我们创建了唯一一个 master 分支，所以，<code>git commit</code> 就是往 master 分支上提交更改。可以简单理解为，需要提交的文件修改通通放到<strong>暂存区</strong>，然后，一次性提交暂存区的所有修改。</p><p>所以我们将文件往 Git 版本库添加的时候是分两步进行的：</p><ul><li>第一步用 <code>git add</code> 将文件添加到暂存区</li><li>第二步用 <code>git commit</code> 将暂存区中的内容提交到当前分支</li></ul><p>可以使用<code>git status</code>命令来查看当前工作区的状态</p><br><h1 id="版本创建与回退"><a href="#版本创建与回退" class="headerlink" title="版本创建与回退"></a>版本创建与回退</h1><p>先在目录下创建一个 <code>code</code> 文件，内容如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux git_test# vim code</span><br><span class="line">root@Linux git_test# cat code </span><br><span class="line">This is the first line</span><br></pre></td></tr></table></figure><p>然后使用如下两条命令可以创建一个版本：</p><ul><li><strong><code>git add code</code></strong></li><li><strong><code>git commit -m &#39;版本1&#39;</code></strong></li></ul><p>如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux git_test# git add code</span><br><span class="line">root@Linux git_test# git commit -m &#39;版本1&#39;</span><br><span class="line">[master (root-commit) b83de1e] 版本1</span><br><span class="line"> 1 file changed, 1 insertion(+)</span><br><span class="line"> create mode 100644 code</span><br></pre></td></tr></table></figure><p>可以使用<code>git log</code>命令查看版本记录：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux git_test# git log</span><br><span class="line">commit b83de1e3cce9f3c7b903a677906ca0c513f7f086</span><br><span class="line">Author: Janvy Sun &lt;JanvySun@gmail.com&gt;</span><br><span class="line">Date:   Sat Mar 30 14:19:58 2019 +0800</span><br><span class="line"></span><br><span class="line">    版本1</span><br></pre></td></tr></table></figure><p>然后继续编辑 <code>code</code> 文件，添加提交，如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux git_test# vim code</span><br><span class="line">root@Linux git_test# cat code</span><br><span class="line">This is the first line</span><br><span class="line">This is the second line</span><br><span class="line">root@Linux git_test# git add code</span><br><span class="line">root@Linux git_test# git commit -m &#39;版本2&#39;</span><br><span class="line">[master 7318522] 版本2</span><br><span class="line"> 1 file changed, 1 insertion(+)</span><br><span class="line">root@Linux git_test# git log</span><br><span class="line">commit 7318522d370086d9a9c856c0404bb6c4999b893d</span><br><span class="line">Author: Janvy Sun &lt;JanvySun@gmail.com&gt;</span><br><span class="line">Date:   Sat Mar 30 14:22:04 2019 +0800</span><br><span class="line"></span><br><span class="line">    版本2</span><br><span class="line"></span><br><span class="line">commit b83de1e3cce9f3c7b903a677906ca0c513f7f086</span><br><span class="line">Author: Janvy Sun &lt;JanvySun@gmail.com&gt;</span><br><span class="line">Date:   Sat Mar 30 14:19:58 2019 +0800</span><br><span class="line"></span><br><span class="line">    版本1</span><br></pre></td></tr></table></figure><p><strong>回退</strong>到版本 1：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux git_test# git reset --hard HEAD^</span><br><span class="line">HEAD is now at b83de1e 版本1</span><br><span class="line">root@Linux git_test# cat code </span><br><span class="line">This is the first line</span><br></pre></td></tr></table></figure><p>回到回退之前的版本 2，使用 <code>git reflog</code> 命令查看版本 2 的版本号，然后使用版本号进行回退，如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux git_test# git reflog </span><br><span class="line">b83de1e HEAD@&#123;0&#125;: reset: moving to HEAD^</span><br><span class="line">7318522 HEAD@&#123;1&#125;: commit: 版本2</span><br><span class="line">b83de1e HEAD@&#123;2&#125;: commit (initial): 版本1</span><br><span class="line">root@Linux git_test# git reset --hard 7318522</span><br><span class="line">HEAD is now at 7318522 版本2</span><br><span class="line">root@Linux git_test# cat code </span><br><span class="line">This is the first line</span><br><span class="line">This is the second line</span><br></pre></td></tr></table></figure><br><h1 id="撤销修改"><a href="#撤销修改" class="headerlink" title="撤销修改"></a>撤销修改</h1><p>当我们做完工作区的修改之后 (未进行 <code>add</code> 操作) 使用 <code>git status</code> 来查看工作区的状态时，可以发现：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux git_test# vim code </span><br><span class="line">root@Linux git_test# git status</span><br><span class="line"># On branch master</span><br><span class="line"># Changes not staged for commit:</span><br><span class="line">#   (use &quot;git add &lt;file&gt;...&quot; to update what will be committed)</span><br><span class="line">#   (use &quot;git checkout -- &lt;file&gt;...&quot; to discard changes in working directory)</span><br><span class="line">#</span><br><span class="line">#       modified:   code</span><br><span class="line">#</span><br><span class="line">no changes added to commit (use &quot;git add&quot; and&#x2F;or &quot;git commit -a&quot;)</span><br></pre></td></tr></table></figure><p>Git 会有两个提示：</p><ul><li>一个是使用 <code>git add &lt;FILE&gt;</code> 来提交工作区的内容</li><li>另一个是使用 <code>git checkout -- &lt;FILE&gt;</code> 来撤销所编辑的内容(也就是恢复到之前保存到暂存区的内容)</li></ul><p>因此我们可以使用撤销所编辑的内容，如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux git_test# git checkout -- code</span><br><span class="line">root@Linux git_test# git status</span><br><span class="line"># On branch master</span><br><span class="line">nothing to commit, working directory clean</span><br><span class="line">root@Linux git_test# cat code </span><br><span class="line">This is the first line</span><br><span class="line">This is the second line</span><br></pre></td></tr></table></figure><p>如果我们在编辑完之后将内容添加到暂存区的话，我们也可以使用 <code>git status</code> 来查看信息：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux git_test# vim code </span><br><span class="line">root@Linux git_test# git add code</span><br><span class="line">root@Linux git_test# git status </span><br><span class="line"># On branch master</span><br><span class="line"># Changes to be committed:</span><br><span class="line">#   (use &quot;git reset HEAD &lt;file&gt;...&quot; to unstage)</span><br><span class="line">#</span><br><span class="line">#       modified:   code</span><br><span class="line">#</span><br></pre></td></tr></table></figure><p>这里 Git 提示我们可以使用 <code>git reset HEAD &lt;FILE&gt;</code> 来撤销这次提交，也就是回退到编辑完的状态<br>如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux git_test# git reset HEAD code</span><br><span class="line">Unstaged changes after reset:</span><br><span class="line">M       code</span><br><span class="line">root@Linux git_test# git status </span><br><span class="line"># On branch master</span><br><span class="line"># Changes not staged for commit:</span><br><span class="line">#   (use &quot;git add &lt;file&gt;...&quot; to update what will be committed)</span><br><span class="line">#   (use &quot;git checkout -- &lt;file&gt;...&quot; to discard changes in working directory)</span><br><span class="line">#</span><br><span class="line">#       modified:   code</span><br><span class="line">#</span><br><span class="line">no changes added to commit (use &quot;git add&quot; and&#x2F;or &quot;git commit -a&quot;)</span><br></pre></td></tr></table></figure><p>然后同样可以使用 <code>git checkout -- &lt;FILE&gt;</code> 来撤销编辑，和上面一样</p><br><h1 id="对比文件"><a href="#对比文件" class="headerlink" title="对比文件"></a>对比文件</h1><p>对比文件使用的命令为：<code>git diff HEAD_F [HEAD_E] -- FILE</code>，如 <code>git diff HEAD -- code</code> 就表示将当前版本中的 <code>code</code> 文件和工作区中的 <code>code</code> 文件进行比较，如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@Linux git_test# git diff HEAD -- code</span><br><span class="line">diff --git a&#x2F;code b&#x2F;code</span><br><span class="line">index 81bc58d..13447e2 100644</span><br><span class="line">--- a&#x2F;code</span><br><span class="line">+++ b&#x2F;code</span><br><span class="line">@@ -1,2 +1,3 @@</span><br><span class="line"> This is the first line</span><br><span class="line"> This is the second line</span><br><span class="line">+This is the third line</span><br></pre></td></tr></table></figure><p>其中的 <code>--- a/code</code> 对应的是 <code>HEAD_F</code> 指针指向的版本文件中的内容，<code>+++ b/code</code> 对应的是 <code>HEAD_E</code> 指针指向的版本中的内容<br>后面的 <code>+</code> 或 <code>-</code> 将多的或少的行显示出来</p><br>]]></content>
      <categories>
        <category>Software</category>
      </categories>
      <tags>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>Git 工作流</title>
    <url>/git/61f0e414-c826-42b0-88a3-5d90705c08f8.html</url>
    <content><![CDATA[<p>工作流有各式各样的用法，但也正因此使得在实际工作中如何上手使用增加了难度。这篇指南通过总览公司团队中最常用的几种 Git 工作流让大家可以上手使用。在阅读的过程中请记住，本文中的几种工作流是作为方案指导而不是条例规定。在展示了各种工作流可能的用法后，你可以从不同的工作流中挑选或揉合出一个满足你自己需求的工作流。</p><a id="more"></a><h1 id="集中式工作流"><a href="#集中式工作流" class="headerlink" title="集中式工作流"></a>集中式工作流</h1><p>像 Subversion 一样，集中式工作流以中央仓库作为项目所有修改的单点实体。相比 SVN 缺省的开发分支 <code>trunk</code>，Git 叫做 <code>master</code>，所有修改提交到这个分支上。该工作流只用到 <code>master</code> 这一个分支。</p><p>开发者开始先克隆中央仓库。在自己的项目拷贝中，像 SVN 一样的编辑文件和提交修改；但修改是存在本地的，和中央仓库是完全隔离的。开发者可以把和上游的同步延后到一个方便时间点。</p><p>要发布修改到正式项目中，开发者要把本地 master 分支的修改 <strong>推送 (push)</strong> 到中央仓库中。这相当于 <code>svn commit</code> 操作，但 push 操作会把所有还不在中央仓库的本地提交都推上去。</p><p><img src="/images/ldSAlHYHMhlOBMz1/f7PxlJXuXZR9LT8d.png" alt=""></p><p>中央仓库代表了正式项目，所以提交历史应该被尊重且是稳定不变的。如果开发者本地的提交历史和中央仓库有分歧，Git 会拒绝 push 提交，因为会覆盖已经在中央库的正式提交。</p><p><img src="/images/ldSAlHYHMhlOBMz1/kyfBucAO2HLtcQvf.png" alt=""></p><p>在开发者提交自己功能修改到中央库前，需要先 <strong>fetch</strong> 在中央库的新增提交，<strong>rebase</strong> 自己提交到中央库提交历史之上。这样就把自己的修改加到别人已经完成的修改上。最终的结果是一个完美的线性历史，就像以前的SVN 的工作流中一样。</p><p>如果本地修改和上游提交有冲突，Git 会暂停 <strong>rebase</strong> 过程，给你手动解决冲突的机会。Git 解决合并冲突，用和生成提交一样的 <code>git status</code> 和 <code>git add</code> 命令，很一致方便。</p><h2 id="示例"><a href="#示例" class="headerlink" title="示例"></a>示例</h2><p>现在有两个开发者 A 和 B，看他们是如何开发自己的功能并提交到中央仓库上的。</p><p>第一步，有人在服务器上创建好中央仓库。如果是新项目，你可以初始化一个空仓库；否则你要导入已有的 Git 或 SVN 仓库。</p><p>下一步，各个开发者拷贝仓库，通过 <code>git clone</code> 命令完成：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git clone https:&#x2F;&#x2F;github.com&#x2F;path&#x2F;to&#x2F;repo.git</span><br></pre></td></tr></table></figure><p>然后开发者 A 开发功能。在 A 的本地仓库中，他使用标准的 Git 过程开发功能：<code>编辑、暂存和提交</code>。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git status # 查看本地仓库的修改状态</span><br><span class="line">git add # 暂存文件</span><br><span class="line">git commit # 提交文件</span><br></pre></td></tr></table></figure><p>开发完成之后，A 将提交推送到远程仓库中：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git push origin master</span><br></pre></td></tr></table></figure><p>之后开发者 B 开发功能，在自己的本地仓库中用相同的编辑、暂存和提交过程开发功能。开发完成之后尝试发布功能，但他的本地历史记录和中央仓库有分歧了，Git 会拒绝操作：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git push origin master</span><br><span class="line">error: failed to push some refs to &#39;&#x2F;path&#x2F;to&#x2F;repo.git&#39;</span><br><span class="line">hint: Updates were rejected because the tip of your current branch is behind</span><br><span class="line">hint: its remote counterpart. Merge the remote changes (e.g. &#39;git pull&#39;)</span><br><span class="line">hint: before pushing again.</span><br><span class="line">hint: See the &#39;Note about fast-forwards&#39; in &#39;git push --help&#39; for details.</span><br></pre></td></tr></table></figure><p>为了解决分歧，B 需要将 master 分支中的内容拉取到自己的仓库中：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git pull --rebase origin master</span><br></pre></td></tr></table></figure><p><code>--rebase</code> 选项告诉 Git 把 B 的提交移到同步到了中央仓库修改后的 master 分支的顶部，如下：</p><p><img src="/images/ldSAlHYHMhlOBMz1/jbTtYRpoL0LlTJZ4.png" alt=""></p><p>如果你忘加了这个选项，<code>pull</code> 操作仍然可以完成，但每次 <code>pull</code> 操作要同步中央仓库中别人修改时，提交历史会以一个多余的『合并提交』结尾。对于集中式工作流，最好是使用 rebase 而不是生成一个合并提交。</p><p>B 解决冲突之后就可以进行推送了。</p><h1 id="功能分支工作流"><a href="#功能分支工作流" class="headerlink" title="功能分支工作流"></a>功能分支工作流</h1><p><img src="/images/ldSAlHYHMhlOBMz1/a6dfzigSGKC6yUtV.png" alt=""></p><p>功能分支工作流仍然用中央仓库，并且 master 分支还是代表了正式项目的历史。但不是直接提交本地历史到各自的本地 master 分支，开发者每次在开始新功能前先创建一个新分支。功能分支应该有个有描述性的名字，比如 <code>animated-menu-items</code> 或 <code>issue-#1061</code>，这样可以让分支有个清楚且高聚焦的用途。</p><p>在 master 分支和功能分支之间，Git 是没有技术上的区别，所以开发者可以用和集中式工作流中完全一样的方式编辑、暂存和提交修改到功能分支上。</p><p>另外，功能分支也可以 (且应该) push 到中央仓库中。这样不修改正式代码就可以和其它开发者分享提交的功能。由于 master 仅有的一个<strong>特殊</strong>分支，在中央仓库上存多个功能分支不会有任何问题。当然，这样做也可以很方便地备份各自的本地提交。</p><h2 id="示例-1"><a href="#示例-1" class="headerlink" title="示例"></a>示例</h2><p>开发者 A 开始开发一个功能</p><p><img src="/images/ldSAlHYHMhlOBMz1/nq7hymtg67MPOTWV.png" alt=""></p><p>在开始开发功能之前，A 需要一个独立的分支，使用下面的命令新建一个分支：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git checkout -b dev master</span><br></pre></td></tr></table></figure><p>开发完毕之后进行推送：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git push -u origin dev</span><br></pre></td></tr></table></figure><p>推送完之后，可以发起 <code>Pull Request</code>，请求合并 <code>dev</code> 到 <code>master</code>，团队成员会自动收到通知。然后就可以讨论合并，接受了 <code>Pull Request</code> 之后就可以进行合并，可以使用 <code>git merge</code> 合并，也可以在仓库服务器中进行一键合并。</p><h1 id="GitFlow-工作流"><a href="#GitFlow-工作流" class="headerlink" title="GitFlow 工作流"></a>GitFlow 工作流</h1><p><img src="/images/ldSAlHYHMhlOBMz1/VFZojIcWarS1iYLW.png" alt=""></p><p>GitFlow 工作流定义了一个围绕项目发布的严格分支模型。虽然比功能分支工作流复杂几分，但提供了用于一个健壮的用于管理大型项目的框架。</p><p>GitFlow 工作流没有用超出功能分支工作流的概念和命令，而是为不同的分支分配一个很明确的角色，并定义分支之间如何和什么时候进行交互。除了使用功能分支，在做准备、维护和记录发布也使用各自的分支。当然你可以用上功能分支工作流所有的好处：Pull Requests、隔离实验性开发和更高效的协作。</p><p>GitFlow 工作流仍然用中央仓库作为所有开发者的交互中心。和其它的工作流一样，开发者在本地工作并 push 分支到中央仓库中。</p><p>相对使用仅有的一个 master 分支，GitFlow 工作流使用 2 个分支来记录项目的历史。master 分支存储了正式发布的历史，而 develop 分支作为功能的集成分支。这样也方便 master 分支上的所有提交分配一个版本号。</p><p><img src="/images/ldSAlHYHMhlOBMz1/mdZCbiZkjzaa3dXj.png" alt=""></p><p>每个新功能位于一个自己的分支，这样可以 push 到中央仓库以备份和协作。但功能分支不是从 master 分支上拉出新分支，而是使用 develop 分支作为父分支。当新功能完成时，合并回 develop 分支。新功能提交应该从不直接与 master 分支交互。</p><p><img src="/images/ldSAlHYHMhlOBMz1/evmbudc2lMtRi136.png" alt=""></p><p><strong>注意</strong>，从各种含义和目的上来看，功能分支加上 develop 分支就是功能分支工作流的用法。但 GitFlow 工作流没有在这里止步。</p><p>一旦 develop 分支上有了做一次发布（或者说快到了既定的发布日）的足够功能，就从 develop 分支上 fork 一个<strong>发布分支</strong>。新建的分支用于开始发布循环，所以从这个时间点开始之后新的功能不能再加到这个分支上 —— 这个分支只应该做 Bug 修复、文档生成和其它面向发布任务。一旦对外发布的工作都完成了，发布分支合并到 master 分支并分配一个版本号打好 Tag。另外，这些从新建发布分支以来的做的修改要合并回 develop 分支。</p><p><img src="/images/ldSAlHYHMhlOBMz1/CDIj0ylukK0Yjs1P.png" alt=""></p><p>使用一个用于发布准备的专门分支，使得一个团队可以在完善当前的发布版本的同时，另一个团队可以继续开发下个版本的功能。这也打造定义良好的开发阶段。</p><p><strong>维护分支</strong> 或说是热修复（hotfix）分支用于生成快速给产品发布版本（production releases）打补丁，这是唯一可以直接从 master 分支 fork 出来的分支。修复完成，修改应该马上合并回 master 分支和 develop 分支（当前的发布分支），master 分支应该用新的版本号打好 Tag。</p><p><img src="/images/ldSAlHYHMhlOBMz1/KgA464VF7iCNmgRr.png" alt=""></p><p>为 Bug 修复使用专门分支，让团队可以处理掉问题而不用打断其它工作或是等待下一个发布循环。你可以把维护分支想成是一个直接在 master 分支上处理的临时发布。</p><h2 id="示例-2"><a href="#示例-2" class="headerlink" title="示例"></a>示例</h2><p>第一步为 master 分支配套一个 develop 分支。简单来做可以本地创建一个空的 develop 分支，push 到服务器上：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git branch develop</span><br><span class="line">git push -u origin develop</span><br></pre></td></tr></table></figure><p>以后这个分支将会包含了项目的全部历史，而 master 分支将只包含了部分历史。其它开发者这时应该克隆中央仓库，建好 develop 分支的跟踪分支：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git clone ssh:&#x2F;&#x2F;user@host&#x2F;path&#x2F;to&#x2F;repo.git</span><br><span class="line">git checkout -b develop origin&#x2F;develop</span><br></pre></td></tr></table></figure><p>现在每个开发都有了这些历史分支的本地拷贝，各开发者们开始各自的功能开发，他们需要为各自的功能创建相应的分支。<strong>注意新创建的分支是基于 develop 分支的。</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git checkout -b some-feature develop</span><br></pre></td></tr></table></figure><p>添加了提交后，使用 Pull Requests，发起一个用于合并到 develop 分支的请求。</p><p>当各功能开发完毕并且 develop 分支合并结束后，我们需要准备一个发布分支：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git checkout -b release-1.0.0 develop</span><br></pre></td></tr></table></figure><p>这个分支是清理发布、执行所有测试、更新文档和其它为下个发布做准备操作的地方，像是一个专门用于改善发布的功能分支。</p><p>一旦准备好了对外发布，合并修改到 master 分支和 develop 分支上，删除发布分支。合并回 develop 分支很重要，因为在发布分支中已经提交的更新需要在后面的新功能中也要是可用的。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git checkout master</span><br><span class="line">git merge release-1.0.0</span><br><span class="line">git push</span><br><span class="line">git checkout develop</span><br><span class="line">git merge release-1.0.0</span><br><span class="line">git push</span><br><span class="line">git branch -d release-1.0.0</span><br></pre></td></tr></table></figure><p>发布分支是作为功能开发（develop 分支）和对外发布（master 分支）间的缓冲。只要有合并到 master 分支，就应该打好 Tag 以方便跟踪。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git tag -a 1.0.0-RELEASE -m &quot;Initial public release&quot; master</span><br><span class="line">git push --tags</span><br></pre></td></tr></table></figure><p>对外发布后，当前版本出了一个 Bug。为了处理 Bug，我们需要从 master 分支上拉出了一个维护分支，提交修改以解决问题，然后直接合并回 master 分支：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git checkout -b issue-#001 master</span><br><span class="line"># Fix the bug</span><br><span class="line">git checkout master</span><br><span class="line">git merge issue-#001</span><br><span class="line">git push</span><br></pre></td></tr></table></figure><h1 id="Pull-Request"><a href="#Pull-Request" class="headerlink" title="Pull Request"></a>Pull Request</h1><p>Pull Requests 是各仓库服务器上方便开发者之间协作的功能。提供了一个用户友好的 Web 界面，在集成提交的变更到正式项目前可以对变更进行讨论。</p><p><img src="/images/ldSAlHYHMhlOBMz1/Pull_request.png" alt=""></p><p>开发者向团队成员通知功能开发已经完成，Pull Requests 是最简单的用法。开发者完成功能开发后，发起一个 Pull Request。这样让涉及这个功能的所有人知道，要去做 Code Review 和合并到 master 分支。</p><p>但是，Pull Request 远不止一个简单的通知，而是为讨论提交的功能的一个专门论坛。如果变更有任何问题，团队成员反馈在 Pull Request 中，甚至 push 新的提交微调功能。所有的这些活动都直接跟踪在 Pull Request 中。</p><p><img src="/images/ldSAlHYHMhlOBMz1/pull-request-overview.png" alt=""></p><p>相比其它的协作模型，这种分享提交的形式有助于打造一个更流畅的工作流。SVN 和 Git 都能通过一个简单的脚本收到通知邮件；但是，讨论变更时，开发者通常只能去回复邮件。这样做会变得杂乱，尤其还要涉及后面的几个提交时。Pull Requests 把所有相关功能整合到一个和 Bitbucket 仓库界面集成的用户友好 Web 界面中。</p>]]></content>
      <categories>
        <category>Software</category>
      </categories>
      <tags>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>Git 常用命令</title>
    <url>/git/4bc6a83f-0bd1-4e3b-bee5-2f077921cc4b.html</url>
    <content><![CDATA[<p>本文介绍 Git 的常用命令</p><a id="more"></a><h1 id="git-init"><a href="#git-init" class="headerlink" title="git init"></a><code>git init</code></h1><p>在目录中创建新的 Git 本地仓库。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git init</span><br></pre></td></tr></table></figure><h1 id="git-clone"><a href="#git-clone" class="headerlink" title="git clone"></a><code>git clone</code></h1><p>拷贝一个远程仓库到本地，可以使用 HTTP 或 SSH 的方式：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git clone URL</span><br></pre></td></tr></table></figure><h1 id="git-add"><a href="#git-add" class="headerlink" title="git add"></a><code>git add</code></h1><p>将修改过的内容添加到暂存区中</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git add &lt;FILENAME&gt;</span><br></pre></td></tr></table></figure><h1 id="git-checkout"><a href="#git-checkout" class="headerlink" title="git checkout"></a><code>git checkout</code></h1><p>撤销工作区中编辑的内容，恢复到暂存区中保存的内容</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git ckeckout -- &lt;FILENAME&gt;</span><br></pre></td></tr></table></figure><p><code>ckeckout</code> 也可以用于切换分支 (使用 <code>-b</code> 参数可以在分支不存在的情况下创建一个分支，创建的时候可以指定一个分支，默认为当前分支)</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git checkout [-b] &lt;BRANCH NAME&gt; [BRANCH NAME]</span><br></pre></td></tr></table></figure><h1 id="git-status"><a href="#git-status" class="headerlink" title="git status"></a><code>git status</code></h1><p>查看在上次提交之后是否存在修改</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git status</span><br><span class="line">git status -s</span><br></pre></td></tr></table></figure><h1 id="git-diff"><a href="#git-diff" class="headerlink" title="git diff"></a><code>git diff</code></h1><p>该命令用于显示已写入缓存与已修改但尚未写入缓存的改动的区别，有如下几种用法：</p><ol><li>查看尚未缓存的改动 (<code>--stat</code> 用于显示摘要而非整个信息)：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git diff</span><br><span class="line">git diff --stat</span><br></pre></td></tr></table></figure></li><li>查看已缓存的改动：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git diff --cached</span><br></pre></td></tr></table></figure></li><li>查看已缓存和未缓存的所有改动：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git diff HEAD</span><br></pre></td></tr></table></figure></li><li>对比某个文件在两个版本中的不同 (其中的 <code>HEAD_F</code> 和 <code>HEAD_E</code> 是两个版本指针，后者省略就和工作区的内容进行比较)：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git diff HEAD_F [HEAD_E] -- &lt;FILE&gt;</span><br></pre></td></tr></table></figure></li></ol><h1 id="git-commit"><a href="#git-commit" class="headerlink" title="git commit"></a><code>git commit</code></h1><p>将暂存区中的内容添加到仓库中。注意，Git 会为我们每一次提交记录名字和电子邮箱，因此我们需要在 <code>~/.gitconfig</code> 中配置我们的用户名和邮箱</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[user]</span><br><span class="line">    email &#x3D; janvysun@gmail.com</span><br><span class="line">    name &#x3D; Janvy Sun</span><br></pre></td></tr></table></figure><p>或者我们也可以使用如下命令来配置：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git config --global user.name &#39;YOURNAME&#39;</span><br><span class="line">git config --global user.email &#39;YOUREMAIL&#39;</span><br></pre></td></tr></table></figure><p>然后就可以使用 <code>commit</code> 命令来提交：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git commit -m &#39;UPDATE MESSAGE&#39;</span><br></pre></td></tr></table></figure><h1 id="git-log"><a href="#git-log" class="headerlink" title="git log"></a><code>git log</code></h1><p>该命令用于查看版本记录，包括版本号、提交记录、版本信息等</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git log</span><br><span class="line">git log --pretty&#x3D;oneline --graph</span><br></pre></td></tr></table></figure><p>使用 <code>--pretty=oneline</code> 可以显示较为简单的信息，让每个版本都显示为一行；使用 <code>--graph</code> 可以显示分支的图形信息</p><h1 id="git-reset"><a href="#git-reset" class="headerlink" title="git reset"></a><code>git reset</code></h1><p>该命令用于回退到以前的版本</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git reset --hard 版本指针&#x2F;版本号</span><br></pre></td></tr></table></figure><p>使用版本指针时，<code>HEAD</code> 表示当前的版本，<code>HEAD^</code> 表示上一个版本，<code>HEAD^^</code> 表示当前版本的前前个版本；也可以使用 <code>HEAD~1</code> 表示上一个版本，<code>HEAD~100</code> 表示当前版本的前 100 个版本。<br>使用版本号时，给出版本号的前几位 (能区分每个版本) 即可。</p><p>我们也可以使用 <code>reset</code> 来撤销 <code>add</code> 的内容，如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git reset HEAD &lt;FILE&gt;</span><br></pre></td></tr></table></figure><h1 id="git-reflog"><a href="#git-reflog" class="headerlink" title="git reflog"></a><code>git reflog</code></h1><p>可以查看所有版本的所有操作记录 (包括已经被删除的 <code>commit</code> 记录和 <code>reset</code> 记录)</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git reflog</span><br></pre></td></tr></table></figure><h1 id="git-rm"><a href="#git-rm" class="headerlink" title="git rm"></a><code>git rm</code></h1><p>该命令用于删除一个文件，我们也可以使用 <code>rm</code> 命令来删除文件，删除之后用 <code>add</code> 和 <code>commit</code> 进行提交，但是使用 <code>git rm</code> 则不需要 <code>add</code> 操作。</p><h1 id="git-branch"><a href="#git-branch" class="headerlink" title="git branch"></a><code>git branch</code></h1><p>该命令用于 Git 的分支管理。</p><ul><li>查看当前分支 (<code>-a</code> 选项可显示所有分支，包括远程分支)：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git branch [-a]</span><br></pre></td></tr></table></figure></li><li>创建一个分支：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git branch &lt;BRANCH NAME&gt;</span><br></pre></td></tr></table></figure></li><li>删除分支：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git branch -d &lt;BRANCH NAME&gt;</span><br></pre></td></tr></table></figure></li><li>跟踪远程分支：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git branch --set-upstream-to&#x3D;origin&#x2F;远程分支名 本地分支名</span><br></pre></td></tr></table></figure></li></ul><h1 id="git-merge"><a href="#git-merge" class="headerlink" title="git merge"></a><code>git merge</code></h1><ul><li>将某个分支合并到当前分支上：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git merge &lt;BRANCH NAME&gt;</span><br></pre></td></tr></table></figure></li><li>禁用 <code>fast-forward</code> 模式并进行一次提交：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git merge --no-ff -m &#39;UPDATE MESSAGE&#39; &lt;BRANCH NAME&gt;</span><br></pre></td></tr></table></figure></li></ul><h1 id="git-stash"><a href="#git-stash" class="headerlink" title="git stash"></a><code>git stash</code></h1><ul><li>保存当前工作区：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git stash</span><br></pre></td></tr></table></figure></li><li>恢复到以前保存的工作区：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git stash pop</span><br></pre></td></tr></table></figure></li></ul><h1 id="git-pull"><a href="#git-pull" class="headerlink" title="git pull"></a><code>git pull</code></h1><p>该命令用于从另一个存储库或本地分支获取并集成 (整合)。使用该命令会取回远程主机某个分支的更新，再与本地指定的分支合并，格式如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git pull &lt;远程主机名&gt; &lt;远程分支名&gt;:&lt;本地分支名&gt;</span><br></pre></td></tr></table></figure><p>如果是使用 <code>git clone</code> 下载下来的，可以使用如下命令来整合：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git pull origin 分支名</span><br></pre></td></tr></table></figure><h1 id="git-push"><a href="#git-push" class="headerlink" title="git push"></a><code>git push</code></h1><p>该命令用于将本地分支推送到远程主机</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git push &lt;远程主机名&gt; &lt;远程分支名&gt;:&lt;本地分支名&gt;</span><br></pre></td></tr></table></figure><p>如果是使用 <code>git clone</code> 下载下来的，可以使用如下命令进行推送 (<code>-u</code> 选项可以设置本地分支跟踪远程对应的分支，跟踪之后再推送，可以直接使用 <code>git push</code> 命令)：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git push [-u] origin 分支名</span><br></pre></td></tr></table></figure><p>删除远程分支：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git push --delete origin &lt;BRANCH NAME&gt;</span><br></pre></td></tr></table></figure><h1 id="git-tag"><a href="#git-tag" class="headerlink" title="git tag"></a><code>git tag</code></h1><p>该命令用于给版本打上一个标签。比如说，我们想为我们的商城项目发布一个「1.0.0-RELEASE」版本。我们可以用 <code>git tag -a 1.0.0-RELEASE</code> 命令给最新一次提交 (HEAD) 打上「1.0.0-RELEASE」的标签。</p><p><code>-a</code> 选项意为 “创建一个带注解的标签”。 不用 <code>-a</code> 选项也可以执行的，但它不会记录这标签是啥时候打的，谁打的，也不会让你添加个标签的注解。推荐一直创建带注解的标签。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git tag -a v1.0.0-RELEASE</span><br></pre></td></tr></table></figure><p><code>-m</code> 选项可以添加注释信息</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git tag -a v1.0 -m &#39;comment&#39;</span><br></pre></td></tr></table></figure><p>如果我们要查看所有标签可以使用以下命令：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git tag</span><br></pre></td></tr></table></figure><p>显示指定 tag 的信息</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git show v0.1</span><br><span class="line">tag v1.4</span><br><span class="line">Tagger: Janvy Sun &lt;janvysun@gmail.com&gt;</span><br><span class="line">Date:   Wed Mar 31 08:50:37 2021 +0800</span><br><span class="line"></span><br><span class="line">first version</span><br><span class="line"></span><br><span class="line">commit c56db371a889a1f6f21a78f2023664bdb98e3966 (HEAD -&gt; main, tag: v0.1, origin&#x2F;main, origin&#x2F;HEAD)</span><br><span class="line">Author: Janvy Sun &lt;janvysun@gmail.com&gt;</span><br><span class="line">Date:   Tue Mar 30 23:49:08 2021 +0800</span><br><span class="line"></span><br><span class="line">    init</span><br><span class="line"></span><br><span class="line">diff --git a&#x2F;README.md b&#x2F;README.md</span><br><span class="line">index a505641..3d5605e 100644</span><br></pre></td></tr></table></figure><p>我们在执行 <code>git push</code> 的时候，tag 是不会上传到服务器的，比如现在的github，创建tag后git push，在github网页上是看不到tag的，为了共享这些tag，你必须这样:</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; 将某个 tag push</span><br><span class="line">git push origin v1.0</span><br><span class="line">&#x2F;&#x2F; 将所有 tag 一次全部 push</span><br><span class="line">git push origin --tags</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Software</category>
      </categories>
      <tags>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>IDEA 快捷键</title>
    <url>/idea/13357e7e-6d3b-4f21-9bf2-b601c09f2b13.html</url>
    <content><![CDATA[<p>本文本文介绍 IDEA 常用快捷键及常用的配置</p><a id="more"></a><h2 id="功能（Main-Menu）"><a href="#功能（Main-Menu）" class="headerlink" title="功能（Main Menu）"></a>功能（Main Menu）</h2><ul><li><strong>重写方法</strong>：<code>Ctrl + Alt + O</code>（Code -&gt; Override Methods）</li><li><strong>实现方法</strong>：<code>Ctrl + Alt + I</code>（Code -&gt; Implement Methods）</li><li><strong>代码生成</strong>：<code>Alt + Shift + S</code>（Code -&gt; Generate…）</li><li><strong>变量引入</strong>：<code>Alt + V</code>（Refactor -&gt; Extract -&gt; Introduce Varialbe …）</li><li><strong>优化引入</strong>：<code>Alt + I</code>（Code -&gt; Optimize import）</li><li><strong>Back</strong>：<code>Ctrl + Alt + Left</code>、<code>Ctrl + O</code>（Navigate -&gt; Back）</li><li><strong>Forward</strong>：<code>Ctrl + Alt + Right</code>、<code>Ctrl + I</code>（Navigate -&gt; Forward）</li><li><strong>代码提示</strong>：<code>Alt + /</code>（Code -&gt; Completion -&gt; Basic）</li><li><strong>智能提示</strong>：<code>Alt + Shift + /</code>（Code -&gt; Completion -&gt; Type Matching）</li><li><strong>代码包裹</strong>：<code>Ctrl + Alt + T</code></li></ul><br><h2 id="编辑动作（Editor-Actions）"><a href="#编辑动作（Editor-Actions）" class="headerlink" title="编辑动作（Editor Actions）"></a>编辑动作（Editor Actions）</h2><ul><li><strong>代码自动对齐</strong>：<code>Ctrl + Alt + L</code></li><li><strong>字体放大</strong>：<code>Ctrl + Wheel up</code>（Increase Font Size）</li><li><strong>字体缩小</strong>：<code>Ctrl + Wheel down</code>（Decrease Font Size）</li><li><strong>字体恢复初始状态</strong>：<code>Ctrl + Middle-Click</code>（Reset Font Size）</li></ul><br><h2 id="工具窗（Tool-Windows）"><a href="#工具窗（Tool-Windows）" class="headerlink" title="工具窗（Tool Windows）"></a>工具窗（Tool Windows）</h2><ul><li><strong>项目目录（Project）</strong>：<code>Alt + 1</code></li><li><strong>收藏夹（Favorites）</strong>：<code>Alt + 2</code></li><li><strong>查找（Find）</strong>：<code>Alt + 3</code></li><li><strong>启动（Run）</strong>：<code>Alt + 4</code></li><li><strong>调试（Debug）</strong>：<code>Alt + 5</code></li><li><strong>问题（Problems）</strong>：<code>Alt + 6</code></li><li><strong>代码结构（Structure）</strong>：<code>Alt + 7</code></li><li><strong>服务（Service）</strong>：<code>Alt + 8</code></li><li><strong>Git</strong>：<code>Alt + 9</code></li><li><strong>版本控制工具（Commit）</strong>：<code>Alt + 0</code></li><li><strong>终端（Terminal）</strong>：<code>Alt + =</code>（Terminal）</li><li><strong>Hierarchy</strong>：<code>Alt + H</code>（Hierarchy，<code>Ctrl + H</code> 可将类加入其中进行分析）</li><li><strong>Database</strong>：<code>Alt + D</code>（Database）</li><li><strong>Maven</strong>：<code>Alt + M</code>（Maven）</li></ul>]]></content>
      <categories>
        <category>Software</category>
      </categories>
      <tags>
        <tag>IDEA</tag>
      </tags>
  </entry>
  <entry>
    <title>Maven 的安装与配置</title>
    <url>/maven/41ecc68f-93a4-4de1-aae2-0e48a6d506fa.html</url>
    <content><![CDATA[<p>Maven 是一个项目管理和综合工具。Maven 提供了开发人员构建一个完整的生命周期框架。开发团队可以自动完成项目的基础工具建设，Maven 使用标准的目录结构和默认构建生命周期。</p><a id="more"></a><h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><p>Maven 的安装非常方便，只需在 <a href="http://maven.apache.org/download.cgi" target="_blank" rel="noopener"><strong>Maven 官网</strong></a> 上下载压缩包，解压即可完成安装。需要注意的是 Maven 的运行需要 JDK，Maven 对 JDK 有版本的要求，请尽量使用 JDK 1.8+ 的版本</p><p><strong>注意下载的是 Binary 的压缩包</strong></p><p><img src="https://i.loli.net/2020/09/18/WvEoHCm1KPkGQjl.png" alt=""></p><h3 id="Windows-下-Maven-的安装"><a href="#Windows-下-Maven-的安装" class="headerlink" title="Windows 下 Maven 的安装"></a>Windows 下 Maven 的安装</h3><p>Windows 下直接将压缩包解压即可，解压之后如果要在命令行中使用 Maven，需要配置环境变量（环境变量中需要有 <code>JAVA_HOME</code> 的配置）。</p><p>右击「此电脑」，点击「高级系统设置」，点击「环境变量」加入 <code>MAVEN_HOME</code>（<strong>变量值为 Maven 的解压目录</strong>），然后在 <code>PATH</code> 变量中将 <code>MAVEN_HOME</code> 目录下的 <code>bin</code> 目录添加进去即可。</p><p><img src="https://i.loli.net/2020/09/18/t8Wh95LABK6qbyM.png" alt=""></p><p><img src="https://i.loli.net/2020/09/18/ziv7HjISg3MObGR.png" alt=""></p><p>操作完成后，打开 CMD，输入 <code>mvn -v</code>，出现如下信息代表安装成功：</p><p><img src="https://i.loli.net/2020/09/18/nRwQvoVDHtXikxb.png" alt=""></p><h3 id="Linux-下-Maven-的安装"><a href="#Linux-下-Maven-的安装" class="headerlink" title="Linux 下 Maven 的安装"></a>Linux 下 Maven 的安装</h3><p>在 Linux 中，软件的解压一般放在 <code>/usr/local</code> 或者 <code>$HOME/.local</code>（用户“家”目录下的 <code>.local</code>） 目录下。我个人习惯放 <code>$HOME/.local</code> 目录。（<strong>注意，Maven 的运行需要 JDK</strong>）</p><p>我将 Maven 解压到如下目录：<code>/home/sjw/.local/apache-maven-3.6.1</code></p><p>然后编辑 <code>~/.profile</code> 文件（<strong>注意，Ubuntu 是 <code>.profile</code>，CentOS 是 <code>.bash_profile</code></strong>），在其中将 Maven 加入环境变量（注意我这里包括了 <code>JAVA_HOME</code>）：</p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> JAVA_HOME=<span class="string">'/home/sjw/.local/jdk1.8.0_152'</span></span><br><span class="line"><span class="built_in">export</span> MAVEN_HOME=<span class="string">'/home/sjw/.local/apache-maven-3.6.1'</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$PATH</span>:<span class="variable">$JAVA_HOME</span>/bin:<span class="variable">$MAVEN_HOME</span>/bin</span><br></pre></td></tr></table></figure><p>配置完成之后，运行 <code>mvn -v</code> 命令，看到 Maven 的版本信息则安装成功：</p><p><img src="https://i.loli.net/2020/09/18/yRpWijTuw12Q4em.png" alt=""></p><h2 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h2><h3 id="本地仓库目录"><a href="#本地仓库目录" class="headerlink" title="本地仓库目录"></a>本地仓库目录</h3><p>默认情况下，Maven 的本地仓库目录为：</p><ul><li>Unix/Mac OS：<code>~/.m2</code></li><li>Windows：<code>C:\Users\{your-username}\.m2</code></li></ul><p>为了方便管理，通常自己配置本地仓库目录。在 Maven 的解压目录下打开 <code>conf/settings.xml</code>，在其中找到 <code>localRepository</code> 标签，加入如下信息（标签中设置你所想要的仓库目录）：</p><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">localRepository</span>&gt;</span>D:\Maven\repo<span class="tag">&lt;/<span class="name">localRepository</span>&gt;</span></span><br></pre></td></tr></table></figure><p><img src="https://i.loli.net/2020/09/18/Hsu8GY5jKwydxS3.png" alt="image.png"></p><h3 id="配置镜像仓库"><a href="#配置镜像仓库" class="headerlink" title="配置镜像仓库"></a>配置镜像仓库</h3><p>由于 Maven 在使用的过程中需要从 <a href="http://repo1.maven.org/maven2/" target="_blank" rel="noopener"><strong>中央仓库</strong></a> 下载依赖，但是国内访问中央仓库的速度缓慢，所以我们需要通过配置镜像仓库，让 Maven 在使用过程中从镜像仓库中下载依赖。</p><p>我推荐阿里云的仓库，在 Maven 的解压目录下找到 <code>conf/settings.xml</code> 文件，在 <code>&lt;mirrors&gt;</code> 节点中加入如下内容：</p><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">mirror</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">id</span>&gt;</span>aliyunmaven<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mirrorOf</span>&gt;</span>*<span class="tag">&lt;/<span class="name">mirrorOf</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>阿里云公共仓库<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">url</span>&gt;</span>https://maven.aliyun.com/repository/public<span class="tag">&lt;/<span class="name">url</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">mirror</span>&gt;</span></span><br></pre></td></tr></table></figure><p><img src="https://i.loli.net/2020/09/18/NkTiZ14ODA8nqUo.png" alt=""></p>]]></content>
      <categories>
        <category>Software</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>Maven</tag>
      </tags>
  </entry>
  <entry>
    <title>Nexus 私服</title>
    <url>/maven/970354b2-c305-458d-98c0-80da5861923c.html</url>
    <content><![CDATA[<p>Nexus 是一个强大的仓库管理器，极大地简化了内部仓库的维护和外部仓库的访问。</p>
<a id="more"></a>

<h2 id="基于-Docker-安装-Nexus"><a href="#基于-Docker-安装-Nexus" class="headerlink" title="基于 Docker 安装 Nexus"></a>基于 Docker 安装 Nexus</h2><p><code>docker-compose.yml</code> 如下：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">'3.1'</span></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">nexus:</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">always</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">sonatype/nexus3</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">nexus</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="number">8081</span><span class="string">:8081</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/usr/local/docker/nexus/data:/nexus-data</span></span><br></pre></td></tr></table></figure>
<p><strong>注意</strong>：启动时如果数据卷出现权限问题可以将数据卷目录设置为 <strong>777</strong> 的权限。</p>
<p>启动之后访问 nexus 默认登录账号为 <code>admin</code> ，密码在数据卷根目录下的 <code>admin.password</code> 文件中，第一次登录后会提示修改密码。</p>
<h2 id="私服的使用"><a href="#私服的使用" class="headerlink" title="私服的使用"></a>私服的使用</h2><h3 id="项目部署到私服"><a href="#项目部署到私服" class="headerlink" title="项目部署到私服"></a>项目部署到私服</h3><p>要使用 Nexus 私服，我们首先需要在 Maven 的 <code>settings.xml</code> 中的 <code>services</code> 节点下添加 Nexus 认证信息（<strong>账号与密码要与你 Nexus 中的对应</strong>）：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">server</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">id</span>&gt;</span>nexus-releases<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">username</span>&gt;</span>admin<span class="tag">&lt;/<span class="name">username</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">password</span>&gt;</span>admin123<span class="tag">&lt;/<span class="name">password</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">server</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">server</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">id</span>&gt;</span>nexus-snapshots<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">username</span>&gt;</span>admin<span class="tag">&lt;/<span class="name">username</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">password</span>&gt;</span>admin123<span class="tag">&lt;/<span class="name">password</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">server</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>然后我们在 Maven 根项目的 <code>pom.xml</code> 中添加如下信息（<strong><code>url</code>与你 Nexus 的 url 对应</strong>）：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">distributionManagement</span>&gt;</span>  </span><br><span class="line">  <span class="tag">&lt;<span class="name">repository</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">id</span>&gt;</span>nexus-releases<span class="tag">&lt;/<span class="name">id</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>Nexus Release Repository<span class="tag">&lt;/<span class="name">name</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">url</span>&gt;</span>http://127.0.0.1:8081/repository/maven-releases/<span class="tag">&lt;/<span class="name">url</span>&gt;</span> </span><br><span class="line">  <span class="tag">&lt;/<span class="name">repository</span>&gt;</span>  </span><br><span class="line">  <span class="tag">&lt;<span class="name">snapshotRepository</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">id</span>&gt;</span>nexus-snapshots<span class="tag">&lt;/<span class="name">id</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>Nexus Snapshot Repository<span class="tag">&lt;/<span class="name">name</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">url</span>&gt;</span>http://127.0.0.1:8081/repository/maven-snapshots/<span class="tag">&lt;/<span class="name">url</span>&gt;</span>  </span><br><span class="line">  <span class="tag">&lt;/<span class="name">snapshotRepository</span>&gt;</span>  </span><br><span class="line"><span class="tag">&lt;/<span class="name">distributionManagement</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>之后我们可以使用 <code>mvn deploy</code> 命令将项目部署到私服上去 (要跳过测试可以加 <code>-Dmaven.test.skip=true</code> 选项)：</p>
<h3 id="从私服下载"><a href="#从私服下载" class="headerlink" title="从私服下载"></a>从私服下载</h3><p>如果我们想要优先从私服上下载，则需要在<strong>根项目</strong>中添加如下信息（<strong><code>url</code>对应 Nexus 的 url</strong>）：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">repositories</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">repository</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">id</span>&gt;</span>nexus<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">name</span>&gt;</span>Nexus Repository<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">url</span>&gt;</span>http://127.0.0.1:8081/repository/maven-public/<span class="tag">&lt;/<span class="name">url</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">snapshots</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">enabled</span>&gt;</span>true<span class="tag">&lt;/<span class="name">enabled</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">snapshots</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">releases</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">enabled</span>&gt;</span>true<span class="tag">&lt;/<span class="name">enabled</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">releases</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">repository</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">repositories</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">pluginRepositories</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">pluginRepository</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">id</span>&gt;</span>nexus<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">name</span>&gt;</span>Nexus Plugin Repository<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">url</span>&gt;</span>http://127.0.0.1:8081/repository/maven-public/<span class="tag">&lt;/<span class="name">url</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">snapshots</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">enabled</span>&gt;</span>true<span class="tag">&lt;/<span class="name">enabled</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">snapshots</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">releases</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">enabled</span>&gt;</span>true<span class="tag">&lt;/<span class="name">enabled</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">releases</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">pluginRepository</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">pluginRepositories</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>之后我们再下载依赖的时候就会优先从私服中下载。</p>
<h3 id="上传第三方-Jar"><a href="#上传第三方-Jar" class="headerlink" title="上传第三方 Jar"></a>上传第三方 Jar</h3><p>我们可以使用如下命令上传第三方 Jar 包到私服中 (如上传 <code>com.aliyun.oss.aliyun-sdk-oss:2.2.3</code>)：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mvn deploy:deploy-file \</span><br><span class="line">  -DgroupId&#x3D;com.aliyun.oss \</span><br><span class="line">  -DartifactId&#x3D;aliyun-sdk-oss \</span><br><span class="line">  -Dversion&#x3D;2.2.3 \</span><br><span class="line">  -Dpackaging&#x3D;jar \</span><br><span class="line">  -Dfile&#x3D;D:\aliyun-sdk-oss-2.2.3.jar \</span><br><span class="line">  -Durl&#x3D;http:&#x2F;&#x2F;127.0.0.1:8081&#x2F;repository&#x2F;maven-release&#x2F; \</span><br><span class="line">  -DrepositoryId&#x3D;nexus-releases</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Software</category>
      </categories>
      <tags>
        <tag>Docker</tag>
        <tag>Maven</tag>
        <tag>Nexus</tag>
      </tags>
  </entry>
  <entry>
    <title>Maven 常用命令</title>
    <url>/maven/c058df1e-f54e-4edd-af44-97ce9f74973f.html</url>
    <content><![CDATA[<p>Maven 通常与 IDE 结合使用，在 IDE 中不需要我们输入命令。但是在服务器上使用 Maven 来打包部署则需要输入命令。本文将介绍 Maven 的一些常用命令。</p><a id="more"></a><p>Maven 本质上其实是一个插件工具，其所有的命令也是通过插件来完成的。Maven 命令的格式如下（<strong>注意，执行命令的时候，目录下必须包含 pom.xml 文件</strong>）：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mvn [plugin-name]:[goal-name]</span><br></pre></td></tr></table></figure><h2 id="生命周期命令"><a href="#生命周期命令" class="headerlink" title="生命周期命令"></a>生命周期命令</h2><h3 id="清除-target-目录"><a href="#清除-target-目录" class="headerlink" title="清除 target 目录"></a>清除 target 目录</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mvn clean</span><br></pre></td></tr></table></figure><h3 id="编译主程序"><a href="#编译主程序" class="headerlink" title="编译主程序"></a>编译主程序</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mvn compile</span><br></pre></td></tr></table></figure><p>该命令会编译 <code>main</code> 目录下的所有 <code>.java</code> 文件，并生成一个 <code>target</code> 目录，在其中的 classes 目录下包含编译后的 <code>*.class</code> 文件</p><h3 id="编译测试代码"><a href="#编译测试代码" class="headerlink" title="编译测试代码"></a>编译测试代码</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mvn test-compile</span><br></pre></td></tr></table></figure><p>该命令会编译 <code>test</code> 目录下的所有 <code>.java</code> 文件，生成一个 <code>target</code> 目录。</p><h3 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mvn test</span><br></pre></td></tr></table></figure><p>该命令会执行上述命令，并且会执行测试代码，执行结束后将测试结果保存在 <code>surefire-reports</code> 目录中</p><h3 id="打包"><a href="#打包" class="headerlink" title="打包"></a>打包</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mvn package</span><br><span class="line">mvn package -Dmaven.test.skip&#x3D;true</span><br></pre></td></tr></table></figure><p>该命令会执行上述所有命令，并将代码进行打包，打包的结果在 target 目录下。（如需跳过测试，加上 <code>-Dmaven.test.skip=true</code> 参数即可）</p><h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mvn install</span><br></pre></td></tr></table></figure><p>该命令会将本工程打包，并按照本工程的坐标保存到本地仓库中</p><h3 id="部署"><a href="#部署" class="headerlink" title="部署"></a>部署</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mvn deploy</span><br></pre></td></tr></table></figure><p>该命令会将本工程打包，并按照本工程的坐标部署到本地仓库和私服仓库中。</p>]]></content>
      <categories>
        <category>Software</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>Maven</tag>
      </tags>
  </entry>
  <entry>
    <title>Vim 配置</title>
    <url>/vim/8fd24e8f-3fdd-44ff-a15d-63af80042e95.html</url>
    <content><![CDATA[<p>本文列出 vim 的一些常用配置。</p><a id="more"></a><h2 id="通用配置"><a href="#通用配置" class="headerlink" title="通用配置"></a>通用配置</h2><ul><li><strong>搜索逐字显示</strong>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">set incsearch</span><br></pre></td></tr></table></figure></li><li><strong>搜索忽略大小写</strong>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">set ignorecase</span><br></pre></td></tr></table></figure></li><li><strong>缩进</strong>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">set tabstop&#x3D;4</span><br><span class="line">set shiftwidth&#x3D;4</span><br><span class="line">set softtabstop&#x3D;4</span><br><span class="line">set expandtab</span><br></pre></td></tr></table></figure></li><li><strong>特定文件设置不同的缩进</strong>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">autocmd FileType sh,xhtml,html,htmldjango,css,javascript,markdown,json,vue set tabstop&#x3D;2</span><br><span class="line">autocmd FileType sh,xhtml,html,htmldjango,css,javascript,markdown,json,vue set shiftwidth&#x3D;2</span><br><span class="line">autocmd FileType sh,xhtml,html,htmldjango,css,javascript,markdown,json,vue set softtabstop&#x3D;2</span><br></pre></td></tr></table></figure></li><li><strong>光标与底部间距</strong>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">set scrolloff&#x3D;3</span><br></pre></td></tr></table></figure></li></ul><br><h2 id="显示配置"><a href="#显示配置" class="headerlink" title="显示配置"></a>显示配置</h2><ul><li><strong>语法高亮</strong>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">syntax on</span><br></pre></td></tr></table></figure></li><li><strong>打开行号</strong>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">set nu</span><br></pre></td></tr></table></figure></li><li><strong>高亮显示行</strong>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">set cursorline</span><br></pre></td></tr></table></figure></li><li><strong>高亮显示列</strong>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">set cursorcolumn</span><br></pre></td></tr></table></figure></li><li><strong>搜索高亮</strong>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">set hlsearch</span><br></pre></td></tr></table></figure></li><li><strong>显示命令</strong>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">set showcmd</span><br></pre></td></tr></table></figure></li><li><strong>配色</strong>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">set background&#x3D;dark</span><br><span class="line">colorscheme solarized</span><br></pre></td></tr></table></figure></li></ul><br><p>【未完待续…】</p>]]></content>
      <categories>
        <category>Software</category>
      </categories>
      <tags>
        <tag>Vim</tag>
      </tags>
  </entry>
  <entry>
    <title>Vim 使用</title>
    <url>/vim/94767f55-f93e-431b-b35f-0fa56b7a4904.html</url>
    <content><![CDATA[<p>Vim 是一个类似于 Vi 的著名的功能强大、高度可定制的文本编辑器，在 Vi 的基础上改进和增加了很多特性。Vim普遍被推崇为类Vi编辑器中最好的一个，事实上真正的劲敌来自 Emacs 的不同变体。1999 年 Emacs 被选为 Linuxworld 文本编辑分类的优胜者，Vim 屈居第二。但在 2000 年 2 月 Vim 赢得了 Slashdot Beanie 的最佳开放源代码文本编辑器大奖，又将 Emacs 推至二线， 总的来看，Vim 和 Emacs 在文本编辑方面都是非常优秀的。本文列出 vim 的一些操作。</p><a id="more"></a><h1 id="打开文件"><a href="#打开文件" class="headerlink" title="打开文件"></a>打开文件</h1><p>Vim 一共三大模式：<strong>编辑模式(命令模式)</strong>、<strong>输入模式</strong>、<strong>末行模式</strong>。打开文件默认处于编辑模式。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vim &#x2F;path&#x2F;to&#x2F;somefile</span><br></pre></td></tr></table></figure><ul><li><code>vim +#</code>：打开文件，并定位于第#行</li><li><code>vim +</code>：打开文件，定位至最后一行</li><li><code>vim +/PATTERN</code>：打开文件，定位至第一次被 PATTERN 匹配到的行的行首</li></ul><h2 id="打开多个文件"><a href="#打开多个文件" class="headerlink" title="打开多个文件"></a>打开多个文件</h2><p><code>vim FILE1 FILE2...</code>（默认显示第一个文件）</p><p>末行模式下输入 <code>next</code> 则进入下一个文件，<code>prev</code> 进入前一个文件，<code>last</code> 表示切换至最后一个文件，<code>first</code> 表示切换至第一个文件</p><h2 id="分屏打开多个文件"><a href="#分屏打开多个文件" class="headerlink" title="分屏打开多个文件"></a>分屏打开多个文件</h2><ul><li><code>vim -o FILE1 FILE2</code>：水平显示2个窗口</li><li><code>vim -O FILE1 FILE2</code>：垂直显示2个窗口</li></ul><br><h1 id="关闭文件"><a href="#关闭文件" class="headerlink" title="关闭文件"></a>关闭文件</h1><ul><li><code>:q</code>：退出</li><li><code>:wq</code>：保存并退出</li><li><code>:q!</code>：不保存并退出</li><li><code>:x</code>：相当于 wq</li><li><code>ZZ:</code>：保存并退出</li></ul><br><h1 id="编辑模式命令"><a href="#编辑模式命令" class="headerlink" title="编辑模式命令"></a>编辑模式命令</h1><h2 id="移动光标"><a href="#移动光标" class="headerlink" title="移动光标"></a>移动光标</h2><ul><li><code>h</code>: 光标左移</li><li><code>j</code>: 光标下移</li><li><code>k</code>: 光标上移</li><li><code>l</code>: 光标右移</li><li><code>w</code>: 移至下一个单词的词首</li><li><code>e</code>: 跳至当前或下一个单词的词尾</li><li><code>b</code>: 跳至当前或前一个单词的词首</li></ul><h2 id="行内跳转"><a href="#行内跳转" class="headerlink" title="行内跳转"></a>行内跳转</h2><ul><li><code>0</code>: 绝对行首</li><li><code>^</code>: 行首的第一个非空白字符</li><li><code>$</code>: 绝对行尾</li></ul><h2 id="行间跳转"><a href="#行间跳转" class="headerlink" title="行间跳转"></a>行间跳转</h2><ul><li><code>gg</code>：跳转至行首</li><li><code>NUMG</code>：跳转至第NUM行；</li><li><code>G</code>：最后一行</li></ul><p><strong>末行模式下，直接给出行号即可完成行间跳转</strong></p><h2 id="翻屏"><a href="#翻屏" class="headerlink" title="翻屏"></a>翻屏</h2><ul><li><code>Ctrl+f</code>: 向下翻一屏</li><li><code>Ctrl+b</code>: 向上翻一屏</li><li><code>Ctrl+d</code>: 向下翻半屏</li><li><code>Ctrl+u</code>: 向上翻半屏</li></ul><h2 id="进入输入模式"><a href="#进入输入模式" class="headerlink" title="进入输入模式"></a>进入输入模式</h2><ul><li><code>i</code>: 在当前光标所在字符的前面，转为输入模式</li><li><code>a</code>: 在当前光标所在字符的后面，转为输入模式</li><li><code>o</code>: 在当前光标所在行的下方，新建一行，并转为输入模式</li><li><code>O</code>：在当前光标所在行的上方，新建一行，并转为输入模式</li><li><code>I</code>：在当前光标所在行的行首，转换为输入模式，相当于 <code>^i</code></li><li><code>A</code>：在当前光标所在行的行尾，转换为输入模式，相当于 <code>$a</code></li><li><code>ce/cw</code>：将光标所在位置到单词尾的字符删除，并进入输入模式，相当于 <code>dei</code></li><li><code>ciw</code>：将光标所在位置的单词删除，并进入输入模式，相当于 <code>diwi</code></li><li><code>C</code>：将光标所在位置一直到行尾的字符删除，并进入输入模式，相当于 <code>Da</code></li><li><code>s</code>：将光标所在位置的字符删除，并进入输入模式，相当于 <code>xi</code></li><li><code>S</code>：将光标所在位置的行删除（保留缩进），并进入输入模式，相当于 <code>^C</code></li></ul><p><strong>注</strong>：如果要从输入模式回到编辑模式，则需要按 <strong>ESC</strong> 键</p><h2 id="删除"><a href="#删除" class="headerlink" title="删除"></a>删除</h2><ul><li><code>x</code>：删除光标所在处的单个字符</li><li><code>NUMx</code>：删除光标所在处及向后的共NUM个字符</li><li><code>dw/de/db</code>：根据单词删除光标前后内容</li><li><code>dd</code>：删除当前光标所在行</li><li><code>D</code>：删除光标到行尾的内容</li></ul><h2 id="复制"><a href="#复制" class="headerlink" title="复制"></a>复制</h2><ul><li><code>y</code>：用法同 d 命令</li></ul><h2 id="粘贴"><a href="#粘贴" class="headerlink" title="粘贴"></a>粘贴</h2><ul><li><code>p</code>：如果删除或复制为整行内容，则粘贴至光标所在行的下方，如果复制或删除的内容为非整行，则粘贴至光标所在字符的后面；</li><li><code>P</code>：如果删除或复制为整行内容，则粘贴至光标所在行的上方，如果复制或删除的内容为非整行，则粘贴至光标所在字符的前面；</li></ul><h2 id="替换"><a href="#替换" class="headerlink" title="替换"></a>替换</h2><ul><li><code>r</code>：替换单个字符</li><li><code>R</code>：进入替换模式</li></ul><h2 id="撤消编辑操作"><a href="#撤消编辑操作" class="headerlink" title="撤消编辑操作"></a>撤消编辑操作</h2><ul><li><code>u</code>：撤消前一次的编辑操作</li><li><code>Ctrl + r</code>：撤消最近一次撤消操作</li></ul><h2 id="重复前一次编辑操作"><a href="#重复前一次编辑操作" class="headerlink" title="重复前一次编辑操作"></a>重复前一次编辑操作</h2><p>按 <code>.</code>（点）可重复前一次编辑操作</p><h2 id="拆分窗口"><a href="#拆分窗口" class="headerlink" title="拆分窗口"></a>拆分窗口</h2><ul><li><code>Ctrl-w s</code>：水平拆分窗口</li><li><code>Ctrl-w v</code>：垂直拆分窗口</li></ul><br><h1 id="末行模式"><a href="#末行模式" class="headerlink" title="末行模式"></a>末行模式</h1><h2 id="删除-1"><a href="#删除-1" class="headerlink" title="删除"></a>删除</h2><p><code>StartADD,EndADDd</code> （如1,3d表示删除1到3行）</p><ul><li><code>.</code>：表示当前行</li><li><code>$</code>：最后一行</li><li><code>+NUM</code>：向下的NUM行</li></ul><h2 id="删除空白行"><a href="#删除空白行" class="headerlink" title="删除空白行"></a>删除空白行</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">:g&#x2F;^\s*$&#x2F;d</span><br></pre></td></tr></table></figure><ul><li><code>g//</code>：全区命令</li><li><code>^\s*$</code>：匹配空行</li><li><code>d</code>：删除</li></ul><h2 id="查找"><a href="#查找" class="headerlink" title="查找"></a>查找</h2><ul><li><code>/PATTERN</code></li><li><code>?PATTERN</code></li></ul><h2 id="查找并替换"><a href="#查找并替换" class="headerlink" title="查找并替换"></a>查找并替换</h2><p>在末行模式下使用 s 命令（用法和 sed 中的 s 相同）</p><h3 id="分组替换"><a href="#分组替换" class="headerlink" title="分组替换"></a>分组替换</h3><p>如将 <code>&lt;h1&gt;Hello&lt;/h1&gt;</code> 替换为 <code>&lt;span&gt;Hello&lt;/span&gt;</code></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">s&#x2F;&lt;h1&gt;\(.*\)&lt;\&#x2F;h1&gt;&#x2F;&lt;span&gt;\1&lt;\&#x2F;span&gt;</span><br></pre></td></tr></table></figure><h2 id="分屏"><a href="#分屏" class="headerlink" title="分屏"></a>分屏</h2><ul><li><code>:vsp FILENAME</code>：垂直分屏打开某个文件</li><li><code>:sp FILENAME</code>：水平分屏打开某个文件</li></ul><p><strong>在窗口间切换光标：<code>Ctrl-w</code>，上下左右光标</strong></p><h2 id="截取文件内容"><a href="#截取文件内容" class="headerlink" title="截取文件内容"></a>截取文件内容</h2><ul><li><code>:[ADDR1,ADDR2]w [/path/to/somewhere]</code>：将文件中的ADDR1行至ADDR2行保存在另一个文件中</li></ul><h2 id="填充文件"><a href="#填充文件" class="headerlink" title="填充文件"></a>填充文件</h2><ul><li><code>:r /path/to/somewhere</code>：将另一个文件的所有内容填充至当前光标后</li></ul><br><h1 id="可视化模式"><a href="#可视化模式" class="headerlink" title="可视化模式"></a>可视化模式</h1><h2 id="逐行"><a href="#逐行" class="headerlink" title="逐行"></a>逐行</h2><ul><li><code>v</code>：按字符选取</li><li><code>V</code>：按行选取</li><li><code>viw</code>：选取当前光标的单词</li><li><code>vis/vip</code>：选取当前光标所在段落</li><li><code>gu/gU</code>：选中之后，<code>gu</code> 或者 <code>gU</code> 改变大小写</li></ul><h2 id="逐列"><a href="#逐列" class="headerlink" title="逐列"></a>逐列</h2><ul><li><code>Ctrl-v</code>：进入列选择</li></ul><br>]]></content>
      <categories>
        <category>Software</category>
      </categories>
      <tags>
        <tag>Vim</tag>
      </tags>
  </entry>
  <entry>
    <title>Windows Terminal 配置</title>
    <url>/wt/987d500f-b4d8-4e72-993b-a72b488bf768.html</url>
    <content><![CDATA[<p>本文列出 Windows Terminal 的一些常用配置。全局配置可去 <a href="https://aka.ms/terminal-global-settings" target="_blank" rel="noopener">https://aka.ms/terminal-global-settings</a> 网站查看；profiles 配置可去 <a href="https://aka.ms/terminal-profile-settings" target="_blank" rel="noopener">https://aka.ms/terminal-profile-settings</a> 网站查看。</p><a id="more"></a><figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="attr">"$schema"</span>: <span class="string">"https://aka.ms/terminal-profiles-schema"</span>,</span><br><span class="line"></span><br><span class="line">    <span class="attr">"defaultProfile"</span>: <span class="string">"&#123;2c4de342-38b7-51cf-b940-2309a097f518&#125;"</span>,</span><br><span class="line">    <span class="attr">"copyOnSelect"</span>: <span class="literal">false</span>,</span><br><span class="line">    <span class="attr">"copyFormatting"</span>: <span class="literal">false</span>,</span><br><span class="line">    <span class="attr">"tabWidthMode"</span>: <span class="string">"compact"</span>,</span><br><span class="line"></span><br><span class="line">    <span class="attr">"profiles"</span>:</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="attr">"defaults"</span>:</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="attr">"acrylicOpacity"</span>: <span class="number">1</span>,              <span class="comment">//背景透明度(0-1)</span></span><br><span class="line">            <span class="attr">"useAcrylic"</span>: <span class="literal">false</span>,                 <span class="comment">// 启用毛玻璃</span></span><br><span class="line">            <span class="comment">//"backgroundImage": "C:/Users/xxx/Pictures/xxx.jpg", //背景图片</span></span><br><span class="line">            <span class="comment">//"backgroundImageOpacity": 0.5,      //图片透明度（0-1）</span></span><br><span class="line">            <span class="comment">//"backgroundImageStretchMode": "uniformToFill", //填充模式  uniformToFill | none | fill | uniform</span></span><br><span class="line">            <span class="attr">"fontFace"</span>: <span class="string">"Consolas"</span>,       <span class="comment">//字体</span></span><br><span class="line">            <span class="attr">"fontSize"</span>: <span class="number">14</span>,                     <span class="comment">//文字大小</span></span><br><span class="line">            <span class="attr">"fontWeight"</span>: <span class="string">"thin"</span>,              <span class="comment">//文字宽度，可设置 thin | extra-light | light | semi-light | normal | medium | semi-bold | bold | extra-bold | black | extra-black</span></span><br><span class="line">            <span class="attr">"colorScheme"</span>: <span class="string">"Solarized Dark"</span>,    <span class="comment">//主题名字</span></span><br><span class="line">            <span class="attr">"cursorColor"</span>: <span class="string">"#ddd"</span>,              <span class="comment">//光标颜色</span></span><br><span class="line">            <span class="attr">"cursorShape"</span>: <span class="string">"filledBox"</span>,               <span class="comment">//光标形状 bar | vintage | underscore | filledBox | emptyBox</span></span><br><span class="line">            <span class="comment">//"startingDirectory": "D://Projects//", //起始目录</span></span><br><span class="line">            <span class="attr">"antialiasingMode"</span>: <span class="string">"cleartype"</span>,    <span class="comment">//消除文字锯 grayscale | cleartype | aliased</span></span><br><span class="line">            <span class="attr">"closeOnExit"</span>: <span class="string">"always"</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="attr">"list"</span>:</span><br><span class="line">        [</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">// Make changes here to the powershell.exe profile.</span></span><br><span class="line">                <span class="attr">"guid"</span>: <span class="string">"&#123;61c54bbd-c2c6-5271-96e7-009a87ff44bf&#125;"</span>,</span><br><span class="line">                <span class="attr">"name"</span>: <span class="string">"PowerShell"</span>,</span><br><span class="line">                <span class="attr">"commandline"</span>: <span class="string">"powershell.exe"</span>,</span><br><span class="line">                <span class="attr">"hidden"</span>: <span class="literal">false</span></span><br><span class="line">            &#125;,</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">// Make changes here to the cmd.exe profile.</span></span><br><span class="line">                <span class="attr">"guid"</span>: <span class="string">"&#123;0caa0dad-35be-5f56-a8ff-afceeeaa6101&#125;"</span>,</span><br><span class="line">                <span class="attr">"name"</span>: <span class="string">"cmd"</span>,</span><br><span class="line">                <span class="attr">"commandline"</span>: <span class="string">"cmd.exe"</span>,</span><br><span class="line">                <span class="attr">"hidden"</span>: <span class="literal">false</span></span><br><span class="line">            &#125;,</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="attr">"guid"</span>: <span class="string">"&#123;2c4de342-38b7-51cf-b940-2309a097f518&#125;"</span>,</span><br><span class="line">                <span class="attr">"hidden"</span>: <span class="literal">false</span>,</span><br><span class="line">                <span class="attr">"name"</span>: <span class="string">"Ubuntu"</span>,</span><br><span class="line">                <span class="comment">//"source": "Windows.Terminal.Wsl"</span></span><br><span class="line">                <span class="attr">"commandline"</span>: <span class="string">"ubuntu.exe"</span></span><br><span class="line">            &#125;,</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="attr">"guid"</span>: <span class="string">"&#123;b453ae62-4e3d-5e58-b989-0a998ec441b8&#125;"</span>,</span><br><span class="line">                <span class="attr">"hidden"</span>: <span class="literal">false</span>,</span><br><span class="line">                <span class="attr">"name"</span>: <span class="string">"ACS"</span>,</span><br><span class="line">                <span class="attr">"source"</span>: <span class="string">"Windows.Terminal.Azure"</span></span><br><span class="line">            &#125;</span><br><span class="line">        ]</span><br><span class="line">    &#125;,</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Add custom color schemes to this array.</span></span><br><span class="line">    <span class="comment">// To learn more about color schemes, visit https://aka.ms/terminal-color-schemes</span></span><br><span class="line">    <span class="attr">"schemes"</span>: [</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="attr">"name"</span> : <span class="string">"Solarized Dark"</span>,</span><br><span class="line">            <span class="attr">"background"</span> : <span class="string">"#002B36"</span>,</span><br><span class="line">            <span class="attr">"black"</span> : <span class="string">"#002B36"</span>,</span><br><span class="line">            <span class="attr">"blue"</span> : <span class="string">"#268BD2"</span>,</span><br><span class="line">            <span class="attr">"brightBlack"</span> : <span class="string">"#073642"</span>,</span><br><span class="line">            <span class="attr">"brightBlue"</span> : <span class="string">"#005FFF"</span>,</span><br><span class="line">            <span class="attr">"brightCyan"</span> : <span class="string">"#93A1A1"</span>,</span><br><span class="line">            <span class="attr">"brightGreen"</span> : <span class="string">"#00FF00"</span>,</span><br><span class="line">            <span class="attr">"brightPurple"</span> : <span class="string">"#BC71C4"</span>,</span><br><span class="line">            <span class="attr">"brightRed"</span> : <span class="string">"#CB4B16"</span>,</span><br><span class="line">            <span class="attr">"brightWhite"</span> : <span class="string">"#586E75"</span>,</span><br><span class="line">            <span class="attr">"brightYellow"</span> : <span class="string">"#FFFF00"</span>,</span><br><span class="line">            <span class="attr">"cyan"</span> : <span class="string">"#2AA198"</span>,</span><br><span class="line">            <span class="attr">"foreground"</span> : <span class="string">"#839496"</span>,</span><br><span class="line">            <span class="attr">"green"</span> : <span class="string">"#859900"</span>,</span><br><span class="line">            <span class="attr">"purple"</span> : <span class="string">"#D33682"</span>,</span><br><span class="line">            <span class="attr">"red"</span> : <span class="string">"#DC322F"</span>,</span><br><span class="line">            <span class="attr">"white"</span> : <span class="string">"#839496"</span>,</span><br><span class="line">            <span class="attr">"yellow"</span> : <span class="string">"#B58900"</span></span><br><span class="line">        &#125;</span><br><span class="line">    ],</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Add custom actions and keybindings to this array.</span></span><br><span class="line">    <span class="comment">// To unbind a key combination from your defaults.json, set the command to "unbound".</span></span><br><span class="line">    <span class="comment">// To learn more about actions and keybindings, visit https://aka.ms/terminal-keybindings</span></span><br><span class="line">    <span class="attr">"actions"</span>:</span><br><span class="line">    [</span><br><span class="line">        <span class="comment">// Copy and paste are bound to Ctrl+Shift+C and Ctrl+Shift+V in your defaults.json.</span></span><br><span class="line">        <span class="comment">// These two lines additionally bind them to Ctrl+C and Ctrl+V.</span></span><br><span class="line">        <span class="comment">// To learn more about selection, visit https://aka.ms/terminal-selection</span></span><br><span class="line">        <span class="comment">//&#123; "command": &#123;"action": "copy", "singleLine": false &#125;, "keys": "ctrl+c" &#125;,</span></span><br><span class="line">        <span class="comment">//&#123; "command": &#123;"action": "paste", "singleLine": false &#125;, "keys": "ctrl+v" &#125;,</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// Press Ctrl+Shift+F to open the search box</span></span><br><span class="line">        &#123; <span class="attr">"command"</span>: <span class="string">"find"</span>, <span class="attr">"keys"</span>: <span class="string">"ctrl+shift+f"</span> &#125;,</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Press Alt+Shift+D to open a new pane.</span></span><br><span class="line">        <span class="comment">// - "split": "auto" makes this pane open in the direction that provides the most surface area.</span></span><br><span class="line">        <span class="comment">// - "splitMode": "duplicate" makes the new pane use the focused pane's profile.</span></span><br><span class="line">        <span class="comment">// To learn more about panes, visit https://aka.ms/terminal-panes</span></span><br><span class="line">        &#123; <span class="attr">"command"</span>: &#123; <span class="attr">"action"</span>: <span class="string">"splitPane"</span>, <span class="attr">"split"</span>: <span class="string">"auto"</span>, <span class="attr">"splitMode"</span>: <span class="string">"duplicate"</span> &#125;, <span class="attr">"keys"</span>: <span class="string">"alt+shift+d"</span> &#125;</span><br><span class="line">    ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Software</category>
      </categories>
      <tags>
        <tag>Windows Terminal</tag>
      </tags>
  </entry>
  <entry>
    <title>Python 虚拟环境</title>
    <url>/python/f6a83fd6-85e9-4c12-9085-88c9f9da9968.html</url>
    <content><![CDATA[<p>在同一个 Python 环境中安装同一个包的不同版本，后安装的包会把原来安装的包覆盖掉。 这样，如果同一台机器上两个项目依赖于相同包的不同版本，则会导致一些项目运行失败。解决的方案就是：<strong>虚拟环境</strong>。</p><a id="more"></a><h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>虚拟环境是真实 Python 环境的复制版本。在虚拟环境中使用的 Python 是复制的 Python。安装 Python 包也是安装在复制的 Python 中。</p><h2 id="安装和配置"><a href="#安装和配置" class="headerlink" title="安装和配置"></a>安装和配置</h2><ol><li><strong>安装虚拟环境</strong><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo pip install virtualenv</span><br></pre></td></tr></table></figure></li><li><strong>安装虚拟环境扩展包</strong><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo pip install virtualenvwrapper</span><br></pre></td></tr></table></figure></li><li><strong>编辑 <code>.bashrc</code> 文件，添加下面两行</strong>：<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> WORKON_HOME=<span class="variable">$HOME</span>/.virtualenvs</span><br><span class="line"><span class="built_in">source</span> /usr/<span class="built_in">local</span>/bin/virtualenvwrapper.sh</span><br></pre></td></tr></table></figure></li></ol><h2 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h2><ul><li><strong>创建虚拟环境</strong>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mkvirtualenv 虚拟环境名</span><br></pre></td></tr></table></figure></li><li><strong>创建 Python3 虚拟环境</strong>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mkvirtualenv -p python3 test</span><br></pre></td></tr></table></figure></li><li><strong>进入虚拟环境工作</strong>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">workon 虚拟环境名</span><br></pre></td></tr></table></figure></li><li><strong>退出虚拟环境</strong>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">deactivate</span><br></pre></td></tr></table></figure></li><li><strong>删除虚拟环境</strong>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">rmvirtualenv 虚拟环境名</span><br></pre></td></tr></table></figure></li><li><strong>虚拟环境下安装包</strong>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">pip install 包名</span><br></pre></td></tr></table></figure><strong>注意</strong>：不能使用 <code>sudo pip install</code>，这个命令会把包安装到真实的主机环境上而不是安装到虚拟环境中。</li><li><strong>查看当前虚拟环境中安装了哪些 python 包</strong>：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">pip list</span><br><span class="line">pip freeze</span><br></pre></td></tr></table></figure></li></ul>]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>Selenium 的安装与使用</title>
    <url>/python/7f26abe0-3630-4a6f-942e-c93126cbdcb5.html</url>
    <content><![CDATA[<p>Selenium 是一个 Web 的自动化测试工具，最初是为网站自动化测试而开发的。Selenium 可以直接运行在浏览器上，它支持所有主流的浏览器，可以接收指令，让浏览器自动加载页面，获取需要的数据，甚至页面截屏。使用 Selenium 可以让我们加载动态的页面，也就是说我们使用 Selenium 获取到的内容就是浏览器加载完毕的内容。本文介绍 Selenium 的安装与基础使用。</p><a id="more"></a><h1 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h1><p>Windows 下进入 Python 的 <strong>Scripts</strong> 目录，运行：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">pip install selenium</span><br></pre></td></tr></table></figure><p>至此，Selenium 安装完毕，可以通过 <code>pip list</code> 命令来查看是否安装成功。</p><h1 id="浏览器驱动"><a href="#浏览器驱动" class="headerlink" title="浏览器驱动"></a>浏览器驱动</h1><p>以 Chrome 为例，去 <a href="http://npm.taobao.org/mirrors/chromedriver/" target="_blank" rel="noopener">淘宝镜像</a> 上下载驱动。</p><p>注意要与 Chrome 的版本一致，版本可以通过 <a href="chrome://version">chrome://version</a> 来查看</p><h1 id="基本使用"><a href="#基本使用" class="headerlink" title="基本使用"></a>基本使用</h1><p>代码示例如下（<strong>注意浏览器驱动的位置</strong>）：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> selenium <span class="keyword">import</span> webdriver</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建 WebDriver 对象，指明使用 chrome 浏览器驱动</span></span><br><span class="line">wd = webdriver.Chrome(<span class="string">'../driver/chromedriver.exe'</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 调用 WebDriver 对象的 get 方法 可以让浏览器打开指定网址</span></span><br><span class="line">wd.get(<span class="string">'https://www.baidu.com'</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 根据 id 查找元素</span></span><br><span class="line">element = wd.find_element_by_id(<span class="string">'kw'</span>)</span><br><span class="line"><span class="comment"># 发送字符串到元素中</span></span><br><span class="line">element.send_keys(<span class="string">"hello"</span>)</span><br><span class="line"></span><br><span class="line">element = wd.find_element_by_id(<span class="string">'su'</span>)</span><br><span class="line"><span class="comment"># 点击元素</span></span><br><span class="line">element.click()</span><br><span class="line"></span><br><span class="line"><span class="keyword">pass</span></span><br></pre></td></tr></table></figure><h1 id="常用方法"><a href="#常用方法" class="headerlink" title="常用方法"></a>常用方法</h1><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 设置浏览器的窗口</span></span><br><span class="line">driver.set_window_size(<span class="number">1920</span>,<span class="number">1680</span>)</span><br><span class="line"><span class="comment"># 最大化浏览器窗口</span></span><br><span class="line">driver.maximize_window()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 发送请求</span></span><br><span class="line">driver.get(<span class="string">"http://www.baidu.com"</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 进行页面截屏</span></span><br><span class="line">driver.save_screenshot(<span class="string">"./baidu.png"</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取html字符串</span></span><br><span class="line">print(driver.page_source)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取cookies</span></span><br><span class="line">cookies = driver.get_cookies()</span><br><span class="line">print(cookies)</span><br><span class="line">print(<span class="string">'-'</span>*<span class="number">100</span>)</span><br><span class="line">cookies = &#123;i[<span class="string">"name"</span>]:i[<span class="string">"value"</span>] <span class="keyword">for</span> i <span class="keyword">in</span> cookies&#125;</span><br><span class="line">print(cookies)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取当前url</span></span><br><span class="line">print(driver.current_url)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 退出浏览器</span></span><br><span class="line">driver.quit()</span><br></pre></td></tr></table></figure><h1 id="页面元素定位"><a href="#页面元素定位" class="headerlink" title="页面元素定位"></a>页面元素定位</h1><ul><li><strong>find_element_by_id</strong><br>该方法会根据id来查找元素，返回一个元素</li><li><strong>find_element_by_class_name</strong><br>该方法通过元素类名来获取元素，<strong>注意该方法只能传入一个class_name</strong><br>与之对应的是find_elements_by_class_name方法，返回一个列表</li><li><strong>find_element_by_xpath</strong><br>该方法可以通过xpath语法来查找元素，返回第一个找到的元素<br>该方法传入的参数就是xpath语法，但是需要<strong>注意</strong>的是:<br>该方法找到的只能是一个元素而不能是文本或者属性<br>如果想要获取元素的属性或文本需要再调用元素的get_attribute()/text方法</li><li><strong>find_elements_by_xpath</strong><br>该方法通过xpath语法来查找元素，返回一个列表，里面包含所有匹配的元素<br>如下：<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">dirver.get(<span class="string">"http://neihanduanzi.com/"</span>)</span><br><span class="line">ret = dirver.find_elements_by_xpath(<span class="string">"//ul[@id='detail-list']/li"</span>)</span><br><span class="line"><span class="keyword">for</span> li <span class="keyword">in</span> ret:</span><br><span class="line">    print(li.find_element_by_xpath(<span class="string">".//a[@class='image share_url']"</span>).get_attribute(<span class="string">"href"</span>))</span><br><span class="line">dirver.quit()</span><br></pre></td></tr></table></figure></li><li><strong>find_element_by_link_text</strong><br>该方法通过元素的文本内容来获取元素，传入的参数需和元素的文本内容一致才能匹配<br>与之对应的为find_elements_by_link_text方法，返回的是一个列表</li><li><strong>find_element_by_partial_link_text</strong><br>该方法和上面的方法一样，也是通过元素的文本内容来获取元素，但是它只需要参数和元素内容部分匹配即可<br>与之对应的为find_elements_by_partial_link_text，返回的是一个列表</li></ul><h1 id="selenium-注意事项"><a href="#selenium-注意事项" class="headerlink" title="selenium 注意事项"></a>selenium 注意事项</h1><h2 id="获取文本和获取属性"><a href="#获取文本和获取属性" class="headerlink" title="获取文本和获取属性"></a>获取文本和获取属性</h2><ul><li>先定位到元素，然后调用<code>.text</code>或者<code>get_attribute</code>方法来获取元素文本或属性</li><li>selenium获取的页面数据是浏览器中<strong>elements</strong>的内容</li><li>find_element和find_elements的区别<ul><li>find_element返回一个element，如果没有会报错</li><li>find_elements返回一个列表，没有就是空列表</li><li>在判断是否有下一页的时候，使用find_elements来根据结果的列表长度来判断</li></ul></li></ul><h2 id="内嵌-frame"><a href="#内嵌-frame" class="headerlink" title="内嵌 frame"></a>内嵌 frame</h2><p>如果页面中含有 iframe、frame，需要先调用 <code>driver.switch_to.frame</code> 的方法切换到 frame 中才能定位元素</p><h2 id="翻页"><a href="#翻页" class="headerlink" title="翻页"></a>翻页</h2><p>selenium 请求第一页的时候会<strong>等待页面加载完了之后再获取数据</strong>，但是在点击翻页之后，会直接获取数据，此时可能会报错，因为数据还没有加载出来，需要等待几秒</p>]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>Selenium</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Boot 日志</title>
    <url>/spring/016decda-d238-4c63-9b75-543f948bfe45.html</url>
    <content><![CDATA[<p>本文介绍 Spring Boot 日志。</p><a id="more"></a><h2 id="spring-logback-配置"><a href="#spring-logback-配置" class="headerlink" title="spring logback 配置"></a>spring logback 配置</h2><p>logback-spring.xml</p><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">configuration</span> <span class="attr">scan</span>=<span class="string">"true"</span> <span class="attr">scanPeriod</span>=<span class="string">"60 seconds"</span> <span class="attr">debug</span>=<span class="string">"false"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">contextName</span>&gt;</span>logback<span class="tag">&lt;/<span class="name">contextName</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">conversionRule</span> <span class="attr">conversionWord</span>=<span class="string">"clr"</span> <span class="attr">converterClass</span>=<span class="string">"org.springframework.boot.logging.logback.ColorConverter"</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">conversionRule</span> <span class="attr">conversionWord</span>=<span class="string">"wEx"</span> <span class="attr">converterClass</span>=<span class="string">"org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"CONSOLE_LOG_PATTERN"</span> <span class="attr">value</span>=<span class="string">"$&#123;CONSOLE_LOG_PATTERN:-%clr(%d&#123;$&#123;LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS&#125;&#125;)&#123;faint&#125; %clr($&#123;LOG_LEVEL_PATTERN:-%5p&#125;) %clr($&#123;PID:- &#125;)&#123;magenta&#125; %clr(---)&#123;faint&#125; %clr([%11.11t])&#123;faint&#125; %clr(%-40.40logger&#123;39&#125;)&#123;cyan&#125; %clr(:)&#123;faint&#125; %m%n$&#123;LOG_EXCEPTION_CONVERSION_WORD:-%wEx&#125;&#125;"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"FILE_LOG_PATTERN"</span> <span class="attr">value</span>=<span class="string">"$&#123;FILE_LOG_PATTERN:-%d&#123;$&#123;LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS&#125;&#125; $&#123;LOG_LEVEL_PATTERN:-%5p&#125; $&#123;PID:- &#125; --- [%11.11t] %-40.40logger&#123;39&#125; : %m%n$&#123;LOG_EXCEPTION_CONVERSION_WORD:-%wEx&#125;&#125;"</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--输出到控制台--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">appender</span> <span class="attr">name</span>=<span class="string">"CONSOLE"</span> <span class="attr">class</span>=<span class="string">"ch.qos.logback.core.ConsoleAppender"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">encoder</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">pattern</span>&gt;</span>$&#123;CONSOLE_LOG_PATTERN&#125;<span class="tag">&lt;/<span class="name">pattern</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">charset</span>&gt;</span>UTF-8<span class="tag">&lt;/<span class="name">charset</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">encoder</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">appender</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--按天生成日志--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">appender</span> <span class="attr">name</span>=<span class="string">"FILE"</span> <span class="attr">class</span>=<span class="string">"ch.qos.logback.core.rolling.RollingFileAppender"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">encoder</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">pattern</span>&gt;</span>$&#123;FILE_LOG_PATTERN&#125;<span class="tag">&lt;/<span class="name">pattern</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">encoder</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">rollingPolicy</span> <span class="attr">class</span>=<span class="string">"ch.qos.logback.core.rolling.TimeBasedRollingPolicy"</span>&gt;</span></span><br><span class="line">            <span class="comment">&lt;!--日志文件输出的文件名--&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">FileNamePattern</span>&gt;</span>logs/%d&#123;yyyy-MM-dd&#125;.log<span class="tag">&lt;/<span class="name">FileNamePattern</span>&gt;</span></span><br><span class="line">            <span class="comment">&lt;!--日志文件保留天数--&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">MaxHistory</span>&gt;</span>15<span class="tag">&lt;/<span class="name">MaxHistory</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">rollingPolicy</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">appender</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- 日志级别配置 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">root</span> <span class="attr">level</span>=<span class="string">"INFO"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">appender-ref</span> <span class="attr">ref</span>=<span class="string">"CONSOLE"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">appender-ref</span> <span class="attr">ref</span>=<span class="string">"FILE"</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">root</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">configuration</span>&gt;</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>Spring Boot</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Boot 基础</title>
    <url>/spring/e52ef0ff-ffe9-4471-815e-76727d599756.html</url>
    <content><![CDATA[<p>Spring Boot 是由 Pivotal 团队提供的全新框架，其设计目的是用来简化新 Spring 应用的初始搭建以及开发过程。该框架使用了特定的方式来进行配置，从而使开发人员不再需要定义样板化的配置。通过这种方式，Spring Boot 致力于在蓬勃发展的快速应用开发领域 (rapid application development) 成为领导者。</p><a id="more"></a><h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>Spring Boot 是 Spring 家族中的一个全新的框架，它用来简化 Spring 应用程序的创建和开发过程，也可以说 Spring Boot 能简化我们之前采用 Spring + SpringMVC + MyBatis 框架进行开发的过程。</p><p>在以往我们采用 Spring + SpringMVC + MyBatis 框架进行开发的时候，搭建和整合三大框架，我们需要做很多工作，比如配置 <code>web.xml</code>，配置 MyBatis，并将它们整合在一起等，而 Spring Boot 框架对此开发过程进行了革命性的颠覆，完全抛弃了繁琐的 xml 配置过程，采用大量的默认配置简化开发过程。</p><p>所以采用 Spring Boot 可以非常容易和快速地创建基于 Spring 框架的应用程序，它让编码、配置、部署及监控变得简单了。正因为 Spring Boot 它化繁为简，让开发变得极其简单和快速，所以在业界备受关注。</p><h3 id="Spring-Boot-特点"><a href="#Spring-Boot-特点" class="headerlink" title="Spring Boot 特点"></a>Spring Boot 特点</h3><ul><li><strong>快速构建项目，为基于 Spring 的开发提供更快的入门体验</strong></li><li><strong>对主流开发框架的无配置集成</strong></li><li><strong>项目可独立运行，无需外部依赖 Servlet 容器</strong></li><li><strong>提供了一些大型项目中常见的非功能性特性，如嵌入式服务器、安全指标、健康检测</strong></li><li><strong>极大地提高了开发、部署效率</strong></li><li><strong>与云计算的天然集成</strong></li></ul><h2 id="Spring-Initializr"><a href="#Spring-Initializr" class="headerlink" title="Spring Initializr"></a>Spring Initializr</h2><p>我们可以使用 <a href="https://start.spring.io/" target="_blank" rel="noopener"><strong>Spring 官网</strong></a> 上自带的模版（Spring Initializr）来创建 Spring Boot 应用程序，也可以使用 IDEA 上的 <strong>Spring Initializr</strong> 来创建，这里以 <strong>IDEA 2019.3.5（不同版本的 IDEA 可能会有部分差异，请以自己的 IDEA 为准）</strong> 为例。</p><p><img src="https://wx2.sbimg.cn/2020/09/16/GmNNo.png" alt=""></p><p><img src="https://wx1.sbimg.cn/2020/09/16/GmyBJ.png" alt=""></p><p><img src="https://pic.downk.cc/item/5f622173160a154a678e00a3.jpg" alt=""></p><p><img src="https://pic.downk.cc/item/5f6221bc160a154a678e176f.jpg" alt=""></p><p>这种方法需要从 Spring 官网上下载模版包，需要网络，同时国内访问外网比较慢，因此这种方式不推荐使用。</p><h2 id="自行创建工程"><a href="#自行创建工程" class="headerlink" title="自行创建工程"></a>自行创建工程</h2><h3 id="Maven-目录结构"><a href="#Maven-目录结构" class="headerlink" title="Maven 目录结构"></a>Maven 目录结构</h3><p>首先我们应该创建 Maven 目录结构，如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">├─src</span><br><span class="line">│  ├─main</span><br><span class="line">│  │  ├─java</span><br><span class="line">│  │  └─resources</span><br><span class="line">│  └─test</span><br><span class="line">│      └─java</span><br><span class="line">└─pom.xml</span><br></pre></td></tr></table></figure><h3 id="pom-xml-文件"><a href="#pom-xml-文件" class="headerlink" title="pom.xml 文件"></a><code>pom.xml</code> 文件</h3><p>然后我们应该创建 <code>pom.xml</code> 文件，如下所示：</p><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">"http://maven.apache.org/POM/4.0.0"</span> <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>my.projects<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>springboot-demo<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>0.0.1-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>springboot-demo<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">properties</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 环境配置 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">project.build.sourceEncoding</span>&gt;</span>UTF-8<span class="tag">&lt;/<span class="name">project.build.sourceEncoding</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">project.reporting.outputEncoding</span>&gt;</span>UTF-8<span class="tag">&lt;/<span class="name">project.reporting.outputEncoding</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">java.version</span>&gt;</span>1.8<span class="tag">&lt;/<span class="name">java.version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">properties</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="Spring-Boot-依赖"><a href="#Spring-Boot-依赖" class="headerlink" title="Spring Boot 依赖"></a>Spring Boot 依赖</h3><p>在 <code>pom.xml</code> 文件中加入 Spring Boot 的父依赖（这里以 Spring Boot 2.2.9 为例），如下：</p><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.2.9.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br></pre></td></tr></table></figure><p>由于我们要基于 Spring MVC 的 Controller 进行开发，所以项目要导入 <strong>web 的起步依赖</strong>：</p><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure><p>如果我们要使用 Maven 对 Spring Boot 应用程序进行打包的话，还需要引入一个打包插件，如下：</p><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">build</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">plugins</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-maven-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">plugins</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">build</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="引导类"><a href="#引导类" class="headerlink" title="引导类"></a>引导类</h3><p>我们需要在我们的包下面（与我们的包平级目录）创建一个 <strong>Spring Bood 引导类</strong>，如下：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">App</span> </span>&#123;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SpringApplication.run(App<span class="class">.<span class="keyword">class</span>, <span class="title">args</span>)</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="Controller"><a href="#Controller" class="headerlink" title="Controller"></a>Controller</h3><p>在引导类的目录下创建一个 <code>controller</code> 的包，并编写一个 Controller：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyController</span> </span>&#123;</span><br><span class="line">    <span class="meta">@RequestMapping</span>(<span class="string">""</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">quick</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"Hello Spring Boot!"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后启动 <code>App.main</code> 方法，访问 <code>http://localhost:8080</code>，可以看到我们写的 <code>Hello Spring Boot!</code>，如下：</p><p><img src="https://pic.downk.cc/item/5f622782160a154a678fcb04.jpg" alt=""></p><h2 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h2><p>Spring Boot 是约定优于配置的，所以其中很多配置都是有默认值的。但如果想使用自己的配置替换默认配置的话，就可以使用 <code>application.properties</code> 或 <code>application.yml</code> 进行配置 (推荐使用 <code>application.yml</code>)</p><p>YML 文件是 YAML (YAML Aint Markup Language) 文件格式，YAML 是一种直观的能够被电脑识别的数据序列化格式，并且容易被人类阅读，容易和脚本语言交互的，可以被支持 YAML 库的不同的编程语言程序导入，比如： C/C++, Ruby, Python, Java, Perl, C#, PHP 等。YML 文件是以数据为核心的，比传统的 xml 文件更加简洁。</p><p>Spring Boot 所有默认配置在 <a href="https://docs.spring.io/spring-boot/docs/2.2.9.RELEASE/reference/htmlsingle/#common-application-properties" target="_blank" rel="noopener"><strong>Spring 官方文档中</strong></a> 列出。<strong>注意</strong>：官方为了方便，使用 properties 的格式列出所有配置，我们可以将其改为 yaml 格式。</p><p>如 Spring 应用名、 <code>port</code> 和 <code>context-path</code> 的配置：</p><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">application:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">hello-spring</span></span><br><span class="line"></span><br><span class="line"><span class="attr">server:</span></span><br><span class="line">  <span class="attr">port:</span> <span class="number">80</span></span><br><span class="line">  <span class="attr">servlet:</span></span><br><span class="line">    <span class="attr">context-path:</span> <span class="string">/demo</span></span><br></pre></td></tr></table></figure><h3 id="自定义配置"><a href="#自定义配置" class="headerlink" title="自定义配置"></a>自定义配置</h3><p>我们可以自定义配置，如下：</p><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">person:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">zhangsan</span></span><br><span class="line">  <span class="attr">age:</span> <span class="number">18</span></span><br></pre></td></tr></table></figure><p>然后我们可以使用 <code>@Value</code> 注解来读取配置：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyController</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Value</span>(<span class="string">"$&#123;person.name&#125;"</span>)</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="meta">@Value</span>(<span class="string">"$&#123;person.age&#125;"</span>)</span><br><span class="line">    <span class="keyword">private</span> Integer age;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>也可以使用 <code>@ConfigurationProperties</code> 注解来将配置注入到 Bean 中：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="meta">@ConfigurationProperties</span>(prefix = <span class="string">"person"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyClass</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> Integer age;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Integer <span class="title">getAge</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setAge</span><span class="params">(Integer age)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>注</strong>：使用 <code>@ConfigurationProperties</code> 注解时，IDEA 可能会报警告，如需解除警告需要在 <code>pom.xml</code> 中添加如下依赖：</p><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-configuration-processor<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">optional</span>&gt;</span>true<span class="tag">&lt;/<span class="name">optional</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure><h2 id="banner"><a href="#banner" class="headerlink" title="banner"></a>banner</h2><p>在 Spring Boot 启动的时候会有一个默认的启动图案：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">  .   ____          _            __ _ _</span><br><span class="line"> &#x2F;\\ &#x2F; ___&#39;_ __ _ _(_)_ __  __ _ \ \ \ \</span><br><span class="line">( ( )\___ | &#39;_ | &#39;_| | &#39;_ \&#x2F; _&#96; | \ \ \ \</span><br><span class="line"> \\&#x2F;  ___)| |_)| | | | | || (_| |  ) ) ) )</span><br><span class="line">  &#39;  |____| .__|_| |_|_| |_\__, | &#x2F; &#x2F; &#x2F; &#x2F;</span><br><span class="line"> &#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;|_|&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;|___&#x2F;&#x3D;&#x2F;_&#x2F;_&#x2F;_&#x2F;</span><br><span class="line"> :: Spring Boot ::        (v2.2.9.RELEASE)</span><br></pre></td></tr></table></figure><p>我们可以更改这个启动图案，在 <code>src/main/resources</code> 目录下建立一个 <code>banner.txt</code> 的文件，如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;                          _ooOoo_                            &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;                         o8888888o                           &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;                         88&quot; . &quot;88                           &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;                         (| -_- |)                           &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;                         O\  &#x3D;  &#x2F;O                           &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;                      ____&#x2F;&#96;---&#39;\____                        &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;                    .&#39;  \\|     |&#x2F;&#x2F;  &#39;.                      &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;                   &#x2F;  \\|||  :  |||&#x2F;&#x2F;  \                     &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;                  &#x2F;  _||||| -:- |||||_  \                    &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;                  |   | \\\  -  &#x2F;&#x2F;&#x2F; |   |                    &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;                  | \_|  &#39;&#39;\---&#x2F;&#39;&#39;  |_&#x2F; |                    &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;                  \  .-\__  &#96;-&#96;  ___&#x2F;-. &#x2F;                    &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;                ___&#96;. .&#39;  &#x2F;--.--\  &#96;. . ___                  &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;              .&quot;&quot; &#39;&lt;  &#96;.___\_&lt;|&gt;_&#x2F;___.&#39;  &gt;&#39;&quot;&quot;.               &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;            | | :  &#96;- \&#96;.;&#96;\ _ &#x2F;&#96;;.&#96;&#x2F; - &#96; : | |              &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;            \  \ &#96;-.   \_ __\ &#x2F;__ _&#x2F;   .-&#96; &#x2F;  &#x2F;              &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;      &#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#96;-.____&#96;-.___\_____&#x2F;___.-&#96;____.-&#39;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;      &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;                                                             &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;       ...............................................       &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;                                                             &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;              佛祖镇楼                  BUG 辟易              &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;       _______________________________________________       &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;                                                             &#x2F;&#x2F;</span><br><span class="line">&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;&#x2F;</span><br><span class="line">         $&#123;AnsiColor.CYAN&#125;</span><br><span class="line">         :: Spring Boot ::             $&#123;spring-boot.formatted-version&#125;</span><br></pre></td></tr></table></figure><p>其中可以使用如下属性：</p><ul><li><strong><code>${AnsiColor.RED}</code></strong>：设置控制台中输出内容的颜色为红色</li><li><strong><code>${application.version}</code></strong>：用来获取项目的版本号</li><li><strong><code>${application.formatted-version}</code></strong>：格式化后的 <code>${application.version}</code> 版本信息</li><li><strong><code>${spring-boot.version}</code></strong>：Spring Boot 的版本号</li><li><strong><code>${spring-boot.formatted-version}</code></strong>：格式化后的 <code>${spring-boot.version}</code> 版本信息</li></ul><br>]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>Spring Boot</tag>
      </tags>
  </entry>
  <entry>
    <title>Python 爬虫</title>
    <url>/python/2eb6c6ca-e6ee-40b2-8aab-df84770e2b79.html</url>
    <content><![CDATA[<p>网络爬虫（又称为网页蜘蛛，网络机器人，在FOAF社区中间，更经常的称为网页追逐者），是一种按照一定的规则，自动地抓取万维网信息的程序或者脚本。另外一些不常使用的名字还有蚂蚁、自动索引、模拟程序或者蠕虫。</p><a id="more"></a><h2 id="爬虫基本概念"><a href="#爬虫基本概念" class="headerlink" title="爬虫基本概念"></a>爬虫基本概念</h2><p>网络爬虫(又被称之为网页蜘蛛，网络机器人)，就是<strong>模拟客户端发送网络请求，接收请求响应，按照一定的规则自动地抓取互联网信息的程序</strong>。<br>一般来说，<strong>只要是浏览器能做的事情，爬虫都能做</strong>。</p><h3 id="爬虫的流程"><a href="#爬虫的流程" class="headerlink" title="爬虫的流程"></a>爬虫的流程</h3><ul><li>url—&gt;发送请求，获取响应—&gt;提取数据—&gt;保存</li><li>发送请求，获取响应—&gt;提取url</li></ul><h2 id="Requests-模块"><a href="#Requests-模块" class="headerlink" title="Requests 模块"></a>Requests 模块</h2><p>该模块作用：发送网络请求，返回响应数据</p><p><a href="http://docs.python-requests.org/zh_CN/latest/index.html" target="_blank" rel="noopener">中文文档</a></p><h3 id="Requests-基本操作"><a href="#Requests-基本操作" class="headerlink" title="Requests 基本操作"></a>Requests 基本操作</h3><p>要使用Requests模块，首先需要导入Requests:<br><code>import Requests</code><br>然后就可以获取某个网页，如用get方法获取百度网：<br><code>response = requests.get(&quot;https://www.baidu.com&quot;)</code><br>现在，我们有一个名为response的Response对象。我们可以从这个对象中获取所有我们想要的信息。</p><p>response常用属性</p><ul><li><strong>response.text</strong><br>获取网页HTML源码(str类型)<br>解码类型：根据HTTP头部对响应的编码作出有根据的推测，推测的文本编码<br>可以修改编码方式：<code>response.encoding=&quot;utf8&quot;</code></li><li><strong>response.content</strong><br>获取网页数据(byte类型)<br>可以使用decode方法进行解码：<code>response.content.decode(&quot;utf8&quot;)</code></li><li><strong>response.status_code</strong><br>获取响应状态码</li><li><strong>response.request.headers</strong><br>获取请求头</li><li><strong>response.headers</strong><br>获取响应头</li></ul><h3 id="发送带-header-的请求"><a href="#发送带-header-的请求" class="headerlink" title="发送带 header 的请求"></a>发送带 header 的请求</h3><p>作用：模拟浏览器，欺骗服务器，获取和浏览器一致的内容<br>header形式：字典<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">headers = &#123;</span><br><span class="line">    <span class="string">"User-Agent"</span>:<span class="string">"Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/54.0.2840.99 Safari/537.36"</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>用法：<code>request.get(url, headers=HEADERS)</code></p><h3 id="发送带参数的请求"><a href="#发送带参数的请求" class="headerlink" title="发送带参数的请求"></a>发送带参数的请求</h3><p>参数就是url后面”?”之后所带的内容，参数的形式也是字典<br>如：<code>kw = {&#39;wd&#39;:&#39;python&#39;}</code><br>用法：<code>request.get(url,params=PARAMS)</code></p><p>案例如下：爬取任意贴吧，保存网页到本地</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#-*- encoding: utf-8 -*- </span></span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TiebaSpider</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, name)</span>:</span></span><br><span class="line">        self.tieba_name = name</span><br><span class="line">        self.url_temp = <span class="string">"https://tieba.baidu.com/f?kw="</span> + self.tieba_name + <span class="string">"&amp;ie=utf-8&amp;pn=&#123;&#125;"</span></span><br><span class="line">        self.headers = &#123;</span><br><span class="line">            <span class="string">"User_Agent"</span>:<span class="string">"Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/71.0.3578.98 Safari/537.36"</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">get_url_list</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> [self.url_temp.format(i*<span class="number">50</span>) <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">10</span>)]</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse_url</span><span class="params">(self, url)</span>:</span></span><br><span class="line">        print(url)</span><br><span class="line">        response = requests.get(url, headers=self.headers)</span><br><span class="line">        <span class="keyword">return</span> response.content.decode()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">save_html</span><span class="params">(self, html_str, page_num)</span>:</span></span><br><span class="line">        file_path = <span class="string">"&#123;&#125;_第&#123;&#125;页.html"</span>.format(self.tieba_name, page_num)</span><br><span class="line">        <span class="keyword">with</span> open(file_path, <span class="string">"w"</span>, encoding=<span class="string">'utf-8'</span>) <span class="keyword">as</span> f:</span><br><span class="line">            f.write(html_str)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="comment"># 1.构造URL列表</span></span><br><span class="line">        url_list = self.get_url_list()</span><br><span class="line">        <span class="comment"># 2.遍历，发送请求，获取响应</span></span><br><span class="line">        <span class="keyword">for</span> url <span class="keyword">in</span> url_list:</span><br><span class="line">            html_str = self.parse_url(url)</span><br><span class="line">            <span class="comment"># 3.保存</span></span><br><span class="line">            page_num = url_list.index(url) + <span class="number">1</span></span><br><span class="line">            self.save_html(html_str, page_num)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    tieba_spider = TiebaSpider(<span class="string">"李毅"</span>)</span><br><span class="line">    tieba_spider.run()</span><br></pre></td></tr></table></figure><h3 id="发送-POST-请求"><a href="#发送-POST-请求" class="headerlink" title="发送 POST 请求"></a>发送 POST 请求</h3><p>当我们需要登录或者注册的时候，这我们就要发送POST请求<br>爬虫也同样可以模拟浏览器发送POST请求<br>发送POST请求需要一个参数data，该参数为字典类型<br>requests类中的post方法可以发送POST请求，用法如下：<br><code>requests.post(URL, data=DATA)</code></p><p>案例：爬取有道翻译：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#-*- encoding: utf-8 -*- </span></span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"><span class="keyword">import</span> sys</span><br><span class="line"><span class="keyword">import</span> re</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Translate</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, string)</span>:</span></span><br><span class="line">        self.url = <span class="string">"http://m.youdao.com/translate"</span></span><br><span class="line">        self.string = string</span><br><span class="line">        self.headers = &#123;</span><br><span class="line">            <span class="string">"User-Agent"</span>: <span class="string">"Mozilla/5.0 (iPhone; CPU iPhone OS 10_3_1 like Mac OS X) AppleWebKit/603.1.30 (KHTML, like Gecko) Version/10.0 Mobile/14E304 Safari/602.1"</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">send_post</span><span class="params">(self)</span>:</span></span><br><span class="line">        post_data = &#123;</span><br><span class="line">            <span class="string">'inputtext'</span>:self.string,</span><br><span class="line">            <span class="string">'type'</span>:<span class="string">'AUTO'</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> requests.post(self.url, data=post_data, headers=self.headers)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            <span class="keyword">try</span>:</span><br><span class="line">                response = self.send_post()</span><br><span class="line">                ret = re.search(<span class="string">r'&lt;ul id="translateResult"&gt;\s+&lt;li&gt;(.*)&lt;/li&gt;'</span>, response.content.decode()).group(<span class="number">1</span>)</span><br><span class="line">            <span class="keyword">except</span> AttributeError:</span><br><span class="line">                <span class="keyword">pass</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                <span class="keyword">break</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> ret</span><br><span class="line">  </span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    fanyi = Translate(sys.argv[<span class="number">1</span>])</span><br><span class="line">    print(fanyi.run())</span><br></pre></td></tr></table></figure><h3 id="使用代理"><a href="#使用代理" class="headerlink" title="使用代理"></a>使用代理</h3><p>我们使用代理的目的是为了让服务器以为不是同一个客户在请求，也可以防止我们的真实IP被泄露<br>使用代理需要一个参数proxies，该参数为字典类型，如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">proxies = &#123;</span><br><span class="line">    <span class="string">'http'</span>:<span class="string">"http://12.34.56.78:9000"</span>,</span><br><span class="line">    <span class="string">'https'</span>:<span class="string">'https://12.34.56.78:9000'</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>用法如下：<br><code>requests.get(url, proxies=PROXIES)</code></p><p>我们使用代理的时候可以准备一堆的IP地址，组成IP池，要用的时候随机选择一个IP来用<br>在使用代理的时候，有些代理可以速度很慢，这时候需要检查IP地址的可用性：<br><strong>可以在使用requests的方法的时候添加超时参数，判断IP地址的质量</strong><br><strong>可以在线检测IP质量</strong></p><h3 id="Cookies-和-Session"><a href="#Cookies-和-Session" class="headerlink" title="Cookies 和 Session"></a>Cookies 和 Session</h3><p>Cookies和Session的区别：</p><blockquote><p>cookie数据存放在<strong>客户的浏览器</strong>上，session数据放在<strong>服务器</strong>上。<br>cookie不是很安全，别人可以分析存放在本地的cookie并进行cookie欺骗。<br>session会在一定时间内保存在服务器上。当访问增多，会比较占用你服务器的性能。<br>单个cookie保存的数据不能超过4K，很多浏览器都限制一个站点最多保存20个cookie。</p></blockquote><p>requests 提供了一个叫做session类，来实现客户端和服务端的会话保持<br>使用方法：先实例化一个session对象，然后让session发送get或者post请求<br>session中的get和post方法的使用方式和requests中的get和post是一样的<br>如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">session = requests.session()</span><br><span class="line">response = session.get(url,headers)</span><br></pre></td></tr></table></figure><p>如果要使用session请求登陆后的页面，<strong>需要发送两次请求</strong>：</p><blockquote><p>先使用session发送请求，登录对网站，把cookie保存在session中<br>再使用session请求登陆之后才能访问的网站，session能够自动的携带登录成功时保存在其中的cookie，进行请求</p></blockquote><ul><li><strong>Cookie的处理</strong><br>当我们得到一串cookies的时候，我们需要将cookies以列表的方式存储<br>这时候需要用到列表推导式：<br>如下：<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">cookies = <span class="string">"""anonymid=j3jxk555-nrn0wh; _r01_=1;</span></span><br><span class="line"><span class="string">    _ga=GA1.2.1274811859.1497951251;</span></span><br><span class="line"><span class="string">    _de=BF09EE3A28DED52E6B65F6A4705D973F1383380866D39FF5;</span></span><br><span class="line"><span class="string">    ln_uact=mr_mao_hacker@163.com; depovince=BJ;</span></span><br><span class="line"><span class="string">    jebecookies=54f5d0fd-9299-4bb4-801c-eefa4fd3012b|||||;</span></span><br><span class="line"><span class="string">    JSESSIONID=abcI6TfWH4N4t_aWJnvdw;</span></span><br><span class="line"><span class="string">    ick_login=4be198ce-1f9c-4eab-971d-48abfda70a50;</span></span><br><span class="line"><span class="string">    p=0cbee3304bce1ede82a56e901916d0949; first_login_flag=1;</span></span><br><span class="line"><span class="string">    ln_hurl=http://hdn.xnimg.cn/photos/hdn421/20171230/1635/main_JQzq_ae7b0000a8791986.jpg;</span></span><br><span class="line"><span class="string">    t=79bdd322e760beae79c0b511b8c92a6b9;</span></span><br><span class="line"><span class="string">    societyguester=79bdd322e760beae79c0b511b8c92a6b9; id=327550029;</span></span><br><span class="line"><span class="string">    xnsid=2ac9a5d8; loginfrom=syshome; ch_id=10016; wp_fold=0"""</span></span><br><span class="line">cookies = &#123;i.split(<span class="string">"="</span>)[<span class="number">0</span>]:i.split(<span class="string">"="</span>)[<span class="number">1</span>] <span class="keyword">for</span> i <span class="keyword">in</span> cookies.split(<span class="string">"; "</span>)&#125;</span><br></pre></td></tr></table></figure>在requests模块中的get和post方法中可以添加cookies，也可以在headers中添加cookies，如下：<br><code>get(url, headers=HEADERS, cookies=COOKIES)</code></li></ul><h3 id="requests-模块的小技巧"><a href="#requests-模块的小技巧" class="headerlink" title="requests 模块的小技巧"></a>requests 模块的小技巧</h3><ul><li><p><strong>cookies对象和字典的转换</strong><br>我们可以在请求的响应中拿到cookies，这是一个对象，我们可以将它转化为一个字典，或者将一个字典转化为一个cookies对象<br>如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">requests.utils.dict_from_cookiejar(COOKIES)</span><br><span class="line">requests.utils.cookiejar_from_dict(DICT)</span><br></pre></td></tr></table></figure></li><li><p><strong>URL地址的编码和解码</strong></p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">requests.utils.unquote(URL)  <span class="comment"># URL的解码</span></span><br><span class="line">requests.utils.quote(URL)    <span class="comment"># URL的编码</span></span><br></pre></td></tr></table></figure></li><li><p><strong>SSL证书的验证</strong><br>有的时候我们请求https协议的网址的时候，如果该网站的SSL证书有问题，程序就会报一个SSLError的异常，此时我们就需要取消SSL证书的认证<br>用法如下：<br><code>resquests.get(URL, verify=False)</code><br>这样使用的时候会有一个警告，说明该链接不安全，但是并不影响程序的运行</p></li><li><p><strong>设置超时参数</strong><br>我们可以在方法中添加一个超时参数，用法如下：<br><code>requests.get(URL, timeout=NUM)</code></p></li></ul><h3 id="自制请求模块"><a href="#自制请求模块" class="headerlink" title="自制请求模块"></a>自制请求模块</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#-*- encoding: utf-8 -*- </span></span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> retrying <span class="keyword">import</span> retry</span><br><span class="line"></span><br><span class="line">headers = &#123; </span><br><span class="line">    <span class="string">'User-Agent'</span>:</span><br><span class="line">    <span class="string">'Mozilla/5.0 (Macintosh; Intel Mac OS X 10_14_2) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/71.0.3578.98 Safari/537.36'</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@retry(stop_max_attempt_number=3)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">_parse_url</span><span class="params">(url, method, data, proxies)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> method = <span class="string">"POST"</span>:</span><br><span class="line">        response = requests.post(url, data=data, headers=headers, proxies=proxies)</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        response = requests.get(url, headers=headers, timeout=<span class="number">3</span>, proxies=proxies)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">assert</span> response.status_code == <span class="number">200</span> </span><br><span class="line">    <span class="keyword">return</span> response.content.decode()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">parse_url</span><span class="params">(url, method=<span class="string">"GET"</span>, data=None, proxies=&#123;&#125;)</span>:</span></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        html_str = _parse_url(url, method, data, proxies)</span><br><span class="line">    <span class="keyword">except</span>:</span><br><span class="line">        html_str = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> html_str</span><br></pre></td></tr></table></figure><h2 id="数据提取"><a href="#数据提取" class="headerlink" title="数据提取"></a>数据提取</h2><p>简单来说，数据提取就是<strong>从响应中获取我们想要的数据</strong>的过程<br>数据通常分为两类：<strong>非结构化数据和结构化数据</strong></p><ul><li><p><strong>非结构化数据</strong><br>如：HTML，通常用正则表达式和xpath来处理</p></li><li><p><strong>结构化数据</strong><br>如：json、xml等，通常转化为Python数据类型来处理</p></li></ul><h3 id="JSON"><a href="#JSON" class="headerlink" title="JSON"></a>JSON</h3><blockquote><p>JSON(JavaScript Object Notation) 是一种轻量级的数据交换格式，它使得人们很容易的进行阅读和编写<br>同时也方便了机器进行解析和生成<br>适用于进行数据交互的场景，比如网站前台与后台之间的数据交互。</p></blockquote><p>由于把json数据转化为python内建数据类型很简单，所以爬虫中，如果我们能够找到返回json数据的URL，就会尽量使用这种URL</p><p>在python中有个模块专门用于对json字符串进行处理，在使用的时候需要导入json模块：<br><code>import json</code></p><ul><li><p><strong>loads</strong><br>这个方法用于将json字符串转换为python数据类型<br>用法如下：<br><code>json.loads(JSON_str)</code><br>返回值是Python数据类型(列表或者字典)</p></li><li><p><strong>dumps</strong><br>这个方法用于将Python数据类型转换为json字符串<br>用法如下：<br><code>json.dumps(PYTHON_TYPE, ensure_ascii=True, indent=NUM)</code><br>该方法有2个比较常用的参数:</p><ul><li><strong>ensure_ascii</strong><br>指定是否保存为ascii格式，默认为True，该为False则会以utf-8的形式保存</li><li><strong>indent</strong><br>保存时用较为美观的样式，该值为缩进值，通常指定为2或4</li></ul></li><li><p><strong>load</strong><br>该方法用于将包含json的类文件对象中的数据提取为Python数据类型<br>类文件对象：</p><blockquote><p>具有read()或者write()方法的对象就是类文件对象<br>如f = open(“a.txt”,”r”) f就是类文件对象</p></blockquote><p>用法和loads相同<br>如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用json.load提取类文件对象中的数据</span></span><br><span class="line"><span class="keyword">with</span> open(<span class="string">"test.json"</span>,<span class="string">"r"</span>,encoding=<span class="string">"utf-8"</span>) <span class="keyword">as</span> f:</span><br><span class="line">    ret = json.load(f)</span><br></pre></td></tr></table></figure></li></ul><ul><li><strong>dump</strong><br>该方法用于将Python数据类型转化为类文件对象，用法和dumps类似，需要多接收一个要转换的python类型变量<br>如下：<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># json.dump能够把python类型放入类文件对象中</span></span><br><span class="line"><span class="keyword">with</span> open(<span class="string">"test.json"</span>,<span class="string">"w"</span>,encoding=<span class="string">"utf-8"</span>) <span class="keyword">as</span> f:</span><br><span class="line">    json.dump(ret,f,ensure_ascii=<span class="literal">False</span>,indent=<span class="number">2</span>)</span><br></pre></td></tr></table></figure></li></ul><p><strong>注意</strong>：</p><blockquote><p>json中的字符串都是双引号引起来的，如果不是双引号，则需要对其进行转换，如：</p><ul><li>eval：能实现简单的字符串和python类型的转化</li><li>replace：把单引号替换为双引号</li></ul><p>如果要往一个文件中写入多个json串，则其不再是一个json串，不能直接读取，我们可以一行写一个json串，按照行来读取</p></blockquote><p>爬取豆瓣如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#-*- encoding: utf-8 -*- </span></span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"><span class="keyword">import</span> json</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DouBanSpider</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.url_temp_list = [</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="string">"url_temp"</span>: <span class="string">"https://m.douban.com/filter_tv_american_hot/items?start=&#123;&#125;&amp;count=18&amp;loc_id=108288"</span>,</span><br><span class="line">                <span class="string">"country"</span>: <span class="string">"US"</span></span><br><span class="line">            &#125;,</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="string">"url_temp"</span>: <span class="string">"https://m.douban.com/filter_tv_english_hot/items?start=&#123;&#125;&amp;count=18&amp;loc_id=108288"</span>,</span><br><span class="line">                <span class="string">"country"</span>: <span class="string">"UK"</span></span><br><span class="line">            &#125;,</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="string">"url_temp"</span>: <span class="string">"https://m.douban.com/filter_tv_domestic_hot/items?start=&#123;&#125;&amp;count=18&amp;loc_id=108288"</span>,</span><br><span class="line">                <span class="string">"country"</span>: <span class="string">"CN"</span></span><br><span class="line">            &#125;</span><br><span class="line">        ]</span><br><span class="line">        self.headers = &#123;</span><br><span class="line">            <span class="string">'User-Agent'</span>:</span><br><span class="line">            <span class="string">'Mozilla/5.0 (Macintosh; Intel Mac OS X 10_14_2) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/71.0.3578.98 Safari/537.36'</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse_url</span><span class="params">(self, url)</span>:</span>  <span class="comment"># 发送请求，获取响应</span></span><br><span class="line">        print(url)</span><br><span class="line">        response = requests.get(url, headers=self.headers)</span><br><span class="line">        <span class="keyword">return</span> response.content.decode()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">get_content_list</span><span class="params">(self, json_str)</span>:</span>  <span class="comment"># 提取请求数据</span></span><br><span class="line">        dict_ret = json.loads(json_str)</span><br><span class="line">        content_list = dict_ret[<span class="string">"subject_collection_items"</span>]</span><br><span class="line">        <span class="keyword">return</span> content_list</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">save_content_list</span><span class="params">(self, content_list, country)</span>:</span>  <span class="comment"># 保存数据</span></span><br><span class="line">        <span class="keyword">with</span> open(<span class="string">"test.txt"</span>, <span class="string">'a'</span>, encoding=<span class="string">'utf-8'</span>) <span class="keyword">as</span> f:</span><br><span class="line">            <span class="keyword">for</span> content <span class="keyword">in</span> content_list:</span><br><span class="line">                content[<span class="string">'country'</span>] = country</span><br><span class="line">                f.write(json.dumps(content, ensure_ascii=<span class="literal">False</span>))</span><br><span class="line">                f.write(<span class="string">'\n'</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">for</span> url_temp <span class="keyword">in</span> self.url_temp_list:</span><br><span class="line">            num = <span class="number">0</span></span><br><span class="line">            <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">                <span class="comment"># 1.请求起始页面</span></span><br><span class="line">                url = url_temp[<span class="string">'url_temp'</span>].format(num)</span><br><span class="line">                <span class="comment"># 2. 发送请求，获取响应</span></span><br><span class="line">                json_str = self.parse_url(url)</span><br><span class="line">                <span class="comment"># 3.提取数据</span></span><br><span class="line">                content_list = self.get_content_list(json_str)</span><br><span class="line"></span><br><span class="line">                <span class="comment"># 4.保存</span></span><br><span class="line">                self.save_content_list(content_list, url_temp[<span class="string">'country'</span>])</span><br><span class="line">                <span class="keyword">if</span> len(content_list) &lt; <span class="number">18</span>:</span><br><span class="line">                    <span class="keyword">break</span></span><br><span class="line"></span><br><span class="line">                <span class="comment"># 5.构造下一页url地址</span></span><br><span class="line">                num += <span class="number">18</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    douban = DouBanSpider()</span><br><span class="line">    douban.run()</span><br></pre></td></tr></table></figure><h3 id="正则表达式"><a href="#正则表达式" class="headerlink" title="正则表达式"></a>正则表达式</h3><blockquote><p>正则表达式就是用事先定义好的一些特定字符、及这些特定字符的组合，组成一个”规则字符串”<br>这个<strong>规则字符串</strong>用来表达对字符串的一种<strong>过滤</strong>逻辑。</p></blockquote><p>常用的正则表达式的方法：<br>编译：re.compile()<br>从头匹配：pattern.match()<br>找一个：pattern.search()<br>找所有(<strong>常用</strong>)：pattern.findall()<br>替换：pattern.sub()</p><p>使用正则表达式的注意点：</p><blockquote><p><code>.</code>默认不能匹配<code>\n</code>，但是在匹配的时候加上<code>re.S</code>或者<code>re.DOTALL</code>就可以匹配<br><code>re.findall</code>方法中，如果模式没有分组则匹配模式，如果有分组则匹配分组内容</p></blockquote><p>内涵段子爬虫：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#-*- encoding: utf-8 -*- </span></span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"><span class="keyword">import</span> re</span><br><span class="line"><span class="keyword">import</span> json</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Neihan</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.start_url = <span class="string">'http://neihanshequ.com/'</span></span><br><span class="line">        self.next_url_temp = <span class="string">'http://neihanshequ.com/joke?is_json=1&amp;app_name=neihanshequ_web&amp;max_time=&#123;&#125;'</span></span><br><span class="line">        self.headers = &#123;</span><br><span class="line">            <span class="string">"User-Agent"</span>:</span><br><span class="line">            <span class="string">"Mozilla/5.0 (Macintosh; Intel Mac OS X 10_14_2) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/71.0.3578.98 Safari/537.36"</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse_url</span><span class="params">(self, url)</span>:</span></span><br><span class="line">        print(url)</span><br><span class="line">        response = requests.get(url, headers=self.headers)</span><br><span class="line">        <span class="keyword">return</span> response.content.decode()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">get_first_page_content_list</span><span class="params">(self, html_str)</span>:</span></span><br><span class="line">        content_list = re.findall(<span class="string">r'&lt;h1 class="title"&gt;.*?&lt;p&gt;(.*?)&lt;/p&gt;'</span>, html_str, re.S)</span><br><span class="line">        max_time = re.findall(<span class="string">"max_time: '(.*?)',"</span>, html_str)[<span class="number">0</span>]</span><br><span class="line">        <span class="keyword">return</span> content_list,max_time</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">save_content_list</span><span class="params">(self, content_list)</span>:</span></span><br><span class="line">        <span class="keyword">with</span> open(<span class="string">'neihan.txt'</span>, <span class="string">'a'</span>, encoding=<span class="string">'utf-8'</span>) <span class="keyword">as</span> f:</span><br><span class="line">            <span class="keyword">for</span> content <span class="keyword">in</span> content_list:</span><br><span class="line">                f.write(json.dumps(content, ensure_ascii=<span class="literal">False</span>))</span><br><span class="line">                f.write(<span class="string">'\n'</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">get_content_list</span><span class="params">(self, json_str)</span>:</span></span><br><span class="line">        dict_ret = json.loads(json_str)</span><br><span class="line">        data = dict_ret[<span class="string">'data'</span>][<span class="string">'data'</span>]</span><br><span class="line">        content_list = [i[<span class="string">'group'</span>][<span class="string">'max_time'</span>] <span class="keyword">for</span> i <span class="keyword">in</span> data]</span><br><span class="line">        max_time = dict_ret[<span class="string">'data'</span>][<span class="string">'max_time'</span>]</span><br><span class="line">        has_more = dict_ret[<span class="string">'data'</span>][<span class="string">'has_more'</span>]</span><br><span class="line">        <span class="keyword">return</span> content_list,max_time,has_more</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="comment"># 1.start_url</span></span><br><span class="line">        <span class="comment"># 2.发送请求，获取响应</span></span><br><span class="line">        html_str = self.parse_url(self.start_url)</span><br><span class="line">        <span class="comment"># 3.提取数据</span></span><br><span class="line">        content_list,maxtime = self.get_first_page_content_list(html_str)</span><br><span class="line">        <span class="comment"># 4.保存</span></span><br><span class="line">        self.save_content_list(content_list)</span><br><span class="line">        has_more = <span class="literal">True</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span> has_more:</span><br><span class="line">            next_url = self.next_url_temp.format(max_time)</span><br><span class="line">            json_str = self.parse_url(next_url)</span><br><span class="line">            content_list,max_time,has_more = self.get_content_list(json_str)</span><br><span class="line">            self.save_content_list(content_list)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    neihan = Neihan()</span><br><span class="line">    neihan.run()</span><br></pre></td></tr></table></figure><h3 id="xpath"><a href="#xpath" class="headerlink" title="xpath"></a>xpath</h3><p>什么是XPATH：</p><blockquote><p>XPath(XML Path Language)是一门在<strong>HTML\XML文档</strong>中查找信息的语言<br>可用来在HTML\XML文档中<strong>对元素和属性进行遍历</strong>。</p></blockquote><p>我们可以在Chrome浏览器中使用插件<strong>XPath Helper</strong>来帮助我们学习Xpath语法<br>使用Xpath Helper插件选择标签的时候，选中的标签会自动添加一个属性<strong>class=”xh-highlight”</strong><br><strong>注意</strong>：</p><blockquote><p>使用xpath helper是从element中提取的数据<br>但是我们的爬虫获取的是url对应的响应，往往和elements不一样</p></blockquote><p>XPath 使用路径表达式来选取<strong>HTML\XML文档</strong>中的节点或者节点集(这里的节点也可以是标签)。<br>这些路径表达式和我们在常规的电脑文件系统中看到的表达式非常相似。</p><p>xpath语法：</p><table><thead><tr><th align="center">表达式</th><th>描述</th></tr></thead><tbody><tr><td align="center">/</td><td>从根节点选取</td></tr><tr><td align="center">//</td><td>从任意位置选取</td></tr><tr><td align="center">NODENAME</td><td>选取此<strong>节点\标签</strong>的所有子<strong>节点\标签</strong></td></tr><tr><td align="center">.</td><td>选取当前节点</td></tr><tr><td align="center">..</td><td>选取当前节点的父节点</td></tr><tr><td align="center">@</td><td>选取属性，也可用于筛选属性</td></tr><tr><td align="center">*</td><td>通配符，用于匹配所有节点</td></tr><tr><td align="center">node()</td><td>和<code>*</code>一样，用于匹配所有节点</td></tr><tr><td align="center">@*</td><td>匹配所有属性值</td></tr><tr><td align="center">`</td><td>`</td></tr></tbody></table><p>xpath案例：</p><table><thead><tr><th align="center">路径表达式</th><th>结果</th></tr></thead><tbody><tr><td align="center">/bookstore/book[1]</td><td>选取属于bookstore子元素的第一个book元素</td></tr><tr><td align="center">/bookstore/book[last()]</td><td>选取属于bookstore子元素的最后一个book元素</td></tr><tr><td align="center">/bookstore/book[last()-1]</td><td>选取属于bookstore子元素的倒数第二个book元素</td></tr><tr><td align="center">bookstore/book[position()&lt;3]</td><td>选取最前面两个属于bookstore子元素的book元素</td></tr><tr><td align="center">//title[@lang]</td><td>选取所有拥有名为lang属性的title元素</td></tr><tr><td align="center">//title[@lang=”eng”]</td><td>选取所有lang属性值为’eng’的title元素</td></tr></tbody></table><p>常用：</p><ul><li><p><strong>获取文本</strong></p><ul><li><code>a/text()</code> 表示获取a下的文本</li><li><code>a//text()</code> 表示获取a下的所有标签的文本</li><li><code>//a[text()=&#39;下一页&#39;]</code> 表示选择文本为下一页三个字的a标签</li></ul></li><li><p><strong>@符号</strong></p><ul><li><code>a/@href</code> 表示获取a标签的href属性值</li><li><code>//ul[@id=&quot;detail-list&quot;]</code> 表示获取id为”detail-list”的ul标签</li></ul></li><li><p><strong>//</strong></p><ul><li>在xpath最前面表示从当前html中任意位置开始选择</li><li><code>li//a</code> 表示的是li下任何一个a标签</li></ul></li><li><p><strong>包含(contains)</strong><br><code>//div[contains(@class, &#39;i&#39;)]</code>表示class属性包含’i’的所有div标签</p></li></ul><h3 id="lxml"><a href="#lxml" class="headerlink" title="lxml"></a>lxml</h3><blockquote><p>lxml是一款高性能的Python<strong>HTML/XML</strong>解析器<br>我们可以利用其中的XPath，来快速的定位特定元素以及获取节点信息</p></blockquote><p>在使用之前需要导入lxml中的etree库：<br><code>from lxml import etree</code><br>然后我们可以利用<code>etree.HTML</code>，将字符串转化为<strong>Element对象</strong>:<br><code>html = etree.HTML(HTML_STR)</code><br><strong>注意：lxml可以自动修正html代码</strong><br>Element对象具有Xpath的方法</p><p>案例如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#-*- encoding: utf-8 -*- </span></span><br><span class="line"><span class="keyword">from</span> lxml <span class="keyword">import</span> etree</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">text = <span class="string">'''</span></span><br><span class="line"><span class="string">    &lt;div&gt;&lt;ul&gt; </span></span><br><span class="line"><span class="string">    &lt;li class="item-1"&gt;&lt;a&gt;first item&lt;/a&gt;&lt;/li&gt; </span></span><br><span class="line"><span class="string">    &lt;li class="item-1"&gt;&lt;a href="link2.html"&gt;second item&lt;/a&gt;&lt;/li&gt; </span></span><br><span class="line"><span class="string">    &lt;li class="item-inactive"&gt;&lt;a href="link3.html"&gt;third item&lt;/a&gt;&lt;/li&gt; </span></span><br><span class="line"><span class="string">    &lt;li class="item-1"&gt;&lt;a href="link4.html"&gt;fourth item&lt;/a&gt;&lt;/li&gt; </span></span><br><span class="line"><span class="string">    &lt;li class="item-0"&gt;&lt;a href="link5.html"&gt;fifth item&lt;/a&gt;</span></span><br><span class="line"><span class="string">    &lt;/ul&gt;&lt;/div&gt;</span></span><br><span class="line"><span class="string">'''</span></span><br><span class="line"></span><br><span class="line">html = etree.HTML(text)</span><br><span class="line"><span class="comment">#查看element对象中包含的字符串</span></span><br><span class="line">print(etree.tostring(html).decode())</span><br><span class="line"></span><br><span class="line"><span class="comment">#获取class为item-1 li下的a的herf</span></span><br><span class="line">ret1 = html.xpath(<span class="string">"//li[@class='item-1']/a/@href"</span>)</span><br><span class="line">print(ret1)</span><br><span class="line"></span><br><span class="line"><span class="comment">#获取class为item-1 li下的a的文本</span></span><br><span class="line">ret2 = html.xpath(<span class="string">"//li[@class='item-1']/a/text()"</span>)</span><br><span class="line">print(ret2)</span><br><span class="line"></span><br><span class="line"><span class="comment">#每个li是一条新闻，把url和文本组成字典</span></span><br><span class="line"><span class="keyword">for</span> href <span class="keyword">in</span> ret1:</span><br><span class="line">    item = &#123;&#125;</span><br><span class="line">    item[<span class="string">"href"</span>] = href</span><br><span class="line">    item[<span class="string">"title"</span>] = ret2[ret1.index(href)]</span><br><span class="line">    print(item)</span><br><span class="line"></span><br><span class="line">print(<span class="string">"*"</span>*<span class="number">100</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">#分组，根据li标签进行分组，对每一组继续写xpath</span></span><br><span class="line">ret3 = html.xpath(<span class="string">"//li[@class='item-1']"</span>)</span><br><span class="line">print(ret3)</span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> ret3:</span><br><span class="line">    item=  &#123;&#125;</span><br><span class="line">    item[<span class="string">"title"</span>] = i.xpath(<span class="string">"a/text()"</span>)[<span class="number">0</span>] <span class="keyword">if</span> len(i.xpath(<span class="string">"./a/text()"</span>))&gt;<span class="number">0</span> <span class="keyword">else</span> <span class="literal">None</span></span><br><span class="line">    item[<span class="string">"href"</span>] = i.xpath(<span class="string">"./a/@href"</span>)[<span class="number">0</span>] <span class="keyword">if</span> len( i.xpath(<span class="string">"./a/@href"</span>))&gt;<span class="number">0</span> <span class="keyword">else</span> <span class="literal">None</span></span><br><span class="line">    print(item)</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;html&gt;&lt;body&gt;&lt;div&gt;&lt;ul&gt; </span><br><span class="line">    &lt;li class&#x3D;&quot;item-1&quot;&gt;&lt;a&gt;first item&lt;&#x2F;a&gt;&lt;&#x2F;li&gt; </span><br><span class="line">    &lt;li class&#x3D;&quot;item-1&quot;&gt;&lt;a href&#x3D;&quot;link2.html&quot;&gt;second item&lt;&#x2F;a&gt;&lt;&#x2F;li&gt; </span><br><span class="line">    &lt;li class&#x3D;&quot;item-inactive&quot;&gt;&lt;a href&#x3D;&quot;link3.html&quot;&gt;third item&lt;&#x2F;a&gt;&lt;&#x2F;li&gt; </span><br><span class="line">    &lt;li class&#x3D;&quot;item-1&quot;&gt;&lt;a href&#x3D;&quot;link4.html&quot;&gt;fourth item&lt;&#x2F;a&gt;&lt;&#x2F;li&gt; </span><br><span class="line">    &lt;li class&#x3D;&quot;item-0&quot;&gt;&lt;a href&#x3D;&quot;link5.html&quot;&gt;fifth item&lt;&#x2F;a&gt;</span><br><span class="line">    &lt;&#x2F;li&gt;&lt;&#x2F;ul&gt;&lt;&#x2F;div&gt;</span><br><span class="line">&lt;&#x2F;body&gt;&lt;&#x2F;html&gt;</span><br><span class="line">[&#39;link2.html&#39;, &#39;link4.html&#39;]</span><br><span class="line">[&#39;first item&#39;, &#39;second item&#39;, &#39;fourth item&#39;]</span><br><span class="line">&#123;&#39;href&#39;: &#39;link2.html&#39;, &#39;title&#39;: &#39;first item&#39;&#125;</span><br><span class="line">&#123;&#39;href&#39;: &#39;link4.html&#39;, &#39;title&#39;: &#39;second item&#39;&#125;</span><br><span class="line">****************************************************************************************************</span><br><span class="line">[&lt;Element li at 0x7f596c25cc88&gt;, &lt;Element li at 0x7f596c25cc48&gt;, &lt;Element li at 0x7f596c25ce08&gt;]</span><br><span class="line">&#123;&#39;title&#39;: &#39;first item&#39;, &#39;href&#39;: None&#125;</span><br><span class="line">&#123;&#39;title&#39;: &#39;second item&#39;, &#39;href&#39;: &#39;link2.html&#39;&#125;</span><br><span class="line">&#123;&#39;title&#39;: &#39;fourth item&#39;, &#39;href&#39;: &#39;link4.html&#39;&#125;</span><br></pre></td></tr></table></figure><p>lxml使用的<strong>注意点</strong>：</p><blockquote><p>lxml能够修正HTML代码，但是可能会改错了<br>我们可以先使用etree.tostring观察修改之后的html的样子<br>然后根据修改之后的html字符串写xpath</p><p>lxml能够接受bytes和str的字符串<br>提取页面数据的思路:</p><ul><li>先分组，取到一个包含分组标签的列表</li><li>然后遍历，取其中每一组数据，再进行提取，这样就不会造成数据的对应错乱</li></ul></blockquote><h3 id="爬虫套路"><a href="#爬虫套路" class="headerlink" title="爬虫套路"></a>爬虫套路</h3><ul><li><p><strong>准备url</strong></p><blockquote><p>当url地址规律不明显或者总数不确定的时候，我们就需要准备一个start_url<br>然后通过获取的响应代码提取下一页的url<br>如果url规律很明显，页码总数明确的话，我们就可以准备url_list</p></blockquote></li><li><p><strong>发送请求，获取响应</strong></p><blockquote><ul><li>添加随机的User-Agent,反反爬虫</li><li>添加随机的代理ip，反反爬虫</li><li>在对方判断出我们是爬虫之后，应该添加更多的headers字段，包括cookie</li><li>cookie的处理可以使用session来解决</li><li>准备一堆能用的cookie，组成cookie池<br>如果不登录就准备刚开始能够成功请求对方网站的cookie<br>即接收对方网站设置在response的cookie<br>下一次请求的时候，使用之前的列表中的cookie来请求<br>如果登录的话，就需要：<ul><li>准备多个账号</li><li>使用程序获取每个账号的cookie</li><li>之后请求登录之后才能访问的网站随机的选择cookie</li></ul></li></ul></blockquote></li><li><p><strong>提取数据</strong></p><blockquote><p>首先我们需要确定数据的位置</p><ul><li><p>如果数据在当前的url地址中<br>提取的是列表页的数据，就直接请求列表页的url地址，不用进入详情页<br>提取的是详情页的数据就需要：</p><ul><li><ol><li>确定url</li></ol></li><li><ol start="2"><li>发送请求</li></ol></li><li><ol start="3"><li>提取数据</li></ol></li><li><ol start="4"><li>返回</li></ol></li></ul></li><li><p>如果数据不在当前的url地址中<br>那么我们就要在其他的响应中，寻找数据的位置:</p><ul><li><ol><li>从network中从上往下找</li></ol></li><li><ol start="2"><li>使用chrome中的过滤条件，选择出了js,css,img之外的按钮</li></ol></li><li><ol start="3"><li>使用chrome的search all file，搜索数字和英文</li></ol></li></ul></li></ul><p>获取数据之后就要进行数据的提取</p><ul><li>xpath,从html中提取整块的数据，先分组，之后每一组再提取</li><li>re，提取max_time,price,html中的json字符串</li><li>json</li></ul></blockquote></li><li><p><strong>保存</strong></p><blockquote><p>可以选择保存在本地，如text,json,csv<br>也可以保存在数据库</p></blockquote></li></ul><h3 id="案例：贴吧爬虫"><a href="#案例：贴吧爬虫" class="headerlink" title="案例：贴吧爬虫"></a>案例：贴吧爬虫</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#-*- encoding: utf-8 -*- </span></span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"><span class="keyword">import</span> json</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> lxml <span class="keyword">import</span> etree</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TiebaSpider</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, name)</span>:</span></span><br><span class="line">        self.name = name</span><br><span class="line">        self.start_url = <span class="string">"http://tieba.baidu.com/mo/q----,sz@320_240-1-3---2/m?kw="</span>+name+<span class="string">"&amp;pn=0"</span></span><br><span class="line">        self.part_url = <span class="string">"http://tieba.baidu.com/mo/q----,sz@320_240-1-3---2/"</span></span><br><span class="line">        self.headers = &#123;</span><br><span class="line">            <span class="string">"User-Agent"</span>:</span><br><span class="line">            <span class="string">"Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/71.0.3578.98 Safari/537.36"</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse_url</span><span class="params">(self, url)</span>:</span></span><br><span class="line">        <span class="string">"""发送请求，获取响应"""</span></span><br><span class="line">        print(url)</span><br><span class="line">        response = requests.get(url, headers=self.headers)</span><br><span class="line">        <span class="keyword">return</span> response.content</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">get_content_list</span><span class="params">(self, html_str)</span>:</span></span><br><span class="line">        <span class="string">"""提取数据"""</span></span><br><span class="line">        html = etree.HTML(html_str)</span><br><span class="line"></span><br><span class="line">        div_list = html.xpath(<span class="string">"//div[contains(@class, 'i')]"</span>)  <span class="comment"># 根据div进行分组</span></span><br><span class="line">        content_list = []</span><br><span class="line">        <span class="keyword">for</span> div <span class="keyword">in</span> div_list:</span><br><span class="line">            item = &#123;&#125;</span><br><span class="line">            item[<span class="string">"title"</span>] = div.xpath(<span class="string">'./a/text()'</span>)[<span class="number">0</span>] <span class="keyword">if</span> len(div.xpath(<span class="string">'./a/text()'</span>))&gt;<span class="number">0</span> <span class="keyword">else</span> <span class="literal">None</span></span><br><span class="line">            item[<span class="string">"href"</span>] = self.part_url+div.xpath(<span class="string">'./a/@href'</span>)[<span class="number">0</span>] <span class="keyword">if</span> len(div.xpath(<span class="string">'./a/@href'</span>))&gt;<span class="number">0</span> <span class="keyword">else</span> <span class="literal">None</span></span><br><span class="line">            item[<span class="string">"img_list"</span>] = self.get_img_list(item[<span class="string">'href'</span>], [])</span><br><span class="line">            <span class="keyword">if</span> len(item[<span class="string">'img_list'</span>])&gt;<span class="number">0</span>:</span><br><span class="line">                item[<span class="string">"img_list"</span>] = [requests.utils.unquote(i).split(<span class="string">"src="</span>)[<span class="number">-1</span>] <span class="keyword">for</span> i <span class="keyword">in</span> item[<span class="string">'img_list'</span>]]</span><br><span class="line">            content_list.append(item)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 提取下一页url地址</span></span><br><span class="line">        next_url = self.part_url + html.xpath(<span class="string">'//a[text()="下一页"]/@href'</span>)[<span class="number">0</span>] <span class="keyword">if</span> len(html.xpath(<span class="string">'//a[text()="下一页"]/@href'</span>))&gt;<span class="number">0</span> <span class="keyword">else</span> <span class="literal">None</span></span><br><span class="line">        <span class="keyword">return</span> content_list, next_url</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">get_img_list</span><span class="params">(self, detail_url, total_img_list)</span>:</span></span><br><span class="line">        <span class="string">"""获取图片"""</span></span><br><span class="line">        <span class="comment"># 1. 请求列表页的url地址，获取详情页的第一页</span></span><br><span class="line">        detail_html_str = self.parse_url(detail_url)</span><br><span class="line">        detail_html = etree.HTML(detail_html_str)</span><br><span class="line">        <span class="comment"># 2. 提取所有图片的URL和下一页的url</span></span><br><span class="line">        img_url = detail_html.xpath(<span class="string">'//img[@class="BDE_Image"]/@src'</span>)[<span class="number">0</span>] <span class="keyword">if</span> len(detail_html.xpath(<span class="string">'//img[@class="BDE_Image"]/@src'</span>))&gt;<span class="number">0</span> <span class="keyword">else</span> <span class="literal">None</span></span><br><span class="line">        <span class="keyword">if</span> img_url <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">            total_img_list.append(img_url)</span><br><span class="line">        detail_next_url = detail_html.xpath(<span class="string">'//a[text()="下一页"]/@href'</span>)</span><br><span class="line">        <span class="comment"># 3. 请求下一页，进入循环(此处用递归)</span></span><br><span class="line">        <span class="keyword">if</span> len(detail_next_url)&gt;<span class="number">0</span>:</span><br><span class="line">            detail_next_url = self.part_url + detail_next_url[<span class="number">0</span>]</span><br><span class="line">            <span class="keyword">return</span> self.get_img_list(detail_next_url, total_img_list)</span><br><span class="line">        <span class="comment"># 所有数据获取完毕后跳出递归</span></span><br><span class="line">        <span class="keyword">return</span> total_img_list</span><br><span class="line">      </span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">save_content_list</span><span class="params">(self, content_list)</span>:</span></span><br><span class="line">        <span class="string">"""保存数据"""</span></span><br><span class="line">        file_path = self.name + <span class="string">".txt"</span></span><br><span class="line">        <span class="keyword">with</span> open(file_path, <span class="string">'a'</span>, encoding=<span class="string">'utf-8'</span>) <span class="keyword">as</span> f:</span><br><span class="line">            <span class="keyword">for</span> content <span class="keyword">in</span> content_list:</span><br><span class="line">                f.write(json.dumps(content, ensure_ascii=<span class="literal">False</span>, indent=<span class="number">2</span>))</span><br><span class="line">                f.write(<span class="string">'\n'</span>)</span><br><span class="line"></span><br><span class="line">        print(<span class="string">"保存成功"</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="comment"># 1. 构造start_url</span></span><br><span class="line">        url = self.start_url</span><br><span class="line">        <span class="keyword">while</span> url <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">            <span class="comment"># 2. 发送请求，获取响应</span></span><br><span class="line">            html_str = self.parse_url(url)</span><br><span class="line">            <span class="comment"># 3. 提取数据，构造下一页url地址</span></span><br><span class="line">            content_list, url = self.get_content_list(html_str)</span><br><span class="line">            <span class="comment"># 4. 保存数据</span></span><br><span class="line">            self.save_content_list(content_list)</span><br><span class="line">            <span class="comment"># 5. 请求下一页地址，进入循环</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    tieba_spider = TiebaSpider(<span class="string">'vim'</span>)</span><br><span class="line">    tieba_spider.run()</span><br></pre></td></tr></table></figure><h3 id="多线程爬虫"><a href="#多线程爬虫" class="headerlink" title="多线程爬虫"></a>多线程爬虫</h3><p>我们可以使用队列来让多个线程之间相互通信<br>因此这个多线程爬虫注意是利用队列来进行数据在不同线程中的共享</p><blockquote><p>Queue的<strong>注意点</strong>:<br>Queue使用put方法的时候会让计数加一，但是使用get方法取元素的时候计数并不会减一<br>get方法需要和<strong>task_done</strong>方法在一起的时候才会让计数减一<br>Queue的join方法可以让主线程等待阻塞，等待队列的任务完成之后再完成</p></blockquote><p>多线程爬虫案例如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#-*- encoding: utf-8 -*- </span></span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"><span class="keyword">import</span> threading</span><br><span class="line"><span class="keyword">from</span> lxml <span class="keyword">import</span> etree</span><br><span class="line"><span class="keyword">from</span> queue <span class="keyword">import</span> Queue</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">QiubaiSpider</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.url_temp = <span class="string">"https://www.qiushibaike.com/8hr/page/&#123;&#125;/"</span></span><br><span class="line">        self.headers = &#123;</span><br><span class="line">            <span class="string">"User-Agent"</span>:</span><br><span class="line">            <span class="string">"Mozilla/5.0 (Macintosh; Intel Mac OS X 10_13_2) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/63.0.3239.84 Safari/537.36"</span></span><br><span class="line">        &#125;</span><br><span class="line">        self.url_queue = Queue()</span><br><span class="line">        self.html_queue = Queue()</span><br><span class="line">        self.content_queue = Queue()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">get_url_list</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>,<span class="number">4</span>):</span><br><span class="line">            self.url_queue.put(self.url_temp.format(i))</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse_url</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            url = self.url_queue.get()</span><br><span class="line"></span><br><span class="line">            <span class="comment">#print(url)</span></span><br><span class="line">            response = requests.get(url, headers=self.headers)</span><br><span class="line">            self.html_queue.put(response.content.decode())</span><br><span class="line">            self.url_queue.task_done()</span><br><span class="line">      </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">get_content_list</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            html_str = self.html_queue.get()</span><br><span class="line">            html = etree.HTML(html_str)</span><br><span class="line">            div_list = html.xpath(<span class="string">"//div[@id='content-left']/div"</span>)</span><br><span class="line">            content_list = []</span><br><span class="line"></span><br><span class="line">            <span class="keyword">for</span> div <span class="keyword">in</span> div_list:</span><br><span class="line">                item = &#123;&#125;</span><br><span class="line">                item[<span class="string">"content"</span>] = div.xpath(<span class="string">".//div[@class='content']/span/text()"</span>)</span><br><span class="line">                item[<span class="string">"content"</span>] = [i.replace(<span class="string">"\n"</span>,<span class="string">""</span>) <span class="keyword">for</span> i <span class="keyword">in</span> item[<span class="string">"content"</span>]]</span><br><span class="line">                item[<span class="string">"author_gender"</span>] = div.xpath(<span class="string">".//div[contains(@class,'articleGender')]/@class"</span>)</span><br><span class="line">                item[<span class="string">"author_gender"</span>] = item[<span class="string">"author_gender"</span>][<span class="number">0</span>].split(<span class="string">" "</span>)[<span class="number">-1</span>].replace(<span class="string">"Icon"</span>,<span class="string">""</span>) <span class="keyword">if</span> len(item[<span class="string">"author_gender"</span>])&gt;<span class="number">0</span> <span class="keyword">else</span> <span class="literal">None</span></span><br><span class="line">                item[<span class="string">"auhtor_age"</span>] = div.xpath(<span class="string">".//div[contains(@class,'articleGender')]/text()"</span>)</span><br><span class="line">                item[<span class="string">"auhtor_age"</span>] = item[<span class="string">"auhtor_age"</span>][<span class="number">0</span>] <span class="keyword">if</span> len(item[<span class="string">"auhtor_age"</span>])&gt;<span class="number">0</span> <span class="keyword">else</span> <span class="literal">None</span></span><br><span class="line">                item[<span class="string">"content_img"</span>] = div.xpath(<span class="string">".//div[@class='thumb']/a/img/@src"</span>)</span><br><span class="line">                item[<span class="string">"content_img"</span>] = <span class="string">"https:"</span>+item[<span class="string">"content_img"</span>][<span class="number">0</span>] <span class="keyword">if</span> len(item[<span class="string">"content_img"</span>])&gt;<span class="number">0</span> <span class="keyword">else</span> <span class="literal">None</span></span><br><span class="line">                item[<span class="string">"author_img"</span>] = div.xpath(<span class="string">".//div[@class='author clearfix']//img/@src"</span>)</span><br><span class="line">                item[<span class="string">"author_img"</span>] = <span class="string">"https:"</span>+item[<span class="string">"author_img"</span>][<span class="number">0</span>] <span class="keyword">if</span> len(item[<span class="string">"author_img"</span>])&gt;<span class="number">0</span> <span class="keyword">else</span> <span class="literal">None</span></span><br><span class="line">                item[<span class="string">"stats_vote"</span>] = div.xpath(<span class="string">".//span[@class='stats-vote']/i/text()"</span>)</span><br><span class="line">                item[<span class="string">"stats_vote"</span>] = item[<span class="string">"stats_vote"</span>][<span class="number">0</span>] <span class="keyword">if</span> len(item[<span class="string">"stats_vote"</span>])&gt;<span class="number">0</span> <span class="keyword">else</span> Non</span><br><span class="line">                content_list.append(item)</span><br><span class="line"></span><br><span class="line">            self.content_queue.put(content_list)</span><br><span class="line">            self.html_queue.task_done()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">save_content_list</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            content_list = self.content_queue.get()</span><br><span class="line">            <span class="keyword">for</span> i <span class="keyword">in</span> content_list:</span><br><span class="line">                print(i)</span><br><span class="line">            self.content_queue.task_done()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">        thread_list = []</span><br><span class="line">        t_url = threading.Thread(target=self.get_url_list)</span><br><span class="line">        t_parse = threading.Thread(target=self.parse_url)</span><br><span class="line">        t_html = threading.Thread(target=self.get_content_list)</span><br><span class="line">        t_save = threading.Thread(target=self.save_content_list)</span><br><span class="line">        thread_list.append(t_url)</span><br><span class="line">        thread_list.append(t_parse)</span><br><span class="line">        thread_list.append(t_html)</span><br><span class="line">        thread_list.append(t_save)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> t <span class="keyword">in</span> thread_list:</span><br><span class="line">            t.setDaemon(<span class="literal">True</span>)  <span class="comment"># 将子线程设置为守护进程，主线程结束后该线程结束</span></span><br><span class="line">            t.start()</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> q <span class="keyword">in</span> [self.url_queue,self.html_queue,self.content_queue]:</span><br><span class="line">            <span class="comment"># 等待队列使用完毕</span></span><br><span class="line">            q.join()</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    qiubai = QiubaiSpider()</span><br><span class="line">    qiubai.run()</span><br></pre></td></tr></table></figure><h2 id="Scrapy"><a href="#Scrapy" class="headerlink" title="Scrapy"></a>Scrapy</h2><blockquote><p>Scrapy是一个为了爬取网站数据，提取结构性数据而编写的<strong>应用框架</strong><br>我们只需要实现少量的代码，就能够快速的抓取<br>Scrapy使用了Twisted异步网络框架，可以加快我们的下载速度。</p></blockquote><h3 id="Scrapy-流程"><a href="#Scrapy-流程" class="headerlink" title="Scrapy 流程"></a>Scrapy 流程</h3><p>我们上面所学的爬虫流程如下：</p><p>Scrapy框架就是一个爬虫框架，因此它的流程和爬虫流程差不多，如下：</p><p>其中的内容如下：</p><table><thead><tr><th align="center">结构</th><th>作用</th><th align="center">是否需要实现</th></tr></thead><tbody><tr><td align="center">Scrapy Engine(引擎)</td><td>总指挥：负责数据和信号在不同模块间的传递</td><td align="center">Scrapy已实现</td></tr><tr><td align="center">Scheduler(调度器)</td><td>一个队列，存放引擎发送过来的Request请求</td><td align="center">Scrapy已实现</td></tr><tr><td align="center">Downloader(下载器)</td><td>下载引擎发送过来的Request请求，将结果返回给引擎</td><td align="center">Scrapy已实现</td></tr><tr><td align="center">Spider(爬虫)</td><td>处理引擎发送的response，提取数据和URL，并返回给引擎</td><td align="center">需要手写</td></tr><tr><td align="center">Item Pipeline(管道)</td><td>处理引擎发送过来的数据，比如存储</td><td align="center">需要手写</td></tr><tr><td align="center">Downloader Middlewares(下载中间件)</td><td>可以自定义的下载扩展，比如设置代理</td><td align="center">一般不用手写</td></tr><tr><td align="center">Spider Middlewares(爬虫中间件)</td><td>可以自定义requests请求和进行response过滤</td><td align="center">一般不用手写</td></tr></tbody></table><h3 id="Scrapy-入门"><a href="#Scrapy-入门" class="headerlink" title="Scrapy 入门"></a>Scrapy 入门</h3><p>要使用Scrapy框架，首先需要下载这个框架，这里不予说明<br>创建一个Scrapy项目需要使用scrapy命令，如下：<br><code>scrapy startproject mySpider</code><br>然后进入项目目录中生成一个爬虫，如下：<br><code>scrapy genspider NAME &quot;NAME.com/cn&quot;</code><br>这样，一个scrapy框架就完成了</p><p>例如我这里使用<code>scrapy startproject itcastSpider</code>创建一个爬虫项目，<br>然后使用<code>scrapy genspider itcast itcast.cn</code>生成一个爬虫，目录结构如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.</span><br><span class="line">├── itcastSpider</span><br><span class="line">│   ├── __init__.py</span><br><span class="line">│   ├── items.py        ---&gt; 自己预计需要爬取的内容</span><br><span class="line">│   ├── middlewares.py  ---&gt; 自定义中间件</span><br><span class="line">│   ├── pipelines.py    ---&gt; 管道，保存数据</span><br><span class="line">│   ├── __pycache__</span><br><span class="line">│   ├── settings.py     ---&gt; 设置文件，UA，启动管道</span><br><span class="line">│   └── spiders         ---&gt; 自己定义的爬虫文件夹</span><br><span class="line">│       ├── __init__.py</span><br><span class="line">│       ├── itcast.py</span><br><span class="line">│       └── __pycache__</span><br><span class="line">└── scrapy.cfg          ---&gt; 项目的配置文件</span><br></pre></td></tr></table></figure><p>首先编辑spider中的爬虫文件，内容如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># -*- coding: utf-8 -*-</span></span><br><span class="line"><span class="keyword">import</span> scrapy</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ItcastSpider</span><span class="params">(scrapy.Spider)</span>:</span></span><br><span class="line">    name = <span class="string">'itcast'</span>  <span class="comment"># 爬虫名</span></span><br><span class="line">    allowed_domains = [<span class="string">'itcast.cn'</span>]  <span class="comment"># 允许爬取的范围</span></span><br><span class="line">    start_urls = [<span class="string">'http://www.itcast.cn/channel/teacher.shtml'</span>]  <span class="comment"># 最开始请求的url</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse</span><span class="params">(self, response)</span>:</span></span><br><span class="line">        <span class="string">"""处理start_url地址对应的响应"""</span></span><br><span class="line">        <span class="comment">#ret = response.xpath('//div[@class="tea_con"]//h3/text()').extract()</span></span><br><span class="line">        <span class="comment">#print(ret)</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 分组</span></span><br><span class="line">        li_list = response.xpath(<span class="string">'//div[@class="tea_con"]//li'</span>)</span><br><span class="line">        <span class="keyword">for</span> li <span class="keyword">in</span> li_list:</span><br><span class="line">            item = &#123;&#125;</span><br><span class="line">            item[<span class="string">"name"</span>] = li.xpath(<span class="string">'.//h3/text()'</span>).extract_first()</span><br><span class="line">            item[<span class="string">"title"</span>] = li.xpath(<span class="string">'.//h4/text()'</span>).extract_first()</span><br><span class="line">            <span class="comment">#print(item)</span></span><br><span class="line">            <span class="keyword">yield</span> item</span><br></pre></td></tr></table></figure><p><strong>注意:</strong></p><blockquote><p>爬虫类中的name值需要和使用<code>spider genspider</code>命令中定义的爬虫名一致<br>allowed_domains值需要和命令中的域名一致<br>parse方法是必须要有的方法，其中进行爬取操作<br>response对象是一个响应对象，可以对其进行xpath操作，会得到<strong>Selector</strong>对象<br>如果要获取Selector对象的值，需要调用<strong>extract</strong>方法获取其中的内容，该方法返回一个包含有字符串数据的列表<br>但是如果得到的Selector对象中没有内容，extract方法会抛异常，因此常常使用<strong>extract_first</strong>方法，该方法返回列表中的第一个字符串<br>该方法如果没有获取值不会抛异常，只会将值置为None<br>爬虫中的parse方法可以使用<strong>yield</strong>将函数变成一个<strong>生成器</strong>，可以将数据传到Pipeline中<br>爬虫使用yield之后，可以直接在Pipeline中使用这个yield的值了(<strong>该值在Pipeline中用item接收</strong>)<br>只能yield这几种：<code>Request, BaseItem, dict or None</code></p></blockquote><p>Pipeline文件如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># -*- coding: utf-8 -*-</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MySpiderPipeline1</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">process_item</span><span class="params">(self, item, spider)</span>:</span></span><br><span class="line">        print(item)</span><br><span class="line">        item[<span class="string">"hello"</span>] = <span class="string">"world"</span></span><br><span class="line">        <span class="keyword">return</span> item</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MySpiderPipeline2</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">process_item</span><span class="params">(self, item, spider)</span>:</span></span><br><span class="line">        print(item)</span><br><span class="line">        <span class="keyword">return</span> item</span><br></pre></td></tr></table></figure><p>写完Pipeline文件之后，需要在setting文件中启动它：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">ITEM_PIPELINES = &#123;</span><br><span class="line">    <span class="string">'itcastSpider.pipelines.MySpiderPipeline1'</span>: <span class="number">300</span>,</span><br><span class="line">    <span class="string">'itcastSpider.pipelines.MySpiderPipeline2'</span>: <span class="number">301</span>,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>setting文件中的ITEM_PIPELINES字典中的键为<strong>pipeline的位置</strong>，值为<strong>权重</strong><br>如果有多个Pipeline，则元素会先到权重小的管道中，然后再到权重高的管道中<br>如这个爬虫的执行结果如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">&gt;&#123;<span class="string">'name'</span>: <span class="string">'潘老师'</span>, <span class="string">'title'</span>: <span class="string">'高级讲师'</span>, <span class="string">'hello'</span>: <span class="string">'world'</span>&#125;</span><br><span class="line">&gt;&#123;<span class="string">'name'</span>: <span class="string">'胡老师'</span>, <span class="string">'title'</span>: <span class="string">'高级讲师'</span>&#125;</span><br><span class="line">&gt;&#123;<span class="string">'name'</span>: <span class="string">'胡老师'</span>, <span class="string">'title'</span>: <span class="string">'高级讲师'</span>, <span class="string">'hello'</span>: <span class="string">'world'</span>&#125;</span><br><span class="line">&gt;&#123;<span class="string">'name'</span>: <span class="string">'贡老师'</span>, <span class="string">'title'</span>: <span class="string">'高级讲师'</span>&#125;</span><br><span class="line">&gt;&#123;<span class="string">'name'</span>: <span class="string">'贡老师'</span>, <span class="string">'title'</span>: <span class="string">'高级讲师'</span>, <span class="string">'hello'</span>: <span class="string">'world'</span>&#125;</span><br><span class="line">&gt;&#123;<span class="string">'name'</span>: <span class="string">'王老师'</span>, <span class="string">'title'</span>: <span class="string">'高级讲师'</span>&#125;</span><br><span class="line">&gt;&#123;<span class="string">'name'</span>: <span class="string">'王老师'</span>, <span class="string">'title'</span>: <span class="string">'高级讲师'</span>, <span class="string">'hello'</span>: <span class="string">'world'</span>&#125;</span><br><span class="line">&gt;&#123;<span class="string">'name'</span>: <span class="string">'肖老师'</span>, <span class="string">'title'</span>: <span class="string">'高级讲师'</span>&#125;</span><br><span class="line">&gt;&#123;<span class="string">'name'</span>: <span class="string">'肖老师'</span>, <span class="string">'title'</span>: <span class="string">'高级讲师'</span>, <span class="string">'hello'</span>: <span class="string">'world'</span>&#125;</span><br><span class="line">&gt;&#123;<span class="string">'name'</span>: <span class="string">'信老师'</span>, <span class="string">'title'</span>: <span class="string">'高级讲师'</span>&#125;</span><br><span class="line">&gt;&#123;<span class="string">'name'</span>: <span class="string">'信老师'</span>, <span class="string">'title'</span>: <span class="string">'高级讲师'</span>, <span class="string">'hello'</span>: <span class="string">'world'</span>&#125;</span><br></pre></td></tr></table></figure><p>可见，每个元素先到300的管道中，添加了一个键值对后再到301的管道中<br><strong>注意</strong>：pipeline中的process_item方法名不能修改</p></blockquote><h3 id="设置-log"><a href="#设置-log" class="headerlink" title="设置 log"></a>设置 log</h3><p>为了让我们自己希望输出到终端的内容能容易看一些：<br>我们可以在setting中设置<strong>log级别</strong><br>在setting中添加一行（全部大写）：<code>LOG_LEVEL = &quot;WARNING&quot;</code></p><p>关于log日志，有一个模块可以用来输出log日志信息，该模块就是<code>logging</code><br>我们可以自己通过logging模块写一个log模块，如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#-*- encoding: utf-8 -*- </span></span><br><span class="line"><span class="keyword">import</span> logging</span><br><span class="line"></span><br><span class="line"><span class="comment"># 设置日志的输出格式</span></span><br><span class="line">logging.basicConfig(level=logging.INFO,</span><br><span class="line">        format=<span class="string">'[%(asctime)s] '</span></span><br><span class="line">        <span class="string">'%(levelname)s [%(filename)s:%(lineno)d] '</span></span><br><span class="line">        <span class="string">': %(message)s'</span>, </span><br><span class="line">        datefmt=<span class="string">'%d/%b/%Y %H:%M:%S'</span>,)</span><br><span class="line"></span><br><span class="line">logger = logging.getLogger(__name__)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    logger.info(<span class="string">"This is a info log"</span>)</span><br><span class="line">    logger.info(<span class="string">"This is a info log 1"</span>)</span><br></pre></td></tr></table></figure><p>输出结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[21&#x2F;Mar&#x2F;2019 12:48:09] INFO [log.py:15] : This is a info log</span><br><span class="line">[21&#x2F;Mar&#x2F;2019 12:48:09] INFO [log.py:16] : This is a info log 1</span><br></pre></td></tr></table></figure><p>这个模块定义完之后，可以在代码中使用这个模块：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#-*- encoding: utf-8 -*- </span></span><br><span class="line"><span class="keyword">from</span> log <span class="keyword">import</span> logger</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    logger.warning(<span class="string">"This is a log 1"</span>)</span><br><span class="line">    logger.warning(<span class="string">"This is a log 2"</span>)</span><br></pre></td></tr></table></figure><p>输出结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[21&#x2F;Mar&#x2F;2019 12:49:20] WARNING [log_print.py:6] : This is a log 1</span><br><span class="line">[21&#x2F;Mar&#x2F;2019 12:49:20] WARNING [log_print.py:7] : This is a log 2</span><br></pre></td></tr></table></figure><blockquote><p>log模块关键是要通过getLogger方法获取模块名来实例化一个logging对象<br>如果没有获取<strong>name</strong>就无法输出log信息的位置</p></blockquote><h3 id="scrapy-Request"><a href="#scrapy-Request" class="headerlink" title="scrapy.Request"></a>scrapy.Request</h3><p>如果我们获取到一个URL，那么如何再次发送请求呢？<br>scrapy.Request能构建一个requests，可以用来发送请求<br>如下：<br><code>scrapy.Request(url [, callback, method=&#39;GET&#39;, headers, body, cookies, meta, dont_filter=False])</code><br>其中常用的参数为：</p><ul><li><strong>callback</strong>：指定传入的URL交给哪个解析函数去处理</li><li><strong>meta</strong>：实现在不同的解析函数中传递数据，meta默认会携带部分信息</li><li><strong>dont_filter</strong>：让scrapy的去重不会过滤当前的URL，scrapy默认有URL去重的功能，对需要重复请求的URL有重要用途</li></ul><p>例如使用Request实现翻页功能(爬虫parse其他的功能实现完毕)：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">next_url = response.xpath(<span class="string">'//a[test()="下一页"]/@href'</span>).extract_first()</span><br><span class="line"><span class="keyword">if</span> next_url:</span><br><span class="line">    <span class="keyword">yield</span> scrapy.Request(next_url, callback=self.parse)</span><br></pre></td></tr></table></figure><p>又如使用Request请求子页面：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">parse</span><span class="params">(self, response)</span>:</span></span><br><span class="line">    tr_list = response.xpath(<span class="string">'//div[@class="greyframe"]/table//table/tr'</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> tr_list:</span><br><span class="line">        item = Item()</span><br><span class="line">        <span class="keyword">yield</span> scrapy.Request(url, callback=self.get_content, meta=&#123;<span class="string">"item"</span>:item&#125;)</span><br><span class="line">  </span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_content</span><span class="params">(self, response)</span>:</span></span><br><span class="line">    item = response.meta[<span class="string">'item'</span>]</span><br><span class="line">    item[<span class="string">'content'</span>] = response.xpath(<span class="string">'//div[@class="content"]//text()'</span>).extract()</span><br><span class="line">    <span class="keyword">yield</span> item</span><br></pre></td></tr></table></figure><h3 id="Item"><a href="#Item" class="headerlink" title="Item"></a>Item</h3><p>可以把Item理解成一个字典，和我们平常使用的字典一样，在爬虫中可以yield这个Item<br>在Item.py文件中可以定义它的<strong>键</strong>，需要使用scrapy.Field()方法来定义这个键<br>如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># -*- coding: utf-8 -*-</span></span><br><span class="line"><span class="keyword">import</span> scrapy</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TencentItem</span><span class="params">(scrapy.Item)</span>:</span></span><br><span class="line">    <span class="comment"># name = scrapy.Field()</span></span><br><span class="line">    title = scrapy.Field()</span><br><span class="line">    position = scrapy.Field()</span><br><span class="line">    publish_date = scrapy.Field()</span><br></pre></td></tr></table></figure><p>然后在我们的爬虫文件中需要先实例化这个Item，才能给这个Item的键赋值，赋值方法和字典一样，唯一不同的是<strong>如果item中没有这个键，就会报错</strong></p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># -*- coding: utf-8 -*-</span></span><br><span class="line"><span class="keyword">import</span> scrapy</span><br><span class="line"><span class="keyword">from</span> tencent.items <span class="keyword">import</span> TencentItem</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">HrSpider</span><span class="params">(scrapy.Spider)</span>:</span></span><br><span class="line">    name = <span class="string">'hr'</span></span><br><span class="line">    allowed_domains = [<span class="string">'tencent.com'</span>]</span><br><span class="line">    start_urls = [<span class="string">'http://hr.tencent.com/position.php'</span>]</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse</span><span class="params">(self, response)</span>:</span></span><br><span class="line">        tr_list = response.xpath(<span class="string">"//table[@class='tablelist']/tr"</span>)[<span class="number">1</span>:<span class="number">-1</span>]</span><br><span class="line">        <span class="keyword">for</span> tr <span class="keyword">in</span> tr_list:</span><br><span class="line">            <span class="comment"># 实例化我们定义好的Item</span></span><br><span class="line">            item = TencentItem()</span><br><span class="line">            item[<span class="string">'title'</span>] = tr.xpath(<span class="string">'./td[1]/a/text()'</span>).extract_first()</span><br><span class="line">            item[<span class="string">'position'</span>] = tr.xpath(<span class="string">'.//td[2]/text()'</span>).extract_first()</span><br><span class="line">            item[<span class="string">'publish_date'</span>] = tr.xpath(<span class="string">'./td[5]/text()'</span>).extract_first()</span><br><span class="line">            <span class="keyword">yield</span> item</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 找到下一页的URL地址</span></span><br><span class="line">        next_url = response.xpath(<span class="string">'//a[@id="next"]/@href'</span>).extract_first()</span><br><span class="line">        <span class="keyword">if</span> next_url != <span class="string">'javascripy:;'</span>:</span><br><span class="line">            next_url = <span class="string">'http://hr.tencent.com/'</span> + next_url</span><br><span class="line">            <span class="keyword">yield</span> scrapy.Request(</span><br><span class="line">                next_url,</span><br><span class="line">                <span class="comment"># 由于请求下一页的URL地址和这一页使用的方式是一样的，因此调用自身</span></span><br><span class="line">                callback=self.parse</span><br><span class="line">            )</span><br></pre></td></tr></table></figure><p>在把数据交给pipeline的时候可以通过<code>isinstance(item, ITEMNAME)</code>来判断数据是属于哪个item，用于进行不同的处理</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># -*- coding: utf-8 -*-</span></span><br><span class="line"><span class="keyword">from</span> tencent.items <span class="keyword">import</span> TencentItem</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TencentPipeline</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">process_item</span><span class="params">(self, item, spider)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> isinstance(item, TencentItem):</span><br><span class="line">            print(item)</span><br><span class="line">            <span class="keyword">return</span> item</span><br></pre></td></tr></table></figure><h3 id="scrapy-shell"><a href="#scrapy-shell" class="headerlink" title="scrapy shell"></a>scrapy shell</h3><p><strong>Scrapy shell</strong>是一个交互终端，我们可以在<strong>未启动spider的情况下</strong>尝试及调试代码<br>也可以用来测试XPath表达式</p><p>使用方法：<br><code>scrapy shell URL</code></p><p>常用的属性:</p><ul><li>response.url：当前响应的url地址</li><li>response.request.url：当前响应对应的请求的url地址</li><li>response.headers：响应头</li><li>response.body：响应体，也就是html代码，默认是byte类型</li><li>response.requests.headers：当前响应的请求头</li></ul><h3 id="setting"><a href="#setting" class="headerlink" title="setting"></a>setting</h3><p>setting.py文件是一个配置文件，存放一些公共的变量(比如数据库的地址等)<br>使用配置文件可以方便自己和其他人进行修改<br>在配置文件中一般使用全大写字母命名变量名<br>在setting.py文件中定义了变量之后可以在爬虫的setting属性中找到，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Spider</span><span class="params">(scrapy.Spider)</span>:</span></span><br><span class="line">    name = <span class="string">''</span></span><br><span class="line">    allowed_domains = []</span><br><span class="line">    start_urls = []</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse</span><span class="params">(self, response)</span>:</span></span><br><span class="line">        <span class="comment"># 获取settings文件中定义的变量</span></span><br><span class="line">        self.settings[<span class="string">"MONGO_HOST"</span>]</span><br><span class="line">        self.settings.get(<span class="string">"MONGO_HOST"</span>,<span class="literal">None</span>)</span><br></pre></td></tr></table></figure><p>在pipeline中直接通过传入的spider参数调用即可</p><h3 id="pipeline"><a href="#pipeline" class="headerlink" title="pipeline"></a>pipeline</h3><p>在爬虫开启的时候会执行一个open_spider方法，仅执行一次<br>与之对应的是在爬虫关闭的时候会执行一个close_spider方法，也仅执行一次<br>我们可以在这两个方法中添加一些属性等，如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Pipeline</span><span class="params">(opject)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">open_spider</span><span class="params">(self, spider)</span>:</span></span><br><span class="line">        self.file = open(spider.settings.get(<span class="string">"SAVE_FILE"</span>, <span class="string">"./temp.txt"</span>), <span class="string">'w'</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">close_spider</span><span class="params">(self, spider)</span>:</span></span><br><span class="line">        self.file.close()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">process_item</span><span class="params">(self, item, spider)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> item</span><br></pre></td></tr></table></figure><h3 id="CrawlSpider"><a href="#CrawlSpider" class="headerlink" title="CrawlSpider"></a>CrawlSpider</h3><p>CrawlSpider是scrapy框架中的一个继承自spiders的类<br>其中有一些特殊的功能能帮助我们减少代码量<br>用命令创建一个crawlspider的模板：<br><code>scrapy genspider -t crawl NAME ALLOW_DOMAIN</code><br>创建好之后的CrawlSpider内容如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># -*- coding: utf-8 -*-</span></span><br><span class="line"><span class="keyword">import</span> scrapy</span><br><span class="line"><span class="keyword">from</span> scrapy.linkextractors <span class="keyword">import</span> LinkExtractor</span><br><span class="line"><span class="keyword">from</span> scrapy.spiders <span class="keyword">import</span> CrawlSpider, Rule</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TtSpider</span><span class="params">(CrawlSpider)</span>:</span></span><br><span class="line">    name = <span class="string">''</span></span><br><span class="line">    allowed_domains = [<span class="string">''</span>]</span><br><span class="line">    start_urls = [<span class="string">''</span>]</span><br><span class="line"></span><br><span class="line">    rules = (</span><br><span class="line">        Rule(LinkExtractor(allow=<span class="string">r'\Items'</span>), callback=<span class="string">'parse_item'</span>, follow=<span class="literal">True</span>),</span><br><span class="line">    )</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse_item</span><span class="params">(self, response)</span>:</span></span><br><span class="line">        <span class="keyword">pass</span></span><br></pre></td></tr></table></figure><p>Rule中的LinkExtractor是一个连接提取器，其常用的参数如下：</p><ul><li><strong>allow</strong>：满足括号中正则表达式的URL会被提取，如果为空则全部匹配</li><li><strong>deny</strong>：满足括号中正则表达式的URL不提取(优先级高于allow)</li><li><strong>restrict_xpaths</strong>：使用xpath表达式，和allow共同作用</li></ul><p>scrapy.Rule常见参数如下：</p><ul><li><strong>link_extractor</strong>: 一个LinkExtractor对象，用于定义需要提取的链接</li><li><strong>callback</strong>：从LinkExtractor中获取到链接时所调用的方法</li><li><strong>follow</strong>：指定根据该规则从response提取的链接是否需要跟进</li><li><strong>process_links</strong>：指定该spider中哪个函数将会被调用，从link_extractor中获取的链接列表时将会调用该函数，<strong>该方法主要用来过滤URL</strong></li><li><strong>process_request</strong>：指定该spider中哪个函数将会被调用，该规则提取到每个request时都调用该函数，<strong>主要用来过滤request</strong></li></ul><p><strong>注意</strong>：</p><blockquote><p>CrawlSpider中不能再有以parse为名字的数据提取方法，这个方法被用来实现基础url提取等功能</p></blockquote><p>案例如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># -*- coding: utf-8 -*-</span></span><br><span class="line"><span class="keyword">import</span> scrapy</span><br><span class="line"><span class="keyword">import</span> re</span><br><span class="line"><span class="keyword">from</span> scrapy.linkextractors <span class="keyword">import</span> LinkExtractor</span><br><span class="line"><span class="keyword">from</span> scrapy.spiders <span class="keyword">import</span> CrawlSpider, Rule</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ChufaSpider</span><span class="params">(CrawlSpider)</span>:</span></span><br><span class="line">    name = <span class="string">'chufa'</span></span><br><span class="line">    allowed_domains = [<span class="string">'circ.gov.cn'</span>]</span><br><span class="line">    start_urls = [<span class="string">'http://bxjg.circ.gov.cn/web/site0/tab5240/module14430/page1.htm'</span>]</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 定义提取URL地址规则</span></span><br><span class="line">    rules = ( </span><br><span class="line">        <span class="comment"># LinkExtractor 连接提取器，提取URL地址</span></span><br><span class="line">        <span class="comment"># callback 提取出来的URL地址的response会交给callback函数处理</span></span><br><span class="line">        <span class="comment"># follow 当前URL地址的响应是否重新经过rules来提取url地址</span></span><br><span class="line">        Rule(LinkExtractor(allow=<span class="string">r'/web/site0/tab5240/info\d+\.htm'</span>), callback=<span class="string">'parse_item'</span>),</span><br><span class="line">        Rule(LinkExtractor(allow=<span class="string">r'/web/site0/tab5240/module14430/page\d+\.htm'</span>), follow=<span class="literal">True</span>),</span><br><span class="line">    ) </span><br><span class="line"></span><br><span class="line">    <span class="comment"># parse函数有特殊功能，不能定义</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse_item</span><span class="params">(self, response)</span>:</span></span><br><span class="line">        item = &#123;&#125;</span><br><span class="line">        item[<span class="string">'title'</span>] = re.findall(<span class="string">r'&lt;!--TitleStart--&gt;(.*?)&lt;!--TitleEnd--&gt;'</span>, response.body.decode())[<span class="number">0</span>]</span><br><span class="line">        item[<span class="string">'publish_date'</span>] = re.findall(<span class="string">r'发布时间：(20\d&#123;2&#125;-\d&#123;2&#125;-\d&#123;2&#125;)'</span>, response.body.decode())[<span class="number">0</span>]</span><br><span class="line">        print(item)</span><br></pre></td></tr></table></figure><p>该案例中的Rule一个用于获取子页面URL进行请求，另一个用于翻页</p><p><strong>注意点</strong>：</p><blockquote><ul><li>url地址不完整，crawlspider会自动补充完整之后在请求</li><li>parse函数不能定义，他有特殊的功能需要实现</li><li>callback：连接提取器提取出来的url地址对应的响应交给他处理</li><li>follow：连接提取器提取出来的url地址对应的响应是否继续被rules来过滤</li></ul></blockquote>]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>爬虫</tag>
      </tags>
  </entry>
  <entry>
    <title>RestTemplate</title>
    <url>/spring/d0047044-eebf-4892-b657-c29b27f93574.html</url>
    <content><![CDATA[<p>本文介绍 Spring Boot RestTemplate。</p><a id="more"></a><h2 id="RestTemplate"><a href="#RestTemplate" class="headerlink" title="RestTemplate"></a>RestTemplate</h2><p>带参 post 请求（<code>postForEntity</code> 方法）</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">RestTemplate restTemplate = <span class="keyword">new</span> RestTemplate();</span><br><span class="line">HashMap&lt;String, String&gt; map = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">map.put(<span class="string">"credential"</span>, userTemp.username);</span><br><span class="line">map.put(<span class="string">"password"</span>, userTemp.password);</span><br><span class="line">ResponseEntity&lt;String&gt; ret = restTemplate.postForEntity(url, map, String<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">MyResponse pojo = JSON.parseObject(ret.getBody(), MyResponse<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">System.out.println(pojo.getData());</span><br></pre></td></tr></table></figure><p>无参 get 请求图片（<code>exchange</code> 方法）：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ResponseEntity&lt;<span class="keyword">byte</span>[]&gt; responseEntity = <span class="keyword">new</span> RestTemplate().exchange(url, HttpMethod.GET, <span class="keyword">null</span>, <span class="keyword">byte</span>[]<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line"><span class="keyword">byte</span>[] body = responseEntity.getBody();</span><br></pre></td></tr></table></figure><p>带参 post 请求（<code>exchange</code> 方法）：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">HttpHeaders headers = <span class="keyword">new</span> HttpHeaders();</span><br><span class="line">MimeType mimeType = MimeTypeUtils.parseMimeType(<span class="string">"application/json"</span>);</span><br><span class="line">MediaType mediaType = <span class="keyword">new</span> MediaType(mimeType.getType(), mimeType.getSubtype(), StandardCharsets.UTF_8);</span><br><span class="line">headers.setContentType(mediaType);</span><br><span class="line">Map&lt;String, String&gt; params = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">params.put(<span class="string">"phoneNumber"</span>, param.getPhoneNumber());</span><br><span class="line">params.put(<span class="string">"title"</span>, param.getTitle());</span><br><span class="line">params.put(<span class="string">"userName"</span>, param.getUserName());</span><br><span class="line">params.put(<span class="string">"time"</span>, param.getTime());</span><br><span class="line">params.put(<span class="string">"note"</span>, param.getNote());</span><br><span class="line">params.put(<span class="string">"remark"</span>, param.getRemark());</span><br><span class="line">HttpEntity&lt;Object&gt; entity = <span class="keyword">new</span> HttpEntity&lt;&gt;(params, headers);</span><br><span class="line">RestTemplate restTemplate = <span class="keyword">new</span> RestTemplate();</span><br><span class="line">ResponseEntity&lt;String&gt; response = restTemplate.exchange(url, HttpMethod.POST, entity, String<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">ret = JSON.parseObject(response.getBody(), DingdongResult<span class="class">.<span class="keyword">class</span>)</span>;</span><br></pre></td></tr></table></figure><p>post 带参请求，并获取 cookie（<code>postForEntity</code> 方法）：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">HashMap&lt;String, String&gt; map = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">map.put(<span class="string">"credential"</span>, userTemp.username);</span><br><span class="line">map.put(<span class="string">"password"</span>, userTemp.password);</span><br><span class="line">ResponseEntity&lt;String&gt; ret = restTemplate.postForEntity(url, map, String<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line"><span class="comment">// 设置登录后的 cookie</span></span><br><span class="line">List&lt;String&gt; header = ret.getHeaders().get(<span class="string">"Set-Cookie"</span>);</span><br></pre></td></tr></table></figure><p>get 请求带 cookie （<code>exchange</code> 方法）：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">HttpHeaders headers = <span class="keyword">new</span> HttpHeaders();</span><br><span class="line">MimeType mimeType = MimeTypeUtils.parseMimeType(<span class="string">"application/json"</span>);</span><br><span class="line">MediaType mediaType = <span class="keyword">new</span> MediaType(mimeType.getType(), mimeType.getSubtype(), StandardCharsets.UTF_8);</span><br><span class="line">headers.setContentType(mediaType);</span><br><span class="line">headers.addAll(<span class="string">"Cookie"</span>, header);</span><br><span class="line">HttpEntity&lt;String&gt; entity = <span class="keyword">new</span> HttpEntity&lt;&gt;(headers);</span><br><span class="line">ResponseEntity&lt;String&gt; response = restTemplate.exchange(url, HttpMethod.GET, entity, String<span class="class">.<span class="keyword">class</span>)</span>;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>Spring Boot</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Boot 集成 Swagger</title>
    <url>/spring/37e08633-e7d4-4c3b-8611-4a2255e6bb51.html</url>
    <content><![CDATA[<p>Swagger™的目标是为REST APIs 定义一个标准的，与语言无关的接口，使人和计算机在看不到源码或者看不到文档或者不能通过网络流量检测的情况下能发现和理解各种服务的功能。当服务通过Swagger定义，消费者就能与远程的服务互动通过少量的实现逻辑。类似于低级编程接口，Swagger去掉了调用服务时的很多猜测。<br>浏览 Swagger-Spec 去了解更多关于Swagger 项目的信息，包括附加的支持其他语言的库。本文介绍在 Spring Boot 中集成 Swagger。</p><a id="more"></a><h2 id="Swagger3"><a href="#Swagger3" class="headerlink" title="Swagger3"></a>Swagger3</h2><p><strong>注意 Spring boot 版本需为 2.2.X 以上版本</strong>。</p><p>pom.xml 中加入依赖：</p><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>io.springfox<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>springfox-boot-starter<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.0.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure><p>启动类中加入 <code>@EnableOpenApi</code> 注解，如下：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@EnableOpenApi</span></span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Application</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SpringApplication.run(Application<span class="class">.<span class="keyword">class</span>, <span class="title">args</span>)</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Controller 类上使用 <code>@Api</code> 注解，方法上使用 <code>@ApiOperation</code> 注解，如下：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Api</span>(tags=<span class="string">"用户管理"</span>)</span><br><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UserController</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ApiOperation</span>(<span class="string">"创建用户"</span>)</span><br><span class="line">    <span class="meta">@PostMapping</span>(<span class="string">"/users"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> User <span class="title">create</span><span class="params">(@RequestBody @Valid User user)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> user;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ApiOperation</span>(<span class="string">"用户详情"</span>)</span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"/users/&#123;id&#125;"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> User <span class="title">findById</span><span class="params">(@PathVariable Long id)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> User(<span class="string">"bbb"</span>, <span class="number">21</span>, <span class="string">"上海"</span>, <span class="string">"aaa@bbb.com"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ApiOperation</span>(<span class="string">"用户列表"</span>)</span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"/users"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> List&lt;User&gt; <span class="title">list</span><span class="params">(@ApiParam(<span class="string">"查看第几页"</span>)</span> @RequestParam <span class="keyword">int</span> pageIndex,</span></span><br><span class="line"><span class="function">                           @<span class="title">ApiParam</span><span class="params">(<span class="string">"每页多少条"</span>)</span> @RequestParam <span class="keyword">int</span> pageSize) </span>&#123;</span><br><span class="line">        List&lt;User&gt; result = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        result.add(<span class="keyword">new</span> User(<span class="string">"aaa"</span>, <span class="number">50</span>, <span class="string">"北京"</span>, <span class="string">"aaa@ccc.com"</span>));</span><br><span class="line">        result.add(<span class="keyword">new</span> User(<span class="string">"bbb"</span>, <span class="number">21</span>, <span class="string">"广州"</span>, <span class="string">"aaa@ddd.com"</span>));</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ApiIgnore</span></span><br><span class="line">    <span class="meta">@DeleteMapping</span>(<span class="string">"/users/&#123;id&#125;"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">deleteById</span><span class="params">(@PathVariable Long id)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"delete user : "</span> + id;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Domain 类上使用 <code>@ApiModel</code> 注解，属性上使用 <code>@ApiModelProperty</code> 注解，如下：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="meta">@NoArgsConstructor</span></span><br><span class="line"><span class="meta">@AllArgsConstructor</span></span><br><span class="line"><span class="meta">@ApiModel</span>(<span class="string">"用户基本信息"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ApiModelProperty</span>(<span class="string">"姓名"</span>)</span><br><span class="line">    <span class="meta">@Size</span>(max = <span class="number">20</span>)</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="meta">@ApiModelProperty</span>(<span class="string">"年龄"</span>)</span><br><span class="line">    <span class="meta">@Max</span>(<span class="number">150</span>)</span><br><span class="line">    <span class="meta">@Min</span>(<span class="number">1</span>)</span><br><span class="line">    <span class="keyword">private</span> Integer age;</span><br><span class="line">    <span class="meta">@NotNull</span></span><br><span class="line">    <span class="keyword">private</span> String address;</span><br><span class="line">    <span class="meta">@Pattern</span>(regexp = <span class="string">"^[a-zA-Z0-9_-]+@[a-zA-Z0-9_-]+(\\.[a-zA-Z0-9_-]+)+$"</span>)</span><br><span class="line">    <span class="keyword">private</span> String email;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>启动后访问 <a href="http://localhost:8080/swagger-ui/index.html" target="_blank" rel="noopener">http://localhost:8080/swagger-ui/index.html</a> 即可看到 swagger3 页面</p><p><strong>注意：生产环境中，要关闭Swagger</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">application.properties中配置:</span><br><span class="line">springfox.documentation.swagger-ui.enabled&#x3D;false</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>Spring Boot</tag>
        <tag>Swagger</tag>
      </tags>
  </entry>
  <entry>
    <title>Markdown 语法</title>
    <url>/other/39882391-33f1-4c32-89ca-39e0d28f1461.html</url>
    <content><![CDATA[<p>Markdown 是一种轻量级标记语言，它允许人们使用易读易写的纯文本格式编写文档，然后转换成有效的 XHTML（或者HTML）文档。本文介绍 Markdown 语法。</p><a id="more"></a><h2 id="标题"><a href="#标题" class="headerlink" title="标题"></a>标题</h2><p>Markdown 标题支持两种形式的语法：<strong>类 Setext</strong> 和 <strong>类 Atx 形式</strong></p><h3 id="类-Setext"><a href="#类-Setext" class="headerlink" title="类 Setext"></a>类 Setext</h3><p>类 Setext 形式是使用底线的形式，利用 <code>=</code> (最高阶标题) 和 <code>-</code> (第二阶标题)，例如：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">这是一级标题</span><br><span class="line">&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;</span><br><span class="line">这是二级标题</span><br><span class="line">-----------</span><br></pre></td></tr></table></figure><p>效果与 HTML 下 <strong>一级标题（h1 标签）</strong> 与 <strong>二级标题（h2 标签）</strong> 相同</p><h3 id="类-Atx"><a href="#类-Atx" class="headerlink" title="类 Atx"></a>类 Atx</h3><p>类 Atx 形式是使用 <code>#</code> 来设置标题。一个 <code>#</code> 为一级标题，两个 <code>#</code> 为二级标题，依此类推，最多 6 个 <code>#</code> 用于设置六级标题。</p><p>可以选择性地「闭合」类 atx 样式的标题，这纯粹只是美观用的。若是觉得这样看起来比较舒适，你就可以在行尾加上 <code>#</code>，而行尾的 <code>#</code> 数量也不用和开头一样。</p><p>示例：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 这是一级标题</span><br><span class="line">## 这是二级标题</span><br><span class="line">### 这是三级标题</span><br><span class="line">#### 这是四级标题</span><br><span class="line">##### 这是五级标题</span><br><span class="line">###### 这是六级标题 ######</span><br></pre></td></tr></table></figure><p>效果与 HTML 下的标题相同</p><h2 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h2><p>在引用的文字前加 <code>&gt;</code> 即可，引用也可以嵌套，如加两个或三个 <code>&gt;</code>。引用的区块中也可以使用其他的 Markdowm 语法，包括标题、列表等。</p><p>语法如下所示：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&gt; 引用内容</span><br><span class="line">&gt; 引用内容</span><br><span class="line">&gt;&gt;&gt; 引用内容</span><br></pre></td></tr></table></figure><p>效果如下：</p><blockquote><p>引用内容<br>引用内容</p><blockquote><blockquote><p>引用内容</p></blockquote></blockquote></blockquote><h2 id="列表"><a href="#列表" class="headerlink" title="列表"></a>列表</h2><p>Markdown 支持 <strong>有序列表</strong> 和 <strong>无序列表</strong></p><h3 id="无序列表"><a href="#无序列表" class="headerlink" title="无序列表"></a>无序列表</h3><p>无序列表用 <code>-</code>、<code>+</code>、<code>*</code> 作为列表标记</p><p>如下所示：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">-   内容 1</span><br><span class="line">+   内容 2</span><br><span class="line">*   内容 3</span><br></pre></td></tr></table></figure><p>效果如下：</p><ul><li>内容 1</li></ul><ul><li>内容 2</li></ul><ul><li>内容 3</li></ul><h3 id="有序列表"><a href="#有序列表" class="headerlink" title="有序列表"></a>有序列表</h3><p>语法为 <strong>数字加点</strong> (可以不按顺序来，从第一个数字开始)，如下所示：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">2.  内容 1</span><br><span class="line">1.  内容 2</span><br><span class="line">1.  内容 3</span><br></pre></td></tr></table></figure><p>效果如下：<br>2. 内容 1</p><ol><li>内容 2</li><li>内容 3</li></ol><h3 id="列表嵌套"><a href="#列表嵌套" class="headerlink" title="列表嵌套"></a>列表嵌套</h3><p>要将列表嵌套，下级列表的列表符需在上级列表符空格后（通常用制表符）。</p><p>语法如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">-   内容 1</span><br><span class="line">    -    嵌套内容</span><br><span class="line">-   内容 2</span><br></pre></td></tr></table></figure><p>效果如下：</p><ul><li>内容 1<ul><li>嵌套内容</li></ul></li><li>内容 2</li></ul><h2 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h2><h3 id="行内代码"><a href="#行内代码" class="headerlink" title="行内代码"></a>行内代码</h3><p>在 Markdown 中使用行内代码只需将代码用 <strong>反引号</strong> 引起来即可。<strong>注：如果要在代码中使用反引号，则可以用多个反引号来包裹代码。</strong></p><p>语法如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#96;printf(&quot;Hello World&quot;)&#96;</span><br><span class="line">&#96;&#96;printf(&quot;&#96;&quot;);&#96;&#96;</span><br><span class="line">&#96;&#96; &#96;printf&#96; &#96;&#96;</span><br></pre></td></tr></table></figure><p>效果如下：<br><code>printf(&quot;Hello World&quot;)</code><br><code>printf(&quot;`&quot;);</code><br><code>`printf`</code></p><h3 id="代码块"><a href="#代码块" class="headerlink" title="代码块"></a>代码块</h3><p>语法：用 <strong>三对（或以上）反引号</strong> 引起来，其中可以指明语言类型，如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#96;&#96;&#96;c</span><br><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">int main(void)&#123;</span><br><span class="line">    printf(&quot;Hello World&quot;);</span><br><span class="line">&#125;</span><br><span class="line">&#96;&#96;&#96;</span><br></pre></td></tr></table></figure><p>效果如下：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span>&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"Hello World"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>或者使用缩进（没有语法高亮，一般不用）：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">#include&lt;stlib.h&gt;</span><br><span class="line">int main(void)&#123;</span><br><span class="line">    printf(&quot;Hello World&quot;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="分割线"><a href="#分割线" class="headerlink" title="分割线"></a>分割线</h2><p>在一行中用三个以上的 <code>*</code>、<code>-</code>、<code>_</code> 可以建立一个分割线，行内不能有其他东西。可以在其中插入空格。如下所示：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">- - -</span><br><span class="line">****</span><br><span class="line">----</span><br><span class="line">********</span><br><span class="line">_____________</span><br></pre></td></tr></table></figure><p>效果如下：</p><hr><hr><hr><hr><hr><h2 id="链接"><a href="#链接" class="headerlink" title="链接"></a>链接</h2><p>语法如下：<code>[显示文字](超链接 &quot;Title&quot;)</code>，其中 Title 可以省略。</p><p>如下所示：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">This is [an example](http:&#x2F;&#x2F;example.com&#x2F; &quot;Title&quot;) inline link.</span><br><span class="line">[This link](http:&#x2F;&#x2F;example.net&#x2F;) has no title attribute.</span><br></pre></td></tr></table></figure><p>效果如下：<br>This is <a href="http://example.com/" target="_blank" rel="noopener" title="Title">an example</a> inline link.<br><a href="http://example.net/" target="_blank" rel="noopener">This link</a> has no title attribute.</p><h2 id="文字效果"><a href="#文字效果" class="headerlink" title="文字效果"></a>文字效果</h2><p>Markdown 中的文字效果有：<strong>斜体</strong>、<strong>加粗</strong>、<strong>斜体加粗</strong>、<strong>删除</strong>。</p><p>语法格式如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">*single asterisks*</span><br><span class="line">_single underscores_</span><br><span class="line">**double asterisks**</span><br><span class="line">__double underscores__</span><br><span class="line">***triple asterisks***</span><br><span class="line">___triple underscores___</span><br><span class="line">~~delete line~~</span><br></pre></td></tr></table></figure><p>效果如下：<br><em>single asterisks</em><br><em>single underscores</em><br><strong>double asterisks</strong><br><strong>double underscores</strong><br><strong><em>triple asterisks</em></strong><br><strong><em>triple underscores</em></strong><br><del>delete line</del></p><h2 id="图片"><a href="#图片" class="headerlink" title="图片"></a>图片</h2><p>语法如下：<code>![Alt test](/path/to/img &quot;optional title&quot;)</code></p><p><strong>注意</strong>：Markdown 目前还不能指定图片的宽高，如果需要的话，可以使用普通的 <code>&lt;img&gt;</code> 标签。</p><h2 id="表格"><a href="#表格" class="headerlink" title="表格"></a>表格</h2><p>语法如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">表头|表头|表头</span><br><span class="line">--|:--:|--:</span><br><span class="line">内容|内容|内容</span><br><span class="line">内容|内容|内容</span><br><span class="line">内容|内容|内容</span><br></pre></td></tr></table></figure><p>第二行用于分割表头和内容，<code>-</code> 有一个就行，默认左对齐，两边加 <code>:</code> 表示居中，右边加 <code>:</code> 表示右对齐</p><p>效果如下：</p><table><thead><tr><th>表头</th><th align="center">表头</th><th align="right">表头</th></tr></thead><tbody><tr><td>内容</td><td align="center">内容</td><td align="right">内容</td></tr><tr><td>内容</td><td align="center">内容</td><td align="right">内容</td></tr><tr><td>内容</td><td align="center">内容</td><td align="right">内容</td></tr></tbody></table>]]></content>
      <categories>
        <category>Other</category>
      </categories>
      <tags>
        <tag>Markdown</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Boot 线程池</title>
    <url>/spring/4c329869-8c64-4942-8aef-3cb0fa91016c.html</url>
    <content><![CDATA[<p>本文介绍 Spring Boot 线程池。</p><a id="more"></a><h2 id="Spring-boot-线程池配置"><a href="#Spring-boot-线程池配置" class="headerlink" title="Spring boot 线程池配置"></a>Spring boot 线程池配置</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@EnableAsync</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AsyncConfiguration</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span>(<span class="string">"getPersonPropertyExecutor"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Executor <span class="title">doSomethingExecutor</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        ThreadPoolTaskExecutor executor = <span class="keyword">new</span> ThreadPoolTaskExecutor();</span><br><span class="line">        <span class="comment">// 核心线程数：线程池创建时候初始化的线程数</span></span><br><span class="line">        executor.setCorePoolSize(<span class="number">20</span>);</span><br><span class="line">        <span class="comment">// 最大线程数：线程池最大的线程数，只有在缓冲队列满了之后才会申请超过核心线程数的线程</span></span><br><span class="line">        executor.setMaxPoolSize(<span class="number">40</span>);</span><br><span class="line">        <span class="comment">// 缓冲队列：用来缓冲执行任务的队列</span></span><br><span class="line">        executor.setQueueCapacity(<span class="number">1000</span>);</span><br><span class="line">        <span class="comment">// 允许线程的空闲时间60秒：当超过了核心线程之外的线程在空闲时间到达之后会被销毁</span></span><br><span class="line">        executor.setKeepAliveSeconds(<span class="number">60</span>);</span><br><span class="line">        <span class="comment">// 线程池名的前缀：设置好了之后可以方便我们定位处理任务所在的线程池</span></span><br><span class="line">        executor.setThreadNamePrefix(<span class="string">"get-per-prop"</span>);</span><br><span class="line">        <span class="comment">// 缓冲队列满了之后的拒绝策略：由调用线程处理（一般是主线程）</span></span><br><span class="line">        executor.setRejectedExecutionHandler(<span class="keyword">new</span> ThreadPoolExecutor.DiscardPolicy());</span><br><span class="line">        executor.initialize();</span><br><span class="line">        <span class="keyword">return</span> executor;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="Service-异步执行方法"><a href="#Service-异步执行方法" class="headerlink" title="Service 异步执行方法"></a>Service 异步执行方法</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Slf</span>4j</span><br><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AsyncService</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Async</span>(<span class="string">"getPersonPropertyExecutor"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">testAsync</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        log.info(<span class="string">"testAsync"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Async</span>(<span class="string">"getPersonPropertyExecutor"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">downLoadPic</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            String filename = UUID.randomUUID().toString().replace(<span class="string">"-"</span>, <span class="string">""</span>);</span><br><span class="line">            String url = <span class="string">"http://www.gravatar.com/avatar/"</span>+filename+<span class="string">"?d=identicon"</span>;</span><br><span class="line">            ResponseEntity&lt;<span class="keyword">byte</span>[]&gt; responseEntity = <span class="keyword">new</span> RestTemplate().exchange(url, HttpMethod.GET, <span class="keyword">null</span>, <span class="keyword">byte</span>[]<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">            <span class="keyword">byte</span>[] body = responseEntity.getBody();</span><br><span class="line">            writepic(filename, body);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 图片写入</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">writepic</span><span class="params">(String fileName, <span class="keyword">byte</span>[] picData)</span> </span>&#123;</span><br><span class="line">        String filePath = <span class="string">"C:/avatars/"</span> + fileName + <span class="string">".png"</span>;</span><br><span class="line">        File file = <span class="keyword">new</span> File(filePath);</span><br><span class="line">        FileOutputStream out = <span class="keyword">null</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (!file.exists()) &#123;</span><br><span class="line">                <span class="keyword">boolean</span> newFile = file.createNewFile();</span><br><span class="line">                <span class="keyword">if</span> (!newFile)&#123;</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(<span class="string">""</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            out = <span class="keyword">new</span> FileOutputStream(file);</span><br><span class="line">            out.write(picData);</span><br><span class="line">            log.info(filePath);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (out != <span class="keyword">null</span>) &#123;</span><br><span class="line">                    out.close();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (out != <span class="keyword">null</span>) &#123;</span><br><span class="line">                    out.close();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 对象写入</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">writeObjectToFile</span><span class="params">(Object obj)</span> </span>&#123;</span><br><span class="line">        File file = <span class="keyword">new</span> File(<span class="string">"./test.txt"</span>);</span><br><span class="line">        FileOutputStream out = <span class="keyword">null</span>;</span><br><span class="line">        OutputStreamWriter osw = <span class="keyword">null</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (!file.exists()) &#123;</span><br><span class="line">                <span class="keyword">boolean</span> newFile = file.createNewFile();</span><br><span class="line">                out = <span class="keyword">new</span> FileOutputStream(file);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                out = <span class="keyword">new</span> FileOutputStream(file, <span class="keyword">true</span>);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            osw = <span class="keyword">new</span> OutputStreamWriter(out, StandardCharsets.UTF_8);</span><br><span class="line">            osw.write(obj.toString());</span><br><span class="line">            log.info(<span class="string">"write success!"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            log.error(<span class="string">"wirte failed"</span>);</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (osw != <span class="keyword">null</span>) &#123;</span><br><span class="line">                    osw.close();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (out != <span class="keyword">null</span>) &#123;</span><br><span class="line">                    out.close();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>Spring Boot</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Boot 整合 JSP</title>
    <url>/spring/b817e6e8-dc9e-4e66-a8b1-5bc3e5bc2616.html</url>
    <content><![CDATA[<p>虽然 Spring Boot 默认的模版引擎为 Thymeleaf，但是由于 JSP 仍然是目前比较流行的技术，因此有时候需要将 JSP 整合进 Spring Boot 当中。本文将以 IDEA 为例。</p><a id="more"></a><h2 id="webapp"><a href="#webapp" class="headerlink" title="webapp"></a>webapp</h2><p>传统的 tomcat 项目中需要有一个 <code>webapp</code> 目录，因此我们需要手动添加这个目录，并将其标记为 <code>Web</code> 目录。</p><p>在 <code>src/main</code> 目录下新建一个 <code>webapp</code> 目录，如下：</p><p><img src="https://pic.downk.cc/item/5f643339160a154a67259f3a.jpg" alt=""></p><p>打开 <code>Project Structure (Ctrl + Alt + Shift + S)</code>，将该目录标记为 Web 目录，如下：</p><p><img src="https://pic.downk.cc/item/5f6433d7160a154a6725bbd7.jpg" alt=""></p><p>标记完成之后，在 IDEA 中该目录图标会发生变化：</p><p><img src="https://pic.downk.cc/item/5f643429160a154a6725ca07.jpg" alt=""></p><p>至此，完成 webapp 目录的添加。</p><h2 id="POM"><a href="#POM" class="headerlink" title="POM"></a>POM</h2><h3 id="引入依赖"><a href="#引入依赖" class="headerlink" title="引入依赖"></a>引入依赖</h3><p>由于 JSP 不是 Spring Boot 默认的模版引擎，因此需引入 JSP 的依赖以解析 JSP 页面。</p><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.tomcat.embed<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>tomcat-embed-jasper<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="指定编译路径"><a href="#指定编译路径" class="headerlink" title="指定编译路径"></a>指定编译路径</h3><p>要让 Spring Boot 集成 JSP，需要手动指定 JSP 最后编译的路径，需要在 <code>build</code> 标签下加入如下配置：</p><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">resources</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">resource</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 源目录 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">directory</span>&gt;</span>src/main/webapp<span class="tag">&lt;/<span class="name">directory</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 指定编译到 META-INF/resources --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">targetPath</span>&gt;</span>META-INF/resources<span class="tag">&lt;/<span class="name">targetPath</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 指定源目录中的哪个资源要编译 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">includes</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">include</span>&gt;</span>*.*<span class="tag">&lt;/<span class="name">include</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">includes</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">resource</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">resources</span>&gt;</span></span><br></pre></td></tr></table></figure><h2 id="视图解析器"><a href="#视图解析器" class="headerlink" title="视图解析器"></a>视图解析器</h2><p>在 Spring Boot 的配置文件 <code>application.yml</code> 加入如下配置：</p><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">mvc:</span></span><br><span class="line">    <span class="attr">view:</span></span><br><span class="line">      <span class="attr">prefix:</span> <span class="string">/</span></span><br><span class="line">      <span class="attr">suffix:</span> <span class="string">.jsp</span></span><br></pre></td></tr></table></figure><p>至此，JSP 已成功整合进 Spring Boot 中。</p><h2 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h2><h3 id="Controller"><a href="#Controller" class="headerlink" title="Controller"></a>Controller</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyController</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@RequestMapping</span>(<span class="string">"/say"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> ModelAndView <span class="title">say</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        ModelAndView mv = <span class="keyword">new</span> ModelAndView(<span class="string">"say"</span>);</span><br><span class="line">        mv.addObject(<span class="string">"message"</span>, <span class="string">"Hello, JSP"</span>);</span><br><span class="line">        <span class="keyword">return</span> mv;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="JSP"><a href="#JSP" class="headerlink" title="JSP"></a>JSP</h3><p><code>say.jsp</code>：</p><figure class="highlight"><table><tr><td class="code"><pre><span class="line">&lt;%@ page contentType=<span class="string">"text/html;charset=UTF-8"</span> language=<span class="string">"java"</span> %&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">    &lt;title&gt;Title&lt;/title&gt;</span><br><span class="line">&lt;/head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">&lt;h1&gt;$&#123;message&#125;&lt;/h1&gt;</span><br><span class="line">&lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure><h3 id="启动"><a href="#启动" class="headerlink" title="启动"></a>启动</h3><p>启动 Spring Boot，打开浏览器访问 <a href="localhost:8080/say">localhost:8080/say</a>，如下：</p><p><img src="https://pic.downk.cc/item/5f6438bd160a154a6726d196.jpg" alt=""></p>]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>Spring Boot</tag>
        <tag>JSP</tag>
      </tags>
  </entry>
  <entry>
    <title>MongoDB 的简单使用</title>
    <url>/mongodb/56876896-7cd9-488e-b0ef-74d1a9c5b198.html</url>
    <content><![CDATA[<p>本文介绍 MongoDB 的简单使用。</p><a id="more"></a><p>给数据库创建用户(记得先切换数据库，记得修改其中的 <code>test</code>)： <code>db.createUser({user: &quot;root&quot;, pwd: &quot;123456&quot;, roles: [{ role: &quot;dbOwner&quot;, db: &quot;test&quot; }]})</code></p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 查看所有数据库</span></span><br><span class="line">show dbs</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用某个数据库</span></span><br><span class="line">use runoob</span><br><span class="line">use test</span><br><span class="line"></span><br><span class="line"><span class="comment">// 删除数据库</span></span><br><span class="line">db.dropDatabase()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 查看集合</span></span><br><span class="line">show tables</span><br><span class="line">show collections</span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建集合</span></span><br><span class="line">db.createCollection(<span class="string">"mgtest"</span>)</span><br><span class="line"><span class="comment">// 创建固定集合，整个集合空间大小为 6142800KB，文档最大个数为 10000 个</span></span><br><span class="line">db.createCollection(<span class="string">"mycol"</span>, &#123;capped: <span class="literal">true</span>, autoIndexId: <span class="literal">true</span>, size: <span class="number">6142800</span>, max: <span class="number">10000</span>&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 删除集合</span></span><br><span class="line">db.mycol.drop()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 集合中插入文档 (如果集合不存在会自动创建)</span></span><br><span class="line">db.mgtest.insert(&#123;</span><br><span class="line">    title: 'MongoDB 教程',</span><br><span class="line">    description: 'MongoDB 是一个 Nosql 数据库',</span><br><span class="line">    by: '菜鸟教程',</span><br><span class="line">    url: 'http://www.runoob.com',</span><br><span class="line">    tags: ['mongodb', 'database', 'NoSQL'],</span><br><span class="line">    likes: <span class="number">100</span></span><br><span class="line">&#125;)</span><br><span class="line"><span class="comment">// 插入一条数据</span></span><br><span class="line">db.mgtest.insertOne(&#123;<span class="string">"a"</span>: <span class="number">3</span>&#125;)</span><br><span class="line"><span class="comment">// 插入多条数据</span></span><br><span class="line">db.mgtest.insertMany([&#123;<span class="string">"a"</span>: <span class="number">5</span>&#125;, &#123;<span class="string">"a"</span>: <span class="number">4</span>&#125;])</span><br><span class="line">db.mgtest.insert([&#123;<span class="string">"a"</span>: <span class="number">5</span>&#125;, &#123;<span class="string">"a"</span>: <span class="number">4</span>&#125;])</span><br><span class="line"><span class="comment">// 先定义为变量再进行插入操作</span></span><br><span class="line">document = (&#123;</span><br><span class="line">    title: 'MongoDB 教程',</span><br><span class="line">    description: 'MongoDB 是一个 Nosql 数据库',</span><br><span class="line">    by: '菜鸟教程',</span><br><span class="line">    url: 'http://www.runoob.com',</span><br><span class="line">    tags: ['mongodb', 'database', 'NoSQL'],</span><br><span class="line">    likes: <span class="number">100</span></span><br><span class="line">&#125;)</span><br><span class="line">db.mgtest.insert(document)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 查看集合</span></span><br><span class="line">db.mgtest.find()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 更新文档(更新文档中的标题)</span></span><br><span class="line">db.mgtest.update(&#123;'title': 'MongoDB 教程'&#125;, &#123;$set: &#123;'title': 'MongoDB'&#125;&#125;)</span><br><span class="line"><span class="comment">// 从 3.2 版本开始，MongoDB，提供以下更新集合的方法：updateOne、updateMany</span></span><br><span class="line">db.mgtest.insert([</span><br><span class="line">    &#123;<span class="string">"name"</span>: <span class="string">"abc"</span>, <span class="string">"age"</span>: <span class="string">"25"</span>, <span class="string">"status"</span>: <span class="string">"zxc"</span>&#125;,</span><br><span class="line">    &#123;<span class="string">"name"</span>: <span class="string">"dec"</span>, <span class="string">"age"</span>: <span class="string">"19"</span>, <span class="string">"status"</span>: <span class="string">"qwe"</span>&#125;,</span><br><span class="line">    &#123;<span class="string">"name"</span>: <span class="string">"asd"</span>, <span class="string">"age"</span>: <span class="string">"30"</span>, <span class="string">"status"</span>: <span class="string">"nmn"</span>&#125;,</span><br><span class="line">])</span><br><span class="line">db.mgtest.updateOne(&#123;<span class="string">"name"</span>: <span class="string">"abc"</span>&#125;, &#123;$<span class="built_in">set</span>: &#123;<span class="string">"age"</span>: <span class="string">"28"</span>&#125;&#125;)</span><br><span class="line">db.mgtest.updateMany(&#123;<span class="string">"age"</span>: &#123;$gt: <span class="string">"10"</span>&#125;&#125;, &#123;$<span class="built_in">set</span>: &#123;<span class="string">"status"</span>: <span class="string">"xyz"</span>&#125;&#125;)</span><br><span class="line">db.mgtest.update(&#123;"title": "MongoDB"&#125;, &#123;$unset: &#123;'name2': ''&#125;&#125;)</span><br><span class="line">db.mgtest.update(&#123;'title': 'MongoDB'&#125;, &#123;$set: &#123;'name2': '1234'&#125;&#125;, &#123;upsert: true&#125;)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">db.mgtest.find()</span><br><span class="line"><span class="comment">// 删除文档 (remove 方法已经过时)</span></span><br><span class="line">db.mgtest.remove(&#123;'title': 'MongoDB'&#125;)</span><br><span class="line"><span class="comment">// 删除集合中的所有文档</span></span><br><span class="line">db.mgtest.remove(&#123;&#125;)</span><br><span class="line"><span class="comment">// deleteOne、deleteMany</span></span><br><span class="line">db.mgtest.deleteOne(&#123;'likes': &#123;$gt: 1&#125;&#125;)</span><br><span class="line">db.mgtest.deleteMany(&#123;'title': 'MongoDB 教程'&#125;)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// OR 查询</span></span><br><span class="line">db.mgtest.find(&#123;$<span class="keyword">or</span>: [&#123;<span class="string">"by"</span>: <span class="string">"菜鸟教程"</span>&#125;, &#123;<span class="string">"title"</span>: <span class="string">"MongoDB 教程"</span>&#125;]&#125;)</span><br><span class="line"><span class="comment">// AND 和 OR 联合 （likes &gt; 50 &amp;&amp; (by == 'xx' || title == 'xx')）</span></span><br><span class="line">db.mgtest.find(&#123;<span class="string">"likes"</span>: &#123;$gt: <span class="number">50</span>&#125;, $<span class="keyword">or</span>: [&#123;<span class="string">"by"</span>: <span class="string">"菜鸟教程"</span>&#125;, &#123;<span class="string">"title"</span>: <span class="string">"MongoDB 教程"</span>&#125;]&#125;)</span><br><span class="line"><span class="comment">// _id 默认为 1</span></span><br><span class="line">db.mgtest.find(&#123;&#125;, &#123;title: <span class="number">1</span>, name: <span class="number">1</span>&#125;)</span><br><span class="line">db.mgtest.find(&#123;&#125;, &#123;title: <span class="number">0</span>, name: <span class="number">0</span>&#125;)</span><br><span class="line">db.mgtest.find(&#123;likes: &#123;$lt: <span class="number">200</span>, $gte: <span class="number">100</span>&#125;&#125;)</span><br><span class="line"><span class="comment">// 模糊查询：查询 title 中包含 o 的文档</span></span><br><span class="line">db.mgtest.find(&#123;<span class="string">"title"</span>: /o/&#125;)</span><br><span class="line"><span class="comment">// 模糊查询：查询 title 中以 M 开头的文档</span></span><br><span class="line">db.mgtest.find(&#123;<span class="string">"title"</span>: /^M/&#125;)</span><br><span class="line"><span class="comment">// 模糊查询：查询 title 中以 B 结尾的文档</span></span><br><span class="line">db.mgtest.find(&#123;<span class="string">"title"</span>: /B$/&#125;)</span><br><span class="line"><span class="comment">// 查询集合中 title 为 字符串的文档</span></span><br><span class="line">db.mgtest.find(&#123;'title': &#123;$type: 'string'&#125;&#125;)</span><br><span class="line"><span class="comment">// limit 限定条数 相当于分页中的 rows</span></span><br><span class="line">db.mgtest.find().limit(<span class="number">5</span>)</span><br><span class="line"><span class="comment">// skip 跳过 相当于分页中的 start</span></span><br><span class="line">db.mgtest.find().skip(<span class="number">6</span>).limit(<span class="number">3</span>)</span><br><span class="line"><span class="comment">// 排序 1 为升序，-1 为降序</span></span><br><span class="line">db.mgtest.find().sort(&#123;_id: <span class="number">-1</span>&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 聚合</span></span><br><span class="line">db.mgtest.aggregate([&#123;$group : &#123;_id : <span class="string">"$title"</span>, count : &#123;$sum : <span class="number">1</span>&#125;&#125;&#125;])</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * $sum	计算总和。	db.mycol.aggregate([&#123;$group : &#123;_id : "$by_user", num_tutorial : &#123;$sum : "$likes"&#125;&#125;&#125;])</span></span><br><span class="line"><span class="comment"> * $avg	计算平均值	db.mycol.aggregate([&#123;$group : &#123;_id : "$by_user", num_tutorial : &#123;$avg : "$likes"&#125;&#125;&#125;])</span></span><br><span class="line"><span class="comment"> * $min	获取集合中所有文档对应值得最小值。	db.mycol.aggregate([&#123;$group : &#123;_id : "$by_user", num_tutorial : &#123;$min : "$likes"&#125;&#125;&#125;])</span></span><br><span class="line"><span class="comment"> * $max	获取集合中所有文档对应值得最大值。	db.mycol.aggregate([&#123;$group : &#123;_id : "$by_user", num_tutorial : &#123;$max : "$likes"&#125;&#125;&#125;])</span></span><br><span class="line"><span class="comment"> * $push	在结果文档中插入值到一个数组中。	db.mycol.aggregate([&#123;$group : &#123;_id : "$by_user", url : &#123;$push: "$url"&#125;&#125;&#125;])</span></span><br><span class="line"><span class="comment"> * $addToSet	在结果文档中插入值到一个数组中，但不创建副本。	db.mycol.aggregate([&#123;$group : &#123;_id : "$by_user", url : &#123;$addToSet : "$url"&#125;&#125;&#125;])</span></span><br><span class="line"><span class="comment"> * $first	根据资源文档的排序获取第一个文档数据。	db.mycol.aggregate([&#123;$group : &#123;_id : "$by_user", first_url : &#123;$first : "$url"&#125;&#125;&#125;])</span></span><br><span class="line"><span class="comment"> * $last	根据资源文档的排序获取最后一个文档数据	db.mycol.aggregate([&#123;$group : &#123;_id : "$by_user", last_url : &#123;$last : "$url"&#125;&#125;&#125;])</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 管道</span></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * $project：修改输入文档的结构。可以用来重命名、增加或删除域，也可以用于创建计算结果以及嵌套文档。</span></span><br><span class="line"><span class="comment"> * $match：用于过滤数据，只输出符合条件的文档。$match使用MongoDB的标准查询操作。</span></span><br><span class="line"><span class="comment"> * $limit：用来限制MongoDB聚合管道返回的文档数。</span></span><br><span class="line"><span class="comment"> * $skip：在聚合管道中跳过指定数量的文档，并返回余下的文档。</span></span><br><span class="line"><span class="comment"> * $unwind：将文档中的某一个数组类型字段拆分成多条，每条包含数组中的一个值。</span></span><br><span class="line"><span class="comment"> * $group：将集合中的文档分组，可用于统计结果。</span></span><br><span class="line"><span class="comment"> * $sort：将输入文档排序后输出。</span></span><br><span class="line"><span class="comment"> * $geoNear：输出接近某一地理位置的有序文档。</span></span><br><span class="line"><span class="comment"> */</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Database</category>
      </categories>
      <tags>
        <tag>MongoDB</tag>
      </tags>
  </entry>
  <entry>
    <title>常用 SQL</title>
    <url>/sql/4636f9c3-2eb6-489f-8f38-85fe394886ee.html</url>
    <content><![CDATA[<p>SQL (Structured Query Language): 结构化查询语言。SQL 定义了操作所有关系型数据库的规则。本文介绍常用的 SQL。</p><a id="more"></a><p><strong>SQL 通用语法</strong>：</p><ul><li>SQL 语句可以单行或多行书写，以分号结尾。</li><li>可使用空格和缩进来增强语句的可读性。</li><li>SQL 语句不区分大小写，关键字建议使用大写。</li><li>注释<ul><li>单行注释: <code>-- 注释内容</code></li><li>多行注释: <code>/* 注释 */</code></li></ul></li></ul><p><strong>SQL 分为以下四种</strong>：</p><ul><li><strong>DDL</strong> (Data Definition Language)：数据定义语言。</li><li><strong>DML</strong> (Data Manipulation Language)：数据操作语言。</li><li><strong>DQL</strong> (Data Query Language)：数据查询语言。</li><li><strong>DCL</strong> (Data Control Language)：数据控制语言。</li></ul><h2 id="DDL"><a href="#DDL" class="headerlink" title="DDL"></a>DDL</h2><p><strong>DDL</strong>(Data Definition Language)，数据定义语言，用来 <strong>定义数据库对象</strong>，如：数据库，表，列等。</p><h3 id="操作数据库"><a href="#操作数据库" class="headerlink" title="操作数据库"></a>操作数据库</h3><ul><li><strong>创建</strong> 数据库：<code>create database 数据库名称;</code></li><li>创建数据库，<strong>判断不存在</strong>，再创建：<code>create database if not exists 数据库名称;</code></li><li>创建数据库，并 <strong>指定字符集</strong>：<code>create database 数据库名称 character set 字符集名;</code></li><li><strong>查询</strong> 所有数据库的名称：<code>show databases;</code></li><li>查询某个数据库的 <strong>创建语句</strong>：<code>show create database 数据库名称;</code></li><li><strong>修改</strong> 数据库的 <strong>字符集</strong>：<code>alter database 数据库名称 character set 字符集名称;</code></li><li><strong>删除</strong> 数据库：<code>drop database 数据库名称;</code></li><li><strong>判断数据库是否存在</strong>，存在再删除：<code>drop database if exists 数据库名称;</code></li><li>查询 <strong>当前正在使用</strong> 的数据库名称：<code>select database();</code></li><li><strong>选择使用</strong> 数据库：<code>use 数据库名称;</code></li></ul><p>具体操作如下：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 直接创建数据库 db1</span></span><br><span class="line"><span class="keyword">create</span> <span class="keyword">database</span> db1; </span><br><span class="line"> </span><br><span class="line"><span class="comment">-- 判断是否存在，如果不存在则创建数据库 db2</span></span><br><span class="line"><span class="keyword">create</span> <span class="keyword">database</span> <span class="keyword">if</span> <span class="keyword">not</span> <span class="keyword">exists</span> db2; </span><br><span class="line"> </span><br><span class="line"><span class="comment">-- 创建数据库并指定字符集为 gbk</span></span><br><span class="line"><span class="keyword">create</span> <span class="keyword">database</span> db3 <span class="keyword">default</span> <span class="built_in">character</span> <span class="keyword">set</span> gbk; </span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查看某个数据库的定义信息</span></span><br><span class="line"><span class="keyword">show</span> <span class="keyword">create</span> <span class="keyword">database</span> db3;</span><br><span class="line"><span class="keyword">show</span> <span class="keyword">create</span> <span class="keyword">database</span> db1; </span><br><span class="line"></span><br><span class="line"><span class="comment">-- 将 db3 数据库的字符集改成 utf8</span></span><br><span class="line"><span class="keyword">alter</span> <span class="keyword">database</span> db3 <span class="built_in">character</span> <span class="keyword">set</span> utf8;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 删除 db2 数据库</span></span><br><span class="line"><span class="keyword">drop</span> <span class="keyword">database</span> db2;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查看正在使用的数据库</span></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">database</span>(); </span><br><span class="line"> </span><br><span class="line"><span class="comment">-- 改变要使用的数据库</span></span><br><span class="line"><span class="keyword">use</span> db4;</span><br></pre></td></tr></table></figure><h3 id="操作表"><a href="#操作表" class="headerlink" title="操作表"></a>操作表</h3><h4 id="数据类型"><a href="#数据类型" class="headerlink" title="数据类型"></a>数据类型</h4><p>数据库中的数据类型如下所示：</p><table><thead><tr><th align="center">类型名称</th><th>类型说明</th></tr></thead><tbody><tr><td align="center">tinyInt</td><td>微整型：很小的整数 (占 8 位二进制)</td></tr><tr><td align="center">smallint</td><td>小整型：小的整数 (占 16 位二进制)</td></tr><tr><td align="center">mediumint</td><td>中整型：中等长度的整数 (占 24 位二进制)</td></tr><tr><td align="center">int</td><td>整型：整数类型 (占 32 位二进制)</td></tr><tr><td align="center">float</td><td>单精度浮点数，占 4 个字节</td></tr><tr><td align="center">double</td><td>双精度浮点数，占 8 个字节</td></tr><tr><td align="center">time</td><td>表示时间类型</td></tr><tr><td align="center">date</td><td>表示日期类型</td></tr><tr><td align="center">datetime</td><td>同时表示日期和时间</td></tr><tr><td align="center">char(M)</td><td>固定长度的字符串，无论使用几个字符都占满全部，M 为 0~255 之间的整数</td></tr><tr><td align="center">varchar(M)</td><td>可变长度的字符串，使用几个字符就占用几个，M 为 0~65535 之间的整数</td></tr><tr><td align="center">tinyblob</td><td>允许长度 0~255 字节</td></tr><tr><td align="center">blob</td><td>允许长度 0~65535 字节</td></tr><tr><td align="center">mediumblob</td><td>允许长度 0~167772150 字节</td></tr><tr><td align="center">longblob</td><td>允许长度 0~4294967295 字节</td></tr><tr><td align="center">tinytext</td><td>文本类型，允许长度 0~255 字节</td></tr><tr><td align="center">text</td><td>允许长度 0~65535 字节</td></tr><tr><td align="center">mediumtext</td><td>允许长度 0~167772150 字节</td></tr><tr><td align="center">longtext</td><td>允许长度 0~4294967295 字节</td></tr></tbody></table><h4 id="创建表"><a href="#创建表" class="headerlink" title="创建表"></a>创建表</h4><p>语法如下：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">table</span> <span class="string">`表名`</span>(</span><br><span class="line">  列名<span class="number">1</span> 数据类型<span class="number">1</span>,</span><br><span class="line">  列名<span class="number">2</span> 数据类型<span class="number">2</span>,</span><br><span class="line">  ....</span><br><span class="line">  列名n 数据类型n</span><br><span class="line">);</span><br></pre></td></tr></table></figure><p><strong>注意：最后一列，不需要加逗号(,)；使用反引号是为了避免和其他关键字产生冲突</strong></p><p>如创建一个 student 表:</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">table</span> student(</span><br><span class="line">  <span class="keyword">id</span> <span class="built_in">int</span> <span class="keyword">COMMENT</span> <span class="string">'编号'</span>,</span><br><span class="line">  <span class="keyword">name</span> <span class="built_in">varchar</span>(<span class="number">32</span>),</span><br><span class="line">  age <span class="built_in">int</span>,</span><br><span class="line">  sex <span class="built_in">int</span> <span class="keyword">check</span>(sex=<span class="number">1</span> <span class="keyword">or</span> sex=<span class="number">0</span>),</span><br><span class="line">  score <span class="keyword">double</span>(<span class="number">4</span>,<span class="number">1</span>),</span><br><span class="line">  birthday <span class="built_in">date</span>,</span><br><span class="line">  insert_time <span class="built_in">timestamp</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure><h4 id="复制表"><a href="#复制表" class="headerlink" title="复制表"></a>复制表</h4><p>命令如下：<code>create table 表名 like 被复制的表名;</code></p><h4 id="查询表"><a href="#查询表" class="headerlink" title="查询表"></a>查询表</h4><ul><li>查询某个数据库中 <strong>所有表</strong> 名称：<code>show tables;</code></li><li>查询 <strong>创建表的命令</strong>：<code>show create table `表名`;</code></li><li>查询 <strong>表结构</strong>：<code>desc `表名`;</code></li></ul><h4 id="修改表"><a href="#修改表" class="headerlink" title="修改表"></a>修改表</h4><ul><li>修改 <strong>表名</strong>：<code>alter table `表名` rename to `新表名`;</code></li><li>修改表的<strong>字符集</strong>：<code>alter table `表名` character set 字符集名称;</code></li><li><strong>添加一列</strong>：<code>alter table `表名` add `列名` 数据类型;</code></li><li>修改<strong>列名和类型</strong><ul><li><code>alter table `表名` change `列名` `新列名` 新数据类型;</code></li><li><code>alter table `表名` modify `列名` 新数据类型;</code></li></ul></li><li><strong>删除列</strong>：<code>alter table `表名` drop `列名`;</code></li></ul><h4 id="删除表"><a href="#删除表" class="headerlink" title="删除表"></a>删除表</h4><p>命令如下：</p><ul><li><code>drop table `表名`;</code></li><li><code>drop table if exists `表名`;</code></li></ul><h2 id="DML"><a href="#DML" class="headerlink" title="DML"></a>DML</h2><p><strong>DML</strong> (Data Manipulation Language)，数据操作语言，用来对数据库中表的数据进行增删改。</p><h3 id="添加数据"><a href="#添加数据" class="headerlink" title="添加数据"></a>添加数据</h3><p>语法如下：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">insert</span> <span class="keyword">into</span> 表名 (列名<span class="number">1</span>,列名<span class="number">2</span>,...列名n) <span class="keyword">values</span> (值<span class="number">1</span>,值<span class="number">2</span>,...值n);</span><br></pre></td></tr></table></figure><p><strong>注意</strong>：</p><ul><li>列名和值要一一对应；</li><li>如果表名后不指定列名，则需要给所有列添加值：<code>insert into 表名 values(值1,值2,...值n);</code></li><li>除了数字类型，其他类型需要使用引号（单双都可以）引起来。</li></ul><h3 id="删除数据"><a href="#删除数据" class="headerlink" title="删除数据"></a>删除数据</h3><p>语法如下：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">delete</span> <span class="keyword">from</span> 表名 [<span class="keyword">where</span> 条件]</span><br></pre></td></tr></table></figure><p><strong>注意</strong>：</p><ul><li>如果不加条件，则删除表中所有记录。</li><li>如果要删除所有记录：<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">delete</span> <span class="keyword">from</span> 表名; <span class="comment">-- 不推荐使用。有多少条记录就会执行多少次删除操作，效率低下。</span></span><br><span class="line"><span class="keyword">TRUNCATE</span> <span class="keyword">TABLE</span> 表名; <span class="comment">-- 推荐使用，效率更高 先删除表，然后再创建一张一样的表。</span></span><br></pre></td></tr></table></figure></li></ul><h3 id="修改数据"><a href="#修改数据" class="headerlink" title="修改数据"></a>修改数据</h3><p>语法如下：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">update</span> 表名 <span class="keyword">set</span> 列名<span class="number">1</span> = 值<span class="number">1</span>, 列名<span class="number">2</span> = 值<span class="number">2</span>,... [<span class="keyword">where</span> 条件];</span><br></pre></td></tr></table></figure><p><strong>注意</strong>：</p><ul><li>如果不加任何条件，则会将表中所有记录全部修改。</li></ul><h2 id="DQL"><a href="#DQL" class="headerlink" title="DQL"></a>DQL</h2><p><strong>DQL</strong>(Data Query Language)，数据查询语言，用来查询数据库中表的记录 (数据) 。</p><p>查询基本语法如下：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> 字段列表</span><br><span class="line"><span class="keyword">from</span> 表名列表</span><br><span class="line"><span class="keyword">where</span> 条件列表</span><br><span class="line"><span class="keyword">group</span> <span class="keyword">by</span> 分组字段</span><br><span class="line"><span class="keyword">having</span> 分组之后的条件</span><br><span class="line"><span class="keyword">order</span> <span class="keyword">by</span> 排序</span><br><span class="line"><span class="keyword">limit</span> 分页限定;</span><br></pre></td></tr></table></figure><h3 id="基础查询"><a href="#基础查询" class="headerlink" title="基础查询"></a>基础查询</h3><ul><li><p>多个字段的查询：<code>select 字段名1，字段名2... from 表名；</code><br><strong>注意</strong>：如果查询所有字段，则可以使用 <code>*</code> 来替代字段列表，如：<code>select * from 表名</code>。</p></li><li><p>去除重复：<code>distinct</code> 可以消除重复行，如：<code>select distinct gender from student;</code></p></li><li><p>起别名：<br><code>as</code>：as 也可以省略</p></li></ul><h3 id="条件查询"><a href="#条件查询" class="headerlink" title="条件查询"></a>条件查询</h3><p><code>where</code> 子句后可跟条件，如比较运算符、<code>between...and</code> 等。</p><h4 id="比较运算符"><a href="#比较运算符" class="headerlink" title="比较运算符"></a>比较运算符</h4><p>SQL 中的比较运算符有： <code>&gt;</code>、<code>&lt;</code>、<code>&lt;=</code>、<code>&gt;=</code>、<code>=</code>、<code>&lt;&gt;</code></p><p>如查询 student 表中年龄大于 20 岁的人:</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查询年龄大于 20 岁</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> age &gt; <span class="number">20</span>;</span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> age &gt;= <span class="number">20</span>;</span><br><span class="line"><span class="comment">-- 查询年龄等于 20 岁</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> age = <span class="number">20</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询年龄不等于 20 岁</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> age != <span class="number">20</span>;</span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> age &lt;&gt; <span class="number">20</span>;</span><br></pre></td></tr></table></figure><h4 id="BETWEEN…AND"><a href="#BETWEEN…AND" class="headerlink" title="BETWEEN…AND"></a>BETWEEN…AND</h4><p>如查询 student 表中年龄为 20~30 之间的学生</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> age <span class="keyword">BETWEEN</span> <span class="number">20</span> <span class="keyword">AND</span> <span class="number">30</span>;</span><br></pre></td></tr></table></figure><h4 id="IN"><a href="#IN" class="headerlink" title="IN"></a>IN</h4><p><code>IN</code> 关键字可以用于指定一个集合。</p><p>如查询 student 表中年龄在 22, 18, 25 这些值中的学生：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> age <span class="keyword">IN</span> (<span class="number">22</span>,<span class="number">18</span>,<span class="number">25</span>);</span><br></pre></td></tr></table></figure><h4 id="LIKE"><a href="#LIKE" class="headerlink" title="LIKE"></a>LIKE</h4><p><code>LIKE</code> 关键字用于模糊查询，可以使用<strong>占位符</strong>，如：</p><ul><li><code>_</code>: 单个任意字符</li><li><code>%</code>: 多个任意字符</li></ul><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查询姓马的人</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> <span class="keyword">NAME</span> <span class="keyword">LIKE</span> <span class="string">'马%'</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询姓名第二个字是化的人</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> <span class="keyword">NAME</span> <span class="keyword">LIKE</span> <span class="string">"_化%"</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询姓名是 3 个字的人</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> <span class="keyword">NAME</span> <span class="keyword">LIKE</span> <span class="string">'___'</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询姓名中包含德的人</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> <span class="keyword">NAME</span> <span class="keyword">LIKE</span> <span class="string">'%德%'</span>;</span><br></pre></td></tr></table></figure><h4 id="IS-NULL"><a href="#IS-NULL" class="headerlink" title="IS NULL"></a>IS NULL</h4><p><code>IS NULL</code> 关键字用于指定空字段</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查询英语成绩为 null 的学生</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> english = <span class="literal">NULL</span>; <span class="comment">-- 不对, null值不能使用=/!=来判断</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> english <span class="keyword">IS</span> <span class="literal">NULL</span>;</span><br></pre></td></tr></table></figure><h4 id="逻辑控制"><a href="#逻辑控制" class="headerlink" title="逻辑控制"></a>逻辑控制</h4><p>和编程语言类似，SQL 中可以使用 <code>&amp;&amp; / and</code>、<code>|| / or</code>、<code>! / not</code> 来表示 <strong>与或非</strong></p><p>如下：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查询年龄在 20~30 岁之间的学生j</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> age &gt;= <span class="number">20</span> &amp;&amp; age &lt;=<span class="number">30</span>;</span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> age &gt;= <span class="number">20</span> <span class="keyword">AND</span> age &lt;=<span class="number">30</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询年龄 22 岁，18 岁，25 岁的信息</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> age = <span class="number">22</span> <span class="keyword">OR</span> age = <span class="number">18</span> <span class="keyword">OR</span> age = <span class="number">25</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询英语成绩不为 null</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> english <span class="keyword">IS</span> <span class="keyword">NOT</span> <span class="literal">NULL</span>;</span><br></pre></td></tr></table></figure><h3 id="排序查询"><a href="#排序查询" class="headerlink" title="排序查询"></a>排序查询</h3><p>为了方便查看数据，可以对数据进行 <strong>排序</strong>。</p><p>语法：<code>order by 子句</code></p><p>语法格式如下：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> 表名</span><br><span class="line"><span class="keyword">order</span> <span class="keyword">by</span> 列<span class="number">1</span> <span class="keyword">asc</span>|<span class="keyword">desc</span>,列<span class="number">2</span> <span class="keyword">asc</span>|<span class="keyword">desc</span>,...</span><br></pre></td></tr></table></figure><p><strong>注意</strong>：如果有多个排序条件，只有当前边的条件值一样时，才会判断第二条件。</p><p>排序方式如下：</p><ul><li>ASC：升序，默认。</li><li>DESC：降序。</li></ul><p>如：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> <span class="keyword">id</span>,<span class="keyword">name</span>,gender <span class="keyword">from</span> student</span><br><span class="line"><span class="keyword">order</span> <span class="keyword">by</span> <span class="keyword">name</span>;</span><br></pre></td></tr></table></figure><p>结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">+----+-----------+--------+</span><br><span class="line">| id | name      | gender |</span><br><span class="line">+----+-----------+--------+</span><br><span class="line">|  6 | LaoWang   | Men    |</span><br><span class="line">|  1 | XiaoHong  | Women  |</span><br><span class="line">|  8 | XiaoHua   | Women  |</span><br><span class="line">|  3 | XiaoWang  | Men    |</span><br><span class="line">|  9 | XiaoYan   | Women  |</span><br><span class="line">| 11 | XiaoZhang | Men    |</span><br><span class="line">|  4 | XiaoZhao  | Men    |</span><br><span class="line">+----+-----------+--------+</span><br><span class="line">7 rows in set (0.00 sec)</span><br></pre></td></tr></table></figure><h3 id="聚合函数"><a href="#聚合函数" class="headerlink" title="聚合函数"></a>聚合函数</h3><p>为了快速得到统计数据，SQL 中提供了 5 个 <strong>聚合函数</strong></p><h4 id="count"><a href="#count" class="headerlink" title="count"></a>count</h4><p><code>count</code> 表示计算总行数，括号中写星与列名，结果是相同的。</p><p>如：<code>select count(*) from student;</code></p><h4 id="max"><a href="#max" class="headerlink" title="max"></a>max</h4><p><code>max</code> 表示某一列的最大值。</p><p>如：<code>select max(id) from student;</code></p><h4 id="min"><a href="#min" class="headerlink" title="min"></a>min</h4><p><code>min</code> 表示求某一列的最小值。</p><p>如：<code>select min(id) from student;</code></p><h4 id="sum"><a href="#sum" class="headerlink" title="sum"></a>sum</h4><p><code>sum</code> 表示求某一列的和。</p><p>如：<code>select sum(id) from student where gender=&#39;Men&#39;;</code></p><h4 id="avg"><a href="#avg" class="headerlink" title="avg"></a>avg</h4><p><code>avg</code> 表示求此列的平均值。</p><p>如：<code>select arg(id) from student where isDelete=0;</code></p><h3 id="分组查询"><a href="#分组查询" class="headerlink" title="分组查询"></a>分组查询</h3><p>语法：<code>group by 分组字段</code></p><p><em>注意</em>：</p><ol><li>where 在分组之前进行限定，如果不满足条件，则不参与分组。having 在分组之后进行限定，如果不满足结果，则不会被查询出来</li><li>where 后不可以跟聚合函数，having 可以进行聚合函数的判断。</li></ol><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 按照性别分组。分别查询男、女同学的平均分</span></span><br><span class="line"><span class="keyword">SELECT</span> sex, <span class="keyword">AVG</span>(math) <span class="keyword">FROM</span> student <span class="keyword">GROUP</span> <span class="keyword">BY</span> sex;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 按照性别分组。分别查询男、女同学的平均分,人数</span></span><br><span class="line"><span class="keyword">SELECT</span> sex, <span class="keyword">AVG</span>(math),<span class="keyword">COUNT</span>(<span class="keyword">id</span>) <span class="keyword">FROM</span> student <span class="keyword">GROUP</span> <span class="keyword">BY</span> sex;</span><br><span class="line"></span><br><span class="line"><span class="comment">--  按照性别分组。分别查询男、女同学的平均分,人数 要求：分数低于 70 分的人，不参与分组</span></span><br><span class="line"><span class="keyword">SELECT</span> sex, <span class="keyword">AVG</span>(math),<span class="keyword">COUNT</span>(<span class="keyword">id</span>) <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> math &gt; <span class="number">70</span> <span class="keyword">GROUP</span> <span class="keyword">BY</span> sex;</span><br><span class="line"></span><br><span class="line"><span class="comment">--  按照性别分组。分别查询男、女同学的平均分,人数 要求：分数低于 70 分的人，不参与分组,分组之后。人数要大于2个人</span></span><br><span class="line"><span class="keyword">SELECT</span> sex, <span class="keyword">AVG</span>(math),<span class="keyword">COUNT</span>(<span class="keyword">id</span>) <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> math &gt; <span class="number">70</span> <span class="keyword">GROUP</span> <span class="keyword">BY</span> sex <span class="keyword">HAVING</span> <span class="keyword">COUNT</span>(<span class="keyword">id</span>) &gt; <span class="number">2</span>;</span><br><span class="line"><span class="keyword">SELECT</span> sex, <span class="keyword">AVG</span>(math),<span class="keyword">COUNT</span>(<span class="keyword">id</span>) <span class="keyword">as</span> 人数 <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> math &gt; <span class="number">70</span> <span class="keyword">GROUP</span> <span class="keyword">BY</span> sex <span class="keyword">HAVING</span> 人数 &gt; <span class="number">2</span>;</span><br></pre></td></tr></table></figure><h2 id="DCL"><a href="#DCL" class="headerlink" title="DCL"></a>DCL</h2><p><strong>DCL</strong>(Data Control Language)，数据控制语言，用来定义数据库的 <strong>访问权限和安全级别</strong>，以及 <strong>创建用户</strong>。</p><h3 id="管理用户"><a href="#管理用户" class="headerlink" title="管理用户"></a>管理用户</h3><h4 id="添加用户"><a href="#添加用户" class="headerlink" title="添加用户"></a>添加用户</h4><p>语法：<code>CREATE USER &#39;用户名&#39;@&#39;主机名&#39; IDENTIFIED BY &#39;密码&#39;;</code></p><h4 id="删除用户"><a href="#删除用户" class="headerlink" title="删除用户"></a>删除用户</h4><p>语法：<code>DROP USER &#39;用户名&#39;@&#39;主机名&#39;;</code></p><h4 id="修改用户密码"><a href="#修改用户密码" class="headerlink" title="修改用户密码"></a>修改用户密码</h4><p>如下所示：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">UPDATE</span> <span class="keyword">USER</span> <span class="keyword">SET</span> <span class="keyword">PASSWORD</span> = <span class="keyword">PASSWORD</span>(<span class="string">'新密码'</span>) <span class="keyword">WHERE</span> <span class="keyword">USER</span> = <span class="string">'用户名'</span>;</span><br><span class="line"><span class="keyword">UPDATE</span> <span class="keyword">USER</span> <span class="keyword">SET</span> <span class="keyword">PASSWORD</span> = <span class="keyword">PASSWORD</span>(<span class="string">'abc'</span>) <span class="keyword">WHERE</span> <span class="keyword">USER</span> = <span class="string">'lisi'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">SET</span> <span class="keyword">PASSWORD</span> <span class="keyword">FOR</span> <span class="string">'用户名'</span>@<span class="string">'主机名'</span> = <span class="keyword">PASSWORD</span>(<span class="string">'新密码'</span>);</span><br><span class="line"><span class="keyword">SET</span> <span class="keyword">PASSWORD</span> <span class="keyword">FOR</span> <span class="string">'root'</span>@<span class="string">'localhost'</span> = <span class="keyword">PASSWORD</span>(<span class="string">'123'</span>);</span><br></pre></td></tr></table></figure><h3 id="权限管理"><a href="#权限管理" class="headerlink" title="权限管理"></a>权限管理</h3><h4 id="查询权限"><a href="#查询权限" class="headerlink" title="查询权限"></a>查询权限</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查询权限</span></span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">GRANTS</span> <span class="keyword">FOR</span> <span class="string">'用户名'</span>@<span class="string">'主机名'</span>;</span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">GRANTS</span> <span class="keyword">FOR</span> <span class="string">'lisi'</span>@<span class="string">'%'</span>;</span><br></pre></td></tr></table></figure><h4 id="授予权限"><a href="#授予权限" class="headerlink" title="授予权限"></a>授予权限</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 授予权限</span></span><br><span class="line"><span class="keyword">grant</span> 权限列表 <span class="keyword">on</span> 数据库名.表名 <span class="keyword">to</span> <span class="string">'用户名'</span>@<span class="string">'主机名'</span>;</span><br><span class="line"><span class="comment">-- 给张三用户授予所有权限，在任意数据库任意表上</span></span><br><span class="line"><span class="keyword">GRANT</span> <span class="keyword">ALL</span> <span class="keyword">ON</span> *.* <span class="keyword">TO</span> <span class="string">'zhangsan'</span>@<span class="string">'localhost'</span>;</span><br></pre></td></tr></table></figure><h4 id="撤销权限"><a href="#撤销权限" class="headerlink" title="撤销权限"></a>撤销权限</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 撤销权限：</span></span><br><span class="line"><span class="keyword">revoke</span> 权限列表 <span class="keyword">on</span> 数据库名.表名 <span class="keyword">from</span> <span class="string">'用户名'</span>@<span class="string">'主机名'</span>;</span><br><span class="line"><span class="keyword">REVOKE</span> <span class="keyword">UPDATE</span> <span class="keyword">ON</span> db3.<span class="string">`account`</span> <span class="keyword">FROM</span> <span class="string">'lisi'</span>@<span class="string">'%'</span>;</span><br></pre></td></tr></table></figure><h2 id="约束"><a href="#约束" class="headerlink" title="约束"></a>约束</h2><p>约束的概念：<strong>对表中的数据进行限定</strong>，以保证数据的正确性、有效性和完整性。</p><p><strong>约束分为</strong>：</p><ul><li>主键约束</li><li>非空约束</li><li>唯一约束</li><li>外键约束</li></ul><h3 id="主键约束"><a href="#主键约束" class="headerlink" title="主键约束"></a>主键约束</h3><p>关键字：<code>primary key</code></p><p><strong>注意</strong>：</p><ul><li>含义：非空且唯一</li><li>一张表<strong>只能有一个字段</strong>为主键</li><li>主键就是表中记录的<strong>唯一标识</strong></li></ul><h4 id="创建表时定义"><a href="#创建表时定义" class="headerlink" title="创建表时定义"></a>创建表时定义</h4><p>创建表时，给字段添加主键约束，直接在字段后加上 <code>primary key</code> 即可，如下：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">table</span> stu(</span><br><span class="line">  <span class="keyword">id</span> <span class="built_in">int</span> primary <span class="keyword">key</span>,   <span class="comment">-- 给id添加主键约束</span></span><br><span class="line">  <span class="keyword">name</span> <span class="built_in">varchar</span>(<span class="number">20</span>)</span><br><span class="line">);</span><br></pre></td></tr></table></figure><h4 id="删除主键"><a href="#删除主键" class="headerlink" title="删除主键"></a>删除主键</h4><p>删除主键如下：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> 表名 <span class="keyword">DROP</span> PRIMARY <span class="keyword">KEY</span>;</span><br></pre></td></tr></table></figure><h4 id="创建表后添加"><a href="#创建表后添加" class="headerlink" title="创建表后添加"></a>创建表后添加</h4><p>创建完表后，添加主键：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> 表名 <span class="keyword">MODIFY</span> 列名 <span class="built_in">INT</span> PRIMARY <span class="keyword">KEY</span>;  <span class="comment">-- 默认主键名称为列名</span></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> 表名 <span class="keyword">ADD</span> <span class="keyword">CONSTRAINT</span> 主键名称 PRIMARY <span class="keyword">KEY</span>(列名);</span><br></pre></td></tr></table></figure><h4 id="自动增长"><a href="#自动增长" class="headerlink" title="自动增长"></a>自动增长</h4><p>通常情况下，在插入数据时不指定主键的值，而是使用默认的值，因此主键常常使用<strong>自动增长</strong><br>概念如下：如果某一列是数值类型的，使用 <code>auto_increment</code> 可以来完成值得自动增长</p><ul><li>在创建表时，添加主键约束，并且完成主键自增长<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">table</span> stu(</span><br><span class="line">  <span class="keyword">id</span> <span class="built_in">int</span> primary <span class="keyword">key</span> auto_increment,  <span class="comment">-- 主键自动增长</span></span><br><span class="line">  <span class="keyword">name</span> <span class="built_in">varchar</span>(<span class="number">20</span>)</span><br><span class="line">);</span><br></pre></td></tr></table></figure></li><li>删除自动增长<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> 表名 <span class="keyword">MODIFY</span> 列名 数据类型;  <span class="comment">-- 就是重新调整列</span></span><br></pre></td></tr></table></figure></li><li>添加自动增长<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> 表名 <span class="keyword">MODIFY</span> 列名 数据类型 AUTO_INCREMENT;</span><br></pre></td></tr></table></figure></li></ul><h3 id="非空约束"><a href="#非空约束" class="headerlink" title="非空约束"></a>非空约束</h3><p>关键字：<code>not null</code></p><h4 id="创建表时指定"><a href="#创建表时指定" class="headerlink" title="创建表时指定"></a>创建表时指定</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> stu(</span><br><span class="line">  <span class="keyword">id</span> <span class="built_in">INT</span>,</span><br><span class="line">  <span class="keyword">NAME</span> <span class="built_in">VARCHAR</span>(<span class="number">20</span>) <span class="keyword">NOT</span> <span class="literal">NULL</span> <span class="comment">-- name为非空</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure><h4 id="删除约束"><a href="#删除约束" class="headerlink" title="删除约束"></a>删除约束</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">`<span class="keyword">ALTER</span> <span class="keyword">TABLE</span> 表名 <span class="keyword">MODIFY</span> 列名 数据类型;`</span><br></pre></td></tr></table></figure><h4 id="表创建后添加"><a href="#表创建后添加" class="headerlink" title="表创建后添加"></a>表创建后添加</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> 表名 <span class="keyword">MODIFY</span> 列名 数据类型 <span class="keyword">NOT</span> <span class="literal">NULL</span>;</span><br></pre></td></tr></table></figure><h3 id="唯一约束"><a href="#唯一约束" class="headerlink" title="唯一约束"></a>唯一约束</h3><p>关键字：<code>unique</code></p><h4 id="创建表时指定-1"><a href="#创建表时指定-1" class="headerlink" title="创建表时指定"></a>创建表时指定</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> stu(</span><br><span class="line">  <span class="keyword">id</span> <span class="built_in">INT</span>,</span><br><span class="line">  phone_number <span class="built_in">VARCHAR</span>(<span class="number">20</span>) <span class="keyword">UNIQUE</span> <span class="comment">-- 添加了唯一约束</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure><h4 id="删除约束-1"><a href="#删除约束-1" class="headerlink" title="删除约束"></a>删除约束</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> 表名 <span class="keyword">DROP</span> <span class="keyword">INDEX</span> 约束名(如果没指定默认为列名);</span><br></pre></td></tr></table></figure><h4 id="表创建后添加-1"><a href="#表创建后添加-1" class="headerlink" title="表创建后添加"></a>表创建后添加</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> 表名 <span class="keyword">MODIFY</span> 列名 数据类型 <span class="keyword">UNIQUE</span>;</span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> 表名 <span class="keyword">ADD</span> <span class="keyword">CONSTRAINT</span> 约束名 <span class="keyword">UNIQUE</span> <span class="keyword">KEY</span>(列名);</span><br></pre></td></tr></table></figure><h3 id="外键约束"><a href="#外键约束" class="headerlink" title="外键约束"></a>外键约束</h3><p>关键字：<code>foreign key</code></p><h4 id="创建表时指定-2"><a href="#创建表时指定-2" class="headerlink" title="创建表时指定"></a>创建表时指定</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">table</span> 表名(</span><br><span class="line">  ....</span><br><span class="line">  列...</span><br><span class="line">  <span class="keyword">constraint</span> 外键名称 <span class="keyword">foreign</span> <span class="keyword">key</span> (外键列名称) <span class="keyword">references</span> 主表名称(主表列名称)</span><br><span class="line">);</span><br></pre></td></tr></table></figure><h4 id="删除外键"><a href="#删除外键" class="headerlink" title="删除外键"></a>删除外键</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> 表名 <span class="keyword">DROP</span> <span class="keyword">FOREIGN</span> <span class="keyword">KEY</span> 外键名称;</span><br></pre></td></tr></table></figure><h4 id="表创建后添加-2"><a href="#表创建后添加-2" class="headerlink" title="表创建后添加"></a>表创建后添加</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> 表名 <span class="keyword">ADD</span> <span class="keyword">CONSTRAINT</span> 外键名称 <span class="keyword">FOREIGN</span> <span class="keyword">KEY</span> (外键字段名称) <span class="keyword">REFERENCES</span> 主表名称(主表列名称);</span><br></pre></td></tr></table></figure><h4 id="自关联"><a href="#自关联" class="headerlink" title="自关联"></a>自关联</h4><p>某个表中的某个键让自身的某个键作为这个键的外键，这种情况就是<strong>自关联</strong>。</p><p>自关联可以将多个表合为一体，减少数据库的开销 (每创建一个表的开销是比较大的) 。</p><p>如创建中国的区域表：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">table</span> areas(</span><br><span class="line">  <span class="keyword">id</span> <span class="built_in">int</span> primary <span class="keyword">key</span>,</span><br><span class="line">  atitle <span class="built_in">varchar</span>(<span class="number">20</span>),</span><br><span class="line">  pid <span class="built_in">int</span>,</span><br><span class="line">  <span class="keyword">foreign</span> <span class="keyword">key</span>(pid) <span class="keyword">references</span> areas(<span class="keyword">id</span>)</span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>其中的 pid 就是让 id 作为外键。</p><p>如查询山西省的所有市：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> city.* <span class="keyword">from</span> areas <span class="keyword">as</span> city</span><br><span class="line"><span class="keyword">inner</span> <span class="keyword">join</span> areas <span class="keyword">as</span> province <span class="keyword">on</span> city.pid=province.id</span><br><span class="line"><span class="keyword">where</span> province.atitle=<span class="string">"山西省"</span>;</span><br></pre></td></tr></table></figure><p>结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">+--------+-----------+--------+</span><br><span class="line">| id     | atitle    | pid    |</span><br><span class="line">+--------+-----------+--------+</span><br><span class="line">| 140100 | 太原市     | 140000 |</span><br><span class="line">| 140200 | 大同市     | 140000 |</span><br><span class="line">| 140300 | 阳泉市     | 140000 |</span><br><span class="line">| 140400 | 长治市     | 140000 |</span><br><span class="line">| 140500 | 晋城市     | 140000 |</span><br><span class="line">| 140600 | 朔州市     | 140000 |</span><br><span class="line">| 140700 | 晋中市     | 140000 |</span><br><span class="line">| 140800 | 运城市     | 140000 |</span><br><span class="line">| 140900 | 忻州市     | 140000 |</span><br><span class="line">| 141000 | 临汾市     | 140000 |</span><br><span class="line">| 141100 | 吕梁市     | 140000 |</span><br><span class="line">+--------+-----------+--------+</span><br><span class="line">11 rows in set (0.00 sec)</span><br></pre></td></tr></table></figure><p>查询广州市的所有区县：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> dis.*, dis2.* <span class="keyword">from</span> areas <span class="keyword">as</span> dis</span><br><span class="line"><span class="keyword">inner</span> <span class="keyword">join</span> areas <span class="keyword">as</span> city <span class="keyword">on</span> city.id=dis.pid</span><br><span class="line"><span class="keyword">inner</span> <span class="keyword">join</span> areas <span class="keyword">as</span> dis2 <span class="keyword">on</span> dis.id=dis2.pid</span><br><span class="line"><span class="keyword">where</span> city.atitle=<span class="string">"广州市"</span>;</span><br></pre></td></tr></table></figure><h3 id="级联操作"><a href="#级联操作" class="headerlink" title="级联操作"></a>级联操作</h3><p>在删除一个表的数据时，如果这个表的某个键在另一个表中被引用为外键并该值已经存在，则会抛异常。</p><p>推荐使用<strong>逻辑删除</strong> (设立一个 isDelete 字段)，可以解决这个问题。</p><p>可以在创建表时指定级联操作，也可以在创建表后再修改外键的级联操作。</p><h4 id="添加级联"><a href="#添加级联" class="headerlink" title="添加级联"></a>添加级联</h4><p>语法：<code>ALTER TABLE 表名 ADD CONSTRAINT FOREIGN KEY (外键字段名称) REFERENCES 主表名称(主表列名称) ON UPDATE|DELETE CASCADE;</code></p><h4 id="级联类型"><a href="#级联类型" class="headerlink" title="级联类型"></a>级联类型</h4><ul><li>restrict(限制)：默认值，抛异常</li><li>cascade(级联)：如果主表的记录删掉，则从表中相关联的记录都将被删除</li><li>set null：将外键设置为空</li><li>no action：什么都不做</li></ul><h2 id="多表查询"><a href="#多表查询" class="headerlink" title="多表查询"></a>多表查询</h2><p><strong>基本查询语法如下</strong>：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span></span><br><span class="line">  列名列表</span><br><span class="line"><span class="keyword">from</span></span><br><span class="line">  表名列表</span><br><span class="line">where....</span><br></pre></td></tr></table></figure><p>准备 sql 如下：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建部门表</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> dept(</span><br><span class="line">  <span class="keyword">id</span> <span class="built_in">INT</span> PRIMARY <span class="keyword">KEY</span> AUTO_INCREMENT,</span><br><span class="line">  <span class="keyword">NAME</span> <span class="built_in">VARCHAR</span>(<span class="number">20</span>)</span><br><span class="line">);</span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> dept (<span class="keyword">NAME</span>) <span class="keyword">VALUES</span> (<span class="string">'开发部'</span>),(<span class="string">'市场部'</span>),(<span class="string">'财务部'</span>);</span><br><span class="line"><span class="comment"># 创建员工表</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> emp (</span><br><span class="line">  <span class="keyword">id</span> <span class="built_in">INT</span> PRIMARY <span class="keyword">KEY</span> AUTO_INCREMENT,</span><br><span class="line">  <span class="keyword">NAME</span> <span class="built_in">VARCHAR</span>(<span class="number">10</span>),</span><br><span class="line">  gender <span class="built_in">CHAR</span>(<span class="number">1</span>), <span class="comment">-- 性别</span></span><br><span class="line">  salary <span class="keyword">DOUBLE</span>, <span class="comment">-- 工资</span></span><br><span class="line">  join_date <span class="built_in">DATE</span>, <span class="comment">-- 入职日期</span></span><br><span class="line">  dept_id <span class="built_in">INT</span>,</span><br><span class="line">  <span class="keyword">FOREIGN</span> <span class="keyword">KEY</span> (dept_id) <span class="keyword">REFERENCES</span> dept(<span class="keyword">id</span>) <span class="comment">-- 外键，关联部门表 (部门表的主键)</span></span><br><span class="line">);</span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> emp(<span class="keyword">NAME</span>,gender,salary,join_date,dept_id) <span class="keyword">VALUES</span>(<span class="string">'孙悟空'</span>,<span class="string">'男'</span>,<span class="number">7200</span>,<span class="string">'2013-02-24'</span>,<span class="number">1</span>);</span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> emp(<span class="keyword">NAME</span>,gender,salary,join_date,dept_id) <span class="keyword">VALUES</span>(<span class="string">'猪八戒'</span>,<span class="string">'男'</span>,<span class="number">3600</span>,<span class="string">'2010-12-02'</span>,<span class="number">2</span>);</span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> emp(<span class="keyword">NAME</span>,gender,salary,join_date,dept_id) <span class="keyword">VALUES</span>(<span class="string">'唐僧'</span>,<span class="string">'男'</span>,<span class="number">9000</span>,<span class="string">'2008-08-08'</span>,<span class="number">2</span>);</span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> emp(<span class="keyword">NAME</span>,gender,salary,join_date,dept_id) <span class="keyword">VALUES</span>(<span class="string">'白骨精'</span>,<span class="string">'女'</span>,<span class="number">5000</span>,<span class="string">'2015-10-07'</span>,<span class="number">3</span>);</span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> emp(<span class="keyword">NAME</span>,gender,salary,join_date,dept_id) <span class="keyword">VALUES</span>(<span class="string">'蜘蛛精'</span>,<span class="string">'女'</span>,<span class="number">4500</span>,<span class="string">'2011-03-14'</span>,<span class="number">1</span>);</span><br></pre></td></tr></table></figure><h3 id="内连接查询"><a href="#内连接查询" class="headerlink" title="内连接查询"></a>内连接查询</h3><h4 id="隐式内连接"><a href="#隐式内连接" class="headerlink" title="隐式内连接"></a>隐式内连接</h4><p>隐式内连接使用 where 条件消除无用数据<br>如下：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查询所有员工信息和对应的部门信息</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> emp,dept <span class="keyword">WHERE</span> emp.<span class="string">`dept_id`</span> = dept.<span class="string">`id`</span>;</span><br><span class="line"><span class="comment">-- 查询员工表的名称，性别。部门表的名称</span></span><br><span class="line"><span class="keyword">SELECT</span> emp.name,emp.gender,dept.name <span class="keyword">FROM</span> emp,dept <span class="keyword">WHERE</span> emp.<span class="string">`dept_id`</span> = dept.<span class="string">`id`</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span> </span><br><span class="line">  t1.name,    <span class="comment">-- 员工表的姓名</span></span><br><span class="line">  t1.gender,  <span class="comment">-- 员工表的性别</span></span><br><span class="line">  t2.name     <span class="comment">-- 部门表的名称</span></span><br><span class="line"><span class="keyword">FROM</span></span><br><span class="line">  emp t1,</span><br><span class="line">  dept t2</span><br><span class="line"><span class="keyword">WHERE</span> </span><br><span class="line">  t1.<span class="string">`dept_id`</span> = t2.<span class="string">`id`</span>;</span><br></pre></td></tr></table></figure><h4 id="显式内连接"><a href="#显式内连接" class="headerlink" title="显式内连接"></a>显式内连接</h4><p>语法：<code>select 字段列表 from 表名1 [inner] join 表名2 on 条件;</code><br>例如：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> emp <span class="keyword">INNER</span> <span class="keyword">JOIN</span> dept <span class="keyword">ON</span> emp.<span class="string">`dept_id`</span> = dept.<span class="string">`id`</span>;</span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> emp <span class="keyword">JOIN</span> dept <span class="keyword">ON</span> emp.<span class="string">`dept_id`</span> = dept.<span class="string">`id`</span>;</span><br></pre></td></tr></table></figure><h3 id="外链接查询"><a href="#外链接查询" class="headerlink" title="外链接查询"></a>外链接查询</h3><h4 id="左外连接"><a href="#左外连接" class="headerlink" title="左外连接"></a>左外连接</h4><p>语法：<code>select 字段列表 from 表1 left [outer] join 表2 on 条件;</code></p><p><strong>查询的是左表所有数据以及其交集部分。</strong></p><p>如下：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查询所有员工信息，如果员工有部门，则查询部门名称，没有部门，则不显示部门名称</span></span><br><span class="line"><span class="keyword">SELECT</span> 	t1.*,t2.<span class="string">`name`</span> <span class="keyword">FROM</span> emp t1 <span class="keyword">LEFT</span> <span class="keyword">JOIN</span> dept t2 <span class="keyword">ON</span> t1.<span class="string">`dept_id`</span> = t2.<span class="string">`id`</span>;</span><br></pre></td></tr></table></figure><h4 id="右外连接"><a href="#右外连接" class="headerlink" title="右外连接"></a>右外连接</h4><p>语法：<code>select 字段列表 from 表1 right [outer] join 表2 on 条件;</code></p><p><strong>查询的是右表所有数据以及其交集部分。</strong></p><p>如下：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> 	* <span class="keyword">FROM</span> dept t2 <span class="keyword">RIGHT</span> <span class="keyword">JOIN</span> emp t1 <span class="keyword">ON</span> t1.<span class="string">`dept_id`</span> = t2.<span class="string">`id`</span>;</span><br></pre></td></tr></table></figure><h3 id="子查询"><a href="#子查询" class="headerlink" title="子查询"></a>子查询</h3><p>概念：查询中 <strong>嵌套查询</strong>，称嵌套查询为子查询。</p><p>如下所示：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查询工资最高的员工信息</span></span><br><span class="line"><span class="comment">-- 1 查询最高的工资是多少 9000</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="keyword">MAX</span>(salary) <span class="keyword">FROM</span> emp;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 2 查询员工信息，并且工资等于 9000 的</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> emp.<span class="string">`salary`</span> = <span class="number">9000</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 一条 sql 就完成这个操作。子查询</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> emp.<span class="string">`salary`</span> = (<span class="keyword">SELECT</span> <span class="keyword">MAX</span>(salary) <span class="keyword">FROM</span> emp);</span><br></pre></td></tr></table></figure><p>子查询的结果是有多种的，不同的子查询结果有不同的处理方式。</p><h4 id="单行单列"><a href="#单行单列" class="headerlink" title="单行单列"></a>单行单列</h4><p>当子查询结果为单行单列时，子查询可以作为条件，使用运算符去判断。运算符：<code>&gt;</code>、<code>&gt;=</code>、<code>&lt;</code>、<code>&lt;=</code>、<code>=</code></p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查询员工工资小于平均工资的人</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> emp.salary &lt; (<span class="keyword">SELECT</span> <span class="keyword">AVG</span>(salary) <span class="keyword">FROM</span> emp);</span><br></pre></td></tr></table></figure><h4 id="多行单列"><a href="#多行单列" class="headerlink" title="多行单列"></a>多行单列</h4><p>当子查询的结果是多行单列时，这时子查询可以作为条件，使用关键字 <code>IN</code> 来判断</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查询「财务部」和「市场部」所有的员工信息</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="keyword">id</span> <span class="keyword">FROM</span> dept <span class="keyword">WHERE</span> <span class="keyword">NAME</span> = <span class="string">'财务部'</span> <span class="keyword">OR</span> <span class="keyword">NAME</span> = <span class="string">'市场部'</span>;</span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> dept_id = <span class="number">3</span> <span class="keyword">OR</span> dept_id = <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 子查询</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> dept_id <span class="keyword">IN</span> (<span class="keyword">SELECT</span> <span class="keyword">id</span> <span class="keyword">FROM</span> dept <span class="keyword">WHERE</span> <span class="keyword">NAME</span> = <span class="string">'财务部'</span> <span class="keyword">OR</span> <span class="keyword">NAME</span> = <span class="string">'市场部'</span>);</span><br></pre></td></tr></table></figure><h4 id="多行多列"><a href="#多行多列" class="headerlink" title="多行多列"></a>多行多列</h4><p>当子查询的结果为多行多列时，此时子查询可以作为一张虚拟表参与查询</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查询员工入职日期是 2011-11-11 日之后的员工信息和部门信息</span></span><br><span class="line"><span class="comment">-- 子查询</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> dept t1, (<span class="keyword">SELECT</span> * <span class="keyword">FROM</span> emp <span class="keyword">WHERE</span> emp.<span class="string">`join_date`</span> &gt; <span class="string">'2011-11-11'</span>) t2</span><br><span class="line"><span class="keyword">WHERE</span> t1.id = t2.dept_id;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 普通内连接</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> emp t1,dept t2 <span class="keyword">WHERE</span> t1.<span class="string">`dept_id`</span> = t2.<span class="string">`id`</span> <span class="keyword">AND</span> t1.<span class="string">`join_date`</span> &gt;  <span class="string">'2011-11-11'</span></span><br></pre></td></tr></table></figure><h3 id="多表查询练习"><a href="#多表查询练习" class="headerlink" title="多表查询练习"></a>多表查询练习</h3><p>数据库准备如下：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 部门表</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> dept (</span><br><span class="line">  <span class="keyword">id</span> <span class="built_in">INT</span> PRIMARY <span class="keyword">KEY</span> PRIMARY <span class="keyword">KEY</span>, <span class="comment">-- 部门id</span></span><br><span class="line">  dname <span class="built_in">VARCHAR</span>(<span class="number">50</span>), <span class="comment">-- 部门名称</span></span><br><span class="line">  loc <span class="built_in">VARCHAR</span>(<span class="number">50</span>) <span class="comment">-- 部门所在地</span></span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 添加 4 个部门</span></span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> dept(<span class="keyword">id</span>,dname,loc) <span class="keyword">VALUES</span> </span><br><span class="line">  (<span class="number">10</span>,<span class="string">'教研部'</span>,<span class="string">'北京'</span>),</span><br><span class="line">  (<span class="number">20</span>,<span class="string">'学工部'</span>,<span class="string">'上海'</span>),</span><br><span class="line">  (<span class="number">30</span>,<span class="string">'销售部'</span>,<span class="string">'广州'</span>),</span><br><span class="line">  (<span class="number">40</span>,<span class="string">'财务部'</span>,<span class="string">'深圳'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 职务表，职务名称，职务描述</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> job (</span><br><span class="line">  <span class="keyword">id</span> <span class="built_in">INT</span> PRIMARY <span class="keyword">KEY</span>,</span><br><span class="line">  jname <span class="built_in">VARCHAR</span>(<span class="number">20</span>),</span><br><span class="line">  description <span class="built_in">VARCHAR</span>(<span class="number">50</span>)</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 添加 4 个职务</span></span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> job (<span class="keyword">id</span>, jname, description) <span class="keyword">VALUES</span></span><br><span class="line">  (<span class="number">1</span>, <span class="string">'董事长'</span>, <span class="string">'管理整个公司，接单'</span>),</span><br><span class="line">  (<span class="number">2</span>, <span class="string">'经理'</span>, <span class="string">'管理部门员工'</span>),</span><br><span class="line">  (<span class="number">3</span>, <span class="string">'销售员'</span>, <span class="string">'向客人推销产品'</span>),</span><br><span class="line">  (<span class="number">4</span>, <span class="string">'文员'</span>, <span class="string">'使用办公软件'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 员工表</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> emp (</span><br><span class="line">  <span class="keyword">id</span> <span class="built_in">INT</span> PRIMARY <span class="keyword">KEY</span>,     <span class="comment">-- 员工 id</span></span><br><span class="line">  ename <span class="built_in">VARCHAR</span>(<span class="number">50</span>),      <span class="comment">-- 员工姓名</span></span><br><span class="line">  job_id <span class="built_in">INT</span>,             <span class="comment">-- 职务 id</span></span><br><span class="line">  mgr <span class="built_in">INT</span>,                <span class="comment">-- 上级领导</span></span><br><span class="line">  joindate <span class="built_in">DATE</span>,          <span class="comment">-- 入职日期</span></span><br><span class="line">  salary <span class="built_in">DECIMAL</span>(<span class="number">7</span>,<span class="number">2</span>),    <span class="comment">-- 工资</span></span><br><span class="line">  bonus <span class="built_in">DECIMAL</span>(<span class="number">7</span>,<span class="number">2</span>),     <span class="comment">-- 奖金</span></span><br><span class="line">  dept_id <span class="built_in">INT</span>,            <span class="comment">-- 所在部门编号</span></span><br><span class="line">  <span class="keyword">CONSTRAINT</span> emp_jobid_ref_job_id_fk <span class="keyword">FOREIGN</span> <span class="keyword">KEY</span> (job_id) <span class="keyword">REFERENCES</span> job (<span class="keyword">id</span>),</span><br><span class="line">  <span class="keyword">CONSTRAINT</span> emp_deptid_ref_dept_id_fk <span class="keyword">FOREIGN</span> <span class="keyword">KEY</span> (dept_id) <span class="keyword">REFERENCES</span> dept (<span class="keyword">id</span>)</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 添加员工</span></span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> emp(<span class="keyword">id</span>,ename,job_id,mgr,joindate,salary,bonus,dept_id) <span class="keyword">VALUES</span> </span><br><span class="line">  (<span class="number">1001</span>,<span class="string">'孙悟空'</span>,<span class="number">4</span>,<span class="number">1004</span>,<span class="string">'2000-12-17'</span>,<span class="string">'8000.00'</span>,<span class="literal">NULL</span>,<span class="number">20</span>),</span><br><span class="line">  (<span class="number">1002</span>,<span class="string">'卢俊义'</span>,<span class="number">3</span>,<span class="number">1006</span>,<span class="string">'2001-02-20'</span>,<span class="string">'16000.00'</span>,<span class="string">'3000.00'</span>,<span class="number">30</span>),</span><br><span class="line">  (<span class="number">1003</span>,<span class="string">'林冲'</span>,<span class="number">3</span>,<span class="number">1006</span>,<span class="string">'2001-02-22'</span>,<span class="string">'12500.00'</span>,<span class="string">'5000.00'</span>,<span class="number">30</span>),</span><br><span class="line">  (<span class="number">1004</span>,<span class="string">'唐僧'</span>,<span class="number">2</span>,<span class="number">1009</span>,<span class="string">'2001-04-02'</span>,<span class="string">'29750.00'</span>,<span class="literal">NULL</span>,<span class="number">20</span>),</span><br><span class="line">  (<span class="number">1005</span>,<span class="string">'李逵'</span>,<span class="number">4</span>,<span class="number">1006</span>,<span class="string">'2001-09-28'</span>,<span class="string">'12500.00'</span>,<span class="string">'14000.00'</span>,<span class="number">30</span>),</span><br><span class="line">  (<span class="number">1006</span>,<span class="string">'宋江'</span>,<span class="number">2</span>,<span class="number">1009</span>,<span class="string">'2001-05-01'</span>,<span class="string">'28500.00'</span>,<span class="literal">NULL</span>,<span class="number">30</span>),</span><br><span class="line">  (<span class="number">1007</span>,<span class="string">'刘备'</span>,<span class="number">2</span>,<span class="number">1009</span>,<span class="string">'2001-09-01'</span>,<span class="string">'24500.00'</span>,<span class="literal">NULL</span>,<span class="number">10</span>),</span><br><span class="line">  (<span class="number">1008</span>,<span class="string">'猪八戒'</span>,<span class="number">4</span>,<span class="number">1004</span>,<span class="string">'2007-04-19'</span>,<span class="string">'30000.00'</span>,<span class="literal">NULL</span>,<span class="number">20</span>),</span><br><span class="line">  (<span class="number">1009</span>,<span class="string">'罗贯中'</span>,<span class="number">1</span>,<span class="literal">NULL</span>,<span class="string">'2001-11-17'</span>,<span class="string">'50000.00'</span>,<span class="literal">NULL</span>,<span class="number">10</span>),</span><br><span class="line">  (<span class="number">1010</span>,<span class="string">'吴用'</span>,<span class="number">3</span>,<span class="number">1006</span>,<span class="string">'2001-09-08'</span>,<span class="string">'15000.00'</span>,<span class="string">'0.00'</span>,<span class="number">30</span>),</span><br><span class="line">  (<span class="number">1011</span>,<span class="string">'沙僧'</span>,<span class="number">4</span>,<span class="number">1004</span>,<span class="string">'2007-05-23'</span>,<span class="string">'11000.00'</span>,<span class="literal">NULL</span>,<span class="number">20</span>),</span><br><span class="line">  (<span class="number">1012</span>,<span class="string">'李逵'</span>,<span class="number">4</span>,<span class="number">1006</span>,<span class="string">'2001-12-03'</span>,<span class="string">'9500.00'</span>,<span class="literal">NULL</span>,<span class="number">30</span>),</span><br><span class="line">  (<span class="number">1013</span>,<span class="string">'小白龙'</span>,<span class="number">4</span>,<span class="number">1004</span>,<span class="string">'2001-12-03'</span>,<span class="string">'30000.00'</span>,<span class="literal">NULL</span>,<span class="number">20</span>),</span><br><span class="line">  (<span class="number">1014</span>,<span class="string">'关羽'</span>,<span class="number">4</span>,<span class="number">1007</span>,<span class="string">'2002-01-23'</span>,<span class="string">'13000.00'</span>,<span class="literal">NULL</span>,<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 工资等级表</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> salarygrade (</span><br><span class="line">  grade <span class="built_in">INT</span> PRIMARY <span class="keyword">KEY</span>,    <span class="comment">-- 级别</span></span><br><span class="line">  losalary <span class="built_in">INT</span>,             <span class="comment">-- 最低工资</span></span><br><span class="line">  hisalary <span class="built_in">INT</span>              <span class="comment">-- 最高工资</span></span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 添加 5 个工资等级</span></span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> salarygrade(grade,losalary,hisalary) <span class="keyword">VALUES</span> </span><br><span class="line">  (<span class="number">1</span>,<span class="number">7000</span>,<span class="number">12000</span>),</span><br><span class="line">  (<span class="number">2</span>,<span class="number">12010</span>,<span class="number">14000</span>),</span><br><span class="line">  (<span class="number">3</span>,<span class="number">14010</span>,<span class="number">20000</span>),</span><br><span class="line">  (<span class="number">4</span>,<span class="number">20010</span>,<span class="number">30000</span>),</span><br><span class="line">  (<span class="number">5</span>,<span class="number">30010</span>,<span class="number">99990</span>);</span><br></pre></td></tr></table></figure><h4 id="需求-1"><a href="#需求-1" class="headerlink" title="需求 1"></a>需求 1</h4><p>查询所有员工信息。查询员工编号，员工姓名，工资，职务名称，职务描述</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">  分析：</span></span><br><span class="line"><span class="comment">  1.员工编号，员工姓名，工资，需要查询 emp 表  职务名称，职务描述需要查询 job 表</span></span><br><span class="line"><span class="comment">  2.查询条件 emp.job_id = job.id</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">SELECT</span> </span><br><span class="line">  t1.<span class="string">`id`</span>,          <span class="comment">-- 员工编号</span></span><br><span class="line">  t1.<span class="string">`ename`</span>,       <span class="comment">-- 员工姓名</span></span><br><span class="line">  t1.<span class="string">`salary`</span>,      <span class="comment">-- 工资</span></span><br><span class="line">  t2.<span class="string">`jname`</span>,       <span class="comment">-- 职务名称</span></span><br><span class="line">  t2.<span class="string">`description`</span>  <span class="comment">-- 职务描述</span></span><br><span class="line"><span class="keyword">FROM</span> </span><br><span class="line">  emp t1, job t2</span><br><span class="line"><span class="keyword">WHERE</span> </span><br><span class="line">  t1.<span class="string">`job_id`</span> = t2.<span class="string">`id`</span>;</span><br></pre></td></tr></table></figure><h4 id="需求-2"><a href="#需求-2" class="headerlink" title="需求 2"></a>需求 2</h4><p>查询员工编号，员工姓名，工资，职务名称，职务描述，部门名称，部门位置</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">  分析：</span></span><br><span class="line"><span class="comment">    1.员工编号，员工姓名，工资 emp 职务名称，职务描述 job 部门名称，部门位置 dept</span></span><br><span class="line"><span class="comment">    2. 条件： emp.job_id = job.id and emp.dept_id = dept.id</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">SELECT</span> </span><br><span class="line">  t1.<span class="string">`id`</span>,      <span class="comment">-- 员工编号</span></span><br><span class="line">  t1.<span class="string">`ename`</span>,   <span class="comment">-- 员工姓名</span></span><br><span class="line">  t1.<span class="string">`salary`</span>,  <span class="comment">-- 工资</span></span><br><span class="line">  t2.<span class="string">`jname`</span>,   <span class="comment">-- 职务名称</span></span><br><span class="line">  t2.<span class="string">`description`</span>,   <span class="comment">-- 职务描述</span></span><br><span class="line">  t3.<span class="string">`dname`</span>,   <span class="comment">-- 部门名称</span></span><br><span class="line">  t3.<span class="string">`loc`</span>      <span class="comment">-- 部门位置</span></span><br><span class="line"><span class="keyword">FROM</span> </span><br><span class="line">  emp t1, job t2, dept t3</span><br><span class="line"><span class="keyword">WHERE</span> </span><br><span class="line">  t1.<span class="string">`job_id`</span> = t2.<span class="string">`id`</span> <span class="keyword">AND</span> t1.<span class="string">`dept_id`</span> = t3.<span class="string">`id`</span>;</span><br></pre></td></tr></table></figure><h4 id="需求-3"><a href="#需求-3" class="headerlink" title="需求 3"></a>需求 3</h4><p>查询员工姓名，工资，工资等级</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">  分析：</span></span><br><span class="line"><span class="comment">    1.员工姓名，工资 emp 工资等级 salarygrade</span></span><br><span class="line"><span class="comment">    2.条件 emp.salary &gt;= salarygrade.losalary and emp.salary &lt;= salarygrade.hisalary</span></span><br><span class="line"><span class="comment">      emp.salary BETWEEN salarygrade.losalary and salarygrade.hisalary</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">SELECT</span> </span><br><span class="line">  t1.ename ,</span><br><span class="line">  t1.<span class="string">`salary`</span>,</span><br><span class="line">  t2.*</span><br><span class="line"><span class="keyword">FROM</span> emp t1, salarygrade t2</span><br><span class="line"><span class="keyword">WHERE</span> t1.<span class="string">`salary`</span> <span class="keyword">BETWEEN</span> t2.<span class="string">`losalary`</span> <span class="keyword">AND</span> t2.<span class="string">`hisalary`</span>;</span><br></pre></td></tr></table></figure><h4 id="需求-4"><a href="#需求-4" class="headerlink" title="需求 4"></a>需求 4</h4><p>查询员工姓名，工资，职务名称，职务描述，部门名称，部门位置，工资等级</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">  分析：</span></span><br><span class="line"><span class="comment">    1. 员工姓名，工资 emp ， 职务名称，职务描述 job 部门名称，部门位置，dept  工资等级 salarygrade</span></span><br><span class="line"><span class="comment">    2. 条件：emp.job_id = job.id and emp.dept_id = dept.id and emp.salary BETWEEN salarygrade.losalary and salarygrade.hisalary</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">SELECT</span> </span><br><span class="line">  t1.<span class="string">`ename`</span>,</span><br><span class="line">  t1.<span class="string">`salary`</span>,</span><br><span class="line">  t2.<span class="string">`jname`</span>,</span><br><span class="line">  t2.<span class="string">`description`</span>,</span><br><span class="line">  t3.<span class="string">`dname`</span>,</span><br><span class="line">  t3.<span class="string">`loc`</span>,</span><br><span class="line">  t4.<span class="string">`grade`</span></span><br><span class="line"><span class="keyword">FROM</span> </span><br><span class="line">  emp t1,job t2,dept t3,salarygrade t4</span><br><span class="line"><span class="keyword">WHERE</span> </span><br><span class="line">  t1.<span class="string">`job_id`</span> = t2.<span class="string">`id`</span> </span><br><span class="line">  <span class="keyword">AND</span> t1.<span class="string">`dept_id`</span> = t3.<span class="string">`id`</span></span><br><span class="line">  <span class="keyword">AND</span> t1.<span class="string">`salary`</span> <span class="keyword">BETWEEN</span> t4.<span class="string">`losalary`</span> <span class="keyword">AND</span> t4.<span class="string">`hisalary`</span>;</span><br></pre></td></tr></table></figure><h4 id="需求-5"><a href="#需求-5" class="headerlink" title="需求 5"></a>需求 5</h4><p>查询出部门编号、部门名称、部门位置、部门人数</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">  分析：</span></span><br><span class="line"><span class="comment">    1.部门编号、部门名称、部门位置 dept 表。 部门人数 emp表</span></span><br><span class="line"><span class="comment">    2.使用分组查询。按照emp.dept_id完成分组，查询count(id)</span></span><br><span class="line"><span class="comment">    3.使用子查询将第2步的查询结果和dept表进行关联查询</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">SELECT</span> </span><br><span class="line">  t1.<span class="string">`id`</span>,t1.<span class="string">`dname`</span>,t1.<span class="string">`loc`</span> , t2.total</span><br><span class="line"><span class="keyword">FROM</span> </span><br><span class="line">  dept t1,</span><br><span class="line">  (<span class="keyword">SELECT</span></span><br><span class="line">    dept_id,<span class="keyword">COUNT</span>(<span class="keyword">id</span>) total</span><br><span class="line">  <span class="keyword">FROM</span> </span><br><span class="line">    emp</span><br><span class="line">  <span class="keyword">GROUP</span> <span class="keyword">BY</span> dept_id) t2</span><br><span class="line"><span class="keyword">WHERE</span> t1.<span class="string">`id`</span> = t2.dept_id;</span><br></pre></td></tr></table></figure><h4 id="需求-6"><a href="#需求-6" class="headerlink" title="需求 6"></a>需求 6</h4><p>查询所有员工的姓名及其直接上级的姓名,没有领导的员工也需要查询</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">  分析：</span></span><br><span class="line"><span class="comment">    1.姓名 emp， 直接上级的姓名 emp</span></span><br><span class="line"><span class="comment">      * emp表的id 和 mgr 是自关联</span></span><br><span class="line"><span class="comment">    2.条件 emp.id = emp.mgr</span></span><br><span class="line"><span class="comment">    3.查询左表的所有数据，和 交集数据</span></span><br><span class="line"><span class="comment">      * 使用左外连接查询</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">select</span></span><br><span class="line">  t1.ename,</span><br><span class="line">  t1.mgr,</span><br><span class="line">  t2.<span class="string">`id`</span>,</span><br><span class="line">  t2.ename</span><br><span class="line"><span class="keyword">from</span> emp t1, emp t2</span><br><span class="line"><span class="keyword">where</span> t1.mgr = t2.<span class="string">`id`</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span> </span><br><span class="line">  t1.ename,</span><br><span class="line">  t1.mgr,</span><br><span class="line">  t2.<span class="string">`id`</span>,</span><br><span class="line">  t2.<span class="string">`ename`</span></span><br><span class="line"><span class="keyword">FROM</span> emp t1</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> emp t2</span><br><span class="line"><span class="keyword">ON</span> t1.<span class="string">`mgr`</span> = t2.<span class="string">`id`</span>;</span><br></pre></td></tr></table></figure><h2 id="事务"><a href="#事务" class="headerlink" title="事务"></a>事务</h2><p><strong>概念</strong>：如果一个包含多个步骤的业务操作，被事务管理，那么这些操作要么同时成功，要么同时失败。</p><h3 id="操作"><a href="#操作" class="headerlink" title="操作"></a>操作</h3><ol><li>开启事务： <code>start transaction;</code></li><li>回滚：<code>rollback;</code></li><li>提交：<code>commit;</code></li></ol><p>如下：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> <span class="keyword">account</span> (</span><br><span class="line">  <span class="keyword">id</span> <span class="built_in">INT</span> PRIMARY <span class="keyword">KEY</span> AUTO_INCREMENT,</span><br><span class="line">  <span class="keyword">NAME</span> <span class="built_in">VARCHAR</span>(<span class="number">10</span>),</span><br><span class="line">  balance <span class="keyword">DOUBLE</span></span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 添加数据</span></span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> <span class="keyword">account</span> (<span class="keyword">NAME</span>, balance) <span class="keyword">VALUES</span> (<span class="string">'zhangsan'</span>, <span class="number">1000</span>), (<span class="string">'lisi'</span>, <span class="number">1000</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> <span class="keyword">account</span>;</span><br><span class="line"><span class="keyword">UPDATE</span> <span class="keyword">account</span> <span class="keyword">SET</span> balance = <span class="number">1000</span>;</span><br><span class="line"><span class="comment">-- 张三给李四转账 500 元</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 0. 开启事务</span></span><br><span class="line"><span class="keyword">START</span> <span class="keyword">TRANSACTION</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 1. 张三账户 -500</span></span><br><span class="line"><span class="keyword">UPDATE</span> <span class="keyword">account</span> <span class="keyword">SET</span> balance = balance - <span class="number">500</span> <span class="keyword">WHERE</span> <span class="keyword">NAME</span> = <span class="string">'zhangsan'</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 2. 李四账户 +500</span></span><br><span class="line"><span class="comment">-- 出错了...</span></span><br><span class="line"><span class="keyword">UPDATE</span> <span class="keyword">account</span> <span class="keyword">SET</span> balance = balance + <span class="number">500</span> <span class="keyword">WHERE</span> <span class="keyword">NAME</span> = <span class="string">'lisi'</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 发现执行没有问题，提交事务</span></span><br><span class="line"><span class="keyword">COMMIT</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 发现出问题了，回滚事务</span></span><br><span class="line"><span class="keyword">ROLLBACK</span>;</span><br></pre></td></tr></table></figure><h3 id="事务提交"><a href="#事务提交" class="headerlink" title="事务提交"></a>事务提交</h3><p>事务提交的两种方式：</p><ul><li><strong>自动提交</strong>：<ul><li>mysql 就是自动提交的</li><li>一条 DML 语句会自动提交一次事务。</li></ul></li><li><strong>手动提交</strong>：<ul><li>Oracle 数据库默认是手动提交事务</li><li>需要先开启事务，再提交</li></ul></li></ul><p>修改事务的默认提交方式：</p><ul><li>查看事务的默认提交方式：<code>SELECT @@autocommit;</code>。其中 1 代表自动提交；0 代表手动提交`</li><li>修改默认提交方式：<code>set @@autocommit = 0;</code></li></ul><h3 id="四大特征"><a href="#四大特征" class="headerlink" title="四大特征"></a>四大特征</h3><p>事务的四大特征如下：</p><ul><li>原子性：是不可分割的最小操作单位，要么同时成功，要么同时失败。</li><li>持久性：当事务提交或回滚后，数据库会持久化的保存数据。</li><li>隔离性：多个事务之间。相互独立。</li><li>一致性：事务操作前后，数据总量不变</li></ul><h3 id="隔离级别"><a href="#隔离级别" class="headerlink" title="隔离级别"></a>隔离级别</h3><p>概念：多个事务之间隔离的，相互独立的。但是如果多个事务操作同一批数据，则会引发一些问题，设置不同的隔离级别就可以解决这些问题。</p><p>存在问题：</p><ul><li>脏读：一个事务，读取到另一个事务中没有提交的数据。</li><li>不可重复读 (虚读) ：在同一个事务中，两次读取到的数据不一样。</li><li>幻读：一个事务操作 (DML) 数据表中所有记录，另一个事务添加了一条数据，则第一个事务查询不到自己的修改。</li></ul><p>有如下隔离级别：</p><ul><li>read uncommitted：读未提交。产生的问题：脏读、不可重复读、幻读。</li><li>read committed：读已提交 (Oracle 默认)。产生的问题：不可重复读、幻读。</li><li>repeatable read：可重复读 (MySQL默认)。产生的问题：幻读</li><li>serializable：串行化。可以解决所有的问题，但是效率低下。</li></ul><p><strong>注意</strong>：隔离级别从小到大安全性越来越高，但是效率越来越低</p><p><strong>数据库查询隔离级别</strong>：<code>select @@tx_isolation;</code></p><p><strong>数据库设置隔离级别</strong>：<code>set global transaction isolation level 级别字符串;</code></p>]]></content>
      <categories>
        <category>Database</category>
      </categories>
      <tags>
        <tag>SQL</tag>
      </tags>
  </entry>
  <entry>
    <title>IDEA 的安装与配置</title>
    <url>/idea/26204f6b-c677-45b4-9dd2-3cb5d1c3cc4d.html</url>
    <content><![CDATA[<p>IDEA 全称 IntelliJ IDEA，是用于 Java 语言开发的集成环境(也可用于其他语言)。IntelliJ IDEA 在业界被公认为最好的 Java 开发工具之一，尤其在智能代码助手、代码自动提示、重构、J2EE 支持、Ant、JUnit、CVS 整合、代码审查、创新的 GUI 设计等方面的功能可以说是超常的。</p><a id="more"></a><h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><p>Jetbrains <a href="https://www.jetbrains.com/idea/download/previous.html" target="_blank" rel="noopener"><strong>官网</strong></a> 下载安装程序，安装该过程不详细介绍，可以自定义安装路径。</p><h2 id="IDEA-的配置"><a href="#IDEA-的配置" class="headerlink" title="IDEA 的配置"></a>IDEA 的配置</h2><h3 id="字体配置"><a href="#字体配置" class="headerlink" title="字体配置"></a>字体配置</h3><p><code>Settings -&gt; Editor -&gt; Font/Fallback Font</code></p><p>推荐 Font 使用 <strong>Consolas</strong>，Fallback Font 使用 <strong>Monospaced</strong></p><h3 id="隐藏项目文件"><a href="#隐藏项目文件" class="headerlink" title="隐藏项目文件"></a>隐藏项目文件</h3><p><code>Settings -&gt; Editor -&gt; File Types -&gt; Ignore</code></p><p>由于项目的配置文件（如 <code>.idea</code> 目录、<code>.iml</code> 文件）是我们开发中用不到的，我们可以通过配置将其隐藏。在 <code>Ignore</code> 后追加 <code>.idea;*.iml</code> 即可。</p><h3 id="隐藏项目目录"><a href="#隐藏项目目录" class="headerlink" title="隐藏项目目录"></a>隐藏项目目录</h3><p><code>Help -&gt; Editor Custom Properties</code></p><p>会打开一个文件，在其中加入如下两行：</p><figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="meta">project.tree.structure.show.url</span>=<span class="string">false</span></span><br><span class="line"><span class="meta">ide.tree.horizontal.default.autoscrolling</span>=<span class="string">false</span></span><br></pre></td></tr></table></figure><h3 id="代码模板"><a href="#代码模板" class="headerlink" title="代码模板"></a>代码模板</h3><p><code>Settings</code> -&gt; <code>Editor</code> -&gt; <code>Live Templates</code></p><h3 id="取消自动启动项目"><a href="#取消自动启动项目" class="headerlink" title="取消自动启动项目"></a>取消自动启动项目</h3><p><code>Appearance &amp; Behavior</code> -&gt; <code>System Setting</code> -&gt; <code>Reopen last project on startup</code></p><h3 id="取消双击-shift"><a href="#取消双击-shift" class="headerlink" title="取消双击 shift"></a>取消双击 shift</h3><p><code>Ctrl + Shift + A</code> -&gt; 输入 <code>registry</code> -&gt; 勾选 <code>ide.suppress.double.click.handler</code></p>]]></content>
      <categories>
        <category>Software</category>
      </categories>
      <tags>
        <tag>IDEA</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式</title>
    <url>/other/ea83f4ee-b0ba-4265-a4da-cca39fa63f0a.html</url>
    <content><![CDATA[<p>设计模式是在特定环境下人们解决某类重复出现问题的一套成功或有效的解决方案。简单来说，设计模式就是在一定环境下，用固定的套路解决问题。</p><a id="more"></a><p>设计模式可分为三大类：创建型模式（Creational Patterns）、结构型模式（Structural Patterns）、行为型模式（Behavioral Patterns）</p><ul><li>创建型模式，共五种：工厂方法模式、抽象工厂模式、单例模式、建造者模式、原型模式。</li><li>结构型模式，共七种：适配器模式、装饰器模式、代理模式、外观模式、桥接模式、组合模式、享元模式。</li><li>行为型模式，共十一种：策略模式、模板方法模式、观察者模式、迭代子模式、责任链模式、命令模式、备忘录模式、状态模式、访问者模式、中介者模式、解释器模式。</li></ul><h2 id="创建型模式"><a href="#创建型模式" class="headerlink" title="创建型模式"></a>创建型模式</h2><p>这些设计模式提供了一种在创建对象是同时隐藏创建逻辑的方式，而不是使用 new 运算符直接实例化对象。这使得程序在判断针对某个给定实例需要创建哪些对象时更加灵活。</p><h3 id="简单工厂模式"><a href="#简单工厂模式" class="headerlink" title="简单工厂模式"></a>简单工厂模式</h3><p>简单工厂模式(Simple Factory Pattern)是属于创建型模式，但不属于23种GOF设计模式之一。简单工厂模式是由一个工厂对象决定创建出哪一种产品类的实例。简单工厂模式是工厂模式家族中最简单实用的模式，可以理解为是不同工厂模式的一个特殊实现。</p><p>其类图如下：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/778A0459F89D48D99045AA78A2EE5F7C/259" alt=""></p><p>模式中的角色和职责：</p><ul><li>工厂(Factory)角色：简单工厂模式的核心，它负责实现创建所有实例的内部逻辑。工厂类可以直接被外界调用，创建所需要的产品对象。</li><li>抽象产品(Abstract Product)角色：简单工厂模式所创建的所有对象的父类，它负责描述所有实例所共有的公共接口。</li><li>具体产品(Concrete Product)角色：简单工厂模式所创建的具体实例对象。</li></ul><p>简单工厂模式案例：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/171056091C414857A184AE703171C7AB/261" alt=""></p><p>代码（C++）：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//抽象水果</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AbsFruit</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">showName</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//苹果</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Apple</span> :</span> <span class="keyword">public</span> AbsFruit &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">showName</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"I am apple..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//香蕉</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Banana</span> :</span> <span class="keyword">public</span> AbsFruit &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">showName</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"I am banana..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//鸭梨</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Pear</span> :</span> <span class="keyword">public</span> AbsFruit &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">showName</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"I am pear... "</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//水果工厂</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">FruitFactory</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">static</span> AbsFruit *<span class="title">CreatFruit</span><span class="params">(<span class="built_in">string</span> flag)</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">if</span> (flag == <span class="string">"apple"</span>)&#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">new</span> Apple;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> <span class="keyword">if</span> (flag == <span class="string">"banana"</span>)&#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">new</span> Banana;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> <span class="keyword">if</span> (flag == <span class="string">"pear"</span>)&#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">new</span> Pear;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span>&#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>然后就可以通过水果工厂来生产水果了：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">FruitFactory *factory = <span class="keyword">new</span> FruitFactory;</span><br><span class="line">AbsFruit *fruit = factory-&gt;CreatFruit(<span class="string">"apple"</span>);</span><br><span class="line">fruit-&gt;showName();</span><br><span class="line"><span class="keyword">delete</span> fruit;</span><br><span class="line">fruit = factory-&gt;CreatFruit(<span class="string">"banana"</span>);</span><br><span class="line">fruit-&gt;showName();</span><br><span class="line"><span class="keyword">delete</span> fruit;</span><br><span class="line">fruit = factory-&gt;CreatFruit(<span class="string">"pear"</span>);</span><br><span class="line">fruit-&gt;showName();</span><br><span class="line"><span class="keyword">delete</span> fruit;</span><br><span class="line"><span class="keyword">delete</span> factory;</span><br></pre></td></tr></table></figure><p>简单工厂模式优缺点：</p><ul><li>优点：<br>工厂类是整个模式的关键.包含了必要的逻辑判断,根据外界给定的信息,决定究竟应该创建哪个具体类的对象.通过使用工厂类,外界可以从直接创建具体产品对象的尴尬局面摆脱出来,仅仅需要负责“消费”对象就可以了。而不必管这些对象究竟如何创建及如何组织的．明确了各自的职责和权利，有利于整个软件体系结构的优化。</li><li>缺点：<br>工厂类职责过重，不符合单一职责原则，工厂类和其他的具体类之间的耦合关系很强。如果需要添加新的类就需要改变工厂类了，这就违反了开闭原则。因此简单工厂模式对系统的维护和扩展非常不利，所以它不属于 23 种 GOF 之一。</li></ul><p>简单工厂模式适用场景：</p><ol><li>工厂类负责创建的对象比较少，由于创建的对象较少，不会造成工厂方法中的业务逻辑太过复杂</li><li>客户端只知道传入工厂类的参数，对于如何创建对象并不关心</li></ol><h3 id="工厂模式"><a href="#工厂模式" class="headerlink" title="工厂模式"></a>工厂模式</h3><p>工厂模式(Factory Pattern) 提供了一种创建对象的最佳方式。在工厂模式中，我们在创建对象时不会对客户端暴露创建逻辑，并且是通过使用一个共同的接口来指向新创建的对象。</p><p><strong>工厂模式定义一个创建对象的接口，让其子类自己决定实例化哪一个工厂类，工厂模式使其创建过程延长到子类进行。</strong></p><p>工厂模式主要解决接口选择问题，当我们明确的计划在不同条件下创建不同实例时可以使用工厂模式</p><p>工厂模式中的角色和职责：</p><ul><li>抽象工厂(Abstract Factory)角色：工厂方法模式的核心，任何工厂类都必须实现这个接口。</li><li>工厂(Factory)角色：具体工厂类是抽象工厂的一个实现，负责实例化产品对象。</li><li>抽象产品(Abstract Product)角色：工厂方法模式所创建的所有对象的父类，它负责描述所有实例所共有的公共接口。</li><li>具体产品(Concrete Product)角色：工厂方法模式所创建的具体实例对象。</li></ul><p>简单来说：<strong>工厂模式 = 简单工厂模式 + 开闭原则</strong></p><p>工厂方法模式其类图如下：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/1AABE2F857DA4FC08F9140E31C7CECBA/263" alt=""></p><p>工厂模式案例：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/B13BD41B17B74F918DE668E8A13A513D/265" alt=""></p><p>代码：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//抽象水果</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AbsFruit</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">showName</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//苹果</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Apple</span> :</span> <span class="keyword">public</span> AbsFruit &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">showName</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"I am apple..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//香蕉</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Banana</span> :</span> <span class="keyword">public</span> AbsFruit &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">showName</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"I am banana..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//鸭梨</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Pear</span> :</span> <span class="keyword">public</span> AbsFruit &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">showName</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"I am pear... "</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//抽象水果工厂</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AbsFruitFactory</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> AbsFruit *<span class="title">CreateFruit</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//苹果工厂</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AppleFactory</span> :</span> <span class="keyword">public</span> AbsFruitFactory &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> AbsFruit *<span class="title">CreateFruit</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> Apple;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//香蕉工厂</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">BananaFactory</span> :</span> <span class="keyword">public</span> AbsFruitFactory &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> AbsFruit *<span class="title">CreateFruit</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> Banana;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//鸭梨工厂</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">PearFactory</span> :</span> <span class="keyword">public</span> AbsFruitFactory &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> AbsFruit *<span class="title">CreateFruit</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> Pear;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>调用：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">AbsFruitFactory *factory = <span class="literal">NULL</span>;</span><br><span class="line">AbsFruit *fruit = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">//创建一个苹果工厂</span></span><br><span class="line">factory = <span class="keyword">new</span> AppleFactory;</span><br><span class="line">fruit = factory-&gt;CreateFruit();</span><br><span class="line">fruit-&gt;showName();</span><br><span class="line"><span class="keyword">delete</span> factory;</span><br><span class="line"><span class="keyword">delete</span> fruit;</span><br><span class="line"></span><br><span class="line"><span class="comment">//创建一个香蕉工厂</span></span><br><span class="line">factory = <span class="keyword">new</span> BananaFactory;</span><br><span class="line">fruit = factory-&gt;CreateFruit();</span><br><span class="line">fruit-&gt;showName();</span><br><span class="line"><span class="keyword">delete</span> factory;</span><br><span class="line"><span class="keyword">delete</span> fruit;</span><br><span class="line"></span><br><span class="line"><span class="comment">//创建一个鸭梨工厂</span></span><br><span class="line">factory = <span class="keyword">new</span> PearFactory;</span><br><span class="line">fruit = factory-&gt;CreateFruit();</span><br><span class="line">fruit-&gt;showName();</span><br><span class="line"><span class="keyword">delete</span> factory;</span><br><span class="line"><span class="keyword">delete</span> fruit;</span><br></pre></td></tr></table></figure><p>工厂方法模式优缺点：</p><ul><li>优点：不需要记住具体类名，甚至连具体参数都不需要记忆；实现了对象创建和使用的分离；系统的可扩展性变得非常好，无序修改接口和原类。</li><li>缺点：增加系统中类的个数，复杂度和理解度增加；增加了系统的抽象性和理解难度</li></ul><p>工厂方法模式适用场景：</p><ol><li>客户端不知道它所需要的对象的类</li><li>抽象工厂类通过其子类来指定创建哪个对象</li></ol><p><strong>注意</strong>：作为一种创建类模式，在任何需要生成复杂对象的地方，都可以使用工厂方法模式。有一点需要注意的地方就是复杂对象适合使用工厂模式，而简单对象，特别是只需要通过 new 就可以完成创建的对象，无需使用工厂模式。如果使用工厂模式，就需要引入一个工厂类，会增加系统的复杂度。</p><h3 id="抽象工厂模式"><a href="#抽象工厂模式" class="headerlink" title="抽象工厂模式"></a>抽象工厂模式</h3><p>抽象工厂模式（Abstract Factory Pattern）是围绕一个超级工厂创建其他工厂。该超级工厂又称为其他工厂的工厂。这种类型的设计模式属于创建型模式，它提供了一种创建对象的最佳方式。在抽象工厂模式中，接口是负责创建一个相关对象的工厂，不需要显式指定它们的类。每个生成的工厂都能按照工厂模式提供对象。</p><p>抽象工厂模式提供了一个接口，可以创建一系列相关或相互依赖对象的，而无需指定它们具体的类。</p><p>抽象工厂模式主要解决接口选择问题，当系统的产品有多于一个的产品族，而系统只消费其中某一种产品的时候使用抽象工厂模式。简单来说，就是在一个工厂里聚合多个同类产品。</p><p>其类图如下：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/384245CCF5244A83971C3E49B0B6EC61/268" alt=""></p><p>抽象工厂模式实例如下：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/BD29903C38684C8892D6A13FF0F02F7F/270" alt=""></p><p>代码：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//抽象苹果</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AbsApple</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">showName</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//中国苹果</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ChinaApple</span> :</span> <span class="keyword">public</span> AbsApple &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">showName</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"China Apple..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//美国苹果</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">USAApple</span> :</span> <span class="keyword">public</span> AbsApple &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">showName</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"USA Apple..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//抽象香蕉</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AbsBanana</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">showName</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//中国香蕉</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ChinaBanana</span> :</span> <span class="keyword">public</span> AbsBanana &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">showName</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"China Banana..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//美国香蕉</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">USABanana</span> :</span> <span class="keyword">public</span> AbsBanana &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">showName</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"USA Banana..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//抽象工厂</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AbsFactory</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> AbsApple *<span class="title">CreateApple</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> AbsBanana *<span class="title">CreateBanana</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//中国工厂</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ChinaFactory</span> :</span> <span class="keyword">public</span> AbsFactory &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> AbsApple *<span class="title">CreateApple</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> ChinaApple;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> AbsBanana *<span class="title">CreateBanana</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> ChinaBanana;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//美国工厂</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">USAFactory</span> :</span> <span class="keyword">public</span> AbsFactory &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> AbsApple *<span class="title">CreateApple</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> USAApple;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> AbsBanana *<span class="title">CreateBanana</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> USABanana;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>调用：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">AbsFactory *factory = <span class="literal">NULL</span>;</span><br><span class="line">AbsApple *apple = <span class="literal">NULL</span>;</span><br><span class="line">AbsBanana *banana = <span class="literal">NULL</span>;</span><br><span class="line"><span class="comment">//创建中国工厂</span></span><br><span class="line">factory = <span class="keyword">new</span> ChinaFactory;</span><br><span class="line">apple = factory-&gt;CreateApple();</span><br><span class="line">banana = factory-&gt;CreateBanana();</span><br><span class="line"></span><br><span class="line">apple-&gt;showName();</span><br><span class="line">banana-&gt;showName();</span><br><span class="line"></span><br><span class="line"><span class="keyword">delete</span> banana;</span><br><span class="line"><span class="keyword">delete</span> apple;</span><br><span class="line"><span class="keyword">delete</span> factory;</span><br></pre></td></tr></table></figure><h3 id="单例模式"><a href="#单例模式" class="headerlink" title="单例模式"></a>单例模式</h3><p>单例模式(Singleton Pattern)又叫单身模式，是一种常用的软件设计模式。在它的核心结构中只包含一个被称为单例的特殊类，通过单例模式可以保证系统一个类只有一个实例（对象）而且该实例易于外界访问，从而方便对实例个数的控制并节约系统资源。<br>单例模式保证一个类只有一个实例，并提供一个访问它的全局访问点</p><p>实现单例步骤：</p><ol><li>构造函数私有化</li><li>增加静态私有当前类指针变量</li><li>提供静态对外接口，可以让用户获得单例对象</li><li>类外初始化类指针变量</li></ol><p>如：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> &#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	A()</span><br><span class="line">	&#123;</span><br><span class="line">		a = <span class="keyword">new</span> A;</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">static</span> A *<span class="title">getInstance</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">return</span> a;</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="keyword">static</span> A *a;</span><br><span class="line">&#125;;</span><br><span class="line">A *A::a = <span class="literal">NULL</span>;</span><br></pre></td></tr></table></figure><p>单例模式分为 <strong>懒汉式</strong> 和 <strong>饿汉式</strong> 创建方法：</p><p>其中懒汉式创建方法为：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//懒汉式：</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Singleton_lazy</span> &#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	Singleton_lazy()&#123;&#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">static</span> Singleton_lazy *<span class="title">getInstance</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">if</span> (pSingleton != <span class="literal">NULL</span>)&#123;</span><br><span class="line">			pSingleton = <span class="keyword">new</span> Singleton_lazy;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> pSingleton;</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="keyword">static</span> Singleton_lazy *pSingleton;</span><br><span class="line">&#125;;</span><br><span class="line">Singleton_lazy *Singleton_lazy::pSingleton = <span class="literal">NULL</span>;</span><br></pre></td></tr></table></figure><p>饿汉式创建方法为：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//饿汉式</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Singleton_hungry</span> &#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	Singleton_hungry()&#123;	&#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">static</span> Singleton_hungry *<span class="title">getInstance</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">return</span> pSingleton;</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="keyword">static</span> Singleton_hungry *pSingleton;</span><br><span class="line">&#125;;</span><br><span class="line">Singleton_hungry *Singleton_hungry::pSingleton = </span><br><span class="line"><span class="keyword">new</span> Singleton_hungry;</span><br></pre></td></tr></table></figure><p>两者区别为：懒汉式只在 getInstance 方法的时候才会创建对象，而饿汉式在main函数调用之前就已经创建了对象</p><p>单例模式的验证：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Singleton_hungry *p1 = Singleton_hungry::getInstance();</span><br><span class="line">Singleton_hungry *p2 = Singleton_hungry::getInstance();</span><br><span class="line"><span class="keyword">if</span> (p1 == p2)&#123;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="string">"Is..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span>&#123;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="string">"Is NOT..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>结果证明这两个指针 (p1 和 p2) 指向同一块内存空间</p><p>一般情况下，不需要考虑单例的内存释放问题，因为单例只有一个对象，不会造成内存溢出的情况，程序结束时单例会自动释放。如果一定要写释放，那就在单例类中加一个类，类中的析构函数释放单例对象，然后在单例类属性中添加内部类对象</p><p>如果在多线程中用懒汉式创建单例，会造成线程不安全。因为 2 个线程同时执行，此时懒汉式中的 pSingleton 指针都为空，然后 2 个线程调用 getInstance 函数时，就创建了 2 个对象并返回给自的线程中。因此在多线程中用懒汉式创建单例就要加锁。多线程中用饿汉式是安全的，因为饿汉式的对象在 main 函数调用之前就已经创建完毕了。</p><h3 id="建造者模式"><a href="#建造者模式" class="headerlink" title="建造者模式"></a>建造者模式</h3><p>建造者模式（Builder Pattern）使用多个简单的对象一步一步构建成一个复杂的对象。这种类型的设计模式属于创建型模式，它提供了一种创建对象的最佳方式。一个 Builder 类会一步一步构造最终的对象。该 Builder 类是独立于其他对象的。</p><p>建造者模式将一个复杂的构建与其表示相分离，使得同样的构建过程可以创建不同的表示。主要解决在软件系统中，有时候面临“一个复杂对象”的创建工作，其通常有各个部分的子对象用一定的算法构成；由于需求的变化，这个复杂对象的各个部分经常面临着剧烈的变化，但是将它们组合在一起的算法却相对问题。</p><p>建造者模式中的角色及其职责：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/EAAD7D33135341AB8B46AAFD0750ED5A/273" alt=""></p><ul><li>抽象建造者(Abstract Builder)：为创建一个产品对象的各个部件指定抽象接口</li><li>建造者(Builder)：实现AbstractBuilder的接口以构造和装配该产品的各个部件，定义并明确它所创建的表示，并 提供一个检索产品的接口。</li><li>指挥者(Director)：构造一个AbstractBuilder的接口对象</li><li>抽象产品(Abstract Product)：表示被构造的复杂对象。</li><li>具体产品(Concrete Builder)：创建该产品的内部表示并定义它的装配过程，包含定义组成部件的类，包括将这些部件装配成最终产品的接口。</li></ul><p>案例：</p><p>构造一个人物需要对每个部位进行创建，简单来说就是创建头部、身体和四肢</p><p>代码：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//产品类：</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Person</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">setHead</span><span class="params">(<span class="built_in">string</span> head)</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;head = head;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">setBody</span><span class="params">(<span class="built_in">string</span> body)</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;body = body;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">setFoot</span><span class="params">(<span class="built_in">string</span> foot)</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;foot = foot;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="built_in">string</span> <span class="title">getHead</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">this</span>-&gt;head;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="built_in">string</span> <span class="title">getBody</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">this</span>-&gt;body;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="built_in">string</span> <span class="title">getFoot</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">this</span>-&gt;foot;</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="built_in">string</span> head;</span><br><span class="line">	<span class="built_in">string</span> body;</span><br><span class="line">	<span class="built_in">string</span> foot;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//抽象建造者</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AbstractBuilder</span> &#123;</span></span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">BuildHead</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">BuildBody</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">BuildFoot</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//具体建造者</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">PersonBuilder</span> :</span> <span class="keyword">public</span> AbstractBuilder &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	PersonBuilder()</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;person = <span class="keyword">new</span> Person;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">BuildHead</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;person-&gt;setHead(<span class="string">"Head"</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">BuildBody</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;person-&gt;setBody(<span class="string">"Body"</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">BuildFoot</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;person-&gt;setFoot(<span class="string">"Foot"</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function">Person *<span class="title">buildPerson</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">return</span> person;</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	Person * person;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//指挥者</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">PersonDirector</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function">Person <span class="title">concretePerson</span><span class="params">(PersonBuilder *pb)</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		pb-&gt;BuildBody();</span><br><span class="line">		pb-&gt;BuildHead();</span><br><span class="line">		pb-&gt;BuildFoot();</span><br><span class="line">		<span class="keyword">return</span> pb-&gt;buildPerson;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="原型模式"><a href="#原型模式" class="headerlink" title="原型模式"></a>原型模式</h3><p>原型模式（Prototype Pattern）是用于创建重复的对象，同时又能保证性能。这种类型的设计模式属于创建型模式，它提供了一种创建对象的最佳方式。这种模式是实现了一个原型接口，该接口用于创建当前对象的克隆。当直接创建对象的代价比较大时，则采用这种模式。</p><p>原型模式是为了用原型指定创建对象的种类，并且通过拷贝这些原型创建新的对象，主要解决在运行期建立和删除原型。</p><p>其类图如下：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/43F3D3B460CC413281BA3380269CE70D/276" alt=""></p><h2 id="结构型模式"><a href="#结构型模式" class="headerlink" title="结构型模式"></a>结构型模式</h2><p>这些设计模式提供了关注类和对象的组合。继承的概念被用来组合接口和定义组合对象所获得新功能的方式。</p><h3 id="适配器模式"><a href="#适配器模式" class="headerlink" title="适配器模式"></a>适配器模式</h3><p>适配器模式（Adapter Pattern）是作为两个不兼容的接口之间的桥梁。这种类型的设计模式属于结构型模式，它结合了两个独立接口的功能。</p><p>适配器模式将一个类的接口转换成客户希望的另一个接口，使得原本由于接口不兼容而不能一起工作的那些类可以一起工作，主要解决在软件系统中常常要将一些“现存的对象”放到新的环境中，而新环境要求的接口是现有对象不能满足的</p><p>其类图如下：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/550D7A65C2634EEFBAC40A9A17947D62/279" alt=""></p><p>适配器模式中的角色及其职责：</p><ul><li>目标接口(Target)角色：客户所期待的接口，目标可以是具体的或抽象的类，也可以是接口</li><li>源接口(Source)角色：需要适配的类</li><li>适配器(Adapter)角色：将源接口转换为目标接口</li></ul><p>例如：现有一个接口：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//已有的不符合客户期待接口 (需要适配的接口)</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyPrint</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="keyword">int</span> v1, <span class="keyword">int</span> v2)</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; v1 + v2 &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>并且有需求需要用 for_each 打印：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; v;</span><br></pre></td></tr></table></figure><p>但是 for_each 后只能跟上单参数的函数，所以有一个目标接口：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//定义目标接口，客户需要的接口</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Target</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="keyword">int</span> v)</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>之后可以适配源接口：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//适配器，将已有接口适配成目标接口</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Adapter</span> :</span> <span class="keyword">public</span> Target &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	Adapter(<span class="keyword">int</span> param)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;param = param;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="keyword">int</span> v)</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">print</span>(v, param);</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	MyPrint <span class="built_in">print</span>;</span><br><span class="line">	<span class="keyword">int</span> param;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>然后可以通过 for_each 打印容器 v：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">for_each(v.<span class="built_in">begin</span>(), v.<span class="built_in">end</span>(), Adapter(<span class="number">0</span>));</span><br></pre></td></tr></table></figure><h3 id="桥接模式"><a href="#桥接模式" class="headerlink" title="桥接模式"></a>桥接模式</h3><p>桥接（Bridge）是用于把抽象化与实现化解耦，使得二者可以独立变化。这种类型的设计模式属于结构型模式，它通过提供抽象化和实现化之间的桥接结构，来实现二者的解耦。这种模式涉及到一个作为桥接的接口，使得实体类的功能独立于接口实现类。这两种类型的类可被结构化改变而互不影响。</p><p>桥接模式将抽象部分和实现部分分离，使它们都可以独立变化，主要解决在有很多种可能会变化的情况下，用继承会造成类爆炸的问题，扩展起来不灵活</p><p>其类图如下：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/44E7AD4827D942459E33200CDCCAD2B1/282" alt=""></p><p>桥接模式中的角色及其职责：</p><ul><li>抽象化(Abstraction)角色：抽象化给出的定义，并把保存一个对实现化对象的引用</li><li>修正抽象化(Refine Abstraction)：扩展抽象化角色，改变和修正父类对抽象化的定义</li><li>实现化(Implementor)角色：这个角色给出实现化角色的接口，但不会给出具体的实现。必须指出的是：这个接口不一定和抽象化角色定义的接口相同，实际上，这两个接口可以不一样。实现化角色应当只给出底层的操作，而抽象化角色应当只给出基于底层操作的更高一层的操作</li><li>具体实现化(Concrete Implementor)角色：这个角色给出实现化角色接口的具体实现。</li></ul><p>案例：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/76804902E6BB417588CDFE372EF9B69C/285" alt=""></p><p>代码：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Color</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">showShape</span><span class="params">(<span class="built_in">string</span> shape)</span> </span>= <span class="number">0</span>;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">string</span> name;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Black</span> :</span> <span class="keyword">public</span> Color &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	Black() &#123; <span class="keyword">this</span>-&gt;name = <span class="string">"Black"</span>; &#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">showShape</span><span class="params">(<span class="built_in">string</span> shape)</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="keyword">this</span>-&gt;name &lt;&lt; <span class="string">" "</span> &lt;&lt; shape &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">White</span> :</span> <span class="keyword">public</span> Color &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	White() &#123; <span class="keyword">this</span>-&gt;name = <span class="string">"White"</span>; &#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">showShape</span><span class="params">(<span class="built_in">string</span> shape)</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="keyword">this</span>-&gt;name &lt;&lt; <span class="string">" "</span> &lt;&lt; shape &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Shape</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">draw</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">string</span> name;</span><br><span class="line">	Color *color;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Circle</span> :</span> <span class="keyword">public</span> Shape &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	Circle(Color *color)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;name = <span class="string">"Circle"</span>;</span><br><span class="line">		<span class="keyword">this</span>-&gt;color = color;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">draw</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;color-&gt;showShape(<span class="keyword">this</span>-&gt;name);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Square</span> :</span> <span class="keyword">public</span> Shape &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	Square(Color *color)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;name = <span class="string">"Square"</span>;</span><br><span class="line">		<span class="keyword">this</span>-&gt;color = color;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">draw</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;color-&gt;showShape(<span class="keyword">this</span>-&gt;name);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p><strong>注意</strong>：对于两个独立变化的维度，使用桥接模式再合适不过了</p><h3 id="组合模式"><a href="#组合模式" class="headerlink" title="组合模式"></a>组合模式</h3><p>组合模式（Composite Pattern），又叫部分整体模式，是用于把一组相似的对象当作一个单一的对象。组合模式依据树形结构来组合对象，用来表示部分以及整体层次。这种类型的设计模式属于结构型模式，它创建了对象组的树形结构。这种模式创建了一个包含自己对象组的类。该类提供了修改相同对象组的方式。</p><p>组合模式将对象组合成树形结构以表示”部分-整体”的层次结构，使得用户对单个对象的使用具有一致性</p><p>其类图如下：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/A90D3B8996974083847ECA4F3ED2FE75/288" alt=""></p><p>组合模式的角色及其职责：</p><ul><li>Component：组合中的对象声明接口，在适当的情况下，实现所有类公共接口的默认行为。声明一个接口用于访问和管理Component子部件</li><li>Leaf：在组合中表示叶子节点对象，叶子节点没有子节点</li><li>Composite：定义有枝节点行为，用来存储子部件，在Component接口中实现与子部件有关操作，如增加和删除等</li></ul><h3 id="装饰器模式"><a href="#装饰器模式" class="headerlink" title="装饰器模式"></a>装饰器模式</h3><p>装饰器模式（Decorator Pattern）允许向一个现有的对象添加新的功能，同时又不改变其结构。这种类型的设计模式属于结构型模式，它是作为现有的类的一个包装。这种模式创建了一个装饰类，用来包装原有的类，并在保持类方法签名完整性的前提下，提供了额外的功能。</p><p>装饰器模式动态地给一个对象添加一些额外的职责，就增加功能来说，装饰器模式相比生成子类更为灵活</p><p>装饰器模式指的是在不必改变原类文件和使用继承的情况下，动态地扩展一个对象的功能。它是通过创建一个包装对象，也就是装饰来包裹真实对象。</p><p>其类图如下：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/621BB7D988FE43169D8A3F36BF65CB06/291" alt=""></p><p>装饰器模式的角色及其职责：</p><ul><li>Component：定义一个对象的接口，可以给这些对象动态的添加职责</li><li>Concrete Component：定义一个具体的对象，也可以给这个对象添加一些职责</li><li>Decorater：装饰抽象类，继承了Component，从外类来扩展Component类的功能，但对于Component来说是无需知道Decorater的存在的。</li><li>Concrete Decorater：具体的装饰对象，起到给Component添加职责的功能。</li></ul><p>案例：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/6D6B0F0A4FBA4667B84D8E322B5FA7AD/294" alt=""></p><p>代码：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//抽象英雄</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AbstractHero</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">showStatus</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="keyword">int</span> Hp;</span><br><span class="line">	<span class="keyword">int</span> Mp;</span><br><span class="line">	<span class="keyword">int</span> At;</span><br><span class="line">	<span class="keyword">int</span> Df;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//具体英雄</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">HeroA</span> :</span> <span class="keyword">public</span> AbstractHero &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	HeroA()</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;Hp = <span class="number">0</span>;</span><br><span class="line">		<span class="keyword">this</span>-&gt;Mp = <span class="number">0</span>;</span><br><span class="line">		<span class="keyword">this</span>-&gt;At = <span class="number">0</span>;</span><br><span class="line">		<span class="keyword">this</span>-&gt;Df = <span class="number">0</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">showStatus</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"血量"</span> &lt;&lt; <span class="keyword">this</span>-&gt;Hp &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"魔法"</span> &lt;&lt; <span class="keyword">this</span>-&gt;Mp &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"攻击"</span> &lt;&lt; <span class="keyword">this</span>-&gt;At &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"防御"</span> &lt;&lt; <span class="keyword">this</span>-&gt;Df &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//抽象装备英雄(即穿了装备的英雄)</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AbstractEquipment</span> :</span> <span class="keyword">public</span> AbstractHero &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	AbstractEquipment(AbstractHero *hero)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;pHero = hero;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">showStatus</span><span class="params">()</span></span>&#123;&#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	AbstractHero *pHero;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//装备狂徒的英雄</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">KuangtuEquipment</span> :</span> <span class="keyword">public</span> AbstractEquipment &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	KuangtuEquipment(AbstractHero *hero) :AbstractEquipment(hero)&#123;&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">AddKuangtu</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		 <span class="keyword">this</span>-&gt;Hp = <span class="keyword">this</span>-&gt;pHero-&gt;Hp;</span><br><span class="line">		 <span class="keyword">this</span>-&gt;Mp = <span class="keyword">this</span>-&gt;pHero-&gt;Mp;</span><br><span class="line">		 <span class="keyword">this</span>-&gt;At = <span class="keyword">this</span>-&gt;pHero-&gt;At;</span><br><span class="line">		 <span class="keyword">this</span>-&gt;Df = <span class="keyword">this</span>-&gt;pHero-&gt;Df + <span class="number">30</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">showStatus</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		AddKuangtu();</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"血量"</span> &lt;&lt; <span class="keyword">this</span>-&gt;Hp &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"魔法"</span> &lt;&lt; <span class="keyword">this</span>-&gt;Mp &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"攻击"</span> &lt;&lt; <span class="keyword">this</span>-&gt;At &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"防御"</span> &lt;&lt; <span class="keyword">this</span>-&gt;Df &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//无尽之刃</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">KnifeOfWujin</span> :</span> <span class="keyword">public</span> AbstractEquipment &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	KnifeOfWujin(AbstractHero *hero) :AbstractEquipment(hero)&#123;&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">AddWujin</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;Hp = <span class="keyword">this</span>-&gt;pHero-&gt;Hp;</span><br><span class="line">		<span class="keyword">this</span>-&gt;Mp = <span class="keyword">this</span>-&gt;pHero-&gt;Mp;</span><br><span class="line">		<span class="keyword">this</span>-&gt;At = <span class="keyword">this</span>-&gt;pHero-&gt;At + <span class="number">80</span>;</span><br><span class="line">		<span class="keyword">this</span>-&gt;Df = <span class="keyword">this</span>-&gt;pHero-&gt;Df;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">showStatus</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		AddWujin();</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"血量"</span> &lt;&lt; <span class="keyword">this</span>-&gt;Hp &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"魔法"</span> &lt;&lt; <span class="keyword">this</span>-&gt;Mp &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"攻击"</span> &lt;&lt; <span class="keyword">this</span>-&gt;At &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"防御"</span> &lt;&lt; <span class="keyword">this</span>-&gt;Df &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>调用：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">AbstractHero *hero = <span class="keyword">new</span> HeroA;</span><br><span class="line">hero-&gt;showStatus();</span><br><span class="line"><span class="built_in">cout</span> &lt;&lt; <span class="string">"----------"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line"><span class="comment">//给英雄上装备</span></span><br><span class="line">hero = <span class="keyword">new</span> KuangtuEquipment(hero);</span><br><span class="line">hero-&gt;showStatus();</span><br><span class="line"><span class="built_in">cout</span> &lt;&lt; <span class="string">"----------"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">hero = <span class="keyword">new</span> KnifeOfWujin(hero);</span><br><span class="line">hero-&gt;showStatus();</span><br></pre></td></tr></table></figure><h3 id="外观模式"><a href="#外观模式" class="headerlink" title="外观模式"></a>外观模式</h3><p>根据迪米特法则，如果2个类不必彼此直接通信，那么这两个类就不应当发生直接的相互作用</p><p>外观模式（Facade Pattern）隐藏系统的复杂性，并向客户端提供了一个客户端可以访问系统的接口。这种类型的设计模式属于结构型模式，它向现有的系统添加一个接口，来隐藏系统的复杂性。</p><p>外观模式定义了一个高层，为子系统中的一组接口提供一个一致的外观，从而简化了该子系统的使用</p><p>其类图如下：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/C6ECDCE4249548C293E2FC3FEBB4A9E7/297" alt=""></p><p>外观模式中的角色及其职责：</p><ul><li>Facade(外观角色)：为调用方定义简单的调用接口。</li><li>SubSystem(子系统角色)：功能提供者。指提供功能的类群(模块或子系统)。</li></ul><p>案例：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/75905A8A41B94526BA665E89BEC5C41E/300" alt=""></p><p>代码：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//电视类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Television</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">On</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"TV turn on..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">Off</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"TV turn off..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//灯类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Light</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">On</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"Light turn on..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">Off</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"Light turn off..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//音响类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Audio</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">On</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"Audio turn on..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">Off</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"Audio turn on..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//麦克风类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Mircophone</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">On</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"Mircophone turn on..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">Off</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"Mircophone turn off..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//DVD类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DVDPlayer</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">On</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"DVDPlayer turn on..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">Off</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"DVDPlayer turn off..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//KTV模式(外观类)</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">KTVMode</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	KTVMode()</span><br><span class="line">	&#123;</span><br><span class="line">		pTV = <span class="keyword">new</span> Television;</span><br><span class="line">		pLight = <span class="keyword">new</span> Light;</span><br><span class="line">		pAudio = <span class="keyword">new</span> <span class="built_in">Audio</span>;</span><br><span class="line">		pMircophone = <span class="keyword">new</span> Mircophone;</span><br><span class="line">		pDvd = <span class="keyword">new</span> DVDPlayer;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	~KTVMode()</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">delete</span> pTV;</span><br><span class="line">		<span class="keyword">delete</span> pLight;</span><br><span class="line">		<span class="keyword">delete</span> pAudio;</span><br><span class="line">		<span class="keyword">delete</span> pMircophone;</span><br><span class="line">		<span class="keyword">delete</span> pDvd;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">OnKTV</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		pTV-&gt;On();</span><br><span class="line">		pLight-&gt;Off();</span><br><span class="line">		pAudio-&gt;On();</span><br><span class="line">		pMircophone-&gt;On();</span><br><span class="line">		pDvd-&gt;On();</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">OffKTV</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		pTV-&gt;Off();</span><br><span class="line">		pLight-&gt;On();</span><br><span class="line">		pAudio-&gt;Off();</span><br><span class="line">		pMircophone-&gt;On();</span><br><span class="line">		pDvd-&gt;Off();</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	Television *pTV;</span><br><span class="line">	Light *pLight;</span><br><span class="line">	<span class="built_in">Audio</span> *pAudio;</span><br><span class="line">	Mircophone *pMircophone;</span><br><span class="line">	DVDPlayer *pDvd;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>然后客户就不需要和每个类打交道，直接和外观打交道即可：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">KTVMode ktv;</span><br><span class="line">ktv.OnKTV();</span><br></pre></td></tr></table></figure><p>优点：实现了子系统和客户端之间的松耦合关系；客户端屏蔽了子系统组件，减少了客户端所需处理的对象数目，并使得子系统使用起来更加容易。</p><h3 id="享元模式"><a href="#享元模式" class="headerlink" title="享元模式"></a>享元模式</h3><p>享元模式（Flyweight Pattern）主要用于减少创建对象的数量，以减少内存占用和提高性能。这种类型的设计模式属于结构型模式，它提供了减少对象数量从而改善应用所需的对象结构的方式。享元模式尝试重用现有的同类对象，如果未找到匹配的对象，则创建新对象。</p><p>享元模式运用共享技术有效地支持大量细粒度的对象</p><p>主要解决在有大量对象的时候，有可能会有内存溢出的情况。我们把其中共同的部分抽象出来，如果有相同的业务请求，直接返回在内存中已有的对象，避免重新创建</p><p>其类图如下：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/81A0468FA7794997814AC1B11F7DA5F9/303" alt=""></p><p>享元模式中的角色及其职责：</p><ul><li>抽象享元(Flyweight)角色：所有具体享元类的接口，通过这个接口，Flyweight可以接收并作用于外部</li><li>具体享元(Concrete Flyweight)角色：指定内部状态，为内部状态增加存储空间</li><li>非共享具体享元(UnShared Concrete Flyweight)角色：指出那些不需要共享的Flyweight子类</li><li>享元工厂(Flyweight Factory)角色：用来创建并管理Flyweight对象，它主要用来确保合理地共享Flyweight，当用户请求一个Flyweight时，FlyweightFactory就会提供一个已经创建好的Flyweight对象或者新建一个(如果不存在)。</li><li>享元模式的核心在于享元工厂类，享元工厂类的作用在于提供一个用于存储享元对象的享元池，用户需要对象时，首先从享元池中获取，如果享元池中不存在，则新建一个享元对象返回给用户，并在享元池中保存该新建对象</li></ul><p>案例代码：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Shape</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">draw</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Circle</span> :</span> <span class="keyword">public</span> Shape &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	Circle(<span class="built_in">string</span> color)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;color = color;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">draw</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"Draw a "</span> &lt;&lt; <span class="keyword">this</span>-&gt;color &lt;&lt; <span class="string">" Circle"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="built_in">string</span> color;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">FlyweightFactory</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function">Shape *<span class="title">getShape</span><span class="params">(<span class="built_in">string</span> key)</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">map</span>&lt;<span class="built_in">string</span>, Shape*&gt;::iterator it = shapes.<span class="built_in">find</span>(key);</span><br><span class="line">		</span><br><span class="line">		Shape *shape = <span class="literal">NULL</span>;</span><br><span class="line">		<span class="keyword">if</span> (it == shapes.<span class="built_in">end</span>()) &#123;</span><br><span class="line">			shape = <span class="keyword">new</span> Circle(key);</span><br><span class="line">			shapes.insert(make_pair(key, shape));</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span> &#123;</span><br><span class="line">			shape = it-&gt;second;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> shape;</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">map</span>&lt;<span class="built_in">string</span>, Shape*&gt; shapes;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>调用：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">FlyweightFactory *fw = <span class="keyword">new</span> FlyweightFactory;</span><br><span class="line">Shape *sh1 = fw-&gt;getShape(<span class="string">"Red"</span>);</span><br><span class="line">sh1-&gt;draw();</span><br><span class="line">Shape *sh2 =  fw-&gt;getShape(<span class="string">"White"</span>);</span><br><span class="line">sh2-&gt;draw();</span><br><span class="line">Shape *sh3 = fw-&gt;getShape(<span class="string">"Black"</span>);</span><br><span class="line">sh3-&gt;draw();</span><br><span class="line">Shape *sh4 = fw-&gt;getShape(<span class="string">"Red"</span>);</span><br><span class="line">sh4-&gt;draw();</span><br><span class="line">Shape *sh5 = fw-&gt;getShape(<span class="string">"White"</span>);</span><br><span class="line">sh5-&gt;draw();</span><br><span class="line">Shape *sh6 = fw-&gt;getShape(<span class="string">"Black"</span>);</span><br><span class="line">sh6-&gt;draw();</span><br><span class="line"><span class="keyword">int</span> n = fw-&gt;shapes.<span class="built_in">size</span>();</span><br><span class="line"><span class="built_in">cout</span> &lt;&lt; n &lt;&lt; <span class="built_in">endl</span>;</span><br></pre></td></tr></table></figure><p>由输出结果可以只创建了 3 个 Circle 对象</p><h3 id="代理模式"><a href="#代理模式" class="headerlink" title="代理模式"></a>代理模式</h3><p>代理模式(Proxy Pattern)定义：为其他对象提供一种代理以控制对这个对象的访问。在代理模式中，一个类代表另一个类的功能。这种类型的设计模式属于结构型模式。在代理模式中，我们创建具有现有对象的对象，以便向外界提供功能接口。</p><p>代理模式为其他对象提供一种代理以控制对这个对象的访问。主要解决的问题是在直接访问对象时带来的问题</p><p>在某些情况下，一个对象不适合或者不能直接引用另一个对象，而代理对象可以在客户端和目标对象之间起到中介的作用。</p><p>其类图如下：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/FD6061FE0902443999C1ECF0A9353236/306" alt=""></p><p>代理模式中的角色及其职责：</p><ul><li>抽象主题(Subject)角色：真实主题与代理主题的共同接口。</li><li>真实主题(RealSubject)角色：定义了代理角色所代表的真实对象。</li><li>代理主题(Proxy)角色：含有对真实主题角色的引用，代理角色通常在将客户段调用传递给真实主题对象之前或者之后执行某些操作，而不是单纯返回真实对象。</li></ul><p>案例：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/37099C27A2894812B9D2C849619C0125/309" alt=""></p><p>代码：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//抽象系统</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AbstractSystem</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//我已经写好的系统</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MySystem</span> :</span> <span class="keyword">public</span> AbstractSystem &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"系统启动..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//通过代理进入系统</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MySystemProxy</span> :</span> <span class="keyword">public</span> AbstractSystem &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	MySystemProxy(<span class="built_in">string</span> UserName, <span class="built_in">string</span> Password)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;UserName = UserName;</span><br><span class="line">		<span class="keyword">this</span>-&gt;Password = Password;</span><br><span class="line">		pSystem = <span class="keyword">new</span> MySystem;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">if</span> (checkUser())&#123;</span><br><span class="line">			<span class="built_in">cout</span> &lt;&lt; <span class="string">"用户名和密码正确，验证通过..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">			pSystem-&gt;<span class="built_in">run</span>();</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span>&#123;</span><br><span class="line">			<span class="built_in">cout</span> &lt;&lt; <span class="string">"用户名或密码错误，无法登陆..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	~MySystemProxy()</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">if</span> (pSystem != <span class="literal">NULL</span>)&#123;</span><br><span class="line">			<span class="keyword">delete</span> pSystem;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="function"><span class="keyword">bool</span> <span class="title">checkUser</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">if</span> (<span class="keyword">this</span>-&gt;UserName == <span class="string">"admin"</span> &amp;&amp; <span class="keyword">this</span>-&gt;Password == <span class="string">"admin"</span>)&#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">else</span>&#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">string</span> UserName;</span><br><span class="line">	<span class="built_in">string</span> Password;</span><br><span class="line">	MySystem *pSystem;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>然后通过代理调用系统run函数</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">MySystemProxy <span class="title">msp</span><span class="params">(<span class="string">"admin"</span>, <span class="string">"admin"</span>)</span></span>;</span><br><span class="line">msp.<span class="built_in">run</span>();</span><br></pre></td></tr></table></figure><h2 id="行为型模式"><a href="#行为型模式" class="headerlink" title="行为型模式"></a>行为型模式</h2><p>这些设计模式特别关注对象之间的通信</p><h3 id="责任链模式"><a href="#责任链模式" class="headerlink" title="责任链模式"></a>责任链模式</h3><p>责任链模式（Chain of Responsibility Pattern）为请求创建了一个接收者对象的链。这种模式给予请求的类型，对请求的发送者和接收者进行解耦。这种类型的设计模式属于行为型模式。在这种模式中，通常每个接收者都包含对另一个接收者的引用。如果一个对象不能处理该请求，那么它会把相同的请求传给下一个接收者，依此类推。</p><p>责任链模式通过给多个对象处理请求的机会，减少请求的发送者与接收者之间的耦合。将接收对象链接起来，在链中传递请求，直到有一个对象处理这个请求</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/1669AAC6417743C7B64E06F5EFC06BF9/312" alt=""></p><p>责任链模式中的角色及其职责：</p><ul><li>抽象处理者（Handle）角色：定义出一个处理请求的接口。如果需要，接口可以定义 出一个方法以设定和返回对下家的引用。</li><li>具体处理者（Concrete Handle）角色：具体处理者接到请求后，可以选择将请求处理掉，或者将请求传给下家。由于具体处理者持有对下家的引用，因此，如果需要，具体处理者可以访问下家。</li></ul><h3 id="命令模式"><a href="#命令模式" class="headerlink" title="命令模式"></a>命令模式</h3><p>命令模式（Command Pattern）是一种数据驱动的设计模式。请求以命令的形式包裹在对象中，并传给调用对象。调用对象寻找可以处理该命令的合适的对象，并把该命令传给相应的对象，该对象执行命令。</p><p>命令模式将一个请求封装为一个对象，从而让我们可用不同的请求对客户进行参数化；对请求排队或者记录请求日志，以及支持可撤销操作。命令模式是一种对象行为型模式。</p><p>命令模式可以将请求发送者和请求接收者完全解耦，发送者与接收者之间没有直接引用关系，发送请求的对象只需要知道如何发送请求，而不必知道如何完成请求。</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/F75652BAA5774D16820373A8CCB08271/315" alt=""></p><p>命令模式的角色及其职责：</p><ul><li>AbstractCommand(命令接口)角色：定义命令的接口，声明执行的方法。</li><li>ConcreteCommand(具体命令)角色：命令接口的具体实现，通常会持有接收者，并调用接收者的功能来完成命令要执行的操作</li><li>Receiver(接收者)角色：真正执行命令的对象，任何类都可能成为接收者，只要它能实现命令要求的功能。</li><li>Server(服务器)角色：要求命令对象执行请求，可以持有很多的命令对象。这个是客户端真正触发命令并开始要求命令执行相应操作的地方，也就是相当于使用命令对象的入口</li></ul><p>案例：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/F83E2527EC9C411F9D8AAB93D61C5ED1/318" alt=""></p><p>代码：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//协议处理类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">HandleClientProtocol</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="comment">//处理增加金币</span></span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">AddMoney</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"给玩家增加金币..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//处理增加钻石</span></span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">AddDiamond</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"给玩家增加钻石..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//处理玩家装备</span></span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">AddEquipment</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"给玩家穿装备..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//命令接口</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AbstractCommand</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">handle</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//处理增加金币请求</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AddMoneyCommand</span> :</span> <span class="keyword">public</span> AbstractCommand &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	AddMoneyCommand(HandleClientProtocol *protocol)&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;pProtocol = protocol;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">handle</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;pProtocol-&gt;AddMoney();</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	HandleClientProtocol *pProtocol;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//处理增加钻石请求</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AddDiamondCommand</span> :</span> <span class="keyword">public</span> AbstractCommand &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	AddDiamondCommand(HandleClientProtocol *protocol)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;pProtocol = protocol;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">handle</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;pProtocol-&gt;AddDiamond();</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	HandleClientProtocol *pProtocol;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//处理增加装备请求</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AddEquipmentCommand</span> :</span> <span class="keyword">public</span> AbstractCommand &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	AddEquipmentCommand(HandleClientProtocol *protocol)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;pProtocol = protocol;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">handle</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;pProtocol-&gt;AddEquipment();</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	HandleClientProtocol *pProtocol;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//服务器程序</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Server</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">addRequest</span><span class="params">(AbstractCommand *command)</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;command.push(command);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">startHandle</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">while</span> (!command.empty())&#123;</span><br><span class="line">			AbstractCommand *command = <span class="keyword">this</span>-&gt;command.front();</span><br><span class="line">			command-&gt;handle();</span><br><span class="line">			<span class="keyword">this</span>-&gt;command.pop();</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">queue</span>&lt;AbstractCommand*&gt; command;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>客户端通过服务器发出请求：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">HandleClientProtocol *protocol = <span class="keyword">new</span> HandleClientProtocol;</span><br><span class="line"><span class="comment">//客户端增加金币的请求</span></span><br><span class="line">AbstractCommand *addMoney = <span class="keyword">new</span> AddMoneyCommand(protocol);</span><br><span class="line"><span class="comment">//客户端增加钻石的请求</span></span><br><span class="line">AbstractCommand *addDiamond = <span class="keyword">new</span> AddDiamondCommand(protocol);</span><br><span class="line"><span class="comment">//客户端增加装备的请求</span></span><br><span class="line">AbstractCommand *addEquipment = <span class="keyword">new</span> AddEquipmentCommand(protocol);</span><br><span class="line"></span><br><span class="line"><span class="built_in">Server</span> *server = <span class="keyword">new</span> <span class="built_in">Server</span>;</span><br><span class="line">server-&gt;addRequest(addMoney);</span><br><span class="line">server-&gt;addRequest(addDiamond);</span><br><span class="line">server-&gt;addRequest(addEquipment);</span><br><span class="line"></span><br><span class="line"><span class="comment">//服务器开始处理</span></span><br><span class="line">server-&gt;startHandle();</span><br></pre></td></tr></table></figure><h3 id="解释器模式"><a href="#解释器模式" class="headerlink" title="解释器模式"></a>解释器模式</h3><p>解释器模式（Interpreter Pattern）提供了评估语言的语法或表达式的方式，它属于行为型模式。这种模式实现了一个表达式接口，该接口解释一个特定的上下文。这种模式被用在 SQL 解析、符号处理引擎等。</p><p>解释器模式给定一种语言，定义它的文法表示，并定义一个解释器，该解释器用于根据文法表示来翻译语言中的句子。主要用于解决对于一些固定文法构建一个解释句子的解释器</p><h3 id="迭代器模式"><a href="#迭代器模式" class="headerlink" title="迭代器模式"></a>迭代器模式</h3><p>迭代器模式（Iterator Pattern）用于顺序访问集合对象的元素，不需要知道集合对象的底层表示。</p><p>迭代器模式提供一种方法顺序访问一个聚合对象中的各个元素，而又无需暴露该对象的内部表示。主要是用不同的方式来遍历整个对象。</p><h3 id="中介者模式"><a href="#中介者模式" class="headerlink" title="中介者模式"></a>中介者模式</h3><p>中介者模式（Mediator Pattern）是用来降低多个对象和类之间的通信复杂性。这种模式提供了一个中介类，该类通常处理不同类之间的通信，并支持松耦合，使代码易于维护。中介者模式属于行为型模式。</p><p>中介者模式用一个中介对象来封装一系列的对象交互，中介者使个各对象不需要显式地相互引用，从而使其耦合松散，而且可以独立地改变它们之间的交互。</p><h3 id="备忘录模式"><a href="#备忘录模式" class="headerlink" title="备忘录模式"></a>备忘录模式</h3><p>备忘录模式（Memento Pattern）保存一个对象的某个状态，以便在适当的时候恢复对象。备忘录模式属于行为型模式。</p><p>备忘录模式在不破坏封装性的前提下，捕获一个对象的内部状态，并在该对象之外保存这个状态，从而可以在以后将该对象恢复到原来保存的状态。</p><h3 id="观察者模式"><a href="#观察者模式" class="headerlink" title="观察者模式"></a>观察者模式</h3><p>观察者模式（Observer Pattern）是用于建立一种对象与对象之间的依赖关系，一个对象发生改变时将自动通知其他对象，其他对象将相应作出反应。在观察者模式中，发生改变的对象称为观察目标，而被通知的对象称为观察者，一个观察目标可以对应多个观察者。</p><p>观察者模式定义对象间的一种一对多的依赖关系，当一个对象的状态发生改变时，所有依赖与它的对象都得到通知并更新。</p><p>简答来说，观察者模式就是一种”注册–删除–通知”的形式。</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/4A9D1AEF63D74F119945D904CD00391D/321" alt=""></p><p>观察者模式中的角色及其作用：</p><ul><li>观察者(Observer)角色：将自己注册到观察目标中，被观察目标放在一个容器里</li><li>观察目标(Subject)角色：观察目标发生变化后通知容器中的所有观察者</li></ul><p>案例：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/FDF19820E62A4B20A7EE282F51E730E6/324" alt=""></p><p>代码：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//抽象英雄 观察者</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AbstractHero</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Update</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">HeroA</span> :</span> <span class="keyword">public</span> AbstractHero &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	HeroA()</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"A打boss中..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Update</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"A停止打boss，待机中... "</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">HeroB</span> :</span> <span class="keyword">public</span> AbstractHero &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	HeroB()</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"B打boss中..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Update</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"B停止打boss，待机中... "</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">HeroC</span> :</span> <span class="keyword">public</span> AbstractHero &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	HeroC()</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"C打boss中..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Update</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"C停止打boss，待机中... "</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//抽象BOSS 观察目标</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AbstractBoss</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="comment">//添加观察者</span></span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">addHero</span><span class="params">(AbstractHero *hero)</span> </span>= <span class="number">0</span>;</span><br><span class="line">	<span class="comment">//删除观察者</span></span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">delHero</span><span class="params">(AbstractHero *hero)</span> </span>= <span class="number">0</span>;</span><br><span class="line">	<span class="comment">//通知所有观察者</span></span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">notify</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Boss</span> :</span> <span class="keyword">public</span> AbstractBoss &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">addHero</span><span class="params">(AbstractHero *hero)</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		pHeroList.push_back(hero);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">delHero</span><span class="params">(AbstractHero *hero)</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		pHeroList.<span class="built_in">remove</span>(hero);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">notify</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">for</span> (<span class="built_in">list</span>&lt;AbstractHero*&gt;::iterator it = </span><br><span class="line">		pHeroList.<span class="built_in">begin</span>(); </span><br><span class="line">it != pHeroList.<span class="built_in">end</span>(); it++)&#123;</span><br><span class="line">			(*it)-&gt;Update();</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">list</span>&lt;AbstractHero*&gt; pHeroList;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>调用：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//创建观察者</span></span><br><span class="line">AbstractHero *heroA = <span class="keyword">new</span> HeroA;</span><br><span class="line">AbstractHero *heroB = <span class="keyword">new</span> HeroB;</span><br><span class="line">AbstractHero *heroC = <span class="keyword">new</span> HeroC;</span><br><span class="line"><span class="comment">//创建观察目标</span></span><br><span class="line">AbstractBoss *boss = <span class="keyword">new</span> Boss;</span><br><span class="line">boss-&gt;addHero(heroA);</span><br><span class="line">boss-&gt;addHero(heroB);</span><br><span class="line">boss-&gt;addHero(heroC);</span><br><span class="line"></span><br><span class="line"><span class="built_in">cout</span> &lt;&lt; <span class="string">"heroC阵亡..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">boss-&gt;delHero(heroC);</span><br><span class="line"></span><br><span class="line"><span class="built_in">cout</span> &lt;&lt; <span class="string">"Boss 死亡..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">boss-&gt;notify();</span><br></pre></td></tr></table></figure><h3 id="状态模式"><a href="#状态模式" class="headerlink" title="状态模式"></a>状态模式</h3><p>状态模式（State Pattern）中，类的行为是基于它的状态改变的。这种类型的设计模式属于行为型模式。</p><p>状态模式允许对象在内部状态发生改变时改变它的行为，对象看起来好像修改了它的类。</p><h3 id="策略模式"><a href="#策略模式" class="headerlink" title="策略模式"></a>策略模式</h3><p>策略模式（Strategy Pattern）定义了一系列的算法，并将每一个算法封装起来，而且使它们还可以相互替换。策略模式让算法独立于使用它的客户而独立变化。</p><p>策略模式定义了一系列的算法，把它们一个个封装起来，并且使它们可以相互替换，从而让算法可以独立于使用它的用户而变化。</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/CDD55227F2EA49F890EB8673EA3B857E/327" alt=""></p><p>策略模式中的角色及其职责：</p><ul><li>抽象策略(Strategy)角色：策略类，通常由一个接口或者抽象类来实现</li><li>具体策略(Concrete Strategy)角色：包装了相关的算法和行为</li><li>环境(Context)角色：持有一个策略类的引用，最终给客户端调用</li></ul><p>案例：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/39B97899156B4E608BC0BD5F3124A46E/330" alt=""></p><p>代码：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//武器策略</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">WeaponStrategy</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">UseWeapon</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//匕首类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Knife</span> :</span> <span class="keyword">public</span> WeaponStrategy &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">UseWeapon</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"Use knife... "</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//AK47类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AK47</span> :</span> <span class="keyword">public</span> WeaponStrategy &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">UseWeapon</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"Use AK47..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//角色类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Character</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">setWeapon</span><span class="params">(WeaponStrategy *weapon)</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;pWeapon = weapon;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">UseMyWeapon</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;pWeapon-&gt;UseWeapon();</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	WeaponStrategy *pWeapon;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>通过角色使用武器：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Character *character = <span class="keyword">new</span> Character;</span><br><span class="line">WeaponStrategy *knife = <span class="keyword">new</span> Knife;</span><br><span class="line">WeaponStrategy *ak47 = <span class="keyword">new</span> AK47;</span><br><span class="line"></span><br><span class="line">character-&gt;setWeapon(knife);</span><br><span class="line">character-&gt;UseMyWeapon();</span><br><span class="line"></span><br><span class="line">character-&gt;setWeapon(ak47);</span><br><span class="line">character-&gt;UseMyWeapon();</span><br></pre></td></tr></table></figure><p>策略模式主要解决的问题：在多种算法相似的情况下，使用if…else所带来的复杂性和难以维护</p><p>优点：</p><ol><li>算法可以自由切换</li><li>避免使用多重条件判断</li><li>扩展性良好</li></ol><p>缺点：</p><ol><li>策略类会增多</li><li>所有的策略类都需要对外暴露</li></ol><h3 id="模板模式"><a href="#模板模式" class="headerlink" title="模板模式"></a>模板模式</h3><p>在模板模式（Template Pattern）中，一个抽象类公开定义了执行它的方法的方式/模板。它的子类可以按需要重写方法实现，但调用将以抽象类中定义的方式进行。这种类型的设计模式属于行为型模式。</p><p>模板模式（Template Pattern）定义一个操作中算法的框架，而将一些步骤延迟到子类中。模板方法模式使得子类可以不改变一个算法的结构即可重定义该算法的某些特定步骤。</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/0D83FEC83E1142F8A1A5756C435516D8/333" alt=""></p><p>模板模式中的角色及其职责：</p><ul><li>AbstractClass(抽象类)：在抽象类中定义了一系列基本操作，这些基本操作可以是具体的，也可以是抽象的，每一个基本操作对应算法的一个步骤，在其子类中可以重定义或实现这些步骤。同时，在抽象类中实现了一个模板方法(Template Method)，用于定义一个算法的框架，模板方法不仅可以调用在抽象类中实现的基本方法，也可以调用在抽象类的子类中实现的基本方法，还可以调用其他对象中的方法。</li><li>ConcreteClass(具体子类)：它是抽象类的子类，用于实现在父类中声明的抽象基本操作以完成子类特定算法的步骤，也可以覆盖在父类中已经实现的具体基本操作。</li></ul><p>案例：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/435862019A2F4A79BCED14FCB1DC6DB3/336" alt=""></p><p>代码：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//制作饮料</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DrinkTemplate</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="comment">//煮开水</span></span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">BoilWater</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">	<span class="comment">//冲泡</span></span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Brew</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">	<span class="comment">//倒入杯中</span></span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">PourInCup</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">	<span class="comment">//加料</span></span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">AddSomething</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">	<span class="comment">//模板方法：</span></span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">Make</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		BoilWater();</span><br><span class="line">		Brew();</span><br><span class="line">		PourInCup();</span><br><span class="line">		AddSomething();</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//冲泡咖啡</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Coffee</span> :</span> <span class="keyword">public</span> DrinkTemplate &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="comment">//煮开水</span></span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">BoilWater</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"煮山泉水..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//冲泡</span></span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Brew</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"冲泡咖啡..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//倒入杯中</span></span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">PourInCup</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"咖啡倒入杯中..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//加料</span></span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">AddSomething</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"加糖和牛奶..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//冲泡茶水</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Tea</span> :</span> <span class="keyword">public</span> DrinkTemplate &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="comment">//煮开水</span></span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">BoilWater</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"煮自来水... "</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//冲泡</span></span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Brew</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"冲泡铁观音..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//倒入杯中</span></span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">PourInCup</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"茶水倒入杯中..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//加料</span></span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">AddSomething</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		<span class="built_in">cout</span> &lt;&lt; <span class="string">"加柠檬和生姜..."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>然后就可以创建对象并调用了：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Tea *tea = <span class="keyword">new</span> Tea;</span><br><span class="line">tea-&gt;Make();</span><br><span class="line"><span class="built_in">cout</span> &lt;&lt; <span class="string">"-------------"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">Coffee *coffee = <span class="keyword">new</span> Coffee;</span><br><span class="line">coffee-&gt;Make();</span><br></pre></td></tr></table></figure><p>模板方法模式优点：</p><ul><li>在父类中形式化地定义一个算法，而由子类来实现细节的处理，在子类实现详细的处理算法时并不会改变算法中步骤的执行次序。</li><li>模板方法模式是一种代码复用技术，它在类库设计中尤为重要，它提取了类库中的公共行为，将公共行为放在父类中，而通过其子类来实现不同的行为，它鼓励我们恰当使用继承来实现代码复用。</li><li>可实现一种反向控制结构，通过子类覆盖父类的构造方法来决定某一特定步骤是否执行。</li><li>在模板方法模式中可以通过子类来覆盖父类的基本方法，不同的子类可以提供基本方法的不同实现，更换和增加新的子类很方便，符合单一职责原则和开闭原则。</li></ul><p>缺点：需要为每一个基本方法的不同实现提供一个子类，如果父类中可变的基本方法太多，将会导致类的个数增加，系统更加庞大，设计也更加抽象。</p><p>适用场景：</p><ol><li>具有统一的操作步骤或操作过程；</li><li>具有不同的操作细节；</li><li>存在多个具有同样操作步骤的应用场景，但某些具体的操作细节却各不相同；</li></ol><p><strong>在抽象类中统一操作步骤，并规定好接口；让子类实现接口。这样可以把各个具体的子类和操作步骤解耦合。</strong></p><h3 id="访问者模式"><a href="#访问者模式" class="headerlink" title="访问者模式"></a>访问者模式</h3><p>在访问者模式（Visitor Pattern）中，我们使用了一个访问者类，它改变了元素类的执行算法。通过这种方式，元素的执行算法可以随着访问者改变而改变。这种类型的设计模式属于行为型模式。根据模式，元素对象已接受访问者对象，这样访问者对象就可以处理元素对象上的操作。</p><p>访问者模式表示一个作用于某对象结构中的各元素的操作，使得在不改变各元素的类的前提下定义作用于这些元素的新操作。</p>]]></content>
      <categories>
        <category>Other</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>Java 面试题</title>
    <url>/java/48704b01-97d8-4702-a476-d9e013ac36c2.html</url>
    <content><![CDATA[<p>这篇文章为本人整理的 Java 方向面试题，其中包含 JavaSE、JavaEE、SSM 框架、MySQL、网络、Redis 等内容的面试题及其答案。</p><a id="more"></a><h2 id="Java-基础"><a href="#Java-基础" class="headerlink" title="Java 基础"></a>Java 基础</h2><h3 id="JDK-和-JRE"><a href="#JDK-和-JRE" class="headerlink" title="JDK 和 JRE"></a>JDK 和 JRE</h3><ul><li>JDK(Java Development Kit)，java 开发工具包，提供了 java 的开发环境和运行环境。</li><li>JRE(Java Runtime Environment)，java 运行环境，为 java 的运行提供了所需环境。</li></ul><h3 id="final"><a href="#final" class="headerlink" title="final"></a>final</h3><ul><li>final 修饰的类叫最终类，该类不能被继承。</li><li>final 修饰的方法不能被重写。</li><li>final 修饰的变量叫常量，常量必须初始化，初始化之后值就不能被修改。</li></ul><p><strong>final、finally、finalize 之间的区别</strong></p><ul><li>final 可以修饰类、变量、方法，修饰类表示该类不能被继承、修饰方法表示该方法不能被重写、修饰变量表示该变量是一个常量不能被重新赋值。</li><li>finally 一般作用在 try-catch 代码块中，在处理异常的时候，通常我们将一定要执行的代码方法 finally 代码块中，表示不管是否出现异常，该代码块都会执行，一般用来存放一些关闭资源的代码。</li><li>finalize 是一个方法，属于 Object 类的一个方法，而 Object 类是所有类的父类，该方法一般由垃圾回收器来调用，当我们调用 System 的 gc() 方法的时候，由垃圾回收器调用 finalize(),回收垃圾。</li></ul><h3 id="switch"><a href="#switch" class="headerlink" title="switch"></a>switch</h3><p>Java5 以前 <code>switch(expression)</code> 中，expression 只能是 byte、short、char、int，严格意义上来讲 Java5 以前只支持 int，之所以能使用byte short char 是因为存在自动类型转换。从 Java 5 开始，Java 中引入了枚举类型，expression 也可以是 enum 类型。从 Java 7 开始，expression 还可以是字符串（String），但是长整型（long）在目前所有的版本中都是不可以的。</p><h3 id="amp-、-amp-amp"><a href="#amp-、-amp-amp" class="headerlink" title="&amp;、&amp;&amp;"></a>&amp;、&amp;&amp;</h3><ul><li>&amp; 运算符是：逻辑与；&amp;&amp; 运算符是：短路与。</li><li>&amp; 和 &amp;&amp; 在程序中最终的运算结果是完全一致的，只不过 &amp;&amp; 存在短路现象，当 &amp;&amp; 运算符左边的表达式结果为 false 的时候，右边的表达式不执行，此时就发生了短路现象。如果是 &amp; 运算符，那么不管左边的表达式是 true 还是 false，右边表达式是一定会执行的。这就是他们俩的本质区别。</li><li>当然，&amp; 运算符还可以使用在二进制位运算上，例如按位与操作。</li></ul><p>各运算符的功能如下：</p><table><thead><tr><th align="center">运算符</th><th align="center">描述</th><th align="center">示例</th><th align="center">结果</th></tr></thead><tbody><tr><td align="center"><code>&amp;</code></td><td align="center">与</td><td align="center"><code>false &amp; true</code></td><td align="center">false</td></tr><tr><td align="center"><code>|</code></td><td align="center">或</td><td align="center"><code>false | true</code></td><td align="center">true</td></tr><tr><td align="center"><code>^</code></td><td align="center">异或</td><td align="center"><code>true ^ false</code></td><td align="center">true</td></tr><tr><td align="center"><code>!</code></td><td align="center">非</td><td align="center"><code>!true</code></td><td align="center">false</td></tr><tr><td align="center"><code>&amp;&amp;</code></td><td align="center">短路与</td><td align="center"><code>flase &amp;&amp; true</code></td><td align="center">false</td></tr><tr><td align="center"><code>||</code></td><td align="center">短路或</td><td align="center"><code>false || true</code></td><td align="center">true</td></tr></tbody></table><h3 id="多重循环"><a href="#多重循环" class="headerlink" title="多重循环"></a>多重循环</h3><p><strong>在 Java 中，如何跳出多重循环？</strong><br>在最外层循环前加一个标记如outfor，然后用break outfor;可以跳出多重循环。例如以下代码：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestBreak</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        outfor: <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++)&#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; <span class="number">10</span>; j++)&#123;</span><br><span class="line">                <span class="keyword">if</span> (j == <span class="number">5</span>)&#123;</span><br><span class="line">                    <span class="keyword">break</span> outfor;</span><br><span class="line">                &#125;</span><br><span class="line">                System.out.println(<span class="string">"j = "</span> + j);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>运行结果如下所示：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">j &#x3D; 0</span><br><span class="line">j &#x3D; 1</span><br><span class="line">j &#x3D; 2</span><br><span class="line">j &#x3D; 3</span><br><span class="line">j &#x3D; 4</span><br></pre></td></tr></table></figure><h3 id="数据类型"><a href="#数据类型" class="headerlink" title="数据类型"></a>数据类型</h3><p><strong>Java 的基本数据类型有哪些，各占几个字节？</strong></p><ul><li>数据类型：byte short int long float double boolean char</li><li>占用字节数：12484812（byte对应1，short对应2，以此类推）</li></ul><p><strong><code>short s1 = 1; s1 = s1 + 1;</code> 与 <code>short s1 = 1; s1 += 1;</code></strong></p><ul><li>前者不正确，后者正确。</li><li>对于 <code>short s1 = 1; s1 = s1 + 1;</code> 由于 1 是 int 类型，因此 <code>s1+1</code> 运算结果也是 int 型，需要强制转换类型才能赋值给 short 型。</li><li>而 <code>short s1 = 1; s1 += 1;</code> 可以正确编译，因为 <code>s1+= 1;</code> 相当于 <code>s1 = (short)(s1 + 1);</code> 其中有隐含的强制类型转换。</li></ul><p><strong>int 和 Integer 有什么区别？</strong><br>java 是一个完全面向对象编程语言，但是为了编程的方便还是引入了基本数据类型，为了能够将这些基本数据类型当成对象操作，Java 为每一个基本数据类型都引入了对应的包装类型（wrapper class），int 的包装类就是Integer，从 Java 5 开始引入了自动装箱/拆箱机制，使得二者可以相互转换。<br>java 为每个原始类型提供了包装类型：</p><ul><li>基本数据类型: boolean，char，byte，short，int，long，float，double</li><li>包装类型：Boolean，Character，Byte，Short，Integer，Long，Float，Double</li></ul><p><strong>基本数据类型与 String 之间的转换</strong></p><ul><li>字符串如何转基本数据类型：调用基本数据类型对应的包装类中的方法 <code>parseXXX(String)</code> 或 <code>valueOf(String)</code> 即可返回相应基本类型。</li><li>基本数据类型如何转字符串：一种方法是将基本数据类型与空字符串（””）连接（+）即可获得其所对应的字符串；另一种方法是调用 String 类中的 valueOf() 方法返回相应字符串。</li></ul><h3 id="抽象类"><a href="#抽象类" class="headerlink" title="抽象类"></a>抽象类</h3><p><strong>普通类和抽象类有哪些区别？</strong></p><ul><li>普通类不能包含抽象方法，抽象类可以包含抽象方法。</li><li>抽象类不能直接实例化，普通类可以直接实例化。</li></ul><p><strong>抽象类必须要有抽象方法吗？</strong></p><ul><li>不需要，抽象类不一定非要有抽象方法。</li></ul><p><strong>抽象类能使用 final 修饰吗？</strong></p><ul><li>不能，定义抽象类就是让其他类继承的，如果定义为 final 该类就不能被继承，这样彼此就会产生矛盾</li></ul><p><strong>接口和抽象类有什么区别？</strong></p><ul><li>实现：抽象类的子类使用 extends 来继承；接口必须使用 implements 来实现接口。</li><li>构造函数：抽象类可以有构造函数；接口不能有。</li><li>main 方法：抽象类可以有 main 方法，并且我们能运行它；接口不能有 main 方法。</li><li>实现数量：类可以实现很多个接口；但是只能继承一个抽象类。</li><li>访问修饰符：接口中的方法默认使用 public 修饰；抽象类中的方法可以是任意访问修饰符。</li></ul><h3 id="Files"><a href="#Files" class="headerlink" title="Files"></a>Files</h3><p><strong>Files的常用方法都有哪些？</strong></p><ul><li>Files.exists()：检测文件路径是否存在。</li><li>Files.createFile()：创建文件。</li><li>Files.createDirectory()：创建文件夹。</li><li>Files.delete()：删除一个文件或目录。</li><li>Files.copy()：复制文件。</li><li>Files.move()：移动文件。</li><li>Files.size()：查看文件个数。</li><li>Files.read()：读取文件。</li><li>Files.write()：写入文件。</li></ul><h3 id="反射"><a href="#反射" class="headerlink" title="反射"></a>反射</h3><p>反射主要是指在程序运行期间可以访问、检测和修改它本身状态或行为的一种能力</p><p>Java反射机制主要提供了以下功能：</p><ul><li>在运行时判断任意一个对象所属的类。</li><li>在运行时构造任意一个类的对象。</li><li>在运行时判断任意一个类所具有的成员变量和方法。</li><li>在运行时调用任意一个对象的方法。</li></ul><p>获取一个对象对应的反射类，在 Java 中有下列方法可以获取一个对象的反射类</p><ul><li>通过 <code>getClass()</code> 方法</li><li>通过 <code>Class.forName()</code> 方法</li><li>使用 <code>类.class</code></li><li>通过类加载器实现，<code>getClassLoader()</code></li></ul><h3 id="序列化"><a href="#序列化" class="headerlink" title="序列化"></a>序列化</h3><p>简单说就是为了保存在内存中的各种对象的状态（也就是实例变量，不是方法），并且可以把保存的对象状态再读出来。虽然你可以用你自己的各种各样的方法来保存object states，但是Java给你提供一种应该比你自己好的保存对象状态的机制，那就是序列化。</p><p>什么情况下需要序列化：</p><ul><li>a）当你想把的内存中的对象状态保存到一个文件中或者数据库中时候；</li><li>b）当你想用套接字在网络上传送对象的时候；</li><li>c）当你想通过RMI传输对象的时候；</li></ul><h3 id="对象克隆"><a href="#对象克隆" class="headerlink" title="对象克隆"></a>对象克隆</h3><p>有两种方式：</p><ul><li>1). 实现Cloneable接口并重写Object类中的clone()方法；</li><li>2). 实现Serializable接口，通过对象的序列化和反序列化实现克隆，可以实现真正的深度克隆</li></ul><h3 id="深拷贝和浅拷贝"><a href="#深拷贝和浅拷贝" class="headerlink" title="深拷贝和浅拷贝"></a>深拷贝和浅拷贝</h3><ul><li>浅拷贝只是复制了对象的引用地址，两个对象指向同一个内存地址，所以修改其中任意的值，另一个值都会随之变化，这就是浅拷贝（例：assign()）</li><li>深拷贝是将对象及值复制过来，两个对象修改其中任意的值另一个值不会改变，这就是深拷贝（例：JSON.parse()和JSON.stringify()，但是此方法无法复制函数类型）</li></ul><h3 id="JDK-8-新特性"><a href="#JDK-8-新特性" class="headerlink" title="JDK 8 新特性"></a>JDK 8 新特性</h3><ul><li>接口的默认方法只需要使用 default 关键字即可，这个特征又叫做 <strong>扩展方法</strong></li><li>Lambda 表达式</li><li>使用 <code>::</code> 双冒号关键字来传递方法(静态方法和非静态方法)</li><li>Functional 接口 函数式接口 是指仅仅只包含一个抽象方法的接口，每一个该类型的 Lambda 表达式都会被匹配到这个抽象方法。你只需要给你的接口添加 @FunctionalInterface 注解</li><li>Function 接口<ul><li>Function 有一个参数并且返回一个结果，并附带了一些可以和其他函数组合的默认方法</li><li>compose 方法表示在某个方法之前执行</li><li>andThen 方法表示在某个方法之后执行</li><li>注意：compose 和 andThen 方法调用之后都会把对象自己本身返回，这可以 方便链式编程</li></ul></li></ul><h2 id="面向对象"><a href="#面向对象" class="headerlink" title="面向对象"></a>面向对象</h2><p><strong>谈谈你对面向对象的理解</strong><br>所谓对象就是由一组数据结构和处理它们的方法组成的，重点“数据”包括对象的特性、状态等的静态信息；“方法” 也就是行为，包括该对象的对数据的操作、功能等能动信息。把相同行为的对象归纳为类，类是一个抽象的概念，对象是类的具体。简单点说：对象就是类的实例。例如：小品演员就是一个类，赵本山就是一个对象。</p><p>面向对象的目的：解决软件系统的可扩展性，可维护性和可重用性。</p><p><strong>什么叫对象？什么叫类？什么是面向对象（OOP）？</strong></p><ul><li>类的概念：类是具有相同属性和行为的一组对象的集合。它为属于该类的所有对象提供了统一的抽象描述，其内部包括属性和行为两个主要部分。在面向对象的编程语言中，类是一个独立的程序单位，它应该有一个类名并包括属性说明和服务说明两个主要部分。</li><li>对象的概念：对象是系统中用来描述客观事物的一个实体，它是构成系统的一个基本单位。一个对象由一组属性和对这组属性进行操作的一组服务组成。从更抽象的角度来说，对象是问题域或实现域中某些事物的一个抽象，它反映该事物在系统中需要保存的信息和发挥的作用；它是一组属性和有权对这些属性进行操作的一组服务的封装体。客观世界是由对象和对象之间的联系组成的。</li></ul><h3 id="特性"><a href="#特性" class="headerlink" title="特性"></a>特性</h3><p><strong>面向对象包括哪些特性，如何理解的？</strong></p><ul><li>封装：通常认为封装是把数据和操作数据的方法绑定起来，对数据的访问只能通过已定义的接口。面向对象的本质就是将现实世界描绘成一系列完全自治、封闭的对象。我们在类中编写的方法就是对实现细节的一种封装；我们编写一个类就是对数据和数据操作的封装。可以说，封装就是隐藏一切可隐藏的东西，只向外界提供最简单的编程接口。</li><li>继承：继承是从已有类得到继承信息创建新类的过程。提供继承信息的类被称为父类（超类、基类）；得到继承信息的类被称为子类（派生类）。继承让变化中的软件系统有了一定的延续性，同时继承也是封装程序中可变因素的重要手段。</li><li>多态：多态性是指允许不同子类型的对象对同一消息作出不同的响应。简单的说就是用同样的对象引用调用同样的方法但是做了不同的事情。多态性分为编译时的多态性和运行时的多态性。如果将对象的方法视为对象向外界提供的服务，那么运行时的多态性可以解释为：当 A系统访问B系统提供的服务时，B 系统有多种提供服务的方式，但一切对 A 系统来说都是透明的。方法重载（overload）实现的是编译时的多态性（也称为前绑定），而方法重写（override）实现的是运行时的多态性（也称为后绑定）。运行时的多态是面向对象最精髓的东西，要实现多态需要做两件事：</li></ul><h3 id="权限修饰符"><a href="#权限修饰符" class="headerlink" title="权限修饰符"></a>权限修饰符</h3><table><thead><tr><th align="center">修饰符</th><th align="center">当前类</th><th align="center">同包类</th><th align="center">子类</th><th align="center">其他包</th></tr></thead><tbody><tr><td align="center">public</td><td align="center">✓</td><td align="center">✓</td><td align="center">✓</td><td align="center">✓</td></tr><tr><td align="center">protected</td><td align="center">✓</td><td align="center">✓</td><td align="center">✓</td><td align="center">✗</td></tr><tr><td align="center">缺省</td><td align="center">✓</td><td align="center">✓</td><td align="center">✗</td><td align="center">✗</td></tr><tr><td align="center">private</td><td align="center">✓</td><td align="center">✗</td><td align="center">✗</td><td align="center">✗</td></tr></tbody></table><h3 id="多态机制"><a href="#多态机制" class="headerlink" title="多态机制"></a>多态机制</h3><p><strong>Java 中多态的机制是什么？</strong><br>Java中的多态靠的是父类或接口定义的引用变量可以指向子类或具体实现类的实例对象，而程序调用的方法在运行期才动态绑定，就是引用变量所指向的具体实例对象的方法，也就是内存里正在运行的那个对象的方法，而不是引用变量的类型中定义的方法。</p><p><strong>谈谈你对多态的理解</strong><br>多态就是指程序中定义的引用变量所指向的具体类型和通过该引用变量发出的方法调用在编程时并不确定，而是在程序运行期间才确定，即一个引用变量到底会指向哪个类的实例对象，该引用变量发出的方法调用到底是哪个类中实现的方法，必须在程序运行期间才能决定。因为在程序运行时才确定具体的类，这样，不用修改源代码，就可以让引用变量绑定到各种不同的对象上，从而导致该引用调用的具体方法随之改变，即不修改程序代码就可以改变程序运行时所绑定的具体代码，让程序可以选择多个运行状态，这就是多态性。</p><h2 id="异常"><a href="#异常" class="headerlink" title="异常"></a>异常</h2><p><strong>谈谈你对 Java 异常机制的理解</strong></p><ul><li>Java 对异常进行了分类，不同类型的异常分别用不同的 Java 类表示，所有异常的根类为 java.lang.Throwable，Throwable 下面又派生了两个子类：Error 和 Exception。<ul><li>Error 表示应用程序本身无法克服和恢复的一种严重问题。</li><li>Exception 表示程序还能够克服和恢复的问题，其中又分为系统异常和普通异常。<ul><li>系统异常是软件本身缺陷所导致的问题，也就是软件开发人员考虑不周所导致的问题，软件使用者无法克服和恢复这种问题，但在这种问题下还可以让软件系统继续运行或者让软件死掉，例如，数组下标越界（ArrayIndexOutOfBoundsException），空指针异常（NullPointerException）、类转换异常（ClassCastException）。</li><li>普通异常是运行环境的变化或异常所导致的问题，是用户能够克服的问题，例如，网络断线，硬盘空间不够，发生这样的异常后，程序不应该死掉。</li></ul></li></ul></li><li>Java 为系统异常和普通异常提供了不同的解决方案，编译器强制普通异常必须 try..catch 处理或用 throws 声明继续抛给上层调用方法处理，所以普通异常也称为 checked 异常，而系统异常可以处理也可以不处理，所以编译器不强制用 try..catch 处理或用 throws 声明，所以系统异常也称为 unchecked 异常。</li></ul><h3 id="异常种类"><a href="#异常种类" class="headerlink" title="异常种类"></a>异常种类</h3><p>按照异常需要处理的时机分为编译时异常(也叫受控异常)也叫 CheckedException 和运行时异常(也叫非受控异常)也叫 UnCheckedException。Java 认为 Checked 异常都是可以被处理的异常，所以Java 程序必须显式处理 Checked 异常。如果程序没有处理 Checked 异常，该程序在编译时就会发生错误无法编译。这体现了Java 的设计哲学：没有完善错误处理的代码根本没有机会被执行。对 Checked 异常处理方法有两种：</p><ul><li>第一种：当前方法知道如何处理该异常，则用try…catch块来处理该异常。</li><li>第二种：当前方法不知道如何处理，则在定义该方法时声明抛出该异常。</li></ul><p>运行时异常只有当代码在运行时才发行的异常，编译的时候不需要 try…catch。如除数是 0 和数组下标越界等，其产生频繁，处理麻烦，若显示申明或者捕获将会对程序的可读性和运行效率影响很大。所以由系统自动检测并将它们交给缺省的异常处理程序。当然如果你有处理要求也可以显示捕获它们。</p><h3 id="Error、Exception"><a href="#Error、Exception" class="headerlink" title="Error、Exception"></a>Error、Exception</h3><p>Error 类和 Exception 类的父类都是 Throwable 类，他们的区别如下：</p><ul><li>Error 类一般是指与虚拟机相关的问题，如系统崩溃，虚拟机错误，内存空间不足，方法调用栈溢出等。对于这类错误的导致的应用程序中断，仅靠程序本身无法恢复和预防，遇到这样的错误，建议让程序终止。</li><li>Exception 类表示程序可以处理的异常，可以捕获且可能恢复。遇到这类异常，应该尽可能处理异常，使程序恢复运行，而不应该随意终止异常。</li><li>Exception 类又分为未检查异常（UnCheckedException）和受检查的异常(CheckedException)。运行时异常 ArithmeticException，IllegalArgumentException 编译能通过，但是一运行就终止了，程序不会处理运行时异常，出现这类异常，程序会终止。而受检查的异常，要么用 try…catch 捕获，要么用 throws 字句声明抛出，交给它的父类处理，否则编译不会通过。</li></ul><h3 id="throw、throws"><a href="#throw、throws" class="headerlink" title="throw、throws"></a>throw、throws</h3><ul><li>throw：throw 语句用在方法体内，表示抛出异常，由方法体内的语句处理。throw 是具体向外抛出异常的动作，所以它抛出的是一个异常实例，执行 throw 一定是抛出了某种异常。</li><li>throws：throws 语句是用在方法声明后面，表示如果抛出异常，由该方法的调用者来进行异常的处理。throws 主要是声明这个方法会抛出某种类型的异常，让它的使用者要知道需要捕获的异常的类型。</li><li>throws 表示出现异常的一种可能性，并不一定会发生这种异常。</li></ul><h3 id="常见的异常类"><a href="#常见的异常类" class="headerlink" title="常见的异常类"></a>常见的异常类</h3><ul><li>NullPointerException：空指针异常，调用了未经初始化的对象或者是不存在的对象。</li><li>ClassNotFoundException：指定的类找不到，类的名称和路径加载错误；通常都是程序试图通过字符串来加载某个类时可能引发异常。</li><li>IndexOutOfBoundsException：数组角标越界异常，指示某排序索引（例如对数组、字符串或向量的排序）超出范围时抛出。</li><li>IllegalArgumentException：方法传递参数错误，向方法传递了一个不合法或不正确的参数。</li><li>ClassCastException：数据类型转换异常，当试图将对象强制转换为不是实例的子类时，抛出该异常。</li><li>NoClassDefFoundException：未找到类定义错误。</li><li>InstantiationException：实例化异常。</li><li>NoSuchMethodException：方法不存在异常。</li><li>SQLException：SQL 异常，常见于操作数据库时的 SQL 语句错误。</li><li>NumberFormatException：当应用程序试图将字符串转换成一种数值类型，但该字符串不能转换为适当格式时，抛出该异常。</li><li>FileNotFoundException：当试图打开指定路径名表示的文件失败时，抛出此异常。</li><li>IOException：当发生某种I/O异常时，抛出此异常。此类是失败或中断的I/O操作生成的异常的通用类。</li><li>ArrayStoreException：试图将错误类型的对象存储到一个对象数组时抛出的异常。</li><li>ArithmeticException：当出现异常的运算条件时，抛出此异常。例如，一个整数“除以零”时，抛出此类的一个实例。</li><li>NegativeArraySizeException：如果应用程序试图创建大小为负的数组，则抛出该异常。</li><li>NoSuchMethodException：无法找到某一特定方法时，抛出该异常。</li><li>SecurityException：由安全管理器抛出的异常，指示存在安全侵犯。</li><li>UnsupportedOperationException：当不支持请求的操作时，抛出该异常。</li><li>RuntimeException：是那些可能在Java虚拟机正常运行期间抛出的异常的超类。</li></ul><h2 id="常用-API"><a href="#常用-API" class="headerlink" title="常用 API"></a>常用 API</h2><h3 id="Math"><a href="#Math" class="headerlink" title="Math"></a>Math</h3><p><strong><code>Math.round(11.5)</code> 等于多少，<code>Math.round(-11.5)</code> 又等于多少？</strong></p><ul><li><code>Math.round(11.5)</code> 的返回值是12</li><li><code>Math.round(-11.5)</code> 的返回值是-11</li></ul><h3 id="String"><a href="#String" class="headerlink" title="String"></a>String</h3><p>String 常用方法：</p><ul><li>indexOf()：返回指定字符的索引。</li><li>charAt()：返回指定索引处的字符。</li><li>replace()：字符串替换。</li><li>trim()：去除字符串两端空白。</li><li>split()：分割字符串，返回一个分割后的字符串数组。</li><li>getBytes()：返回字符串的 byte 类型数组。</li><li>length()：返回字符串长度。</li><li>toLowerCase()：将字符串转成小写字母。</li><li>toUpperCase()：将字符串转成大写字符。</li><li>substring()：截取字符串。</li><li>equals()：字符串比较。</li></ul><p><strong>如何将字符串反转？</strong></p><ul><li>使用 StringBuilder 或者 stringBuffer 的 reverse() 方法。</li></ul><p><strong>String str=”i”与 String str=new String(“i”)一样吗？</strong></p><ul><li>不一样，因为内存的分配方式不一样。<code>String str=&quot;i&quot;</code> 的方式，java 虚拟机会将其分配到常量池中；而 String str=new String(“i”) 则会被分到堆内存中。</li></ul><p><strong>String 类可以被继承吗？</strong></p><ul><li>String类在声明时使用final关键字修饰，被final关键字修饰的类无法被继承。</li></ul><p><strong>为什么 String 会被定义为 final 的呢？</strong></p><ul><li>因为只有当字符串是不可变的，字符串池才有可能实现。字符串池的实现可以在运行时节约很多 heap 空间，因为不同的字符串变量都指向池中的同一个字符串。但如果字符串是可变的，那么 String interning 将不能实现，因为这样的话，如果变量改变了它的值，那么其它指向这个值的变量的值也会一起改变。如果字符串是可变的，那么会引起很严重的安全问题。譬如，数据库的用户名、密码都是以字符串的形式传入来获得数据库的连接，或者在 socket 编程中，主机名和端口都是以字符串的形式传入。因为字符串是不可变的，所以它的值是不可改变的，否则黑客们可以钻到空子，改变字符串指向的对象的值，造成安全漏洞。</li><li>因为字符串是不可变的，所以是多线程安全的，同一个字符串实例可以被多个线程共享。这样便不用因为线程安全问题而使用同步。字符串自己便是线程安全的。</li><li>因为字符串是不可变的，所以在它创建的时候 HashCode 就被缓存了，不需要重新计算。这就使得字符串很适合作为 Map 中的键，字符串的处理速度要快过其它的键对象。这就是 HashMap 中的键往往都使用字符串。</li></ul><p><strong>String 、StringBuilder 、StringBuffer 的区别？</strong><br>Java 平台提供了两种类型的字符串：String 和 StringBuffer/StringBuilder，它们都可以储存和操作字符串，区别如下：</p><ul><li>String 是只读字符串，也就意味着 String 引用的字符串内容是不能被改变的。</li><li>StringBuffer/StringBuilder 表示的字符串对象可以直接进行修改。</li><li>StringBuilder 是 Java5 中引入的，它和 StringBuffer 的方法完全相同，区别在于它是在单线程环境下使用的，因为它的所有方法都没有被 synchronized 修饰，因此它的效率理论上也比 StringBuffer 要高。</li></ul><h2 id="IO"><a href="#IO" class="headerlink" title="IO"></a>IO</h2><p><strong>Java 中有几种类型的流？</strong></p><ul><li>按照流的方向：输入流（inputStream）和输出流（outputStream）</li><li>按照处理数据的单位： 字节流和字符流。字节流继承于 InputStream 和 OutputStream， 字符流继承于InputStreamReader 和 OutputStreamWriter 。</li></ul><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/77D5699E34FA4BA79D22876C5B849840/364" alt=""></p><h3 id="流之间的转换"><a href="#流之间的转换" class="headerlink" title="流之间的转换"></a>流之间的转换</h3><p><strong>字节流如何转换为字符流？</strong></p><ul><li>字节输入流转字符输入流通过 InputStreamReader 实现，该类的构造函数可以传入 InputStream 对象。</li><li>字节输出流转字符输出流通过 OutputStreamWriter 实现，该类的构造函数可以传入 OutputStream 对象。</li></ul><h3 id="BIO、NIO、AIO"><a href="#BIO、NIO、AIO" class="headerlink" title="BIO、NIO、AIO"></a>BIO、NIO、AIO</h3><ul><li>BIO：Block IO 同步阻塞式 IO，就是我们平常使用的传统 IO，它的特点是模式简单使用方便，并发处理能力低。</li><li>NIO：New IO 同步非阻塞 IO，是传统 IO 的升级，客户端和服务器端通过 Channel（通道）通讯，实现了多路复用。</li><li>AIO：Asynchronous IO 是 NIO 的升级，也叫 NIO2，实现了异步非堵塞 IO ，异步 IO 的操作基于事件和回调机制。</li></ul><h2 id="Java-集合"><a href="#Java-集合" class="headerlink" title="Java 集合"></a>Java 集合</h2><p>Java 中各类集合如下：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/DFC47037F8144680BD974BD12401B5E1/366" alt=""></p><h3 id="Collection-和-Collections"><a href="#Collection-和-Collections" class="headerlink" title="Collection 和 Collections"></a>Collection 和 Collections</h3><ul><li>java.util.Collection 是一个集合接口（集合类的一个顶级接口）。它提供了对集合对象进行基本操作的通用接口方法。Collection接口在Java 类库中有很多具体的实现。Collection接口的意义是为各种具体的集合提供了最大化的统一操作方式，其直接继承接口有List与Set。</li><li>Collections则是集合类的一个工具类/帮助类，其中提供了一系列静态方法，用于对集合中元素进行排序、搜索以及线程安全等各种操作。</li></ul><h3 id="List、Set、Map"><a href="#List、Set、Map" class="headerlink" title="List、Set、Map"></a>List、Set、Map</h3><table><thead><tr><th align="center">区别</th><th align="center">List</th><th align="center">Set</th><th align="center">Map</th></tr></thead><tbody><tr><td align="center">继承接口</td><td align="center">Collection</td><td align="center">Collection</td><td align="center"></td></tr><tr><td align="center">常见实现类</td><td align="center">ArrayList、LinkedList、Vector</td><td align="center">HashSet、LinkedHashSet、TreeSet</td><td align="center">HashMap、HashTable</td></tr><tr><td align="center">常见方法</td><td align="center">add()、remove()、clear()、get()、contains()、size()</td><td align="center">add()、remove()、clear()、contains()、size()</td><td align="center">put()、get()、remove()、clear()、containsKey()、containsValue()、keySet()、values()、size()</td></tr><tr><td align="center">元素</td><td align="center">可重复</td><td align="center">不可重复</td><td align="center">不可重复（key）</td></tr><tr><td align="center">顺序</td><td align="center">有序</td><td align="center">无序</td><td align="center">无序</td></tr></tbody></table><p><strong>List 和 Set 的区别：</strong></p><ul><li>List, Set 都是继承自 Collection 接口</li><li>List 特点：元素有放入顺序，元素可重复。Set 特点：元素无放入顺序，元素不可重复（注意：元素虽然无放入顺序，但是元素在 set 中的位置是有该元素的 HashCode 决定的，其位置其实是固定的）</li><li>List 接口有三个实现类：LinkedList，ArrayList，Vector。Set 接口有两个实现类：HashSet(底层由 HashMap 实现)，LinkedHashSet</li></ul><p><strong>List 和 Map 的区别：</strong></p><ul><li>List 特点：元素有放入顺序，元素可重复;</li><li>Map 特点：元素按键值对存储，无放入顺序 ;</li><li>List 接口有三个实现类：LinkedList，ArrayList，Vector;</li><li>LinkedList：底层基于链表实现，链表内存是散乱的，每一个元素存储本身内存地址的同时还存储下一个元素的地址。链表增删快，查找慢;</li><li>Map 接口有三个实现类：HashMap，HashTable，LinkedHashMap</li><li>Map 相当于和 Collection 一个级别的；Map 集合存储键值对，且要求保持键的唯一性；</li></ul><h3 id="ArrayList"><a href="#ArrayList" class="headerlink" title="ArrayList"></a>ArrayList</h3><p><strong>ArrayList 和 LinkedList 的区别：</strong></p><ul><li>因为 Array 是基于索引(index)的数据结构，它使用索引在数组中搜索和读取数据是很快的。Array 获取数据的时间复杂度是 O(1),但是要删除数据却是开销很大的，因为这需要重排数组中的所有数据。</li><li>相对于 ArrayList，LinkedList 插入是更快的。因为 LinkedList 不像 ArrayList 一样，不需要改变数组的大小，也不需要在数组装满的时候要将所有的数据重新装入一个新的数组，这是 ArrayList 最坏的一种情况，时间复杂度是 O(n)，而 LinkedList 中插入或删除的时间复杂度仅为 O(1)。ArrayList 在插入数据时还需要更新索引（除了插入数组的尾部）。</li><li>类似于插入数据，删除数据时，LinkedList 也优于 ArrayList。</li><li>LinkedList 需要更多的内存，因为 ArrayList 的每个索引的位置是实际的数据，而 LinkedList 中的每个节点中存储的是实际的数据和前后节点的位置。</li><li>你的应用不会随机访问数据。因为如果你需要 LinkedList 中的第 n 个元素的时候，你需要从第一个元素顺序数到第 n 个数据，然后读取数据。</li><li>你的应用更多的插入和删除元素，更少的读取数据。因为插入和删除元素不涉及重排数据，所以它要比 ArrayList 要快。</li></ul><p><strong>ArrayList 和 Vector 的区别：</strong></p><ul><li>同步性：Vector 是线程安全的，也就是说是同步的 ，而 ArrayList 是线程不安全的，不是同步的。</li><li>数据增长：当需要增长时，Vector 默认增长为原来一倍 ，而 ArrayList 却是原来的 50% ，这样 ArrayList 就有利于节约内存空间。</li></ul><p>说明：如果涉及到堆栈，队列等操作，应该考虑用 Vector，如果需要快速随机访问元素，应该使用 ArrayList</p><h3 id="HashMap"><a href="#HashMap" class="headerlink" title="HashMap"></a>HashMap</h3><p><strong>HashMap 和 HashTable 的区别：</strong></p><ul><li>HashMap 去掉了 HashTable 的 contains方法，但是加上了 containsValue() 和 containsKey() 方法。</li><li>HashTable 是同步的，而 HashMap 是非同步的，效率上比 HashTable 要高。</li><li>HashMap 允许空键值，而 HashTable 不允许。</li></ul><p><strong>HashMap 和 TreeMap 的区别</strong></p><ul><li>对于在 Map 中插入、删除和定位元素这类操作，HashMap是最好的选择。然而，假如你需要对一个有序的key集合进行遍历，TreeMap是更好的选择。基于你的collection的大小，也许向HashMap中添加元素会更快，将map换为TreeMap进行有序key的遍历。</li></ul><p><strong>HashMap 和 HashSet 的区别：</strong></p><table><thead><tr><th align="center">HashMap</th><th align="center">HashSet</th></tr></thead><tbody><tr><td align="center">HashMap 实现了 Map 接口</td><td align="center">HashSet 实现了 Set 接口</td></tr><tr><td align="center">HashMap 储存键值对</td><td align="center">HashSet 仅仅存储对象</td></tr><tr><td align="center">使用 put() 方法将元素放入 map 中</td><td align="center">使用 add() 方法将元素放入 set 中</td></tr><tr><td align="center">HashMap 中使用键对象来计算 hashcode 值</td><td align="center">HashSet 使用成员对象来计算 hashcode 值，对于两个对象来说 hashcode 可能相同，所以 equals() 方法用来判断对象的相等性，如果两个对象不同的话，那么返回 false</td></tr><tr><td align="center">HashMap 比较快，因为是使用唯一的键来获取对象</td><td align="center">HashSet 较 HashMap 来说比较慢</td></tr></tbody></table><h3 id="HashMap-的实现原理"><a href="#HashMap-的实现原理" class="headerlink" title="HashMap 的实现原理"></a>HashMap 的实现原理</h3><p>HashMap概述： HashMap是基于哈希表的Map接口的非同步实现。此实现提供所有可选的映射操作，并允许使用null值和null键。此类不保证映射的顺序，特别是它不保证该顺序恒久不变。</p><p>HashMap的数据结构： 在java编程语言中，最基本的结构就是两种，一个是数组，另外一个是模拟指针（引用），所有的数据结构都可以用这两个基本结构来构造的，HashMap也不例外。HashMap实际上是一个“链表散列”的数据结构，即数组和链表的结合体。</p><p>当我们往Hashmap中put元素时,首先根据key的hashcode重新计算hash值,根绝hash值得到这个元素在数组中的位置(下标),如果该数组在该位置上已经存放了其他元素,那么在这个位置上的元素将以链表的形式存放,新加入的放在链头,最先加入的放入链尾.如果数组中该位置没有元素,就直接将该元素放到数组的该位置上。</p><p>需要注意Jdk 1.8中对HashMap的实现做了优化,当链表中的节点数据超过八个之后,该链表会转为红黑树来提高查询效率,从原来的O(n)到O(logn)</p><h3 id="HashSet-的实现原理"><a href="#HashSet-的实现原理" class="headerlink" title="HashSet 的实现原理"></a>HashSet 的实现原理</h3><ul><li>HashSet底层由HashMap实现</li><li>HashSet的值存放于HashMap的key上</li><li>HashMap的value统一为PRESENT</li></ul><h3 id="数组和-List-转换"><a href="#数组和-List-转换" class="headerlink" title="数组和 List 转换"></a>数组和 List 转换</h3><ul><li>List 转换成为数组：调用 ArrayList 的 toArray 方法。</li><li>数组转换成为 List：调用 Arrays 的 asList 方法。</li></ul><h3 id="Array-和-ArrayList"><a href="#Array-和-ArrayList" class="headerlink" title="Array 和 ArrayList"></a>Array 和 ArrayList</h3><ul><li>Array可以容纳基本类型和对象，而ArrayList只能容纳对象。</li><li>Array是指定大小的，而ArrayList大小是固定的。</li><li>Array没有提供ArrayList那么多功能，比如addAll、removeAll和iterator等。</li></ul><h3 id="线程安全的集合"><a href="#线程安全的集合" class="headerlink" title="线程安全的集合"></a>线程安全的集合</h3><ul><li>Vector：就比 arraylist 多了个同步化机制（线程安全），因为效率较低，现在已经不太建议使用。在 web 应用中，特别是前台页面，往往效率（页面响应速度）是优先考虑的。</li><li>Stack：堆栈类，先进后出。</li><li>Hashtable：就比 Hashmap 多了个线程安全。</li><li>enumeration：枚举，相当于迭代器。</li></ul><p>Collections 工具类提供了相关的 API，可以将不安全的集合是转换成线程安全的集合。</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Collections.synchronizedCollection(c);</span><br><span class="line">Collections.synchronizedList(list);</span><br><span class="line">Collections.synchronizedMap(m);</span><br><span class="line">Collections.synchronizedSet(s);</span><br></pre></td></tr></table></figure><h3 id="Map-中的-null"><a href="#Map-中的-null" class="headerlink" title="Map 中的 null"></a>Map 中的 null</h3><p>HashMap 对象的 key、value 值均可为 null。Hahtable 对象的 key、value 值均不可为 null。且两者的的 key 值均不能重复，若添加 key 相同的键值对，后面的 value 会自动覆盖前面的 value，但不会报错。</p><h3 id="Iterator"><a href="#Iterator" class="headerlink" title="Iterator"></a>Iterator</h3><p>迭代器(Iterator)是一种设计模式，它是一个对象，它可以遍历并选择序列中的对象，而开发人员不需要了解该序列的底层结构。迭代器通常被称为“轻量级”对象，因为创建它的代价小。</p><p><strong>Iterator 怎么使用？有什么特点？</strong><br>Java中的Iterator功能比较简单，并且只能单向移动：</p><ul><li>(1) 使用方法iterator()要求容器返回一个Iterator。第一次调用Iterator的next()方法时，它返回序列的第一个元素。注意：iterator()方法是java.lang.Iterable接口,被Collection继承。</li><li>(2) 使用next()获得序列中的下一个元素。</li><li>(3) 使用hasNext()检查序列中是否还有元素。</li><li>(4) 使用remove()将迭代器新返回的元素删除。</li></ul><p>Iterator是Java迭代器最简单的实现，为List设计的ListIterator具有更多的功能，它可以从两个方向遍历List，也可以从List中插入和删除元素。</p><p><strong>Iterator 和 ListIterator 有什么区别？</strong></p><ul><li>Iterator可用来遍历Set和List集合，但是ListIterator只能用来遍历List。</li><li>Iterator对集合只能是前向遍历，ListIterator既可以前向也可以后向。</li><li>ListIterator实现了Iterator接口，并包含其他的功能，比如：增加元素，替换元素，获取前一个和后一个元素的索引，等等。</li></ul><h2 id="多线程"><a href="#多线程" class="headerlink" title="多线程"></a>多线程</h2><h3 id="并行和并发"><a href="#并行和并发" class="headerlink" title="并行和并发"></a>并行和并发</h3><ul><li>并行是指两个或者多个事件在同一时刻发生；而并发是指两个或多个事件在同一时间间隔发生。</li><li>并行是在不同实体上的多个事件，并发是在同一实体上的多个事件。</li><li>在一台处理器上“同时”处理多个任务，在多台处理器上同时处理多个任务。如hadoop分布式集群。</li></ul><p>所以并发编程的目标是充分的利用处理器的每一个核，以达到最高的处理性能。</p><h3 id="线程和进程"><a href="#线程和进程" class="headerlink" title="线程和进程"></a>线程和进程</h3><ul><li><strong>进程：</strong> 程序开始运行后，加载到内存的内容就是进程（资源分配的基本单位），一个程序至少有一个进程，一个进程至少有一个线程。进程在执行过程中拥有独立的内存单元，而多个线程共享内存资源，减少切换次数，从而效率更高。</li><li><strong>线程</strong>：线程是程序执行的基本单位，是进程的一个实体，是cpu调度和分派的基本单位，是比程序更小的能独立运行的基本单位。同一进程中的多个线程之间可以并发执行。</li></ul><h3 id="程序运行过程"><a href="#程序运行过程" class="headerlink" title="程序运行过程"></a>程序运行过程</h3><p>程序是由一个一个指令组成的，程序运行的过程就是 CPU 读指令，PC（程序计数器）存储指令地址，然后读数据，将数据存储在 Register 中，CPU对数据进行计算与回写，再下一条指令</p><p><strong>线程切换</strong>：CPU 保存原线程执行进度，加载新线程的一个过程</p><h3 id="创建线程"><a href="#创建线程" class="headerlink" title="创建线程"></a>创建线程</h3><p><strong>继承 Thread 类创建线程类</strong></p><ul><li>定义 Thread 类的子类，并重写该类的 run 方法，该 run 方法的方法体就代表了线程要完成的任务。因此把 run() 方法称为执行体。</li><li>创建 Thread 子类的实例，即创建了线程对象。</li><li>调用线程对象的 start() 方法来启动该线程。</li></ul><p><strong>通过 Runnable 接口创建线程类</strong></p><ul><li>定义 runnable 接口的实现类，并重写该接口的 run() 方法，该 run() 方法的方法体同样是该线程的线程执行体。</li><li>创建 Runnable 实现类的实例，并依此实例作为 Thread 的 target 来创建 Thread 对象，该 Thread 对象才是真正的线程对象。</li><li>调用线程对象的 start() 方法来启动该线程。</li></ul><p><strong>通过 Callable 和 Future 创建线程</strong></p><ul><li>创建 Callable 接口的实现类，并实现 call() 方法，该 call() 方法将作为线程执行体，并且有返回值。</li><li>创建 Callable 实现类的实例，使用 FutureTask 类来包装 Callable 对象，该 FutureTask 对象封装了该 Callable 对象的 call() 方法的返回值。</li><li>使用 FutureTask 对象作为 Thread 对象的 target 创建并启动新线程。</li><li>调用 FutureTask 对象的 get() 方法来获得子线程执行结束后的返回值。</li></ul><p><strong>各种方法的优势与劣势：</strong></p><ul><li>Rannable、Callable<ul><li>优势是：线程类只是实现了 Runnable 接口或 Callable 接口，还可以继承其他类。在这种方式下，多个线程可以共享同一个 target 对象，所以非常适合多个相同线程来处理同一份资源的情况，从而可以将 CPU、代码和数据分开，形成清晰的模型，较好地体现了面向对象的思想。</li><li>劣势是：编程稍微复杂，如果要访问当前线程，则必须使用 Thread.currentThread() 方法。</li></ul></li><li>Thread<ul><li>优势是：编写简单，如果需要访问当前线程，则无需使用 Thread.currentThread() 方法，直接使用 this 即可获得当前线程。</li><li>劣势是：线程类已经继承了 Thread 类，所以不能再继承其他父类。</li></ul></li></ul><h3 id="runnable-和-callable"><a href="#runnable-和-callable" class="headerlink" title="runnable 和 callable"></a>runnable 和 callable</h3><ul><li>Runnable接口中的run()方法的返回值是void，它做的事情只是纯粹地去执行run()方法中的代码而已；</li><li>Callable接口中的call()方法是有返回值的，是一个泛型，和Future、FutureTask配合可以用来获取异步执行的结果。</li></ul><h3 id="线程状态"><a href="#线程状态" class="headerlink" title="线程状态"></a>线程状态</h3><p>线程通常都有五种状态，<strong>创建、就绪、运行、阻塞和死亡</strong></p><ul><li>创建状态。在生成线程对象，并没有调用该对象的start方法，这是线程处于创建状态。</li><li>就绪状态。当调用了线程对象的start方法之后，该线程就进入了就绪状态，但是此时线程调度程序还没有把该线程设置为当前线程，此时处于就绪状态。在线程运行之后，从等待或者睡眠中回来之后，也会处于就绪状态。</li><li>运行状态。线程调度程序将处于就绪状态的线程设置为当前线程，此时线程就进入了运行状态，开始运行run函数当中的代码。</li><li>阻塞状态。线程正在运行的时候，被暂停，通常是为了等待某个时间的发生(比如说某项资源就绪)之后再继续运行。sleep,suspend，wait等方法都可以导致线程阻塞。</li><li>死亡状态。如果一个线程的run方法执行结束或者调用stop方法后，该线程就会死亡。对于已经死亡的线程，无法再使用start方法令其进入就绪 　　</li></ul><h3 id="sleep-wait-yield-join"><a href="#sleep-wait-yield-join" class="headerlink" title="sleep(),wait(),yield(),join()"></a>sleep(),wait(),yield(),join()</h3><ul><li>sleep() 方法是线程类（Thread）的静态方法，让调用线程进入睡眠状态，让出执行机会给其他线程，等到休眠时间结束后，线程进入就绪状态和其他线程一起竞争cpu的执行时间。因为sleep() 是static静态的方法，他不能改变对象的机锁，当一个synchronized块中调用了sleep() 方法，线程虽然进入休眠，但是对象的机锁没有被释放，其他线程依然无法访问这个对象。</li><li>wait() 是Object类的方法，当一个线程执行到wait方法时，它就进入到一个和该对象相关的等待池，同时释放对象的机锁，使得其他线程能够访问，可以通过notify，notifyAll方法来唤醒等待的线程</li><li>yield() 方法和 sleep() 方法类似，也不会释放“锁标志”，区别在于，它没有参数，即 yield() 方法只是使当前线程重新回到可执行状态，所以执行 yield() 的线程有可能在进入到可执行状态后马上又被执行，另外 yield() 方法只能使同优先级或者高优先级的线程得到执行机会，这也和 sleep() 方法不同。</li><li>join() 方法会使当前线程等待调用 join() 的线程结束后才能继续执行</li></ul><h3 id="notify-notifyAll"><a href="#notify-notifyAll" class="headerlink" title="notify(),notifyAll()"></a>notify(),notifyAll()</h3><ul><li>如果线程调用了对象的 wait()方法，那么线程便会处于该对象的等待池中，等待池中的线程不会去竞争该对象的锁。</li><li>当有线程调用了对象的 notifyAll()方法（唤醒所有 wait 线程）或 notify()方法（只随机唤醒一个 wait 线程），被唤醒的的线程便会进入该对象的锁池中，锁池中的线程会去竞争该对象锁。也就是说，调用了notify后只要一个线程会由等待池进入锁池，而notifyAll会将该对象等待池内的所有线程移动到锁池中，等待锁竞争。</li><li>优先级高的线程竞争到对象锁的概率大，假若某线程没有竞争到该对象锁，它还会留在锁池中，唯有线程再次调用 wait()方法，它才会重新回到等待池中。而竞争到对象锁的线程则继续往下执行，直到执行完了 synchronized 代码块，它会释放掉该对象锁，这时锁池中的线程会继续竞争该对象锁。</li></ul><h3 id="run-start"><a href="#run-start" class="headerlink" title="run(),start()"></a>run(),start()</h3><ul><li>每个线程都是通过某个特定Thread对象所对应的方法run()来完成其操作的，方法run()称为线程体。通过调用Thread类的start()方法来启动一个线程。</li><li>start()方法来启动一个线程，真正实现了多线程运行。这时无需等待run方法体代码执行完毕，可以直接继续执行下面的代码； 这时此线程是处于就绪状态， 并没有运行。 然后通过此Thread类调用方法run()来完成其运行状态， 这里方法run()称为线程体，它包含了要执行的这个线程的内容， Run方法运行结束， 此线程终止。然后CPU再调度其它线程。</li><li>run()方法是在本线程里的，只是线程里的一个函数,而不是多线程的。 如果直接调用run(),其实就相当于是调用了一个普通函数而已，直接待用run()方法必须等待run()方法执行完毕才能执行下面的代码，所以执行路径还是只有一条，根本就没有线程的特征，所以在多线程执行时要使用start()方法而不是run()方法。</li></ul><h3 id="线程池"><a href="#线程池" class="headerlink" title="线程池"></a>线程池</h3><p><strong>什么是线程池，如何使用？</strong><br>线程池就是事先将多个线程对象放到一个容器中，当使用的时候就不用 new 线程而是直接去池中拿线程即可，节省了开辟子线程的时间，提高的代码执行效率。在 JDK 的 <code>java.util.concurrent.Executors</code> 中提供了生成多种线程池的静态方法。调用他们的 execute 方法即可使用。</p><p><strong>创建线程池的方式</strong></p><ul><li>①. newFixedThreadPool(int nThreads)<br>创建一个固定长度的线程池，每当提交一个任务就创建一个线程，直到达到线程池的最大数量，这时线程规模将不再变化，当线程发生未预期的错误而结束时，线程池会补充一个新的线程。</li><li>②. newCachedThreadPool()<br>创建一个可缓存的线程池，如果线程池的规模超过了处理需求，将自动回收空闲线程，而当需求增加时，则可以自动添加新线程，线程池的规模不存在任何限制。</li><li>③. newSingleThreadExecutor()<br>这是一个单线程的Executor，它创建单个工作线程来执行任务，如果这个线程异常结束，会创建一个新的来替代它；它的特点是能确保依照任务在队列中的顺序来串行执行。</li><li>④. newScheduledThreadPool(int corePoolSize)<br>创建了一个固定长度的线程池，而且以延迟或定时的方式来执行任务，类似于Timer。</li></ul><p>线程池有5种状态：<strong>Running、ShutDown、Stop、Tidying、Terminated</strong>。</p><p>线程池各个状态切换框架图：<br><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/3CB3D89FFA81418A866EA572A45CB180/351" alt=""></p><p><strong>请叙述一下您对线程池的理解？</strong><br>（如果问到了这样的问题，可以展开的说一下线程池如何用、线程池的好处、线程池的启动策略）合理利用线程池能够带来三个好处。</p><ul><li>第一：降低资源消耗。通过重复利用已创建的线程降低线程创建和销毁造成的消耗。</li><li>第二：提高响应速度。当任务到达时，任务可以不需要等到线程创建就能立即执行。</li><li>第三：提高线程的可管理性。线程是稀缺资源，如果无限制的创建，不仅会消耗系统资源，还会降低系统的稳定性，使用线程池可以进行统一的分配，调优和监控。</li></ul><p><strong>线程池的启动策略？</strong></p><ul><li>线程池刚创建时，里面没有一个线程。任务队列是作为参数传进来的。不过，就算队列里面有任务，线程池也不会马上执行它们。</li><li>当调用execute()方法添加一个任务时，线程池会做如下判断：<ul><li>（1）如果正在运行的线程数量小于corePoolSize，那么马上创建线程运行这个任务；</li><li>（2）如果正在运行的线程数量大于或等于corePoolSize，那么将这个任务放入队列；</li><li>（3）如果这时候队列满了，而且正在运行的线程数量小于maximumPoolSize，那么还是要创建线程运行这个任务；</li><li>（4）如果队列满了，而且正在运行的线程数量大于或等于maximumPoolSize，那么线程池会抛出异常，告诉调用者“我不能再接受任务了”。</li><li>（5）当一个线程完成任务时，它会从队列中取下一个任务来执行。</li><li>（6）当一个线程无事可做，超过一定的时间（keepAliveTime）时，线程池会判断，如果当前运行的线程数大于corePoolSize，那么这个线程就被停掉。所以线程池的所有任务完成后，它最终会收缩到corePoolSize的大小。</li></ul></li></ul><h3 id="submit-execute"><a href="#submit-execute" class="headerlink" title="submit(),execute()"></a>submit(),execute()</h3><ul><li>接收的参数不一样</li><li>submit有返回值，而execute没有</li><li>submit方便Exception处理</li></ul><h3 id="线程安全"><a href="#线程安全" class="headerlink" title="线程安全"></a>线程安全</h3><p>线程安全在三个方面体现：</p><ul><li>原子性：提供互斥访问，同一时刻只能有一个线程对数据进行操作，（atomic,synchronized）；</li><li>可见性：一个线程对主内存的修改可以及时地被其他线程看到，（synchronized,volatile）；</li><li>有序性：一个线程观察其他线程中的指令执行顺序，由于指令重排序，该观察结果一般杂乱无序，（happens-before原则）。</li></ul><p>线程安全是多线程领域的问题，线程安全可以简单理解为一个方法或者一个实例可以在多线程环境中使用而不会出现问题。</p><p>在 Java 多线程编程当中，提供了多种实现 Java 线程安全的方式：</p><ul><li>最简单的方式，使用 Synchronization 关键字</li><li>使用 java.util.concurrent.atomic 包中的原子类，例如 AtomicInteger</li><li>使用 java.util.concurrent.locks 包中的锁</li><li>使用线程安全的集合 ConcurrentHashMap</li><li>使用 volatile 关键字，保证变量可见性（直接从内存读，而不是从线程 cache 读）</li></ul><h2 id="锁"><a href="#锁" class="headerlink" title="锁"></a>锁</h2><h3 id="锁升级"><a href="#锁升级" class="headerlink" title="锁升级"></a>锁升级</h3><p>在 Java 中，锁共有 4 种状态，级别从低到高依次为：无状态锁，偏向锁，轻量级锁和重量级锁状态，这几个状态会随着竞争情况逐渐升级。锁可以升级但不能降级。</p><p>锁升级的图示过程：<br><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/CCB8716423194F22B6018ABD9AC5EA3E/354" alt=""></p><ul><li>当只有一个线程来争夺资源时，该资源是一个偏向锁，当有其他线程来争夺时，会从偏向锁升级为自旋锁</li><li>因此偏向锁就是 synchronized 在只有一个线程时对锁的一个优化</li><li>轻量级锁中，如何线程等待时间过长，会再次进行升级为重量级锁</li></ul><h3 id="死锁"><a href="#死锁" class="headerlink" title="死锁"></a>死锁</h3><p>死锁是指两个或两个以上的进程在执行过程中，由于竞争资源或者由于彼此通信而造成的一种阻塞的现象，若无外力作用，它们都将无法推进下去。此时称系统处于死锁状态或系统产生了死锁，这些永远在互相等待的进程称为死锁进程。是操作系统层面的一个错误，是进程死锁的简称，最早在 1965 年由 Dijkstra 在研究银行家算法时提出的，它是计算机操作系统乃至整个并发程序设计领域最难处理的问题之一。</p><h3 id="怎么防止死锁"><a href="#怎么防止死锁" class="headerlink" title="怎么防止死锁"></a>怎么防止死锁</h3><p>死锁的四个必要条件：</p><ul><li>互斥条件：进程对所分配到的资源不允许其他进程进行访问，若其他进程访问该资源，只能等待，直至占有该资源的进程使用完成后释放该资源</li><li>请求和保持条件：进程获得一定的资源之后，又对其他资源发出请求，但是该资源可能被其他进程占有，此事请求阻塞，但又对自己获得的资源保持不放</li><li>不可剥夺条件：是指进程已获得的资源，在未完成使用之前，不可被剥夺，只能在使用完后自己释放</li><li>环路等待条件：是指进程发生死锁后，若干进程之间形成一种头尾相接的循环等待资源关系</li></ul><p>这四个条件是死锁的必要条件，只要系统发生死锁，这些条件必然成立，而只要上述条件之一不满足，就不会发生死锁。</p><p>理解了死锁的原因，尤其是产生死锁的四个必要条件，就可以最大可能地避免、预防和 解除死锁。</p><p>所以，在系统设计、进程调度等方面注意如何不让这四个必要条件成立，如何确 定资源的合理分配算法，避免进程永久占据系统资源。</p><p>此外，也要防止进程在处于等待状态的情况下占用资源。因此，对资源的分配要给予合理的规划。</p><h3 id="ThreadLocal"><a href="#ThreadLocal" class="headerlink" title="ThreadLocal"></a>ThreadLocal</h3><p><strong>ThreadLocal：</strong></p><ul><li>线程局部变量是局限于线程内部的变量，属于线程自身所有，不在多个线程间共享。</li><li>Java提供ThreadLocal类来支持线程局部变量，是一种实现线程安全的方式。</li><li>但是在管理环境下（如 web 服务器）使用线程局部变量的时候要特别小心，在这种情况下，工作线程的生命周期比任何应用变量的生命周期都要长。</li><li>任何线程局部变量一旦在工作完成后没有释放，Java 应用就存在内存泄露的风险。</li></ul><p><strong>ThreadLocal 实现原理：</strong></p><ul><li>对象在使用 set 方法的时候，会将 ThreadLocal 对象本身和值设置到当前线程的 threadLocals 对象中去，</li><li>由于每个线程的 threadLocals（ThreadLocalMap） 对象都不一样，因此ThreadLocal 对象 set 的值并不是线程共享的</li><li>threadLocals 中的 key 就是一个 弱引用，指向 ThreadLocal 对象</li><li>为什么 threadLocals 中的 key 不能是强引用：</li><li>因为如果我们不需要 ThreadLocal 对象的时候，这时候线程中的 threadLocals key 对 TL 是一个强引用，不会被GC清理，就会造成内存泄漏</li><li>但是 threadLocals 中的 value 是一个强引用，还是会有内存泄漏，因此在使用 ThreadLocal 时，如果该对象确定不用了，要记得 remove</li></ul><h3 id="volatile"><a href="#volatile" class="headerlink" title="volatile"></a>volatile</h3><p><strong>两大作用：</strong></p><ul><li>① 线程间可见：使用 volatile 修饰的内存，只要其在某个线程中发生改变，其他线程中立马可见</li><li>② 禁止指令重排序（使用内存屏障实现）：用 volatile 修饰的内存，在其上面执行的指令，不可以乱序</li></ul><p><strong>DCL：Double Check Lock 双重锁检查</strong></p><ul><li>锁必须要加 volatile，这里就是使用 volatile 的禁止指令重排序的功能，因为创建对象的时候</li><li>构造器和建立关联的指令是可能会重排的，也就是说先建立了关联但是没有构造，</li><li>此时对象引用的已经不为空了，但是实际内容是空，这时候来了一个线程进行判断就会将其当成一个不为空的对象</li></ul><p><strong>对象创建的过程（半初始化）：</strong></p><ol><li>先申请一块内存空间（new 指令）</li><li>调用构造器方法（invokespecial 指令）</li><li>建立关联（astore 指令）</li></ol><p><strong>volatile 实现原理：</strong></p><ul><li>在 JVM 底层 volatile 是采用“内存屏障”来实现的</li><li>缓存一致性协议（MESI协议）它确保每个缓存中使用的共享变量的副本是一致的。其核心思想如下：当某个 CPU 在写数据时，如果发现操作的变量是共享变量，则会通知其他 CPU 告知该变量的缓存行是无效的，因此其他 CPU 在读取该变量时，发现其无效会重新从主存中加载数据</li></ul><h3 id="synchronized"><a href="#synchronized" class="headerlink" title="synchronized"></a>synchronized</h3><p>synchronized可以保证方法或者代码块在运行时，同一时刻只有一个方法可以进入到临界区，同时它还可以保证共享变量的内存可见性。</p><p>使用这个关键字一定会有一把锁，如果是 synchronized() 方法，锁就是中括号是的对象，如果是修饰的方法，那锁就是 this，如果修改的静态方法，那锁就是当前的 class</p><p><strong>不持有锁的线程怎么办？</strong></p><ul><li>① 忙等待（自旋锁，轻量级锁）</li><li>② 进队列等待（重量级锁）</li></ul><p><strong>注意</strong>：不同的场景用不同的锁，并不是说自旋锁效率一定比重量级锁高</p><blockquote><p>synchronized 关键字最早的时候用的是重量级锁，在 1.5 的时候，增加了一个 JUC（java.util.concurrent） 包用于处理并发</p></blockquote><blockquote><p>JUC 中的 Atomic 相关类就是一个 轻量级锁（自旋锁，实现方法：CAS，CompareAndSwap，一个线程对一个值进行修改，会先读取这个值，修改后和原值比较是否是期望的那个值，如果是就替换，不是就再来操作一次）</p></blockquote><blockquote><p>CAS 的 ABA 问题：CAS 的过程中修改了值后，发现为期望值，但是这个期望值可能是修改了多次后又改回来的值（也就是说这个值并不是期望值），解决这个问题通常用版本号来解决</p></blockquote><blockquote><p>CAS 修改值时候的原子性问题：CAS 在最底层是由 cmpxchg（也是cas的方式修改值） 的汇编指令来操作的，在其前面加一条 lock（锁总线） 汇编指令就可保证其原子性</p></blockquote><p><strong>synchronized 和 volatile 的区别</strong></p><ul><li>volatile本质是在告诉jvm当前变量在寄存器（工作内存）中的值是不确定的，需要从主存中读取； synchronized则是锁定当前变量，只有当前线程可以访问该变量，其他线程被阻塞住。</li><li>volatile仅能使用在变量级别；synchronized则可以使用在变量、方法、和类级别的。</li><li>volatile仅能实现变量的修改可见性，不能保证原子性；而synchronized则可以保证变量的修改可见性和原子性。</li><li>volatile不会造成线程的阻塞；synchronized可能会造成线程的阻塞。</li><li>volatile标记的变量不会被编译器优化；synchronized标记的变量可以被编译器优化。</li></ul><p><strong>synchronized 和 Lock 的区别</strong></p><ul><li>首先 synchronized 是 java 内置关键字，在 jvm 层面，Lock 是个 java 类；</li><li>synchronized 无法判断是否获取锁的状态，Lock 可以判断是否获取到锁；</li><li>synchronized会自动释放锁(a 线程执行完同步代码会释放锁 ；b 线程执行过程中发生异常会释放锁)，Lock需在finally中手工释放锁（unlock()方法释放锁），否则容易造成线程死锁；</li><li>用 synchronized 关键字的两个线程1和线程2，如果当前线程1获得锁，线程2线程等待。如果线程1阻塞，线程2则会一直等待下去，而Lock锁就不一定会等待下去，如果尝试获取不到锁，线程可以不用一直等待就结束了；</li><li>synchronized 的锁可重入、不可中断、非公平，而Lock锁可重入、可判断、可公平（两者皆可）；</li><li>Lock锁适合大量同步的代码的同步问题，synchronized锁适合代码少量的同步问题。</li></ul><h3 id="atomic-的原理？"><a href="#atomic-的原理？" class="headerlink" title="atomic 的原理？"></a>atomic 的原理？</h3><p>Atomic包中的类基本的特性就是在多线程环境下，当有多个线程同时对单个（包括基本类型及引用类型）变量进行操作时，具有排他性，即当多个线程同时对该变量的值进行更新时，仅有一个线程能成功，而未成功的线程可以向自旋锁一样，继续尝试，一直等到执行成功。</p><p>Atomic系列的类中的核心方法都会调用unsafe类中的几个本地方法。我们需要先知道一个东西就是Unsafe类，全名为：sun.misc.Unsafe，这个类包含了大量的对C代码的操作，包括很多直接内存分配以及原子操作的调用，而它之所以标记为非安全的，是告诉你这个里面大量的方法调用都会存在安全隐患，需要小心使用，否则会导致严重的后果，例如在通过unsafe分配内存的时候，如果自己指定某些区域可能会导致一些类似C++一样的指针越界到其他进程的问题。</p><h3 id="乐观锁和悲观锁"><a href="#乐观锁和悲观锁" class="headerlink" title="乐观锁和悲观锁"></a>乐观锁和悲观锁</h3><ul><li>乐观锁：每次去拿数据的时候都认为别人不会修改，所以不会上锁，但是在提交更新的时候会判断一下在此期间别人有没有去更新这个数据。</li><li>悲观锁：每次去拿数据的时候都认为别人会修改，所以每次在拿数据的时候都会上锁，这样别人想拿这个数据就会阻止，直到这个锁被释放。</li></ul><h2 id="JVM"><a href="#JVM" class="headerlink" title="JVM"></a>JVM</h2><h3 id="主要组成"><a href="#主要组成" class="headerlink" title="主要组成"></a>主要组成</h3><ul><li>类加载器（ClassLoader）</li><li>运行时数据区（Runtime Data Area）</li><li>执行引擎（Execution Engine）</li><li>本地库接口（Native Interface）</li></ul><p>组件的作用： 首先通过类加载器（ClassLoader）会把 Java 代码转换成字节码，运行时数据区（Runtime Data Area）再把字节码加载到内存中，而字节码文件只是 JVM 的一套指令集规范，并不能直接交个底层操作系统去执行，因此需要特定的命令解析器执行引擎（Execution Engine），将字节码翻译成底层系统指令，再交由 CPU 去执行，而这个过程中需要调用其他语言的本地库接口（Native Interface）来实现整个程序的功能。</p><p>java 程序在运行的过程中会先由JVM的 <strong>类装载子系统</strong> 把 class 文件加载到 <strong>运行时数据区（内存模型）</strong><br>然后由 JVM 中的 <strong>字节码执行引擎</strong> 来运行 java 程序</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/27194E66A8B6423ABF10125307A3B3DD/356" alt=""></p><h3 id="运行时数据区"><a href="#运行时数据区" class="headerlink" title="运行时数据区"></a>运行时数据区</h3><ul><li>堆</li><li>虚拟机（栈）</li><li>方法区</li><li>程序计数器</li><li>本地方法栈</li></ul><p><strong>其中</strong>：</p><ul><li>栈：当我们运行一个线程时，JVM 会在栈中分配一个空间给该线程<br>线程中的一些方法则在空间中以 <strong>栈帧</strong> 的方式再分配空间（如执行某个方法就分配一个栈帧压入线程栈中）<br>栈帧中同样会分配不同的空间，一般来说，会有如下空间：<ul><li>局部变量表、操作数栈（存储临时的操作数）、动态链接、方法出口</li></ul></li><li>程序计数器：每个线程都会分配一个空间用于记录线程动态<br>当CPU切换线程时可方便找到这个 Java 线程的运行时刻</li><li>堆：通常 new 出来的对象是保存在堆中的</li><li>方法区（元空间）：通常用来存放常量、静态变量、类信息</li><li>本地方法栈：通常是 native 方法，也就是以 C 语言执行的方法，但是现在基本不用本地方法了</li></ul><h3 id="堆栈区别"><a href="#堆栈区别" class="headerlink" title="堆栈区别"></a>堆栈区别</h3><ul><li>栈内存存储的是局部变量而堆内存存储的是实体；</li><li>栈内存的更新速度要快于堆内存，因为局部变量的生命周期很短；</li><li>栈内存存放的变量生命周期一旦结束就会被释放，而堆内存存放的实体会被垃圾回收机制不定时的回收。</li></ul><h3 id="类加载"><a href="#类加载" class="headerlink" title="类加载"></a>类加载</h3><p><strong>类加载分为以下 5 个步骤：</strong></p><ul><li>加载：根据查找路径找到相应的 class 文件然后导入；</li><li>检查：检查加载的 class 文件的正确性；</li><li>准备：给类中的静态变量分配内存空间；</li><li>初始化：对静态变量和静态代码块执行初始化工作。</li></ul><p><strong>类初始化过程：</strong></p><ul><li>① 一个类要创建实例需要先加载并初始化该类（main 方法所在的类需要先加载和初始化）</li><li>② 一个子类要初始化需要先初始化父类</li><li>③ 一个类初始化就是执行 <code>&lt;clinit&gt;()</code> 方法（可在字节码文件中看到）<br><code>&lt;clinit&gt;()</code> 方法由 静态类变量显示赋值 代码和 静态代码块 组成<br>类变量显示赋值 代码和 静态代码块 代码从上到下顺序执行<br><code>&lt;clinit&gt;()</code> 方法只执行一次</li></ul><p><strong>实例初始化过程：</strong><br>实例初始化就是执行 <code>&lt;init&gt;()</code> 方法</p><ul><li><code>&lt;init&gt;()</code> 方法可能重载有多个，有几个构造器就有几个 <code>&lt;init&gt;()</code> 方法</li><li><code>&lt;init&gt;()</code> 方法由 <strong>非静态实例变量显示赋值</strong> 代码和 <strong>非静态代码块</strong>、对应构造器代码组成</li><li>非静态实例变量显示赋值代码和非静态代码块从上到下顺序执行，对应构造器代码最后执行</li><li>注意：每 new 一次对象，就会调用对应构造器，执行对应的 <code>&lt;init&gt;()</code> 方法</li><li><code>&lt;init&gt;()</code> 方法首行一定是 super() 方法，无论其有没有书写</li></ul><h3 id="类加载器"><a href="#类加载器" class="headerlink" title="类加载器"></a>类加载器</h3><p>从 Java 虚拟机的角度来说，只存在两种不同的类加载器：一种是 <strong>启动类加载器（Bootstrap ClassLoader）</strong>，这个类加载器使用 C++ 语言实现（HotSpot 虚拟机中），是虚拟机自身的一部分；另一种就是所有其他的类加载器，这些类加载器都有 Java 语言实现，独立于虚拟机外部，并且全部继承自 java.lang.ClassLoader。</p><p>从开发者的角度，类加载器可以细分为：</p><ul><li>启动（Bootstrap）类加载器：负责将 Java_Home/lib 下面的类库加载到内存中（比如 rt.jar）。由于引导类加载器涉及到虚拟机本地实现细节，开发者无法直接获取到启动类加载器的引用，所以不允许直接通过引用进行操作。</li><li>标准扩展（Extension）类加载器：是由 Sun 的 ExtClassLoader（sun.misc.Launcher$ExtClassLoader） 实现的。它负责将 Java_Home /lib/ext 或者由系统变量 java.ext.dir 指定位置中的类库加载到内存中。开发者可以直接使用标准扩展类加载器。</li><li>应用程序（Application）类加载器：是由 Sun 的 AppClassLoader（sun.misc.Launcher$AppClassLoader） 实现的。它负责将系统类路径（CLASSPATH）中指定的类库加载到内存中。开发者可以直接使用系统类加载器。由于这个类加载器是 ClassLoader 中的 getSystemClassLoader() 方法的返回值，因此一般称为系统（System）加载器。</li><li>自定义加载器，CustomClassLoader，用户自定义的类加载器,可加载指定路径的 class 文件</li></ul><p>这些加载器的层次关系被称为类加载器的 <strong>双亲委派模型</strong>。该模型要求 <strong>除了顶层的启动类加载器外，其余的类加载器都应该有自己的父类加载器，而这种父子关系一般通过组合（Composition）关系来实现，而不是通过继承（Inheritance）。</strong></p><p>如图所示：<br><img src="https://www.funtl.com/assets1/20160506184936657.jpg" alt=""></p><h3 id="双亲委派"><a href="#双亲委派" class="headerlink" title="双亲委派"></a>双亲委派</h3><p>当某个类加载器需要加载某个 <code>.class</code> 文件时，它首先把这个任务委托给他的上级类加载器，递归这个操作，如果上级的类加载器没有加载，自己才会去加载这个类。</p><p><strong>作用：</strong></p><ol><li>防止重复加载同一个 <code>.class</code> 。通过委托去向上面问一问，加载过了，就不用再加载一遍。保证数据安全。</li><li>保证核心 <code>.class</code> 不能被篡改。通过委托方式，不会去篡改核心 <code>.class</code> ，即使篡改也不会去加载，即使加载也不会是同一个 <code>.class</code> 对象了。不同的加载器加载同一个 <code>.class</code> 也不是同一个Class对象。这样保证了Class执行安全。</li></ol><h3 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h3><p>Java 中一共四种引用：</p><ul><li>强引用<ul><li>通常用的 new 就是强引用</li></ul></li><li>软引用<ul><li>软引用借助 SoftReference 来实现，当堆内存够用的情况下，软引用不会被GC清理</li><li>当堆内存不够的情况下，软引用会被清理，而不关心它是否有引用，软引用通常用于缓存</li></ul></li><li>弱引用<ul><li>弱引用借助 WeekReference 来实现，只要GC来清理垃圾，弱引用就会被清理。ThreadLocal 中的 Entry 就是一个弱引用</li></ul></li><li>虚引用<ul><li>虚引用借助 PhantomReference 来实现，通常开发人员不常用，写类库的人用的较多，如 ByteBuffer（零拷贝） 管理堆外内存就使用了 虚引用</li></ul></li></ul><h3 id="垃圾回收算法"><a href="#垃圾回收算法" class="headerlink" title="垃圾回收算法"></a>垃圾回收算法</h3><ul><li>引用计数算法（废弃）</li><li>根搜索算法（使用）</li><li>Mark-sweep （标记清除）：将垃圾标记，然后清除，缺点：内存碎片化</li><li>Copying （拷贝）：将不是垃圾的对象拷贝到其他的空间，然后清除之前的所有空间，缺点：浪费空间</li><li>Mark-Compact （标记整理）：一边对垃圾进行标记，一边整理非垃圾，缺点：效率低下</li><li>分代收集算法</li></ul><h3 id="垃圾回收器"><a href="#垃圾回收器" class="headerlink" title="垃圾回收器"></a>垃圾回收器</h3><p>JVM 诞生以来一共有十种垃圾回收器</p><ul><li>Serial：最早的单线程串行垃圾回收器。</li><li>Serial Old：Serial 垃圾回收器的老年版本，同样也是单线程的，可以作为 CMS 垃圾回收器的备选预案。</li><li>Parallel 收集器是多线程的，但 Parallel 是吞吐量优先的收集器，可以牺牲等待时间换取系统的吞吐量。</li><li>Parallel Old 是 Parallel 老生代版本，Parallel 使用的是复制的内存回收算法，Parallel Old 使用的是标记-整理的内存回收算法。</li><li>ParNew：多线程收集器，与 Parallel 类似，ParNew 是专门用来配合 CMS 的</li><li>CMS：一种以获得最短停顿时间为目标的收集器，非常适用 B/S 系统。</li><li>G1：一种兼顾吞吐量和停顿时间的 GC 实现，是 JDK 9 以后的默认 GC 选项。</li><li>ZGC：分区模型</li><li>Shenandoah：分区模型</li><li></li></ul><h3 id="找垃圾的方式"><a href="#找垃圾的方式" class="headerlink" title="找垃圾的方式"></a>找垃圾的方式</h3><p><strong>垃圾对象</strong>：当一个对象没有引用指向它时，它就是一个垃圾</p><p>一般有两种方法来判断：</p><ul><li>Reference Count（引用计数）：为每个对象创建一个引用计数，有对象引用时计数器 +1，引用被释放时计数 -1，当计数器为 0 时就可以被回收。它有一个缺点不能解决循环引用的问题；</li><li>Root Searching（JVM 默认）：从 GC Roots 开始向下搜索，搜索所走过的路径称为引用链。当一个对象到 GC Roots 没有任何引用链相连时，则证明此对象是可以被回收的。</li></ul><h3 id="CMS"><a href="#CMS" class="headerlink" title="CMS"></a>CMS</h3><p>CMS(concurrent mark sweep) 回收垃圾的过程有 6 个阶段，通常只考虑其中4个阶段：</p><ul><li>初始标记：STW，标记 GC root</li><li>并发标记：与正在运行的进程并发，从 GC root 开始往下进行标记，其中会产生浮动垃圾，但是没关系，下一次可以顺利清理；主要是会产生 <strong>错标</strong> 问题</li><li>重新标记：STW 修正错标（三色标记算法）</li><li>并发清理：与正在运行的进程并发，进行垃圾的清理</li></ul><h3 id="分代-GC-工作方式"><a href="#分代-GC-工作方式" class="headerlink" title="分代 GC 工作方式"></a>分代 GC 工作方式</h3><p>分代垃圾回收器通常会将堆分成两个部分：年轻代（1/3）、老年代（2/3）。其中年轻代分为两个区，Eden 区（8/10）和 Survivor 的两个空间（1/10）。</p><p><strong>工作方式：</strong></p><ul><li>我们创建的对象通常会放到 Eden 区中，当 Eden 区放满了之后，会进行一次 Minor GC/YGC 使用 <strong>复制算法（Copying）</strong> 将非垃圾复制到 Survivor 区中的其中一个空间，且对象的分代年龄加一；</li><li>Eden 区再次满了之后，又会进行一次 Minor GC/YGC，此时会将 Eden 区和 Survivor 区的所有非垃圾复制到 Survivor 区的另一个空间并清理这两个空间，存活的对象分代年龄会加一；</li><li>GC时如果分代年龄为 15（默认值）的对象会进入老年代中，当老年代空间满了之后，会进行一次 Full GC/Major GC 清理老年代空间，如果无法清理，则会有内存溢出的现象</li></ul><p><strong>对象动态年龄判断</strong>：</p><ul><li>当进行 Minor GC 时，如果存放的对象内存超过 Survivor 中的一个空间的 50% 时，会将其直接放入老年代中</li></ul><h3 id="STW"><a href="#STW" class="headerlink" title="STW"></a>STW</h3><p><strong>STW（stop-the-world）</strong>，每做一次 GC 会使除GC线程的其他所有线程阻塞。<br>JVM 调优的目的就是减少 STW 的次数（主要是减少 Full GC 的次数，因为 Full GC 会检索整个老年代空间从而占用大量的时间）</p><h2 id="Java-Web"><a href="#Java-Web" class="headerlink" title="Java Web"></a>Java Web</h2><h3 id="Servlet"><a href="#Servlet" class="headerlink" title="Servlet"></a>Servlet</h3><p><strong>解释一下什么是 Servlet， 说一说 Servlet 的生命周期</strong></p><p>Servlet 是一种服务器端的 Java 应用程序，具有独立于平台和协议的特性，可以生成动态的 Web 页面。 它担当客户请求（Web 浏览器或其他 HTTP 客户程序）与服务器响应（HTTP 服务器上的数据库或应用程序）的中间层。Servlet是位于 Web 服务器内部的服务器端的 Java 应用程序，与传统的从命令行启动的 Java 应用程序不同，Servlet 由 Web 服务器进行加载，该 Web 服务器必须包含支持 Servlet 的 Java 虚拟机。</p><p>Servlet 生命周期可以分成四个阶段：加载和实例化、初始化、服务、销毁。当客户第一次请求时，首先判断是否存在 Servlet 对象，若不存在，则由 Web 容器创建对象，而后调用 init()方法对其初始化，此初始化方法在整个 Servlet 生命周期中只调用一次。完成 Servlet 对象的创建和实例化之后，Web 容器会调用 Servlet 对象的 service()方法来处理请求。当 Web 容器关闭或者 Servlet 对象要从容器中被删除时，会自动调用 destory()方法。</p><h3 id="JSP"><a href="#JSP" class="headerlink" title="JSP"></a>JSP</h3><p>JSP 本质上就是一个 Servlet，它是 Servlet 的一种特殊形式（由 SUN 公司推出），每个 JSP 页面都是个 Servlet 实例。</p><ul><li>jsp经编译后就变成了Servlet.（JSP的本质就是Servlet，JVM只能识别java的类，不能识别JSP的代码，Web容器将JSP的代码编译成JVM能够识别的java类）</li><li>jsp更擅长表现于页面显示，servlet更擅长于逻辑控制。</li><li>Servlet中没有内置对象，Jsp中的内置对象都是必须通过HttpServletRequest对象，HttpServletResponse对象以及HttpServlet对象得到。</li><li>Jsp是Servlet的一种简化，使用Jsp只需要完成程序员需要输出到客户端的内容，Jsp中的Java脚本如何镶嵌到一个类中，由Jsp容器完成。而Servlet则是个完整的Java类，这个类的Service方法用于生成对客户端的响应。</li></ul><p>JSP 的运行机制如下图所示：</p><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/C85CCA1BB1B94EF3AAE884E24CBAA0AB/348" alt=""></p><p>JVM 只能识别 Java 类，并不能识别 jsp 代码！web 容器收到以 <code>.jsp</code> 为扩展名的 url 请求时，会将访问请求交给 tomcat 中 jsp 引擎处理，每个 jsp 页面第一次被访问时，jsp 引擎将 jsp 代码解释为一个 servlet 源程序，接着编译 servlet 源程序生成 <code>.class</code> 文件，再有 web 容器 servlet 引擎去装载执行 servlet 程序，实现页面交互。</p><h3 id="jsp-内置对象"><a href="#jsp-内置对象" class="headerlink" title="jsp 内置对象"></a>jsp 内置对象</h3><p>JSP 有 9 个内置对象：</p><ul><li>request：封装客户端的请求，其中包含来自GET或POST请求的参数；</li><li>response：封装服务器对客户端的响应；</li><li>pageContext：通过该对象可以获取其他对象；</li><li>session：封装用户会话的对象；</li><li>application：封装服务器运行环境的对象；</li><li>out：输出服务器响应的输出流对象；</li><li>config：Web应用的配置对象；</li><li>page：JSP页面本身（相当于Java程序中的this）；</li><li>exception：封装页面抛出异常的对象。</li></ul><h3 id="jsp-的-4-种作用域"><a href="#jsp-的-4-种作用域" class="headerlink" title="jsp 的 4 种作用域"></a>jsp 的 4 种作用域</h3><p>JSP 中的四种作用域包括 page、request、session 和 application，具体来说：</p><ul><li>page代表与一个页面相关的对象和属性。</li><li>request代表与Web客户机发出的一个请求相关的对象和属性。一个请求可能跨越多个页面，涉及多个Web组件；需要在页面显示的临时数据可以置于此作用域。</li><li>session代表与某个用户与服务器建立的一次会话相关的对象和属性。跟某个用户相关的数据应该放在用户自己的session中。</li><li>application代表与整个Web应用程序相关的对象和属性，它实质上是跨越整个Web应用程序，包括多个页面、请求和会话的一个全局作用域。</li></ul><h3 id="过滤器"><a href="#过滤器" class="headerlink" title="过滤器"></a>过滤器</h3><p><strong>过滤器有哪些作用和用法？</strong></p><p>对于一个 web 应用程序来说，过滤器是处于 web 容器内的一个组件，它会过滤特定请求资源请求信息和响应信息。一个请求来到时，web 容器会判断是否有过滤器与该信息资源相关联，如果有则交给过滤器处理，然后再交给目标资源，响应的时候则以相反的顺序交给过滤器处理，最后再返回给用户浏览器。</p><p>常见的过滤器用途主要包括：对用户请求进行统一认证、对用户的访问请求进行记录和审核、对用户发送的数据进行过滤或替换、转换图象格式、对响应内容进行压缩以减少传输量、对请求或响应进行加解密处理、触发资源访问事件等。</p><h3 id="MVC-设计思想"><a href="#MVC-设计思想" class="headerlink" title="MVC 设计思想"></a>MVC 设计思想</h3><p>MVC 是三个单词的首字母缩写，它们是 Model（模型）、View（视图）和 Controller（控制）。 这个模式认为，程序不论简单或复杂，从结构上看，都可以分成三层：</p><ul><li>最上面的一层，是直接面向最终用户的”视图层”（View）。它是提供给用户的操作界面，是程序的外壳。</li><li>最底下的一层，是核心的”数据层”（Model），也就是程序需要操作的数据或信息。</li><li>中间的一层，就是”控制层”（Controller），它负责根据用户从”视图层”输入的指令，选取”数据层”中的数据，然后对其进行相应的操作，产生最终结果。</li></ul><h3 id="JDBC-流程"><a href="#JDBC-流程" class="headerlink" title="JDBC 流程"></a>JDBC 流程</h3><ul><li>向 DriverManager 类注册驱动数据库驱动程序</li><li>调用 DriverManager.getConnection 方法， 通过 JDBC URL，用户名，密码取得数据库连接的 Connection 对象。</li><li>获取 Connection 后， 便可以通过 createStatement 创建 Statement 用以执行 SQL 语句。</li><li>有时候会得到查询结果，比如 select，得到查询结果，查询（SELECT）的结果存放于结果集（ResultSet）中。</li><li>关闭数据库语句，关闭数据库连接。</li></ul><h3 id="session-和-cookie"><a href="#session-和-cookie" class="headerlink" title="session 和 cookie"></a>session 和 cookie</h3><ul><li>cookie 数据存放在客户的浏览器上，session 数据放在服务器上。</li><li>cookie 不是很安全，别人可以分析存放在本地的 cookie 并进行 cookie 欺骗，考虑到安全应当使用 session。</li><li>session 会在一定时间内保存在服务器上。当访问增多，会比较占用你服务器的性能，考虑到减轻服务器性能方面，应当使用 cookie。</li><li>单个 cookie 保存的数据不能超过 4K，很多浏览器都限制一个站点最多保存 20 个 cookie。</li></ul><p><strong>Cookie 和 Session 的不同点：</strong></p><ul><li>无论客户端做怎样的设置，Session 都能够正常工作。当客户端禁用 Cookie 时将无法使用 Cookie。</li><li>在存储的数据量方面：Session 能够存储任意的 java 对象，Cookie 只能存储 String 类型的对象。</li></ul><h3 id="session-的工作原理"><a href="#session-的工作原理" class="headerlink" title="session 的工作原理"></a>session 的工作原理</h3><p>其实session是一个存在服务器上的类似于一个散列表格的文件。里面存有我们需要的信息，在我们需要用的时候可以从里面取出来。类似于一个大号的map吧，里面的键存储的是用户的sessionid，用户向服务器发送请求的时候会带上这个sessionid。这时就可以从中取出对应的值了。</p><h3 id="如何避免-sql-注入"><a href="#如何避免-sql-注入" class="headerlink" title="如何避免 sql 注入"></a>如何避免 sql 注入</h3><ul><li>PreparedStatement（简单又有效的方法）</li><li>字符串过滤</li><li>JSP页面判断代码</li></ul><h3 id="XSS-攻击"><a href="#XSS-攻击" class="headerlink" title="XSS 攻击"></a>XSS 攻击</h3><p>XSS攻击又称CSS,全称Cross Site Script（跨站脚本攻击），其原理是攻击者向有XSS漏洞的网站中输入恶意的 HTML 代码，当用户浏览该网站时，这段 HTML 代码会自动执行，从而达到攻击的目的。</p><p>XSS防范的总体思路是：对输入(和URL参数)进行过滤，对输出进行编码。</p><h3 id="CSRF-攻击"><a href="#CSRF-攻击" class="headerlink" title="CSRF 攻击"></a>CSRF 攻击</h3><p>CSRF（Cross-site request forgery, <strong>跨站请求伪造</strong>）。一般来说，攻击者通过伪造用户的浏览器的请求，向访问一个用户自己曾经认证访问过的网站发送出去，使目标网站接收并误以为是用户的真实操作而去执行命令。</p><p>如何避免：</p><ul><li>验证 HTTP Referer 字段<br>HTTP头中的Referer字段记录了该 HTTP 请求的来源地址。在通常情况下，访问一个安全受限页面的请求来自于同一个网站，而如果黑客要对其实施 CSRF<br>攻击，他一般只能在他自己的网站构造请求。因此，可以通过验证Referer值来防御CSRF 攻击。</li><li>使用验证码<br>关键操作页面加上验证码，后台收到请求后通过判断验证码可以防御CSRF。</li></ul><h2 id="网络"><a href="#网络" class="headerlink" title="网络"></a>网络</h2><h3 id="http-响应码"><a href="#http-响应码" class="headerlink" title="http 响应码"></a>http 响应码</h3><ul><li>200 （成功） 服务器已成功处理了请求。通常，这表示服务器提供了请求的网页。</li><li>201 （已创建） 请求成功并且服务器创建了新的资源。</li><li>202 （已接受） 服务器已接受请求，但尚未处理。</li><li>203 （非授权信息） 服务器已成功处理了请求，但返回的信息可能来自另一来源。</li><li>204 （无内容） 服务器成功处理了请求，但没有返回任何内容。</li><li>205 （重置内容）服务器成功处理了请求，但没有返回任何内容。</li><li>206 （部分内容） 服务器成功处理了部分 GET 请求。</li><li>300 （多种选择） 针对请求，服务器可执行多种操作。服务器可根据请求者 (user agent) 选择一项操作，或提供操作列表供请求者选择。</li><li>301 （永久移动） 请求的网页已永久移动到新位置。服务器返回此响应（对 GET 或 HEAD 请求的响应）时，会自动将请求者转到新位置。</li><li>302 （临时移动） 服务器目前从不同位置的网页响应请求，但请求者应继续使用原有位置来进行以后的请求。</li><li>303 （查看其他位置）请求者应当对不同的位置使用单独的 GET 请求来检索响应时，服务器返回此代码。</li><li>304 （未修改）自从上次请求后，请求的网页未修改过。服务器返回此响应时，不会返回网页内容。</li><li>305 （使用代理）请求者只能使用代理访问请求的网页。如果服务器返回此响应，还表示请求者应使用代理。</li><li>307 （临时重定向） 服务器目前从不同位置的网页响应请求，但请求者应继续使用原有位置来进行以后的请求。</li><li>400 （错误请求）服务器不理解请求的语法。</li><li>401 （未授权）请求要求身份验证。对于需要登录的网页，服务器可能返回此响应。</li><li>403 （禁止）服务器拒绝请求。</li><li>404 （未找到）服务器找不到请求的网页。</li><li>405 （方法禁用）禁用请求中指定的方法。</li><li>406 （不接受）无法使用请求的内容特性响应请求的网页。</li><li>407 （需要代理授权）此状态代码与 401（未授权）类似，但指定请求者应当授权使用代理。</li><li>408 （请求超时） 服务器等候请求时发生超时。</li><li>409 （冲突） 服务器在完成请求时发生冲突。服务器必须在响应中包含有关冲突的信息。</li><li>410 （已删除） 如果请求的资源已永久删除，服务器就会返回此响应。</li><li>411 （需要有效长度）服务器不接受不含有效内容长度标头字段的请求。</li><li>412 （未满足前提条件）服务器未满足请求者在请求中设置的其中一个前提条件。</li><li>413 （请求实体过大）服务器无法处理请求，因为请求实体过大，超出服务器的处理能力。</li><li>414 （请求的 URI 过长）请求的 URI（通常为网址）过长，服务器无法处理。</li><li>415 （不支持的媒体类型）请求的格式不受请求页面的支持。</li><li>416 （请求范围不符合要求）如果页面无法提供请求的范围，则服务器会返回此状态代码。</li><li>417 （未满足期望值）服务器未满足”期望”请求标头字段的要求。</li><li>500 （服务器内部错误） 服务器遇到错误，无法完成请求。</li><li>501 （尚未实施）服务器不具备完成请求的功能。例如，服务器无法识别请求方法时可能会返回此代码。</li><li>502 （错误网关）服务器作为网关或代理，从上游服务器收到无效响应。</li><li>503 （服务不可用）服务器目前无法使用（由于超载或停机维护）。通常，这只是暂时状态。</li><li>504 （网关超时） 服务器作为网关或代理，但是没有及时从上游服务器收到请求。</li><li>505 （HTTP 版本不受支持）服务器不支持请求中所用的 HTTP 协议版本。</li></ul><h3 id="HTTP-和-HTTPS"><a href="#HTTP-和-HTTPS" class="headerlink" title="HTTP 和 HTTPS"></a>HTTP 和 HTTPS</h3><ul><li>安全性上，HTTPS是安全超文本协议，在HTTP基础上有更强的安全性。简单来说，HTTPS是使用TLS/SSL加密的HTTP协议</li><li>申请证书上，HTTPS需要使用ca申请证书</li><li>传输协议上, HTTP是超文本传输协议，明文传输；HTTPS是具有安全性的 SSL 加密传输协议</li><li>连接方式与端口上，http的连接简单，是无状态的，端口是 80； https 在http的基础上使用了ssl协议进行加密传输，端口是 443</li></ul><h3 id="长连接、短连接"><a href="#长连接、短连接" class="headerlink" title="长连接、短连接"></a>长连接、短连接</h3><p><strong>http 的长连接和短连接区别？</strong></p><ul><li>HTTP 协议有 HTTP/1.0 版本和 HTTP/1.1 版本。HTTP1.1 默认保持长连接（HTTP persistent connection，也翻译为持久连接），数据传输完成了保持 TCP 连接不断开（不发RST包、不四次握手），等待在同域名下继续用这个通道传输数据；相反的就是短连接。</li><li>在 HTTP/1.0 中，默认使用的是短连接。也就是说，浏览器和服务器每进行一次 HTTP 操作，就建立一次连接，任务结束就中断连接。从 HTTP/1.1 起，默认使用的是长连接，用以保持连接特性。</li></ul><h3 id="forward-和-redirect"><a href="#forward-和-redirect" class="headerlink" title="forward 和 redirect"></a>forward 和 redirect</h3><p>Forward 和 Redirect 代表了两种请求转发方式：<strong>直接转发</strong>和<strong>间接转发</strong>。</p><ul><li>直接转发方式（Forward），客户端和浏览器只发出一次请求，Servlet、HTML、JSP 或其它信息资源，由第二个信息资源响应该请求，在请求对象request中，保存的对象对于每个信息资源是共享的。</li><li>间接转发方式（Redirect）实际是两次 HTTP 请求，服务器端在响应第一次请求的时候，让浏览器再向另外一个URL发出请求，从而达到转发的目的。</li></ul><h3 id="tcp-和-udp"><a href="#tcp-和-udp" class="headerlink" title="tcp 和 udp"></a>tcp 和 udp</h3><ul><li>TCP面向连接（如打电话要先拨号建立连接）;UDP是无连接的，即发送数据之前不需要建立连接。</li><li>TCP提供可靠的服务。也就是说，通过TCP连接传送的数据，无差错，不丢失，不重复，且按序到达;UDP尽最大努力交付，即不保证可靠交付。</li><li>Tcp通过校验和，重传控制，序号标识，滑动窗口、确认应答实现可靠传输。如丢包时的重发控制，还可以对次序乱掉的分包进行顺序控制。</li><li>UDP具有较好的实时性，工作效率比TCP高，适用于对高速传输和实时性有较高的通信或广播通信。</li><li>每一条TCP连接只能是点到点的;UDP支持一对一，一对多，多对一和多对多的交互通信。</li><li>TCP对系统资源要求较多，UDP对系统资源要求较少。</li></ul><h3 id="tcp-三次握手"><a href="#tcp-三次握手" class="headerlink" title="tcp 三次握手"></a>tcp 三次握手</h3><p>在TCP/IP协议中，TCP协议提供可靠的连接服务，采用三次握手建立一个连接。</p><ul><li>第一次握手：建立连接时，客户端发送 syn 包 (syn=j) 到服务器，并进入 SYN_SEND 状态，等待服务器确认；SYN：同步序列编号(Synchronize Sequence Numbers)。</li><li>第二次握手：服务器收到 syn 包，必须确认客户的 SYN(ack=j+1)，同时自己也发送一个 SYN 包 (syn=k)，即SYN+ACK 包，此时服务器进入 SYN_RECV 状态。</li><li>第三次握手：客户端收到服务器的 SYN＋ACK 包，向服务器发送确认包 ACK(ack=k+1)，此包发送完毕，客户端和服务器进入 ESTABLISHED 状态，完成三次握手。完成三次握手，客户端与服务器开始传送数据</li></ul><h3 id="OSI-七层模型"><a href="#OSI-七层模型" class="headerlink" title="OSI 七层模型"></a>OSI 七层模型</h3><ul><li>应用层：网络服务与最终用户的一个接口。</li><li>表示层：数据的表示、安全、压缩。</li><li>会话层：建立、管理、终止会话。</li><li>传输层：定义传输数据的协议端口号，以及流控和差错校验。</li><li>网络层：进行逻辑地址寻址，实现不同网络之间的路径选择。</li><li>数据链路层：建立逻辑连接、进行硬件地址寻址、差错校验等功能。</li><li>物理层：建立、维护、断开物理连接。</li></ul><h3 id="get-和-post"><a href="#get-和-post" class="headerlink" title="get 和 post"></a>get 和 post</h3><ul><li>GET在浏览器回退时是无害的，而POST会再次提交请求。</li><li>GET产生的URL地址可以被Bookmark，而POST不可以。</li><li>GET请求会被浏览器主动cache，而POST不会，除非手动设置。</li><li>GET请求只能进行url编码，而POST支持多种编码方式。</li><li>GET请求参数会被完整保留在浏览器历史记录里，而POST中的参数不会被保留。</li><li>GET请求在URL中传送的参数是有长度限制的，而POST么有。</li><li>对参数的数据类型，GET只接受ASCII字符，而POST没有限制。</li><li>GET比POST更不安全，因为参数直接暴露在URL上，所以不能用来传递敏感信息。</li><li>GET参数通过URL传递，POST放在Request body中。</li></ul><h2 id="Spring-Spring-MVC"><a href="#Spring-Spring-MVC" class="headerlink" title="Spring / Spring MVC"></a>Spring / Spring MVC</h2><h3 id="为什么要使用-spring"><a href="#为什么要使用-spring" class="headerlink" title="为什么要使用 spring"></a>为什么要使用 spring</h3><ul><li>目的：解决企业应用开发的复杂性</li><li>功能：使用基本的JavaBean代替EJB，并提供了更多的企业应用功能</li><li>范围：任何Java应用</li></ul><h3 id="Spring-能干什么"><a href="#Spring-能干什么" class="headerlink" title="Spring 能干什么"></a>Spring 能干什么</h3><ul><li>Spring 能帮我们根据配置文件创建及组装对象之间的依赖关系。</li><li>Spring 根据配置文件来进行创建及组装对象间依赖关系，只需要改配置文件即可</li><li>Spring 面向切面编程能帮助我们无耦合的实现日志记录，性能统计，安全控制。</li><li>Spring 面向切面编程能提供一种更好的方式来完成，一般通过配置方式，而且不需要在现有代码中添加任何额外代码，现有代码专注业务逻辑。</li><li>Spring 能非常简单的帮我们管理数据库事务。</li><li>采用 Spring，我们只需获取连接，执行SQL，其他事物相关的都交给Spring来管理了。</li><li>Spring 还能与第三方数据库访问框架（如Hibernate、JPA）无缝集成，而且自己也提供了一套 JDBC 访问模板，来方便数据库访问。</li><li>Spring 还能与第三方 Web（如Struts、JSF）框架无缝集成，而且自己也提供了一套 SpringMVC 框架，来方便 web 层搭建。</li><li>Spring 能方便的与 JavaEE（如JavaMail、任务调度）整合，与更多技术整合（比如缓存框架）。</li></ul><h3 id="BeanFactory"><a href="#BeanFactory" class="headerlink" title="BeanFactory"></a>BeanFactory</h3><p><strong>BeanFactory常用的实现类有哪些？</strong><br>Bean工厂是工厂模式的一个实现，提供了控制反转功能，用来把应用的配置和依赖从正真的应用代码中分离。常用的BeanFactory实现有DefaultListableBeanFactory、XmlBeanFactory、ApplicationContext等。XMLBeanFactory，最常用的就是org.springframework.beans.factory.xml.XmlBeanFactory，它根据XML文件中的定义加载beans。该容器从XML文件读取配置元数据并用它去创建一个完全配置的系统或应用。</p><p><strong>ApplicationContext 的实现类有哪些？</strong></p><ul><li>FileSystemXmlApplicationContext：此容器从一个XML文件中加载beans的定义，XMLBean配置文件的全路径名必须提供给它的构造函数。</li><li>ClassPathXmlApplicationContext：此容器也从一个XML文件中加载beans的定义，这里，你需要正确设置classpath因为这个容器将在classpath里找bean配置。</li><li>WebXmlApplicationContext：此容器加载一个XML文件，此文件定义了一个WEB应用的所有bean。</li></ul><p><strong>BeanFactory 与 AppliacationContext 有什么区别？</strong></p><ul><li>BeanFactory：基础类型的IOC容器，提供完成的IOC服务支持。如果没有特殊指定，默认采用延迟初始化策略。相对来说，容器启动初期速度较快，所需资源有限。</li><li>ApplicationContext：ApplicationContext是在BeanFactory的基础上构建，是相对比较高级的容器实现，除了BeanFactory的所有支持外，ApplicationContext还提供了事件发布、国际化支持等功能。ApplicationContext管理的对象，在容器启动后默认全部初始化并且绑定完成。</li></ul><h3 id="AOP"><a href="#AOP" class="headerlink" title="AOP"></a>AOP</h3><p>AOP（Aspect-Oriented Programming，面向方面编程），可以说是OOP（Object-Oriented Programing，面向对象编程）的补充和完善。OOP引入封装、继承和多态性等概念来建立一种对象层次结构，用以模拟公共行为的一个集合。当我们需要为分散的对象引入公共行为的时候，OOP则显得无能为力。也就是说，OOP允许你定义从上到下的关系，但并不适合定义从左到右的关系。例如日志功能。日志代码往往水平地散布在所有对象层次中，而与它所散布到的对象的核心功能毫无关系。对于其他类型的代码，如安全性、异常处理和透明的持续性也是如此。这种散布在各处的无关的代码被称为横切（cross-cutting）代码，在OOP设计中，它导致了大量代码的重复，而不利于各个模块的重用。</p><p>而AOP技术则恰恰相反，它利用一种称为“横切”的技术，剖解开封装的对象内部，并将那些影响了多个类的公共行为封装到一个可重用模块，并将其名为“Aspect”，即方面。所谓“方面”，简单地说，就是将那些与业务无关，却为业务模块所共同调用的逻辑或责任封装起来，便于减少系统的重复代码，降低模块间的耦合度，并有利于未来的可操作性和可维护性。AOP代表的是一个横向的关系，如果说“对象”是一个空心的圆柱体，其中封装的是对象的属性和行为；那么面向方面编程的方法，就仿佛一把利刃，将这些空心圆柱体剖开，以获得其内部的消息。而剖开的切面，也就是所谓的“方面”了。然后它又以巧夺天功的妙手将这些剖开的切面复原，不留痕迹。</p><p>使用“横切”技术，AOP把软件系统分为两个部分：核心关注点和横切关注点。业务处理的主要流程是核心关注点，与之关系不大的部分是横切关注点。横切关注点的一个特点是，他们经常发生在核心关注点的多处，而各处都基本相似。比如权限认证、日志、事务处理。Aop 的作用在于分离系统中的各种关注点，将核心关注点和横切关注点分离开来。正如Avanade公司的高级方案构架师Adam Magee所说，AOP的核心思想就是“将应用程序中的商业逻辑同对其提供支持的通用服务进行分离。”</p><h3 id="IOC"><a href="#IOC" class="headerlink" title="IOC"></a>IOC</h3><p>IOC是Inversion of Control的缩写，多数书籍翻译成“控制反转”。</p><p>1996年，Michael Mattson在一篇有关探讨面向对象框架的文章中，首先提出了IOC 这个概念。对于面向对象设计及编程的基本思想，前面我们已经讲了很多了，不再赘述，简单来说就是把复杂系统分解成相互合作的对象，这些对象类通过封装以后，内部实现对外部是透明的，从而降低了解决问题的复杂度，而且可以灵活地被重用和扩展。</p><h3 id="spring-主要模块"><a href="#spring-主要模块" class="headerlink" title="spring 主要模块"></a>spring 主要模块</h3><p>Spring框架至今已集成了20多个模块。这些模块主要被分如下图所示的核心容器、数据访问/集成,、Web、AOP（面向切面编程）、工具、消息和测试模块。</p><h3 id="依赖注入"><a href="#依赖注入" class="headerlink" title="依赖注入"></a>依赖注入</h3><p><strong>什么是 Spring 的依赖注入？</strong><br>平常的java开发中，程序员在某个类中需要依赖其它类的方法，则通常是new一个依赖类再调用类实例的方法，这种开发存在的问题是new的类实例不好统一管理，spring提出了依赖注入的思想，即依赖类不由程序员实例化，而是通过spring容器帮我们new指定实例并且将实例注入到需要该对象的类中。依赖注入的另一种说法是“控制反转”，通俗的理解是：平常我们new一个实例，这个实例的控制权是我们程序员，而控制反转是指new实例工作不由我们程序员来做而是交给spring容器来做。</p><p>Spring 通过DI（依赖注入）实现 IOC（控制反转），常用的注入方式主要有三种：</p><ul><li>构造方法注入</li><li>setter注入</li><li>基于注解的注入</li></ul><h3 id="Bean-生命周期"><a href="#Bean-生命周期" class="headerlink" title="Bean 生命周期"></a>Bean 生命周期</h3><ul><li>Bean 定义：在配置文件里面用 <code>&lt;bean&gt;&lt;/bean&gt;</code> 来进行定义。</li><li>Bean 初始化：有两种方式初始化：<ul><li>在配置文件中通过指定 init-method 属性来完成。</li><li>实现 org.springframwork.beans.factory.InitializingBean 接口。</li></ul></li><li>Bean 调用：有三种方式可以得到 Bean 实例，并进行调用</li><li>Bean 销毁：销毁有两种方式：<ul><li>使用配置文件指定的 destroy-method 属性。</li><li>实现 org.springframwork.bean.factory.DisposeableBean。</li></ul></li></ul><h3 id="Bean-的作用域"><a href="#Bean-的作用域" class="headerlink" title="Bean 的作用域"></a>Bean 的作用域</h3><ul><li>singleton：单例模式，在整个Spring IoC容器中，使用singleton定义的Bean将只有一个实例</li><li>prototype：原型模式，每次通过容器的getBean方法获取prototype定义的Bean时，都将产生一个新的Bean实例</li><li>request：对于每次HTTP请求，使用request定义的Bean都将产生一个新实例，即每次HTTP请求将会产生不同的Bean实例。只有在Web应用中使用Spring时，该作用域才有效</li><li>session：对于每次HTTP Session，使用session定义的Bean豆浆产生一个新实例。同样只有在Web应用中使用Spring时，该作用域才有效</li><li>globalsession：每个全局的HTTP Session，使用session定义的Bean都将产生一个新实例。典型情况下，仅在使用portlet context的时候有效。同样只有在Web应用中使用Spring时，该作用域才有效</li></ul><h3 id="事务"><a href="#事务" class="headerlink" title="事务"></a>事务</h3><ul><li>基于 TransactionProxyFactoryBean 的声明式事务管理</li><li>基于 @Transactional 的声明式事务管理</li><li>基于 Aspectj AOP 配置事务</li></ul><h3 id="事务属性"><a href="#事务属性" class="headerlink" title="事务属性"></a>事务属性</h3><p><strong>事务的传播行为（传播属性）：</strong></p><ul><li>当事务方法被另一个事务方法去调用时，必须指定事务应该如何传播，是使用原来的事务还是开启一个新的事务</li></ul><p><strong>属性：</strong></p><ul><li>propagation：用来设置事务的传播行为（Spring 定义了 7 种传播行为，这里介绍常用的两种）<ul><li>REQUIRED：默认值，使用原来的事务</li><li>REQUIRED_NEW：将原来的事务挂起，开启一个新事务</li></ul></li><li>isolation：用来设置事务的隔离级别<ul><li>REPEATABLE_READ：可重复读，MySql 默认的隔离级别，可避免脏读与不可重复读的问题</li><li>READ_COMMITTED：读已提交， Oracle 默认的隔离级别，开发时通常使用的隔离级别，可解决脏读的问题</li><li>READ_UNCOMMITTED：读未提交，可以读取到未提交的数据，不能解决任何问题</li></ul></li></ul><h3 id="事务隔离"><a href="#事务隔离" class="headerlink" title="事务隔离"></a>事务隔离</h3><p>事务隔离级别指的是一个事务对数据的修改与另一个并行的事务的隔离程度，当多个事务同时访问相同数据时，如果没有采取必要的隔离机制，就可能发生以下问题：</p><ul><li>脏读（一个事务读到了另一个事务回滚前的无效值）</li><li>不可重复读（一个事务重复读一个值时被另一个事务修改，造成多次读取的结果不一样）</li><li>幻读（一个事务重复读一组数据时，被另一个事务添加了数据，再次读取时发现多了数据，就像产生了幻觉一样）</li></ul><h3 id="Smvc-运行流程"><a href="#Smvc-运行流程" class="headerlink" title="Smvc 运行流程"></a>Smvc 运行流程</h3><p><img src="https://note.youdao.com/yws/public/resource/6c75bc432448bc1eee8de1816cf5c5d9/xmlnote/E904A5E7BC7949E7A5410330F37B879D/359" alt=""></p><ul><li>用户发送请求，由中央控制器（DispatcherServlet）收到请求，调用 HandlerMapping（处理器映射器）找到处理器</li><li>然后 DispatcherServlet 会将映射器的结果传给 HandlerAdapter （处理器适配器）来调用相应的 Controller（如果有拦截器会执行拦截器的 preHandler 方法）</li><li>Controller 将 ModelAndView 一步一步返回给 DispatcherServlet，然后中央 Servlet 调用 ViewResolver</li><li>（视图解析器）将 view （视图）返回给 中央 Servlet，中央 Servlet 再对 view 进行渲染（如果有拦截器，则执行拦截器的 postHandler 方法）</li><li>将渲染好的视图返回给用户</li></ul><h3 id="Smvc-有哪些组件"><a href="#Smvc-有哪些组件" class="headerlink" title="Smvc 有哪些组件"></a>Smvc 有哪些组件</h3><ul><li>DispatcherServlet：中央控制器，把请求给转发到具体的控制类</li><li>Controller：具体处理请求的控制器</li><li>HandlerMapping：映射处理器，负责映射中央处理器转发给controller时的映射策略</li><li>ModelAndView：服务层返回的数据和视图层的封装类</li><li>ViewResolver：视图解析器，解析具体的视图</li><li>Interceptors ：拦截器，负责拦截我们定义的请求然后做处理工作</li></ul><h3 id="中文乱码"><a href="#中文乱码" class="headerlink" title="中文乱码"></a>中文乱码</h3><ul><li>解决 POST 乱码：在 web.xml 中配置一个过滤器 filter：CharacterEncodingFilter，将其 encoding 的值配置为 utf-8</li><li>解决 Get 乱码：在 Tomcat 的 server.xml 中的 Connector 标签中添加 URIEncoding=”UTF-8” 的属性</li></ul><h3 id="拦截器、过滤器、监听器"><a href="#拦截器、过滤器、监听器" class="headerlink" title="拦截器、过滤器、监听器"></a>拦截器、过滤器、监听器</h3><ul><li>拦截器：是指通过统一拦截从浏览器发往服务器的请求来完成功能的增强。使用场景：解决请求的共性问题（乱码问题、权限验证问题）。</li><li>过滤器：Servlet 中的过滤器 Filter 是实现了javax.servlet.Filter接口的服务器端程序，主要的用途是过滤字符编码、做一些业务逻辑判断等。其工作原理是只要你在web.xml文件配置好要拦截的客户端请求，它都会帮你拦截到请求，此时你就可以对请求或响应(Request、Response)统一设置编码，简化操作；同时还可进行逻辑判断，如用户是否已经登录、有没有权限访问该页面等等工作。它是随你的web应用启动而启动的，只初始化一次，以后就可以拦截相关请求，只有当你的web应用停止或重新部署的时候才销毁。</li><li>监听器：现在来说说 Servlet 的监听器 Listener，其中有一个监听器是监听上下文的，它实现了javax.servlet.ServletContextListener接口，它也是随web应用的启动而启动，只初始化一次，随web应用的停止而销毁。主要作用是：做一些初始化的内容、设置一些基本的内容、比如一些参数或者是一些固定的对象等等。</li></ul><h3 id="常用注解"><a href="#常用注解" class="headerlink" title="常用注解"></a>常用注解</h3><p>Spring 常用注解：</p><ul><li><code>@Required</code>：该注解应用于设值方法。</li><li><code>@Autowired</code>：该注解应用于有值设值方法、非设值方法、构造方法和变量。</li><li><code>@Qualifier</code>：该注解和 <code>@Autowired</code> 搭配使用，用于消除特定 bean 自动装配的歧义。</li></ul><p>Spring MVC 常用注解：</p><ul><li><code>@RequestMapping</code> 用于请求url映射。</li><li><code>@RequestBody</code> 注解实现接收 http 请求的 json 数据，将 json 数据转换为 java 对象。</li><li><code>@ResponseBody</code> 注解实现将 controller 方法返回对象转化为 json 响应给客户。</li></ul><p><strong>注</strong>：注解装配在 spring 中默认是关闭的。所以需要在 spring 的核心配置文件中配置一下才能使用基于注解的装配模式。配置方式如下：<code>&lt;context:annotation-config/&gt;</code>；Spring MVC 需要 <code>&lt;mvc:annotation-driven&gt;</code> 来实现注解的开启</p><h2 id="Mybatis"><a href="#Mybatis" class="headerlink" title="Mybatis"></a>Mybatis</h2><h3 id="ORM"><a href="#ORM" class="headerlink" title="ORM"></a>ORM</h3><p>对象关系映射（Object Relational Mapping，简称ORM）是一种为了解决面向对象与关系数据库存在的互不匹配的现象的技术。 简单的说，ORM 是通过使用描述对象和数据库之间映射的元数据，将 java 程序中的对象自动持久化到关系数据库中。当然反过来也是可以的，例如将数据库表当中的记录查询出来，然后映射为 Java 程序中的 Java 对象。</p><h3 id="和-的区别"><a href="#和-的区别" class="headerlink" title="#{}和 ${}的区别"></a>#{}和 ${}的区别</h3><ul><li>#{}是预编译处理，${}是字符串替换；</li><li>Mybatis在处理#{}时，会将sql中的#{}替换为?号，调用PreparedStatement的set方法来赋值；</li><li>Mybatis在处理${}时，就是把${}替换成变量的值；</li><li>使用#{}可以有效的防止SQL注入，提高系统安全性。</li></ul><h3 id="使用步骤"><a href="#使用步骤" class="headerlink" title="使用步骤"></a>使用步骤</h3><ol><li>创建 SqlSessionFactory</li><li>通过 SqlSessionFactory 创建 SqlSession</li><li>通过 sqlsession 执行数据库操作</li><li>调用 session.commit() 提交事务</li><li>调用 session.close() 关闭会话</li></ol><h3 id="字段名与表名不同"><a href="#字段名与表名不同" class="headerlink" title="字段名与表名不同"></a>字段名与表名不同</h3><ol><li>在写 Sql 语句中的时候起别名，让别名和我们类中的属性名保持一致</li><li>在 MyBatis 的全局配置文件中开启驼峰命名规则（settings 标签中的 setting name=mapUnderscoreToCameCase）</li><li>在 Mapper 映射文件中使用 ResultMap 来自定义映射规则（result 标签中的 colunm 对应数据库表，property 对应实体类）</li></ol><h3 id="延迟加载"><a href="#延迟加载" class="headerlink" title="延迟加载"></a>延迟加载</h3><p>Mybatis仅支持association关联对象和collection关联集合对象的延迟加载，association指的就是一对一，collection指的就是一对多查询。在Mybatis配置文件中，可以配置是否启用延迟加载lazyLoadingEnabled=true|false。</p><p>它的原理是，使用CGLIB创建目标对象的代理对象，当调用目标方法时，进入拦截器方法，比如调用a.getB().getName()，拦截器invoke()方法发现a.getB()是null值，那么就会单独发送事先保存好的查询关联B对象的sql，把B查询上来，然后调用a.setB(b)，于是a的对象b属性就有值了，接着完成a.getB().getName()方法的调用。这就是延迟加载的基本原理。</p><h3 id="缓存"><a href="#缓存" class="headerlink" title="缓存"></a>缓存</h3><ul><li>一级缓存: 基于 PerpetualCache 的 HashMap 本地缓存，其存储作用域为 Session，当 Session flush 或 close 之后，该 Session 中的所有 Cache 就将清空，默认打开一级缓存。</li><li>二级缓存与一级缓存其机制相同，默认也是采用 PerpetualCache，HashMap 存储，不同在于其存储作用域为 Mapper(Namespace)，并且可自定义存储源，如 Ehcache。默认不打开二级缓存，要开启二级缓存，使用二级缓存属性类需要实现Serializable序列化接口(可用来保存对象的状态),可在它的映射文件中配置<code>&lt;cache/&gt;</code></li></ul><h3 id="执行器（Executor）"><a href="#执行器（Executor）" class="headerlink" title="执行器（Executor）"></a>执行器（Executor）</h3><p>Mybatis有三种基本的执行器（Executor）：</p><ul><li>SimpleExecutor：每执行一次update或select，就开启一个Statement对象，用完立刻关闭Statement对象。</li><li>ReuseExecutor：执行update或select，以sql作为key查找Statement对象，存在就使用，不存在就创建，用完后，不关闭Statement对象，而是放置于Map内，供下一次使用。简言之，就是重复使用Statement对象。</li><li>BatchExecutor：执行update（没有select，JDBC批处理不支持select），将所有sql都添加到批处理中（addBatch()），等待统一执行（executeBatch()），它缓存了多个Statement对象，每个Statement对象都是addBatch()完毕后，等待逐一执行executeBatch()批处理。与JDBC批处理相同。</li></ul><h2 id="数据库"><a href="#数据库" class="headerlink" title="数据库"></a>数据库</h2><h3 id="三范式"><a href="#三范式" class="headerlink" title="三范式"></a>三范式</h3><ul><li>第一范式：强调的是列的原子性，即数据库表的每一列都是不可分割的原子数据项。</li><li>第二范式：要求实体的属性完全依赖于主关键字。所谓完全依赖是指不能存在仅依赖主关键字一部分的属性。</li><li>第三范式：任何非主属性不依赖于其它非主属性。</li></ul><h3 id="ACID"><a href="#ACID" class="headerlink" title="ACID"></a>ACID</h3><ul><li>Atomicity（原子性）：一个事务（transaction）中的所有操作，或者全部完成，或者全部不完成，不会结束在中间某个环节。事务在执行过程中发生错误，会被恢复（Rollback）到事务开始前的状态，就像这个事务从来没有执行过一样。即，事务不可分割、不可约简。</li><li>Consistency（一致性）：在事务开始之前和事务结束以后，数据库的完整性没有被破坏。这表示写入的资料必须完全符合所有的预设约束、触发器、级联回滚等。</li><li>Isolation（隔离性）：数据库允许多个并发事务同时对其数据进行读写和修改的能力，隔离性可以防止多个事务并发执行时由于交叉执行而导致数据的不一致。事务隔离分为不同级别，包括读未提交（Read uncommitted）、读提交（read committed）、可重复读（repeatable read）和串行化（Serializable）。</li><li>Durability（持久性）：事务处理结束后，对数据的修改就是永久的，即便系统故障也不会丢失。</li></ul><h3 id="char-和-varchar"><a href="#char-和-varchar" class="headerlink" title="char 和 varchar"></a>char 和 varchar</h3><ul><li>char(n) ：固定长度类型，比如订阅 char(10)，当你输入”abc”三个字符的时候，它们占的空间还是 10 个字节，其他 7 个是空字节。</li><li>chat 优点：效率高；缺点：占用空间；适用场景：存储密码的 md5 值，固定长度的，使用 char 非常合适。</li><li>varchar(n) ：可变长度，存储的值是每个值占用的字节再加上一个用来记录其长度的字节的长度。</li></ul><p>所以，从空间上考虑 varcahr 比较合适；从效率上考虑 char 比较合适，二者使用需要权衡。</p><h3 id="float-和-double"><a href="#float-和-double" class="headerlink" title="float 和 double"></a>float 和 double</h3><ul><li>float 最多可以存储 8 位的十进制数，并在内存中占 4 字节。</li><li>double 最可可以存储 16 位的十进制数，并在内存中占 8 字节。</li></ul><h3 id="内连接、左连接、右连接"><a href="#内连接、左连接、右连接" class="headerlink" title="内连接、左连接、右连接"></a>内连接、左连接、右连接</h3><p>内连接关键字：inner join；左连接：left join；右连接：right join。</p><p>内连接是把匹配的关联数据显示出来；左连接是左边的表全部显示出来，右边的表显示出符合条件的数据；右连接正好相反。</p><h3 id="索引"><a href="#索引" class="headerlink" title="索引"></a>索引</h3><p>索引（Index）是帮助 MySQL 高效获取数据的 <strong>排好序的</strong> 数据结构</p><ul><li>优势：提高数据检索的效率，降低数据库的 IO 成本；通过索引对数据进行排序，降低数据排序成本，降低 CPU 消耗</li><li>劣势：虽然索引大大提高了查询速度，同时却会降低更新表的速度（写操作）；索引会额外占用磁盘空间</li></ul><p>索引的数据结构通常是：B+Tree，一个结点存储多个数据（MySQL通常限制为 16K），可以将树的高度控制在</p><p>一个比较小的范围中，千万级别的数据量也可以在很少的磁盘IO中读取到查找的内容，</p><p>同时，B+树的叶子结点对范围查找也是非常友好的</p><p><strong>InnoDB索引（聚集索引）：</strong></p><ul><li>表数据文件本身就是按 B+Tree 组织的一个索引结构文件</li><li>聚集索引：叶子结点包含了完整的数据记录</li><li>为什么 InnoDB表必须要有主键，并且推荐使用整型的自增主键？</li><li>因为InnoDB的主键索引是一个聚集索引，InnoDB在对表进行操作时候需要维护这个索引</li></ul><p><strong>联合索引的底层存储结构长什么样？</strong></p><ul><li>同样是一颗 B+Tree，只不过是字段的比较不再是比第一个了，而是从上往下</li><li>第一个字段相同时，比第二个字段，第一第二个字段都相同时，比第三个字段</li></ul><p><strong>MySQL什么时候适合建索引，什么时候不适合？</strong></p><ul><li>主键会自动建立唯一索引；</li><li>频繁作为查询条件的字段应该创建索引；</li><li>外键关系关联查询应该创建索引；</li><li>组合键应该创建索引；</li><li>排序字段或统计分组时应该创建索引；</li><li>表记录太少时不适合；经常增删改的表或字段<strong>不适合</strong>；</li><li>Where 里面用不到的字段不适合；过滤性不好的字段<strong>不适合</strong>；</li></ul><h3 id="事务隔离-1"><a href="#事务隔离-1" class="headerlink" title="事务隔离"></a>事务隔离</h3><p>所谓事务是用户定义的一个数据库操作序列，这些操作要么全做要么全不做，是一个不可分割的工作单位。</p><p>MySQL 的事务隔离是在 MySQL. ini 配置文件里添加的，在文件的最后添加：transaction-isolation = REPEATABLE-READ</p><p>可用的配置值：READ-UNCOMMITTED、READ-COMMITTED、REPEATABLE-READ、SERIALIZABLE。</p><ul><li>READ-UNCOMMITTED：未提交读，最低隔离级别、事务未提交前，就可被其他事务读取（会出现幻读、脏读、不可重复读）。</li><li>READ-COMMITTED：提交读，一个事务提交后才能被其他事务读取到（会造成幻读、不可重复读）。</li><li>REPEATABLE-READ：可重复读，默认级别，保证多次读取同一个数据时，其值都和事务开始时候的内容是一致，禁止读取到别的事务未提交的数据（会造成幻读）。</li><li>SERIALIZABLE：序列化，代价最高最可靠的隔离级别，该隔离级别能防止脏读、不可重复读、幻读。</li></ul><p>其中</p><ul><li>脏读 ：表示一个事务能够读取另一个事务中还未提交的数据。比如，某个事务尝试插入记录 A，此时该事务还未提交，然后另一个事务尝试读取到了记录 A。</li><li>不可重复读 ：是指在一个事务内，多次读同一数据。</li><li>幻读 ：指同一个事务内多次查询返回的结果集不一样。比如同一个事务 A 第一次查询时候有 n 条记录，但是第二次同等条件下查询却有 n+1 条记录，这就好像产生了幻觉。发生幻读的原因也是另外一个事务新增或者删除或者修改了第一个事务结果集里面的数据，同一个记录的数据内容被修改了，所有数据行的记录就变多或者变少了。</li></ul><h3 id="常用引擎"><a href="#常用引擎" class="headerlink" title="常用引擎"></a>常用引擎</h3><ul><li>InnoDB 引擎：InnoDB 引擎提供了对数据库 acid 事务的支持，并且还提供了行级锁和外键的约束，它的设计的目标就是处理大数据容量的数据库系统。MySQL 运行的时候，InnoDB 会在内存中建立缓冲池，用于缓冲数据和索引。但是该引擎是不支持全文搜索，同时启动也比较的慢，它是不会保存表的行数的，所以当进行 select count(*) from table 指令的时候，需要进行扫描全表。由于锁的粒度小，写操作是不会锁定全表的,所以在并发度较高的场景下使用会提升效率的。</li><li>MyIASM 引擎：MySQL 的默认引擎，但不提供事务的支持，也不支持行级锁和外键。因此当执行插入和更新语句时，即执行写操作的时候需要锁定这个表，所以会导致效率会降低。不过和 InnoDB 不同的是，MyIASM 引擎是保存了表的行数，于是当进行 select count(*) from table 语句时，可以直接的读取已经保存的值而不需要进行扫描全表。所以，如果表的读操作远远多于写操作时，并且不需要事务的支持的，可以将 MyIASM 作为数据库引擎的首选。</li></ul><h3 id="行锁和表锁"><a href="#行锁和表锁" class="headerlink" title="行锁和表锁"></a>行锁和表锁</h3><p>MyISAM 只支持表锁，InnoDB 支持表锁和行锁，默认为行锁。</p><ul><li>表级锁：开销小，加锁快，不会出现死锁。锁定粒度大，发生锁冲突的概率最高，并发量最低。</li><li>行级锁：开销大，加锁慢，会出现死锁。锁力度小，发生锁冲突的概率小，并发度最高。</li></ul><h3 id="mysql-问题排查都有哪些手段"><a href="#mysql-问题排查都有哪些手段" class="headerlink" title="mysql 问题排查都有哪些手段"></a>mysql 问题排查都有哪些手段</h3><ul><li>使用 show processlist 命令查看当前所有连接信息。</li><li>使用 explain 命令查询 SQL 语句执行计划。</li><li>开启慢查询日志，查看慢查询的 SQL。</li></ul><h3 id="如何做-mysql-的性能优化"><a href="#如何做-mysql-的性能优化" class="headerlink" title="如何做 mysql 的性能优化"></a>如何做 mysql 的性能优化</h3><ul><li>为搜索字段创建索引。</li><li>避免使用 select *，列出需要查询的字段。</li><li>垂直分割分表。</li><li>选择正确的存储引擎。</li></ul><h3 id="JDBC"><a href="#JDBC" class="headerlink" title="JDBC"></a>JDBC</h3><p><strong>说下原生 JDBC 操作数据库流程？</strong></p><ul><li>第一步：Class.forName() 加载数据库连接驱动；</li><li>第二步：DriverManager.getConnection() 获取数据连接对象;</li><li>第三步：根据 SQL 获取 sql 会话对象，有 2 种方式 Statement、PreparedStatement ;</li><li>第四步：执行 SQL，执行 SQL 前如果有参数值就设置参数值 setXXX();</li><li>第五步：处理结果集；</li><li>第六步：关闭结果集、关闭会话、关闭连接。</li></ul><h2 id="Redis"><a href="#Redis" class="headerlink" title="Redis"></a>Redis</h2><h3 id="是什么"><a href="#是什么" class="headerlink" title="是什么"></a>是什么</h3><p>Redis是一个开源的使用ANSI C语言编写、支持网络、可基于内存亦可持久化的日志型、Key-Value数据库，并提供多种语言的API。</p><h3 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h3><ul><li>数据高并发的读写</li><li>海量数据的读写</li><li>对扩展性要求高的数据</li></ul><h3 id="功能"><a href="#功能" class="headerlink" title="功能"></a>功能</h3><ul><li>数据缓存功能</li><li>分布式锁的功能</li><li>支持数据持久化</li><li>支持事务</li><li>支持消息队列</li></ul><h3 id="缓存穿透"><a href="#缓存穿透" class="headerlink" title="缓存穿透"></a>缓存穿透</h3><ul><li>缓存穿透：指查询一个一定不存在的数据，由于缓存是不命中时需要从数据库查询，查不到数据则不写入缓存，这将导致这个不存在的数据每次请求都要到数据库去查询，造成缓存穿透。</li><li>解决方案：最简单粗暴的方法如果一个查询返回的数据为空（不管是数据不存在，还是系统故障），我们就把这个空结果进行缓存，但它的过期时间会很短，最长不超过五分钟。</li></ul><h3 id="支持的数据类型"><a href="#支持的数据类型" class="headerlink" title="支持的数据类型"></a>支持的数据类型</h3><p>string、list、hash、set、zset。</p><h3 id="支持的-java-客户端"><a href="#支持的-java-客户端" class="headerlink" title="支持的 java 客户端"></a>支持的 java 客户端</h3><p>Redisson、Jedis、lettuce等等，官方推荐使用Redisson。</p><h3 id="jedis-和-redisson"><a href="#jedis-和-redisson" class="headerlink" title="jedis 和 redisson"></a>jedis 和 redisson</h3><ul><li>Jedis是Redis的Java实现的客户端，其API提供了比较全面的Redis命令的支持。</li><li>Redisson实现了分布式和可扩展的Java数据结构，和Jedis相比，功能较为简单，不支持字符串操作，不支持排序、事务、管道、分区等Redis特性。Redisson的宗旨是促进使用者对Redis的关注分离，从而让使用者能够将精力更集中地放在处理业务逻辑上。</li></ul><h3 id="怎么保证缓存和数据库数据的一致性"><a href="#怎么保证缓存和数据库数据的一致性" class="headerlink" title="怎么保证缓存和数据库数据的一致性"></a>怎么保证缓存和数据库数据的一致性</h3><ul><li>合理设置缓存的过期时间。</li><li>新增、更改、删除数据库操作时同步更新 Redis，可以使用事物机制来保证数据的一致性。</li></ul><h3 id="持久化"><a href="#持久化" class="headerlink" title="持久化"></a>持久化</h3><ul><li>RDB（Redis Database）：指定的时间间隔能对你的数据进行快照存储。<ul><li>优点：节省磁盘空间，恢复速度快</li><li>缺点：数据庞大时比较消耗性能，Redis 意外 down 掉的话，可能会丢失最后一次快照的所有修改</li></ul></li><li>AOF（Append Only File）：每一个收到的写命令都通过write函数追加到文件中。<ul><li>优点：备份机制更稳健，丢失数据概率较低；可读的日志文本，可以处理误操作</li><li>缺点：占用更多的空间；恢复备份速度慢；</li></ul></li></ul><h3 id="redis-怎么实现分布式锁"><a href="#redis-怎么实现分布式锁" class="headerlink" title="redis 怎么实现分布式锁"></a>redis 怎么实现分布式锁</h3><ul><li>Redis 分布式锁其实就是在系统里面占一个“坑”，其他程序也要占“坑”的时候，占用成功了就可以继续执行，失败了就只能放弃或稍后重试。</li><li>占坑一般使用 setnx(set if not exists)指令，只允许被一个程序占有，使用完调用 del 释放锁。</li></ul><h3 id="redis-分布式锁有什么缺陷"><a href="#redis-分布式锁有什么缺陷" class="headerlink" title="redis 分布式锁有什么缺陷"></a>redis 分布式锁有什么缺陷</h3><p>Redis 分布式锁不能解决超时的问题，分布式锁有一个超时时间，程序的执行如果超出了锁的超时时间就会出现问题。</p><h3 id="redis-如何做内存优化"><a href="#redis-如何做内存优化" class="headerlink" title="redis 如何做内存优化"></a>redis 如何做内存优化</h3><ul><li>尽可能使用散列表（hashes），散列表（是说散列表里面存储的数少）使用的内存非常小，所以你应该尽可能的将你的数据模型抽象到一个散列表里面。</li><li>比如你的web系统中有一个用户对象，不要为这个用户的名称，姓氏，邮箱，密码设置单独的key,而是应该把这个用户的所有信息存储到一张散列表里面。</li></ul><h2 id="Spring-Boot-Spring-Cloud"><a href="#Spring-Boot-Spring-Cloud" class="headerlink" title="Spring Boot/Spring Cloud"></a>Spring Boot/Spring Cloud</h2><h3 id="什么是-spring-boot"><a href="#什么是-spring-boot" class="headerlink" title="什么是 spring boot"></a>什么是 spring boot</h3><p>SpringBoot是一个框架，一种全新的编程规范，他的产生简化了框架的使用，所谓简化是指简化了Spring众多框架中所需的大量且繁琐的配置文件，所以 SpringBoot是一个服务于框架的框架，服务范围是简化配置文件。</p><h3 id="为什么要用-spring-boot"><a href="#为什么要用-spring-boot" class="headerlink" title="为什么要用 spring boot"></a>为什么要用 spring boot</h3><ul><li>Spring Boot使编码变简单</li><li>Spring Boot使配置变简单</li><li>Spring Boot使部署变简单</li><li>Spring Boot使监控变简单</li></ul><h3 id="什么是-spring-cloud"><a href="#什么是-spring-cloud" class="headerlink" title="什么是 spring cloud"></a>什么是 spring cloud</h3><p>从字面理解，Spring Cloud 就是致力于分布式系统、云服务的框架用于对微服务进行治理。</p><p>使用 Spring Cloud 开发人员可以开箱即用的实现这些模式的服务和应用程序。这些服务可以任何环境下运行，包括分布式环境，也包括开发人员自己的笔记本电脑以及各种托管平台。</p><h3 id="spring-cloud-断路器的作用是什么"><a href="#spring-cloud-断路器的作用是什么" class="headerlink" title="spring cloud 断路器的作用是什么"></a>spring cloud 断路器的作用是什么</h3><p>在Spring Cloud中使用了Hystrix 来实现断路器的功能，断路器可以防止一个应用程序多次试图执行一个操作，即很可能失败，允许它继续而不等待故障恢复或者浪费 CPU 周期，而它确定该故障是持久的。断路器模式也使应用程序能够检测故障是否已经解决，如果问题似乎已经得到纠正，应用程序可以尝试调用操作。</p><p>断路器增加了稳定性和灵活性，以一个系统，提供稳定性，而系统从故障中恢复，并尽量减少此故障的对性能的影响。它可以帮助快速地拒绝对一个操作，即很可能失败，而不是等待操作超时（或者不返回）的请求，以保持系统的响应时间。如果断路器提高每次改变状态的时间的事件，该信息可以被用来监测由断路器保护系统的部件的健康状况，或以提醒管理员当断路器跳闸，以在打开状态。</p><h3 id="spring-cloud-的核心组件"><a href="#spring-cloud-的核心组件" class="headerlink" title="spring cloud 的核心组件"></a>spring cloud 的核心组件</h3><ul><li>①. 服务发现——Netflix Eureka<br>一个RESTful服务，用来定位运行在AWS地区（Region）中的中间层服务。由两个组件组成：Eureka服务器和Eureka客户端。Eureka服务器用作服务注册服务器。Eureka客户端是一个java客户端，用来简化与服务器的交互、作为轮询负载均衡器，并提供服务的故障切换支持。Netflix在其生产环境中使用的是另外的客户端，它提供基于流量、资源利用率以及出错状态的加权负载均衡。</li><li>②. 客服端负载均衡——Netflix Ribbon<br>Ribbon，主要提供客户侧的软件负载均衡算法。Ribbon客户端组件提供一系列完善的配置选项，比如连接超时、重试、重试算法等。Ribbon内置可插拔、可定制的负载均衡组件。</li><li>③. 断路器——Netflix Hystrix<br>断路器可以防止一个应用程序多次试图执行一个操作，即很可能失败，允许它继续而不等待故障恢复或者浪费 CPU 周期，而它确定该故障是持久的。断路器模式也使应用程序能够检测故障是否已经解决。如果问题似乎已经得到纠正，应用程序可以尝试调用操作。</li><li>④. 服务网关——Netflix Zuul<br>类似nginx，反向代理的功能，不过netflix自己增加了一些配合其他组件的特性。</li><li>⑤. 分布式配置——Spring Cloud Config<br>这个还是静态的，得配合Spring Cloud Bus实现动态的配置更新。</li></ul><h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><h3 id="自我介绍"><a href="#自我介绍" class="headerlink" title="自我介绍"></a>自我介绍</h3><ul><li>面试官好，我叫 XXX，来自 XXXX 计算机系</li><li>这一次我应聘的是 Java 开发的工作，虽然我是网络工程专业的，但是我对软件开发一直有浓厚的兴趣</li><li>为此，在大学阶段我就结合 Java 开发岗位的要求进行了准备，选修了许多 Java 方面的知识</li><li>同时，我也自学了一些当前比较流行的技术，如 Spring Boot、Spring Cloud 等。</li><li>希望能加入贵公司，从事 Java 开发的工作，谢谢</li></ul><h3 id="薪资"><a href="#薪资" class="headerlink" title="薪资"></a>薪资</h3><p><strong>如果我们给你的工资，没有达到简历上的要求，你还会来吗？</strong></p><ul><li>相比于工资，公司的选择对我来说是更重要的，我更看重公司的发展前景和我在公司的发展平台。</li><li>在我这个年纪呢，我觉得前途比薪水更重要。</li><li>再说了，我相信只要我的能力达到贵公司的要求，你们不会给我比别人低的工资。</li></ul>]]></content>
      <categories>
        <category>题库</category>
      </categories>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>Python 基础</title>
    <url>/python/4081a847-b578-4e2f-97df-b6e03bab216f.html</url>
    <content><![CDATA[<p>Python是一种跨平台的计算机程序设计语言。 是一个高层次的结合了解释性、编译性、互动性和面向对象的脚本语言。最初被设计用于编写自动化脚本(shell)，随着版本的不断更新和语言新功能的添加，越多被用于独立的、大型项目的开发。本文介绍 Python 的基础内容。</p><a id="more"></a><h2 id="解释器"><a href="#解释器" class="headerlink" title="解释器"></a>解释器</h2><blockquote><p>计算机不能直接理解任何除机器语言以外的语言，所以必须要把程序员所写的程序语言翻译成机器语言，计算机才能执行程序。<br>将其他语言翻译成机器语言的工具，被称为编译器。<br>编译器翻译的方式有两种：一个是编译，另外一个是解释。<br>两种方式之间的区别在于翻译时间点的不同。当编译器以解释方式运行的时候，也称之为解释器。</p></blockquote><p>编译型和解释型语言工作对比：</p><p><img src="https://note.youdao.com/yws/public/resource/2c8aa2e8642b2832d32ab9f8ee20ccf1/xmlnote/6128448D6F90409082B23D7D55DCCFB5/139" alt=""></p><ul><li><p><strong>编译型语言</strong><br>程序在执行之前需要一个专门的编译过程。把程序编译成为机器语言的文件，运行时不需要重新翻译，直接使用编译的结果就行了。程序执行效率高，依赖编译器，跨平台性差些。如 C、C++ 等。</p></li><li><p><strong>解释型语言</strong><br>解释型语言编写的程序不进行预先编译，以文本方式存储程序代码，会将代码一句一句直接运行。在发布程序时，看起来省了道编译工序，但是在运行程序的时候，必须先解释再运行。</p></li></ul><h2 id="Python-程序"><a href="#Python-程序" class="headerlink" title="Python 程序"></a>Python 程序</h2><p>Python 程序的文件扩展名通常都是 <strong><code>.py</code></strong></p><h3 id="交互式-Python"><a href="#交互式-Python" class="headerlink" title="交互式 Python"></a>交互式 Python</h3><blockquote><p>交互式 Python 是直接在终端中运行解释器，而不输入要执行的文件名。</p></blockquote><p>在 Linux 中直接输入 <strong><code>python</code></strong> 命令即可进入 Python 的交互式界面，然后输入 Python 的代码，会立即看到程序执行结果。在交互式界面中输入 <strong><code>exit()</code></strong> 即可退出，或者按 <strong>Ctrl+D</strong> 也能退出。</p><h3 id="注释"><a href="#注释" class="headerlink" title="注释"></a>注释</h3><p>注释的作用：使用用自己熟悉的语言，在程序中对某些代码进行标注说明，增强程序的可读性<br><strong>单行注释</strong>：在Python中以 “<strong>#</strong>“ 号开头的文字都是注释。<br><strong>多行注释</strong>：如果希望编写的注释信息很多，一行无法显示，就可以使用多行注释。<br>要在Python程序中使用多行注释，可以用三对连续的引号(单引号和双引号都可以)，如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&quot;&quot;&quot;</span><br><span class="line">多行注释...</span><br><span class="line">&quot;&quot;&quot;</span><br></pre></td></tr></table></figure><blockquote><p>注意：这里的多行注释实际上是字符串，是要占用内存空间的，只不过没有让变量接收它罢了。</p></blockquote><p><strong>注意</strong>：Python 2.x默认是 <strong>不支持</strong> 中文的，而Python 3.x是默认支持中文的。<br>如果要使用python 2.x的版本支持中文，则要在Python文件的开头加上 <code>#encoding=utf-8</code> 即可让Python2.x支持中文<br>标准写法应该是 <code>#-*- coding:utf-8 -*-</code> ，当然也可以写成 <code>#coding=utf-8</code></p><h3 id="运算符"><a href="#运算符" class="headerlink" title="运算符"></a>运算符</h3><blockquote><p>Python中的运算符分为算术运算符、赋值运算符、复合赋值运算符<br>算数运算符是运算符的一种，是完成基本的算术运算使用的符号，用来处理四则运算。</p></blockquote><table><thead><tr><th align="center">运算符</th><th>描述</th></tr></thead><tbody><tr><td align="center">+</td><td>加法</td></tr><tr><td align="center">-</td><td>减法</td></tr><tr><td align="center">*</td><td>乘法</td></tr><tr><td align="center">/</td><td>除法</td></tr><tr><td align="center">//</td><td>取整除，返回除法的整数部分</td></tr><tr><td align="center">%</td><td>取余</td></tr><tr><td align="center">**</td><td>幂运算</td></tr></tbody></table><p>在Python中， <strong>“ * “</strong> 运算符还可以用于字符串，计算结果就是字符串重复指定次数的结果<br>如:</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">In [<span class="number">1</span>]: <span class="string">"-"</span> * <span class="number">5</span></span><br><span class="line">Out[<span class="number">1</span>]: <span class="string">'-----'</span></span><br></pre></td></tr></table></figure><p>算术运算符的优先级和数学中的运算符的优先级一致<br>先乘除后加减、同级运算符是从左至右计算、可以使用括号()调整计算的优先级<br>以下表格的算数优先级有高到低顺序排列</p><table><thead><tr><th align="center">运算符</th><th>描述</th></tr></thead><tbody><tr><td align="center">**</td><td>幂（最高优先级）</td></tr><tr><td align="center">*、/、%、//</td><td>乘、除、取余、取整除</td></tr><tr><td align="center">+、-</td><td>加、减</td></tr></tbody></table><p>赋值运算符 <strong>=</strong> 是用来将一个值赋给其他的变量</p><p>复合赋值运算符也是运算符的一种，但是这些运算符还能进行其他的操作</p><table><thead><tr><th align="center">运算符</th><th>描述</th></tr></thead><tbody><tr><td align="center">+=</td><td>加法赋值运算符</td></tr><tr><td align="center">-=</td><td>减法赋值运算符</td></tr><tr><td align="center">*=</td><td>乘法赋值运算符</td></tr><tr><td align="center">/=</td><td>除法赋值运算符</td></tr><tr><td align="center">%=</td><td>取模赋值运算符</td></tr><tr><td align="center">**=</td><td>幂赋值运算符</td></tr><tr><td align="center">//=</td><td>取整除赋值运算符</td></tr></tbody></table><h2 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h2><blockquote><p>程序用来操作数据，而变量是用来存储数据</p></blockquote><h3 id="变量定义"><a href="#变量定义" class="headerlink" title="变量定义"></a>变量定义</h3><blockquote><p>Python中，每个变量在使用前都必须赋值，变量赋值以后该变量才会被创建。<br>等号 <strong>=</strong> 用来给变量赋值，等号左边是一个变量名，等号右边是存储在变量中的值。</p></blockquote><p>因此定义变量的方法：<strong>变量名 = 值</strong><br>如果想要数据变量的内容，用print(NAME)即可打印出来变量的内容<br>如果在交互式界面，直接输入变量名回车即可打印变量内容<br><strong>案例</strong>：<br>苹果的价格是8.5元/斤，买了7.5斤苹果，计算付款金额<br>代码如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 定义苹果单价</span></span><br><span class="line">price = <span class="number">8.5</span> </span><br><span class="line"><span class="comment"># 挑选苹果</span></span><br><span class="line">weight = <span class="number">7.5</span> </span><br><span class="line"><span class="comment"># 计算付款金额</span></span><br><span class="line">money = weight * price</span><br><span class="line">print(money)</span><br></pre></td></tr></table></figure><h3 id="变量类型"><a href="#变量类型" class="headerlink" title="变量类型"></a>变量类型</h3><p>在内存中创建一个变量，会包括：<br>1.变量的名称<br>2.变量保存的数据<br>3.变量存储数据的类型<br>4.变量的地址</p><blockquote><p>在Python中，定义变量时是不需要指定变量的类型的<br>Python解释器运行的时候，会自动根据赋值语句等号右侧的数据来自动推导出变量中保存数据的准确类型<br>变量类型分为数字型和非数字型<br>其中数字型有: 整型(int)、浮点型(float)、布尔型(bool)<br>非数字型有: 字符串(string)、列表、元组、字典等</p></blockquote><p>如果想要查看某个变量的类型，在交互式界面下可以使用 <strong>type(NAME)</strong> 函数来查看<br>在Python中，数字型变量之间是可以直接计算的<br>如果变量是bool类型的在计算时True对应的值为1，False对应的值为0<br>字符串的计算：字符串之间可以使用”+”号拼接生成新的字符串，也可以使用”*”号来重复拼接相同的字符串。<br><strong>数字型变量和字符串之间不能进行其他计算</strong></p><h3 id="变量的输入"><a href="#变量的输入" class="headerlink" title="变量的输入"></a>变量的输入</h3><p>所谓输入，就是用代码获取用户通过键盘输入的信息<br>要通过变量读取键盘上的信息，需要用到 <strong>input</strong> 函数<br>用法：<code>input(&quot;提示信息&quot;)</code><br>该函数的返回值就是从键盘读取到的内容，可以用某个变量来接收该值，如:<br><code>VALUE = input()</code><br><strong>用户输入的任何内容该函数都会返回字符串（Python3中）</strong><br><strong>Python 2 和Python 3的input是有区别的：</strong><br>Python2输入的时候会将输入的结果当做命令来看待<br>而Python3的输入结果就是字符串，Python3中的input就相当于Python2中的raw_input</p><h3 id="变量的转换"><a href="#变量的转换" class="headerlink" title="变量的转换"></a>变量的转换</h3><p>Python中的变量之间可以进行变量转换，需要用到类型转换函数<br>数据类型转换的函数：</p><table><thead><tr><th align="center">函数</th><th>说明</th></tr></thead><tbody><tr><td align="center">int(x)</td><td>将x转换成整型变量</td></tr><tr><td align="center">long(x)</td><td>将x转换成长整型变量</td></tr><tr><td align="center">float(x)</td><td>将x转换成字符型变量</td></tr><tr><td align="center">str(x)</td><td>将x转换成字符串</td></tr><tr><td align="center">repr(x)</td><td>将对象x转换成表达式字符串</td></tr><tr><td align="center">eval(str)</td><td>计算字符串x中的有效表达式，返回计算后的结果</td></tr><tr><td align="center">tuple(x)</td><td>将序列x转换成元组</td></tr><tr><td align="center">list(x)</td><td>将序列x转换成列表</td></tr><tr><td align="center">chr(x)</td><td>将整数x转换成字符</td></tr><tr><td align="center">ord(x)</td><td>将字符x转换成整数值</td></tr><tr><td align="center">hex(x)</td><td>将整数x转换成十六进制字符串</td></tr><tr><td align="center">oct(x)</td><td>将整数x转换成八进制字符串</td></tr></tbody></table><h3 id="变量的输出"><a href="#变量的输出" class="headerlink" title="变量的输出"></a>变量的输出</h3><blockquote><p>在Python中可以使用print函数将信息输出到控制台<br>如果希望输出文字信息的同时，一起输出数据，就需要使用到格式化操作符<br>“%”被称为格式化操作符，专门用于处理字符串中的格式<br>包含”%”的字符串，被称为格式化字符串，”%”和不同的字符连用，不同类型的数据需要使用不同的格式化字符</p></blockquote><table><thead><tr><th align="center">格式符号</th><th>转换</th></tr></thead><tbody><tr><td align="center">%c</td><td>字符</td></tr><tr><td align="center">%s</td><td>字符串</td></tr><tr><td align="center">%d</td><td>有符号十进制整数</td></tr><tr><td align="center">%u</td><td>无符号十进制整数</td></tr><tr><td align="center">%o</td><td>八进制整数</td></tr><tr><td align="center">%x</td><td>十六进制整数(小写字母)</td></tr><tr><td align="center">%Y</td><td>十六进制整数(大写字母)</td></tr><tr><td align="center">%f</td><td>浮点数</td></tr></tbody></table><p>语法格式如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">print(<span class="string">"格式化字符串"</span> % 变量)</span><br><span class="line">print(<span class="string">"格式化字符串"</span> % (变量<span class="number">1</span>, 变量<span class="number">2</span>, ...))</span><br></pre></td></tr></table></figure><p>和C语言一样，\n也能换行输出<br>print案例:<br>需求如下：<br>1.定义字符串变量name，输出我的名字叫小明，请多多关照！<br>2.定义整数变量student_no，输出我的学号是000001<br>3.定义小数price、weight、money，输出苹果单价9.00元／斤，购买了5.00斤，需要支付45.00元<br>4.定义一个小数scale，输出数据比例是80.00%<br>代码：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 定义字符串变量 name，输出 我的名字叫 小明，请多多关照！</span></span><br><span class="line">name = <span class="string">"大小明"</span></span><br><span class="line">print(<span class="string">"我的名字叫 %s，请多多关照！"</span> % name)</span><br><span class="line"><span class="comment"># 定义整数变量 student_no，输出 我的学号是 000001</span></span><br><span class="line">student_no = <span class="number">100123456</span></span><br><span class="line">print(<span class="string">"我的学号是 %06d"</span> % student_no)</span><br><span class="line"><span class="comment"># 定义小数 price、weight、money，</span></span><br><span class="line"><span class="comment"># 输出 苹果单价 9.00 元／斤，购买了 5.00 斤，需要支付 45.00 元</span></span><br><span class="line">price = <span class="number">8.5</span> </span><br><span class="line">weight = <span class="number">7.5</span> </span><br><span class="line">money = price * weight</span><br><span class="line">print(<span class="string">"苹果单价%.2f元/斤，购买了%.3f斤，需要支付%.4f元"</span> % (price, weight, money))</span><br><span class="line"><span class="comment"># 定义一个小数 scale，输出 数据比例是 80.00%</span></span><br><span class="line">scale = <span class="number">0.8</span> </span><br><span class="line">print(<span class="string">"数据比例是 %.2f%%"</span> % (scale * <span class="number">100</span>))</span><br></pre></td></tr></table></figure><p><strong>用print函数打印到屏幕的时候默认是换行的</strong><br>如果想让它不换行，可以这样：<code>print(&quot;STRING&quot;, end=&quot;&quot;)</code></p><h3 id="变量的命名"><a href="#变量的命名" class="headerlink" title="变量的命名"></a>变量的命名</h3><blockquote><p>标识符：程序员定义的变量名和函数名就是标识符。<br>标识符可以由字母、下划线和数字组成，并且不能以数字开头，标识符不能与关键字重名<br>关键字就是在Python内部已经使用的标识符，具有特定的功能和含义<br>可以通过以下命令来查看Python中定义的关键字：<br>In[1]:import keyword<br>In[2]:print(keyword.kwlist)</p></blockquote><p>Python推荐的命名规则：</p><table><thead><tr><th>Type</th><th>Name</th></tr></thead><tbody><tr><td>Modules</td><td>lower_with_under</td></tr><tr><td>Packages</td><td>lower_with_under</td></tr><tr><td>Classes</td><td>CapWords</td></tr><tr><td>Exceptions</td><td>CapWords</td></tr><tr><td>Functions</td><td>lower_with_under()</td></tr><tr><td>Global/Class Constants</td><td>CAPS_WITH_UNDER</td></tr><tr><td>Global/Class Variables</td><td>lower_with_under</td></tr><tr><td>Instance Variables</td><td>lower_with_under</td></tr><tr><td>Method Names</td><td>lower_with_under()</td></tr><tr><td>Function/Method Parameters</td><td>lower_with_under</td></tr><tr><td>Local Variables</td><td>lower_with_under</td></tr></tbody></table><h3 id="字符串"><a href="#字符串" class="headerlink" title="字符串"></a>字符串</h3><blockquote><p>在Python中，双引号或者单引号中的数据就是字符串<br>字符串的格式化输出可以用%s来进行输出<br>另外，input函数接收的值就是字符串<br>还可以用str()函数将其他类型的变量转换为字符串类型的变量</p></blockquote><p>字符串的下标和C语言的一样，第一个字符下标为0<br>不同的是STR[-1]表示字符串的倒数第一个字符，STR[-2]表示字符串倒数第二个字符，以此类推</p><p>Python还支持字符串切片，所谓的切片就是指对操作的对象截取其中一部分的操作。<br><strong>字符串、列表、元组</strong>都支持切片操作<br>字符串切片语法：<code>STR[起始位置:结束位置:步长]</code><br><strong>切片不包括结束位置的那个字符</strong><br><strong>还有，两个位置如果省略的话，Python解释器会自动根据步长来查找最终位置，步长省略默认为1</strong><br>因此可以用STR[::-1]来进行字符串的翻转，当然也可以写成STR[-1::-1]</p><p>字符串常用方法：</p><ul><li><strong>find/rfind</strong><br>find函数用于检测某个子串是否包含在字符串中，如果是就返回第一次找到的开始的索引值，否则返回-1<br><code>STRING.find(str[, start, end])</code><br>find函数中的start和end分别是字符串的其实和结束位置，可以指定从某个位置开始查找<br>find是从左往右找，而rfind是从右往左找</li><li><strong>index/rindex</strong><br>用法和find函数一样，只不过如果没找到就会报错<br><code>STRING.index(str[, start, end])</code><br>index是从左往右找，而rindex是从右往左找</li><li><strong>count</strong><br>此函数返回子串str在STRING中的start和end之间里出现的次数<br><code>STRING.count(str, start, end)</code></li><li><strong>replace</strong><br>此函数将STRING中的str1替换成str2，如果count指定，则替换不超过count次<br><code>STRING.replace(str1, str2[, STRING.count(str1)])</code><br>注意：该函数不能直接修改字符串，而是将修改过的值返回出来，字符串本身是没有改变的</li><li><strong>split/rsplit</strong><br>以str为分隔符切片STRING，如果指定maxsplit的值则仅分隔maxsplit个字符串，返回一个列表<br><code>STRING.split([sep=str, maxsplit=NUM])</code><br>split中的sep默认为空格，maxsplit默认为-1，就是切完所有的<br>split是从左往右，rsplit是从右往左</li><li><strong>capitalize</strong><br>把字符串的第一个字母大写<br><code>STRING.capitalize()</code></li><li><strong>title</strong><br>把字符串的每个单词首字母大写</li><li><strong>startswith/endswith</strong><br>判断字符串STRING是否以str开头/结尾，是则返回True，否则返回False<br><code>STRING.startwith(str)</code><br><code>STRING.endswith(str)</code></li><li><strong>lower/upper</strong><br>将STRING中的所有字母改为大写/小写<br><code>STRING.lower()</code><br><code>STRING.upper</code></li><li><strong>ljust/rjust/center</strong><br>将一个字符串STRING左对齐/右对齐/居中，并使用空格将字符串填充至width长度<br><code>STRING.ljust/rjust/center(width)</code></li><li><strong>lstrip/rstrip/strip</strong><br>删除字符串STRING左边/右边/两边的空白字符<br><code>STRING.lstrip/rstrip/strip()</code></li><li><strong>partition/rpartition</strong><br>将STRING以str分割成3个部分，返回元组<br><code>STRING.[r]partition(str)</code><br>r是从右往左找到第一个str</li><li><strong>splitlines</strong><br>按照行分隔，返回一个包含各行作为元素的列表</li><li><strong>isalpha/isdigit/isalnum/isspace</strong><br>如果字符串STRING只包含字母/数字/字母和数字/空格，则返回True，否则返回False<br><code>STRING.isalpha/digit/alnum/space()</code></li><li><strong>join</strong><br>将某个列表list中的元素以STRING为分隔符组成一个新的字符串<br>如：<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>string=<span class="string">'-'</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>list = [<span class="string">'hello'</span>, <span class="string">'world'</span>, <span class="string">'my'</span>, <span class="string">'love'</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>string.join(list)</span><br><span class="line"><span class="string">'hello-world-my-love'</span></span><br></pre></td></tr></table></figure></li></ul><h3 id="列表"><a href="#列表" class="headerlink" title="列表"></a>列表</h3><p>列表和C语言中的数组类似，但是其比C语言数组强大的地方在于列表中的元素可以是不同类型的<br>如：<code>list = [1, &quot;abcd&quot;, 1.2, False]</code><br>要想得到列表中的某个元素，可以使用下标来进行操作<br>如list[0]代表第一个元素1，list[1]代表第二个元素”abcd”，依此类推</p><ul><li><p><strong>添加元素</strong><br>列表添加元素有3种方法：<strong>append、extend、insert</strong><br>append函数是在列表后面追加元素<br>extend可以将另一个列表中的元素逐一追加到列表中<br>insert用于在指定位置插入元素<br>案例代码如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">A = [<span class="string">"xiaoZhao"</span>, <span class="string">"xiaoQian"</span>, <span class="string">"xiaoSun"</span>, <span class="string">"xiaoLi"</span>]</span><br><span class="line">B = [<span class="string">"GongSun"</span>, <span class="string">"OuYang"</span>, <span class="string">"WuKong"</span>]</span><br><span class="line"></span><br><span class="line">print(<span class="string">"列表元素如下："</span>)</span><br><span class="line"><span class="keyword">for</span> temp <span class="keyword">in</span> A:</span><br><span class="line">   print(<span class="string">"%s  "</span> %temp, end=<span class="string">""</span>)</span><br><span class="line">print()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 添加元素</span></span><br><span class="line">temp = input(<span class="string">"请输入要添加的名字："</span>)</span><br><span class="line">A.append(temp)</span><br><span class="line"></span><br><span class="line">print(<span class="string">"现有另一个元素如下："</span>)</span><br><span class="line"><span class="keyword">for</span> temp <span class="keyword">in</span> B:</span><br><span class="line">   print(<span class="string">"%s  "</span> %temp, end=<span class="string">""</span>)</span><br><span class="line">print(<span class="string">"\n将此列表添加值第一个列表中之后："</span>)</span><br><span class="line">A.extend(B)</span><br><span class="line"></span><br><span class="line">print(<span class="string">"扩展之后列表元素如下："</span>)</span><br><span class="line"><span class="keyword">for</span> temp <span class="keyword">in</span> A:</span><br><span class="line">   print(<span class="string">"%s  "</span> %temp, end=<span class="string">""</span>)</span><br><span class="line">print()</span><br><span class="line"></span><br><span class="line">index = int(input(<span class="string">"请输入想要插入元素的位置："</span>))</span><br><span class="line">obj = input(<span class="string">"请输入要插入元素的值："</span>)</span><br><span class="line">A.insert(index, obj)</span><br><span class="line">print(<span class="string">"插入之后列表元素如下："</span>)</span><br><span class="line"><span class="keyword">for</span> temp <span class="keyword">in</span> A:</span><br><span class="line">   print(<span class="string">"%s  "</span> %temp, end=<span class="string">""</span>)</span><br><span class="line">print()</span><br></pre></td></tr></table></figure></li><li><p><strong>修改元素</strong><br>列表修改元素没有什么花里胡哨的函数，直接通过下标来修改元素值即可<br>案例如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">A = [<span class="string">"xiaoZhao"</span>, <span class="string">"xiaoQian"</span>, <span class="string">"xiaoSun"</span>, <span class="string">"xiaoLi"</span>]</span><br><span class="line">print(<span class="string">"---- 修改之前 ----"</span>)</span><br><span class="line"><span class="keyword">for</span> temp <span class="keyword">in</span> A:</span><br><span class="line">    print(<span class="string">"%s  "</span> %temp, end=<span class="string">""</span>)</span><br><span class="line">print()</span><br><span class="line"></span><br><span class="line">A[<span class="number">1</span>] = <span class="string">"xiaoHu"</span></span><br><span class="line">print(<span class="string">"%s"</span> %(<span class="string">"-"</span>*<span class="number">20</span>))</span><br><span class="line">print(<span class="string">"修改了1号位置的元素"</span>)</span><br><span class="line">print(<span class="string">"%s"</span> %(<span class="string">"-"</span>*<span class="number">20</span>))</span><br><span class="line"></span><br><span class="line">print(<span class="string">"---- 修改之后 ----"</span>)</span><br><span class="line"><span class="keyword">for</span> temp <span class="keyword">in</span> A:</span><br><span class="line">    print(<span class="string">"%s  "</span> %temp, end=<span class="string">""</span>)</span><br><span class="line">print()</span><br></pre></td></tr></table></figure></li><li><p><strong>删除元素</strong><br>要想删除列表中的某个元素，可以使用<strong>del、pop、remove</strong>函数<br>其中del函数是根据列表下标进行删除，pop是删除最后一个元素（也就是弹出元素），remove是根据元素的值进行删除<br>案例如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">A = [<span class="string">"xiaoZhao"</span>, <span class="string">"xiaoQian"</span>, <span class="string">"xiaoSun"</span>, <span class="string">"xiaoLi"</span>, </span><br><span class="line">    <span class="string">"xiaoZhou"</span>, <span class="string">"xiaoWu"</span>, <span class="string">"xiaoZhen"</span>, <span class="string">"xiaoWang"</span>]</span><br><span class="line">print(<span class="string">"删除之前的元素: "</span>)</span><br><span class="line"><span class="keyword">for</span> temp <span class="keyword">in</span> A:</span><br><span class="line">    print(<span class="string">"%s  "</span> %temp, end=<span class="string">""</span>)</span><br><span class="line">print(<span class="string">"\n%s"</span> %(<span class="string">"-"</span>*<span class="number">10</span>))</span><br><span class="line"></span><br><span class="line"><span class="keyword">del</span> A[<span class="number">2</span>]</span><br><span class="line">print(<span class="string">"删除了2号元素\n"</span>)</span><br><span class="line"></span><br><span class="line">print(<span class="string">"删除之后的元素: "</span>)</span><br><span class="line"><span class="keyword">for</span> temp <span class="keyword">in</span> A:</span><br><span class="line">    print(<span class="string">"%s  "</span> %temp, end=<span class="string">""</span>)</span><br><span class="line">print(<span class="string">"\n%s"</span> %(<span class="string">"-"</span>*<span class="number">10</span>))</span><br><span class="line"></span><br><span class="line">A.pop()</span><br><span class="line">print(<span class="string">"弹出了最后一个元素\n"</span>)</span><br><span class="line">print(<span class="string">"弹出之后的元素: "</span>)</span><br><span class="line"><span class="keyword">for</span> temp <span class="keyword">in</span> A:</span><br><span class="line">    print(<span class="string">"%s  "</span> %temp, end=<span class="string">""</span>)</span><br><span class="line">print(<span class="string">"\n%s"</span> %(<span class="string">"-"</span>*<span class="number">10</span>))</span><br><span class="line"></span><br><span class="line">A.remove(<span class="string">"xiaoZhou"</span>)</span><br><span class="line">print(<span class="string">"删除了xiaoZhou\n"</span>)</span><br><span class="line">print(<span class="string">"删除之后的元素: "</span>)</span><br><span class="line"><span class="keyword">for</span> temp <span class="keyword">in</span> A:</span><br><span class="line">    print(<span class="string">"%s  "</span> %temp, end=<span class="string">""</span>)</span><br><span class="line">print(<span class="string">"\n%s"</span> %(<span class="string">"-"</span>*<span class="number">10</span>))</span><br><span class="line"><span class="comment"># ---- py ----</span></span><br></pre></td></tr></table></figure><p><strong>注意</strong>：在for循环如果用remove删除一个列表中的元素是会有坑的。如:</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> a:</span><br><span class="line">    <span class="keyword">if</span> i == <span class="number">1</span>:</span><br><span class="line">        a.remove(i)</span><br></pre></td></tr></table></figure><p>这样删除一个元素的时候列表中会去掉一个元素，如去掉a[2]，但是后面的元素仍然会补上<br>这样i还是指向的a[2]，也就是原来a[2]的后一个元素<br>在下次for的时候i就会跳到a[3]也就是原来的a[4]，中间就会漏掉一个元素<br>如果删除的元素是挨在一起的，那么后面的那个元素就删不掉。<br>解决方法：我们可以重新创建一个空列表来存储要删的元素，然后遍历那个列表来删除原来的列表<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">b = []</span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> a:</span><br><span class="line">    <span class="keyword">if</span> i == <span class="number">1</span>:</span><br><span class="line">        b.append(i)</span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> b:</span><br><span class="line">    a.remove(i)</span><br></pre></td></tr></table></figure><p>这样做就不会有坑，或者也可以用while循环来做。</p></li><li><p><strong>查找元素</strong><br>查找元素可以通过<strong>in或not in</strong>来查找，也能通过<strong>index、count函数</strong>来进行查找<br>其中in或not in是需要有if语句的配合进行查找<br><strong>index</strong>是给定一个范围，看这个范围内有没有值，如果找到，返回元素位置，没有找到会报错<br><strong>count</strong>计某个元素的数<br>案例如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">A = [<span class="string">'a'</span>, <span class="string">'b'</span>, <span class="string">'c'</span>, <span class="string">'a'</span>, <span class="string">'e'</span>]</span><br><span class="line">name = input(<span class="string">"请输入要找的字母："</span>)</span><br><span class="line"><span class="keyword">if</span> name <span class="keyword">in</span> A:</span><br><span class="line">    print(<span class="string">"找到字母，位置为："</span>, end=<span class="string">""</span>)</span><br><span class="line">    print(A.index(name, <span class="number">0</span>, <span class="number">5</span>)) </span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    print(<span class="string">"没有找到相应的字母"</span>)</span><br></pre></td></tr></table></figure></li><li><p><strong>排序</strong><br><strong>sort函数</strong>可以将列表按照特定的顺序进行重新排序，默认为由小到大<br>其中指定参数为<strong>lreverse=True</strong>可改为倒序，由大到小<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>, <span class="number">4</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">1</span>, <span class="number">4</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a.reverse()</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">3</span>, <span class="number">2</span>, <span class="number">4</span>, <span class="number">1</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a.sort()</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a.sort(reverse=<span class="literal">True</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">4</span>, <span class="number">3</span>, <span class="number">2</span>, <span class="number">1</span>]</span><br></pre></td></tr></table></figure><p>其中的reserve函数可以将列表翻转，同样也可以用切片的方法进行翻转，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">1</span>, <span class="number">3</span>, <span class="number">2</span>, <span class="number">4</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a[::<span class="number">-1</span>]</span><br><span class="line">[<span class="number">4</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">1</span>]</span><br></pre></td></tr></table></figure></li><li><p><strong>列表推导式</strong><br>所谓的列表推导式，就是指轻量级循环创建列表<br>range函数可以创建数列，用法为range([起始位置, ]结束位置[, 步长])<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [x <span class="keyword">for</span> x <span class="keyword">in</span> range(<span class="number">4</span>)]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [x <span class="keyword">for</span> x <span class="keyword">in</span> range(<span class="number">3</span>,<span class="number">4</span>)]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [x <span class="keyword">for</span> x <span class="keyword">in</span> range(<span class="number">3</span>,<span class="number">10</span>)]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>, <span class="number">7</span>, <span class="number">8</span>, <span class="number">9</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [x <span class="keyword">for</span> x <span class="keyword">in</span> range(<span class="number">3</span>, <span class="number">20</span>, <span class="number">2</span>)]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">3</span>, <span class="number">5</span>, <span class="number">7</span>, <span class="number">9</span>, <span class="number">11</span>, <span class="number">13</span>, <span class="number">15</span>, <span class="number">17</span>, <span class="number">19</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [x <span class="keyword">for</span> x <span class="keyword">in</span> range(<span class="number">3</span>,<span class="number">10</span>) <span class="keyword">if</span> x%<span class="number">2</span>==<span class="number">0</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">4</span>, <span class="number">6</span>, <span class="number">8</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [x <span class="keyword">for</span> x <span class="keyword">in</span> range(<span class="number">3</span>,<span class="number">10</span>) <span class="keyword">if</span> x%<span class="number">2</span>!=<span class="number">0</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">3</span>, <span class="number">5</span>, <span class="number">7</span>, <span class="number">9</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [(x,y) <span class="keyword">for</span> x <span class="keyword">in</span> range(<span class="number">1</span>,<span class="number">3</span>) <span class="keyword">for</span> y <span class="keyword">in</span> range(<span class="number">3</span>)]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[(<span class="number">1</span>, <span class="number">0</span>), (<span class="number">1</span>, <span class="number">1</span>), (<span class="number">1</span>, <span class="number">2</span>), (<span class="number">2</span>, <span class="number">0</span>), (<span class="number">2</span>, <span class="number">1</span>), (<span class="number">2</span>, <span class="number">2</span>)]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [(x,y,z) <span class="keyword">for</span> x <span class="keyword">in</span> range(<span class="number">1</span>,<span class="number">3</span>) <span class="keyword">for</span> y <span class="keyword">in</span> range(<span class="number">3</span>) <span class="keyword">for</span> z <span class="keyword">in</span> 	(<span class="number">4</span>,<span class="number">6</span>)]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[(<span class="number">1</span>, <span class="number">0</span>, <span class="number">4</span>), (<span class="number">1</span>, <span class="number">0</span>, <span class="number">5</span>), (<span class="number">1</span>, <span class="number">1</span>, <span class="number">4</span>), (<span class="number">1</span>, <span class="number">1</span>, <span class="number">5</span>), (<span class="number">1</span>, <span class="number">2</span>, <span class="number">4</span>), (<span class="number">1</span>, <span class="number">2</span>, <span class="number">5</span>), (<span class="number">2</span>, <span class="number">0</span>, <span class="number">4</span>), (<span class="number">2</span>, <span class="number">0</span>, <span class="number">5</span>), (<span class="number">2</span>, <span class="number">1</span>, <span class="number">4</span>), (<span class="number">2</span>, <span class="number">1</span>, <span class="number">5</span>), (<span class="number">2</span>, <span class="number">2</span>, <span class="number">4</span>), (<span class="number">2</span>, <span class="number">2</span>, <span class="number">5</span>)]</span><br></pre></td></tr></table></figure><p><strong>注意</strong>：<br>Python2中的range是直接开辟完内存空间，创造range中所有的值<br>这样做如果range的范围很大就会导致内存不足而出现错误<br>而Python3中的range是返回一个迭代器<br>如果要使用这个值的时候就给，然后再创建一个值，这样就避免了内存不足的问题。<br><strong>还有</strong>:</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">a = [i <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>,<span class="number">10</span>)]</span><br><span class="line">a = [<span class="number">10</span> <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>,<span class="number">10</span>)]</span><br></pre></td></tr></table></figure><p>这两个推导式是有区别的，前者是创建1~10，10个数，而后者是创建10个数都是10</p></li><li><p><strong>列表综合案例</strong><br>名字管理系统，代码如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 打印功能提示：</span></span><br><span class="line">print(<span class="string">"="</span>*<span class="number">30</span>)</span><br><span class="line">print(<span class="string">"  名字管理系统"</span>)</span><br><span class="line">print(<span class="string">" 1:添加一个名字"</span>)</span><br><span class="line">print(<span class="string">" 2:删除一个名字"</span>)</span><br><span class="line">print(<span class="string">" 3:修改一个名字"</span>)</span><br><span class="line">print(<span class="string">" 4:查询一个名字"</span>)</span><br><span class="line">print(<span class="string">" 5:退出系统"</span>)</span><br><span class="line">print(<span class="string">"="</span>*<span class="number">30</span>)</span><br><span class="line"></span><br><span class="line">names = []  <span class="comment">#定义一个空的列表用来存储添加的名字</span></span><br><span class="line">count = <span class="number">0</span> </span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">    num = int(input(<span class="string">"请输入功能序号："</span>))</span><br><span class="line">    <span class="keyword">if</span> num == <span class="number">1</span>:</span><br><span class="line">        new_name = input(<span class="string">"请输入名字: "</span>)</span><br><span class="line">        names.append(new_name)</span><br><span class="line">        print(names)</span><br><span class="line">        count += <span class="number">1</span></span><br><span class="line">    <span class="keyword">elif</span> num == <span class="number">2</span>:</span><br><span class="line">        del_name = input(<span class="string">"请输入要删除的名字："</span>)</span><br><span class="line">        names.remove(del_name)</span><br><span class="line">        count -= <span class="number">1</span></span><br><span class="line">    <span class="keyword">elif</span> num == <span class="number">3</span>:</span><br><span class="line">        ch_name = input(<span class="string">"请输入要修改的名字："</span>)</span><br><span class="line">        ch_name_af = input(<span class="string">"请输入修改后的名字："</span>)</span><br><span class="line">        names[names.index(ch_name,<span class="number">0</span>,count)] = ch_name_af</span><br><span class="line">    <span class="keyword">elif</span> num == <span class="number">4</span>:</span><br><span class="line">        find_name = input(<span class="string">"请输入要查询的名字："</span>)</span><br><span class="line">        <span class="keyword">if</span> find_name <span class="keyword">in</span> names:</span><br><span class="line">            print(<span class="string">"以找到，位置为：%d"</span> %(names.index(find_name,<span class="number">0</span>,count)))</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            print(<span class="string">"Sorry，查无此人"</span>)</span><br><span class="line">    <span class="keyword">elif</span> num == <span class="number">5</span>:</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        print(<span class="string">"Input ERROR, Please input again"</span>)</span><br><span class="line">print(<span class="string">"EXIT"</span>)</span><br></pre></td></tr></table></figure></li></ul><h3 id="元组"><a href="#元组" class="headerlink" title="元组"></a>元组</h3><p>元组和列表类似，不同之处在于元组中的元素不能修改，元组使用小括号，列表使用方括号<br>如：<code>aTuple = (&quot;et&quot;, 11, 99.9)</code><br>元组的访问和列表一样，也是通过下标对其进行访问，但是不能修改其值，元组也不能删除其中的某个值<br>元组中有的内置函数count, index用法和列表一样，分别用来计数和定位</p><h3 id="字典"><a href="#字典" class="headerlink" title="字典"></a>字典</h3><p>字典和列表类似，也能够存储多个数据。<br>和列表不同的是，字典存放数据是按照key:value的方式存储的<br>列表中找某个元素时是根据下标来进行的，而字典找某个元素时是根据键(key)来查找值(value)的<br>字典中的每个元素都由2部分组成，<strong>key:value</strong><br>例如：info就是一种字典<br><code>info = {&#39;name&#39;:&#39;班长&#39;, &#39;id&#39;:100, &#39;sex&#39;:&#39;f&#39;, &#39;address&#39;:&#39;北京&#39;}</code><br>可以根据键访问值：<br><code>print(info[&#39;name&#39;])</code><br><code>print(info[&#39;id&#39;])</code><br>如果访问不存在的键会报错</p><ul><li><p><strong>get</strong><br>如果我们不确定字典中是否存在某个键而又想获取其值时，可以使用get方法，还可以设定默认值：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>age = info.get(<span class="string">'age'</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>age <span class="comment">#'age'键不存在，所以age为None</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>type(age)</span><br><span class="line">&lt;type <span class="string">'NoneType'</span>&gt;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>age = info.get(<span class="string">'age'</span>, <span class="number">18</span>) <span class="comment"># 若info中不存在'age'这个键，就返回默认值18</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>age</span><br><span class="line"><span class="number">18</span></span><br></pre></td></tr></table></figure></li><li><p><strong>修改元素</strong><br>和列表类似，不过字典修改元素是根据键值进行对元素的修改：<br><code>info[&quot;key&quot;] = VALUE</code><br>如果字典中没有这个键值，那么可以用这个方法添加元素</p></li><li><p><strong>删除元素</strong><br>和列表类似，可以使用del删除某个元素<br>del info[“key”]<br>del方法还可以删除整个字典：del info，删除之后该字典不存在，无法访问<br>clear方法用于清空列表：<br>info.clear()，清空之后字典还在，只不过是变成了一个空字典</p></li><li><p><strong>常见操作</strong><br><strong>len()</strong>，此方法和字符串以及列表元组中的len方法一样，都是用来测元素个数，在字典中就测的是键值对的个数<br><strong>keys()</strong>，返回一个包含字典所有key值的列表，注意：返回的是列表<br><strong>values()</strong>，返回一个包含字典所有value值的列表<br><strong>items()</strong>，返回一个包含所有键值元组的列表，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>dict = &#123;<span class="string">"name"</span>:<span class="string">"xiaowang"</span>, <span class="string">"sex"</span>:<span class="string">"Men"</span>&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>dict.items()</span><br><span class="line">[(<span class="string">'name'</span>, <span class="string">'xiaowang'</span>), (<span class="string">'sex'</span>, <span class="string">'Men'</span>)]</span><br></pre></td></tr></table></figure><p><strong>注意</strong>：其中的键值对是用元组来表示的<br><strong>has_key(KEY)</strong>，如果KEY在字典中返回True，否则返回False</p></li><li><p><strong>遍历</strong><br>字典的遍历和其他的大体相同，都是使用循环来进行遍历<br>1、遍历字典的key：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">for</span> key <span class="keyword">in</span> dict.keys():</span><br><span class="line"><span class="meta">... </span>    <span class="keyword">print</span> key</span><br></pre></td></tr></table></figure><p>2、遍历字典的value：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">for</span> value <span class="keyword">in</span> dict.values():</span><br><span class="line"><span class="meta">... </span>    <span class="keyword">print</span> value</span><br></pre></td></tr></table></figure><p>3、遍历字典的元素（item）：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">for</span> item <span class="keyword">in</span> dict.items():</span><br><span class="line"><span class="meta">... </span>    <span class="keyword">print</span> item</span><br></pre></td></tr></table></figure><p>关于元组还可以进行拆包操作，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = (<span class="number">10</span>, <span class="number">20</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b,c = a</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b</span><br><span class="line"><span class="number">10</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>c</span><br><span class="line"><span class="number">20</span></span><br></pre></td></tr></table></figure><p>因此在for循环中可以用2个值来接收元组中的key、value<br>所以遍历字典中的元素还可以这样执行：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">for</span> key,value <span class="keyword">in</span> dict.items():</span><br><span class="line"><span class="meta">... </span>    print(<span class="string">"key=%s, value=%s"</span> %(key,value))</span><br></pre></td></tr></table></figure></li></ul><h3 id="集合"><a href="#集合" class="headerlink" title="集合"></a>集合</h3><p>集合类似于元组，不过其中的元素可以添加，但是其中的元素<strong>不能有重复</strong>。<br>集合是用大括号描述其元素的，如：<br><code>a = {1,2,3,4}</code><br>由于集合中的元素不能重复，因此可以将列表转换为集合然后再转换成列表进行重复元素的消除，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">10</span>,<span class="number">12</span>,<span class="number">10</span>,<span class="number">14</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = set(a)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = list(a)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">10</span>, <span class="number">12</span>, <span class="number">14</span>]</span><br></pre></td></tr></table></figure><p>同样可以用这种方法对元组进行消除重复元素的操作，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = (<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">10</span>,<span class="number">12</span>,<span class="number">10</span>,<span class="number">14</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = set(a)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = tuple(a)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">10</span>, <span class="number">12</span>, <span class="number">14</span>)</span><br></pre></td></tr></table></figure><p>由此可以看出：列表(list)、元组(tuple)、集合(set)之间可以互相转换<br>集合对象还支持union(联合), intersection(交), difference(差)和sysmmetric_difference(对称差集)等数学运算</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>x = set(<span class="string">"abcd"</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>x</span><br><span class="line">&#123;<span class="string">'d'</span>, <span class="string">'b'</span>, <span class="string">'a'</span>, <span class="string">'c'</span>&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>type(x)</span><br><span class="line">&lt;<span class="class"><span class="keyword">class</span> '<span class="title">set</span>'&gt;</span></span><br><span class="line">&gt;&gt;&gt; y = set("hello")</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>y</span><br><span class="line">&#123;<span class="string">'e'</span>, <span class="string">'h'</span>, <span class="string">'o'</span>, <span class="string">'l'</span>&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>z = set(<span class="string">"spam"</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>z</span><br><span class="line">&#123;<span class="string">'s'</span>, <span class="string">'a'</span>, <span class="string">'p'</span>, <span class="string">'m'</span>&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>y&amp;z  <span class="comment">#交集，为空返回set()</span></span><br><span class="line">set()</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>x|y  <span class="comment">#并集</span></span><br><span class="line">&#123;<span class="string">'e'</span>, <span class="string">'o'</span>, <span class="string">'b'</span>, <span class="string">'c'</span>, <span class="string">'d'</span>, <span class="string">'l'</span>, <span class="string">'h'</span>, <span class="string">'a'</span>&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>x-y  <span class="comment">#差集</span></span><br><span class="line">&#123;<span class="string">'d'</span>, <span class="string">'b'</span>, <span class="string">'a'</span>, <span class="string">'c'</span>&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>x^z  <span class="comment">#对称差集（在x或y中，不会同时在二者中）</span></span><br><span class="line">&#123;<span class="string">'b'</span>, <span class="string">'c'</span>, <span class="string">'s'</span>, <span class="string">'d'</span>, <span class="string">'m'</span>, <span class="string">'p'</span>&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>len(x)</span><br><span class="line"><span class="number">4</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>len(y)</span><br><span class="line"><span class="number">4</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>len(z)</span><br><span class="line"><span class="number">4</span></span><br></pre></td></tr></table></figure><h3 id="可变类型和不可变类型"><a href="#可变类型和不可变类型" class="headerlink" title="可变类型和不可变类型"></a>可变类型和不可变类型</h3><p>在Python中，<strong>可变类型</strong>只有2种，就是<strong>列表</strong>和<strong>字典</strong>，其他的变量类型都是<strong>不可变类型</strong><br>Python中函数参数是引用传递(和C++类似，相当于引用和变量本身指向同一块内存区域)<br>对于不可变类型，因变量名不能修改，所以运算的时候不会影响到变量自身<br>而对于可变类型来说，函数体中的运算有可能会更改传入的参数变量</p><h3 id="和-的区别"><a href="#和-的区别" class="headerlink" title="+= 和 =+ 的区别"></a>+= 和 =+ 的区别</h3><p>有如下代码：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">selfAdd</span><span class="params">(a)</span>:</span></span><br><span class="line">    a += a</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">selfAdd2</span><span class="params">(a)</span>:</span></span><br><span class="line">    a = a+a </span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test1</span><span class="params">()</span>:</span></span><br><span class="line">    a_int = <span class="number">1</span> </span><br><span class="line">    a_list = [<span class="number">1</span>, <span class="number">2</span>]</span><br><span class="line"></span><br><span class="line">    selfAdd(a_int)</span><br><span class="line">    print(a_int)</span><br><span class="line">    selfAdd(a_list)</span><br><span class="line">    print(a_list)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test2</span><span class="params">()</span>:</span></span><br><span class="line">    a_int = <span class="number">1</span> </span><br><span class="line">    a_list = [<span class="number">1</span>, <span class="number">2</span>]</span><br><span class="line"></span><br><span class="line">    selfAdd2(a_int)</span><br><span class="line">    print(a_int)</span><br><span class="line">    selfAdd2(a_list)</span><br><span class="line">    print(a_list)</span><br><span class="line"></span><br><span class="line">test1()</span><br><span class="line">test2()</span><br></pre></td></tr></table></figure><p>运行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">1</span><br><span class="line">[1, 2, 1, 2]</span><br><span class="line">1</span><br><span class="line">[1, 2]</span><br></pre></td></tr></table></figure><p>Python和C++不一样，+=和=+是不同的<br>其中”+=”是直接对内存空间中的变量进行操作<br>如果内存中的变量为不可变类型，则会重新开辟空间计算2个变量相加的结果；<br>如果变量为可变类型的变量，则直接对其进行操作，因此在函数中计算后显示的结果为1和[1,2,1,2]<br>而”a=a+a”是先看后面的结果，直接新开辟一块空间计算结果后赋值给a<br>因此这里的结果都没有产生改变。</p><h3 id="变量交换"><a href="#变量交换" class="headerlink" title="变量交换"></a>变量交换</h3><p>对变量进行交换的方式有多种，已知有2个变量a和b<br>第一种：通过<strong>第三个变量</strong>来进行交换：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">c &#x3D; a</span><br><span class="line">a &#x3D; b</span><br><span class="line">b &#x3D; c</span><br></pre></td></tr></table></figure><p>第二种：通过<strong>计算</strong>来进行交换（只能用于能够计算的两个相同类型的变量）</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">a &#x3D; a+b</span><br><span class="line">b &#x3D; a-b</span><br><span class="line">a &#x3D; a-b</span><br></pre></td></tr></table></figure><p><strong>第三种</strong>：<br><code>a,b = b,a</code></p><h2 id="控制语句"><a href="#控制语句" class="headerlink" title="控制语句"></a>控制语句</h2><blockquote><p>和C语言一样，Python中也有控制语句<br>这些语句能让程序不按顺序执行，而是按照自己想要的方式去执行</p></blockquote><h3 id="if-语句"><a href="#if-语句" class="headerlink" title="if 语句"></a>if 语句</h3><blockquote><p>if语句是一种判断语句，所谓判断的定义就是：<br>如果条件满足就执行某件事情，如果条件不满足就执行另外一件事情</p></blockquote><p>判断语句又被称为分支语句，正因为有了判断，才让程序有了很多分支<br>if语句基本语法：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">if 要判断的条件:</span><br><span class="line">    条件成立时，要进行的事情</span><br><span class="line">    ...</span><br><span class="line">elif 要判断的条件:</span><br><span class="line">    条件成立时要进行的事情</span><br><span class="line">    ...</span><br><span class="line">...</span><br><span class="line">else:</span><br><span class="line">    所有条件都不成立时，要进行的事情</span><br><span class="line">    ...</span><br></pre></td></tr></table></figure><p>注意，if下行代码的缩进要么使用tab，要么用4个空格<br>在Python的开发中，tab和空格不能混用，并且if-else语句以及缩进部分是一个完整的代码块</p><p>条件判断语句中使用的比较运算符：</p><table><thead><tr><th align="center">运算符</th><th>描述</th></tr></thead><tbody><tr><td align="center">==</td><td>检查两个操作数的值是否相等，相等为真</td></tr><tr><td align="center">!=</td><td>检查两个操作数的值是否不相等，不相等为真</td></tr><tr><td align="center">&lt;&gt;</td><td>检查两个操作数的值是否不相等，不相等为真</td></tr><tr><td align="center">&gt;</td><td>检查左操作数的值是否大于右操作数的值</td></tr><tr><td align="center">&lt;</td><td>检查左操作数的值是否小于右操作数的值</td></tr><tr><td align="center">&gt;=</td><td>检查左操作数的值是否大于等于右操作数的值</td></tr><tr><td align="center">&lt;=</td><td>检查左操作数的值是否小于等于右操作数的值</td></tr></tbody></table><p>年龄判断案例：<br>需求：<br>1.输入用户年龄<br>2.判断是否满 18 岁<br>3.如果满 18 岁，允许进网吧嗨皮<br>4.如果未满 18 岁，提示回家写作业</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 输入用户年龄</span></span><br><span class="line">age = int(input(<span class="string">"请输入年龄："</span>))</span><br><span class="line"><span class="comment"># 判断是否满 18 岁</span></span><br><span class="line"><span class="keyword">if</span> age &gt;= <span class="number">18</span>: </span><br><span class="line">    <span class="comment"># 如果满 18 岁，允许进网吧嗨皮</span></span><br><span class="line">    print(<span class="string">"你已经成年，欢迎来网吧嗨皮"</span>)</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    <span class="comment"># 如果未满 18 岁，提示回家写作业</span></span><br><span class="line">    print(<span class="string">"你还没有成年，请回家写作业吧"</span>)</span><br></pre></td></tr></table></figure><p>与if类似的有一个assert(断言)，后面跟的是一个bool类型的参数<br>如果断言成功(也就是后面的bool为True)，则不会发生什么，如果断言失败，则会报错<br>assert通常用于判断网页请求是否成功</p><h3 id="逻辑判断"><a href="#逻辑判断" class="headerlink" title="逻辑判断"></a>逻辑判断</h3><p>在程序开发中，通常在判断条件时，会需要同时判断多个条件。<br>只有多个条件都满足，才能够执行后续代码，这个时候需要使用到逻辑运算符。<br>逻辑运算符可以把多个条件按照逻辑进行连接，变成更复杂的条件。<br>Python中的逻辑运算符包括：与and/或or/非not三种</p><table><thead><tr><th align="center">运算符</th><th>逻辑表达式</th><th>描述</th></tr></thead><tbody><tr><td align="center">and</td><td>x and y</td><td>布尔”与”：如果x为False,它返回False;否则返回y的值</td></tr><tr><td align="center">or</td><td>x or y</td><td>布尔”或”：如果x为True,它返回True;否则返回y的值</td></tr><tr><td align="center">not</td><td>not x</td><td>布尔”非”：如果x为True,返回False;否则返回相反的值</td></tr></tbody></table><p>登陆案例：<br>从键盘输入用户名和密码，如果正确则打印欢迎登陆，否则打印不能登陆系统：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Input the username and password</span></span><br><span class="line">username = input(<span class="string">"Please input the username:"</span>)</span><br><span class="line">password = input(<span class="string">"Please input the password:"</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 判断用户名或密码是否输入正确</span></span><br><span class="line"><span class="keyword">if</span> username == <span class="string">"admin"</span> <span class="keyword">and</span> password == <span class="string">"123456"</span>:</span><br><span class="line">    print(<span class="string">"用户名密码输入正确！"</span>)</span><br><span class="line">    print(<span class="string">"欢迎登陆"</span>)</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    print(<span class="string">"用户名或密码输入错误！"</span>)</span><br><span class="line">    print(<span class="string">"对不起，您无法进入系统"</span>)</span><br></pre></td></tr></table></figure><h3 id="while循环语句"><a href="#while循环语句" class="headerlink" title="while循环语句"></a>while循环语句</h3><blockquote><p>一般情况下，需要多次重复执行的代码，都可以用循环的方式来完成<br>循环不是必须要使用的，但是为了提高代码的重复使用率，所以有经验的开发者都会采用循环</p></blockquote><p>while循环的格式：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">while 条件表达式:</span><br><span class="line">    条件满足时要进行的事情</span><br><span class="line">    ......</span><br><span class="line">当条件不满足时，跳出循环</span><br></pre></td></tr></table></figure><p>案例：计算1-100中所有偶数的和：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">i = <span class="number">1</span> </span><br><span class="line">sum = <span class="number">0</span> </span><br><span class="line"><span class="keyword">while</span> i&lt;=<span class="number">100</span>:</span><br><span class="line">    <span class="keyword">if</span> i%<span class="number">2</span> == <span class="number">0</span>:</span><br><span class="line">        sum += i</span><br><span class="line">    i += <span class="number">1</span></span><br><span class="line">print(sum)</span><br></pre></td></tr></table></figure><p>案例：打印九九乘法表</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">y = <span class="number">1</span> </span><br><span class="line"><span class="keyword">while</span> y &lt; <span class="number">10</span>: </span><br><span class="line">    x = <span class="number">1</span> </span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> x &lt;= y <span class="keyword">and</span> y &lt; <span class="number">10</span>: </span><br><span class="line">        print(<span class="string">"%d*%d=%-2d "</span> % (x, y, x*y), end=<span class="string">""</span>)</span><br><span class="line">        x += <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    print()</span><br><span class="line">    y += <span class="number">1</span></span><br></pre></td></tr></table></figure><p>和C语言一样，可以在循环中用break跳出整个循环，用continue跳出当前次循环</p><h3 id="for-循环"><a href="#for-循环" class="headerlink" title="for 循环"></a>for 循环</h3><p>for循环和while一样，也能完成循环的功能<br>在Python中，for循环可以遍历任何序列的项目，如一个列表或者一个字符串等<br>for循环格式：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">for 临时变量 in 列表或字符串等:</span><br><span class="line">    循环满足时执行的代码</span><br><span class="line">else:</span><br><span class="line">    循环不满足时执行的代码</span><br></pre></td></tr></table></figure><h2 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h2><p>函数是一组一起执行一个任务的语句。函数也是进行模块化设计的基础语句</p><h3 id="函数定义"><a href="#函数定义" class="headerlink" title="函数定义"></a>函数定义</h3><p>函数定义的格式如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">def 函数名：</span><br><span class="line">    函数功能代码</span><br><span class="line">    ......</span><br></pre></td></tr></table></figure><p>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 定义一个函数，能够完成打印信息的功能</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">printInfo</span><span class="params">()</span>:</span></span><br><span class="line">   <span class="keyword">print</span> <span class="string">'------------------------------------'</span></span><br><span class="line">   <span class="keyword">print</span> <span class="string">'         人生苦短，我用Python'</span></span><br><span class="line">   <span class="keyword">print</span> <span class="string">'------------------------------------'</span></span><br></pre></td></tr></table></figure><h3 id="函数调用"><a href="#函数调用" class="headerlink" title="函数调用"></a>函数调用</h3><p>定义了函数之后，就相当于有了一个具有某些功能的代码，想要这些代码能够执行，需要调用它<br>调用函数的方法很简单，和C语言一样，直接函数名加个括号即可调用，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 函数定义完之后调用</span></span><br><span class="line">printInfo()</span><br></pre></td></tr></table></figure><h3 id="函数参数"><a href="#函数参数" class="headerlink" title="函数参数"></a>函数参数</h3><blockquote><p>为了让一个函数更通用，在定义函数的时候可以让函数接收数据，此时函数接收的数据就是参数</p></blockquote><p>定义有参数的函数：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">addNum</span><span class="params">(a, b)</span>:</span></span><br><span class="line">    c = a+b</span><br><span class="line">    print(c)</span><br></pre></td></tr></table></figure><p>调用有参数的函数：<code>addNum(1,2)</code></p><p><strong>注意</strong>：<br>在函数定义时小括号中的参数，是用来接收参数用的，称为”形参”<br>而调用时小括号中的参数使用来传递给函数用的，称为”实参”</p><p>在调用函数的时候如果知道函数中的形参名，则可以通过形参名来传递实参，而不用按顺序来传递实参，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">printInfo</span><span class="params">(name, age)</span>:</span></span><br><span class="line">    print(<span class="string">"name:%s"</span> %name)</span><br><span class="line">    print(<span class="string">"age:%s"</span> %age)</span><br><span class="line">printInfo(age = <span class="number">13</span>, name = <span class="string">"XiaoWang"</span>)</span><br></pre></td></tr></table></figure><p>结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">name:XiaoWang</span><br><span class="line">age:13</span><br></pre></td></tr></table></figure><ul><li><p><strong>缺省参数</strong><br>在函数定义的时候可以指定缺省参数<br><strong>注意</strong>：缺省参数只能放在参数列表的最后，如果有不定长参数则不定长参数在最后<br>在函数调用的时候，缺省参数的值如果没有传入，则被认为是默认值，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">printinfo</span><span class="params">( name, age = <span class="number">35</span> )</span>:</span></span><br><span class="line">    print(<span class="string">"name:%s"</span> %name)</span><br><span class="line">    print(<span class="string">"age:%s"</span> %age)</span><br></pre></td></tr></table></figure></li><li><p><strong>不定长参数</strong><br>有时可能需要一个函数能处理比当初声明时更多的参数<br>这些参数叫做不定长参数，声明时不会命名。<br>基本语法如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">functionNAME</span><span class="params">([formal_args], *args, **kwargs)</span>:</span></span><br><span class="line">    ... </span><br><span class="line">    <span class="keyword">return</span> ...</span><br></pre></td></tr></table></figure><p>加了星号”*“的变量args会存放所有未命名的变量参数，args为元组<br>而加两个星号”**“的变量kwargs会存放命名的变量参数<br>即形如key=value的参数，kwargs为字典<br>其中的args本身是将变量保存为元组的，因此也可以直接传元组进来，不过要在元组名前加上*号进行传递<br>kwargs是将变量保存为字典的，也可以将字典直接传进来，在字典名上加2个*号进行传递即可<br>案例：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">fun</span><span class="params">(a, b, *args, **kwargs)</span>:</span></span><br><span class="line">    print(<span class="string">"a = "</span>, end=<span class="string">""</span>)</span><br><span class="line">    print(a)</span><br><span class="line">    print(<span class="string">"b = "</span>, end=<span class="string">""</span>)</span><br><span class="line">    print(b)</span><br><span class="line">    print(<span class="string">"args = "</span>, end=<span class="string">""</span>)</span><br><span class="line">    print(args)</span><br><span class="line">    print(<span class="string">"kwargs = "</span>)</span><br><span class="line">    print(kwargs)</span><br><span class="line"></span><br><span class="line">fun(<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,m=<span class="number">6</span>,n=<span class="number">7</span>,p=<span class="number">8</span>)</span><br><span class="line">c = (<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>)</span><br><span class="line">d = &#123;<span class="string">"m"</span>:<span class="number">6</span>, <span class="string">"n"</span>:<span class="number">7</span>, <span class="string">"p"</span>:<span class="number">8</span>&#125;</span><br><span class="line">print(<span class="string">"-"</span>*<span class="number">20</span>)</span><br><span class="line">fun(<span class="number">1</span>,<span class="number">2</span>,*c,**d)</span><br></pre></td></tr></table></figure></li></ul><p>2者打印的结果相同。由上面可以知道不定长参数在调用的时候还可以进行拆包。<br>传进去是用 <code>*args</code> 和 <code>**kwargs</code> 来接收的<br>并且 <code>*args</code> 和 <code>**kwargs</code> 会将多余的参数转化为列表和字典。<br>如果本来就有列表和字典想传入函数让args和kwargs来接收，就可以利用 <code>*</code> 和 <code>**</code> 将列表和字典进行拆包，如上所示。</p><h3 id="函数返回值"><a href="#函数返回值" class="headerlink" title="函数返回值"></a>函数返回值</h3><p>想要在函数中把结果返回给调用者，需要在函数中使用return<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">addNum</span><span class="params">(a, b)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> a+b</span><br></pre></td></tr></table></figure><p>当一个函数有返回值时，可以用一个变量来接收这个返回值，如：<br><code>c = addNum(1, 2)</code></p><h3 id="函数类型"><a href="#函数类型" class="headerlink" title="函数类型"></a>函数类型</h3><blockquote><p>函数根据有没有参数，有没有返回值，可以相互结合，一共有4种：<br>无参数无返回值；无参数有返回值；有参数无返回值；有参数有返回值</p></blockquote><h3 id="局部变量和全局变量"><a href="#局部变量和全局变量" class="headerlink" title="局部变量和全局变量"></a>局部变量和全局变量</h3><p><strong>局部变量</strong>:<br>定义在函数内部的变量，不同的函数可以定义相同名字的局部变量，但是各用各的不会对其他函数中的变量产生影响。<br><strong>全局变量</strong>:<br>定义在函数外边的变量，它既能在一个函数中使用，也能其他函数中使用，这就是全局变量。</p><p>当函数中的局部变量名字和全局变量名字相同时，该变量在函数中只是一个局部变量，不能对全局变量产生影响，<br>如果要在函数中对一个全局变量进行修改，则需要使用<strong>global</strong>关键字将该变量声明为全局变量，这样才能对其进行修改。<br><strong>注意：</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">如果全局变量类型为不可变类型的全局变量，则在函数中不能直接修改其值，需要加上global关键字</span><br><span class="line">如果全局变量类型为可变类型的全局变量，则不用加关键字也能对全局变量进行修改</span><br></pre></td></tr></table></figure><p>案例：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">a = <span class="number">0</span> </span><br><span class="line">b = []</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test01</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">global</span> a</span><br><span class="line">    a += <span class="number">100</span> </span><br><span class="line">    print(a)</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test02</span><span class="params">()</span>:</span></span><br><span class="line">    b.append(<span class="number">1</span>)</span><br><span class="line">    print(b)</span><br><span class="line"></span><br><span class="line">test01()</span><br><span class="line">test02()</span><br></pre></td></tr></table></figure><h3 id="函数文档说明"><a href="#函数文档说明" class="headerlink" title="函数文档说明"></a>函数文档说明</h3><p>在函数定义完之后的下一行，可以使用注释来对这个函数进行说明<br>这个注释就是函数的说明文档<br>函数的说明文档可以使用<strong>help(函数名)</strong>来进行查看<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">(a,b)</span>:</span></span><br><span class="line"><span class="meta">... </span>    <span class="string">"用来完成对2个数求和"</span></span><br><span class="line"><span class="meta">... </span>    print(<span class="string">"%d"</span>%(a+b))</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>help(test)</span><br></pre></td></tr></table></figure><p>然后就能看到test函数的帮助说明：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Help on function test in module __main__:</span><br><span class="line">test(a, b)</span><br><span class="line">    用来完成对2个数求和</span><br><span class="line">(END)</span><br></pre></td></tr></table></figure><h3 id="函数递归"><a href="#函数递归" class="headerlink" title="函数递归"></a>函数递归</h3><p>递归算法各个语言的都大同小异，直接上阶乘案例：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">calNum</span><span class="params">(num)</span>:</span></span><br><span class="line">    <span class="keyword">if</span> num &gt;=<span class="number">1</span>:</span><br><span class="line">        <span class="keyword">return</span> num*calNum(num<span class="number">-1</span>)</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span></span><br><span class="line"></span><br><span class="line">num = input()</span><br><span class="line">print(calNum(num))</span><br></pre></td></tr></table></figure><h3 id="匿名函数"><a href="#匿名函数" class="headerlink" title="匿名函数"></a>匿名函数</h3><p>当一个函数体不大的时候，可以使用<strong>匿名函数</strong>来创建，从而省略用def声明函数的标准步骤。<br><strong>lambda关键字</strong>用于创建匿名函数，用法如下：<br><code>lambda [arg1[ , arg2, ...]]:expression</code></p><p>案例如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">add</span><span class="params">(a, b)</span>:</span> </span><br><span class="line">    <span class="keyword">return</span> a+b </span><br><span class="line"></span><br><span class="line">func = <span class="keyword">lambda</span> a,b:a+b</span><br><span class="line"></span><br><span class="line">print(<span class="string">"add: %d"</span> % add(<span class="number">11</span>, <span class="number">22</span>))</span><br><span class="line">print(<span class="string">"func: %d"</span> % func(<span class="number">22</span>, <span class="number">33</span>))</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">cal</span><span class="params">(a, b, option)</span>:</span></span><br><span class="line">    print(<span class="string">"a, b is: %d, %d"</span> %(a, b)) </span><br><span class="line">    print(<span class="string">"The result is : %d"</span> % option(a, b)) </span><br><span class="line"></span><br><span class="line">cal(<span class="number">1</span>, <span class="number">2</span>, <span class="keyword">lambda</span> x,y:x+y)</span><br></pre></td></tr></table></figure><p><strong>匿名函数还有一些骚操作</strong><br>比如有一个列表，列表中的元素是字典，现在想让这个列表中的元素按某个KEY进行排序，就可以用到匿名函数<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>stus = [</span><br><span class="line"><span class="meta">... </span>    &#123;<span class="string">'name'</span>:<span class="string">'zhangsan'</span>,<span class="string">'age'</span>:<span class="number">18</span>&#125;,</span><br><span class="line"><span class="meta">... </span>    &#123;<span class="string">'name'</span>:<span class="string">'lisi'</span>,<span class="string">'age'</span>:<span class="number">19</span>&#125;,</span><br><span class="line"><span class="meta">... </span>    &#123;<span class="string">'name'</span>:<span class="string">'wangwu'</span>,<span class="string">'age'</span>:<span class="number">17</span>&#125;</span><br><span class="line"><span class="meta">... </span>]</span><br><span class="line"><span class="comment"># 让stus按name排序</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>stus.sort(key = <span class="keyword">lambda</span> x:x[<span class="string">'name'</span>])</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>stus</span><br><span class="line">[&#123;<span class="string">'name'</span>: <span class="string">'lisi'</span>, <span class="string">'age'</span>: <span class="number">19</span>&#125;, &#123;<span class="string">'name'</span>: <span class="string">'wangwu'</span>, <span class="string">'age'</span>: <span class="number">17</span>&#125;, &#123;<span class="string">'name'</span>: <span class="string">'zhangsan'</span>, <span class="string">'age'</span>: <span class="number">18</span>&#125;]</span><br><span class="line"><span class="comment"># 让stus按age排序</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>stus.sort(key = <span class="keyword">lambda</span> x:x[<span class="string">'age'</span>]) </span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>stus</span><br><span class="line">[&#123;<span class="string">'name'</span>: <span class="string">'wangwu'</span>, <span class="string">'age'</span>: <span class="number">17</span>&#125;, &#123;<span class="string">'name'</span>: <span class="string">'zhangsan'</span>, <span class="string">'age'</span>: <span class="number">18</span>&#125;, &#123;<span class="string">'name'</span>: <span class="string">'lisi'</span>, <span class="string">'age'</span>: <span class="number">19</span>&#125;]</span><br></pre></td></tr></table></figure><h3 id="综合案例"><a href="#综合案例" class="headerlink" title="综合案例"></a>综合案例</h3><p>学生管理系统：<br>需实现如下功能：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;</span><br><span class="line">          学生管理系统</span><br><span class="line">        1. 添加一个学生</span><br><span class="line">        2. 删除一个学生</span><br><span class="line">        3. 修改一个学生</span><br><span class="line">        4. 查询学生</span><br><span class="line">        5. 显示所有学生</span><br><span class="line">        6. 退出系统</span><br><span class="line">&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;</span><br></pre></td></tr></table></figure><p>代码如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 定义全局列表用来保存学生信息</span></span><br><span class="line">card_infors = []</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">printMenu</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="string">"""</span></span><br><span class="line"><span class="string">    此函数用来打印菜单</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    print(<span class="string">"="</span>*<span class="number">35</span>)</span><br><span class="line">    print(<span class="string">"  \t  学生管理系统"</span>)</span><br><span class="line">    print(<span class="string">"\t1. 添加一个学生"</span>)</span><br><span class="line">    print(<span class="string">"\t2. 删除一个学生"</span>)</span><br><span class="line">    print(<span class="string">"\t3. 修改一个学生"</span>)</span><br><span class="line">    print(<span class="string">"\t4. 查询学生"</span>)</span><br><span class="line">    print(<span class="string">"\t5. 显示所有学生"</span>)</span><br><span class="line">    print(<span class="string">"\t6. 退出系统"</span>)</span><br><span class="line">    print(<span class="string">"="</span>*<span class="number">35</span>)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">addNewStu</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="string">"""</span></span><br><span class="line"><span class="string">    此函数用于添加学生信息</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    new_name = input(<span class="string">"请输入姓名: "</span>)</span><br><span class="line">    new_qq = input(<span class="string">"请输入QQ: "</span>)</span><br><span class="line">    new_wx = input(<span class="string">"请输入微信: "</span>)</span><br><span class="line">    new_addr = input(<span class="string">"请输入住址: "</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 定义一个字典用于存储学生信息</span></span><br><span class="line">    new_infor = &#123;&#125;</span><br><span class="line">    new_infor[<span class="string">'name'</span>] = new_name</span><br><span class="line">    new_infor[<span class="string">'qq'</span>] = new_qq</span><br><span class="line">    new_infor[<span class="string">'wx'</span>] = new_wx</span><br><span class="line">    new_infor[<span class="string">'addr'</span>] = new_addr</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 将字典添加到列表中</span></span><br><span class="line">    <span class="keyword">global</span> card_infors</span><br><span class="line">    card_infors.append(new_infor)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">delStu</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="string">"""此函数用于删除学生"""</span></span><br><span class="line">    find_name = input(<span class="string">"请输入要删除的学生姓名："</span>)</span><br><span class="line">    find_flag = <span class="number">0</span></span><br><span class="line">    i = <span class="number">0</span></span><br><span class="line">    <span class="keyword">while</span> i &lt; len(card_infors):</span><br><span class="line">        <span class="keyword">if</span> find_name == card_infors[i][<span class="string">'name'</span>]:</span><br><span class="line">            <span class="keyword">del</span> card_infors[i]</span><br><span class="line">            find_flag = <span class="number">1</span></span><br><span class="line">        i += <span class="number">1</span></span><br><span class="line">    <span class="keyword">if</span> find_flag == <span class="number">0</span>:</span><br><span class="line">        print(<span class="string">"Sorry, 查无此人..."</span>)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">changeStu</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="string">"""此函数用于修改学生"""</span></span><br><span class="line">    find_name = input(<span class="string">"请输入要修改的学生姓名："</span>)</span><br><span class="line">    find_flag = <span class="number">0</span></span><br><span class="line">    i = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> temp <span class="keyword">in</span> card_infors:</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> find_name == temp[<span class="string">'name'</span>]:</span><br><span class="line">            new_name = input(<span class="string">"请输入修改后的姓名: "</span>)</span><br><span class="line">            new_qq = input(<span class="string">"请输入修改后的QQ: "</span>)</span><br><span class="line">            new_wx = input(<span class="string">"请输入修改后的微信: "</span>)</span><br><span class="line">            new_addr = input(<span class="string">"请输入修改后的住址: "</span>)</span><br><span class="line">            card_infors[i][<span class="string">'name'</span>] = new_name</span><br><span class="line">            card_infors[i][<span class="string">'qq'</span>] = new_qq</span><br><span class="line">            card_infors[i][<span class="string">'wx'</span>] = new_wx</span><br><span class="line">            card_infors[i][<span class="string">'addr'</span>] = new_addr</span><br><span class="line">            find_flag = <span class="number">1</span></span><br><span class="line">        i += <span class="number">1</span> </span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> find_flag == <span class="number">0</span>:</span><br><span class="line">        print(<span class="string">"Sorry, 查无此人..."</span>)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">findStu</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="string">"""</span></span><br><span class="line"><span class="string">    此函数用于查找学生</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    find_name = input(<span class="string">"请输入要查找的学生姓名："</span>)</span><br><span class="line">    find_flag = <span class="number">0</span>  <span class="comment"># 默认表示没有找到</span></span><br><span class="line">    <span class="keyword">for</span> temp <span class="keyword">in</span> card_infors:</span><br><span class="line">        <span class="keyword">if</span> find_name == temp[<span class="string">"name"</span>]:</span><br><span class="line">            print(<span class="string">"%s\t%s\t%s\t%s"</span> %(temp[<span class="string">'name'</span>], temp[<span class="string">'qq'</span>], temp[<span class="string">'wx'</span>], temp[<span class="string">'addr'</span>]))</span><br><span class="line">            find_flag = <span class="number">1</span>  <span class="comment"># 1表示已经找到</span></span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 判断是否找到</span></span><br><span class="line">    <span class="keyword">if</span> find_flag == <span class="number">0</span>:</span><br><span class="line">        print(<span class="string">"Sorry, 查无此人..."</span>)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">showStu</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="string">"""此函数用于打印所有学生信息"""</span></span><br><span class="line">    <span class="keyword">global</span> card_infors</span><br><span class="line">    print(<span class="string">"\n姓名\tQQ\t微信\t住址"</span>)</span><br><span class="line">    <span class="keyword">for</span> temp <span class="keyword">in</span> card_infors:</span><br><span class="line">        print(<span class="string">"%s\t%s\t%s\t%s"</span> %(temp[<span class="string">'name'</span>], temp[<span class="string">'qq'</span>], temp[<span class="string">'wx'</span>], temp[<span class="string">'addr'</span>]))</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="string">"""main函数"""</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># Print the Menu</span></span><br><span class="line">    printMenu()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        <span class="comment"># 获取用户的输入</span></span><br><span class="line">        num = input(<span class="string">"请输入操作序列号: "</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 根据用户的数据执行响应的功能</span></span><br><span class="line">        <span class="keyword">if</span> num == <span class="string">'1'</span>:</span><br><span class="line">            addNewStu()</span><br><span class="line">        <span class="keyword">elif</span> num == <span class="string">'2'</span>:</span><br><span class="line">            delStu()</span><br><span class="line">        <span class="keyword">elif</span> num == <span class="string">'3'</span>:</span><br><span class="line">            changeStu()</span><br><span class="line">        <span class="keyword">elif</span> num == <span class="string">'4'</span>:</span><br><span class="line">            findStu()</span><br><span class="line">        <span class="keyword">elif</span> num == <span class="string">'5'</span>:</span><br><span class="line">            showStu()</span><br><span class="line">        <span class="keyword">elif</span> num == <span class="string">'6'</span>:</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            print(<span class="string">"输入有误，请重新输入！"</span>)</span><br><span class="line"></span><br><span class="line">        print()</span><br><span class="line"></span><br><span class="line">main()</span><br></pre></td></tr></table></figure><h2 id="文件"><a href="#文件" class="headerlink" title="文件"></a>文件</h2><blockquote><p>程序执行的时候通常会将数据保存在内存中，但是程序结束的时候这些数据会被释放掉<br>因此就需要用到磁盘空间来将数据存储起来。存储的方式就是将数据保存在文件中。</p></blockquote><h3 id="文体的打开和关闭"><a href="#文体的打开和关闭" class="headerlink" title="文体的打开和关闭"></a>文体的打开和关闭</h3><p>在Python中，使用<strong>open函数</strong>可以打开或者创建一个文件<br>格式：<code>open(文件名，访问方式)</code><br>使用open函数需要用一个变量来存储，类似于C语言中的文件指针，如：<br><code>f = open(&#39;test.txt&#39;, &#39;w&#39;)</code><br>访问方式：</p><ul><li><strong>r</strong><br>以只读方式打开文件。<br>文件的指针将会放在文件的开头。这是默认模式，如果只有r的话可以不写</li><li><strong>w</strong><br>打开一个文件只用于写入。<br>如果该文件已存在则将其覆盖。如果该文件不存在，创建新文件。</li><li><strong>a</strong><br>打开一个文件用于追加。<br>如果该文件已存在，文件指针将会放在文件的结尾。也就是说，新的内容将会被写入到已有内容之后。<br>如果该文件不存在，创建新文件进行写入。</li><li><strong>rb</strong><br>以二进制格式打开一个文件用于只读。<br>文件指针将会放在文件的开头。</li><li><strong>wb</strong><br>以二进制格式打开一个文件只用于写入。<br>如果该文件已存在则将其覆盖。如果该文件不存在，创建新文件。</li><li><strong>ab</strong><br>以二进制格式打开一个文件用于追加。<br>如果该文件已存在，文件指针将会放在文件的结尾。也就是说，新的内容将会被写入到已有内容之后。<br>如果该文件不存在，创建新文件进行写入。</li><li><strong>r+</strong><br>打开一个文件用于读写。文件指针将会放在文件的开头。</li><li><strong>w+</strong><br>打开一个文件用于读写。<br>如果该文件已存在则将其覆盖。<br>如果该文件不存在，创建新文件。</li><li><strong>a+</strong><br>打开一个文件用于读写。<br>如果该文件已存在，文件指针将会放在文件的结尾。<br>文件打开时会是追加模式。如果该文件不存在，创建新文件用于读写。</li><li><strong>rb+/wb+/ab+</strong><br>和上面的r+/w+/a+类似，只不过是用来操作二进制文件的</li></ul><p>关闭文件，一个文件打开操作完之后需要关闭，关闭文件的函数为<strong>close</strong></p><h3 id="文件的读写"><a href="#文件的读写" class="headerlink" title="文件的读写"></a>文件的读写</h3><ul><li><strong>write函数</strong><br>如果要写一个文件，则需要在打开文件的时候指定为w（也可以指定其他的写操作）<br>然后使用write函数即可将某些内容写入文件中。如：<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">f = open(<span class="string">'test.txt'</span>, <span class="string">'w'</span>)</span><br><span class="line">f.write(<span class="string">'hello world, i am here!'</span>)</span><br><span class="line">f.close()</span><br></pre></td></tr></table></figure><strong>注意</strong>：使用w打开文件的时候如果文件不存在就创建文件，如果存在就清空文件<br>如果要读一个文件，则需要在打开文件的时候指定为r（也可以指定其他的读操作）</li><li><strong>read函数</strong><br>使用read函数可以从文件中读取数据，其中可指定一个长度（单位是字节）<br>如果没有指定则表示读取文件中的所有数据，如：<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">f = open(<span class="string">'test.txt'</span>, <span class="string">'r'</span>)</span><br><span class="line">content = f.read(<span class="number">5</span>)</span><br><span class="line">print(content)</span><br><span class="line">print(<span class="string">"-"</span>*<span class="number">30</span>)</span><br><span class="line">content = f.read()</span><br><span class="line">print(content)</span><br><span class="line">f.close()</span><br></pre></td></tr></table></figure><strong>注意</strong>：read每读一下就会将文件读取的光标跳转值读完的下一个字节，即还没读到的第一个字节</li><li><strong>readlines</strong><br>就像read函数没有参数一样，readlines可以按照行的方式把整个文件中的内容进行一次性读取，并且返回的是一个列表，其中每一行的数据为一个元素</li><li><strong>readline</strong><br>readline函数是一行一行的读取</li></ul><h3 id="制作文件备份"><a href="#制作文件备份" class="headerlink" title="制作文件备份"></a>制作文件备份</h3><p>要求，输入文件名字，然后在文件名最后一个点前面加上一个.bak</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">old_file_name = input(<span class="string">"请输入要备份的文件名："</span>)</span><br><span class="line">old_file = open(old_file_name, <span class="string">"r"</span>)</span><br><span class="line"><span class="comment"># Find the "." of the file name</span></span><br><span class="line">index = old_file_name.rfind(<span class="string">"."</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># If old file name has "."</span></span><br><span class="line"><span class="keyword">if</span> index &gt; <span class="number">0</span>:</span><br><span class="line">    file_flag = old_file_name[index:]</span><br><span class="line">    new_file_name = old_file_name[<span class="number">0</span>:index] + <span class="string">".bak"</span> + file_flag</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    new_file_name = old_file_name + <span class="string">".bak"</span></span><br><span class="line"></span><br><span class="line">new_file = open(new_file_name, <span class="string">"w"</span>)</span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>: </span><br><span class="line">    content=old_file.read(<span class="number">1024</span>)</span><br><span class="line">    new_file.write(content)</span><br><span class="line">    <span class="keyword">if</span> content == <span class="string">""</span>: </span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line"></span><br><span class="line">old_file.close()</span><br><span class="line">new_file.close()</span><br></pre></td></tr></table></figure><h3 id="文件定位读写"><a href="#文件定位读写" class="headerlink" title="文件定位读写"></a>文件定位读写</h3><p>在读写文件的过程中有一个光标位置，用<strong>tell()</strong>函数可以获取光标位置</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 打开一个已经存在的文件</span></span><br><span class="line">f = open(<span class="string">"test.txt"</span>, <span class="string">"r"</span>)</span><br><span class="line">str = f.read(<span class="number">3</span>)</span><br><span class="line">print(<span class="string">"读取的数据是 : "</span>, end=<span class="string">""</span>)</span><br><span class="line">print(str)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查找当前位置</span></span><br><span class="line">position = f.tell()</span><br><span class="line">print(<span class="string">"当前文件位置 : %d"</span> %position)</span><br><span class="line"></span><br><span class="line">str = f.read(<span class="number">3</span>)</span><br><span class="line">print(<span class="string">"读取的数据是 : "</span>, end=<span class="string">""</span>)</span><br><span class="line">print(str)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查找当前位置</span></span><br><span class="line">position = f.tell()</span><br><span class="line">print(<span class="string">"当前文件位置 : %d"</span> %position)</span><br><span class="line">f.close()</span><br></pre></td></tr></table></figure><p>如果在写文件的过程中，需要从另一个位置进行操作的话，可以使用seek函数<br><strong>seek(offset,from)</strong><br>其中offset为偏移量,偏移量可以是负数；<br>from为方向，有3个值，0表示文件开头，1表示当前位置，2表示文件末尾</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test1</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="comment"># 打开一个已经存在的文件</span></span><br><span class="line">    f = open(<span class="string">"test.txt"</span>, <span class="string">"r"</span>)</span><br><span class="line">    str = f.read(<span class="number">30</span>)</span><br><span class="line"></span><br><span class="line">    print(<span class="string">"读取的数据是 : %s"</span> %str)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 查找当前位置</span></span><br><span class="line">    position = f.tell()</span><br><span class="line">    print(<span class="string">"当前文件位置 : %d"</span> %position)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 重新设置位置</span></span><br><span class="line">    f.seek(<span class="number">5</span>,<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 查找当前位置</span></span><br><span class="line">    position = f.tell()</span><br><span class="line">    print(<span class="string">"当前文件位置 : %d"</span> %position)</span><br><span class="line"></span><br><span class="line">    f.close()</span><br><span class="line">test1()</span><br></pre></td></tr></table></figure><h3 id="os-模块对文件的操作"><a href="#os-模块对文件的操作" class="headerlink" title="os 模块对文件的操作"></a>os 模块对文件的操作</h3><p>要使用os模块中的东西，需要导入os模块：<br><code>import os</code></p><ul><li><strong>rename</strong><br>rename函数用来修改文件名<br><code>os.rename(&quot;OldName&quot;, &quot;NewName&quot;)</code></li><li><strong>remove</strong><br>remove函数可以完成对文件的删除操作<br><code>os.rename(&quot;FILENAME&quot;)</code></li><li><strong>mkdir</strong><br>mkdir函数可以创建文件夹，和Linux下的mkdir命令一样的作用<br><code>os.mkdir(&quot;DirName&quot;)</code></li><li><strong>getcwd</strong><br>getcwd函数用于获取当前目录，和Linux下的pwd命令一样的作用<br><code>os.getcwd()</code></li><li><strong>chdir</strong><br>chdir函数用于改变当前目录，和Linux下的cd命令一样的作用<br><code>os.chdir(&quot;../&quot;)</code></li><li><strong>listdir</strong><br>listdir用于获取目录列表，和Linux下的ls命令一样的作用<br><code>os.listdir(&quot;./&quot;)</code></li><li><strong>rmdir</strong><br>rmdir用于删除文件夹，和Linux下的rm -r一样<br><code>os.rmdir(&quot;DirName&quot;)</code></li></ul><h2 id="面向对象"><a href="#面向对象" class="headerlink" title="面向对象"></a>面向对象</h2><blockquote><p>在编写程序的时候我们往往用的是面向过程的编程思维，面向过程就是根据业务逻辑从上到下写代码。<br>这种编程思维最容易被初学者接收，但是有一定的缺陷，就是代码可维护性很差，要修改某一个功能的时候往往需要修改很多内容。因此就需要有一种其他的编程思维来改变。<br>面向对象就可以解决这种问题，其思维是将数据与函数绑定到一起，进行封装，这样能够更快速的开发程序，减少代码的重写过程。<br><strong>面向对象(object-oriented;简称:OO)</strong>至今还没有统一的概念 我这里把它定义为：按人们认识客观世界的系统思维方式,采用基于对象(实体)的概念建立模型,模拟客观世界分析、设计、实现软件的办法。<br><strong>面向对象编程(Object Oriented Programming-OOP)</strong>是一种解决软件复用的设计和编程方法。<br>这种方法把软件系统中相近相似的操作逻辑和操作应用数据、状态，以类的型式描述出来，以对象实例的形式在软件系统中复用，以达到提高软件开发效率的作用。</p></blockquote><h3 id="类和对象"><a href="#类和对象" class="headerlink" title="类和对象"></a>类和对象</h3><p>面向对象编程的2个非常重要的概念：<strong>类和对象</strong><br>对象是面向对象编程的核心，在使用的过程中，为了将具有共同特征和行为的一组对象抽象定义，提出了另外一个新的概念：<strong>类</strong>。<br>人以类聚，物以群分。具有相似内部状态和运动规律的实体的集合(或统称为抽象)。<br>类是抽象的,在使用的时候通常会找到这个类的一个具体的存在,使用这个具体的存在。<br>对象则是某个具体事物的存在，在显示世界中可以是看得见摸得着的。可以是直接使用的。<br>简单来说，<strong>类就是创建对象的模板</strong>。<br>在Python中定义类的格式为：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> 类名：</span></span><br><span class="line"><span class="class">    方法...</span></span><br></pre></td></tr></table></figure><p>Python中也可以根据已经定义的类去创建一个个对象，创建格式为：<br><code>对象名 = 类名()</code><br>如果使用<code>对象名.属性名 = 值</code>还可以给对象添加属性，如果后面再出现这个属性名，那就是对这个属性进行修改</p><h3 id="init-方法"><a href="#init-方法" class="headerlink" title="__init__ 方法"></a><code>__init__</code> 方法</h3><p><code>__init__</code> 方法是初始化函数，用来完成一些默认的设定，格式如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> 类名:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        ...</span><br></pre></td></tr></table></figure><p>当创建对象的时候，<code>__init__</code>函数会被默认调用，所以不需要手动调用<br><code>__init__(self)</code>中，默认有一个参数名字为<strong>self</strong>，如果需要传递参数如a,b，则还需要添加2个形参, 如<code>__init__(self, a, b)</code><br><code>__init__(self)</code>中的self参数不需要开发者传递，Python解释器会自动把当前对象引用传递进去</p><blockquote><p><strong>init</strong>方法往往是用来对创建完的对象进行初始化工作<br>如果在子类中重写了父类的<strong>init</strong>方法，即意味着父类中的很多初始化工作没有做，这样就不保证程序的稳定了<br>所以在以后的开发中，如果重写了父类的<strong>init</strong>方法，最好是先调用父类的这个方法，然后再添加自己的功能，调用父类的<strong>init</strong>方法为：<code>super().__init__()</code></p></blockquote><h3 id="str-方法"><a href="#str-方法" class="headerlink" title="__str__ 方法"></a><code>__str__</code> 方法</h3><p>如果创建一个对象，然后直接用print函数打印这个对象的时候，会出现如下信息：<br><code>&lt;__main__.test object at 0x7f4621d7a2e8&gt;</code><br>这个打印信息是用<code>__str__</code>方法显示出来的<br>如果要指定显示某些信息的时候可以定义<code>__str__</code>方法：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">__str__</span><span class="params">(self)</span>:</span></span><br><span class="line">    msg = <span class="string">"STRING"</span></span><br><span class="line">    <span class="keyword">return</span> msg</span><br></pre></td></tr></table></figure><p>在使用print函数输出对象的时候，只要自己定义了<code>__str__(self)</code>方法，那么就会打印从这个方法中返回的数据</p><h3 id="self"><a href="#self" class="headerlink" title="self"></a>self</h3><blockquote><p>Python中的self和C++中的this指针有着相同的作用，所谓的self可以理解为自己。<br>某个对象调用其方法的时候，Python解释器会自动把这个对象作为第一个参数传递给self，所以只需要开发者传递后面的参数即可。<br>类中的方法中都需要有一个参数为self，因为在调用方法时，Python解释器会默认的传入一个参数就是对象本身，也就是其中的self。</p></blockquote><h3 id="面向对象案例"><a href="#面向对象案例" class="headerlink" title="面向对象案例"></a>面向对象案例</h3><p>房子家具案例：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Home</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, area)</span>:</span></span><br><span class="line">        <span class="comment"># 定义房子可用面积</span></span><br><span class="line">        self.area = area</span><br><span class="line">        <span class="comment"># 定义列表存放房子家具</span></span><br><span class="line">        self.contains = []</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__str__</span><span class="params">(self)</span>:</span></span><br><span class="line">        msg = <span class="string">"当前房子可用面积为："</span> + str(self.area)</span><br><span class="line">        <span class="keyword">if</span> len(self.contains) &gt; <span class="number">0</span>:</span><br><span class="line">            msg = msg + <span class="string">"\n容纳的物品有："</span></span><br><span class="line">            <span class="keyword">for</span> temp <span class="keyword">in</span> self.contains:</span><br><span class="line">                msg = msg + temp.getName() + <span class="string">", "</span></span><br><span class="line">            msg = msg.rstrip(<span class="string">", "</span>)  <span class="comment">#去掉最后一个逗号</span></span><br><span class="line">        <span class="keyword">return</span> msg </span><br><span class="line"></span><br><span class="line">    <span class="comment"># 容纳物品</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">accommdoateItem</span><span class="params">(self, item)</span>:</span></span><br><span class="line">        needArea = item.getUsedArea()</span><br><span class="line">        <span class="comment"># 如果可用面积大于物品的占用面积</span></span><br><span class="line">        <span class="keyword">if</span> self.area &gt; needArea:</span><br><span class="line">            self.contains.append(item)</span><br><span class="line">            self.area -= needArea</span><br><span class="line">            print(<span class="string">"Ok, 此物品已经放进房间中"</span>)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            print(<span class="string">"Sorry，面积不足，该物品不能放进房间中"</span>)</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Bed</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, area, name = <span class="string">"床"</span>)</span>:</span></span><br><span class="line">        self.name = name</span><br><span class="line">        self.area = area</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__str__</span><span class="params">(self)</span>:</span></span><br><span class="line">        msg = <span class="string">"床的面积为："</span> + str(self.area)</span><br><span class="line">        <span class="keyword">return</span> msg</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">getUsedArea</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> self.area</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">getName</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> self.name</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">newHome = Home(<span class="number">100</span>)</span><br><span class="line">print(newHome)</span><br><span class="line"></span><br><span class="line">newBed = Bed(<span class="number">20</span>)</span><br><span class="line">print(newBed)</span><br><span class="line"></span><br><span class="line">newHome.accommdoateItem(newBed)</span><br><span class="line">print(newHome)</span><br><span class="line"></span><br><span class="line">newBed2 = Bed(<span class="number">30</span>, <span class="string">"席梦思"</span>)</span><br><span class="line">print(newBed2)</span><br><span class="line"></span><br><span class="line">newHome.accommdoateItem(newBed2)</span><br><span class="line">print(newHome)</span><br></pre></td></tr></table></figure><p>输出结果为：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">当前房子可用面积为：100</span><br><span class="line">床的面积为：20</span><br><span class="line">Ok, 此物品已经放进房间中</span><br><span class="line">当前房子可用面积为：80</span><br><span class="line">容纳的物品有：床</span><br><span class="line">床的面积为：30</span><br><span class="line">Ok, 此物品已经放进房间中</span><br><span class="line">当前房子可用面积为：50</span><br><span class="line">容纳的物品有：床, 席梦思</span><br></pre></td></tr></table></figure><h3 id="私有属性和方法"><a href="#私有属性和方法" class="headerlink" title="私有属性和方法"></a>私有属性和方法</h3><p>如果有一个对象，当需要对其进行修改属性的时候，有2种办法：<br><code>对象名.属性名 = 数据 （直接修改）</code><br><code>对象名.方法名() （间接修改）</code><br>为了更好的保护属性安全，即不能随意修改，一般的处理方式为：<br><strong>将属性定义为私有属性</strong><br>Python中的私有方法就是在名字前加2个下划线”__”<br>添加一个可以调用的方法，提供属性的修改调用<br>如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">People</span><span class="params">(object)</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, name)</span>:</span></span><br><span class="line">        self.__name = name</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">getName</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> self.__name</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">setName</span><span class="params">(self, newName)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> len(newName) &gt;= <span class="number">5</span>:</span><br><span class="line">            self.__name = newName</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            print(<span class="string">"ERROR, the lenth of the name must &gt;=5"</span>)</span><br></pre></td></tr></table></figure><blockquote><p>Python中没有像C++中public和private这些关键字来区别公有属性和私有属性。<br>它是以属性命名方式来区分，如果在属性名前面加了2个下划线’__’，则表明该属性是私有属性，否则为公有属性（方法也是一样，方法名前面加了2个下划线的话表示该方法是私有的，否则为公有的）。</p></blockquote><h3 id="del-方法"><a href="#del-方法" class="headerlink" title="__del__ 方法"></a><code>__del__</code> 方法</h3><blockquote><p>创建对象后，Python解释器默认调用<strong>init</strong>方法；<br>当删除一个对象时，Python解释器也会默认调用一个方法，这个方法就是<strong>del</strong>方法<br>在Python中赋值都是引用赋值，当一个对象被创建之后会将其引用赋值给一个变量，此时这个对象的引用计数就为1。<br>每当这个对象的引用被保存至一个变量中时，该对象的引用计数就会加一，使用del可以删除某个引用，此时对象的引用计数会减一。<br>当一个对象的引用计数为0的时候，该对象就会被删除，删除前会调用<strong>del</strong>方法<br><strong>del</strong>方法也相当于C++中的析构方法</p></blockquote><h3 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h3><p>Python中继承的效果和C++的一样，语法格式为：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> 类名<span class="params">(父类名)</span>:</span></span><br><span class="line">    ......</span><br></pre></td></tr></table></figure><p>子类创建对象的时候，可以使用父类的公有方法和属性。9<br>子类如果没有<strong>init</strong>方法，在创建对象的时候会自动调用父类的<strong>init</strong>方法。<br>需要注意的是：<strong>私有的属性、方法，不会被子类继承，也不能被子类访问。</strong><br><strong>多继承</strong>是子类有多个父类，并且具有他们的特征，多继承格式如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> 类名<span class="params">(父类<span class="number">1</span>，父类<span class="number">2</span>, ...)</span>:</span></span><br><span class="line">    ......</span><br></pre></td></tr></table></figure><h3 id="重写"><a href="#重写" class="headerlink" title="重写"></a>重写</h3><p>所谓重写就是子类中有和父类同名的方法，在子类中的方法会覆盖掉父类中的方法。<br>如果想在子类中调用父类的方法，需要用到<strong>super()</strong><br>如：<code>super().__init__()</code></p><h3 id="多态"><a href="#多态" class="headerlink" title="多态"></a>多态</h3><p>这里多态的概念和C++中的一样，不详细介绍。<br>有区别的是C++那一类强类型语言中需要指定参数的类型，指定的就是父类的类型，然后用父类形参来接收子类对象实参，达到多态的效果。<br>而Python是弱类型语言，定义变量或执行形参的时候不需要指定参数类型，但是也能用形参接收其他子类参数达到多态的效果<br>如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Father</span><span class="params">(object)</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">show</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">"F.show"</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">S1</span><span class="params">(Father)</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">show</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">"S1.show"</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">S2</span><span class="params">(Father)</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">show</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">"S2.show"</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">func</span><span class="params">(f)</span>:</span></span><br><span class="line">    print(f.show())</span><br><span class="line"></span><br><span class="line">s1_obj = S1()</span><br><span class="line">s2_obj = S2()</span><br><span class="line">func(s1_obj)</span><br><span class="line">func(s2_obj)</span><br></pre></td></tr></table></figure><p>结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">S1.show</span><br><span class="line">S2.show</span><br></pre></td></tr></table></figure><h3 id="类属性和实例属性"><a href="#类属性和实例属性" class="headerlink" title="类属性和实例属性"></a>类属性和实例属性</h3><p>在函数中定义的属性就是<strong>实例属性</strong>，也可以称之为对象属性，一个对象创建出来的时候它之中的属性就是实例属性。<br>而<strong>类属性</strong>就是一个类的属性，是定义在函数之外的，它被所有类的对象所共有，在内存中只有一片空间，和C++中的静态成员变量有类似之处。<br>对于公有的类属性，在类外可以通过类和对象对其进行访问。</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">People</span><span class="params">(object)</span>:</span></span><br><span class="line">    address = <span class="string">"China"</span>  <span class="comment">#类属性</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, name, age)</span>:</span></span><br><span class="line">        self.name = name  <span class="comment">#实例属性</span></span><br><span class="line">        self.age = age <span class="comment">#实例属性</span></span><br><span class="line"></span><br><span class="line">p = People(<span class="string">"XiaoWang"</span>, <span class="number">20</span>) </span><br><span class="line">p.age = <span class="number">19</span></span><br><span class="line">print(p.name)</span><br><span class="line">print(p.age)</span><br><span class="line">print(p.address)</span><br><span class="line"></span><br><span class="line">print(People.address)</span><br><span class="line"><span class="comment"># 不能通过类对象访问实例属性</span></span><br><span class="line"><span class="comment">#print(People.name)</span></span><br><span class="line"><span class="comment">#print(People.age)</span></span><br></pre></td></tr></table></figure><p>如果需要在类外修改类属性，<strong>必须通过类对象</strong>去引用然后进行修改。<br>如果通过实例对象去引用，会产生一个同名的实例属性，这种方式修改的是实例属性，不会影响到类属性，并且之后如果通过实例对象去引用该名称的属性，实例属性会强制屏蔽掉类属性，即引用的是实例属性，除非删除了该实例属性。</p><h3 id="类方法和静态方法"><a href="#类方法和静态方法" class="headerlink" title="类方法和静态方法"></a>类方法和静态方法</h3><p><strong>类方法</strong>是类对象所拥有的方法，需要用修饰器<code>@classmethod</code>来标识其为类方法<br>对于类方法，<strong>第一个参数必须是类对象</strong>，一般以cls作为第一个参数（当然可以用其他名称的变量作为其第一个参数，但是大部分人都习惯以’cls’作为第一个参数的名字，就最好用’cls’了），能够通过实例对象和类对象去访问。<br>类方法还可以对类属性进行修改。</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">People</span><span class="params">(object)</span>:</span></span><br><span class="line">    country = <span class="string">"China"</span></span><br><span class="line">    <span class="comment"># 类方法</span></span><br><span class="line"><span class="meta">    @classmethod</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">getCountry</span><span class="params">(cls)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> cls.country</span><br><span class="line"></span><br><span class="line"><span class="meta">    @classmethod</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">setCountry</span><span class="params">(cls, country)</span>:</span></span><br><span class="line">        cls.country = country</span><br><span class="line"></span><br><span class="line">p1 = People()</span><br><span class="line">p2 = People()</span><br><span class="line">print(p1.getCountry())</span><br><span class="line">print(p2.getCountry())</span><br><span class="line">print(People.getCountry())</span><br><span class="line">p1.setCountry(<span class="string">"Japan"</span>)</span><br><span class="line">print(p1.getCountry())</span><br><span class="line">print(p2.getCountry())</span><br><span class="line">print(People.getCountry())</span><br><span class="line">p2.setCountry(<span class="string">"America"</span>)</span><br><span class="line">print(p1.getCountry())</span><br><span class="line">print(p2.getCountry())</span><br><span class="line">print(People.getCountry())</span><br></pre></td></tr></table></figure><p>结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">China</span><br><span class="line">China</span><br><span class="line">China</span><br><span class="line">Japan</span><br><span class="line">Japan</span><br><span class="line">Japan</span><br><span class="line">America</span><br><span class="line">America</span><br><span class="line">America</span><br></pre></td></tr></table></figure><p>所以无论是类对象还是实例对象调用类方法，都可以操作类属性</p><p>静态方法需要用<code>@staticmethod</code>来修饰<br>静态方法不需要任何参数，cls和self也不需要，它可以直接操作类属性或类方法</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># ---- py ----</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">People</span><span class="params">(object)</span>:</span></span><br><span class="line">    country = <span class="string">'china'</span></span><br><span class="line"></span><br><span class="line"><span class="meta">    @staticmethod</span></span><br><span class="line">    <span class="comment">#静态方法</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">getCountry</span><span class="params">()</span>:</span></span><br><span class="line">        <span class="keyword">return</span> People.country</span><br><span class="line"></span><br><span class="line"><span class="keyword">print</span> People.getCountry()</span><br></pre></td></tr></table></figure><p>总结：</p><blockquote><p>从类方法和实例方法以及静态方法的定义形式就可以看出来，类方法的第一个参数是类对象cls，那么通过cls引用的必定是类对象的属性和方法；<br>而实例方法的第一个参数是实例对象self，那么通过self引用的可能是类属性、也有可能是实例属性（这个需要具体分析），不过在存在相同名称的类属性和实例属性的情况下，实例属性优先级更高。<br>静态方法中不需要额外定义参数，因此在静态方法中引用类属性的话，必须通过类对象来引用。</p></blockquote><h3 id="new-方法"><a href="#new-方法" class="headerlink" title="__new__ 方法"></a><code>__new__</code> 方法</h3><p>new方法是创建对象时会自动调用的方法，该方法至少要有一个参数cls，代表要实例化的类，此参数在实例化时由Python解释器自动提供。<br><strong>new</strong>方法必须要有返回值，返回实例化出来的实例，这点在自己实现<strong>new</strong>时要特别注意，可以直接返回有父类通过<strong>new</strong>方法创建出来的实例，或者直接是object的<strong>new</strong>方法创建出来的实例。<br><strong>new</strong>方法格式如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">__new__</span><span class="params">(cls)</span>:</span></span><br><span class="line">    ......</span><br><span class="line">    <span class="keyword">return</span> object.__new__(cls)</span><br></pre></td></tr></table></figure><p><strong>new</strong>方法和<strong>init</strong>方法是有区别的，前者是用来创建对象的，而后者是完成对象的初始化的。<br>这两者结合起来就相当于C++中的构造函数，C++中的构造函数就是完成对象的创建和初始化的。<br><strong>init</strong>有一个参数self，就是这个<strong>new</strong>返回的实例，<strong>init</strong>在<strong>new</strong>的基础上可以完成一些其它初始化的动作，<strong>init</strong>不需要返回值。<br><strong>注意</strong>：在创建并实例化一个对象的时候，是先调用<strong>new</strong>方法创建对象，然后用<strong>init</strong>方法对该对象进行实例化操作。</p><h3 id="单例"><a href="#单例" class="headerlink" title="单例"></a>单例</h3><p>确保某一个类只有一个实例，而且自行实例化并向整个系统提供这个实例，这个类称为单例类，单例模式是一种对象创建型模式。<br>单例模式可以保证一个类只能创建一个对象。<br>案例如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Singleton</span><span class="params">(object)</span>:</span></span><br><span class="line">    __instance = <span class="literal">None</span></span><br><span class="line">    __first_init = <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__new__</span><span class="params">(cls, age, name)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> cls.__instance:</span><br><span class="line">            cls.__instance = object.__new__(cls)</span><br><span class="line">        <span class="keyword">return</span> cls.__instance</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, age, name)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> self.__first_init:</span><br><span class="line">            self.age = age </span><br><span class="line">            self.name = name</span><br><span class="line">            Singleton.__first_init = <span class="literal">True</span></span><br><span class="line"></span><br><span class="line">a = Singleton(<span class="number">18</span>, <span class="string">"XiaoMing"</span>)</span><br><span class="line">b = Singleton(<span class="number">8</span>, <span class="string">"XiaoWang"</span>)</span><br><span class="line"></span><br><span class="line">print(id(a))</span><br><span class="line">print(id(b))</span><br><span class="line"></span><br><span class="line">print(a.age)</span><br><span class="line">print(b.age)</span><br><span class="line"></span><br><span class="line">a.age = <span class="number">19</span></span><br><span class="line">print(b.age)</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">139931241234897</span><br><span class="line">139931241234897</span><br><span class="line">18</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td></tr></table></figure><p>Python中的单例模式没有C++中的那么复杂，只需要重写<strong>init</strong>方法和<strong>new</strong>方法，在这两个方法中添加一个判断即可。</p><h3 id="dict-属性"><a href="#dict-属性" class="headerlink" title="__dict__ 属性"></a><code>__dict__</code> 属性</h3><p>当我们<strong>调用一个对象的属性或方法</strong>的时候，Python解释器会从对象的<strong>dict</strong>属性中找有没有这个属性/方法，没有的话会从类的<strong>dict</strong>属性中找有没有这个属性/方法。<br>那么这个属性就是存储对象或者类的属性和方法名的一个字典。<br>如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test</span><span class="params">(object)</span>:</span></span><br><span class="line">    c_pro = <span class="number">10</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.obj_pro = <span class="number">11</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>test.__dict__</span><br><span class="line">&#123;<span class="string">'obj_pro'</span>: <span class="number">11</span>&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>Test.__dict__</span><br><span class="line">mappingproxy(&#123;<span class="string">'__dict__'</span>: &lt;attribute <span class="string">'__dict__'</span> of <span class="string">'Test'</span> objects&gt;,</span><br><span class="line">              <span class="string">'__doc__'</span>: <span class="literal">None</span>,</span><br><span class="line">              <span class="string">'__init__'</span>: &lt;function test.Test.__init__&gt;,</span><br><span class="line">              <span class="string">'__module__'</span>: <span class="string">'test'</span>,</span><br><span class="line">              <span class="string">'__weakref__'</span>: &lt;attribute <span class="string">'__weakref__'</span> of <span class="string">'Test'</span> objects&gt;,</span><br><span class="line">              <span class="string">'c_pro'</span>: <span class="number">10</span>&#125;)</span><br></pre></td></tr></table></figure><h3 id="getattr-方法"><a href="#getattr-方法" class="headerlink" title="__getattr__ 方法"></a><code>__getattr__</code> 方法</h3><p>上面我们说过，调用一个对象的属性或方法的时候会先从对象的<strong>dict</strong>属性中查找，然后再从类的<strong>dict</strong>属性中查找。如果找不到呢？<br>当对象的<strong>dict</strong>属性和类的<strong>dict</strong>属性中都没有要调用的属性或方法的话，Python解释器会将这个东西扔给对象<strong>getattr</strong>方法中。<br>默认情况是产生异常，我们也可以自己去定义这样一个方法。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># ---- py ----</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="string">""""""</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">pass</span></span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__getattr__</span><span class="params">(self, item)</span>:</span></span><br><span class="line">        print(<span class="string">"%s "</span> %item,end=<span class="string">''</span>)</span><br><span class="line">        <span class="keyword">return</span> self</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__str__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">""</span></span><br></pre></td></tr></table></figure><p>输出结果如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">from</span> test <span class="keyword">import</span> Test</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(Test().think.hello.world)</span><br><span class="line">think hello world</span><br></pre></td></tr></table></figure><h3 id="面向对象综合案例"><a href="#面向对象综合案例" class="headerlink" title="面向对象综合案例"></a>面向对象综合案例</h3><p>案例描述：一人拿起枪打死敌人，敌人一共100血，一颗子弹打他15血<br>代码如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">People</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="string">"""人类"""</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, name)</span>:</span></span><br><span class="line">        super(People, self).__init__()</span><br><span class="line">        self.name = name</span><br><span class="line">        self.hp = <span class="number">100</span>    <span class="comment">#记录人的血量</span></span><br><span class="line">        self.gun = <span class="literal">None</span>  <span class="comment">#用来保存枪的引用</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">anzhuang_zidan</span><span class="params">(self, dan_jia_temp, zi_dan_temp)</span>:</span></span><br><span class="line">        <span class="string">"""将子弹安装到弹夹中"""</span></span><br><span class="line">        dan_jia_temp.baocun_zidan(zi_dan_temp)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">anzhuang_danjia</span><span class="params">(self, gun_temp, dan_jia_temp)</span>:</span></span><br><span class="line">        <span class="string">"""把弹夹安装到枪中"""</span></span><br><span class="line">        gun_temp.baocun_danjia(dan_jia_temp)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">naqiang</span><span class="params">(self, gun_temp)</span>:</span></span><br><span class="line">        <span class="string">"""拿枪"""</span></span><br><span class="line">        self.gun = gun_temp</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">kou_ban_ji</span><span class="params">(self, enermy)</span>:</span></span><br><span class="line">        <span class="string">"""让枪发射子弹打敌人"""</span></span><br><span class="line">        self.gun.fire(enermy)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">diaoxue</span><span class="params">(self, shang_hai)</span>:</span></span><br><span class="line">        <span class="string">"""根据伤害掉血"""</span></span><br><span class="line">        self.hp -= shang_hai</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__str__</span><span class="params">(self)</span>:</span></span><br><span class="line">        msg = <span class="string">"%s的血量为：%d, "</span>%(self.name, self.hp)</span><br><span class="line">        <span class="keyword">if</span> self.gun:</span><br><span class="line">            msg += <span class="string">"他有枪，%s  "</span>%(self.gun) </span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            msg += <span class="string">"他没有枪  "</span></span><br><span class="line">    </span><br><span class="line">        <span class="keyword">if</span> self.hp&lt;=<span class="number">0</span>:</span><br><span class="line">            msg += <span class="string">"死了..."</span></span><br><span class="line">        <span class="keyword">return</span> msg</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Gun</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="string">"""枪类"""</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self,name)</span>:</span></span><br><span class="line">        super(Gun, self).__init__()</span><br><span class="line">        self.name = name</span><br><span class="line">        self.danjia = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">baocun_danjia</span><span class="params">(self, dan_jia_temp)</span>:</span></span><br><span class="line">        <span class="string">"""用一个属性来保存这个弹夹的引用"""</span></span><br><span class="line">        self.danjia = dan_jia_temp</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">fire</span><span class="params">(self, enermy)</span>:</span></span><br><span class="line">        <span class="string">"""枪从弹夹中获取一颗子弹，然后这颗子弹打敌人"""</span></span><br><span class="line">        zidan_temp = self.danjia.tanchu_zidan()</span><br><span class="line">        <span class="keyword">if</span> zidan_temp:</span><br><span class="line">            zidan_temp.dazhong(enermy)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            print(<span class="string">"Not found zidan..."</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__str__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> self.danjia:</span><br><span class="line">            <span class="keyword">return</span> <span class="string">"枪的信息为：%s, 弹夹信息为：%s"</span>%(self.name, self.danjia)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="string">""</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DanJia</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="string">"""弹夹类"""</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, max_num)</span>:</span></span><br><span class="line">        super(DanJia, self).__init__()</span><br><span class="line">        self.max_num = max_num  <span class="comment">#记录弹夹的最大装弹数</span></span><br><span class="line">        self.zidan_list = []    <span class="comment">#用来记录所有子弹的引用</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">baocun_zidan</span><span class="params">(self, zi_dan_temp)</span>:</span></span><br><span class="line">        <span class="string">"""将这颗子弹压到弹夹中"""</span></span><br><span class="line">        self.zidan_list.append(zi_dan_temp)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">tanchu_zidan</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""弹出最上面的一颗子弹"""</span></span><br><span class="line">        <span class="keyword">if</span> self.zidan_list:</span><br><span class="line">            <span class="keyword">return</span> self.zidan_list.pop()</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__str__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">"弹夹信息为：%d/%d"</span>%(len(self.zidan_list), self.max_num)</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ZiDan</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="string">"""子弹"""</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, shang_hai)</span>:</span></span><br><span class="line">        super(ZiDan, self).__init__()</span><br><span class="line">        self.shang_hai = shang_hai  <span class="comment">#记录子弹威力</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">dazhong</span><span class="params">(self, enermy)</span>:</span></span><br><span class="line">        <span class="string">"""让人掉血"""</span></span><br><span class="line">        enermy.diaoxue(self.shang_hai)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="comment"># 1 创建奥巴马</span></span><br><span class="line">    obama = People(<span class="string">"奥巴马"</span>)</span><br><span class="line">    <span class="comment"># 2 创建一把枪</span></span><br><span class="line">    ak47 = Gun(<span class="string">"AK47"</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 3 创建一个弹夹</span></span><br><span class="line">    dan_jia = DanJia(<span class="number">10</span>)</span><br><span class="line">    <span class="comment"># 4 创建一些子弹</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">8</span>):</span><br><span class="line">        zi_dan = ZiDan(<span class="number">15</span>)</span><br><span class="line">        <span class="comment"># 5 奥巴马将子弹装进弹夹中</span></span><br><span class="line">        obama.anzhuang_zidan(dan_jia, zi_dan)</span><br><span class="line">    <span class="comment">#print(dan_jia)</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 6 奥巴马将弹夹装进枪中</span></span><br><span class="line">    obama.anzhuang_danjia(ak47, dan_jia)</span><br><span class="line">    <span class="comment">#print(ak47)</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 7 奥巴马拿枪</span></span><br><span class="line">    obama.naqiang(ak47)</span><br><span class="line">    <span class="comment">#print(obama)</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 8 创建敌人锤石</span></span><br><span class="line">    chui_shi = People(<span class="string">"锤石"</span>)</span><br><span class="line">    print(chui_shi)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 9 奥巴马开枪打锤石</span></span><br><span class="line">    obama.kou_ban_ji(chui_shi)</span><br><span class="line">    print(obama)</span><br><span class="line">    print(chui_shi)</span><br><span class="line">    obama.kou_ban_ji(chui_shi)</span><br><span class="line">    print(obama)</span><br><span class="line">    print(chui_shi)</span><br><span class="line">    obama.kou_ban_ji(chui_shi)</span><br><span class="line">    print(obama)</span><br><span class="line">    print(chui_shi)</span><br><span class="line">    obama.kou_ban_ji(chui_shi)</span><br><span class="line">    print(obama)</span><br><span class="line">    print(chui_shi)</span><br><span class="line">    obama.kou_ban_ji(chui_shi)</span><br><span class="line">    print(obama)</span><br><span class="line">    print(chui_shi)</span><br><span class="line">    obama.kou_ban_ji(chui_shi)</span><br><span class="line">    print(obama)</span><br><span class="line">    print(chui_shi)</span><br><span class="line">    obama.kou_ban_ji(chui_shi)</span><br><span class="line">    print(obama)</span><br><span class="line">    print(chui_shi)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__  == <span class="string">"__main__"</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure><h2 id="异常"><a href="#异常" class="headerlink" title="异常"></a>异常</h2><p>当Python检测到一个错误时，解释器就无法继续执行了，反而出现了一些错误的提示，这就是所谓的<strong>异常</strong>。<br>在Python程序的执行过程中，通常出现异常的时候程序就会直接崩掉，而我们不期待程序直接崩掉，则需要捕获异常。</p><h3 id="捕获异常"><a href="#捕获异常" class="headerlink" title="捕获异常"></a>捕获异常</h3><p>在Python中捕获异常的方式为<code>try...except...</code>，格式如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    可能出错的代码块</span><br><span class="line">    ......</span><br><span class="line"><span class="keyword">except</span> 异常名:</span><br><span class="line">    出现异常后执行的操作</span><br></pre></td></tr></table></figure><p>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    print(<span class="string">"----1----"</span>)</span><br><span class="line">    open(<span class="string">"xxx.txt"</span>, <span class="string">"r"</span>)</span><br><span class="line">    print(<span class="string">"----2----"</span>)</span><br><span class="line"><span class="keyword">except</span> FileNotFoundError:</span><br><span class="line">    print(<span class="string">"The FILE is not exist"</span>)</span><br><span class="line">print(<span class="string">"----3----"</span>)</span><br></pre></td></tr></table></figure><p>出现异常之前的代码会正常执行，到异常的时候会捕获异常，然后异常之后的代码不会执行，直接跳转到捕获异常的except语句中去</p><p>except还可以捕获多个异常，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    print(<span class="string">'----1----'</span>)</span><br><span class="line">    open(<span class="string">'123.txt'</span>,<span class="string">'r'</span>) <span class="comment"># 如果123.txt文件不存在，那么会产生 IOError 异常</span></span><br><span class="line">    print(<span class="string">'----2----'</span>)</span><br><span class="line">    print(num)<span class="comment"># 如果num变量没有定义，那么会产生 NameError 异常</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">except</span> (FileNotFoundError,NameError) <span class="keyword">as</span> errorMsg: </span><br><span class="line">    <span class="comment">#如果想通过一次except捕获到多个异常可以用一个元组的方式</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># errorMsg里会保存捕获到的错误信息</span></span><br><span class="line">    print(errorMsg)</span><br><span class="line"></span><br><span class="line">print(<span class="string">"----3----"</span>)</span><br></pre></td></tr></table></figure><h3 id="捕获所有异常"><a href="#捕获所有异常" class="headerlink" title="捕获所有异常"></a>捕获所有异常</h3><p>如果except语句后不跟异常的时候，except就可以捕获<strong>任何异常</strong>，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    open(<span class="string">"xxx"</span>)</span><br><span class="line"><span class="keyword">except</span>:</span><br><span class="line">    print(<span class="string">"Error"</span>)</span><br></pre></td></tr></table></figure><p>但是这样就不能保存异常信息，所以还可以用Exception来接收所有的异常，这种方式可以保存异常的基本信息，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    open(<span class="string">"xxx"</span>)</span><br><span class="line"><span class="keyword">except</span> Exception <span class="keyword">as</span> errorMsg:</span><br><span class="line">    print(errorMsg)</span><br></pre></td></tr></table></figure><h3 id="else"><a href="#else" class="headerlink" title="else"></a>else</h3><p>咱们应该对else并不陌生，在if中，它的作用是当条件不满足时执行的实行；<br>同样在try…except…中也是如此，即如果没有捕获到异常，那么就执行else中的事情<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    <span class="comment">#num = 100</span></span><br><span class="line">    print(num)</span><br><span class="line"><span class="keyword">except</span> NameError <span class="keyword">as</span> errorMsg:</span><br><span class="line">    print(errorMsg)</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    print(<span class="string">'NO ERROR, Happy! ^_^'</span>)</span><br></pre></td></tr></table></figure><h3 id="finally"><a href="#finally" class="headerlink" title="finally"></a>finally</h3><p>在程序中，如果一个段代码必须要执行，即无论异常是否产生都要执行，那么此时就需要使用finally。<br>比如文件关闭，释放锁，把数据库连接返还给连接池等。</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> time</span><br><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    f = open(<span class="string">'test.txt'</span>)</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            content = f.readline()</span><br><span class="line">            <span class="keyword">if</span> len(content) == <span class="number">0</span>:</span><br><span class="line">                <span class="keyword">break</span></span><br><span class="line">            time.sleep(<span class="number">2</span>)</span><br><span class="line">            print(content)</span><br><span class="line">    <span class="keyword">except</span>:</span><br><span class="line">        <span class="comment">#如果在读取文件的过程中，产生了异常，那么就会捕获到</span></span><br><span class="line">        <span class="comment">#比如 按下了 ctrl+c</span></span><br><span class="line">        <span class="keyword">pass</span></span><br><span class="line">    <span class="keyword">finally</span>:</span><br><span class="line">        f.close()</span><br><span class="line">        print(<span class="string">'关闭文件'</span>)</span><br><span class="line"><span class="keyword">except</span>:</span><br><span class="line">    print(<span class="string">"没有这个文件"</span>)</span><br><span class="line"><span class="comment"># ---- py ----</span></span><br></pre></td></tr></table></figure><p>其实最后需要执行的语句放在try语句外边，也能达到finally的效果。</p><h3 id="异常的传递"><a href="#异常的传递" class="headerlink" title="异常的传递"></a>异常的传递</h3><p>如果try嵌套，那么如果里面的try没有捕获到这个异常，那么外面的try会接收到这个异常，然后进行处理，如果外边的try依然没有捕获到，那么再进行传递。<br>如果一个异常是在一个函数中产生的，例如函数A—-&gt;函数B—-&gt;函数C,而异常是在函数C中产生的，那么如果函数C中没有对这个异常进行处理，那么这个异常会传递到函数B中，如果函数B有异常处理那么就会按照函数B的处理方式进行执行；如果函数B也没有异常处理，那么这个异常会继续传递，以此类推。如果所有的函数都没有处理，那么此时就会进行异常的默认处理，即通常见到的那样</p><h3 id="自定义异常"><a href="#自定义异常" class="headerlink" title="自定义异常"></a>自定义异常</h3><p><strong>异常也是一种类</strong>，而Exception就是所有异常类的一个基类。<br>因此可以用Exception捕获所有的异常，同时也能通过创建一个子类继承Exception类来自定义异常类。<br>有类自然就有对象，捕获的异常都是某个异常类的对象，而这些异常类都是Exception的子类，因此可以通过父类Exception来接收子类的对象，这是一种多态的体现。我们可以用raise关键字来抛出异常对象，来达到自定义异常的效果。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ShortInputException</span><span class="params">(Exception)</span>:</span></span><br><span class="line">    <span class="string">'''自定义的异常类'''</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, length, atleast)</span>:</span></span><br><span class="line">        super().__init__()</span><br><span class="line">        self.length = length</span><br><span class="line">        self.atleast = atleast</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        s = input(<span class="string">'请输入：'</span>)</span><br><span class="line">        <span class="keyword">if</span> len(s) &lt; <span class="number">3</span>:</span><br><span class="line">       	 <span class="comment"># raise引发一个你定义的异常</span></span><br><span class="line">       	 <span class="keyword">raise</span> ShortInputException(len(s), <span class="number">3</span>)</span><br><span class="line">    <span class="keyword">except</span> ShortInputException <span class="keyword">as</span> result:  <span class="comment">#result这个变量被绑定到了错误的实例，也就相当于引用传参</span></span><br><span class="line">        print(<span class="string">'输入的长度是 %d,长度至少应是 %d'</span>% (result.length, result.atleast))</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        print(<span class="string">'没有异常发生.'</span>)</span><br><span class="line"></span><br><span class="line">main()</span><br></pre></td></tr></table></figure><p>关于raise语句，如果raise在except语句中，可以重新抛出捕获的异常，并且不会再被此except捕获，从而会触发默认的处理机制，即崩掉。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    <span class="comment">#num = 100</span></span><br><span class="line">    print(num)</span><br><span class="line"><span class="keyword">except</span> NameError <span class="keyword">as</span> errorMsg:</span><br><span class="line">    print(<span class="string">"------"</span>)</span><br><span class="line">    <span class="keyword">raise</span></span><br></pre></td></tr></table></figure><h2 id="模块"><a href="#模块" class="headerlink" title="模块"></a>模块</h2><p>有过C语言编程经验的朋友都知道在C语言中如果要引用sqrt函数，必须用语句<code>#include &lt;math.h&gt;</code>引入math.h这个头文件，否则是无法正常进行调用的。<br>那么在Python中，如果要引用一些其他的函数，该怎么处理呢？<br>在Python中有一个概念叫做<strong>模块（module）</strong>，这个和C语言中的头文件以及Java中的包很类似，比如在Python中要调用sqrt函数，必须用import关键字引入math这个模块，下面就来了解一下Python中的模块。<br>说的通俗点：模块就好比是工具包，要想使用这个工具包中的工具(就好比函数)，就需要导入这个模块</p><h3 id="模块的导入"><a href="#模块的导入" class="headerlink" title="模块的导入"></a>模块的导入</h3><p>在Python中用关键字import来引入某个模块，比如要引用模块math，就可以在文件最开始的地方用import math来引入。<br>格式如下：<code>import 模块名1, 模块名2, ...</code><br>当解释器遇到import语句时，如果模块在当前的搜索路径就会被导入。<br>在调用math模块中的函数时，必须这样调用：<code>模块名.函数名</code><br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> math</span><br><span class="line"></span><br><span class="line">print(math.sqrt(<span class="number">2</span>))</span><br></pre></td></tr></table></figure><p>有时候我们只需要用到模块中的某个函数，只需要引入该函数即可，此时可以用下面方法实现：<br><code>from 模块名 import 函数名1, 函数名2, ...</code><br>通过这样的方法，不仅可以引入函数，还可以引入一些全局变量、类等<br><strong>注意</strong>：通过这种方式引入的时候，调用函数时只能给出函数名，不能给出模块名，但是当两个模块中含有相同名称函数的时候，后面一次引入会覆盖前一次引入。也就是说假如模块A中有函数function()，在模块B中也有函数function()，如果引入A中的function在先、B中的function在后，那么当调用function函数的时候，是去执行模块B中的function函数。<br>如果想通过from一次性引入某个模块中所有的东西，可以使用通配符，格式如下：<br><code>from 模块名 import *</code><br>from语句让你从模块中导入一个指定的部分到当前的命名空间中，from不会把整个模块导入到命名空间，只会将指定的部分导入到命名空间。通过from语句导入的模块，调用其函数时不需要通过模块名来调用。</p><h3 id="as"><a href="#as" class="headerlink" title="as"></a>as</h3><p>as语句能将导入的模块重新命名，格式如下：<br><code>import 模块名 as NAME</code><br>如果使用了as关键字，以前的模块名将不能被使用，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> time <span class="keyword">as</span> tt</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>time.sleep(<span class="number">2</span>)</span><br><span class="line">Traceback (most recent call last):</span><br><span class="line">  File <span class="string">"&lt;stdin&gt;"</span>, line <span class="number">1</span>, <span class="keyword">in</span> &lt;module&gt;</span><br><span class="line">NameError: name <span class="string">'time'</span> <span class="keyword">is</span> <span class="keyword">not</span> defined</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>tt.sleep(<span class="number">2</span>)</span><br><span class="line">&gt;&gt;&gt;</span><br></pre></td></tr></table></figure><h3 id="定位模块"><a href="#定位模块" class="headerlink" title="定位模块"></a>定位模块</h3><p>当你导入一个模块，Python解析器对模块位置的搜索顺序是：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">1、当前目录</span><br><span class="line">2、如果不在当前目录，Python则搜索在shell变量PYTHONPATH下的每个目录。</span><br><span class="line">3、如果都找不到，Python会察看默认路径。默认路径一般为&#x2F;usr&#x2F;local&#x2F;lib&#x2F;python&#x2F;</span><br><span class="line">4、模块搜索路径存储在system模块的sys.path变量中。变量里包含当前目录，PYTHONPATH和由安装过程决定的默认目录。</span><br></pre></td></tr></table></figure><h3 id="模块制作"><a href="#模块制作" class="headerlink" title="模块制作"></a>模块制作</h3><p>我们可以自己制作模块，在Python中，每个Python文件都可以作为一个模块，模块的名字就是文件的名字。如有一个模块test.py，其中有一个add函数：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">add</span><span class="params">(a,b)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> a+b</span><br></pre></td></tr></table></figure><p>然后我们就可以导入自己的模块了。如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> test</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(test.add(<span class="number">11</span>,<span class="number">33</span>))        </span><br><span class="line"><span class="number">44</span></span><br></pre></td></tr></table></figure><h3 id="模块的安装"><a href="#模块的安装" class="headerlink" title="模块的安装"></a>模块的安装</h3><ul><li><p><strong>通过pip安装</strong><br>如：<code>pip install ***</code></p></li><li><p><strong>通过源码安装</strong><br>下载源码包解压之后，其中会有一个<code>setup.py</code>文件，可以通过<code>python setup.py install</code>命令来安装模块</p></li><li><p><strong>通过whl文件安装</strong><br>可以直接下载whl文件，然后使用<code>pip install ***.whl</code>来进行安装</p></li></ul><h3 id="name"><a href="#name" class="headerlink" title="__name__"></a><code>__name__</code></h3><p>Python在执行一个文件的时候有一个变量<code>__name__</code>，它可以记录此模块是被调用成模块还是直接执行的。如果是直接执行（就是直接用python命令来执行这个文件）的，这个变量保存的值就是<code>__main__</code>，如果是通过模块导入（注意，导入模块的时候会将该模块中的代码全部执行一遍）的，那么这个变量保存的值就是那个包的名字。<br>如有一个模块test.py：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">print(__name__)</span><br></pre></td></tr></table></figure><p>直接使用python3 test.py输出的结果为：<code>__main__</code><br>如果导入test模块，输出的结果为：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> test</span><br><span class="line">test</span><br></pre></td></tr></table></figure><p>因此我们可以通过<code>__name__</code>变量判断模块是否是被导入模块，并且可以在模块中加个判断语句来加入某些调试信息。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">add</span><span class="params">(a,b)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> a+b </span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    print(add(<span class="number">11</span>,<span class="number">22</span>))</span><br></pre></td></tr></table></figure><p>这样，如果是直接通过python命令执行这个模块的时候就可以打印调试信息，而通过模块导入的时候就不会打印调试信息。</p><h3 id="all"><a href="#all" class="headerlink" title="__all__"></a><code>__all__</code></h3><p>当一个模块中有<code>__all__</code>变量的时候，这个变量就存储了可被导入的函数或类。<br><code>__all__</code>变量中没有的函数或其他东西，就不会被导入。<br>没有定义<code>__all__</code>时的默认状态是导入模块中所有的东西，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">(self)</span>:</span></span><br><span class="line">        print(<span class="string">"----Test.test----"</span>)</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test1</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">"----test1----"</span>)</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test2</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">"----test2----"</span>)</span><br></pre></td></tr></table></figure><p>导入模块的情况如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">from</span> test <span class="keyword">import</span> *</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = Test()</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a.test()</span><br><span class="line">----Test.test----</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>test1()</span><br><span class="line">----test1----</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>test2()</span><br><span class="line">----test2----</span><br></pre></td></tr></table></figure><p>如果在定义模块的时候定义了<code>__all__</code>变量，就可以指定全部导入的时候哪些东西会被导入进来。（<strong>注意：这里的导入指的是用<code>from ... import *</code>进行的导入，不包括import …导入</strong>）</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">__all__ = [<span class="string">"Test"</span>, <span class="string">"test1"</span>]</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">(self)</span>:</span></span><br><span class="line">        print(<span class="string">"----Test.test----"</span>)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test1</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">"----test1----"</span>)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test2</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">"----test2----"</span>)</span><br></pre></td></tr></table></figure><p>导入之后由于<code>__all__</code>变量中没有test2，因此就不能执行test2函数</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">from</span> test <span class="keyword">import</span> *</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = Test()</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a.test()</span><br><span class="line">----Test.test---</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>test1()</span><br><span class="line">----test1----</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>test2()</span><br><span class="line">NameError</span><br></pre></td></tr></table></figure><h3 id="包"><a href="#包" class="headerlink" title="包"></a>包</h3><p>某些具有相同作用的模块可以放进一个目录下，这个目录就是一个包，我们可以将这个包（也就是目录）直接导入，以获取目录下中模块的功能。<br>如有2个模块recvmsg.py、sendmsg.py，将这两个模块放入同一目录msg下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.</span><br><span class="line">└── msg</span><br><span class="line">    ├── recvmsg.py</span><br><span class="line">    └── sendmsg.py</span><br></pre></td></tr></table></figure><p>然后我们可以通过import 目录名.模块 的方式导入，如：<br><code>import msg.recvmsg</code><br>然后就通过msg.recvmsg.函数()来对模块中的内容进行调用</p><p>我们也可以通过<code>from 目录 import *</code>的方式导入目录下的所有模块，但是需要目录下有<code>__init__.py</code>文件，这个文件控制着包的导入行为。<br>如果这个文件为空，那就仅仅把包导入，不会导入其中的模块。<br>如果要导入包中的模块，还需要在<code>__init__.py</code>文件中加一个<code>__all__</code>变量，写上将哪些模块导入，这样通过<code>from 目录 import *</code>的方式导入包的时候才能将包下的模块成功导入。这个<code>__all__</code>变量的用法和模块中<code>__all__</code>变量的用法一样。<br>通过<code>from 目录 import *</code>的方式导入包中的模块之后，调用就使用模块名.函数名进行调用。<br><code>__init__.py</code>中还可以写上其他的语句，这些语句也能在包被导入的时候被执行（事实上这个<code>__init__.py</code>也相当于一个模块），如果<code>__init__.py</code>中定义了某些函数的时候，调用可以通过包名.函数()来进行调用。</p><p>包还可以嵌套的，如我们的包有如下目录结构：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Phone&#x2F;</span><br><span class="line">    __init__.py</span><br><span class="line">    common_util.py</span><br><span class="line">    Voicedta&#x2F;</span><br><span class="line">        __init__.py</span><br><span class="line">        Pots.py</span><br><span class="line">        Isdn.py</span><br><span class="line">    Fax&#x2F;</span><br><span class="line">        __init__.py</span><br><span class="line">        G3.py</span><br><span class="line">    Mobile&#x2F;</span><br><span class="line">        __init__.py</span><br><span class="line">        Analog.py</span><br><span class="line">        igital.py</span><br><span class="line">    Pager&#x2F;</span><br><span class="line">        __init__.py</span><br><span class="line">        Numeric.py</span><br></pre></td></tr></table></figure><p>Phone是最顶层的包，Voicedta等是它的子包。我们可以这样导入子包：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> Phone.Mobile.Analog</span><br><span class="line">Phone.Mobile.Analog.dial()</span><br></pre></td></tr></table></figure><p>也可使用 from-import 实现不同需求的导入。<br>第一种方法是只导入顶层的子包，然后使用属性/点操作符向下引用子包树：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> Phone <span class="keyword">import</span> Mobile</span><br><span class="line">Mobile.Analog.dial(<span class="string">'555-1212'</span>)</span><br></pre></td></tr></table></figure><p>此外，我们可以还引用更多的子包：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> Phone.Mobile <span class="keyword">import</span> Analog</span><br><span class="line">Analog.dial(<span class="string">'555-1212'</span>)</span><br></pre></td></tr></table></figure><p>事实上，可以一直沿子包的树状结构导入：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> Phone.Mobile.Analog <span class="keyword">import</span> dial</span><br><span class="line">dial(<span class="string">'555-1212'</span>)</span><br></pre></td></tr></table></figure><blockquote><p>在我们上边的目录结构中，我们可以发现很多的<code>__init__.py</code>文件。<br>这些是初始化模块，from-import 语句导入子包时需要用到它。<br>如果没有用到，他们可以是空文件。</p></blockquote><p>包同样支持 from-import all 语句：<br><code>from package.module import *</code><br>然而，这样的语句会导入哪些文件取决于操作系统的文件系统。<br>所以我们在<code>__init__.py</code>中加入<code>__all__</code>变量。<br>该变量包含执行这样的语句时应该导入的模块的名字。它由一个模块名字符串列表组成。</p><p><strong>注意</strong>：如果要兼容Python2和Python3，<code>__init__.py</code>下如果想导入模块要这样写：<code>from . import 模块</code></p><h3 id="模块发布"><a href="#模块发布" class="headerlink" title="模块发布"></a>模块发布</h3><p>mymodule目录结构体如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.</span><br><span class="line">├── setup.py</span><br><span class="line">├── suba</span><br><span class="line">│   ├── aa.py</span><br><span class="line">│   ├── bb.py</span><br><span class="line">│   └── __init__.py</span><br><span class="line">└── subb</span><br><span class="line">    ├── cc.py</span><br><span class="line">    ├── dd.py</span><br><span class="line">    └── __init__.py</span><br></pre></td></tr></table></figure><p>然后需要编辑<code>setup.py</code>文件。其中<code>py_modules</code>参数需指明所需包含的py文件</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> distutils.core <span class="keyword">import</span> setup</span><br><span class="line"></span><br><span class="line">setup(name=<span class="string">"dongGe"</span>, version=<span class="string">"1.0"</span>, description=<span class="string">"dongGe's module"</span>, author=<span class="string">"dongGe"</span>, py_modules=[<span class="string">'suba.aa'</span>, <span class="string">'suba.bb'</span>, <span class="string">'subb.cc'</span>, <span class="string">'subb.dd'</span>])</span><br></pre></td></tr></table></figure><p>构建模块，通过如下命令：<br><code>python setup.py build</code><br>然后生成发布压缩包，通过如下命令：<br><code>python setup.py sdist</code><br>打包后会生成最终发布压缩包，目录结构如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.</span><br><span class="line">├── build</span><br><span class="line">│   └── lib.linux-i686-2.7</span><br><span class="line">│       ├── suba</span><br><span class="line">│       │   ├── aa.py</span><br><span class="line">│       │   ├── bb.py</span><br><span class="line">│       │   └── __init__.py</span><br><span class="line">│       └── subb</span><br><span class="line">│           ├── cc.py</span><br><span class="line">│           ├── dd.py</span><br><span class="line">│           └── __init__.py</span><br><span class="line">├── dist</span><br><span class="line">│   └── dongGe-1.0.tar.gz</span><br><span class="line">├── MANIFEST</span><br><span class="line">├── setup.py</span><br><span class="line">├── suba</span><br><span class="line">│   ├── aa.py</span><br><span class="line">│   ├── bb.py</span><br><span class="line">│   └── __init__.py</span><br><span class="line">└── subb</span><br><span class="line">    ├── cc.py</span><br><span class="line">    ├── dd.py</span><br><span class="line">    └── __init__.py</span><br></pre></td></tr></table></figure><h3 id="模块的安装和使用"><a href="#模块的安装和使用" class="headerlink" title="模块的安装和使用"></a>模块的安装和使用</h3><p>安装的方式：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">1、找到模块的压缩包</span><br><span class="line">2、解压</span><br><span class="line">3、进入文件夹</span><br><span class="line">4、执行命令python setup.py install</span><br></pre></td></tr></table></figure><p><strong>注意</strong>：<br>如果在install的时候，执行目录安装，可以使用<code>python setup.py install --prefix=安装路径</code><br>安装完之后就可以通过import或from…import在程序中导入安装的模块</p><h3 id="给程序传递参数"><a href="#给程序传递参数" class="headerlink" title="给程序传递参数"></a>给程序传递参数</h3><p>在上一节看到python setup.py install命令，其中的install就是参数，那么我们要怎么给程序传递参数呢？<br>要获取给程序传递的参数，首先要导入一个模块sys，然后模块中有个变量名为argv，这个变量是一个列表，第一个元素(也就是argv[0])为程序名本身，之后的参数就是我们执行程序时给的参数了。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> sys </span><br><span class="line">print(sys.argv)</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># python3 test.py 1 2 3 4 5</span><br><span class="line">[&#39;test.py&#39;, &#39;1&#39;, &#39;2&#39;, &#39;3&#39;, &#39;4&#39;, &#39;5&#39;]</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>Python 进阶</title>
    <url>/python/8541a389-77d7-45ce-a30a-187778c6725a.html</url>
    <content><![CDATA[<p>Python是一种跨平台的计算机程序设计语言。 是一个高层次的结合了解释性、编译性、互动性和面向对象的脚本语言。最初被设计用于编写自动化脚本(shell)，随着版本的不断更新和语言新功能的添加，越多被用于独立的、大型项目的开发。本文介绍 Python 的核心内容。</p><a id="more"></a><h2 id="高级"><a href="#高级" class="headerlink" title="高级"></a>高级</h2><h3 id="import"><a href="#import" class="headerlink" title="import"></a>import</h3><ul><li><p><strong>搜索顺序</strong><br>在基础篇中的模块导入有个搜索顺序，这个顺序是可以查看的<br>如果想查看这个搜索顺序，首先导入sys模块，这个模块中有个<strong>path</strong><br>这个值就是以列表形式保存了所有的搜索路径<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> sys</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>sys.path</span><br><span class="line">[<span class="string">''</span>, <span class="string">'/usr/lib/python35.zip'</span>, <span class="string">'/usr/lib/python3.5'</span>, <span class="string">'/usr/lib/python3.5/plat-x86_64-linux-gnu'</span>, <span class="string">'/usr/lib/python3.5/lib-dynload'</span>, <span class="string">'/usr/local/lib/python3.5/dist-packages'</span>, <span class="string">'/usr/lib/python3/dist-packages'</span>]</span><br></pre></td></tr></table></figure><p>这个搜索路径是有顺序的，按<strong>从上到下</strong>的顺序来搜索，最先搜索的是当前路径，然后依次往下搜索<br>既然这个sys.path是以列表的形式保存的，那么这个列表也是可以被<strong>增加和修改</strong>的，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">sys.path.append(<span class="string">'/home/xxx'</span>)</span><br><span class="line">sys.path.insert(<span class="number">0</span>, <span class="string">'/home/xxx'</span>)</span><br></pre></td></tr></table></figure><p>但是在执行的时候会这样，执行完毕后重新导入这个模块之后会恢复原样</p></li><li><p><strong>模块的重载</strong><br>在导入一个模块之后，程序仍在进行，如果对这个模块进行了修改那么在程序中是不会发生改动的。并且使用import也不能让模块重新导入。<br>这个时候需要重新加载模块，重新加载模块需要导入imp包中的reload模块<br>使用方法如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> imp <span class="keyword">import</span> reload</span><br><span class="line">reload(需要重载的模块)</span><br></pre></td></tr></table></figure><p>例如我首先在test中写一个函数，然后在交互式界面中导入test模块执行函数打印1，然后修改函数中的打印值为2，重载模块，再调用模块中的打印函数：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> test <span class="keyword">as</span> t</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>t.test()</span><br><span class="line">---<span class="number">-1</span>----</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">from</span> imp <span class="keyword">import</span> reload</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>reload(t)</span><br><span class="line">&lt;module <span class="string">'test'</span> <span class="keyword">from</span> <span class="string">'/root/pythonSource/test.py'</span>&gt;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>t.test()              </span><br><span class="line">---<span class="number">-2</span>----</span><br><span class="line">&gt;&gt;&gt;</span><br></pre></td></tr></table></figure></li></ul><h3 id="is-和"><a href="#is-和" class="headerlink" title="is 和 =="></a>is 和 ==</h3><p><code>is</code>和<code>==</code>是有区别的，其中<code>is</code>是比较2个引用是否指向了同一块内存空间，而<code>==</code>是比较两个对象是否相等<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = a</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b <span class="keyword">is</span> a</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b == a</span><br><span class="line"><span class="literal">True</span></span><br></pre></td></tr></table></figure><p>之中的a和b是指向2个引用，指向同一个列表，因此用<code>is</code>还是<code>==</code>返回的都是True<br>又如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a <span class="keyword">is</span> b</span><br><span class="line"><span class="literal">False</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a == b</span><br><span class="line"><span class="literal">True</span></span><br></pre></td></tr></table></figure><p>之中的a和b是分别创建了一个列表，所以这2者不是同一个列表，因此用<code>is</code>会返回False，但由于两者的值是相等的，所以<code>==</code>返回的是True<br>另外：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = <span class="number">257</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = <span class="number">257</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a <span class="keyword">is</span> b</span><br><span class="line"><span class="literal">False</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = <span class="number">256</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = <span class="number">256</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a <span class="keyword">is</span> b</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = <span class="number">-6</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = <span class="number">-6</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a <span class="keyword">is</span> b</span><br><span class="line"><span class="literal">False</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = <span class="number">-5</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = <span class="number">-5</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a <span class="keyword">is</span> b</span><br><span class="line"><span class="literal">True</span></span><br></pre></td></tr></table></figure><p>从-6~256这些整数是固定空间的，不会重新去开辟新的空间。<br>因此用变量保存这些值的时候都是直接返回的引用，不会开辟新的空间然后返回引用。<br>总结：如果<code>==</code>返回的是True，那么<code>is</code>返回的不一定是True；如果<code>is</code>返回的是True，那么<code>==</code>返回的一定的True</p><h3 id="深拷贝和浅拷贝"><a href="#深拷贝和浅拷贝" class="headerlink" title="深拷贝和浅拷贝"></a>深拷贝和浅拷贝</h3><ul><li><p><strong>浅拷贝</strong><br>浅拷贝是对于一个对象的顶层拷贝，也就是说浅拷贝只是拷贝了引用，并没有拷贝内容。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = a</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a)</span><br><span class="line"><span class="number">140023438544520</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(b)</span><br><span class="line"><span class="number">140023438544520</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a.append(<span class="number">4</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b</span><br><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>]</span><br></pre></td></tr></table></figure><p>这就是一个浅拷贝，只是拷贝了引用，这两者还是指向同一内存，一个修改另一个会跟着修改</p></li><li><p><strong>深拷贝</strong><br>深拷贝是对于一个对象所有层次的拷贝，会<strong>递归拷贝</strong><br>想要进行深拷贝的时候需要导入一个<strong>copy模块</strong><br>这个模块中有个函数为<strong>deepcopy</strong>，就是用来进行深拷贝操作的函数。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> copy</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = [<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>c = [a,b]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>d = copy.deepcopy(c)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a.append(<span class="number">4</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>c</span><br><span class="line">[[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>], [<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>d</span><br><span class="line">[[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>], [<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]]</span><br></pre></td></tr></table></figure><p>这里首先创建了a、b两个列表，然后创建一个列表c用来保存a、b两个引用。<br>之后深拷贝了列表c（里面的2个引用被递归进行深拷贝）给d。<br>之后给a列表添加了元素，这样保存a引用的c列表中的元素也会跟着改变，而进行了深拷贝的列表d中的元素就没有改变。<br>这里的c换成元组也是一样的结果：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = [<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>c = (a,b)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>d = copy.deepcopy(c)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a.append(<span class="number">4</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>c</span><br><span class="line">([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>], [<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>])</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>d</span><br><span class="line">([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>], [<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>])</span><br></pre></td></tr></table></figure></li><li><p><strong>copy</strong><br>copy这个函数也需要导入copy模块，这个函数也是一种<strong>浅拷贝</strong>，和引用赋值的区别为copy可以拷贝第一层的空间。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> copy</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = [<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>c = [a,b]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>d = copy.copy(c)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(c)</span><br><span class="line"><span class="number">140417487393544</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(d)</span><br><span class="line"><span class="number">140417523625224</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a.append(<span class="number">4</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>c</span><br><span class="line">[[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>], [<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>d</span><br><span class="line">[[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>], [<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]]</span><br></pre></td></tr></table></figure><p>虽然用copy是重新开辟了一片空间，但是存放的仍然是a、b两个列表的引用，因此a、b改动的时候d还是会跟着改变，因此copy也是一种浅拷贝。<br>另外，copy对可变类型和不可变类型的拷贝也不相同，用copy拷贝可变类型会重新开辟空间；而对于不可变类型，copy是直接返回的引用。如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> copy</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = copy.copy(a)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a)</span><br><span class="line"><span class="number">140417486682888</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(b)</span><br><span class="line"><span class="number">140417487526408</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>c = (<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>d = copy.copy(c)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(c)</span><br><span class="line"><span class="number">140417523595232</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(d)</span><br><span class="line"><span class="number">140417523595232</span></span><br></pre></td></tr></table></figure><p>字典是自带一个copy函数的，可以完成类似于copy的一个操作（不会递归拷贝），如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = &#123;<span class="string">"list"</span>:a, <span class="string">"age"</span>:<span class="number">16</span>&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>c = b.copy()</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(b)</span><br><span class="line"><span class="number">139654870186632</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(c)</span><br><span class="line"><span class="number">139654869678472</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a.append(<span class="number">5</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b</span><br><span class="line">&#123;<span class="string">'age'</span>: <span class="number">16</span>, <span class="string">'list'</span>: [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">5</span>]&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>c</span><br><span class="line">&#123;<span class="string">'age'</span>: <span class="number">16</span>, <span class="string">'list'</span>: [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">5</span>]&#125;</span><br></pre></td></tr></table></figure></li></ul><h3 id="进制和位运算"><a href="#进制和位运算" class="headerlink" title="进制和位运算"></a>进制和位运算</h3><p>数据在机器中的存储是以二进制的方式存储的，关于进制这里不多谈。<br>计算机中的运算是以二进制方式进行的，因此有几个概念需要了解：<strong>原码、反码、补码</strong>。</p><blockquote><p>对于正数而言，原码、反码和补码都是相同的；<br>而对于负数而言，反码就是符号位（对于有符号数来说，最高为就是符号为，为0是正数，为1是负数）不变，其他为取反，补码就是反码+1。</p></blockquote><p>如果要让补码转化为原码，符号位不变，其余位取反+1即可<br><strong>注意：原码转补码和补码转原码是一样的方式，都是符号位不变，其余位取反再加1即可</strong><br>进制间转换：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#10进制转为2进制</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>bin(<span class="number">10</span>)</span><br><span class="line"><span class="string">'0b1010'</span></span><br><span class="line"><span class="comment">#2进制转为10进制</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>int(<span class="string">"1001"</span>,<span class="number">2</span>)</span><br><span class="line"><span class="number">9</span></span><br><span class="line"><span class="comment">#10进制转为16进制</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>hex(<span class="number">10</span>)</span><br><span class="line"><span class="string">'0xa'</span></span><br><span class="line"><span class="comment">#16进制到10进制</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>int(<span class="string">'ff'</span>, <span class="number">16</span>)</span><br><span class="line"><span class="number">255</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>int(<span class="string">'0xab'</span>, <span class="number">16</span>)</span><br><span class="line"><span class="number">171</span></span><br><span class="line"><span class="comment">#16进制到2进制</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>bin(<span class="number">0xa</span>)</span><br><span class="line"><span class="string">'0b1010'</span></span><br><span class="line">&gt;&gt;&gt;</span><br><span class="line"><span class="comment">#10进制到8进制</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>oct(<span class="number">8</span>)</span><br><span class="line"><span class="string">'010'</span></span><br><span class="line"><span class="comment">#2进制到16进制</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>hex(<span class="number">0b1001</span>)</span><br><span class="line"><span class="string">'0x9'</span></span><br></pre></td></tr></table></figure><p>注意：计算机中的运算都是以<strong>补码</strong>来进行的，但是显示的结果是以<strong>原码</strong>的方式显示的。</p><p><strong>位运算</strong>是直接对二进制进行操作，这样做能省内存，效率也很高。</p><p>位运算分为以下几点：<code>按位与(&amp;)</code>、<code>按位或(|)</code>、<code>按位异或(^)</code>、<code>按位取反(~)</code>、<code>按位左移(&lt;&lt;)</code>、<code>按位右移(&gt;&gt;)</code>。</p><ul><li><strong>按位左移：各二进位全部左移n位,高位丢弃,低位补0</strong>，格式如下：<br><code>x &lt;&lt; n #将x的所有二进制位向左移n位</code><br><strong>注意：左移一位就是乘以2，左移n位就是乘以2的n次方，还有一点就是左移可能会改变一个数的正负性</strong></li><li><strong>按位右移：各二进位全部右移n位,保持符号位不变</strong>，格式如下：<br><code>x &gt;&gt; n #将x 的所有二进制为向右移n位</code><br><strong>和左移类似，右移一位是除以2，右移n位就是除以2的n次方</strong></li><li><strong>按位与：各二进制位依次比较，全1为1，否则为0，只有对应的两个二进位均为1时,结果位才为1,否则为0</strong>，格式如下：<br><code>x &amp; y</code></li><li><strong>按位或：有1就1只要对应的二个二进位有一个为1时,结果位就为1,否则为0</strong>，格式如下：<br><code>x | y</code></li><li><strong>按位异或：不同为1 当对应的二进位相异(不相同)时,结果为1,否则为0</strong>，格式如下：<br><code>x ^ y</code></li><li><strong>取反：各二进制位依次取反，格式如下：</strong><br><code>~x</code><blockquote><p>如：<code>~9 = -10</code><br>解释：9的原码为00001001，正数原码和补码相同，因此实际存储的是9的补码也是这个。<br>将其进行取反操作得到11110110为操作之后的补码，然后将这个补码转为原码后为10001010，就是-10的原码。<br>因此<code>~9 = -10</code></p></blockquote></li></ul><h3 id="私有化"><a href="#私有化" class="headerlink" title="私有化"></a>私有化</h3><p>在一个类中，有如下几种命名<br><code>xx</code>: 公有变量<br><code>_x</code>: 单前置下划线,私有化属性或方法，<code>from SomeModule import *</code>禁止导入,类对象和子类可以访问<br><code>__xx</code>：双前置下划线,避免与子类中的属性命名冲突，无法在外部直接访问(名字重整所以访问不到)<br><code>__xx__</code>:双前后下划线,用户名字空间的魔法对象或属性。例如:<code>__init__</code>, 不要自己发明这样的名字<br><code>xx_</code>:单后置下划线,用于避免与Python关键词的冲突，一般不建议这样做</p><p><strong>注意：在类中的私有属性看起来不能用，实际上是能用的。</strong><br>如在Test类中有个<strong>num属性，它实际上是被改成了_Test</strong>num这个名字。<br>所以看起来不能用，但是可以通过_Test__num来访问这个属性。<br>这在Python中称为名字重整(Name Mangling)，就是为了防止子类意外重写基类的方法或者属性</p><h3 id="property"><a href="#property" class="headerlink" title="property"></a>property</h3><p>在一个类中有一些属性不想让外界轻易访问，则需要定义getter和setter方法，但是这样写，调用的时候是调用的方法。<br>而我们的目的是为了修改和得到属性的值，因此这样掉方法挺麻烦的。<br>这时候就可以用<strong>property</strong>让这个方法能像修改成员属性那样调用getter和setter方法。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Money</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.__money = <span class="number">0</span> </span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">getMoney</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> self.__money</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">setMoney</span><span class="params">(self, value)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> isinstance(value, int):</span><br><span class="line">            self.__money = value</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            print(<span class="string">"Error, 传入非整形参数"</span>)</span><br><span class="line"></span><br><span class="line">    money = property(getMoney, setMoney)</span><br></pre></td></tr></table></figure><p>执行如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&gt;&gt;&gt; a &#x3D; Money()</span><br><span class="line">&gt;&gt;&gt; a.money</span><br><span class="line">0</span><br><span class="line">&gt;&gt;&gt; a.money &#x3D; 100</span><br><span class="line">&gt;&gt;&gt; a.money</span><br></pre></td></tr></table></figure><p>这样就可以通过property来达到类似调用属性的形式来调用方法</p><p>关于property还有一种玩法，就是<strong>装饰器</strong>：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Money</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.__money = <span class="number">0</span> </span><br><span class="line"></span><br><span class="line"><span class="meta">    @property</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">money</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> self.__money</span><br><span class="line"></span><br><span class="line"><span class="meta">    @money.setter</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">money</span><span class="params">(self, value)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> isinstance(value, int):</span><br><span class="line">            self.__money = value</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            print(<span class="string">"Error, 传入非整形参数"</span>)</span><br></pre></td></tr></table></figure><p>定义函数的时候直接使用要调用的属性名即可，然后在getter方法前加上<code>@property</code>，在setter方法前加上<code>@属性名.setter</code>就行了，执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&gt;&gt;&gt; a &#x3D; Money()</span><br><span class="line">&gt;&gt;&gt; a.money</span><br><span class="line">0</span><br><span class="line">&gt;&gt;&gt; a.money &#x3D; 100</span><br><span class="line">&gt;&gt;&gt; a.money</span><br><span class="line">100</span><br><span class="line">&gt;&gt;&gt; a.money &#x3D; &quot;abc&quot;</span><br><span class="line">Error, 传入非整形参数</span><br></pre></td></tr></table></figure><h3 id="生成器"><a href="#生成器" class="headerlink" title="生成器"></a>生成器</h3><p>通过列表生成式，我们可以直接创建一个列表。<br>但是，受到内存限制，列表容量肯定是有限的。<br>而且，创建一个包含100万个元素的列表，不仅占用很大的存储空间，如果我们仅仅需要访问前面几个元素，那后面绝大多数元素占用的空间都白白浪费了。<br>所以，如果列表元素可以按照某种算法推算出来，那我们是否可以在循环的过程中不断推算出后续的元素呢？<br>这样就不必创建完整的list，从而节省大量的空间。<br>在Python中，这种一边循环一边计算的机制，称为<strong>生成器(generator)</strong>。<br>前面说过列表推导式，也叫列表生成式，如list = [x for x in range(5)]，可以通过这个来生成一个列表，如果将列表生成式两端的”[]”变成”()”，那么这样就变成了一个生成器。如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>g = (x <span class="keyword">for</span> x <span class="keyword">in</span> range(<span class="number">5</span>))</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>g</span><br><span class="line">&lt;generator object &lt;genexpr&gt; at <span class="number">0x7f69169603b8</span>&gt;</span><br></pre></td></tr></table></figure><p>生成器需要用<strong>next()</strong>函数来一个一个打印其中的值，并且生成器是要一个值就给一个值：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>next(g)</span><br><span class="line"><span class="number">0</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>next(g)</span><br><span class="line"><span class="number">1</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>next(g)</span><br><span class="line"><span class="number">2</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>next(g)</span><br><span class="line"><span class="number">3</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>next(g)</span><br><span class="line"><span class="number">4</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>next(g)</span><br><span class="line">Traceback (most recent call last):</span><br><span class="line">  File <span class="string">"&lt;stdin&gt;"</span>, line <span class="number">1</span>, <span class="keyword">in</span> &lt;module&gt;</span><br><span class="line">StopIteration</span><br></pre></td></tr></table></figure><p>当没有值的时候还调用next函数就会出现异常。<br>当然，通常情况下我们不用next函数，而是直接通过for循环来迭代这个生成器，这样做就会在生成器没值的时候跳出循环而不会出现异常。</p><ul><li><p><strong>yield</strong><br>yield关键字是用来创建生成器的，当一个函数中使用了yield关键字的时候，那么就不能将这个函数当成一个普通的函数来处理，这个函数本质上是一个生成器了。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">5</span>):</span><br><span class="line">        <span class="keyword">yield</span> i</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"Done"</span></span><br></pre></td></tr></table></figure><p>打印结果如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>f = test()</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>f</span><br><span class="line">&lt;generator object test at <span class="number">0x7f639ec47518</span>&gt;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>next(f)</span><br><span class="line"><span class="number">0</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>next(f)</span><br><span class="line"><span class="number">1</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>next(f)</span><br><span class="line"><span class="number">2</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>next(f)</span><br><span class="line"><span class="number">3</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>next(f)</span><br><span class="line"><span class="number">4</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>next(f)</span><br><span class="line">Traceback (most recent call last):</span><br><span class="line">  File <span class="string">"&lt;stdin&gt;"</span>, line <span class="number">1</span>, <span class="keyword">in</span> &lt;module&gt;</span><br><span class="line">StopIteration: Done</span><br></pre></td></tr></table></figure><p>应用：斐波那契数列：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">fib</span><span class="params">(count)</span>:</span></span><br><span class="line">    a,b = <span class="number">0</span>,<span class="number">1</span> </span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(count):</span><br><span class="line">        <span class="keyword">yield</span> b</span><br><span class="line">        a,b = b,a+b</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"Done"</span></span><br></pre></td></tr></table></figure><p>生成器有一个自带的函数为<code>__next__()</code>函数，和<code>next()</code>函数的效果是一样的<br>另外，生成器在生成一个值的时候是会停在yield语句这里的，等待下一个值的获取才会执行。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">"----1----"</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">5</span>):</span><br><span class="line">        print(<span class="string">"----2----"</span>)</span><br><span class="line">        <span class="keyword">yield</span> i</span><br><span class="line">        print(<span class="string">"----3----"</span>)</span><br><span class="line"></span><br><span class="line">    print(<span class="string">"----4----"</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"Done"</span></span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>f = test()</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>f.__next__()</span><br><span class="line">---<span class="number">-1</span>----</span><br><span class="line">---<span class="number">-2</span>----</span><br><span class="line"><span class="number">0</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>f.__next__()</span><br><span class="line">---<span class="number">-3</span>----</span><br><span class="line">---<span class="number">-2</span>----</span><br><span class="line"><span class="number">1</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>f.__next__()</span><br><span class="line">---<span class="number">-3</span>----</span><br><span class="line">---<span class="number">-2</span>----</span><br><span class="line"><span class="number">2</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>f.__next__()</span><br><span class="line">---<span class="number">-3</span>----</span><br><span class="line">---<span class="number">-2</span>----</span><br><span class="line"><span class="number">3</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>f.__next__()</span><br><span class="line">---<span class="number">-3</span>----</span><br><span class="line">---<span class="number">-2</span>----</span><br><span class="line"><span class="number">4</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>f.__next__()</span><br><span class="line">---<span class="number">-3</span>----</span><br><span class="line">---<span class="number">-4</span>----</span><br><span class="line">Traceback (most recent call last):</span><br><span class="line">  File <span class="string">"&lt;stdin&gt;"</span>, line <span class="number">1</span>, <span class="keyword">in</span> &lt;module&gt;</span><br><span class="line">StopIteration: Done</span><br></pre></td></tr></table></figure><p>生成器还带一个函数为<strong>send()</strong>函数，和next函数类似，不过send函数可以传值给生成器。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">"----1----"</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">5</span>):</span><br><span class="line">        print(<span class="string">"----2----"</span>)</span><br><span class="line">        temp = <span class="keyword">yield</span> i</span><br><span class="line">        print(temp)</span><br><span class="line">        print(<span class="string">"----3----"</span>)</span><br><span class="line"></span><br><span class="line">    print(<span class="string">"----4----"</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"Done"</span></span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>f.send(<span class="literal">None</span>)  </span><br><span class="line">---<span class="number">-1</span>----</span><br><span class="line">---<span class="number">-2</span>----</span><br><span class="line"><span class="number">0</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>f.send(<span class="string">"hehe"</span>)</span><br><span class="line">hehe</span><br><span class="line">---<span class="number">-3</span>----</span><br><span class="line">---<span class="number">-2</span>----</span><br><span class="line"><span class="number">1</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>f.send(<span class="string">"haha"</span>)</span><br><span class="line">haha</span><br><span class="line">---<span class="number">-3</span>----</span><br><span class="line">---<span class="number">-2</span>----</span><br><span class="line"><span class="number">2</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>f.send(<span class="string">"heihei"</span>)</span><br><span class="line">heihei</span><br><span class="line">---<span class="number">-3</span>----</span><br><span class="line">---<span class="number">-2</span>----</span><br><span class="line"><span class="number">3</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>f.send(<span class="string">"heihei"</span>)</span><br><span class="line">heihei</span><br><span class="line">---<span class="number">-3</span>----</span><br><span class="line">---<span class="number">-2</span>----</span><br><span class="line"><span class="number">4</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>f.send(<span class="string">"heihei"</span>)</span><br><span class="line">heihei</span><br><span class="line">---<span class="number">-3</span>----</span><br><span class="line">---<span class="number">-4</span>----</span><br><span class="line">Traceback (most recent call last):</span><br><span class="line">  File <span class="string">"&lt;stdin&gt;"</span>, line <span class="number">1</span>, <span class="keyword">in</span> &lt;module&gt;</span><br><span class="line">StopIteration: Done</span><br></pre></td></tr></table></figure><p><strong>注意</strong>：第一次send的时候是不能传入值的，因为生成器刚开始还没有停在yield语句这里，使用send传入值是传入到yield那里，所以第一次需要让生成器停在yield语句这里才能使用send传值。<br>另外，send还可以传<code>None，f.send(None)和f.__next__()、next(f)</code>，其效果是一样的。<br><strong>总结</strong>：<br>生成器是这样一个函数，它记住上一次返回时在函数体中的位置。<br>对生成器函数的第二次（或第 n 次）调用跳转至该函数中间，而上次调用的所有局部变量都保持不变。<br>生成器不仅“记住”了它数据状态；生成器还“记住”了它在流控制构造（在命令式编程中，这种构造不只是数据值）中的位置。<br>生成器的特点：</p><ol><li>节约内存</li><li>迭代到下一次的调用时，所使用的参数都是第一次所保留下的，即是说，在整个所有函数调用的参数都是第一次所调用时保留的，而不是新创建的</li></ol></li></ul><h3 id="迭代器"><a href="#迭代器" class="headerlink" title="迭代器"></a>迭代器</h3><p>迭代是访问集合元素的一种方式。<br>迭代器是一个可以记住遍历的位置的对象。<br>迭代器对象从集合的第一个元素开始访问，直到所有的元素被访问完结束。<br>迭代器只能往前不会后退。</p><ul><li><p><strong>可迭代对象</strong><br>以直接作用于 for 循环的数据类型有以下几种：<br>一类是<strong>集合</strong>数据类型，如 list 、 tuple 、 dict 、 set 、 str 等；<br>一类是<strong>generator</strong>，包括<strong>生成器</strong>和<strong>带yield的generator function</strong>。<br>这些<strong>可以直接作用于for循环的对象</strong>统称为可迭代对象(Iterable)。<br>可以使用<strong>isinstance()</strong>函数判断一个对象是否是可迭代(Iterable)对象<br>Iterable需要<code>from collections import Iterable</code></p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">from</span> collections <span class="keyword">import</span> Iterable</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>isinstance([], Iterable)</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>isinstance(&#123;&#125;, Iterable)  </span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>isinstance(<span class="string">"abc"</span>, Iterable)  </span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>isinstance((x <span class="keyword">for</span> x <span class="keyword">in</span> range(<span class="number">10</span>)), Iterable)     </span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>isinstance(<span class="number">100</span>, Iterable)                      </span><br><span class="line"><span class="literal">False</span></span><br></pre></td></tr></table></figure></li><li><p><strong>迭代器</strong><br><strong>可以被next()函数调用并不断返回下一个值的对象称为迭代器(Iterator)</strong>。<br>可以使用<strong>isinstance()</strong>判断一个对象是否是Iterator对象：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">from</span> collections <span class="keyword">import</span> Iterator</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>isinstance((x <span class="keyword">for</span> x <span class="keyword">in</span> range(<span class="number">5</span>)), Iterator)</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>isinstance([], Iterator)                     </span><br><span class="line"><span class="literal">False</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>isinstance(&#123;&#125;, Iterator)  </span><br><span class="line"><span class="literal">False</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>isinstance(<span class="string">"abc"</span>, Iterator)  </span><br><span class="line"><span class="literal">False</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>isinstance(<span class="number">100</span>, Iterator)     </span><br><span class="line"><span class="literal">False</span></span><br></pre></td></tr></table></figure></li><li><p><strong>iter函数</strong><br>生成器都是Iterator对象，但list、dict、str虽然是Iterable，却不是Iterator。<br>把list、dict、str等Iterable变成Iterator可以使用iter()函数：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = iter(a)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">&lt;list_iterator object at <span class="number">0x7f4f78d7ca58</span>&gt;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>isinstance(a, Iterator)</span><br><span class="line"><span class="literal">True</span></span><br></pre></td></tr></table></figure><p>总结：<br>1、凡是可作用于for循环的对象都是Iterable类型；<br>2、凡是可作用于next()函数的对象都是Iterator类型；<br>3、集合数据类型如list、dict、str等是Iterable但不是Iterator，不过可以通过 iter()函数获得一个Iterator对象。</p></li></ul><h3 id="闭包"><a href="#闭包" class="headerlink" title="闭包"></a>闭包</h3><p>在函数内部再定义一个函数，并且这个函数用到了外边函数的变量，那么将这个函数以及用到的一些变量称之为<strong>闭包</strong>。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">(number)</span>:</span></span><br><span class="line">    print(<span class="string">"---1---"</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">test_in</span><span class="params">()</span>:</span></span><br><span class="line">        print(<span class="string">"---2---"</span>)</span><br><span class="line">        print(number + <span class="number">100</span>)</span><br><span class="line"></span><br><span class="line">    print(<span class="string">'---3---'</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> test_in</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>test(<span class="number">100</span>)</span><br><span class="line">--<span class="number">-1</span>---</span><br><span class="line">--<span class="number">-3</span>---</span><br><span class="line">&lt;function test.&lt;locals&gt;.test_in at <span class="number">0x7f5861b9ca60</span>&gt;</span><br></pre></td></tr></table></figure><p>返回的结果是一个函数引用（类似于C语言中的函数指针）。<br>这个引用可以被调用，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>ret = test(<span class="number">100</span>)</span><br><span class="line">--<span class="number">-1</span>---</span><br><span class="line">--<span class="number">-3</span>---</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>ret()</span><br><span class="line">--<span class="number">-2</span>---</span><br><span class="line"><span class="number">200</span></span><br></pre></td></tr></table></figure><p>闭包中还可以传递参数：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">(k, b)</span>:</span> </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">test_in</span><span class="params">(x)</span>:</span></span><br><span class="line">        print(k*x + b)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> test_in</span><br></pre></td></tr></table></figure><p>结果如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>ret = test(<span class="number">1</span>,<span class="number">3</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>ret(<span class="number">5</span>)</span><br><span class="line"><span class="number">8</span></span><br></pre></td></tr></table></figure><p>这就是一个利用闭包计算y = kx+b的例子</p><h3 id="装饰器"><a href="#装饰器" class="headerlink" title="装饰器"></a>装饰器</h3><p>装饰器是程序开发中经常会用到的一个功能，用好了装饰器，开发效率如虎添翼，所以这也是Python面试中必问的问题。<br>有如下代码：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">w1</span><span class="params">(func)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">inner</span><span class="params">()</span>:</span></span><br><span class="line">        print(<span class="string">"----inner----"</span>)</span><br><span class="line">        func()</span><br><span class="line">    <span class="keyword">return</span> inner</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f1</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">"---f1---"</span>)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f2</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">"---f2---"</span>)</span><br><span class="line"></span><br><span class="line">ret = w1(f1)</span><br><span class="line">ret()</span><br><span class="line">print(<span class="string">"-"</span>*<span class="number">20</span>)</span><br><span class="line">ret = w1(f2)</span><br><span class="line">ret()</span><br></pre></td></tr></table></figure><p>首先创建一个闭包，然后调用这个闭包，传入我们要装饰的函数f1，之后执行那个闭包函数。<br>这样，那个被装饰的函数会被执行一遍，并且还能执行我们添加的装饰语句。<br>既然可以用ret来接收这个被装饰了的函数，那么能不能直接用要装饰的函数名来代替呢？<br>答案是可以的，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>f1 = w1(f1)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>f1()</span><br><span class="line">----inner----</span><br><span class="line">---f1---</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>f2 = w1(f2)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>f2()</span><br><span class="line">----inner----</span><br><span class="line">---f2---</span><br></pre></td></tr></table></figure><p>其实装饰器就是完成这一步的功能<strong>原函数名 = 装饰器(原函数引用)</strong><br>由此我们可以知道，装饰器就是一个闭包函数，它的用法就是在要被装饰的函数上一行加上<code>@装饰器</code>即可，实际上它完成的功能就是<strong>原函数名 = 装饰器(原函数引用)</strong>这一操作。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">w1</span><span class="params">(func)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">inner</span><span class="params">()</span>:</span></span><br><span class="line">        print(<span class="string">"----inner----"</span>)</span><br><span class="line">        func()</span><br><span class="line">    <span class="keyword">return</span> inner</span><br><span class="line"></span><br><span class="line"><span class="meta">@w1</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f1</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">"---f1---"</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">@w1</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f2</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">"---f2---"</span>)</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>f1()</span><br><span class="line">----inner----</span><br><span class="line">---f1---</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>f2()</span><br><span class="line">----inner----</span><br><span class="line">---f2---</span><br></pre></td></tr></table></figure><p>由于装饰器就相当于做了：<strong>原函数名 = 装饰器(原函数引用)</strong>这样的工作，那么在执行<code>@</code>语句的时候实际上会将装饰器函数的外部执行一遍。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">w1</span><span class="params">(func)</span>:</span></span><br><span class="line">    print(<span class="string">"---w1---"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">inner</span><span class="params">()</span>:</span></span><br><span class="line">        print(<span class="string">"---inner----"</span>)</span><br><span class="line">        func()</span><br><span class="line">    <span class="keyword">return</span> inner</span><br><span class="line"></span><br><span class="line"><span class="meta">@w1</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">"---test---"</span>)</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># python3 test.py </span><br><span class="line">---w1---</span><br></pre></td></tr></table></figure><p>所以只要Python解释器执行了@这个代码，那么就会自动的进行装饰，而不是等到要调用的时候才去进行装饰</p><ul><li><p><strong>多个装饰器</strong><br>如果有多个装饰器的时候会怎么执行呢？<br>有如下代码：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">makeBold</span><span class="params">(fn)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">wrapped</span><span class="params">()</span>:</span></span><br><span class="line">        print(<span class="string">"---1---"</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"&lt;b&gt;"</span>+fn()+<span class="string">"&lt;/b&gt;"</span></span><br><span class="line">    <span class="keyword">return</span> wrapped</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">makeItalic</span><span class="params">(fn)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">wrapped</span><span class="params">()</span>:</span></span><br><span class="line">        print(<span class="string">"---2---"</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"&lt;i&gt;"</span>+fn()+<span class="string">"&lt;/i&gt;"</span></span><br><span class="line">    <span class="keyword">return</span> wrapped</span><br><span class="line">    </span><br><span class="line"><span class="meta">@makeBold</span></span><br><span class="line"><span class="meta">@makeItalic</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">"---3---"</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"Hello World"</span></span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>test()</span><br><span class="line">--<span class="number">-1</span>---</span><br><span class="line">--<span class="number">-2</span>---</span><br><span class="line">--<span class="number">-3</span>---</span><br><span class="line"><span class="string">'&lt;b&gt;&lt;i&gt;Hello World&lt;/i&gt;&lt;/b&gt;'</span></span><br></pre></td></tr></table></figure><blockquote><p>分析：首先执行的是makeBold，也就是最上面的装饰器，这个装饰器会将下面内容当成一个整体来执行，因此首先对test函数进行makeItalic的装饰，然后再进行makeBold的装饰。<br>所以调用test函数的时候会先打印1，然后打印2，最后打印3，返回的结果为最后的结果。</p></blockquote><p>我们可以在装饰器中的闭包函数之前加上打印语句来验证：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">w1</span><span class="params">(func)</span>:</span></span><br><span class="line">    print(<span class="string">"---w1---"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">inner</span><span class="params">()</span>:</span></span><br><span class="line">        print(<span class="string">"---inner1s----"</span>)</span><br><span class="line">        func()</span><br><span class="line">        print(<span class="string">"---inner1e----"</span>)</span><br><span class="line">    <span class="keyword">return</span> inner</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">w2</span><span class="params">(func)</span>:</span></span><br><span class="line">    print(<span class="string">"---w2---"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">inner</span><span class="params">()</span>:</span></span><br><span class="line">        print(<span class="string">"---inner2s----"</span>)</span><br><span class="line">        func()</span><br><span class="line">        print(<span class="string">"---inner2e----"</span>)</span><br><span class="line">    <span class="keyword">return</span> inner</span><br><span class="line"></span><br><span class="line"><span class="meta">@w1</span></span><br><span class="line"><span class="meta">@w2</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">"---test---"</span>)</span><br><span class="line"></span><br><span class="line">test()</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">---w2---</span><br><span class="line">---w1---</span><br><span class="line">---inner1s----</span><br><span class="line">---inner2s----</span><br><span class="line">---test---</span><br><span class="line">---inner2e----</span><br><span class="line">---inner1e----</span><br></pre></td></tr></table></figure><p>由此可以看到，<strong>最里面的装饰器先执行，最外的装饰器先打印</strong></p></li><li><p><strong>装饰带参函数</strong><br>我们在闭包中了解到，闭包中是可以传参数的，因此我们装饰器中的闭包也是可以有参数的，那么我们就可以用装饰器来装饰带有参数的函数了。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">w1</span><span class="params">(func)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">inner</span><span class="params">(a,b)</span>:</span></span><br><span class="line">        print(<span class="string">"---inner1s----"</span>)</span><br><span class="line">        func(a,b)</span><br><span class="line">        print(<span class="string">"---inner1e----"</span>)</span><br><span class="line">    <span class="keyword">return</span> inner</span><br><span class="line"></span><br><span class="line"><span class="meta">@w1</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">(a,b)</span>:</span></span><br><span class="line">    print(<span class="string">"%d+%d=%d"</span> %(a,b,a+b))</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>test(<span class="number">10</span>,<span class="number">20</span>)</span><br><span class="line">---inner1s----</span><br><span class="line"><span class="number">10</span>+<span class="number">20</span>=<span class="number">30</span></span><br><span class="line">---inner1e----</span><br></pre></td></tr></table></figure><p>这样做的话就固定了参数个数，如果有多个函数并且各函数参数个数各不相同并且想用同一个装饰器的话该如何是好呢？我们之前学过不定长参数，用这个来接收参数再好不过了。<br>如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">w1</span><span class="params">(func)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">inner</span><span class="params">(*args, **kwargs)</span>:</span></span><br><span class="line">        print(<span class="string">"---inners----"</span>)</span><br><span class="line">        func(*args, **kwargs)</span><br><span class="line">        print(<span class="string">"---innere----"</span>)</span><br><span class="line">    <span class="keyword">return</span> inner</span><br><span class="line"></span><br><span class="line"><span class="meta">@w1</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test1</span><span class="params">(a,b)</span>:</span></span><br><span class="line">    print(<span class="string">"%d+%d=%d"</span> %(a,b,a+b))</span><br><span class="line"></span><br><span class="line"><span class="meta">@w1</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test2</span><span class="params">(a,b,c)</span>:</span></span><br><span class="line">    print(<span class="string">"%d+%d+%d=%d"</span> %(a,b,c,a+b+c))</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>test1(<span class="number">1</span>,<span class="number">2</span>)</span><br><span class="line">---inners----</span><br><span class="line"><span class="number">1</span>+<span class="number">2</span>=<span class="number">3</span></span><br><span class="line">---innere----</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>test2(<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>)</span><br><span class="line">---inners----</span><br><span class="line"><span class="number">1</span>+<span class="number">2</span>+<span class="number">3</span>=<span class="number">6</span></span><br><span class="line">---innere----</span><br></pre></td></tr></table></figure></li><li><p><strong>装饰器返回值</strong><br>装饰器还可以用来装饰带有返回值的函数，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">w1</span><span class="params">(func)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">inner</span><span class="params">(*args, **kwargs)</span>:</span></span><br><span class="line">        print(<span class="string">"---inners----"</span>)</span><br><span class="line">        ret = func(*args, **kwargs)</span><br><span class="line">        print(<span class="string">"---innere----"</span>)</span><br><span class="line">        <span class="keyword">return</span> ret</span><br><span class="line">    <span class="keyword">return</span> inner</span><br><span class="line">              </span><br><span class="line"><span class="meta">@w1           </span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">(a,b,c)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> a+b+c</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>ret = test(<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>)</span><br><span class="line">---inners----</span><br><span class="line">---innere----</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>ret</span><br><span class="line"><span class="number">6</span></span><br></pre></td></tr></table></figure><blockquote><p>实际上装饰器中带有返回值是可以装饰任何函数的，因为函数如果没有返回值，Python解释器会默认返回None的，装饰器中接收到函数的返回值为None，同样也返回None，就相当于没有返回值。</p></blockquote></li><li><p><strong>带参装饰器</strong><br>装饰器中也可以带参数，有如下代码：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">func_arg</span><span class="params">(pre = <span class="string">"hello"</span>)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">func</span><span class="params">(function)</span>:</span></span><br><span class="line">        <span class="function"><span class="keyword">def</span> <span class="title">func_in</span><span class="params">()</span>:</span></span><br><span class="line">            print(pre)</span><br><span class="line">            <span class="keyword">return</span> function()</span><br><span class="line">        <span class="keyword">return</span> func_in</span><br><span class="line">    <span class="keyword">return</span> func</span><br><span class="line"></span><br><span class="line"><span class="meta">@func_arg("hehe")</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test1</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">"---test1---"</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">@func_arg("haha")</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test2</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">"---test2---"</span>)</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>test1()</span><br><span class="line">hehe</span><br><span class="line">---test1---</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>test2()</span><br><span class="line">haha</span><br><span class="line">---test2---</span><br></pre></td></tr></table></figure><p>由此我们可以看出使用带参数的装饰器可以用同一种装饰器利用参数的不同从而达到不同的装饰效果。</p></li><li><p><strong>类装饰器</strong><br>装饰器函数其实是这样一个接口约束，它必须接受一个callable对象作为参数，然后返回一个callable对象。<br>在Python中一般callable对象都是函数，但也有例外。<br>只要某个对象重写了<code>__call__()</code>方法，那么这个对象就是callable的。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__call__</span><span class="params">(self)</span>:</span></span><br><span class="line">        print(<span class="string">'call me!'</span>)</span><br><span class="line"></span><br><span class="line">t = Test()</span><br><span class="line">t()  <span class="comment"># call me</span></span><br></pre></td></tr></table></figure><p>类装饰器：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, func)</span>:</span></span><br><span class="line">        print(<span class="string">"---初始化---"</span>)</span><br><span class="line">        print(<span class="string">"func name is %s"</span>%func.__name__)</span><br><span class="line">        self.__func = func</span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__call__</span><span class="params">(self)</span>:</span></span><br><span class="line">        print(<span class="string">"---装饰器中的功能---"</span>)</span><br><span class="line">        self.__func()</span><br><span class="line"></span><br><span class="line"><span class="comment">#说明：</span></span><br><span class="line"><span class="comment">#1. 当用Test来装作装饰器对test函数进行装饰的时候，首先会创建Test的实例对象</span></span><br><span class="line"><span class="comment">#    并且会把test这个函数名当做参数传递到__init__方法中</span></span><br><span class="line"><span class="comment">#    即在__init__方法中的func变量指向了test函数体</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment">#2. test函数相当于指向了用Test创建出来的实例对象</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment">#3. 当在使用test()进行调用时，就相当于让这个对象()，因此会调用这个对象的__call__方法</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment">#4. 为了能够在__call__方法中调用原来test指向的函数体，所以在__init__方法中就需要一个实例属性来保存这个函数体的引用</span></span><br><span class="line"><span class="comment">#    所以才有了self.__func = func这句代码，从而在调用__call__方法中能够调用到test之前的函数体</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">"----test---"</span>)</span><br><span class="line"></span><br><span class="line">test()</span><br></pre></td></tr></table></figure><p>运行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">---初始化---</span><br><span class="line">func name is test</span><br><span class="line">---装饰器中的功能---</span><br><span class="line">----test---</span><br></pre></td></tr></table></figure></li></ul><h3 id="作用域"><a href="#作用域" class="headerlink" title="作用域"></a>作用域</h3><p>在之前学习变量的作用域时，经常会提到局部变量和全局变量，之所有称之为局部、全局，就是因为他们的自作用的区域不同，这就是作用域。<br>用locals函数和globals函数可以分别打印当前作用域的局部变量和全局变量，如：</p><figure class="highlight"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>globals()</span><br><span class="line">&#123;'__package__': None, '__builtins__': &lt;module 'builtins' (built-in)&gt;, '__spec__': None, '__loader__': &lt;class '_frozen_importlib.BuiltinImporter'&gt;, '__doc__': None, '__name__': '__main__'&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>locals()</span><br><span class="line">&#123;'__package__': None, '__builtins__': &lt;module 'builtins' (built-in)&gt;, '__spec__': None, '__loader__': &lt;class '_frozen_importlib.BuiltinImporter'&gt;, '__doc__': None, '__name__': '__main__'&#125;</span><br></pre></td></tr></table></figure><p>由于当前作用域就是全局，因此打印出的局部和全局变量都是一样的。</p><ul><li><p><strong>LEGB规则</strong><br>Python使用LEGB的顺序来查找一个符号对应的对象：<br><strong>locals -&gt; enclosing function -&gt; globals -&gt; builtins</strong><br>首先从局部(locals)查找;<br>然后从外部嵌套的命名空间(enclosing function，常见于闭包中)中查找;<br>之后从全局(globals)中查找;<br>最后再到内建模块命名空间(builtins)中找。</p><p>Python在启动的时候会自动为我们载入很多内建的函数、类，比如 dict，list，type，print<br>这些都位于<code>__builtin__</code>模块中，可以使用<code>dir(__builtin__)</code>来查看。<br>这也是为什么我们在没有import任何模块的情况下，就能使用这么多丰富的函数和功能了。<br>使用<code>dir(__builtin__)</code>（注意：只能在ipython中查看）：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">In [<span class="number">1</span>]: dir(__builtin__)                                                                                                           </span><br><span class="line">Out[<span class="number">1</span>]: </span><br><span class="line">[<span class="string">'ArithmeticError'</span>,</span><br><span class="line"> <span class="string">'AssertionError'</span>,</span><br><span class="line"> <span class="string">'AttributeError'</span>,</span><br><span class="line"> <span class="string">'BaseException'</span>,</span><br><span class="line"> <span class="string">'BlockingIOError'</span>,</span><br><span class="line"> ...</span><br><span class="line"> <span class="string">'zip'</span>]</span><br></pre></td></tr></table></figure></li></ul><h3 id="Python-的动态性"><a href="#Python-的动态性" class="headerlink" title="Python 的动态性"></a>Python 的动态性</h3><p><strong>动态编程语言</strong>是<strong>高级程序设计语言</strong>的一个类别，在计算机科学领域已被广泛应用。<br>它是一类<strong>在运行时可以改变其结构的语言</strong>：例如新的函数、对象、甚至代码可以被引进，已有的函数可以被删除或是其他结构上的变化。<br>Python自然也是一种动态语言，在Python解释器运行的过程中可以动态地给对象绑定（添加）属性，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="class"><span class="keyword">class</span> <span class="title">Person</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, name = None, age = None)</span>:</span></span><br><span class="line">        self.name = name</span><br><span class="line">        self.age = age</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>P1 = Person(<span class="string">"小明"</span>, <span class="string">"24"</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>P1.sex = <span class="string">"male"</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>P1.sex</span><br><span class="line"><span class="string">'male'</span></span><br></pre></td></tr></table></figure><p>在这里我们没有在类中定义sex这个属性，但是我们在运行的时候可以给对象动态地绑定一个sex属性，照样能打印出结果来。<br>但是如果换一个对象就还是没有sex的属性，如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>P2 = Person(<span class="string">"小丽"</span>, <span class="string">"25"</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>P2.sex</span><br><span class="line">Traceback (most recent call last):</span><br><span class="line">  File <span class="string">"&lt;pyshell#21&gt;"</span>, line <span class="number">1</span>, <span class="keyword">in</span> &lt;module&gt;</span><br><span class="line">    P2.sex</span><br><span class="line">AttributeError: Person instance has no attribute <span class="string">'sex'</span></span><br></pre></td></tr></table></figure><p>那么我们如果想让这个类创建的所有对象都有sex属性怎么办？答案就是我们可以直接给类绑定属性：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>Person.sex = <span class="literal">None</span> <span class="comment">#给类Person添加一个属性</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>P1 = Person(<span class="string">"小丽"</span>, <span class="string">"25"</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(P1.sex)  <span class="comment">#如果P1这个实例对象中没有sex属性的话，那么就会访问它的类属性</span></span><br><span class="line"><span class="literal">None</span>  <span class="comment">#可以看到没有出现异常</span></span><br></pre></td></tr></table></figure><p>既然我们可以绑定属性，那么肯定也能绑定方法，绑定方法<strong>需要导入types模块</strong>，需要用到其中的<strong>MethodType方法</strong><br>如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">In [<span class="number">1</span>]: <span class="class"><span class="keyword">class</span> <span class="title">Person</span><span class="params">(object)</span>:</span> </span><br><span class="line">   ...:     <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, name, age)</span>:</span> </span><br><span class="line">   ...:         self.name = name </span><br><span class="line">   ...:         self.age = age </span><br><span class="line">   ...:     <span class="function"><span class="keyword">def</span> <span class="title">eat</span><span class="params">(self)</span>:</span> </span><br><span class="line">   ...:         print(<span class="string">"Eat food"</span>) </span><br><span class="line">   ...:  </span><br><span class="line">In [<span class="number">2</span>]: <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self, speed)</span>:</span> </span><br><span class="line">   ...:     print(<span class="string">"%s在移动，速度为%d km/h"</span> %(self.name, speed))</span><br><span class="line">In [<span class="number">4</span>]: P = Person(<span class="string">"XiaoMing"</span>, <span class="number">20</span>)</span><br><span class="line">In [<span class="number">5</span>]: P.eat()</span><br><span class="line">Eat food</span><br><span class="line">In [<span class="number">6</span>]: <span class="keyword">from</span> types <span class="keyword">import</span> MethodType</span><br><span class="line">In [<span class="number">7</span>]: P.run = MethodType(run,P)</span><br><span class="line">In [<span class="number">8</span>]: P.run(<span class="number">140</span>)</span><br><span class="line">XiaoMing在移动，速度为<span class="number">140</span> km/h</span><br></pre></td></tr></table></figure><p>所以给类绑定一个方法格式为<code>类名.方法名 = MethodType(另外创建的函数,类)</code></p><p>如果想对一个类绑定类方法和动态方法，则不需要用MethodType函数，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">In [<span class="number">9</span>]: @classmethod </span><br><span class="line">    ...: <span class="function"><span class="keyword">def</span> <span class="title">testClass</span><span class="params">(cls)</span>:</span> </span><br><span class="line">    ...:     cls.num = <span class="number">100</span></span><br><span class="line">In [<span class="number">10</span>]: @staticmethod </span><br><span class="line">    ...: <span class="function"><span class="keyword">def</span> <span class="title">testStatic</span><span class="params">()</span>:</span> </span><br><span class="line">    ...:     print(<span class="string">"--- static method ---"</span>)</span><br><span class="line">In [<span class="number">11</span>]: Person.testClass = testClass</span><br><span class="line">In [<span class="number">12</span>]: Person.testStatic = testStatic</span><br><span class="line">In [<span class="number">13</span>]: print(Person.num)</span><br><span class="line">---------------------------------------------------------------------------</span><br><span class="line">AttributeError                            Traceback (most recent call last)</span><br><span class="line">&lt;ipython-input<span class="number">-18</span><span class="number">-56</span>ef842e1620&gt; <span class="keyword">in</span> &lt;module&gt;</span><br><span class="line">----&gt; 1 print(Person.num)</span><br><span class="line"></span><br><span class="line">AttributeError: type object <span class="string">'Person'</span> has no attribute <span class="string">'num'</span></span><br><span class="line">In [<span class="number">14</span>]: Person.testClass()</span><br><span class="line">In [<span class="number">15</span>]: print(Person.num)</span><br><span class="line"><span class="number">100</span></span><br><span class="line">In [<span class="number">17</span>]: Person.testStatic()</span><br><span class="line">--- static method ---</span><br></pre></td></tr></table></figure><p>如果想要删除属性或方法的话，用del关键字和delattr函数都可以<br>使用方法如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">del 对象.属性名</span><br><span class="line">delattr(对象,&quot;属性名&quot;)</span><br></pre></td></tr></table></figure><ul><li><p><strong><code>__slots__</code></strong><br>现在我们终于明白了，动态语言与静态语言的不同:<br><strong>动态语言</strong>：可以在运行的过程中，修改代码<br><strong>静态语言</strong>：编译时已经确定好代码，运行过程中不能修改<br>如果我们想要限制实例的属性怎么办？<br>比如，只允许对Person实例添加name和age属性。</p><p>为了达到限制的目的，Python允许在定义class的时候，定义一个特殊的<code>__slots__</code>变量，来限制该class实例能添加的属性：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="class"><span class="keyword">class</span> <span class="title">Person</span><span class="params">(object)</span>:</span></span><br><span class="line">    __slots__ = (<span class="string">"name"</span>, <span class="string">"age"</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>P = Person()</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>P.name = <span class="string">"老王"</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>P.age = <span class="number">20</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>P.score = <span class="number">100</span></span><br><span class="line">Traceback (most recent call last):</span><br><span class="line">  File <span class="string">"&lt;pyshell#3&gt;"</span>, line <span class="number">1</span>, <span class="keyword">in</span> &lt;module&gt;</span><br><span class="line">AttributeError: Person instance has no attribute <span class="string">'score'</span></span><br></pre></td></tr></table></figure><p>使用<code>__slots__</code>要注意，<code>__slots__</code>定义的属性仅对当前类实例起作用，对继承的子类是不起作用的:</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="class"><span class="keyword">class</span> <span class="title">Test</span><span class="params">(Person)</span>:</span></span><br><span class="line">         <span class="keyword">pass</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>t = Test()</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>t.score = <span class="number">100</span></span><br></pre></td></tr></table></figure></li></ul><h3 id="元类"><a href="#元类" class="headerlink" title="元类"></a>元类</h3><p>在Python中，<strong>类同样也是一种对象</strong>。<br>是的，没错，就是对象。<br>只要你使用关键字class，Python解释器在执行的时候就会创建一个对象。</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="class"><span class="keyword">class</span> <span class="title">ObjectCreator</span><span class="params">(object)</span>:</span></span><br><span class="line"><span class="meta">... </span>    <span class="keyword">pass</span></span><br><span class="line">...</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>my_object = ObjectCreator()</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(my_object)</span><br><span class="line">&lt;__main__.ObjectCreator object at <span class="number">0x8974f2c</span>&gt;</span><br></pre></td></tr></table></figure><p>既然类的本质是对象，于是我们可以对类进行如下操作：<br>1、将它赋值给一个变量<br>2、可以拷贝它<br>3、可以为它增加属性<br>4、可以将它作为函数参数进行传递</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(ObjectCreator)  <span class="comment"># 你可以打印一个类，因为它其实也是一个对象</span></span><br><span class="line">&lt;<span class="class"><span class="keyword">class</span> '<span class="title">__main__</span>.<span class="title">ObjectCreator</span>'&gt;</span></span><br><span class="line"><span class="class">&gt;&gt;&gt; <span class="title">def</span> <span class="title">echo</span><span class="params">(o)</span>:</span></span><br><span class="line"><span class="meta">... </span>    <span class="keyword">print</span> o</span><br><span class="line">...</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>echo(ObjectCreator)  <span class="comment"># 你可以将类做为参数传给函数</span></span><br><span class="line">&lt;<span class="class"><span class="keyword">class</span> '<span class="title">__main__</span>.<span class="title">ObjectCreator</span>'&gt;</span></span><br><span class="line"><span class="class">&gt;&gt;&gt; <span class="title">print</span><span class="params">(hasattr<span class="params">(ObjectCreator, <span class="string">'new_attribute'</span>)</span>)</span></span></span><br><span class="line"><span class="class"><span class="title">Fasle</span></span></span><br><span class="line">&gt;&gt;&gt; ObjectCreator.new_attribute = 'foo'  # 你可以为类增加属性</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(hasattr(ObjectCreator, <span class="string">'new_attribute'</span>))</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(ObjectCreator.new_attribute))</span><br><span class="line">foo</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>ObjectCreatorMirror = ObjectCreator  <span class="comment"># 你可以将类赋值给一个变量</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(ObjectCreatorMirror())</span><br><span class="line">&lt;__main__.ObjectCreator object at <span class="number">0x8997b4c</span>&gt;</span><br></pre></td></tr></table></figure><p>在Python中有个内建函数type，这个函数能让我们知道一个对象的类型是什么。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="class"><span class="keyword">class</span> <span class="title">MyCls</span><span class="params">(object)</span>:</span></span><br><span class="line"><span class="meta">... </span>    <span class="keyword">pass</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(type(<span class="number">1</span>))</span><br><span class="line">&lt;type <span class="string">'int'</span>&gt;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(type(<span class="string">"1"</span>))</span><br><span class="line">&lt;type <span class="string">'str'</span>&gt;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(type(MyCls()))</span><br><span class="line">&lt;<span class="class"><span class="keyword">class</span> '<span class="title">__main__</span>.<span class="title">MyCls</span>'&gt;</span></span><br><span class="line"><span class="class">&gt;&gt;&gt; <span class="title">print</span><span class="params">(type<span class="params">(MyCls)</span>)</span> </span></span><br><span class="line"><span class="class">&lt;<span class="title">type</span> '<span class="title">type</span>'&gt;</span></span><br></pre></td></tr></table></figure><p>结果发现我们对类的类型进行查看的时候居然的<strong>type</strong><br>type函数还有一种完全不同的功能，那就是它可以动态地创建类。<br>type可以接受一个类的描述作为参数，然后返回一个类。（要知道，根据传入参数的不同，同一个函数拥有两种完全不同的用法是一件很傻的事情，但这在Python中是为了保持向后兼容性）<br>type可以这样工作：<br><code>type(类名，由父类名称组成的元组（针对继承的情况，可以为空），包含属性的字典（名称和值）)</code><br>如用type创建类：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>Test = type(<span class="string">"Test"</span>, () ,&#123;&#125;)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>Test()</span><br><span class="line">&lt;__main__.Test object at <span class="number">0x7f64a074af90</span>&gt;</span><br></pre></td></tr></table></figure><p><strong>注意</strong>：<br>type中第一个参数为类名，为字符串；<br>第二个参数为父类名，为继承所用，父类名不需要传入字符串；<br>第三个参数传入的是类属性，而不是实例属性。</p><p>我们还可以用type创建带有方法的类：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="function"><span class="keyword">def</span> <span class="title">echo_num</span><span class="params">(self)</span>:</span></span><br><span class="line"><span class="meta">... </span>    print(self.num)</span><br><span class="line"><span class="meta">... </span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>@staticmethod</span><br><span class="line"><span class="meta">... </span><span class="function"><span class="keyword">def</span> <span class="title">testStatic</span><span class="params">()</span>:</span></span><br><span class="line"><span class="meta">... </span>    print(<span class="string">"static method..."</span>) </span><br><span class="line"><span class="meta">... </span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>@classmethod</span><br><span class="line"><span class="meta">... </span><span class="function"><span class="keyword">def</span> <span class="title">testClass</span><span class="params">(cls)</span>:</span></span><br><span class="line"><span class="meta">... </span>    print(cls.num)</span><br><span class="line"><span class="meta">... </span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>Test = type(<span class="string">"Test"</span>, (), &#123;<span class="string">"num"</span>:<span class="number">100</span>, <span class="string">"echo_num"</span>:echo_num, <span class="string">"testStatic"</span>:testStatic, <span class="string">"testClass"</span>:testClass&#125;)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>test = Test()</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>test.num</span><br><span class="line"><span class="number">100</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>test.echo_num()</span><br><span class="line"><span class="number">100</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>test.testStatic()</span><br><span class="line">static method...</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>test.testClass()</span><br><span class="line"><span class="number">100</span></span><br></pre></td></tr></table></figure><p>可以看到，用type指定函数不需要太多花里胡哨的东西，无论是什么函数都可以用于创建类。</p><p>说了这么多，那么到底什么是元类呢？<br><strong>元类就是用来创建类的“东西”</strong>。<br>你创建类就是为了创建类的实例对象，不是吗？<br>但是我们已经学习到了Python中的类也是对象。<br>元类就是用来创建这些类（对象）的，元类就是类的类。<br>我们所了解的type就可以创建一个类。<br>这是因为函数type实际上是一个元类。<br>type就是Python在背后用来创建所有类的元类。<br>现在你想知道那为什么type会全部采用小写形式而不是Type呢？<br>好吧，我猜这是为了和str保持一致性，str是用来创建字符串对象的类，而int是用来创建整数对象的类。<br>type就是创建类对象的类。你可以通过检查<code>__class__</code>属性来看到这一点。<br>Python中所有的东西，注意，我是指所有的东西——都是对象。<br>这包括整数、字符串、函数以及类。它们全部都是对象，而且它们都是从一个类创建而来，这个类就是type。</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = <span class="number">100</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a.__class__</span><br><span class="line">&lt;<span class="class"><span class="keyword">class</span> '<span class="title">int</span>'&gt;</span></span><br><span class="line">&gt;&gt;&gt; b = "abc"</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b.__class__</span><br><span class="line">&lt;<span class="class"><span class="keyword">class</span> '<span class="title">str</span>'&gt;</span></span><br><span class="line"><span class="class">&gt;&gt;&gt; <span class="title">def</span> <span class="title">func</span><span class="params">()</span>:</span><span class="keyword">pass</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>func.__class__</span><br><span class="line">&lt;<span class="class"><span class="keyword">class</span> '<span class="title">function</span>'&gt;</span></span><br><span class="line"><span class="class">&gt;&gt;&gt; <span class="title">class</span> <span class="title">Cls</span><span class="params">(object)</span>:</span><span class="keyword">pass</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>Cls().__class__</span><br><span class="line">&lt;<span class="class"><span class="keyword">class</span> '<span class="title">__main__</span>.<span class="title">Cls</span>'&gt;</span></span><br></pre></td></tr></table></figure><p>从上面可以了解到，每个对象都可以查看<code>__class__</code>属性来查看这个对象的类型，也就是这个类是由什么类创建而来的<br>那么我们同样也能通过<code>__class__</code>来查看这个类是由什么类创建的，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a.__class__.__class__</span><br><span class="line">&lt;<span class="class"><span class="keyword">class</span> '<span class="title">type</span>'&gt;</span></span><br><span class="line"><span class="class">&gt;&gt;&gt; <span class="title">b</span>.<span class="title">__class__</span>.<span class="title">__class__</span></span></span><br><span class="line"><span class="class">&lt;<span class="title">class</span> '<span class="title">type</span>'&gt;</span></span><br><span class="line"><span class="class">&gt;&gt;&gt; <span class="title">func</span>.<span class="title">__class__</span>.<span class="title">__class__</span></span></span><br><span class="line"><span class="class">&lt;<span class="title">class</span> '<span class="title">type</span>'&gt;</span></span><br><span class="line"><span class="class">&gt;&gt;&gt; <span class="title">Cls</span><span class="params">()</span>.<span class="title">__class__</span>.<span class="title">__class__</span></span></span><br><span class="line"><span class="class">&lt;<span class="title">class</span> '<span class="title">type</span>'&gt;</span></span><br></pre></td></tr></table></figure><p>由此我们可以看出，无论是int还是str或者function等，都是由type创建出来的类。<br>因此，元类就是创建类这种对象的东西。<br>type就是Python的内建元类，当然了，你也可以创建自己的元类。</p><ul><li><p><strong><code>__metaclass__</code>属性</strong><br>你可以在定义一个类的时候为其添加<code>__metaclass__</code>属性。格式如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Foo</span><span class="params">(object)</span>:</span></span><br><span class="line">    __metaclass__ = something</span><br><span class="line">    ......</span><br></pre></td></tr></table></figure><p>如果你这么做了，Python就会用元类来创建类Foo。<br>你首先写下class Foo(object)，但是类Foo还没有在内存中创建。<br>Python会在类的定义中寻找<code>__metaclass__</code>属性，如果找到了，Python就会用它来创建类Foo，如果没有找到，就会用内建的type来创建这个类。<br>当你写如下代码时 :</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Foo</span><span class="params">(Bar)</span>:</span></span><br><span class="line">    <span class="keyword">pass</span></span><br></pre></td></tr></table></figure><p>Python做了如下的操作：<br>1、Foo中有<code>__metaclass__</code>这个属性吗？如果是，Python会通过<code>__metaclass__</code>创建一个名字为Foo的类(对象)<br>2、如果Python没有找到<code>__metaclass__</code>，它会继续在Bar（父类）中寻找<code>__metaclass__</code>属性，并尝试做和前面同样的操作。<br>3、如果Python在任何父类中都找不到<code>__metaclass__</code>，它就会在模块层次中去寻找<code>__metaclass__</code>，并尝试做同样的操作。<br>4、如果还是找不到<code>__metaclass__</code>,Python就会用内置的type来创建这个类对象。<br>现在的问题就是，你可以在<code>__metaclass__</code>中放置些什么代码呢？答案就是：可以创建一个类的东西。<br>那么什么可以用来创建一个类呢？type，或者任何使用到type或者子类化type的东东都可以。</p></li><li><p><strong>自定义元类</strong><br>元类的主要目的就是为了当创建类时能够自动地改变类。<br>通常，你会为API做这样的事情，你希望可以创建符合当前上下文的类。</p><p>假想一个很傻的例子，你决定在你的模块里所有的类的属性都应该是大写形式。<br>有好几种方法可以办到，但其中一种就是通过在模块级别设定<code>__metaclass__</code>。<br>采用这种方法，这个模块中的所有类都会通过这个元类来创建，我们只需要告诉元类把所有的属性都改成大写形式就万事大吉了。<br>幸运的是，<code>__metaclass__</code>实际上可以被任意调用，它并不需要是一个正式的类。<br>所以，我们这里就先以一个简单的函数作为例子开始。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">upper_attr</span><span class="params">(future_class_name, future_class_parents, future_class_attr)</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">#遍历属性字典，把不是__开头的属性名字变为大写</span></span><br><span class="line">    newAttr = &#123;&#125;</span><br><span class="line">    <span class="keyword">for</span> name,value <span class="keyword">in</span> future_class_attr.items():</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> name.startswith(<span class="string">"__"</span>):</span><br><span class="line">            newAttr[name.upper()] = value</span><br><span class="line"></span><br><span class="line">    <span class="comment">#调用type来创建一个类</span></span><br><span class="line">    <span class="keyword">return</span> type(future_class_name, future_class_parents, newAttr)</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Foo</span><span class="params">(object, metaclass=upper_attr)</span>:</span></span><br><span class="line">    bar = <span class="string">'bip'</span></span><br><span class="line"></span><br><span class="line">print(hasattr(Foo, <span class="string">'bar'</span>))</span><br><span class="line">print(hasattr(Foo, <span class="string">'BAR'</span>))</span><br><span class="line"></span><br><span class="line">f = Foo()</span><br><span class="line">print(f.BAR)</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">False</span><br><span class="line">True</span><br><span class="line">bip</span><br></pre></td></tr></table></figure><p>现在我们用一个真正的class来做一个元类：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">UpperAttrMetaClass</span><span class="params">(type)</span>:</span></span><br><span class="line">    <span class="comment"># __new__ 是在__init__之前被调用的特殊方法</span></span><br><span class="line">    <span class="comment"># __new__是用来创建对象并返回之的方法</span></span><br><span class="line">    <span class="comment"># 而__init__只是用来将传入的参数初始化给对象</span></span><br><span class="line">    <span class="comment"># 你很少用到__new__，除非你希望能够控制对象的创建</span></span><br><span class="line">    <span class="comment"># 这里，创建的对象是类，我们希望能够自定义它，所以我们这里改写__new__</span></span><br><span class="line">    <span class="comment"># 如果你希望的话，你也可以在__init__中做些事情</span></span><br><span class="line">    <span class="comment"># 还有一些高级的用法会涉及到改写__call__特殊方法，但是我们这里不用</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__new__</span><span class="params">(cls, future_class_name, future_class_parents, future_class_attr)</span>:</span></span><br><span class="line">        <span class="comment">#遍历属性字典，把不是__开头的属性名字变为大写</span></span><br><span class="line">        newAttr = &#123;&#125;</span><br><span class="line">        <span class="keyword">for</span> name,value <span class="keyword">in</span> future_class_attr.items():</span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">not</span> name.startswith(<span class="string">"__"</span>):</span><br><span class="line">                newAttr[name.upper()] = value</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 方法1：通过'type'来做类对象的创建</span></span><br><span class="line">        <span class="comment"># return type(future_class_name, future_class_parents, newAttr)</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 方法2：复用type.__new__方法</span></span><br><span class="line">        <span class="comment"># 这就是基本的OOP编程，没什么魔法</span></span><br><span class="line">        <span class="comment"># return type.__new__(cls, future_class_name, future_class_parents, newAttr)</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 方法3：使用super方法</span></span><br><span class="line">        <span class="keyword">return</span> super(UpperAttrMetaClass, cls).__new__(cls, future_class_name, future_class_parents, newAttr)</span><br><span class="line"></span><br><span class="line"><span class="comment">#python2的用法</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Foo</span><span class="params">(object)</span>:</span></span><br><span class="line">        __metaclass__ = UpperAttrMetaClass</span><br><span class="line">    bar = <span class="string">'bip'</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># python3的用法</span></span><br><span class="line"><span class="comment"># class Foo(object, metaclass = UpperAttrMetaClass):</span></span><br><span class="line"><span class="comment">#     bar = 'bip'</span></span><br><span class="line"></span><br><span class="line">print(hasattr(Foo, <span class="string">'bar'</span>))</span><br><span class="line"><span class="comment"># 输出: False</span></span><br><span class="line">print(hasattr(Foo, <span class="string">'BAR'</span>))</span><br><span class="line"><span class="comment"># 输出:True</span></span><br><span class="line"></span><br><span class="line">f = Foo()</span><br><span class="line">print(f.BAR)</span><br><span class="line"><span class="comment"># 输出:'bip'</span></span><br></pre></td></tr></table></figure><p>执行结果和上面的例子相同。<br>就是这样，除此之外，关于元类真的没有别的可说的了。<br>但就元类本身而言，它们其实是很简单的：<br>1、拦截类的创建<br>2、修改类<br>3、返回修改之后的类<br>究竟为什么要使用元类？<br>现在回到我们的大主题上来，究竟是为什么你会去使用这样一种容易出错且晦涩的特性？好吧，一般来说，你根本就用不上它：<br>“元类就是深度的魔法，99%的用户应该根本不必为此操心。如果你想搞清楚究竟是否需要用到元类，那么你就不需要它。那些实际用到元类的人都非常清楚地知道他们需要做什么，而且根本不需要解释为什么要用元类。” —— Python界的领袖 Tim Peters</p></li></ul><h3 id="垃圾回收"><a href="#垃圾回收" class="headerlink" title="垃圾回收"></a>垃圾回收</h3><ul><li><p><strong>小整数对象池</strong><br>整数在程序中的使用非常广泛，Python为了优化速度，使用了小整数对象池，避免为整数频繁申请和销毁内存空间。<br>Python对小整数的定义是 [-5, 257) 这些整数对象是提前建立好的，不会被垃圾回收。<br>在一个Python的程序中，所有位于这个范围内的整数使用的都是同一个对象。<br>同理，单个字母也是这样的。但是当定义2个相同的字符串时，引用计数为0，触发垃圾回收。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = <span class="number">-6</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = <span class="number">-6</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a) == id(b)</span><br><span class="line"><span class="literal">False</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = <span class="number">-5</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = <span class="number">-5</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a) == id(b)</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = <span class="number">256</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = <span class="number">256</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a) == id(b)</span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = <span class="number">257</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = <span class="number">257</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a) == id(b)</span><br><span class="line"><span class="literal">False</span></span><br></pre></td></tr></table></figure><p>但是不在这个范围内的整数，Python是不会固定内存的，每一次都会创建一个新的对象，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = <span class="number">1000</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = <span class="number">1000</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a) == id(b)</span><br><span class="line"><span class="literal">False</span></span><br></pre></td></tr></table></figure></li><li><p><strong>intern机制</strong><br>一个字符串中如果没有空格（也就是说这个字符串是一个”单词”），那么在Python中会采用intern机制。<br>重复创建相同的字符串是不会创建新的对象的，而是直接把之前的字符串的引用返回出来。<br>这就是所谓的intern机制。如果字符串中有空格的话，那么就不会开启intern机制。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = <span class="string">"HelloWorld"</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = <span class="string">"HelloWorld"</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a) == id(b)  </span><br><span class="line"><span class="literal">True</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = <span class="string">"Hello World"</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = <span class="string">"Hello World"</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>id(a) == id(b)   </span><br><span class="line"><span class="literal">False</span></span><br></pre></td></tr></table></figure></li><li><p><strong>引用计数机制</strong><br>Python中的垃圾回收机制是以引用计数为主的。<br>python里每一个东西都是对象，它们的核心就是一个结构体：PyObject：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> struct_object &#123;</span><br><span class="line">    <span class="keyword">int</span> ob_refcnt;</span><br><span class="line">    struct_typeobject *ob_type;</span><br><span class="line">&#125; PyObject;</span><br></pre></td></tr></table></figure><p>PyObject是每个对象必有的内容，其中ob_refcnt就是做为引用计数。<br>当一个对象有新的引用时，它的ob_refcnt就会增加，当引用它的对象被删除，它的ob_refcnt就会减少：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> Py_INCREF(op)   ((op)-&gt;ob_refcnt++) <span class="comment">//增加计数</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> Py_DECREF(op) \ <span class="comment">//减少计数</span></span></span><br><span class="line">    <span class="keyword">if</span> (--(op)-&gt;ob_refcnt != <span class="number">0</span>) \</span><br><span class="line">        ; \</span><br><span class="line">    <span class="keyword">else</span> \</span><br><span class="line">        __Py_Dealloc((PyObject *)(op))</span><br></pre></td></tr></table></figure><p>当引用计数为0的时候，该对象生命就结束了。<br>引用计数机制的优点：<br>1、简单<br>2、实时性：一旦没有引用，内存就直接释放了。不用像其他机制等到特定时机。实时性还带来一个好处：处理回收内存的时间分摊到了平时。<br>引用计数机制的缺点：<br>1、维护引用计数消耗资源<br>2、循环引用</p><p>循环引用如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">list1 = []</span><br><span class="line">list2 = []</span><br><span class="line">list1.append(list2)</span><br><span class="line">list2.append(list1)</span><br></pre></td></tr></table></figure><p>list1与list2相互引用，如果不存在其他对象对它们的引用，list1与list2的引用计数也仍然为1，所占用的内存永远无法被回收，这将是致命的。<br>对于如今的强大硬件，缺点1尚可接受，但是循环引用导致内存泄露，注定python还将引入新的回收机制。(标记清除和分代收集)</p><p>导致引用计数+1的情况：<br>1、对象被创建，例如a=23<br>2、对象被引用，例如b=a<br>3、对象被作为参数，传入到一个函数中，例如func(a)<br>4、对象作为一个元素，存储在容器中，例如list1=[a,a]</p><p>导致引用计数-1的情况：<br>1、对象的别名被显式销毁，例如del a<br>2、对象的别名被赋予新的对象，例如a=24<br>3、一个对象离开它的作用域，例如f函数执行完毕时，func函数中的局部变量（全局变量不会）<br>4、对象所在的容器被销毁，或从容器中删除对象</p><p>查看一个对象的引用计数：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> sys</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = <span class="string">"Hello"</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>sys.getrefcount(a) </span><br><span class="line"><span class="number">2</span></span><br></pre></td></tr></table></figure><p>这样可以查看a对象的引用计数，但是会比正常计数大1，因为调用<code>sys.getrefcount</code>函数的时候将a传入，这会让a的引用计数+1</p></li><li><p><strong>gc模块</strong><br>gc模块提供一个接口给开发者设置垃圾回收的选项。<br>上面说到，采用引用计数的方法管理内存的一个缺陷是循环引用，而gc模块的一个主要功能就是解决循环引用的问题。</p><p>常用函数：<br>1、<code>gc.set_debug(flags)</code> 设置gc的debug日志，一般设置为gc.DEBUG_LEAK<br>2、<code>gc.collect([generation])</code> 显式进行垃圾回收，可以输入参数，0代表只检查第一代的对象，1代表检查一，二代的对象，2代表检查一，二，三代的对象，如果不传参数，执行一个full collection，也就是等于传2。返回不可达（unreachable objects）对象的数目<br>3、<code>gc.get_threshold()</code> 获取的gc模块中自动执行垃圾回收的频率。<br>4、<code>gc.set_threshold(threshold0[, threshold1[, threshold2])</code> 设置自动执行垃圾回收的频率。<br>5、<code>gc.get_count()</code> 获取当前自动执行垃圾回收的计数器，返回一个长度为3的列表</p><p>如果要使用gc模块的自动拉垃圾回收机制，必须要<code>import gc</code>模块，并且<code>is_enable()=True</code>才会启动自动垃圾回收。<br>这个机制的主要作用就是发现并处理不可达的垃圾对象。<br><strong>垃圾回收=垃圾检查+垃圾回收</strong></p><p>在Python中，采用分代收集的方法。<br>把对象分为三代，一开始，对象在创建的时候，放在一代中，如果在一次一代的垃圾检查中，改对象存活下来，就会被放到二代中，同理在一次二代的垃圾检查中，该对象存活下来，就会被放到三代中。<br>gc模块里面会有一个长度为3的列表的计数器，可以通过<code>gc.get_count()</code>获取。<br>例如(488,3,0)，其中488是指距离上一次一代垃圾检查，Python分配内存的数目减去释放内存的数目，注意是内存分配，而不是引用计数的增加。<br>例如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(gc.get_count())</span><br><span class="line">(<span class="number">76</span>, <span class="number">8</span>, <span class="number">3</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = ClassA()</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(gc.get_count())</span><br><span class="line">(<span class="number">77</span>, <span class="number">8</span>, <span class="number">3</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">del</span> a</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(gc.get_count())</span><br><span class="line">(<span class="number">76</span>, <span class="number">8</span>, <span class="number">3</span>)</span><br></pre></td></tr></table></figure></li></ul><h3 id="内建属性"><a href="#内建属性" class="headerlink" title="内建属性"></a>内建属性</h3><p>Python3中的内建属性和方法：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">[<span class="string">'__class__'</span>, <span class="string">'__delattr__'</span>, <span class="string">'__dict__'</span>, <span class="string">'__dir__'</span>, <span class="string">'__doc__'</span>, <span class="string">'__eq__'</span>, <span class="string">'__format__'</span>, <span class="string">'__ge__'</span>, <span class="string">'__getattribute__'</span>, <span class="string">'__gt__'</span>, <span class="string">'__hash__'</span>, <span class="string">'__init__'</span>, <span class="string">'__le__'</span>, <span class="string">'__lt__'</span>, <span class="string">'__module__'</span>, <span class="string">'__ne__'</span>, <span class="string">'__new__'</span>, <span class="string">'__reduce__'</span>, <span class="string">'__reduce_ex__'</span>, <span class="string">'__repr__'</span>, <span class="string">'__setattr__'</span>, <span class="string">'__sizeof__'</span>, <span class="string">'__str__'</span>, <span class="string">'__subclasshook__'</span>, <span class="string">'__weakref__'</span>]</span><br></pre></td></tr></table></figure><p><strong>注意</strong>：在Python3中定义一个类的时候如果不写父类的话会默认继承object类，而在Python2中不会这样，在Python2中无继承父类（称之为经典类）。<br>子类没有实现<code>__init__</code>方法时，默认自动调用父类的。<br>如定义<code>__init__</code>方法时，需自己手动调用父类的<code>__init__</code>方法</p><p>常用的专有属性表如下：</p><table><thead><tr><th align="center">常用专有属性</th><th align="center">说明</th><th>触发方式</th></tr></thead><tbody><tr><td align="center"><code>__init__</code></td><td align="center">构造初始化函数</td><td>创建实例后，赋值时使用，在<code>__new__</code>之后</td></tr><tr><td align="center"><code>__new__</code></td><td align="center">生成实例所需属性</td><td>创建实例时</td></tr><tr><td align="center"><code>__class__</code></td><td align="center">实例所在类</td><td><code>实例.__class__</code></td></tr><tr><td align="center"><code>__str__</code></td><td align="center">实例字符串表示，可读性</td><td>print(类实例)，如果没有实现，会使用repr结果</td></tr><tr><td align="center"><code>__repr__</code></td><td align="center">实例字符串表示，准确性</td><td>类实例回车（交互式），或使用print(repr(类实例))</td></tr><tr><td align="center"><code>__del__</code></td><td align="center">析构方法</td><td>del删除实例或实例被回收</td></tr><tr><td align="center"><code>__dict__</code></td><td align="center">实例自定义属性</td><td><code>vars.(实例.__dict__)</code></td></tr><tr><td align="center"><code>__doc__</code></td><td align="center">类文档，子类不继承</td><td>help(类或实例)</td></tr><tr><td align="center"><code>__getattribute__</code></td><td align="center">属性访问拦截器</td><td>访问实例属性时</td></tr><tr><td align="center"><code>__bases__</code></td><td align="center">类的所有父类构成元素</td><td><code>类名.__bases__</code></td></tr></tbody></table><p>重点说一下<code>__getattribute__</code>，例子如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.a = <span class="string">"a"</span> </span><br><span class="line">        self.b = <span class="string">"b"</span> </span><br><span class="line"></span><br><span class="line">    <span class="comment"># 访问属性时进行拦截</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__getattribute__</span><span class="params">(self, obj)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> obj == <span class="string">"a"</span>:</span><br><span class="line">            print(<span class="string">"log a"</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="string">"A"</span> </span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">return</span> object.__getattribute__(self,obj)</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>t = Test()</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>t.a</span><br><span class="line">log a</span><br><span class="line"><span class="string">'A'</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>t.b</span><br><span class="line"><span class="string">'b'</span></span><br></pre></td></tr></table></figure><p>注意：<code>__getattribute__</code>是有坑的，如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> time</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Person</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__getattribute__</span><span class="params">(self,obj)</span>:</span></span><br><span class="line">        print(<span class="string">"---test---"</span>)</span><br><span class="line">        <span class="keyword">if</span> obj.startswith(<span class="string">"a"</span>):</span><br><span class="line">            <span class="keyword">return</span> <span class="string">"hahha"</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            time.sleep(<span class="number">0.01</span>)</span><br><span class="line">            <span class="keyword">return</span> self.test</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">(self)</span>:</span></span><br><span class="line">        print(<span class="string">"heihei"</span>)</span><br><span class="line"></span><br><span class="line">t = Person()</span><br><span class="line"></span><br><span class="line">print(t.a)  <span class="comment">#返回hahha</span></span><br><span class="line"></span><br><span class="line">print(t.b)  <span class="comment">#会让程序死掉</span></span><br></pre></td></tr></table></figure><p>程序执行后，首先打印了一个test然后跟着打印了haha，之后就一直打印test直到程序死掉，报错：<br><code>RecursionError: maximum recursion depth exceeded while calling a Python object</code>（递归错误）</p><p>原因：当t.b执行时，会调用Person类中定义的<strong>getattribute</strong>方法，但是在这个方法的执行过程中if条件不满足。<br>所以，程序执行else里面的代码，即return self.test。<br>问题就在这，因为return 需要把self.test的值返回，那么首先要获取self.test的值，因为self此时就是t这个对象，所以self.test就是t.test。<br>此时要获取t这个对象的test属性，那么就会跳转到<strong>getattribute</strong>方法去执行，即此时产生了递归调用，由于这个递归过程中没有判断什么时候退出，所以这个程序会永无休止的运行下去，又因为每次调用函数，就需要保存一些数据，那么随着调用的次数越来越多，最终内存吃光，所以程序崩溃<br><strong>注意：以后不要在<strong>getattribute</strong>方法中调用self.xxxx</strong></p><h3 id="内建函数"><a href="#内建函数" class="headerlink" title="内建函数"></a>内建函数</h3><p>启动python解释器，输入dir(<strong>builtins</strong>), 可以看到很多python解释器启动后默认加载的属性和函数，这些函数称之为<strong>内建函数</strong><br>这些函数因为在编程时使用较多，cpython解释器用c语言实现了这些函数，启动解释器时默认加载。<br>这些函数数量众多，不宜记忆，开发时不是都用到的，待用到时再help(function), 查看如何使用，或结合网络查询即可，在这里介绍些常用的内建函数。</p><ul><li><p><strong>range</strong></p><figure class="highlight"><table><tr><td class="code"><pre><span class="line">range(stop) -&gt; list of integers</span><br><span class="line">range(start, stop[, step]) -&gt; list of integers</span><br></pre></td></tr></table></figure><p><strong>start</strong>:计数从start开始。默认是从0开始。例如range（5）等价于range（0， 5）;<br><strong>stop</strong>:到stop结束，但不包括stop.例如：range（0， 5） 是[0, 1, 2, 3, 4]没有5<br><strong>step</strong>:每次跳跃的间距，默认为1。例如：range（0， 5） 等价于 range(0, 5, 1)<br>python2中range返回列表，python3中range返回一个迭代值。如果想得到列表,可通过list函数。<br>如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">Python <span class="number">2.7</span><span class="number">.12</span> (default, Dec  <span class="number">4</span> <span class="number">2017</span>, <span class="number">14</span>:<span class="number">50</span>:<span class="number">18</span>) </span><br><span class="line">[GCC <span class="number">5.4</span><span class="number">.0</span> <span class="number">20160609</span>] on linux2</span><br><span class="line">Type <span class="string">"help"</span>, <span class="string">"copyright"</span>, <span class="string">"credits"</span> <span class="keyword">or</span> <span class="string">"license"</span> <span class="keyword">for</span> more information.</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = range(<span class="number">5</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>]</span><br><span class="line"></span><br><span class="line">Python <span class="number">3.5</span><span class="number">.2</span> (default, Nov <span class="number">23</span> <span class="number">2017</span>, <span class="number">16</span>:<span class="number">37</span>:<span class="number">01</span>) </span><br><span class="line">[GCC <span class="number">5.4</span><span class="number">.0</span> <span class="number">20160609</span>] on linux</span><br><span class="line">Type <span class="string">"help"</span>, <span class="string">"copyright"</span>, <span class="string">"credits"</span> <span class="keyword">or</span> <span class="string">"license"</span> <span class="keyword">for</span> more information.</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = range(<span class="number">5</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">range(<span class="number">0</span>, <span class="number">5</span>)</span><br></pre></td></tr></table></figure></li><li><p><strong>map</strong><br>map函数会根据提供的函数对指定序列做映射</p><figure class="highlight"><table><tr><td class="code"><pre><span class="line">map(function, sequence[, sequence, ...]) -&gt; list</span><br></pre></td></tr></table></figure><p><strong>function</strong>:是一个函数<br><strong>sequence</strong>:是一个或多个序列,取决于function需要几个参数<br>参数序列中的每一个元素分别调用function函数，返回包含每次function函数返回值的list。<br><strong>注意：在Python2中map返回的是一个列表，而在Python3中返回的是一个迭代器。</strong></p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">Python <span class="number">2.7</span><span class="number">.12</span> (default, Dec  <span class="number">4</span> <span class="number">2017</span>, <span class="number">14</span>:<span class="number">50</span>:<span class="number">18</span>) </span><br><span class="line">[GCC <span class="number">5.4</span><span class="number">.0</span> <span class="number">20160609</span>] on linux2</span><br><span class="line">Type <span class="string">"help"</span>, <span class="string">"copyright"</span>, <span class="string">"credits"</span> <span class="keyword">or</span> <span class="string">"license"</span> <span class="keyword">for</span> more information.</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>map(<span class="keyword">lambda</span> x:x*x, [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>])</span><br><span class="line">[<span class="number">1</span>, <span class="number">4</span>, <span class="number">9</span>]</span><br><span class="line"></span><br><span class="line">Python <span class="number">3.5</span><span class="number">.2</span> (default, Nov <span class="number">23</span> <span class="number">2017</span>, <span class="number">16</span>:<span class="number">37</span>:<span class="number">01</span>) </span><br><span class="line">[GCC <span class="number">5.4</span><span class="number">.0</span> <span class="number">20160609</span>] on linux</span><br><span class="line">Type <span class="string">"help"</span>, <span class="string">"copyright"</span>, <span class="string">"credits"</span> <span class="keyword">or</span> <span class="string">"license"</span> <span class="keyword">for</span> more information.</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = map(<span class="keyword">lambda</span> x:x*x, [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>])</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>next(a)</span><br><span class="line"><span class="number">1</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>next(a)</span><br><span class="line"><span class="number">4</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>next(a)</span><br><span class="line"><span class="number">9</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>next(a)</span><br><span class="line">Traceback (most recent call last):</span><br><span class="line">  File <span class="string">"&lt;stdin&gt;"</span>, line <span class="number">1</span>, <span class="keyword">in</span> &lt;module&gt;</span><br><span class="line">StopIteration</span><br></pre></td></tr></table></figure><p>以Python2为例：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>map(<span class="keyword">lambda</span> x,y:x+y, [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>], [<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>])</span><br><span class="line">[<span class="number">5</span>, <span class="number">7</span>, <span class="number">9</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="function"><span class="keyword">def</span> <span class="title">func</span><span class="params">(x,y)</span>:</span></span><br><span class="line"><span class="meta">... </span>    <span class="keyword">return</span> (x,y)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>]  </span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>b = [<span class="string">'a'</span>, <span class="string">'b'</span>, <span class="string">'c'</span>, <span class="string">'d'</span>, <span class="string">'e'</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>map(func, a, b)</span><br><span class="line">[(<span class="number">1</span>, <span class="string">'a'</span>), (<span class="number">2</span>, <span class="string">'b'</span>), (<span class="number">3</span>, <span class="string">'c'</span>), (<span class="number">4</span>, <span class="string">'d'</span>), (<span class="number">5</span>, <span class="string">'e'</span>)]</span><br></pre></td></tr></table></figure><p>简单来说，map就是利用函数来生成列表/迭代器</p></li><li><p><strong>filter</strong><br>filter函数会对指定序列执行过滤操作</p><figure class="highlight"><table><tr><td class="code"><pre><span class="line">filter(function or None, sequence) -&gt; list, tuple, or string</span><br></pre></td></tr></table></figure><p><strong>function</strong>:接受一个参数，返回布尔值True或False<br><strong>sequence</strong>:序列可以是str，tuple，list<br>filter函数会对序列参数sequence中的每个元素调用function函数，最后返回的结果包含调用结果为True的元素。<br>python2中返回值的类型和参数sequence的类型相同，Python3返回迭代器：<br>Python3：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = filter(<span class="keyword">lambda</span> x:x%<span class="number">2</span>, [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>])</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>next(a)</span><br><span class="line"><span class="number">1</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>next(a)</span><br><span class="line"><span class="number">3</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>next(a)</span><br><span class="line"><span class="number">5</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>next(a)</span><br><span class="line">Traceback (most recent call last):</span><br><span class="line">  File <span class="string">"&lt;stdin&gt;"</span>, line <span class="number">1</span>, <span class="keyword">in</span> &lt;module&gt;</span><br><span class="line">StopIteration</span><br></pre></td></tr></table></figure><p>Python2：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>filter(<span class="keyword">lambda</span> x:x%<span class="number">2</span>, [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>])</span><br><span class="line">[<span class="number">1</span>, <span class="number">3</span>, <span class="number">5</span>]</span><br></pre></td></tr></table></figure></li><li><p><strong>reduce</strong><br>reduce函数，reduce函数会对参数序列中元素进行累积</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">reduce(...)</span><br><span class="line">    reduce(function, sequence[, initial]) -&gt; value</span><br><span class="line"></span><br><span class="line">    Apply a function of two arguments cumulatively to the items of a sequence,<span class="keyword">from</span> left to right, so <span class="keyword">as</span> to reduce the sequence to a single value.For example, reduce(<span class="keyword">lambda</span> x, y: x+y, [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>]) calculates    ((((<span class="number">1</span>+<span class="number">2</span>)+<span class="number">3</span>)+<span class="number">4</span>)+<span class="number">5</span>).  If initial <span class="keyword">is</span> present, it <span class="keyword">is</span> placed before the items of the sequence <span class="keyword">in</span> the calculation, <span class="keyword">and</span> serves <span class="keyword">as</span> a default when the sequence <span class="keyword">is</span> empty.</span><br></pre></td></tr></table></figure><p><strong>function</strong>:该函数有两个参数<br><strong>sequence</strong>:序列可以是str，tuple，list<br><strong>initial</strong>:固定初始值<br>reduce依次从sequence中取一个元素，和上一次调用function的结果做参数再次调用function。<br>第一次调用function时，如果提供initial参数，会以sequence中的第一个元素和initial作为参数调用function，否则会以序列sequence中的前两个元素做参数调用function。<br>注意function函数不能为None。</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>reduce(<span class="keyword">lambda</span> x,y:x+y, [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>])</span><br><span class="line"><span class="number">10</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>reduce(<span class="keyword">lambda</span> x,y:x+y, [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>], <span class="number">5</span>) </span><br><span class="line"><span class="number">15</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>reduce(<span class="keyword">lambda</span> x,y:x+y, [<span class="string">'a'</span>,<span class="string">'b'</span>,<span class="string">'c'</span>,<span class="string">'d'</span>], <span class="string">'e'</span>)</span><br><span class="line"><span class="string">'eabcd'</span></span><br><span class="line">&gt;&gt;&gt;</span><br></pre></td></tr></table></figure><p>在Python3里,reduce函数已经被从全局名字空间里移除了, 它现在被放置在fucntools模块里用的话要先引入：<code>from functools import reduce</code></p></li><li><p><strong>sorted</strong></p><figure class="highlight"><table><tr><td class="code"><pre><span class="line">sorted(iterable, cmp=None, key=None, reverse=False) --&gt; new sorted list</span><br></pre></td></tr></table></figure><p>这个函数可以对一个列表进行排序<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>sorted([<span class="number">1</span>,<span class="number">4</span>,<span class="number">2</span>,<span class="number">6</span>,<span class="number">3</span>,<span class="number">5</span>])</span><br><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>sorted([<span class="number">1</span>,<span class="number">4</span>,<span class="number">2</span>,<span class="number">6</span>,<span class="number">3</span>,<span class="number">5</span>],reverse=<span class="number">1</span>) </span><br><span class="line">[<span class="number">6</span>, <span class="number">5</span>, <span class="number">4</span>, <span class="number">3</span>, <span class="number">2</span>, <span class="number">1</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>sorted([<span class="string">'a'</span>,<span class="string">'d'</span>,<span class="string">'b'</span>,<span class="string">'f'</span>,<span class="string">'c'</span>,<span class="string">'e'</span>])          </span><br><span class="line">[<span class="string">'a'</span>, <span class="string">'b'</span>, <span class="string">'c'</span>, <span class="string">'d'</span>, <span class="string">'e'</span>, <span class="string">'f'</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>sorted([<span class="string">'a'</span>,<span class="string">'d'</span>,<span class="string">'b'</span>,<span class="string">'f'</span>,<span class="string">'c'</span>,<span class="string">'e'</span>],reverse=<span class="number">1</span>)</span><br><span class="line">[<span class="string">'f'</span>, <span class="string">'e'</span>, <span class="string">'d'</span>, <span class="string">'c'</span>, <span class="string">'b'</span>, <span class="string">'a'</span>]</span><br></pre></td></tr></table></figure></li></ul><h3 id="functools"><a href="#functools" class="headerlink" title="functools"></a>functools</h3><p>functools 是python2.5被引人的,一些工具函数放在此包里。<br>想使用functools中的函数，需要导入functools模块<br>在Python2中：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> functools</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>dir(functools)</span><br><span class="line">[<span class="string">'WRAPPER_ASSIGNMENTS'</span>, <span class="string">'WRAPPER_UPDATES'</span>, <span class="string">'__builtins__'</span>, <span class="string">'__doc__'</span>, <span class="string">'__file__'</span>, <span class="string">'__name__'</span>, <span class="string">'__package__'</span>, <span class="string">'cmp_to_key'</span>, <span class="string">'partial'</span>, <span class="string">'reduce'</span>, <span class="string">'total_ordering'</span>, <span class="string">'update_wrapper'</span>, <span class="string">'wraps'</span>]</span><br></pre></td></tr></table></figure><p>Python3中：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> functools</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>dir(functools)    </span><br><span class="line">[<span class="string">'MappingProxyType'</span>, <span class="string">'RLock'</span>, <span class="string">'WRAPPER_ASSIGNMENTS'</span>, <span class="string">'WRAPPER_UPDATES'</span>, <span class="string">'WeakKeyDictionary'</span>, <span class="string">'_CacheInfo'</span>, <span class="string">'_HashedSeq'</span>, <span class="string">'__all__'</span>, <span class="string">'__builtins__'</span>, <span class="string">'__cached__'</span>, <span class="string">'__doc__'</span>, <span class="string">'__file__'</span>, <span class="string">'__loader__'</span>, <span class="string">'__name__'</span>, <span class="string">'__package__'</span>, <span class="string">'__spec__'</span>, <span class="string">'_c3_merge'</span>, <span class="string">'_c3_mro'</span>, <span class="string">'_compose_mro'</span>, <span class="string">'_convert'</span>, <span class="string">'_find_impl'</span>, <span class="string">'_ge_from_gt'</span>, <span class="string">'_ge_from_le'</span>, <span class="string">'_ge_from_lt'</span>, <span class="string">'_gt_from_ge'</span>, <span class="string">'_gt_from_le'</span>, <span class="string">'_gt_from_lt'</span>, <span class="string">'_le_from_ge'</span>, <span class="string">'_le_from_gt'</span>, <span class="string">'_le_from_lt'</span>, <span class="string">'_lru_cache_wrapper'</span>, <span class="string">'_lt_from_ge'</span>, <span class="string">'_lt_from_gt'</span>, <span class="string">'_lt_from_le'</span>, <span class="string">'_make_key'</span>, <span class="string">'cmp_to_key'</span>, <span class="string">'get_cache_token'</span>, <span class="string">'lru_cache'</span>, <span class="string">'namedtuple'</span>, <span class="string">'partial'</span>, <span class="string">'partialmethod'</span>, <span class="string">'reduce'</span>, <span class="string">'singledispatch'</span>, <span class="string">'total_ordering'</span>, <span class="string">'update_wrapper'</span>, <span class="string">'wraps'</span>]</span><br></pre></td></tr></table></figure><p>python3中增加了更多工具函数，做业务开发时大多情况下用不到，此处介绍使用频率较高的2个函数。</p><ul><li><p><strong>partial</strong><br>把一个函数的某些参数设置默认值，返回一个新的函数，调用这个新函数会更简单。<br>如有一函数：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">(*args, **kw)</span>:</span></span><br><span class="line">    print(args)</span><br><span class="line">    print(kw)</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">from</span> functools <span class="keyword">import</span> partial</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>func1 = partial(test, <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>) </span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>func1()</span><br><span class="line">(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>)</span><br><span class="line">&#123;&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>func1(<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>)</span><br><span class="line">(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>)</span><br><span class="line">&#123;&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>func1(a=<span class="string">'a'</span>, b=<span class="string">'b'</span>)</span><br><span class="line">(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>)</span><br><span class="line">&#123;<span class="string">'b'</span>: <span class="string">'b'</span>, <span class="string">'a'</span>: <span class="string">'a'</span>&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>func2 = partial(test, a=<span class="number">3</span>, b=<span class="string">'linux'</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>func2()</span><br><span class="line">()</span><br><span class="line">&#123;<span class="string">'b'</span>: <span class="string">'linux'</span>, <span class="string">'a'</span>: <span class="number">3</span>&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>func2(<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>)</span><br><span class="line">(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>)</span><br><span class="line">&#123;<span class="string">'b'</span>: <span class="string">'linux'</span>, <span class="string">'a'</span>: <span class="number">3</span>&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>func2(a=<span class="string">'a'</span>, b=<span class="string">'b'</span>)</span><br><span class="line">()</span><br><span class="line">&#123;<span class="string">'b'</span>: <span class="string">'b'</span>, <span class="string">'a'</span>: <span class="string">'a'</span>&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong>wraps</strong><br>使用装饰器时，有一些细节需要被注意。<br>例如，被装饰后的函数其实已经是另外一个函数了（函数名等函数属性会发生改变）。<br>添加后由于函数名和函数的doc发生了改变，对测试结果有一些影响，例如:</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">note</span><span class="params">(func)</span>:</span></span><br><span class="line">    <span class="string">"note function"</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">inner</span><span class="params">()</span>:</span></span><br><span class="line">        <span class="string">"inner function"</span></span><br><span class="line">        print(<span class="string">'note something'</span>)</span><br><span class="line">        <span class="keyword">return</span> func()</span><br><span class="line">    <span class="keyword">return</span> inner</span><br><span class="line"></span><br><span class="line"><span class="meta">@note</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="string">"test function"</span></span><br><span class="line">    print(<span class="string">'I am test'</span>)</span><br><span class="line"></span><br><span class="line">test()</span><br><span class="line">print(test.__doc__)</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">note something</span><br><span class="line">I am test</span><br><span class="line">inner function</span><br></pre></td></tr></table></figure><p>可见，使用装饰器之后改变了函数的doc信息文档。<br>所以，Python的functools包中提供了一个叫wraps的装饰器来消除这样的副作用。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> functools <span class="keyword">import</span> wraps</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">note</span><span class="params">(func)</span>:</span></span><br><span class="line">    <span class="string">"note function"</span></span><br><span class="line"><span class="meta">    @wraps(func)</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">inner</span><span class="params">()</span>:</span></span><br><span class="line">        <span class="string">"inner function"</span></span><br><span class="line">        print(<span class="string">'note something'</span>)</span><br><span class="line">        <span class="keyword">return</span> func()</span><br><span class="line">    <span class="keyword">return</span> inner</span><br><span class="line"></span><br><span class="line"><span class="meta">@note</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="string">"test function"</span></span><br><span class="line">    print(<span class="string">'I am test'</span>)</span><br><span class="line"></span><br><span class="line">test()</span><br><span class="line">print(test.__doc__)</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">note something</span><br><span class="line">I am test</span><br><span class="line">test function</span><br></pre></td></tr></table></figure><p>函数doc文档又回到以前的文档了</p></li></ul><h3 id="调试"><a href="#调试" class="headerlink" title="调试"></a>调试</h3><p>pdb是基于命令行的调试工具，非常类似gnu的gdb（调试c/c++）。</p><table><thead><tr><th align="center">命令</th><th align="center">简写命令</th><th>作用</th></tr></thead><tbody><tr><td align="center">continue</td><td align="center">c</td><td>继续执行程序</td></tr><tr><td align="center">list</td><td align="center">l</td><td>查看代码</td></tr><tr><td align="center">step</td><td align="center">s</td><td>进入函数</td></tr><tr><td align="center">return</td><td align="center">r</td><td>执行到函数返回处</td></tr><tr><td align="center">quit</td><td align="center">q</td><td>终止并退出</td></tr><tr><td align="center">next</td><td align="center">n</td><td>逐行执行</td></tr><tr><td align="center">print</td><td align="center">p</td><td>显示变量值</td></tr><tr><td align="center">help</td><td align="center">h</td><td>帮助</td></tr><tr><td align="center">args</td><td align="center">a</td><td>查看传入参数</td></tr><tr><td align="center">break</td><td align="center">b</td><td>显示所有断点</td></tr><tr><td align="center">break #</td><td align="center">b #</td><td>在第#行设置断点</td></tr><tr><td align="center">break FILE:#</td><td align="center">b FILE:#</td><td>在FILE文件的第#行设置断点</td></tr><tr><td align="center">clear #</td><td align="center">cl #</td><td>删除#号断点</td></tr><tr><td align="center"></td><td align="center">回车</td><td>重复上条命令</td></tr></tbody></table><p>调试方法有多种，第一种是用Python命令的时候加参数进行pdb调试：<br><code>python -m pdb SOME.py</code><br>第二种是在交互式界面中进行调试（不推荐）：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pdb</span><br><span class="line">pdb.run(<span class="string">'testfun(args)'</span>) <span class="comment">#此时会打开pdb调试，注意：先使用s跳转到这个testfun函数中，然后就可以使用l看到代码了</span></span><br></pre></td></tr></table></figure><p>第三种是在程序中设置断点：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">...</span><br><span class="line"><span class="keyword">import</span> pdb </span><br><span class="line">pdb.set_trace() </span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>pdb 调试有个明显的缺陷就是对于多线程，远程调试等支持得不够好，同时没有较为直观的界面显示，不太适合大型的 python 项目。<br>而在较大的python项目中，这些调试需求比较常见，因此需要使用更为高级的调试工具。</p><h3 id="编码风格"><a href="#编码风格" class="headerlink" title="编码风格"></a>编码风格</h3><p>可以在百度等搜索引擎上搜索pep8中文就可以看见pep8文档的中文版本<br><a href="https://www.python.org/dev/peps/pep-0008/" target="_blank" rel="noopener">pep8 官网规范地址:</a><br>Guido的关键点之一是：代码更多是用来读而不是写。编码规范旨在改善Python代码的可读性。<br>风格指南强调一致性。项目、模块或函数保持一致都很重要。</p><ul><li><p><strong>参数缩进</strong></p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 对准左括号</span></span><br><span class="line">foo = long_function_name(var_one, var_two,</span><br><span class="line">                         var_three, var_four)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 不对准左括号，但加多一层缩进，以和后面内容区别。</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">long_function_name</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">        var_one, var_two, var_three,</span></span></span><br><span class="line"><span class="function"><span class="params">        var_four)</span>:</span></span><br><span class="line">    print(var_one)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 悬挂缩进必须加多一层缩进.</span></span><br><span class="line">foo = long_function_name(</span><br><span class="line">    var_one, var_two,</span><br><span class="line">    var_three, var_four)</span><br></pre></td></tr></table></figure></li><li><p><strong>条件缩进</strong></p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 没有额外缩进，不是很好看，个人不推荐.</span></span><br><span class="line"><span class="keyword">if</span> (this_is_one_thing <span class="keyword">and</span></span><br><span class="line">    that_is_another_thing):</span><br><span class="line">    do_something()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 添加注释</span></span><br><span class="line"><span class="keyword">if</span> (this_is_one_thing <span class="keyword">and</span></span><br><span class="line">    that_is_another_thing):</span><br><span class="line">    <span class="comment"># Since both conditions are true, we can frobnicate.</span></span><br><span class="line">    do_something()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 额外添加缩进,推荐。</span></span><br><span class="line"><span class="comment"># Add some extra indentation on the conditional continuation line.</span></span><br><span class="line"><span class="keyword">if</span> (this_is_one_thing</span><br><span class="line">        <span class="keyword">and</span> that_is_another_thing):</span><br><span class="line">    do_something()</span><br></pre></td></tr></table></figure></li><li><p><strong>列表缩进</strong></p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 右括号不回退，个人不推荐</span></span><br><span class="line">my_list = [</span><br><span class="line">    <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>,</span><br><span class="line">    <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>,</span><br><span class="line">    ]</span><br><span class="line">result = some_function_that_takes_arguments(</span><br><span class="line">    <span class="string">'a'</span>, <span class="string">'b'</span>, <span class="string">'c'</span>,</span><br><span class="line">    <span class="string">'d'</span>, <span class="string">'e'</span>, <span class="string">'f'</span>,</span><br><span class="line">    )</span><br><span class="line"></span><br><span class="line"><span class="comment"># 右括号回退</span></span><br><span class="line">my_list = [</span><br><span class="line">    <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>,</span><br><span class="line">    <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>,</span><br><span class="line">]</span><br><span class="line">result = some_function_that_takes_arguments(</span><br><span class="line">    <span class="string">'a'</span>, <span class="string">'b'</span>, <span class="string">'c'</span>,</span><br><span class="line">    <span class="string">'d'</span>, <span class="string">'e'</span>, <span class="string">'f'</span>,</span><br><span class="line">)</span><br></pre></td></tr></table></figure></li></ul><h2 id="进程"><a href="#进程" class="headerlink" title="进程"></a>进程</h2><p>有很多的场景中的事情是同时进行的，比如开车的时候 手和脚共同来驾驶汽车，再比如唱歌跳舞也是同时进行的。<br>这种概念在计算机中称之为<strong>多任务</strong>。<br>什么叫“多任务”呢？</p><blockquote><p>简单地说，就是操作系统可以同时运行多个任务。<br>打个比方，你一边在用浏览器上网，一边在听MP3，一边在用Word赶作业，这就是多任务，至少同时有3个任务正在运行。<br>还有很多任务悄悄地在后台同时运行着，只是桌面上没有显示而已。</p></blockquote><p>编写完毕的代码，在没有运行的时候，称之为<strong>程序</strong><br>正在运行着的代码，就成为<strong>进程</strong><br>进程，除了包含代码以外，还有需要运行的环境等，所以和程序是有区别的</p><h3 id="fork"><a href="#fork" class="headerlink" title="fork"></a>fork</h3><p>Python的os模块封装了常见的系统调用，其中就包括fork，可以在Python程序中轻松创建子进程：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> os</span><br><span class="line">ret = os.fork()</span><br><span class="line"><span class="keyword">if</span> ret == <span class="number">0</span>:</span><br><span class="line">    print(<span class="string">"Hello 1"</span>)</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    print(<span class="string">"Hello 2"</span>)</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Hello 2</span><br><span class="line">Hello 1</span><br></pre></td></tr></table></figure><p>那么我明明写了个条件语句，为什么会执行2次呢？</p><blockquote><p>当程序执行到os.fork()处时，操作系统会创建一个新的进程（子进程）<br>然后复制父进程的所有信息到子进程中同时执行<br>之后父子进程都会有返回值，父进程返回的是子进程的进程号(pid)，子进程得到的返回值将会是0<br>之后再同时执行之后的代码。</p></blockquote><p><strong>注意</strong>：这里父进程创建了子进程之后是不管子进程了的，父进程结束之后子进程如果没有结束就变成孤儿进程。<br>在Linux中执行Python程序的时候，只要父进程（也可以称之为主进程）一结束，就当做一个程序执行完毕，这时候会弹出命令提示符。如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> os <span class="keyword">import</span> fork</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep</span><br><span class="line">ret = fork()</span><br><span class="line"><span class="keyword">if</span> ret == <span class="number">0</span>:</span><br><span class="line">    print(<span class="string">"---Child-Begin---"</span>)</span><br><span class="line">    sleep(<span class="number">1</span>)</span><br><span class="line">    print(<span class="string">"---Child-End---"</span>)</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    print(<span class="string">"---Father-Begin---"</span>)</span><br><span class="line">    sleep(<span class="number">0.5</span>)</span><br><span class="line">    print(<span class="string">"---Father-End---"</span>)</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">python3 test.py</span><br><span class="line">---Father-Begin---</span><br><span class="line">---Child-Begin---</span><br><span class="line">---Father-End---</span><br><span class="line">root@LINUX pythonSource# ---Child-End---</span><br></pre></td></tr></table></figure><p>可以看到，当父进程结束后，系统是把整个程序当做执行完毕来处理的，这时候子进程还有没有执行完毕，命令提示符出来之后子进程才执行完毕。</p><h3 id="getpid-和-getppid"><a href="#getpid-和-getppid" class="headerlink" title="getpid 和 getppid"></a>getpid 和 getppid</h3><p>每个进程都有自己的进程号，也就是<strong>pid</strong>；<br>同时每个进程也有父进程号，为<strong>ppid</strong>，父进程号也就是创建自己的那个进程的pid。<br>os模块中可以通过getpid和getppid函数来获取当前进程的pid和ppid</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> os <span class="keyword">import</span> fork,getpid,getppid</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep</span><br><span class="line">ret = fork()</span><br><span class="line">print(<span class="string">"ret = %d"</span> %ret)</span><br><span class="line"><span class="keyword">if</span> ret == <span class="number">0</span>:</span><br><span class="line">    print(<span class="string">"---Child, pid:%s, ppid:%s---"</span> %(getpid(), getppid()))</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    print(<span class="string">"---Father, pid:%s, ppid:%s---"</span> %(getpid(), getppid()))</span><br><span class="line"></span><br><span class="line">print(<span class="string">"---%s Done---"</span> %getpid())</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ret &#x3D; 4913</span><br><span class="line">---Father, pid:4912, ppid:3826---</span><br><span class="line">---4912 Done---</span><br><span class="line">ret &#x3D; 0</span><br><span class="line">---Child, pid:4913, ppid:4912---</span><br><span class="line">---4913 Done---</span><br></pre></td></tr></table></figure><h3 id="多进程中的全局变量"><a href="#多进程中的全局变量" class="headerlink" title="多进程中的全局变量"></a>多进程中的全局变量</h3><p>在多进程中，每个进程中所有数据（包括全局变量）都各有拥有一份，互不影响。<br>简单来说，全局变量只对当前进程有效，在其他进程中不能共享。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> os <span class="keyword">import</span> fork</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep</span><br><span class="line"></span><br><span class="line">g_num = <span class="number">100</span> </span><br><span class="line">ret = fork()</span><br><span class="line"><span class="keyword">if</span> ret == <span class="number">0</span>:</span><br><span class="line">    print(<span class="string">"---process-1---"</span>)</span><br><span class="line">    g_num += <span class="number">1</span></span><br><span class="line">    print(<span class="string">"---process-1 g_num=%d---"</span>%g_num)</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    sleep(<span class="number">1</span>)</span><br><span class="line">    print(<span class="string">"---process-2---"</span>)</span><br><span class="line">    print(<span class="string">"---process-2 g_num=%d---"</span>%g_num)</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">---process-1---</span><br><span class="line">---process-1 g_num&#x3D;101---</span><br><span class="line">---process-2---</span><br><span class="line">---process-2 g_num&#x3D;100---</span><br></pre></td></tr></table></figure><h3 id="Process"><a href="#Process" class="headerlink" title="Process"></a>Process</h3><p>fork是Unix/Linux上面的底层函数，在Windows上不能执行fork函数，难道在Windows上无法用Python编写多进程的程序吗？<br>Python作为一个跨品台的语言，自然也应该提供一个跨平台的多进程支持。<br><strong>multiprocessing</strong>模块就是跨平台版本的多进程模块。<br>multiprocessing模块中提供了一个Process类来代表一个进程对象，用法如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Process([group [, target [, name [, args [, kwargs]]]]])</span><br><span class="line">    target：表示这个进程实例所调用对象；</span><br><span class="line">    args：表示调用对象的位置参数元组；</span><br><span class="line">    kwargs：表示调用对象的关键字参数字典；</span><br><span class="line">    name：为当前进程实例的别名；</span><br><span class="line">    group：大多数情况下用不到；</span><br></pre></td></tr></table></figure><p>用Process创建进程之后<code>对象名.start()</code>即可开启进程<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> multiprocessing <span class="keyword">import</span> Process</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep     </span><br><span class="line">                  </span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">()</span>:</span>       </span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">5</span>):</span><br><span class="line">        print(<span class="string">"---%d---"</span>%i)</span><br><span class="line">        sleep(<span class="number">0.1</span>)</span><br><span class="line"></span><br><span class="line">p = Process(target=test)</span><br><span class="line">p.start()</span><br><span class="line"></span><br><span class="line">print(<span class="string">"---main---"</span>)</span><br></pre></td></tr></table></figure><p>打印结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">---main---</span><br><span class="line">---0---</span><br><span class="line">---1---</span><br><span class="line">---2---</span><br><span class="line">---3---</span><br><span class="line">---4---</span><br></pre></td></tr></table></figure><p>可见主进程在创建子进程之后会继续执行，执行到最后一步的时候会等子进程执行完毕后主进程才会执行完毕。</p><p>Process类常用方法：<br><code>is_alive()</code>：判断进程实例是否还在执行；<br><code>join([timeout])</code>：是否等待进程实例执行结束，或等待多少秒；<br><code>start()</code>：启动进程实例（创建子进程）；<br><code>run()</code>：如果没有给定target参数，对这个对象调用start()方法时，就将执行对象中的run()方法；<br>terminate()：不管任务是否完成，立即终止；<br>Process类常用属性：<br><strong>name</strong>：当前进程实例别名，默认为Process-N，N为从1开始递增的整数；<br><strong>pid</strong>：当前进程实例的PID值；</p><h3 id="自定义-Process-子类"><a href="#自定义-Process-子类" class="headerlink" title="自定义 Process 子类"></a>自定义 Process 子类</h3><p>创建新的进程还能够使用类的方式，可以自定义一个类，继承Process类<br>每次实例化这个类的时候，就等同于实例化一个进程对象<br>如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> multiprocessing <span class="keyword">import</span> Process</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep     </span><br><span class="line">                  </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyProcess</span><span class="params">(Process)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">5</span>):</span><br><span class="line">            print(<span class="string">"---%d---"</span>%i)</span><br><span class="line">            sleep(<span class="number">0.1</span>)</span><br><span class="line"></span><br><span class="line">p = MyProcess()</span><br><span class="line">p.start()</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">5</span>):</span><br><span class="line">    print(<span class="string">"---main-%d---"</span>%i)</span><br><span class="line">    sleep(<span class="number">0.1</span>)</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">python3 test.py</span><br><span class="line">---main-0---</span><br><span class="line">---0---</span><br><span class="line">---main-1---</span><br><span class="line">---1---</span><br><span class="line">---main-2---</span><br><span class="line">---2---</span><br><span class="line">---main-3---</span><br><span class="line">---3---</span><br><span class="line">---main-4---</span><br><span class="line">---4---</span><br></pre></td></tr></table></figure><p>在自定义的Process类中，其实只需要重写run方法就行了，因为用start方法开启进程的时候会自动调用类中的run方法<br>因此我们在自定义Process类中的时候在run方法中写一些骚操作就完成了我们的自定义Process类<br>下面的一个比较全面的案例：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> os <span class="keyword">import</span> getpid,getppid</span><br><span class="line"><span class="keyword">from</span> multiprocessing <span class="keyword">import</span> Process</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep,time</span><br><span class="line"></span><br><span class="line"><span class="comment"># 继承Process类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Process_Class</span><span class="params">(Process)</span>:</span></span><br><span class="line">    <span class="comment"># 因为Process类本身也有__init__方法，这个子类相当于重写了这个方法，</span></span><br><span class="line">    <span class="comment"># 但这样就会带来一个问题，我们并没有完全的初始化一个Process类，所以就不能使用从这个类继承的一些方法和属性，</span></span><br><span class="line">    <span class="comment"># 最好的方法就是将继承类本身传递给Process.__init__方法，完成这些初始化操作</span></span><br><span class="line">    <span class="comment"># 然后再定义我们的想要的属性</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self,val)</span>:</span></span><br><span class="line">        Process.__init__(self)</span><br><span class="line">        self.val = val </span><br><span class="line"></span><br><span class="line">    <span class="comment">#重写了Process类的run()方法</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">        print(<span class="string">"子进程(%s) 开始执行，父进程为（%s）"</span>%(getpid(),getppid()))</span><br><span class="line">        t_start = time()</span><br><span class="line">        sleep(self.val)</span><br><span class="line">        t_stop = time()</span><br><span class="line">        print(<span class="string">"(%s)执行结束，耗时%0.2f秒"</span>%(getpid(),t_stop-t_start))</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">"__main__"</span>:</span><br><span class="line">    t_start = time()</span><br><span class="line">    print(<span class="string">"当前程序进程(%s)"</span>%getpid())    </span><br><span class="line">    p1 = Process_Class(<span class="number">2</span>)</span><br><span class="line">    <span class="comment">#对一个不包含target属性的Process类执行start()方法，就会运行这个类中的run()方法，所以这里会执行p1.run()</span></span><br><span class="line">    p1.start()</span><br><span class="line">    p1.join()</span><br><span class="line">    t_stop = time()</span><br><span class="line">    print(<span class="string">"(%s)执行结束，耗时%0.2f"</span>%(getpid(),t_stop-t_start))</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">当前程序进程(6106)</span><br><span class="line">子进程(6107) 开始执行，父进程为（6106）</span><br><span class="line">(6107)执行结束，耗时2.00秒</span><br><span class="line">(6106)执行结束，耗时2.01</span><br></pre></td></tr></table></figure><h3 id="Pool-进程池"><a href="#Pool-进程池" class="headerlink" title="Pool 进程池"></a>Pool 进程池</h3><blockquote><p>当需要创建的子进程数量不多时，可以直接利用multiprocessing中的Process动态成生多个进程，但如果是上百甚至上千个目标，手动的去创建进程的工作量巨大，此时就可以用到multiprocessing模块提供的Pool方法。<br>初始化Pool时，可以指定一个最大进程数，当有新的请求提交到Pool中时，如果池还没有满，那么就会创建一个新的进程用来执行该请求；但如果池中的进程数已经达到指定的最大值，那么该请求就会等待，直到池中有进程结束，才会创建新的进程来执行。</p></blockquote><p>multiprocessing.Pool常用函数解析：<br><code>apply_async(func[, args[, kwds]])</code>：使用非阻塞方式调用func（并行执行，堵塞方式必须等待上一个进程退出才能执行下一个进程），args为传递给func的参数列表，kwds为传递给func的关键字参数列表；<br><code>apply(func[, args[, kwds]])</code>：使用阻塞方式调用func（通常不使用，使用阻塞方式就变成单任务模式了）<br><code>close()</code>：关闭Pool，使其不再接受新的任务；<br><code>terminate()</code>：不管任务是否完成，立即终止；<br><code>join()</code>：主进程阻塞，和Process的join类似。等待子进程的退出， 必须在close或terminate之后使用。</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> multiprocessing <span class="keyword">import</span> Pool</span><br><span class="line"><span class="keyword">from</span> os <span class="keyword">import</span> getpid</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">worker</span><span class="params">(num)</span>:</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">3</span>):</span><br><span class="line">        print(<span class="string">"---pid=%d, num=%d---"</span>%(getpid(), num))</span><br><span class="line">        sleep(<span class="number">0.5</span>)</span><br><span class="line"></span><br><span class="line">pool = Pool(<span class="number">3</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">7</span>):</span><br><span class="line">    print(<span class="string">"---%d---"</span>%i)</span><br><span class="line">    pool.apply_async(worker,(i,))</span><br><span class="line"></span><br><span class="line">pool.close()</span><br><span class="line">pool.join()</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">---0---</span><br><span class="line">---1---</span><br><span class="line">---2---</span><br><span class="line">---3---</span><br><span class="line">---4---</span><br><span class="line">---5---</span><br><span class="line">---6---</span><br><span class="line">---pid&#x3D;6476, num&#x3D;0---</span><br><span class="line">---pid&#x3D;6478, num&#x3D;1---</span><br><span class="line">---pid&#x3D;6477, num&#x3D;2---</span><br><span class="line">---pid&#x3D;6478, num&#x3D;1---</span><br><span class="line">---pid&#x3D;6477, num&#x3D;2---</span><br><span class="line">---pid&#x3D;6476, num&#x3D;0---</span><br><span class="line">---pid&#x3D;6478, num&#x3D;1---</span><br><span class="line">---pid&#x3D;6477, num&#x3D;2---</span><br><span class="line">---pid&#x3D;6476, num&#x3D;0---</span><br><span class="line">---pid&#x3D;6478, num&#x3D;3---</span><br><span class="line">---pid&#x3D;6476, num&#x3D;4---</span><br><span class="line">---pid&#x3D;6477, num&#x3D;5---</span><br><span class="line">---pid&#x3D;6478, num&#x3D;3---</span><br><span class="line">---pid&#x3D;6477, num&#x3D;5---</span><br><span class="line">---pid&#x3D;6476, num&#x3D;4---</span><br><span class="line">---pid&#x3D;6477, num&#x3D;5---</span><br><span class="line">---pid&#x3D;6476, num&#x3D;4---</span><br><span class="line">---pid&#x3D;6478, num&#x3D;3---</span><br><span class="line">---pid&#x3D;6478, num&#x3D;6---</span><br><span class="line">---pid&#x3D;6478, num&#x3D;6---</span><br><span class="line">---pid&#x3D;6478, num&#x3D;6---</span><br></pre></td></tr></table></figure><blockquote><p>用进程池创建进程时，如果如果不用join方法来阻塞主进程的话，主进程执行完毕是不会等待子进程的，子进程将被直接销毁。这一点和Process有很大的区别。<br>使用apply/apply_async方法向进程池中添加任务的时候，如果添加的任务数量超过了进程池中的进程数，那么会导致这些添加的进程等待。直到进程池中有进程完成之后才进去。<br>调用close就会关闭进程池，此时不能再添加新的任务。</p></blockquote><h3 id="进程间通信"><a href="#进程间通信" class="headerlink" title="进程间通信"></a>进程间通信</h3><p>Process之间有时需要通信，操作系统提供了很多机制来实现进程间的通信。<br>这里主要介绍<strong>Queue</strong>来实现进程间通信。<br>Queue本身是一个消息列队程序，初始化Queue()对象时（例如：q=Queue()），若括号中没有指定最大可接收的消息数量，或数量为负值，那么就代表可接受的消息数量没有上限（直到内存的尽头）</p><ul><li><code>Queue.qsize()</code>：返回当前队列包含的消息数量；</li><li><code>Queue.empty()</code>：如果队列为空，返回True，反之False；</li><li><code>Queue.full()</code>：如果队列满了，返回True,反之False；</li><li><code>Queue.get([block[, timeout]])</code>：获取队列中的一条消息，然后将其从列队中移除，block默认值为True；</li><li><code>Queue.get_nowait()</code>：相当Queue.get(False)；</li><li><code>Queue.put(item,[block[, timeout]])</code>：将item消息写入队列，block默认值为True；</li><li><code>Queue.put_nowait(item)</code>：相当于Queue.put(item, False)；</li></ul><blockquote><p>关于get方法，如果block使用默认值，且没有设置timeout（单位秒），消息列队如果为空，此时程序将被阻塞（停在读取状态），直到从消息列队读到消息为止，如果设置了timeout，则会等待timeout秒，若还没读取到任何消息，则抛出”Queue.Empty”异常；如果block值为False，消息列队如果为空，则会立刻抛出”Queue.Empty”异常；<br>而关于put方法：如果block使用默认值，且没有设置timeout（单位秒），消息列队如果已经没有空间可写入，此时程序将被阻塞（停在写入状态），直到从消息列队腾出空间为止，如果设置了timeout，则会等待timeout秒，若还没空间，则抛出”Queue.Full”异常；如果block值为False，消息列队如果没有空间可写入，则会立刻抛出”Queue.Full”异常。</p></blockquote><p>Queue实例：<br>我们以Queue为例，在父进程中创建两个子进程，一个往Queue里写数据，一个从Queue里读数据：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> multiprocessing <span class="keyword">import</span> Process,Queue</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep</span><br><span class="line"></span><br><span class="line"><span class="comment"># 写数据函数</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">write</span><span class="params">(q)</span>:</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> list(<span class="string">"ABCDEFG"</span>):</span><br><span class="line">        print(<span class="string">"Put %s to queue"</span>%i)</span><br><span class="line">        q.put(i)</span><br><span class="line">        sleep(<span class="number">0.1</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 读数据函数</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">read</span><span class="params">(q)</span>:</span></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> q.empty():</span><br><span class="line">            value = q.get()</span><br><span class="line">            print(<span class="string">"Get %s from queue"</span>%value)</span><br><span class="line">            sleep(<span class="number">0.1</span>)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    <span class="comment"># 父进程创建Queue，并传给子进程</span></span><br><span class="line">    q = Queue()</span><br><span class="line"></span><br><span class="line">    pw = Process(target=write, args=(q,))</span><br><span class="line">    pr = Process(target=read, args=(q,))</span><br><span class="line"></span><br><span class="line">    pw.start()</span><br><span class="line">    pw.join()</span><br><span class="line">    pr.start()</span><br><span class="line">    pr.join</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Put A to queue</span><br><span class="line">Put B to queue</span><br><span class="line">Put C to queue</span><br><span class="line">Put D to queue</span><br><span class="line">Put E to queue</span><br><span class="line">Put F to queue</span><br><span class="line">Put G to queue</span><br><span class="line">Get A from queue</span><br><span class="line">Get B from queue</span><br><span class="line">Get C from queue</span><br><span class="line">Get D from queue</span><br><span class="line">Get E from queue</span><br><span class="line">Get F from queue</span><br><span class="line">Get G from queue</span><br></pre></td></tr></table></figure><p>进程池中的Queue<br>如果要使用Pool创建进程，就需要使用multiprocessing.Manager()中的Queue()<br>下面的实例演示了进程池中的进程如何通信：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> multiprocessing <span class="keyword">import</span> Manager,Pool</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep</span><br><span class="line"><span class="keyword">from</span> os <span class="keyword">import</span> getpid,getppid</span><br><span class="line">                     </span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">reader</span><span class="params">(q)</span>:</span>       </span><br><span class="line">    print(<span class="string">"reader start, pid:%s, ppid:%s"</span>%(getpid(), getppid()))</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(q.qsize()):</span><br><span class="line">        print(<span class="string">"reader从Queue获取消息：%s"</span>%q.get())</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">writer</span><span class="params">(q)</span>:</span></span><br><span class="line">    print(<span class="string">"writer start, pid:%s, ppid:%s"</span>%(getpid(), getppid()))</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="string">"ABCDE"</span>:</span><br><span class="line">        q.put(i)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    print(<span class="string">"%s Start"</span>%getpid())</span><br><span class="line">    q = Manager().Queue()</span><br><span class="line">    po = Pool()</span><br><span class="line">    po.apply(writer, (q,))</span><br><span class="line">    po.apply(reader, (q,))</span><br><span class="line">    po.close()</span><br><span class="line">    po.join()</span><br><span class="line">    print(<span class="string">"%s End"</span>%getpid())</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">16575 Start</span><br><span class="line">writer start, pid:16583, ppid:16575</span><br><span class="line">reader start, pid:16584, ppid:16575</span><br><span class="line">reader从Queue获取消息：A</span><br><span class="line">reader从Queue获取消息：B</span><br><span class="line">reader从Queue获取消息：C</span><br><span class="line">reader从Queue获取消息：D</span><br><span class="line">reader从Queue获取消息：E</span><br><span class="line">16575 End</span><br></pre></td></tr></table></figure><h3 id="异步"><a href="#异步" class="headerlink" title="异步"></a>异步</h3><p>我们前面所说的同步就是各线程按顺序执行，那么什么是异步呢？<br>简单来说：<br>同步调用就是你喊你朋友吃饭，你朋友在忙 ，你就一直在那等，等你朋友忙完了，你们一起去<br>异步调用就是你喊你朋友吃饭，你朋友说知道了，待会忙完去找你，你就去做别的了。<br>如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> multiprocessing <span class="keyword">import</span> Pool</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep</span><br><span class="line"><span class="keyword">from</span> os <span class="keyword">import</span> getpid,getppid</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">"---进程池中的进程---pid=%d,ppid=%d--"</span>%(getpid(),getppid()))</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">3</span>):</span><br><span class="line">        print(<span class="string">"----%d---"</span>%i)</span><br><span class="line">        sleep(<span class="number">1</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"hahah"</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test2</span><span class="params">(args)</span>:</span></span><br><span class="line">    print(<span class="string">"---callback func--pid=%d"</span>%getpid())</span><br><span class="line">    print(<span class="string">"---callback func--args=%s"</span>%args)</span><br><span class="line"></span><br><span class="line">pool = Pool(<span class="number">3</span>)</span><br><span class="line">pool.apply_async(func=test,callback=test2)</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">    sleep(<span class="number">1</span>)</span><br><span class="line">    print(<span class="string">"----主进程-pid=%d----"</span>%getpid())</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">---进程池中的进程---pid&#x3D;3446,ppid&#x3D;3445--</span><br><span class="line">----0---</span><br><span class="line">----主进程-pid&#x3D;3445----</span><br><span class="line">----1---</span><br><span class="line">----主进程-pid&#x3D;3445----</span><br><span class="line">----2---</span><br><span class="line">----主进程-pid&#x3D;3445----</span><br><span class="line">---callback func--pid&#x3D;3445</span><br><span class="line">---callback func--args&#x3D;hahah</span><br><span class="line">----主进程-pid&#x3D;3445----</span><br><span class="line">----主进程-pid&#x3D;3445----</span><br><span class="line">----主进程-pid&#x3D;3445----</span><br><span class="line">----主进程-pid&#x3D;3445----</span><br></pre></td></tr></table></figure><p>这里的异步是靠apply_async中的callback参数来实现的，将其中的callback参数设定为某个任务<br>等这个进程执行完之后会通知主进程执行callback设定的任务，此时主进程会放下自己的工作</p><h3 id="案例"><a href="#案例" class="headerlink" title="案例"></a>案例</h3><p>此案例为用多进程复制某个目录中的所有文本文件（不包括目录和其他文件）</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> multiprocessing <span class="keyword">import</span> Pool,Manager</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">copyFileTask</span><span class="params">(fileName,oldDirName,newDirName,queue)</span>:</span></span><br><span class="line">    <span class="string">"完成copy一个文件的功能"</span></span><br><span class="line">    fr = open(oldDirName+<span class="string">'/'</span>+fileName)</span><br><span class="line">    fw = open(newDirName+<span class="string">'/'</span>+fileName, <span class="string">"w"</span>)</span><br><span class="line"></span><br><span class="line">    content = fr.read()</span><br><span class="line">    fw.write(content)</span><br><span class="line"></span><br><span class="line">    fw.close()</span><br><span class="line">    fr.close()</span><br><span class="line"></span><br><span class="line">    queue.put(fileName)</span><br><span class="line">    sleep(<span class="number">0.1</span>)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 1. 获取要copy的目录名字</span></span><br><span class="line">    oldDirName = input(<span class="string">"请输入目录名: "</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 2. 创建目录</span></span><br><span class="line">    newDirName = oldDirName+<span class="string">"-1"</span></span><br><span class="line">    os.mkdir(newDirName)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 3. 获取old目录中所有文件的名字</span></span><br><span class="line">    fileNames = os.listdir(oldDirName)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 4. 使用多进程方式copy原目录中所有文件到新的目录中</span></span><br><span class="line">    pool = Pool(<span class="number">3</span>)</span><br><span class="line">    queue = Manager().Queue()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> tempFile <span class="keyword">in</span> fileNames:</span><br><span class="line">        pool.apply_async(copyFileTask, args=(tempFile,oldDirName,newDirName,queue,))</span><br><span class="line"></span><br><span class="line">    num = <span class="number">0</span></span><br><span class="line">    allNum = len(fileNames)</span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        queue.get()</span><br><span class="line">        num += <span class="number">1</span></span><br><span class="line">        copyRate = num/allNum</span><br><span class="line">        print(<span class="string">"\rcopy进度为：%.2f%%"</span>%(copyRate*<span class="number">100</span>), end=<span class="string">''</span>)</span><br><span class="line">        <span class="keyword">if</span> num == allNum:</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line"></span><br><span class="line">    print(<span class="string">"\n已完成copy..."</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure><h2 id="线程"><a href="#线程" class="headerlink" title="线程"></a>线程</h2><p>进程，能够完成多任务，比如在一台电脑上能够同时运行多个QQ<br>线程，也能够完成多任务，比如一个QQ中的多个聊天窗口<br><strong>进程是系统进行资源分配和调度的一个独立单位</strong><br>线程是进程的一个实体,是CPU调度和分派的基本单位,它是比进程更小的能独立运行的基本单位<br>线程自己基本上不拥有系统资源, 只拥有一点在运行中必不可少的资源(如程序计数器,一组寄存器和栈), 但是它可与同属一个进程的其他的线程共享进程所拥有的全部资源。<br>进程和线程的区别：</p><blockquote><p>1、一个程序至少有一个进程,一个进程至少有一个线程<br>2、 线程的划分尺度小于进程(资源比进程少)，使得多线程程序的并发性高<br>3、进程在执行过程中拥有独立的内存单元，而多个线程共享内存，从而极大地提高了程序的运行效率<br>4、线程不能够独立执行，必须依存在进程中</p></blockquote><p>线程和进程在使用上各有优缺点：线程执行开销小，但不利于资源的管理和保护；而进程正相反。</p><h3 id="threading"><a href="#threading" class="headerlink" title="threading"></a>threading</h3><p>python的thread模块是比较底层的模块<br>python的threading模块是对thread做了一些包装的，可以更加方便的被使用。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">()</span>:</span></span><br><span class="line">    sleep(<span class="number">1</span>)</span><br><span class="line">    print(<span class="string">"Hello World"</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">5</span>):</span><br><span class="line">        t = Thread(target=test)</span><br><span class="line">        t.start()</span><br></pre></td></tr></table></figure><p>执行时等待了1秒后直接打印5遍“Hello World”<br>可以明显看出使用了多线程并发的操作，花费时间要短很多<br>注意：创建好的线程，需要调用start()方法来启动<br>创建了线程之后其实是有个主线程的，主线程会等待所有的子线程执行完毕后结束<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">()</span>:</span></span><br><span class="line">    sleep(<span class="number">1</span>)</span><br><span class="line">    print(<span class="string">"Hello World"</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    print(<span class="string">"Start..."</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">5</span>):</span><br><span class="line">        t = Thread(target=test)</span><br><span class="line">        t.start()</span><br><span class="line">    print(<span class="string">"End..."</span>)</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Start...</span><br><span class="line">End...</span><br><span class="line">Hello World</span><br><span class="line">Hello World</span><br><span class="line">Hello World</span><br><span class="line">Hello World</span><br><span class="line">Hello World</span><br></pre></td></tr></table></figure><p>这时主线程是一路执行到底但是不会退出（因为没有显示命令提示符）<br>等到所有的子线程执行结束之后才弹出命令提示符，代表着主线程执行结束</p><h3 id="自定义线程类"><a href="#自定义线程类" class="headerlink" title="自定义线程类"></a>自定义线程类</h3><p>通过上一小节，能够看出，通过使用threading模块能完成多任务的程序开发<br>为了让每个线程的封装性更完美，所以使用threading模块时，往往会定义一个新的子类class，只要继承<strong>threading.Thread</strong>就可以了，然后重写<strong>run</strong>方法。<br>python的<strong>threading.Thread</strong>类有一个run方法，用于定义线程的功能函数，可以在自己的线程类中覆盖该方法<br>而创建自己的线程实例后，通过Thread类的start方法，可以启动该线程，交给python虚拟机进行调度，当该线程获得执行的机会时，就会调用run方法执行线程。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyThread</span><span class="params">(Thread)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">3</span>):</span><br><span class="line">            sleep(<span class="number">0.1</span>)</span><br><span class="line">            print(<span class="string">"---%s:%d---"</span>%(self.name, i))</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    print(<span class="string">"Start..."</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">3</span>):</span><br><span class="line">        t = MyThread()</span><br><span class="line">        t.start()</span><br><span class="line">    print(<span class="string">"End..."</span>)</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Start...</span><br><span class="line">End...</span><br><span class="line">---Thread-2:0---</span><br><span class="line">---Thread-1:0---</span><br><span class="line">---Thread-3:0---</span><br><span class="line">---Thread-3:1---</span><br><span class="line">---Thread-1:1---</span><br><span class="line">---Thread-2:1---</span><br><span class="line">---Thread-2:2---</span><br><span class="line">---Thread-1:2---</span><br><span class="line">---Thread-3:2---</span><br></pre></td></tr></table></figure><p>和Process类似，线程对象自带的名字是Thread-#，创建第一个就是-1，第二个为-2，以此类推。<br>从代码和执行结果我们可以看出，多线程程序的执行顺序是不确定的。<br>当执行到sleep语句时，线程将被阻塞(Blocked)，到sleep结束后，线程进入就绪(Runnable)状态，等待调度。<br>而线程调度将自行选择一个线程执行。上面的代码中只能保证每个线程都运行完整个run函数，但是线程的启动顺序、run函数中每次循环的执行顺序都不能确定。</p><h3 id="多线程共享全局变量"><a href="#多线程共享全局变量" class="headerlink" title="多线程共享全局变量"></a>多线程共享全局变量</h3><p>在一个进程内的所有线程共享全局变量，能够在不适用其他方式的前提下完成多线程之间的数据共享(这点要比多进程要好)。<br>缺点就是，线程是对全局变量随意遂改可能造成多线程之间对全局变量的混乱(即线程非安全)。</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep</span><br><span class="line"></span><br><span class="line">g_num = <span class="number">100</span> </span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">work1</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">global</span> g_num</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">3</span>):</span><br><span class="line">        g_num += <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    print(<span class="string">"In work1, the num is %d"</span>%g_num)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">work2</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">global</span> g_num</span><br><span class="line">    print(<span class="string">"In work2, the num is %d"</span>%g_num)</span><br><span class="line"></span><br><span class="line">print(<span class="string">"Start, the num is %d"</span>%g_num)</span><br><span class="line">t1 = Thread(target=work1)</span><br><span class="line">t1.start()</span><br><span class="line">sleep(<span class="number">1</span>)</span><br><span class="line">t2 = Thread(target=work2)</span><br><span class="line">t2.start()</span><br><span class="line">print(<span class="string">"End, the num is %d"</span>%g_num)</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Start, the num is 100</span><br><span class="line">In work1, the num is 103</span><br><span class="line">In work2, the num is 103</span><br><span class="line">End, the num is 103</span><br></pre></td></tr></table></figure><p>如果在多线程中频繁地对某个全局变量进行操作的时候，有可能某个线程还没操作完就被其他线程操作了，这时候那个没操作完的也会当成操作完毕了。<br>这时候也许不会发生我们期望的结果，如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep</span><br><span class="line"></span><br><span class="line">g_num = <span class="number">0</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">work1</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">global</span> g_num</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1000000</span>):</span><br><span class="line">        g_num += <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    print(<span class="string">"In work1, the num is %d"</span>%g_num)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">work2</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">global</span> g_num</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1000000</span>):</span><br><span class="line">        g_num += <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    print(<span class="string">"In work2, the num is %d"</span>%g_num)</span><br><span class="line"></span><br><span class="line">t1 = Thread(target=work1)</span><br><span class="line">t1.start()</span><br><span class="line">t2 = Thread(target=work2)</span><br><span class="line">t2.start()</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">In work1, the num is 1217118</span><br><span class="line">In work2, the num is 1368661</span><br></pre></td></tr></table></figure><p>可以看到，我每个线程都对g_num这个全局变量加了1000000次，而得到的结果却不是2000000。<br>这就是所谓的线程不安全。</p><h3 id="互斥锁"><a href="#互斥锁" class="headerlink" title="互斥锁"></a>互斥锁</h3><p>当多个线程几乎同时修改某一个共享数据的时候，需要进行同步控制。<br>线程同步能够保证多个线程安全访问竞争资源，最简单的同步机制是引入互斥锁。</p><blockquote><p>互斥锁为资源引入一个状态：锁定/非锁定<br>某个线程要更改共享数据时，先将其锁定，此时资源的状态为“锁定”，其他线程不能更改<br>直到该线程释放资源，将资源的状态变成“非锁定”，其他的线程才能再次锁定该资源<br>互斥锁保证了每次只有一个线程进行写入操作，从而保证了多线程情况下数据的正确性。</p></blockquote><p>threading模块中定义了Lock类，可以方便的处理锁定：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#创建锁</span></span><br><span class="line">mutex = threading.Lock()</span><br><span class="line"><span class="comment">#锁定</span></span><br><span class="line">mutex.acquire([blocking])</span><br><span class="line"><span class="comment">#释放</span></span><br><span class="line">mutex.release()</span><br></pre></td></tr></table></figure><p>其中，锁定方法acquire可以有一个blocking参数:<br>如果设定blocking为True，则当前线程会堵塞，直到获取到这个锁为止（如果没有指定，那么默认为True）<br>如果设定blocking为False，则当前线程不会堵塞</p><p>现在我们就可以利用这个互斥锁来解决上面的1000000次加法出现的bug：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread, Lock</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep</span><br><span class="line"></span><br><span class="line">g_num = <span class="number">0</span> </span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">work1</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">global</span> g_num</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1000000</span>):</span><br><span class="line">        mutex.acquire()  <span class="comment">#上锁</span></span><br><span class="line">        g_num += <span class="number">1</span></span><br><span class="line">        mutex.release()  <span class="comment">#解锁</span></span><br><span class="line"></span><br><span class="line">    print(<span class="string">"In work1, the num is %d"</span>%g_num)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">work2</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">global</span> g_num</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1000000</span>):</span><br><span class="line">        mutex.acquire()  <span class="comment">#上锁</span></span><br><span class="line">        g_num += <span class="number">1</span></span><br><span class="line">        mutex.release()  <span class="comment">#解锁</span></span><br><span class="line"></span><br><span class="line">    print(<span class="string">"In work2, the num is %d"</span>%g_num)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建一把互斥锁，默认情况下是没有上锁的</span></span><br><span class="line">mutex = Lock()</span><br><span class="line">t1 = Thread(target=work1)</span><br><span class="line">t1.start()</span><br><span class="line">t2 = Thread(target=work2)</span><br><span class="line">t2.start()</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">In work1, the num is 1960577</span><br><span class="line">In work2, the num is 2000000</span><br></pre></td></tr></table></figure><h3 id="线程不共享局部变量"><a href="#线程不共享局部变量" class="headerlink" title="线程不共享局部变量"></a>线程不共享局部变量</h3><p>在多线程开发中，全局变量是多个线程都共享的数据，而局部变量等是各自线程的，是非共享的。</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread,current_thread</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">()</span>:</span></span><br><span class="line">    name = current_thread().name</span><br><span class="line">    print(<span class="string">"--- thread name is %s ---"</span>%name)</span><br><span class="line">    num = <span class="number">100</span></span><br><span class="line">    <span class="keyword">if</span> name == <span class="string">"Thread-1"</span>:</span><br><span class="line">        num += <span class="number">1</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        sleep(<span class="number">1</span>)</span><br><span class="line">    print(<span class="string">"--- thread name is %s, num = %d ---"</span>%(name, num))</span><br><span class="line"></span><br><span class="line">t1 = Thread(target=test)</span><br><span class="line">t2 = Thread(target=test)</span><br><span class="line">t1.start()</span><br><span class="line">t2.start()</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">--- thread name is Thread-1 ---</span><br><span class="line">--- thread name is Thread-1, num &#x3D; 101 ---</span><br><span class="line">--- thread name is Thread-2 ---</span><br><span class="line">--- thread name is Thread-2, num &#x3D; 100 ---</span><br></pre></td></tr></table></figure><p>由此可见，局部变量在各个线程之间是互不影响的。<br>因为各个线程在执行的过程中都是当做执行各自的函数，因此局部变量是不会收到其他线程的影响的。</p><h3 id="死锁"><a href="#死锁" class="headerlink" title="死锁"></a>死锁</h3><p>在线程间共享多个资源的时候，如果两个线程分别占有一部分资源并且同时等待对方的资源，就会造成死锁。<br>尽管死锁很少发生，但一旦发生就会造成应用的停止响应。</p><h3 id="同步"><a href="#同步" class="headerlink" title="同步"></a>同步</h3><p>同步就是<strong>协同步调，按预定的先后次序进行运行</strong>。<br>如:你说完，我再说。<br>“同”字从字面上容易理解为一起动作。其实不是，”同”字应是指协同、协助、互相配合。<br>如进程、线程同步，可理解为进程或线程A和B一块配合，A执行到一定程度时要依靠B的某个结果，于是停下来，示意B运行;B依言执行，再将结果给A;A再继续操作。<br>简单来说，<strong>同步就是按顺序执行</strong>。<br>如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread,Lock</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Task1</span><span class="params">(Thread)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            <span class="keyword">if</span> lock1.acquire():</span><br><span class="line">                print(<span class="string">"------Task 1 -----"</span>)</span><br><span class="line">                sleep(<span class="number">0.5</span>)</span><br><span class="line">                lock2.release()</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Task2</span><span class="params">(Thread)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            <span class="keyword">if</span> lock2.acquire():</span><br><span class="line">                print(<span class="string">"------Task 2 -----"</span>)</span><br><span class="line">                sleep(<span class="number">0.5</span>)</span><br><span class="line">                lock3.release()</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Task3</span><span class="params">(Thread)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            <span class="keyword">if</span> lock3.acquire():</span><br><span class="line">                print(<span class="string">"------Task 3 -----"</span>)</span><br><span class="line">                sleep(<span class="number">0.5</span>)</span><br><span class="line">                lock1.release()</span><br><span class="line"></span><br><span class="line"><span class="comment">#使用Lock创建出的锁默认没有“锁上”</span></span><br><span class="line">lock1 = Lock()</span><br><span class="line"><span class="comment">#创建另外一把锁，并且“锁上”</span></span><br><span class="line">lock2 = Lock()</span><br><span class="line">lock2.acquire()</span><br><span class="line"><span class="comment">#创建另外一把锁，并且“锁上”</span></span><br><span class="line">lock3 = Lock()</span><br><span class="line">lock3.acquire()</span><br><span class="line"></span><br><span class="line">t1 = Task1()</span><br><span class="line">t2 = Task2()</span><br><span class="line">t3 = Task3()</span><br><span class="line"></span><br><span class="line">t1.start()</span><br><span class="line">t2.start()</span><br><span class="line">t3.start()</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">------Task 1 -----</span><br><span class="line">------Task 2 -----</span><br><span class="line">------Task 3 -----</span><br><span class="line">------Task 1 -----</span><br><span class="line">------Task 2 -----</span><br><span class="line">------Task 3 -----</span><br><span class="line">------Task 1 -----</span><br><span class="line">------Task 2 -----</span><br><span class="line">------Task 3 -----</span><br><span class="line">------Task 1 -----</span><br><span class="line">------Task 2 -----</span><br><span class="line">------Task 3 -----</span><br></pre></td></tr></table></figure><p>这就是一个利用互斥锁来达到线程同步的一个效果。</p><h3 id="生产者和消费者"><a href="#生产者和消费者" class="headerlink" title="生产者和消费者"></a>生产者和消费者</h3><p>在线程世界里，生产者就是生产数据的线程，消费者就是消费数据的线程。<br>在多线程开发当中，如果生产者处理速度很快，而消费者处理速度很慢，那么生产者就必须等待消费者处理完，才能继续生产数据。<br>同样的道理，如果消费者的处理能力大于生产者，那么消费者就必须等待生产者。<br>为了解决这个问题于是引入了<strong>生产者和消费者模式</strong>。<br>生产者消费者模式是<strong>通过一个容器来解决生产者和消费者的强耦合问题</strong>。<br>生产者和消费者彼此之间不直接通讯，而通过阻塞队列来进行通讯，所以生产者生产完数据之后不用等待消费者处理，直接扔给阻塞队列，消费者不找生产者要数据，而是直接从阻塞队列里取，阻塞队列就相当于一个缓冲区，平衡了生产者和消费者的处理能力。<br>这个阻塞队列就是用来给生产者和消费者解耦的。<br>下面是一个生产者和消费者的案例：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep</span><br><span class="line"><span class="comment"># python2中</span></span><br><span class="line"><span class="comment">#from Queue import Queue</span></span><br><span class="line"><span class="comment"># python3中</span></span><br><span class="line"><span class="keyword">from</span> queue <span class="keyword">import</span> Queue</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Producer</span><span class="params">(Thread)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">global</span> queue</span><br><span class="line">        count = <span class="number">0</span></span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            <span class="keyword">if</span> queue.qsize() &lt; <span class="number">1000</span>:</span><br><span class="line">                <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">100</span>):</span><br><span class="line">                    count = count +<span class="number">1</span></span><br><span class="line">                    msg = <span class="string">'生成产品'</span>+str(count)</span><br><span class="line">                    queue.put(msg)</span><br><span class="line">                    print(msg)</span><br><span class="line">                sleep(<span class="number">5</span>)</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Consumer</span><span class="params">(Thread)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">global</span> queue</span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            <span class="keyword">if</span> queue.qsize() &gt; <span class="number">100</span>:</span><br><span class="line">                <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">3</span>):</span><br><span class="line">                    msg = self.name + <span class="string">'消费了 '</span>+queue.get()</span><br><span class="line">                    print(msg)</span><br><span class="line">                sleep(<span class="number">5</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    queue = Queue()</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">500</span>):</span><br><span class="line">        queue.put(<span class="string">"初始产品"</span>+str(i))</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 创建2个生产者</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">2</span>):</span><br><span class="line">        p = Producer()</span><br><span class="line">        p.start()</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 创建5个消费者</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">5</span>):</span><br><span class="line">        c = Consumer()</span><br><span class="line">        c.start()</span><br></pre></td></tr></table></figure><p>执行结果不予演示<br>Queue的说明<br>1、对于Queue，在多线程通信之间扮演重要的角色<br>2、添加数据到队列中，使用put()方法<br>3、从队列中取数据，使用get()方法<br>4、判断队列中是否还有数据，使用qsize()方法<br>简单来说，生产者和消费者模式就是创建了一个缓冲区来解决线程两端一边处理数据块而另一边处理数据慢的问题。</p><h3 id="ThreadLocal"><a href="#ThreadLocal" class="headerlink" title="ThreadLocal"></a>ThreadLocal</h3><p>在多线程环境下，每个线程都有自己的数据。<br>一个线程使用自己的局部变量比使用全局变量好，因为局部变量只有线程自己能看见，不会影响其他线程，而全局变量的修改必须加锁。</p><ul><li><p><strong>使用函数传参的方法</strong><br>但是局部变量也有问题，就是在函数调用的时候，传递起来很麻烦：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">process_student</span><span class="params">(name)</span>:</span></span><br><span class="line">    std = Student(name)</span><br><span class="line">    <span class="comment"># std是局部变量，但是每个函数都要用它，因此必须传进去：</span></span><br><span class="line">    do_task_1(std)</span><br><span class="line">    do_task_2(std)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">do_task_1</span><span class="params">(std)</span>:</span></span><br><span class="line">    do_subtask_1(std)</span><br><span class="line">    do_subtask_2(std)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">do_task_2</span><span class="params">(std)</span>:</span></span><br><span class="line">    do_subtask_2(std)</span><br><span class="line">    do_subtask_2(std)</span><br></pre></td></tr></table></figure><p>每个函数一层一层调用都这么传参数那还得了？用全局变量？也不行，因为每个线程处理不同的Student对象，不能共享。</p></li><li><p><strong>使用全局字典的方法</strong><br>如果用一个全局dict存放所有的Student对象，然后以thread自身作为key获得线程对应的Student对象如何？</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">global_dict = &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">std_thread</span><span class="params">(name)</span>:</span></span><br><span class="line">    std = Student(name)</span><br><span class="line">    <span class="comment"># 把std放到全局变量global_dict中：</span></span><br><span class="line">    global_dict[threading.current_thread()] = std</span><br><span class="line">    do_task_1()</span><br><span class="line">    do_task_2()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">do_task_1</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="comment"># 不传入std，而是根据当前线程查找：</span></span><br><span class="line">    std = global_dict[threading.current_thread()]</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">do_task_2</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="comment"># 任何函数都可以查找出当前线程的std变量：</span></span><br><span class="line">    std = global_dict[threading.current_thread()]</span><br><span class="line">    ...</span><br></pre></td></tr></table></figure><p>这种方式理论上是可行的，它最大的优点是消除了std对象在每层函数中的传递问题，但是，每个函数获取std的代码有点low。<br>那么有没有更简单的方式呢？</p></li><li><p><strong>使用ThreadLocal</strong><br>ThreadLocal是一个类，可以在全局定义它的对象。<br>这样一来，它虽然是全局变量，但是每个线程对它的读写都是互不影响的。<br>如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread,local,current_thread</span><br><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建全局ThreadLocal对象:</span></span><br><span class="line">local_school = local()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">process_student</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="comment"># 获取当前线程关联的student:</span></span><br><span class="line">    std = local_school.student</span><br><span class="line">    print(<span class="string">'Hello, %s (in %s)'</span> % (std, current_thread().name))</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">process_thread</span><span class="params">(stu)</span>:</span></span><br><span class="line">    <span class="comment"># 绑定ThreadLocal的student:</span></span><br><span class="line">    local_school.student = stu</span><br><span class="line">    process_student()</span><br><span class="line"></span><br><span class="line">t1 = Thread(target= process_thread, args=(<span class="string">'XiaoMing'</span>,), name=<span class="string">'Thread-A'</span>)</span><br><span class="line">t2 = Thread(target= process_thread, args=(<span class="string">'XiaoWang'</span>,), name=<span class="string">'Thread-B'</span>)</span><br><span class="line">t1.start()</span><br><span class="line">t2.start()</span><br><span class="line">t1.join()</span><br><span class="line">t2.join()</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Hello, XiaoMing (in Thread-A)</span><br><span class="line">Hello, XiaoWang (in Thread-B)</span><br></pre></td></tr></table></figure><p>全局变量local_school就是一个ThreadLocal对象，每个Thread对它都可以读写student属性，但互不影响。<br>你可以把local_school看成全局变量，但每个属性如local_school.student都是线程的局部变量，可以任意读写而互不干扰，也不用管理锁的问题，ThreadLocal内部会处理。<br>可以理解为全局变量local_school是一个dict，不但可以用local_school.student，还可以绑定其他变量，如local_school.teacher等等。<br>ThreadLocal最常用的地方就是为每个线程绑定一个数据库连接，HTTP请求，用户身份信息等，这样一个线程的所有调用到的处理函数都可以非常方便地访问这些资源。<br><strong>总结</strong>：<br>一个ThreadLocal变量虽然是全局变量，但每个线程都只能读写自己线程的独立副本，互不干扰。<br>所以，ThreadLocal解决了参数在一个线程中各个函数之间互相传递的问题</p></li></ul><h3 id="GIL"><a href="#GIL" class="headerlink" title="GIL"></a>GIL</h3><p>GIL就是<strong>全局解释器锁</strong>。<br>如果要在Python中使用多线程的时候，实际上Python中的做法是模拟多线程，但实际上还是单线程。<br>原理就是用GIL锁来让各线程轮流执行(如果是CPU多核心的话，可以用死循环来查看多线程占用CPU的情况，会发现各CPU是不会满资源工作的)，而使用多进程就是满资源工作。<br>我们可以解决这个问题，那就是<strong>让Python执行C代码</strong>。<br>首先用gcc SOURCE.c -shared -o libXXXXX.so将C源代码编译成动态库文件，然后在Python中加载动态库执行就行了。<br>Python代码如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> ctypes <span class="keyword">import</span> *</span><br><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"></span><br><span class="line"><span class="comment"># 加载动态库（这个动态库就是用C源代码编译而来的）</span></span><br><span class="line">lib = cdll.LoadLibrary(<span class="string">"./libdeadloop.so"</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建一个子线程，让其执行C语言编写的函数</span></span><br><span class="line">t = Thread(target=lib.DeadLoop)</span><br><span class="line">t.start()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 主线程也调用那个函数</span></span><br><span class="line">lib.DeadLoop()</span><br></pre></td></tr></table></figure><p>运行之后可以看到CPU满资源工作</p><h3 id="协程"><a href="#协程" class="headerlink" title="协程"></a>协程</h3><p>协程，又称<strong>微线程</strong>。<br>首先我们得知道协程是啥？协程其实可以认为是<strong>比线程更小的执行单元</strong>。<br>为啥说他是一个执行单元，因为他自带CPU上下文。<br>这样只要在合适的时机，我们可以把一个协程切换到另一个协程。<br>只要这个过程中保存或恢复CPU上下文那么程序还是可以运行的。<br>那么这个过程看起来比线程差不多。其实不然, 线程切换从系统层面远不止保存和恢复CPU上下文这么简单。<br>操作系统为了程序运行的高效性每个线程都有自己缓存Cache等等数据，操作系统还会帮你做这些数据的恢复操作。<br>所以线程的切换非常耗性能。但是协程的切换只是单纯的操作CPU的上下文，所以一秒钟切换个上百万次系统都抗的住。<br>在<strong>IO密集型</strong>的程序中由于IO操作远远慢于CPU的操作，所以往往需要CPU去等IO操作。<br>同步IO下系统需要切换线程，让操作系统可以在IO过程中执行其他的东西。<br>这样虽然代码是符合人类的思维习惯但是由于大量的线程切换带来了大量的性能的浪费，尤其是IO密集型的程序。<br>所以人们发明了异步IO。就是当数据到达的时候触发我的回调，来减少线程切换带来性能损失。<br>但是这样的坏处也是很大的，主要的坏处就是操作被 “分片” 了，代码写的不是 “一气呵成” 这种。而是每次来段数据就要判断数据够不够处理哇，够处理就处理吧，不够处理就在等等吧。<br>这样代码的可读性很低，其实也不符合人类的习惯。但是协程可以很好解决这个问题。<br>比如把一个IO操作写成一个协程。当触发IO操作的时候就自动让出CPU给其他协程。<br>要知道协程的切换很轻的。协程通过这种对异步IO的封装既保留了性能也保证了代码的容易编写和可读性。在高IO密集型的程序下很好。但是高CPU密集型的程序下没啥好处。</p><p>生成器是可以当做协程来使用的，如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> sleep</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">A</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        print(<span class="string">"---A---"</span>)</span><br><span class="line">        <span class="keyword">yield</span></span><br><span class="line">        sleep(<span class="number">0.5</span>)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">B</span><span class="params">(c)</span>:</span></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        print(<span class="string">"---B---"</span>)</span><br><span class="line">        next(c)</span><br><span class="line">        sleep(<span class="number">0.5</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    a = A() </span><br><span class="line">    B(a)</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">---B---</span><br><span class="line">---A---</span><br><span class="line">---B---</span><br><span class="line">---A---</span><br><span class="line">---B---</span><br><span class="line">---A---</span><br><span class="line">---B---</span><br><span class="line">---A---</span><br><span class="line">---B---</span><br><span class="line">---A---</span><br><span class="line">---B---</span><br><span class="line">......</span><br></pre></td></tr></table></figure><p>其实用生成器做出来的协程理解起来是比较困难的，代码可读性也比较差。因此我们有另外的模块可以实现协程。</p><ul><li><p><strong>greenlet</strong><br>为了更好使用协程来完成多任务，python中的greenlet模块对其封装，从而使得切换任务变的更加简单<br>案例如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> greenlet <span class="keyword">import</span> greenlet</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test1</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        <span class="keyword">print</span> <span class="string">"---A--"</span></span><br><span class="line">        gr2.switch()</span><br><span class="line">        time.sleep(<span class="number">0.5</span>)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test2</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        <span class="keyword">print</span> <span class="string">"---B--"</span></span><br><span class="line">        gr1.switch()</span><br><span class="line">        time.sleep(<span class="number">0.5</span>)</span><br><span class="line"></span><br><span class="line">gr1 = greenlet(test1)</span><br><span class="line">gr2 = greenlet(test2)</span><br><span class="line"></span><br><span class="line"><span class="comment">#切换到gr1中运行</span></span><br><span class="line">gr1.switch()</span><br></pre></td></tr></table></figure><p>使用greenlet做出来的协程方便理解</p></li><li><p><strong>gevent</strong><br>greenlet已经实现了协程，但是这个还的人工切换，是不是觉得太麻烦了，不要着急，python还有一个比greenlet更强大的并且能够自动切换任务的模块gevent。<br>其原理是当一个greenlet遇到IO(指的是input output输入输出，比如网络、文件操作等)操作时，比如访问网络，就自动切换到其他的greenlet，等到IO操作完成，再在适当的时候切换回来继续执行。<br>由于IO操作非常耗时，经常使程序处于等待状态，有了gevent为我们自动切换协程，就保证总有greenlet在运行，而不是等待IO。<br>下面就是一个gevent实现的TCP服务器：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> sys</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"><span class="keyword">import</span> gevent</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> gevent <span class="keyword">import</span> socket,monkey</span><br><span class="line"><span class="comment"># 该语句会修改Python中的一些方法</span></span><br><span class="line">monkey.patch_all()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">handle_request</span><span class="params">(conn)</span>:</span></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        data = conn.recv(<span class="number">1024</span>)</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> data:</span><br><span class="line">            conn.close()</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">        print(<span class="string">"recv:"</span>, data)</span><br><span class="line">        conn.send(data)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">server</span><span class="params">(port)</span>:</span></span><br><span class="line">    s = socket.socket()</span><br><span class="line">    s.bind((<span class="string">''</span>, port))</span><br><span class="line">    s.listen(<span class="number">5</span>)</span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        cli, addr = s.accept()</span><br><span class="line">        gevent.spawn(handle_request, cli)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    server(<span class="number">7788</span>)</span><br></pre></td></tr></table></figure></li></ul><h2 id="网络"><a href="#网络" class="headerlink" title="网络"></a>网络</h2><p>网络就是一种辅助双方或者多方能够连接在一起的工具<br>所谓的网络编程就是，让在不同的电脑上的软件能够进行数据传递，即<strong>进程之间的通信</strong></p><h3 id="TCP-IP"><a href="#TCP-IP" class="headerlink" title="TCP/IP"></a>TCP/IP</h3><p>就像说不同语言的人沟通一样，只要有一种大家都认可都遵守的协议即可，那么这个计算机都遵守的网络通信协议叫做<strong>TCP/IP协议</strong><br>为了把全世界的所有不同类型的计算机都连接起来，就必须规定一套全球通用的协议，为了实现互联网这个目标，<strong>互联网协议簇(Internet Protocol Suite)</strong>就是通用协议标准。<br>因为互联网协议包含了上百种协议标准，但是最重要的两个协议是TCP和IP协议，所以，大家把互联网的协议简称<strong>TCP/IP协议</strong>。<br><strong>注意</strong>：TCP/IP协议是一个协议族，里面包含了很多协议<br>常用的网络协议如下图所示：</p><p><img src="https://note.youdao.com/yws/public/resource/2c8aa2e8642b2832d32ab9f8ee20ccf1/xmlnote/0A65AFA833A34A7086271CACCF2FC6A3/145" alt=""></p><blockquote><p>TCP/IP协议将网络分为4个层次：<strong>链路层、网络层、传输层、应用层</strong><br>而ISO标准则将网络分为7个层次：<strong>物理层、数据链路层、网络层、传输层、会话层、表示层、应用层</strong><br>其中的物理层和数据链路层对应4层模型中的链路层，网络层和传输层分别对应4层模型中的网络层和传输层，而会话层、表示层和应用层则对应4层模型中的应用层</p></blockquote><h3 id="端口"><a href="#端口" class="headerlink" title="端口"></a>端口</h3><p>那么TCP/IP协议中的端口指的是什么呢？端口就好一个房子的门，是出入这间房子的必经之路。<br>如果一个进程需要收发网络数据，那么就需要有这样的端口。<br>在linux系统中，端口可以有65536（2的16次方）个之多！既然有这么多，操作系统为了统一管理，所以进行了编号，这就是端口号。<br>端口是通过端口号来标记的，端口号只有整数，范围是从0到65535。<br><strong>注意</strong>：端口号不是随意使用的，而是按照一定的规定进行分配。</p><p><strong>端口有什么用呢？</strong></p><blockquote><p>我们知道，一台拥有IP地址的主机可以提供许多服务，比如HTTP、FTP、SMTP等，这些服务完全可以通过1个IP地址来实现。<br>那么，主机是怎样区分不同的网络服务呢？<br>显然不能只靠IP地址，因为IP地址与网络服务的关系是一对多的关系。<br>实际上是通过<strong>IP地址+端口号</strong>来区分不同的服务的。<br>需要注意的是，端口并不是一一对应的。<br>比如你的电脑作为客户机访问一台WWW服务器时，WWW服务器使用“80”端口与你的电脑通信，但你的电脑则可能使用”3457”这样的端口。</p></blockquote><p><strong>注意</strong>：一个端口号只能对应一个进程，如果某个进程的端口号被占用，那么那个进程只能用其他的端口号或者不能打开这个进程。</p><h3 id="IP-地址"><a href="#IP-地址" class="headerlink" title="IP 地址"></a>IP 地址</h3><p>IP地址：<strong>用来在网络中标记一台电脑的一串数字</strong>，如192.168.1.1<br>IP地址在本地局域网上是<strong>惟一</strong>的。<br>每一个IP地址包括两部分：网络地址和主机地址。<br>IP地址分为五类，这几类IP地址不加赘述，只介绍一下私有IP：<br>在这么多网络IP中，国际规定有一部分IP地址是用于我们的局域网使用，也就是属于私网IP，不在公网中使用的，它们的范围是：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">10.0.0.0～10.255.255.255</span><br><span class="line">172.16.0.0～172.31.255.255</span><br><span class="line">192.168.0.0～192.168.255.255</span><br></pre></td></tr></table></figure><p>注意：IP地址127．0．0．1~127．255．255．255用于回路测试。<br>如：127.0.0.1可以代表本机IP地址，用<code>http://127.0.0.1</code>就可以测试本机中配置的Web服务器。</p><h3 id="子网掩码"><a href="#子网掩码" class="headerlink" title="子网掩码"></a>子网掩码</h3><p>要想理解什么是子网掩码，就不能不了解IP地址的构成。</p><blockquote><p>互联网是由许多小型网络构成的，每个网络上都有许多主机，这样便构成了一个有层次的结构。<br>IP地址在设计时就考虑到地址分配的层次特点，将每个IP地址都分割成网络号和主机号两部分，以便于IP地址的寻址操作。<br>IP地址的网络号和主机号各是多少位呢？<br>如果不指定，就不知道哪些位是网络号、哪些是主机号，这就需要通过子网掩码来实现。<br>子网掩码不能单独存在，它必须结合IP地址一起使用。<br>子网掩码只有一个作用，就是将某个IP地址划分成网络地址和主机地址两部分子网掩码的设定必须遵循一定的规则。</p></blockquote><h3 id="socket"><a href="#socket" class="headerlink" title="socket"></a>socket</h3><p><strong>socket</strong>(套接字)是用于网络间进行通信的一种方式。<br>它与其他进程间通信的一个主要不同是：它能实现不同主机间的进程间通信，我们网络上各种各样的服务大多都是基于Socket来完成通信的。<br>在Python中使用socket模块中的socket类即可完成套接字对象的创建：<br><code>socket.socket(AddressFamily, Type)</code><br>函数socket.socket创建一个socket，返回该socket的<strong>描述符</strong>，该函数带有两个参数：</p><ul><li><strong>Address Family</strong><br>可以选择<code>AF_INET</code>(用于Internet进程间通信)或者<code>AF_UNIX</code>(用于同一台机器进程间通信),实际工作中常用AF_INET</li><li><strong>Type</strong><br>套接字类型，可以是<code>SOCK_STREAM</code>(流式套接字，主要用于TCP协议)或者<code>SOCK_DGRAM</code>(数据报套接字，主要用于UDP协议)。</li></ul><p>socket在Python中实际上是一个类，用socket创建的也就是一个对象。</p><h3 id="UDP"><a href="#UDP" class="headerlink" title="UDP"></a>UDP</h3><p><strong>UDP</strong>(用户数据报协议)，是一个<strong>无连接</strong>的简单的<strong>面向数据报</strong>的运输层协议。<br>UDP不提供可靠性，它只是把应用程序传给IP层的数据报发送出去，但是并不能保证它们能到达目的地。<br>由于UDP在传输数据报前不用在客户和服务器之间建立一个连接，且没有超时重发等机制，故而传输速度很快。<br>UDP是一种面向无连接的协议，每个数据报都是一个独立的信息，包括完整的源地址或目的地址，它在网络上以任何可能的路径传往目的地，因此能否到达目的地，到达目的地的时间以及内容的正确性都是不能被保证的。</p><p><strong>UDP特点</strong>：<br>UDP是面向无连接的通讯协议，UDP数据包括目的端口号和源端口号信息，由于通讯不需要连接，所以可以实现广播发送。<br>UDP传输数据时有大小限制，每个被传输的数据报必须限定在64KB之内。<br>UDP是一个不可靠的协议，发送方所发送的数据报并不一定以相同的次序到达接收方。</p><p><strong>UDP程序流程如下</strong>：<br>1、创建套接字；<br>2、接收/发送数据(如果是服务器还要绑定端口)<br>3、关闭套接字</p><p><img src="https://note.youdao.com/yws/public/resource/2c8aa2e8642b2832d32ab9f8ee20ccf1/xmlnote/A283F506D2EC435485A4B4CA51E8F65F/148" alt=""></p><ul><li><p><strong>发送数据</strong><br>socket类中有个方法为sendto，用于发送数据，使用格式如下(这是Python3中的，Python2中sendto的第一个参数是str类型的)：<br><code>sendto(data[, flags], address) -&gt; count</code><br>其中data为<strong>byte类型</strong>的参数，如果需要把字符串转为byte类型的数据的话，在字符串前面加个’b’就行了<br>也可以调用str类中的<strong>encode</strong>方法对字符串进行编码也能转化为byte类型的数据<br>下面是一个UDP套接字发送信息的案例：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> socket <span class="keyword">import</span> * </span><br><span class="line"></span><br><span class="line">udpSocket = socket(AF_INET, SOCK_DGRAM)</span><br><span class="line"></span><br><span class="line">udpSocket.sendto(<span class="string">b"haha"</span>, (<span class="string">"10.106.48.72"</span>, <span class="number">4000</span>))</span><br></pre></td></tr></table></figure><p>执行多次之后，用网络调试助手测试的结果如下：</p><p><img src="https://note.youdao.com/yws/public/resource/2c8aa2e8642b2832d32ab9f8ee20ccf1/xmlnote/923E495217734586B0ADC3D8C610CA8E/152" alt=""></p><p>可以发现，每次程序发送消息的时候都是用不同的端口来发送消息的。<br>原因是我们没有指定发送消息的端口号，这样一来，执行程序的时候操作系统会随机找一个没有被用过的端口号来执行我们的程序。那么如何指定我们要发送消息的端口号呢？</p></li><li><p><strong>绑定端口</strong><br>在socket类中有个bind方法，这个方法就是用来绑定IP地址和端口号的，用法如下：<br><code>bind(address)</code><br>其中的address是一个元组，元组内需要包含IP地址和端口号(IP号用str类型表示，可以为空，这样就代表绑定本机所有IP；端口号是int类型)</p><p>下面我们来绑定端口号：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> socket <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line">udpSocket = socket(AF_INET, SOCK_DGRAM)</span><br><span class="line">udpSocket.bind((<span class="string">""</span>, <span class="number">7788</span>))</span><br><span class="line"></span><br><span class="line">udpSocket.sendto(<span class="string">b"haha"</span>, (<span class="string">"10.106.48.72"</span>, <span class="number">4000</span>))</span><br></pre></td></tr></table></figure><p>绑定端口号之后发送消息，网络调试助手的结果显示如下：</p><p><img src="https://note.youdao.com/yws/public/resource/2c8aa2e8642b2832d32ab9f8ee20ccf1/xmlnote/F9009EA665B54323A8FA78F8E16C4D72/151" alt=""></p><p>可以看到这回是同一个端口接收到的数据。</p></li><li><p><strong>接收数据</strong><br>socket能发送数据，同样也能接收数据。<br>在socket类中有个recv方法可以用于接收数据，用法如下：<br><code>recv(buffersize[, flags]) -&gt; data</code><br>其中的buffersize用于指定可接收的最大字节数；<br>返回值为byte类型的对象，也就是str前加上一个b，如果需要让byte类型的对象转化为str类型的对象，调用byte类型的<strong>decode</strong>方法解除编码即可。</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> socket <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line">udpSocket = socket(AF_INET, SOCK_DGRAM)</span><br><span class="line">udpSocket.bind((<span class="string">""</span>, <span class="number">7788</span>))</span><br><span class="line"></span><br><span class="line">recvData = udpSocket.recv(<span class="number">1024</span>)</span><br><span class="line">print(<span class="string">"接收到的数据为：%s"</span>%recvData.decode(<span class="string">"gb2312"</span>))</span><br></pre></td></tr></table></figure><p>用网络调试工具发送消息之后，接收结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">接收到的数据为：Hello Ubuntu</span><br></pre></td></tr></table></figure><p>除了recv方法，还有一个方法可以接收数据，那就<strong>recvfrom</strong>方法，这个方法不仅能接收数据，还能收到数据的来源，用法如下：<br><code>recvfrom(buffersize[, flags]) -&gt; (data, address info)</code><br>传递的参数和recv是一样的，但是返回值不一样，recvfrom方法的返回值是一个元组，第一个元素为接收到的byte类型的数据，第二个元素为IP地址和端口号。</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> socket <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line">udpSocket = socket(AF_INET, SOCK_DGRAM)</span><br><span class="line">udpSocket.bind((<span class="string">""</span>, <span class="number">7788</span>))</span><br><span class="line"></span><br><span class="line">recvData,recvFrom = udpSocket.recvfrom(<span class="number">1024</span>)</span><br><span class="line">print(<span class="string">"接收到%s的数据为：%s"</span>%(recvFrom[<span class="number">0</span>], recvData.decode(<span class="string">"gb2312"</span>)))</span><br></pre></td></tr></table></figure><p>用调试助手发送数据的结构如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">接收到10.106.48.72的数据为：Hello Ubuntu</span><br></pre></td></tr></table></figure></li><li><p><strong>广播</strong><br>udp同样能进行广播的发送，如下，运行结果不予显示：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> socket </span><br><span class="line"></span><br><span class="line">dest = (<span class="string">'&lt;broadcast&gt;'</span>, <span class="number">7788</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建udp套接字</span></span><br><span class="line">s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)</span><br><span class="line"><span class="comment"># 对这个需要发送广播数据的套接字进行修改设置，否则不能发送广播数据</span></span><br><span class="line">s.setsockopt(socket.SOL_SOCKET, socket.SO_BROADCAST,<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 以广播的形式发送数据到本网络的所有电脑中</span></span><br><span class="line">s.sendto(<span class="string">"Hi"</span>, dest)</span><br><span class="line"></span><br><span class="line"><span class="keyword">print</span> <span class="string">"等待对方回复（按ctrl+c退出）"</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">    (buf, address) = s.recvfrom(<span class="number">2048</span>)</span><br><span class="line">    <span class="keyword">print</span> <span class="string">"Received from %s: %s"</span> % (address, buf)</span><br></pre></td></tr></table></figure></li><li><p><strong>UDP聊天室</strong><br>这个UDP网络聊天室是用2个线程（当然也可以用2个进程）完成的，可以在接收消息的同时发送消息。</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"><span class="keyword">from</span> socket <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line"><span class="comment"># 收数据线程</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">recvData</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        recvInfo = udpSocket.recvfrom(<span class="number">1024</span>)</span><br><span class="line">        print(<span class="string">"\r&gt;&gt;%s:%s\n&lt;&lt;"</span> %(str(recvInfo[<span class="number">1</span>]), recvInfo[<span class="number">0</span>].decode(<span class="string">"gb2312"</span>)), end=<span class="string">""</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 发数据线程</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sendData</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        sendInfo = input(<span class="string">"&lt;&lt;"</span>)</span><br><span class="line">        udpSocket.sendto(sendInfo.encode(<span class="string">"gb2312"</span>), (destIP, destPort))</span><br><span class="line"></span><br><span class="line">udpSocket = <span class="literal">None</span></span><br><span class="line">destIP = <span class="string">"10.106.48.72"</span></span><br><span class="line">destPort = <span class="number">8899</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">global</span> udpSocket</span><br><span class="line">    <span class="keyword">global</span> destIP</span><br><span class="line">    <span class="keyword">global</span> destPort</span><br><span class="line"></span><br><span class="line">    <span class="comment">#destIP = input("对方IP地址为: ")</span></span><br><span class="line">    <span class="comment">#destPort = int(input("对方端口号为: "))</span></span><br><span class="line"></span><br><span class="line">    udpSocket = socket(AF_INET, SOCK_DGRAM)</span><br><span class="line">    udpSocket.bind((<span class="string">""</span>, <span class="number">8000</span>))</span><br><span class="line"></span><br><span class="line">    tr = Thread(target=recvData)</span><br><span class="line">    ts = Thread(target=sendData)</span><br><span class="line"></span><br><span class="line">    tr.start()</span><br><span class="line">    ts.start()</span><br><span class="line"></span><br><span class="line">    tr.join()</span><br><span class="line">    ts.join()</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure><p>运行结果不予显示</p></li></ul><h3 id="TCP"><a href="#TCP" class="headerlink" title="TCP"></a>TCP</h3><p>TCP为传输控制协议，和UDP不同的是：TCP需要先建立连接，然后才能发送数据。<br>TCP程序流程如下：</p><p><img src="https://note.youdao.com/yws/public/resource/2c8aa2e8642b2832d32ab9f8ee20ccf1/xmlnote/12F91ADC0B4249D2A907A065CE723722/155" alt=""></p><ul><li><p><strong>TCP三次握手</strong><br>TCP三次握手是在建立连接的时候使用的一种策略</p><p><img src="https://note.youdao.com/yws/public/resource/2c8aa2e8642b2832d32ab9f8ee20ccf1/xmlnote/830F229EEF2D468AA5B71AE19617AA2F/157" alt=""></p></li><li><p><strong>TCP四次挥手</strong><br>在TCP连接关闭的时候会有4次挥手</p><p><img src="https://note.youdao.com/yws/public/resource/2c8aa2e8642b2832d32ab9f8ee20ccf1/xmlnote/2AB5B4E156B343999ABE95059E83F158/159" alt=""></p></li><li><p><strong>TCP状态</strong><br>TCP一共有10种状态，如下图所示：</p><p><img src="https://note.youdao.com/yws/public/resource/2c8aa2e8642b2832d32ab9f8ee20ccf1/xmlnote/AF34E65CC4C54132AF35ADBAC7752561/161" alt=""></p></li><li><p><strong>MSL</strong><br>MSL就是数据包从客服端到达服务器或从服务器到达客户端的最长时间。<br>TCP连接关闭的时候最后发送关闭确认的一方需要等待2MSL的时间然后关闭连接。</p><p><img src="https://note.youdao.com/yws/public/resource/2c8aa2e8642b2832d32ab9f8ee20ccf1/xmlnote/13713995326F4E24BB696F5D9794835C/163" alt=""></p><blockquote><p>2MSL即两倍的MSL，TCP的TIME_WAIT状态也称为2MSL等待状态<br>当TCP的一端发起主动关闭，在发出最后一个ACK包后，即第3次握手完成后发送了第四次握手的ACK包后就进入了TIME_WAIT状态，必须在此状态上停留两倍的MSL时间<br>等待2MSL时间主要目的是怕最后一个ACK包对方没收到，那么对方在超时后将重发第三次握手的FIN包，主动关闭端接到重发的FIN包后可以再发一个ACK应答包。<br>在TIME_WAIT状态时两端的端口不能使用，要等到2MSL时间结束才可继续使用。<br>当连接处于2MSL等待阶段时任何迟到的报文段都将被丢弃。<br>不过在实际应用中可以通过设置<strong>SO_REUSEADDR</strong>选项达到不必等待2MSL时间结束再使用此端口。</p></blockquote></li><li><p><strong>长连接和短连接</strong><br>TCP在真正的读写操作之前，<strong>server与client之间必须建立一个连接</strong><br>当读写操作完成后，双方不再需要这个连接时它们可以释放这个连接，连接的建立通过三次握手，释放则需要四次握手，所以说每个连接的建立都是需要资源消耗和时间消耗的。<br>模拟一种TCP短连接的情况:<br>1、client 向 server 发起连接请求<br>2、server 接到请求，双方建立连接<br>3、client 向 server 发送消息<br>4、server 回应 client<br>5、一次读写完成，此时双方任何一个都可以发起 close 操作<br>在步骤5中，一般都是client先发起close操作。当然也不排除有特殊的情况。<br>从上面的描述看，短连接一般只会在client/server间传递一次读写操作！<br>再模拟一种长连接的情况:<br>1、client 向 server 发起连接<br>2、server 接到请求，双方建立连接<br>3、client 向 server 发送消息<br>4、server 回应 client<br>5、一次读写完成，连接不关闭<br>6、后续读写操作…<br>7、长时间操作之后client发起关闭请求</p><blockquote><p>由此可知长连接可以省去较多的TCP建立和关闭的操作，减少浪费，节约时间。<br>对于频繁请求资源的客户来说，较适用长连接。<br>client与server之间的连接如果一直不关闭的话，会存在一个问题，随着客户端连接越来越多，server早晚有扛不住的时候，这时候server端需要采取一些策略，如关闭一些长时间没有读写事件发生的连接，这样可以避免一些恶意连接导致server端服务受损<br>如果条件再允许就可以以客户端机器为颗粒度，限制每个客户端的最大长连接数，这样可以完全避免某个蛋疼的客户端连累后端服务。<br>短连接对于服务器来说管理较为简单，存在的连接都是有用的连接，不需要额外的控制手段。<br>但如果客户请求频繁，将在TCP的建立和关闭操作上浪费时间和带宽。</p></blockquote></li><li><p><strong>listen</strong><br>socket类中有个方法为<strong>listen</strong>，这个方法用于监听tcp连接的。<br>只有执行这个方法的时候才能接收连接。<br>还有种说法是socket创建的套接字默认属性是主动的，使用listen方法可以将其变为被动的套接字，这样一来就可以接收别人的连接了。<br>listen使用方法如下：<br><code>listen([backlog])</code><br>其中的backlog表示设定的最大半连接和已连接数。如果连接数超过了这个值，那么客户端的connect将会被阻塞。<br><strong>注意</strong>：在Linux下这个值随便填多少都是一样的。</p></li><li><p><strong>accept</strong><br>socket类中有个方法为<strong>accept</strong>，该函数不需要参数，返回值是一个元组(socket object, address info)。<br>其中第一个元素为套接字，也就是说accept接收连接之后会创建一个新的套接字来使用这个连接；<br>第二个元素为接收连接的地址信息，也是个元组，包含了IP地址和端口号。<br><strong>注意</strong>：accept方法会造成阻塞。</p></li><li><p><strong>TCP服务器</strong><br>下面建立一个简单的TCP服务器：<br>流程如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">1、socket创建一个套接字</span><br><span class="line">2、bind绑定ip和port</span><br><span class="line">3、listen使套接字变为可以被动链接</span><br><span class="line">4、accept等待客户端的链接</span><br><span class="line">5、recv&#x2F;send接收发送数据</span><br></pre></td></tr></table></figure><p>代码如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> socket <span class="keyword">import</span> *</span><br><span class="line"><span class="comment"># 创建套接字</span></span><br><span class="line">tcpSocket = socket(AF_INET, SOCK_STREAM)</span><br><span class="line"><span class="comment"># 绑定端口号</span></span><br><span class="line">tcpSocket.bind((<span class="string">""</span>, <span class="number">8000</span>))</span><br><span class="line"><span class="comment"># 使用listen方法将套接字变为被动套接字</span></span><br><span class="line">tcpSocket.listen(<span class="number">5</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 如果有新的客户端来链接服务器，那么就产生一个新的套接字专门为这个客户端服务器</span></span><br><span class="line"><span class="comment"># newSocket用来为这个客户端服务</span></span><br><span class="line"><span class="comment"># tcpSocket就可以省下来专门等待其他新客户端的链接</span></span><br><span class="line">newSocket,clientInfo = tcpSocket.accept()</span><br><span class="line"></span><br><span class="line">recvData = newSocket.recv(<span class="number">1024</span>)</span><br><span class="line">print(<span class="string">"接收到的数据：%s"</span>%recvData.decode(<span class="string">"gb2312"</span>))</span><br><span class="line">newSocket.send(<span class="string">b"Thank you!"</span>)</span><br><span class="line"></span><br><span class="line">newSocket.close()</span><br><span class="line">tcpSocket.close()</span><br></pre></td></tr></table></figure><p>之后就可以用tcp客户端（如网络调试助手）来连接这个服务器了</p></li><li><p><strong>TCP客户端</strong><br>TCP客户端的搭建比服务器要简单许多，只需4步即可：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">1、创建套接字；</span><br><span class="line">2、连接服务器</span><br><span class="line">3、接收&#x2F;发送数据</span><br><span class="line">4、关闭套接字</span><br></pre></td></tr></table></figure><p>代码如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> socket <span class="keyword">import</span> *</span><br><span class="line"><span class="comment"># 创建套接字</span></span><br><span class="line">clientSocket = socket(AF_INET, SOCK_STREAM)</span><br><span class="line"><span class="comment"># 使用connect方法连接服务器</span></span><br><span class="line">clientSocket.connect((<span class="string">"10.106.48.72"</span>, <span class="number">8899</span>))</span><br><span class="line"><span class="comment"># 发送数据</span></span><br><span class="line">clientSocket.send(<span class="string">"HeiHei"</span>.encode(<span class="string">"utf-8"</span>))</span><br><span class="line"><span class="comment"># 接收数据</span></span><br><span class="line">recvData = clientSocket.recv(<span class="number">1024</span>)</span><br><span class="line"></span><br><span class="line">print(recvData.decode(<span class="string">"utf-8"</span>))</span><br><span class="line">clientSocket.close()</span><br></pre></td></tr></table></figure><p>然后就可以在服务器上看到对应的消息了。</p></li></ul><h3 id="TCP-单进程服务器"><a href="#TCP-单进程服务器" class="headerlink" title="TCP 单进程服务器"></a>TCP 单进程服务器</h3><p>单进程服务器是一种简单的单进程服务器，特点如下：<br>1、同一时刻只能为一个客户进行服务，不能同时为多个客户服务<br>2、当服务器为一个客户端服务时，而另外的客户端发起了connect，只要服务器listen的队列有空闲的位置，就会为这个新客户端进行连接，并且客户端可以发送数据，但当服务器为这个新客户端服务时，可能一次性把所有数据接收完毕<br>3、当recv接收数据时，返回值为空，即没有返回数据，那么意味着客户端已经调用了close关闭了；因此服务器通过判断recv接收数据是否为空来判断客户端是否已经下线<br>代码如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> socket <span class="keyword">import</span> *</span><br><span class="line">serSocket = socket(AF_INET, SOCK_STREAM)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 重复使用绑定的信息(也就是在服务器意外结束之后仍然可以绑定该信息)</span></span><br><span class="line">serSocket.setsockopt(SOL_SOCKET, SO_REUSEADDR, <span class="number">1</span>)</span><br><span class="line"><span class="comment"># 绑定本地信息</span></span><br><span class="line">address = (<span class="string">""</span>, <span class="number">8899</span>)</span><br><span class="line">serSocket.bind(address)</span><br><span class="line"><span class="comment"># 将主动套接字变为被动监听状态</span></span><br><span class="line">serSocket.listen(<span class="number">10</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">    print(<span class="string">"---等待新客户端到来---"</span>)</span><br><span class="line">    newSocket,clientAddr = serSocket.accept()</span><br><span class="line">    print(<span class="string">"--- %s已连接 ---"</span> %clientAddr[<span class="number">0</span>])</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            recvData = newSocket.recv(<span class="number">1024</span>)</span><br><span class="line">            <span class="keyword">if</span> recvData:</span><br><span class="line">                print(<span class="string">"%s:%s"</span> %(str(clientAddr), recvData.decode(<span class="string">"utf-8"</span>)))</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                print(<span class="string">"客户端关闭"</span>)</span><br><span class="line">                <span class="keyword">break</span></span><br><span class="line">    <span class="keyword">finally</span>:</span><br><span class="line">        newSocket.close()</span><br><span class="line"></span><br><span class="line">serSocket.close()</span><br></pre></td></tr></table></figure><p>执行结果自己尝试。</p><ul><li><strong>非阻塞单进程服务器</strong><br>我们都知道accept方法在调用的时候会产生<strong>阻塞</strong>，因此单进程服务器才不能接收多个客户端的请求。<br>所以我们只要accept方法不再阻塞，就可以使用单进程来接收多个客户端的请求了。<br>在socket类中有个方法为<strong>setblocking</strong>，将其中的参数改为False就可以让accept方法不再阻塞。<br><strong>注意</strong>：设置为非阻塞之后，如果accept的时候没有客户端的连接请求会出现异常，因此需要使用try来捕获这个异常。<br>代码如下：<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> socket <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建套接字</span></span><br><span class="line">serSocket = socket(AF_INET, SOCK_STREAM)</span><br><span class="line"><span class="comment"># 设置套接字重复绑定本地信息</span></span><br><span class="line">serSocket.setsockopt(SOL_SOCKET, SO_REUSEADDR, <span class="number">1</span>)</span><br><span class="line"><span class="comment"># 绑定本地信息</span></span><br><span class="line">address = (<span class="string">""</span>, <span class="number">8899</span>)</span><br><span class="line">serSocket.bind(address)</span><br><span class="line"><span class="comment"># 设置accept为非堵塞</span></span><br><span class="line">serSocket.setblocking(<span class="literal">False</span>)</span><br><span class="line"><span class="comment"># 将套接字改为被动监听</span></span><br><span class="line">serSocket.listen(<span class="number">10</span>)</span><br><span class="line"></span><br><span class="line">clientAddrList = []</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        newSocket,newAddr = serSocket.accept()</span><br><span class="line">    <span class="keyword">except</span>:</span><br><span class="line">        <span class="keyword">pass</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        print(<span class="string">"%s connected..."</span>%str(newAddr))</span><br><span class="line">        newSocket.setblocking(<span class="literal">False</span>)</span><br><span class="line">        clientAddrList.append((newSocket, newAddr))</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> clientSocket,clientAddr <span class="keyword">in</span> clientAddrList:</span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            recvData = clientSocket.recv(<span class="number">1024</span>)</span><br><span class="line">        <span class="keyword">except</span>:</span><br><span class="line">            <span class="keyword">pass</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">if</span> recvData:</span><br><span class="line">                print(<span class="string">"%s:%s"</span>%(str(clientAddr), recvData.decode(<span class="string">"gb2312"</span>)))</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                clientSocket.close()</span><br><span class="line">                clientAddrList.remove((clientSocket,clientAddr))</span><br><span class="line">                print(<span class="string">"%s closed..."</span>%(str(clientAddr)))</span><br><span class="line"></span><br><span class="line">setsockopt.close()</span><br></pre></td></tr></table></figure>这样一来，我们就可以使用单进程服务器来接收多个客户端的请求了。</li></ul><h3 id="并发服务器"><a href="#并发服务器" class="headerlink" title="并发服务器"></a>并发服务器</h3><p>所谓的并发服务器就是能在同一时间为多个客户端进行服务。<br>可以用<strong>多进程或者多线程</strong>来实现并发。</p><ul><li><p><strong>多进程服务器</strong></p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> socket <span class="keyword">import</span> *</span><br><span class="line"><span class="keyword">from</span> multiprocessing <span class="keyword">import</span> Process</span><br><span class="line"></span><br><span class="line"><span class="comment"># 为客户端服务进程</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">clinetProcess</span><span class="params">(newSocket, clinetAddr)</span>:</span></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        recvData = newSocket.recv(<span class="number">1024</span>)</span><br><span class="line">        <span class="keyword">if</span> recvData:</span><br><span class="line">            print(<span class="string">"%s:%s"</span> %(str(clinetAddr), recvData.decode(<span class="string">"gb2312"</span>)))</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            print(<span class="string">"%s closed..."</span>%str(clinetAddr))</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">    newSocket.close()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="comment"># 创建套接字</span></span><br><span class="line">    serSocket = socket(AF_INET, SOCK_STREAM)</span><br><span class="line">    <span class="comment"># 重复使用绑定信息</span></span><br><span class="line">    serSocket.setsockopt(SOL_SOCKET, SO_REUSEADDR, <span class="number">1</span>)</span><br><span class="line">    <span class="comment"># 绑定本机信息</span></span><br><span class="line">    address = (<span class="string">""</span>, <span class="number">8899</span>)</span><br><span class="line">    serSocket.bind(address)</span><br><span class="line"></span><br><span class="line">    serSocket.listen(<span class="number">10</span>)</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            newSocket,clinetAddr = serSocket.accept()</span><br><span class="line">            <span class="comment"># 创建新的进程为新的客户端服务</span></span><br><span class="line">            newProcess = Process(target=clinetProcess,args=(newSocket,clinetAddr))</span><br><span class="line">            newProcess.start()</span><br><span class="line"></span><br><span class="line">            newSocket.close()</span><br><span class="line">    <span class="keyword">except</span>:</span><br><span class="line">        <span class="keyword">pass</span></span><br><span class="line">    <span class="keyword">finally</span>:</span><br><span class="line">        serSocket.close()</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure><p><strong>注意</strong>：多进程消耗的资源是比较大的，如果有较多的客户端的时候，使用多进程可能导致服务器崩溃。</p></li><li><p><strong>多线程服务器</strong></p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> socket <span class="keyword">import</span> *</span><br><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">clientThread</span><span class="params">(newSocket, clientAddr)</span>:</span></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        recvData = newSocket.recv(<span class="number">1024</span>)</span><br><span class="line">        <span class="keyword">if</span> recvData:</span><br><span class="line">            print(<span class="string">"%s:%s"</span> %(str(clientAddr), recvData.decode(<span class="string">"gb2312"</span>)))</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            print(<span class="string">"--- %s closed... ---"</span> %str(clientAddr))</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line"></span><br><span class="line">    newSocket.close()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    serSocket = socket(AF_INET, SOCK_STREAM)</span><br><span class="line">    serSocket.setsockopt(SOL_SOCKET, SO_REUSEADDR, <span class="number">1</span>)</span><br><span class="line">    address = (<span class="string">""</span>, <span class="number">8899</span>)</span><br><span class="line">    serSocket.bind(address)</span><br><span class="line"></span><br><span class="line">    serSocket.listen(<span class="number">10</span>)</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            newSocket,clientAddr = serSocket.accept()</span><br><span class="line">            print(<span class="string">"--- %s connected... ---"</span>%str(clientAddr))</span><br><span class="line">            newThread = Thread(target=clientThread, args=(newSocket,clientAddr))</span><br><span class="line">            newThread.start()</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">except</span>:</span><br><span class="line">        <span class="keyword">pass</span></span><br><span class="line">    <span class="keyword">finally</span>:</span><br><span class="line">        newSocket.close()</span><br><span class="line">        serSocket.close()</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure></li></ul><h3 id="select-服务器"><a href="#select-服务器" class="headerlink" title="select 服务器"></a>select 服务器</h3><p>在IO多路复用(单一进程完成多个任务)的模型中，比较常用的有select和epoll模型。<br>这两个都是系统接口，由操作系统提供。当然，Python的select模块进行了更高级的封装。</p><blockquote><p>网络通信被Unix系统抽象为文件的读写，通常是一个设备，由设备驱动程序提供，驱动可以知道自身的数据是否可用。<br>支持阻塞操作的设备驱动通常会实现一组自身的等待队列，如读/写等待队列用于支持上层(用户层)所需的block或non-block操作。<br>设备的文件的资源如果可用（可读或者可写）则会通知进程，反之则会让进程睡眠，等到数据到来可用的时候，再唤醒进程。<br>这些设备的文件描述符被放在一个数组中，然后select调用的时候遍历这个数组，如果对于的文件描述符可读则会返回改文件描述符。<br>当遍历结束之后，如果仍然没有一个可用设备文件描述符，select让用户进程则会睡眠，直到等待资源可用的时候在唤醒，遍历之前那个监视的数组。每次遍历都是依次进行判断的。</p></blockquote><p>select模块中有个select函数，使用方法如下：<br><code>select(rlist, wlist, xlist[, timeout]) -&gt; (rlist, wlist, xlist)</code><br>传入的3个主要参数为<strong>收数据套接字列表、发数据套接字列表、异常套接字列表</strong>。<br>返回值分别对应这3个列表，返回的第一个列表是收数据套接字列表中等待接收数据的套接字组成的列表；返回的第二个列表是等待数据发送的套接字组成的列表；返回的第三个值是产生了异常等待处理的套接字列表。<br>select会以轮询(轮流查询，也就是要遍历列表)的方式来检测每个列表，然后将需要操作的套接字返回在列表当中。<br><strong>注意</strong>：select函数会产生阻塞，只有当传入的套接字列表有数据读写变化，select函数才会解阻塞。<br>下面就是一个select服务器的例子：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> socket <span class="keyword">import</span> *</span><br><span class="line"><span class="keyword">from</span> select <span class="keyword">import</span> select</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建套接字</span></span><br><span class="line">serSocket = socket(AF_INET, SOCK_STREAM)</span><br><span class="line"><span class="comment"># 设置重复绑定</span></span><br><span class="line">serSocket.setsockopt(SOL_SOCKET, SO_REUSEADDR, <span class="number">1</span>)</span><br><span class="line"><span class="comment"># 绑定并将套接字设置为被动监听</span></span><br><span class="line">address = (<span class="string">""</span>, <span class="number">8888</span>)</span><br><span class="line">serSocket.bind(address)</span><br><span class="line">serSocket.listen(<span class="number">10</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 记录所有可接收数据的套接字</span></span><br><span class="line">inputs = [serSocket]</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">    <span class="comment"># 调用select函数，阻塞等待，只要有套接字的变化解阻塞</span></span><br><span class="line">    readable,writeable,exceptional = select(inputs,[],[])</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 遍历所有可接收数据的套接字</span></span><br><span class="line">    <span class="keyword">for</span> sock <span class="keyword">in</span> readable:</span><br><span class="line">        <span class="comment"># 如果是服务器套接字发生变化，则代表有新的连接出现</span></span><br><span class="line">        <span class="keyword">if</span> sock == serSocket:</span><br><span class="line">            newSocket,clientInfo = serSocket.accept()</span><br><span class="line">            print(<span class="string">"%s connected..."</span> %str(clientInfo))</span><br><span class="line">            inputs.append(newSocket)</span><br><span class="line">        <span class="comment"># 如果是客户端的套接字发生变化，则表示有数据传递</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            data = sock.recv(<span class="number">1024</span>)</span><br><span class="line">            <span class="keyword">if</span> data:</span><br><span class="line">                print(data.decode(<span class="string">"utf-8"</span>))</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                print(<span class="string">"--- closed ---"</span>)</span><br><span class="line">                sock.close()</span><br><span class="line">                inputs.remove(sock)</span><br><span class="line"></span><br><span class="line">serSocket.close()</span><br></pre></td></tr></table></figure><p>select目前几乎在所有的平台上支持，其良好跨平台支持也是它的一个优点。<br>select的一个缺点在于单个进程能够监视的文件描述符的数量存在最大限制，在Linux上一般为1024，可以通过修改宏定义甚至重新编译内核的方式提升这一限制，但是这样也会造成效率的降低。<br>还有，当套接字比较多的时候，每次select()都要通过遍历很多个Socket来完成调度，不管哪个Socket是活跃的，都遍历一遍。这会浪费很多CPU时间。<br><strong>因此select基本上没什么卵用，它不能用作高并发的服务器。</strong></p><h3 id="epoll-服务器"><a href="#epoll-服务器" class="headerlink" title="epoll 服务器"></a>epoll 服务器</h3><blockquote><p>上面说过，select有最大数量限制。<br>而poll则解决了这个问题，但是poll和select一样都是采用的轮询的机制来对套接字列表进行检测的，这样会使得服务器的效率变得低下。<br>因此poll也不常用，这里只是和select函数做一个比较。<br>而这一节要说的是epoll服务器，这个是poll的一个增强版本，它不仅没有最大数量限制(这只是理论上的，实际上还要根据内存的大小来考虑)，也不是通过轮询方式进行检测，而是通过事件通知机制来检测套接字是否发生了变化。<br>轮询相当于一个一个的去问有没有变化，事件通知机制则是套接字自己说自己有没有变化。<br>这样一来，效率就比select和poll要高了。</p></blockquote><p>epoll是select模块中的一个类，其中有2个比较重要的方法分别是register和poll。<br>register用法如下：<br><code>register(fd[, eventmask]) -&gt; None</code><br>其中的fd为文件描述符(详情见C语言中的文件描述符)，eventmask为对文件描述符对应的文件的操作，常见的有EPOLLIN, EPOLLOUT, EPOLLET, EPOLLPRO。其中的in和out为可读和可写，后面两种分别为：<br>ET模式：当epoll检测到描述符事件发生并将此事件通知应用程序，应用程序必须立即处理该事件。如果不处理，下次调用epoll时，不会再次响应应用程序并通知此事件。<br>LT模式：当epoll检测到描述符事件发生并将此事件通知应用程序，应用程序可以不立即处理该事件。下次调用epoll时，会再次响应应用程序并通知此事件。<br>注意：如果fd已经注册过，这里会发生异常。<br>poll用法如下：<br><code>poll([timeout=-1[, maxevents=-1]]) -&gt; [(fd, events), (...)]</code><br>poll方法和select方法一样，阻塞（如果没有指定超时时间），然后对注册的文件描述符（这里是套接字）进行扫描，一有套接字变化的通知立马解开阻塞。<br>epoll服务器案例代码如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> socket <span class="keyword">import</span> *</span><br><span class="line"><span class="keyword">import</span> select</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建套接字等基本操作</span></span><br><span class="line">serSocket = socket(AF_INET, SOCK_STREAM)</span><br><span class="line">serSocket.setsockopt(SOL_SOCKET, SO_REUSEADDR, <span class="number">1</span>)</span><br><span class="line">address = (<span class="string">""</span>, <span class="number">8888</span>)</span><br><span class="line">serSocket.bind(address)</span><br><span class="line">serSocket.listen(<span class="number">10</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建一个对象</span></span><br><span class="line">epl = select.epoll()</span><br><span class="line"><span class="comment"># 将服务器套接字注册到epl中（也就是将套接字添加到epl的事件监听中）</span></span><br><span class="line">epl.register(serSocket.fileno(), select.EPOLLIN|select.EPOLLET)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 设置两个字典用于保存连接的套接字和信息</span></span><br><span class="line">connections = &#123;&#125;</span><br><span class="line">addrInfo = &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">    <span class="comment"># 阻塞，当有套接字产生事件的时候解阻塞，并返回所有产生套接字事件的fd和事件</span></span><br><span class="line">    epoll_list = epl.poll()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> fd,events <span class="keyword">in</span> epoll_list:</span><br><span class="line">    </span><br><span class="line">        <span class="comment"># 如果是服务器套接字产生事件，那么代表有新的连接</span></span><br><span class="line">        <span class="keyword">if</span> fd == serSocket.fileno():</span><br><span class="line">            newSocket,clientInfo = serSocket.accept()</span><br><span class="line">            print(<span class="string">"%s connected..."</span> %str(clientInfo))</span><br><span class="line">            connections[newSocket.fileno()] = newSocket</span><br><span class="line">            addrInfo[newSocket.fileno()] = clientInfo</span><br><span class="line">            <span class="comment"># 将新的套接字注册到epl中</span></span><br><span class="line">            epl.register(newSocket.fileno(), select.EPOLLIN|select.EPOLLET)</span><br><span class="line">        <span class="comment"># 如果是其他的套接字产生事件，则代表有数据接收</span></span><br><span class="line">        <span class="keyword">elif</span> events == select.EPOLLIN:</span><br><span class="line">            recvData = connections[fd].recv(<span class="number">1024</span>)</span><br><span class="line">            <span class="keyword">if</span> recvData:</span><br><span class="line">                print(<span class="string">"%s:%s"</span> %(str(addrInfo[fd]),recvData.decode(<span class="string">"utf-8"</span>)))</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                <span class="comment"># 套接字关闭后解除注册</span></span><br><span class="line">                epl.unregister(fd)</span><br><span class="line">                connections[fd].close()</span><br><span class="line">                print(<span class="string">"%s closed..."</span> %str(addrInfo[fd]))</span><br><span class="line">                <span class="keyword">del</span> connections[fd]</span><br><span class="line">                <span class="keyword">del</span> addrInfo[fd]</span><br><span class="line"></span><br><span class="line">setsockopt.close()</span><br></pre></td></tr></table></figure><h2 id="正则表达式"><a href="#正则表达式" class="headerlink" title="正则表达式"></a>正则表达式</h2><p>正则表达式，又称正规表示式、正规表示法、正规表达式、规则表达式、常规表示法(英语：Regular Expression，在代码中常简写为regex、regexp或RE)，是计算机科学的一个概念。<br>正则表达式使用单个字符串来描述、匹配一系列匹配某个句法规则的字符串。在很多文本编辑器里，正则表达式通常被用来检索、替换那些匹配某个模式的文本。<br>Regular Expression的”Regular”一般被译为”正则”、”正规”、”常规”。此处的”Regular”即是”规则”、”规律”的意思，Regular Expression即”描述某种规则的表达式”之意。</p><h3 id="re-模块"><a href="#re-模块" class="headerlink" title="re 模块"></a>re 模块</h3><p>在Python中需要通过正则表达式对字符串进行匹配的时候，可以使用一个模块，名字为<code>re</code><br>因此要使用re模块中的内容，首先要导入re模块：<br><code>import re</code><br>re模块中有个方法为match，使用方法如下：<br><code>match(pattern, string, flags=0)</code><br>第一个参数pattern为正则表达式，第二个参数为要匹配的字符串（一般就使用这两个参数）。<br>match方法会从字符串的开头处开始用正则表达式对其匹配，如果能完整的匹配正则表达式中的所有内容，则会返回匹配对象，否则返回None。<br>在返回的对象中，可以使用group方法来查看匹配到的内容。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> re</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>ret = re.match(<span class="string">"hello"</span>,<span class="string">"hello world"</span>)</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>ret.group()</span><br><span class="line"><span class="string">'hello'</span></span><br></pre></td></tr></table></figure><h3 id="原始字符串"><a href="#原始字符串" class="headerlink" title="原始字符串"></a>原始字符串</h3><blockquote><p>字符串中的反斜杠”&quot;在某些地方是作为转义字符来使用的。<br>正则表达式里也是使用”&quot;作为转义字符，这就可能造成反斜杠困扰。<br>假如你需要匹配文本中的字符”&quot;，那么使用编程语言表示的正则表达式里将需要4个反斜杠”\\“：前两个和后两个分别用于在编程语言里转义成反斜杠，转换成两个反斜杠后再在正则表达式里转义成一个反斜杠。<br>Python里的原生字符串很好地解决了这个问题，有了原始字符串，你再也不用担心是不是漏写了反斜杠，写出来的表达式也更直观。<br>如：</p></blockquote><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>str = <span class="string">"C:\\a"</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(str)</span><br><span class="line">C:\a</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(re.match(<span class="string">"C:\\\\a"</span>,str).group())</span><br><span class="line">C:\a</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>print(re.match(<span class="string">r"C:\\a"</span>,str).group())</span><br><span class="line">C:\a</span><br></pre></td></tr></table></figure><p><strong>要使用原始字符串只需要在字符串前加上一个r字母即可</strong></p><h3 id="正则表达式匹配规则"><a href="#正则表达式匹配规则" class="headerlink" title="正则表达式匹配规则"></a>正则表达式匹配规则</h3><ul><li><p><strong>字符匹配</strong></p><table><thead><tr><th align="center">字符</th><th>功能</th></tr></thead><tbody><tr><td align="center">.</td><td>匹配任意一个字符，’\n’除外，在DOTALL模式中可以匹配’\n’</td></tr><tr><td align="center">[…]</td><td>匹配中括号中列举的所有字符，可使用0-9之类的范围</td></tr><tr><td align="center">\d</td><td>匹配数字，即0-9</td></tr><tr><td align="center">\D</td><td>匹配非数字</td></tr><tr><td align="center">\s</td><td>匹配空白(\n \r \t之类的可以匹配)</td></tr><tr><td align="center">\S</td><td>匹配非空白</td></tr><tr><td align="center">\w</td><td>匹配单词字符，即a-zA-Z0-9_</td></tr><tr><td align="center">\W</td><td>匹配非单词字符</td></tr></tbody></table></li><li><p><strong>数量匹配</strong></p><table><thead><tr><th align="center">字符</th><th>功能</th></tr></thead><tbody><tr><td align="center">*</td><td>匹配前一个字符出现任意次(包括0次)</td></tr><tr><td align="center">+</td><td>匹配前一个字符至少一次</td></tr><tr><td align="center">?</td><td>匹配前一个字符0或1次</td></tr><tr><td align="center">{m}</td><td>匹配前一个字符出现m次(正好m次)</td></tr><tr><td align="center">{m,}</td><td>匹配前一个字符至少m次</td></tr><tr><td align="center">{m,n}</td><td>匹配前一个字符m到n次</td></tr></tbody></table></li><li><p><strong>表示边界</strong></p><table><thead><tr><th align="center">字符</th><th>功能</th></tr></thead><tbody><tr><td align="center">^</td><td>匹配字符串开头</td></tr><tr><td align="center">$</td><td>匹配字符串结尾</td></tr><tr><td align="center">\b</td><td>匹配一个单词的边界</td></tr><tr><td align="center">\B</td><td>匹配非单词边界</td></tr></tbody></table></li><li><p><strong>匹配分组</strong></p><table><thead><tr><th align="center">字符</th><th>功能</th></tr></thead><tbody><tr><td align="center">|</td><td>匹配左右任意一个表达式(一般用于分组)</td></tr><tr><td align="center">()</td><td>将括号中字符串作为一个分组</td></tr><tr><td align="center">\NUM</td><td>引用第NUM个分组</td></tr><tr><td align="center">(?P&lt;NAME&gt;…)</td><td>给分组起别名</td></tr><tr><td align="center">(?P=NAME)</td><td>引用别名为NAME的分组</td></tr></tbody></table><p>注意：在用分组进行匹配的时候，可以用<code>group</code>方法来打印匹配到的分组，<br>如group(1)就是打印第一个分组，group(2)就是打印第二个分组，以此类推。</p></li></ul><h3 id="re-模块中的其他函数"><a href="#re-模块中的其他函数" class="headerlink" title="re 模块中的其他函数"></a>re 模块中的其他函数</h3><ul><li><p><strong>search</strong><br>search和match的使用方法是一样的，唯一不同的是match是从字符串第一个字符开始匹配的，而search可以从任意地方进行匹配<br><strong>注意</strong>：search只匹配一个，返回第一个匹配到的<br>search使用方法如下：<br><code>search(pattern, string, flags=0)</code></p></li><li><p><strong>findall</strong><br>findall和search类似，不过findall是将所有匹配到的字符串组成一个列表返回。<br>使用方法如下：<br><code>findall(pattern, string, flags=0)</code></p></li><li><p><strong>sub</strong><br>sub方法可以将匹配到的字符串进行替换，返回结果为替换之后的结果。<br>使用方法如下：<br><code>sub(pattern, repl, string, count=0, flags=0)</code><br>其中的repl可以是替换后的字符串，也可以是一个替换规则(即函数引用)。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> re</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>re.sub(<span class="string">r"\d+"</span>, <span class="string">"1000"</span>, <span class="string">"python = 238"</span>)</span><br><span class="line"><span class="string">'python = 1000'</span></span><br><span class="line">&gt;&gt;&gt;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="function"><span class="keyword">def</span> <span class="title">add</span><span class="params">(temp)</span>:</span></span><br><span class="line"><span class="meta">... </span>    r = int(temp.group()) + <span class="number">100</span></span><br><span class="line"><span class="meta">... </span>    <span class="keyword">return</span> str(r)</span><br><span class="line"><span class="meta">... </span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>re.sub(<span class="string">r"\d+"</span>, add, <span class="string">"python = 1234"</span>)</span><br><span class="line"><span class="string">'python = 1334'</span></span><br><span class="line">&gt;&gt;&gt;</span><br></pre></td></tr></table></figure></li><li><p><strong>split</strong><br>split函数会根据匹配进行字符串的切割，并且返回一个列表。使用方法如下：<br><code>split(pattern, string, maxsplit=0, flags=0)</code><br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>re.split(<span class="string">r':| '</span>, <span class="string">"info:LaoWang 33 ShangHai"</span>)</span><br><span class="line">[<span class="string">'info'</span>, <span class="string">'LaoWang'</span>, <span class="string">'33'</span>, <span class="string">'ShangHai'</span>]</span><br></pre></td></tr></table></figure></li></ul><h3 id="贪婪和非贪婪"><a href="#贪婪和非贪婪" class="headerlink" title="贪婪和非贪婪"></a>贪婪和非贪婪</h3><p>在Python中正则表达式的匹配中，默认是<strong>贪婪</strong>的(总是尝试匹配尽可能多的字符)。<strong>非贪婪</strong>则相反，总是匹配尽可能少的字符。<br>那么哪些规则字符会匹配尽可能多的字符呢？<br>有 <code>*、?、+、{m,n}</code>, 这些规则会尽可能的匹配更多字符，我们可以在这些字符后面再加上一个<code>?</code>，那么这些规则字符就会从贪婪模式变为非贪婪模式。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>s = <span class="string">"This number is 234-235-838-181"</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>re.match(<span class="string">r".*(\d+-\d+-\d+-\d+)"</span>, s).group(<span class="number">1</span>)</span><br><span class="line"><span class="string">'4-235-838-181'</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>re.match(<span class="string">r".*?(\d+-\d+-\d+-\d+)"</span>, s).group(<span class="number">1</span>)</span><br><span class="line"><span class="string">'234-235-838-181'</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>re.match(<span class="string">r"aa(\d+)"</span>, <span class="string">"aa1234ddd"</span>).group(<span class="number">1</span>)</span><br><span class="line"><span class="string">'1234'</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>re.match(<span class="string">r"aa(\d+?)"</span>, <span class="string">"aa1234ddd"</span>).group(<span class="number">1</span>)</span><br><span class="line"><span class="string">'1'</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>re.match(<span class="string">r"aa(\d+)ddd"</span>, <span class="string">"aa1234ddd"</span>).group(<span class="number">1</span>) </span><br><span class="line"><span class="string">'1234'</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>re.match(<span class="string">r"aa(\d+?)ddd"</span>, <span class="string">"aa1234ddd"</span>).group(<span class="number">1</span>)</span><br><span class="line"><span class="string">'1234'</span></span><br></pre></td></tr></table></figure><p><strong>注意</strong>：所谓的非贪婪也要在能完整匹配字符串的前提下尽可能少的匹配字符。</p><h2 id="web-服务器案例"><a href="#web-服务器案例" class="headerlink" title="web 服务器案例"></a>web 服务器案例</h2><p>在Web应用中，服务器把网页传给浏览器，实际上就是把网页的HTML代码发送给浏览器，让浏览器显示出来。而浏览器和服务器之间的传输协议是HTTP，所以：</p><blockquote><p>HTML是一种用来定义网页的文本，会HTML，就可以编写网页；<br>HTTP是在网络上传输HTML的协议，用于浏览器和服务器的通信。</p></blockquote><h3 id="HTTP-协议"><a href="#HTTP-协议" class="headerlink" title="HTTP 协议"></a>HTTP 协议</h3><p>浏览器通过HTTP协议连接到服务器的时候，会发送一个请求报文。<br>其中最主要的头两行分析如下：<br>第一行如：<br><code>GET / HTTP/1.1</code></p><blockquote><p>GET表示一个读取请求，将从服务器获得网页数据<br>/表示URL的路径，URL总是以/开头，/就表示首页<br>最后的HTTP/1.1指示采用的HTTP协议版本是1.1<br>目前HTTP协议的版本就是1.1，但是大部分服务器也支持1.0版本，主要区别在于1.1版本允许多个HTTP请求复用一个TCP连接，以加快传输速度。</p></blockquote><p>HTTP所有的<strong>请求方式</strong>如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">GET：请求获取数据</span><br><span class="line">POST：请求修改数据</span><br><span class="line">PUT：请求保存数据</span><br><span class="line">DELETE：请求删除数据</span><br><span class="line">OPTION：询问服务器的某种支持特性</span><br><span class="line">HEAD：请求返回报文头</span><br></pre></td></tr></table></figure><p>从第二行开始，每一行都是以KEY:VALUE的形式（类似于字典）。一般开始是：<br><strong>Host: <a href="http://www.xxx.com" target="_blank" rel="noopener">www.xxx.com</a></strong><br>这个表示请求的域名</p><p>客户端请求之后服务器需要给出响应，HTTP响应分为Header和Body两部分(Body是可选项)。最重要的几行如下：<br><code>HTTP/1.1 200 OK</code><br>200表示一个成功的响应，OK是一个说明。<br>如果返回的不是200，那么往往有其他的功能，例如<strong>失败</strong>的响应有:<br><strong>404 Not Found</strong>：网页不存在<br><strong>500 Internal Server Error</strong>：服务器内部出错<br>…等等…</p><p><code>Content-Type: text/html</code><br>Content-Type指示响应的内容，这里是text/html表示HTML网页。<br>请注意，浏览器就是依靠Content-Type来判断响应的内容是网页还是图片，是视频还是音乐。<br>浏览器并不靠URL来判断响应的内容，所以，即使URL是<code>http://www.baidu.com/meimei.jpg</code>，它也不一定就是图片。</p><p>HTTP响应的<strong>Body</strong>就是HTML源码，也就是通过浏览器看到的东西。</p><h3 id="HTTP-请求与响应"><a href="#HTTP-请求与响应" class="headerlink" title="HTTP 请求与响应"></a>HTTP 请求与响应</h3><ul><li><p><strong>GET请求格式</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">GET &#x2F;path HTTP&#x2F;1.1</span><br><span class="line">Header1: Value1</span><br><span class="line">Header2: Value2</span><br><span class="line">Header3: Value3</span><br></pre></td></tr></table></figure><p>每个Header一行一个，换行符是\r\n。</p></li><li><p><strong>POST请求格式</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">POST &#x2F;path HTTP&#x2F;1.1</span><br><span class="line">Header1: Value1</span><br><span class="line">Header2: Value2</span><br><span class="line">Header3: Value3</span><br><span class="line"></span><br><span class="line">body data goes here...</span><br></pre></td></tr></table></figure><p>当遇到连续两个<code>\r\n</code>时，Header部分结束，后面的数据全部是Body。</p></li><li><p><strong>服务器响应格式</strong></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">200 OK</span><br><span class="line">Header1: Value1</span><br><span class="line">Header2: Value2</span><br><span class="line">Header3: Value3</span><br><span class="line"></span><br><span class="line">body data goes here...</span><br></pre></td></tr></table></figure><p>HTTP响应如果包含body，也是通过<code>\r\n\r\n</code>来分隔的。<br>请再次注意，Body的数据类型由<strong>Content-Type</strong>头来确定，如果是网页，Body就是文本，如果是图片，Body就是图片的二进制数据。<br>当存在<strong>Content-Encoding</strong>时，Body数据是被压缩的，最常见的压缩方式是gzip，所以，看到Content-Encoding: gzip时，需要将Body数据先解压缩，才能得到真正的数据。压缩的目的在于减少Body的大小，加快网络传输。</p></li></ul><h3 id="Web-静态服务器"><a href="#Web-静态服务器" class="headerlink" title="Web 静态服务器"></a>Web 静态服务器</h3><ul><li><p><strong>显示固定页面</strong></p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> socket</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">handleClient</span><span class="params">(client_socket)</span>:</span></span><br><span class="line">    <span class="string">"""处理客户端请求线程"""</span></span><br><span class="line">    request_data = client_socket.recv(<span class="number">1024</span>)</span><br><span class="line">    print(<span class="string">"%s"</span> %request_data.decode(<span class="string">"utf-8"</span>))</span><br><span class="line">    <span class="comment"># 构造服务器响应报文</span></span><br><span class="line">    response_start_line = <span class="string">"HTTP/1.1 200 OK\r\n"</span></span><br><span class="line">    response_headers = <span class="string">"Server: Janvy's server\r\n"</span></span><br><span class="line">    response_body = <span class="string">"Hello World"</span></span><br><span class="line">    response = response_start_line + response_headers + <span class="string">"\r\n"</span> + response_body</span><br><span class="line">    print(<span class="string">"%s\n"</span> %response)</span><br><span class="line">    <span class="comment"># 向客户端发送响应数据</span></span><br><span class="line">    client_socket.send(response.encode(<span class="string">"utf-8"</span>))</span><br><span class="line">    <span class="comment"># 关闭客户端连接</span></span><br><span class="line">    client_socket.close()</span><br><span class="line">    </span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="comment"># 创建套接字</span></span><br><span class="line">    server_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)</span><br><span class="line">    server_socket.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, <span class="number">1</span>)</span><br><span class="line">    address = (<span class="string">""</span>, <span class="number">8000</span>)</span><br><span class="line">    server_socket.bind(address)</span><br><span class="line">    server_socket.listen(<span class="number">128</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 接收客户端请求</span></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        client_socket,client_addr = server_socket.accept()</span><br><span class="line">        print(<span class="string">"%s:%d connected...\n"</span> %(client_addr[<span class="number">0</span>], client_addr[<span class="number">1</span>]))</span><br><span class="line">        new_thread = Thread(target=handleClient, args=(client_socket,))</span><br><span class="line">        new_thread.start()</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure></li><li><p><strong>显示静态文件</strong></p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># ---- py ----</span></span><br><span class="line"><span class="keyword">import</span> socket</span><br><span class="line"><span class="keyword">import</span> re</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"></span><br><span class="line"><span class="comment"># 设置静态文件根目录</span></span><br><span class="line">HTML_ROOT_DIR = <span class="string">"./html"</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">handleClient</span><span class="params">(client_socket)</span>:</span></span><br><span class="line">    <span class="string">"""处理客户端请求线程"""</span></span><br><span class="line">    request_data = client_socket.recv(<span class="number">1024</span>)</span><br><span class="line">    </span><br><span class="line">    request_lines = request_data.decode(<span class="string">"utf-8"</span>).splitlines()</span><br><span class="line">    <span class="comment">#for line in request_lines:</span></span><br><span class="line">        <span class="comment">#print(line)</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 获取请求报文的起始行</span></span><br><span class="line">    request_start_line = request_lines[<span class="number">0</span>]</span><br><span class="line">    <span class="comment"># 用正则表达式获取起始行路径</span></span><br><span class="line">    file_name = re.match(<span class="string">r"[A-Z]+ +(/[^ ]*) "</span>, request_start_line).group(<span class="number">1</span>)</span><br><span class="line">    <span class="keyword">if</span> <span class="string">"/"</span> == file_name:</span><br><span class="line">        file_name = <span class="string">"/index.html"</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 打开文件，读取内容</span></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        fr = open(HTML_ROOT_DIR + file_name, <span class="string">"rb"</span>)</span><br><span class="line">    <span class="comment"># 如果文件不存在则发送404报文</span></span><br><span class="line">    <span class="keyword">except</span>:</span><br><span class="line">        response_start_line = <span class="string">"HTTP/1.1 404 Not Found\r\n"</span></span><br><span class="line">        response_headers = <span class="string">"Server: Janvy's server\r\n"</span></span><br><span class="line">        response_body = <span class="string">"The file is not found!"</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        file_data = fr.read()</span><br><span class="line">        fr.close()</span><br><span class="line">        <span class="comment"># 构造服务器响应报文</span></span><br><span class="line">        response_start_line = <span class="string">"HTTP/1.1 200 OK\r\n"</span></span><br><span class="line">        response_headers = <span class="string">"Server: Janvy's server\r\n"</span></span><br><span class="line">        response_body = file_data.decode(<span class="string">"utf-8"</span>)</span><br><span class="line"></span><br><span class="line">    response = response_start_line + response_headers + <span class="string">"\r\n"</span> + response_body</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 向客户端发送响应数据</span></span><br><span class="line">    client_socket.send(response.encode(<span class="string">"utf-8"</span>))</span><br><span class="line">    <span class="comment"># 关闭客户端连接</span></span><br><span class="line">    client_socket.close()</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="comment"># 创建套接字</span></span><br><span class="line">    server_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)</span><br><span class="line">    server_socket.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, <span class="number">1</span>)</span><br><span class="line">    address = (<span class="string">""</span>, <span class="number">8000</span>)</span><br><span class="line">    server_socket.bind(address)</span><br><span class="line">    server_socket.listen(<span class="number">128</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 接收客户端请求</span></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        client_socket,client_addr = server_socket.accept()</span><br><span class="line">        print(<span class="string">"%s:%d connected...\n"</span> %(client_addr[<span class="number">0</span>], client_addr[<span class="number">1</span>]))</span><br><span class="line">        <span class="comment">#handleClient(client_socket)</span></span><br><span class="line">        new_thread = Thread(target=handleClient, args=(client_socket,))</span><br><span class="line">        new_thread.start()</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure></li><li><p><strong>面向对象</strong></p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> socket</span><br><span class="line"><span class="keyword">import</span> re</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">HTTPServer</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="string">""""""</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.server_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)</span><br><span class="line">        self.server_socket.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, <span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">handle_client</span><span class="params">(self, client_socket)</span>:</span></span><br><span class="line">        <span class="string">"""处理客户端请求"""</span></span><br><span class="line">        request_data = client_socket.recv(<span class="number">1024</span>)</span><br><span class="line">    </span><br><span class="line">        request_lines = request_data.decode(<span class="string">"utf-8"</span>).splitlines()</span><br><span class="line">        <span class="comment">#for line in request_lines:</span></span><br><span class="line">            <span class="comment">#print(line)</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 获取请求报文的起始行</span></span><br><span class="line">        request_start_line = request_lines[<span class="number">0</span>]</span><br><span class="line">        <span class="comment"># 用正则表达式获取起始行路径</span></span><br><span class="line">        file_name = re.match(<span class="string">r"[A-Z]+ +(/[^ ]*) "</span>, request_start_line).group(<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">if</span> <span class="string">"/"</span> == file_name:</span><br><span class="line">            file_name = <span class="string">"/index.html"</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 打开文件，读取内容</span></span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            fr = open(HTML_ROOT_DIR + file_name, <span class="string">"rb"</span>)</span><br><span class="line">        <span class="comment"># 如果文件不存在则发送404报文</span></span><br><span class="line">        <span class="keyword">except</span>:</span><br><span class="line">            response_start_line = <span class="string">"HTTP/1.1 404 Not Found\r\n"</span></span><br><span class="line">            response_headers = <span class="string">"Server: Janvy's server\r\n"</span></span><br><span class="line">            response_body = <span class="string">"The file is not found!"</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            file_data = fr.read()</span><br><span class="line">            fr.close()</span><br><span class="line">            <span class="comment"># 构造服务器响应报文</span></span><br><span class="line">            response_start_line = <span class="string">"HTTP/1.1 200 OK\r\n"</span></span><br><span class="line">            response_headers = <span class="string">"Server: Janvy's server\r\n"</span></span><br><span class="line">            response_body = file_data.decode(<span class="string">"utf-8"</span>)</span><br><span class="line"></span><br><span class="line">        response = response_start_line + response_headers + <span class="string">"\r\n"</span> + response_body</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 向客户端发送响应数据</span></span><br><span class="line">        client_socket.send(response.encode(<span class="string">"utf-8"</span>))</span><br><span class="line">        <span class="comment"># 关闭客户端连接</span></span><br><span class="line">        client_socket.close()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">start</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""启动服务"""</span></span><br><span class="line">        self.server_socket.listen(<span class="number">128</span>)</span><br><span class="line">        <span class="comment"># 接收客户端请求</span></span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            client_socket,client_addr = self.server_socket.accept()</span><br><span class="line">            print(<span class="string">"%s:%d connected...\n"</span> %(client_addr[<span class="number">0</span>], client_addr[<span class="number">1</span>]))</span><br><span class="line">            <span class="comment">#handleClient(client_socket)</span></span><br><span class="line">            new_thread = Thread(target=self.handle_client, args=(client_socket,))</span><br><span class="line">            new_thread.start()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">bind</span><span class="params">(self, port)</span>:</span></span><br><span class="line">        <span class="string">"""绑定端口"""</span></span><br><span class="line">        address = (<span class="string">""</span>, port)</span><br><span class="line">        self.server_socket.bind(address)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 设置静态文件根目录</span></span><br><span class="line">HTML_ROOT_DIR = <span class="string">"./html"</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    http_server = HTTPServer()</span><br><span class="line">    http_server.bind(<span class="number">8000</span>)</span><br><span class="line">    http_server.start()</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure></li></ul><h3 id="Web-动态服务器"><a href="#Web-动态服务器" class="headerlink" title="Web 动态服务器"></a>Web 动态服务器</h3><p>我们上面写的服务器是静态服务器，只能返回静态的资源给客户端。<br>那么我们如何响应HTTP客户端请求动态资源呢？<br>服务器可以通过wsgi调用一个属性，那么什么是wsgi呢？</p><blockquote><p>wsgi：Web Server Gateway Interface (或简称 WSGI，读作“wizgy”)。<br>WSGI允许开发者将选择web框架和web服务器分开。<br>可以混合匹配web服务器和web框架，选择一个适合的配对。<br>比如,可以在Gunicorn 或者 Nginx/WSGI 或者 Waitress上运行 Django, Flask, 或 Pyramid。<br>真正的混合匹配，得益于WSGI同时支持服务器和架构。<br>web服务器必须具备WSGI接口，所有的现代Python Web框架都已具备WSGI接口，它让你不对代码作修改就能使服务器和特点的web框架协同工作。</p><p>WSGI由web服务器支持，而web框架允许你选择适合自己的配对，但它同样对于服务器和框架开发者提供便利使他们可以专注于自己偏爱的领域和专长而不至于相互牵制。<br>其他语言也有类似接口：java有Servlet API，Ruby 有 Rack。</p><p>WSGI接口定义非常简单，它只要求Web开发者实现一个函数，就可以响应HTTP请求。<br>如：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">&gt;<span class="function"><span class="keyword">def</span> <span class="title">application</span><span class="params">(environ, start_response)</span>:</span></span><br><span class="line">   start_response(<span class="string">'200 OK'</span>, [(<span class="string">'Content-Type'</span>, <span class="string">'text/html'</span>)])</span><br><span class="line">   <span class="keyword">return</span> <span class="string">'Hello World!'</span></span><br></pre></td></tr></table></figure><p>上面的application()函数就是符合WSGI标准的一个HTTP处理函数，它接收两个参数：</p><ul><li>environ：一个包含所有HTTP请求信息的dict对象；</li><li>start_response：一个发送HTTP响应的函数。</li></ul><p>整个application()函数本身没有涉及到任何解析HTTP的部分，也就是说，把底层web服务器解析部分和应用程序逻辑部分进行了分离，这样开发者就可以专心做一个领域了。<br>但是，这个application函数怎么调用呢？如果我们自己调用，两个参数environ和start_response我们没法提供，返回的str也没法发给浏览器。<br>所以application()函数必须由WSGI服务器来调用。有很多符合WSGI规范的服务器。而我们此时的web服务器项目的目的就是做一个极可能解析静态网页还可以解析动态网页的服务器。<br>注意：WSGI中的application函数需要返回响应体。</p></blockquote><ul><li><p><strong>WSGI服务器简单案例</strong></p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> socket</span><br><span class="line"><span class="keyword">import</span> re</span><br><span class="line"><span class="keyword">import</span> sys</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">HTTPServer</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="string">""""""</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.server_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)</span><br><span class="line">        self.server_socket.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, <span class="number">1</span>)</span><br><span class="line">        self.response_header = <span class="string">""</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">start_response</span><span class="params">(self, status, headers)</span>:</span></span><br><span class="line">        <span class="string">"""对报文头部进行处理"""</span></span><br><span class="line">        response_headers = <span class="string">"HTTP/1.1 "</span> + status + <span class="string">"\r\n"</span></span><br><span class="line">        <span class="keyword">for</span> header <span class="keyword">in</span> headers:</span><br><span class="line">            response_headers += <span class="string">"%s: %s\r\n"</span> %heaser</span><br><span class="line">        self.response_header = response_headers</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">handle_client</span><span class="params">(self, client_socket)</span>:</span></span><br><span class="line">        <span class="string">"""处理客户端请求"""</span></span><br><span class="line">        request_data = client_socket.recv(<span class="number">1024</span>)</span><br><span class="line">        request_lines = request_data.decode(<span class="string">"utf-8"</span>).splitlines()</span><br><span class="line">  </span><br><span class="line">        <span class="comment"># 获取请求报文的起始行</span></span><br><span class="line">        request_start_line = request_lines[<span class="number">0</span>]</span><br><span class="line">        <span class="comment"># 用正则表达式获取起始行路径</span></span><br><span class="line">        file_name = re.match(<span class="string">r"[A-Z]+ +(/[^ ]*) "</span>, request_start_line).group(<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 处理客户端获取动态资源的请求</span></span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            <span class="comment"># 导入客户请求资源的模块</span></span><br><span class="line">            <span class="keyword">if</span> file_name.endswith(<span class="string">".py"</span>):</span><br><span class="line">                m = __import__(file_name[<span class="number">1</span>:<span class="number">-3</span>])</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                m = __import__(file_name[<span class="number">1</span>:])</span><br><span class="line">        <span class="comment"># 获取动态资源失败之后尝试获取静态资源</span></span><br><span class="line">        <span class="keyword">except</span>:</span><br><span class="line">            <span class="keyword">if</span> <span class="string">"/"</span> == file_name:</span><br><span class="line">                file_name = <span class="string">"/index.html"</span></span><br><span class="line">            <span class="keyword">try</span>:</span><br><span class="line">                <span class="comment"># 打开文件，读取内容</span></span><br><span class="line">                fr = open(HTML_ROOT_DIR + file_name, <span class="string">"rb"</span>)</span><br><span class="line">            <span class="comment"># 如果文件不存在则发送404报文</span></span><br><span class="line">            <span class="keyword">except</span>:</span><br><span class="line">                response_start_line = <span class="string">"HTTP/1.1 404 Not Found\r\n"</span></span><br><span class="line">                response_headers = <span class="string">"Server: Janvy's server\r\n"</span></span><br><span class="line">                response_body = <span class="string">"The file is not found!"</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                file_data = fr.read()</span><br><span class="line">                fr.close()</span><br><span class="line">                <span class="comment"># 构造服务器响应报文</span></span><br><span class="line">                response_start_line = <span class="string">"HTTP/1.1 200 OK\r\n"</span></span><br><span class="line">                response_headers = <span class="string">"Server: Janvy's server\r\n"</span></span><br><span class="line">                response_body = file_data.decode(<span class="string">"utf-8"</span>)</span><br><span class="line"></span><br><span class="line">            response = response_start_line + response_headers + <span class="string">"\r\n"</span> + response_body</span><br><span class="line">        <span class="comment"># 成功导入模块则通过WSGI调用模块内接口</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            env = &#123;&#125;</span><br><span class="line">            response_body = m.application(env, self.start_response)</span><br><span class="line">            response = self.response_header + <span class="string">"\r\n"</span> + response_body</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 向客户端发送响应数据</span></span><br><span class="line">        client_socket.send(response.encode(<span class="string">"utf-8"</span>))</span><br><span class="line">        <span class="comment"># 关闭客户端连接</span></span><br><span class="line">        client_socket.close()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">start</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""启动服务"""</span></span><br><span class="line">        self.server_socket.listen(<span class="number">128</span>)</span><br><span class="line">        <span class="comment"># 接收客户端请求</span></span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            client_socket,client_addr = self.server_socket.accept()</span><br><span class="line">            print(<span class="string">"%s:%d connected...\n"</span> %(client_addr[<span class="number">0</span>], client_addr[<span class="number">1</span>]))</span><br><span class="line">            <span class="comment">#self.handle_client(client_socket)</span></span><br><span class="line">            new_thread = Thread(target=self.handle_client, args=(client_socket,))</span><br><span class="line">            new_thread.start()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">bind</span><span class="params">(self, port)</span>:</span></span><br><span class="line">        <span class="string">"""绑定端口"""</span></span><br><span class="line">        address = (<span class="string">""</span>, port)</span><br><span class="line">        self.server_socket.bind(address)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 设置静态文件根目录</span></span><br><span class="line">HTML_ROOT_DIR = <span class="string">"./html"</span></span><br><span class="line"><span class="comment"># 设置WSGI脚本目录</span></span><br><span class="line">WSGI_DIR = <span class="string">"./wsgipython"</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    sys.path = [WSGI_DIR]</span><br><span class="line">    <span class="comment">#sys.path.insert(1,WSGI_DIR) </span></span><br><span class="line">    http_server = HTTPServer()</span><br><span class="line">    http_server.bind(<span class="number">8000</span>)</span><br><span class="line">    http_server.start()</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure><p>wsgi中的ctime模块如下：<br>ctime.py</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> ctime</span><br><span class="line">              </span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">application</span><span class="params">(env, start_response)</span>:</span></span><br><span class="line">    status = <span class="string">"200 OK"</span></span><br><span class="line">    headers = [ </span><br><span class="line">        (<span class="string">"Contect-Type"</span>, <span class="string">"text/plain"</span>)</span><br><span class="line">    ]         </span><br><span class="line">    <span class="keyword">return</span> ctime()</span><br></pre></td></tr></table></figure></li><li><p><strong>服务器框架</strong><br>我们了解到，WSGI服务器可以让服务器响应客户端发出的动态资源请求。<br>但是WSGI中的每个接口都要单独定义一个文件，这样就让我们的服务器变得繁琐，可移植性也会下降。<br>因此我们需要封装所有的模块到一个模块中，这样就形成了一个框架。<br>框架案例如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> time <span class="keyword">import</span> ctime</span><br><span class="line"></span><br><span class="line"><span class="comment"># 设置静态文件根目录</span></span><br><span class="line">HTML_DIR = <span class="string">"./html"</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Application</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="string">"""框架"""</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, urls)</span>:</span></span><br><span class="line">        <span class="comment"># 设置路由信息</span></span><br><span class="line">        self.urls = urls</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__call__</span><span class="params">(self, env, start_response)</span>:</span></span><br><span class="line">        <span class="string">"""设置call方法可以让类像函数一样调用，这里就是WSGI中的app函数"""</span></span><br><span class="line">        path = env.get(<span class="string">"PATH_INFO"</span>)</span><br><span class="line">        headers = [</span><br><span class="line">        ]</span><br><span class="line">        <span class="keyword">if</span> <span class="string">"/"</span> == path <span class="keyword">or</span> <span class="string">"/index.html"</span> == path:</span><br><span class="line">            path = <span class="string">"/static/index.html"</span></span><br><span class="line">        <span class="comment"># 访问静态文件时</span></span><br><span class="line">        <span class="keyword">if</span> path.startswith(<span class="string">"/static"</span>):</span><br><span class="line">            file_name = path[<span class="number">7</span>:]</span><br><span class="line">            <span class="keyword">try</span>:</span><br><span class="line">                fr = open(HTML_DIR + file_name, <span class="string">"rb"</span>)</span><br><span class="line">            <span class="keyword">except</span>:</span><br><span class="line">                <span class="keyword">pass</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                file_data = fr.read()</span><br><span class="line">                fr.close()</span><br><span class="line">                status = <span class="string">"200 OK"</span></span><br><span class="line">                start_response(status, headers)</span><br><span class="line">                <span class="keyword">return</span> file_data.decode(<span class="string">"utf-8"</span>)</span><br><span class="line">        <span class="comment"># 请求静态资源</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">for</span> url,handler <span class="keyword">in</span> self.urls:</span><br><span class="line">                <span class="keyword">if</span> path == url:</span><br><span class="line">                    <span class="keyword">return</span> handler(env, start_response)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 如果静态资源和动态资源都没有找到</span></span><br><span class="line">        status = <span class="string">"404 Not Found"</span></span><br><span class="line">        start_response(status, headers)</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"&lt;body&gt;&lt;h1&gt;404 Not Found&lt;/h1&gt;&lt;/body&gt;"</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">show_ctime</span><span class="params">(env, start_response)</span>:</span></span><br><span class="line">    status = <span class="string">"200 OK"</span></span><br><span class="line">    headers = [</span><br><span class="line">        (<span class="string">"Content-Type"</span>, <span class="string">"text/plain"</span>)</span><br><span class="line">    ]</span><br><span class="line">    start_response(status, headers)</span><br><span class="line">    <span class="keyword">return</span> ctime()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">say_hello</span><span class="params">(env, start_response)</span>:</span></span><br><span class="line">    status = <span class="string">"200 OK"</span></span><br><span class="line">    headers = [</span><br><span class="line">        (<span class="string">"Content-Type"</span>, <span class="string">"text/plain"</span>)</span><br><span class="line">    ]</span><br><span class="line">    start_response(status, headers)</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"Hello"</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">urls = [</span><br><span class="line">    (<span class="string">"/ctime"</span>, show_ctime),</span><br><span class="line">    (<span class="string">"/sayhello"</span>, say_hello)</span><br><span class="line">]</span><br><span class="line"></span><br><span class="line">app = Application(urls)</span><br></pre></td></tr></table></figure><p>定义好框架后我们可以让服务器调用这个框架：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> socket</span><br><span class="line"><span class="keyword">import</span> re</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">HTTPServer</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="string">""""""</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, app)</span>:</span></span><br><span class="line">        self.server_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)</span><br><span class="line">        self.server_socket.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, <span class="number">1</span>)</span><br><span class="line">        self.app = app</span><br><span class="line">        self.response_header = <span class="string">""</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">start_response</span><span class="params">(self, status, headers)</span>:</span></span><br><span class="line">        <span class="string">"""对报文头部进行处理"""</span></span><br><span class="line">        response_headers = <span class="string">"HTTP/1.1 "</span> + status + <span class="string">"\r\n"</span></span><br><span class="line">        <span class="keyword">for</span> header <span class="keyword">in</span> headers:</span><br><span class="line">            response_headers += <span class="string">"%s: %s\r\n"</span> %header</span><br><span class="line">        self.response_header = response_headers</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">handle_client</span><span class="params">(self, client_socket)</span>:</span></span><br><span class="line">        <span class="string">"""处理客户端请求"""</span></span><br><span class="line">        request_data = client_socket.recv(<span class="number">1024</span>)</span><br><span class="line">        request_lines = request_data.decode(<span class="string">"utf-8"</span>).splitlines()</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 获取请求报文的起始行</span></span><br><span class="line">        request_start_line = request_lines[<span class="number">0</span>]</span><br><span class="line">        <span class="comment"># 用正则表达式获取起始行路径</span></span><br><span class="line">        file_name = re.match(<span class="string">r"[A-Z]+ +(/[^ ]*) "</span>, request_start_line).group(<span class="number">1</span>)</span><br><span class="line">        method = re.match(<span class="string">r"([A-Z]+) +/[^ ]* "</span>, request_start_line).group(<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 处理客户端获取资源的请求</span></span><br><span class="line">        env = &#123;</span><br><span class="line">            <span class="string">"PATH_INFO"</span>:file_name,</span><br><span class="line">            <span class="string">"METHOD"</span>:method</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment"># 调用框架解决资源请求</span></span><br><span class="line">        response_body = self.app(env, self.start_response)</span><br><span class="line">        response = self.response_header + <span class="string">"\r\n"</span> + response_body</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 向客户端发送响应数据</span></span><br><span class="line">        client_socket.send(response.encode(<span class="string">"utf-8"</span>))</span><br><span class="line">        <span class="comment"># 关闭客户端连接</span></span><br><span class="line">        client_socket.close()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">start</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""启动服务"""</span></span><br><span class="line">        self.server_socket.listen(<span class="number">128</span>)</span><br><span class="line">        <span class="comment"># 接收客户端请求</span></span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            client_socket,client_addr = self.server_socket.accept()</span><br><span class="line">            print(<span class="string">"%s:%d connected...\n"</span> %(client_addr[<span class="number">0</span>], client_addr[<span class="number">1</span>]))</span><br><span class="line">            <span class="comment">#self.handle_client(client_socket)</span></span><br><span class="line">            new_thread = Thread(target=self.handle_client, args=(client_socket,))</span><br><span class="line">            new_thread.start()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">bind</span><span class="params">(self, port)</span>:</span></span><br><span class="line">        <span class="string">"""绑定端口"""</span></span><br><span class="line">        address = (<span class="string">""</span>, port)</span><br><span class="line">        self.server_socket.bind(address)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    m = __import__(<span class="string">"MyWebFrameWork"</span>)</span><br><span class="line">    http_server = HTTPServer(m.app)</span><br><span class="line">    http_server.bind(<span class="number">8000</span>)</span><br><span class="line">    http_server.start()</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure><p>其中的<code>m = __import__</code>那里就是导入框架。<br>这样一来，我们不用去改写服务器的代码，响应报文全部交给框架来构建，我们只需要在调用不同的框架的时候改服务器中main函数中的import就行了。</p></li></ul><h2 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h2><blockquote><p>数据是一个抽象的概念，将其进行分类后得到程序设计语言中的基本类型。<br>如：int，float，char等。数据元素之间不是独立的，存在特定的关系，这些关系便是结构。数据结构指数据对象中数据元素之间的关系。<br>Python给我们提供了很多现成的数据结构类型，这些系统自己定义好的，不需要我们自己去定义的数据结构叫做Python的<strong>内置数据结构</strong>，比如列表、元组、字典。<br>而有些数据组织方式，Python系统里面没有直接定义，需要我们自己去定义实现这些数据的组织方式，这些数据组织方式称之为Python的扩展数据结构，比如栈，队列等。<br>这里的数据结构和C语言中的一样，不加赘述。</p></blockquote><h3 id="链表"><a href="#链表" class="headerlink" title="链表"></a>链表</h3><ul><li><p><strong>单链表</strong></p><blockquote><p>单链表也叫单向链表，是链表中最简单的一种形式，它的每个节点包含两个域，一个信息域（元素域）和一个链接域。<br>这个链接指向链表中的下一个节点，而最后一个节点的链接域则指向一个空值。</p></blockquote><p>单链表的操作：</p><ul><li>is_empty() 链表是否为空</li><li>length() 链表长度</li><li>travel() 遍历整个链表</li><li>add(item) 链表头部添加元素</li><li>append(item) 链表尾部添加元素</li><li>insert(pos, item) 指定位置添加元素</li><li>remove(item) 删除节点</li><li>search(item) 查找节点是否存在</li></ul><p>下面以Python代码来实现这个单链表：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Node</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="string">"""节点"""</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, item)</span>:</span></span><br><span class="line">        self.item = item  <span class="comment">#item用于储存数据</span></span><br><span class="line">        self.next = <span class="literal">None</span>  <span class="comment">#next用于指向下一个节点</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">LinkList</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="string">"""单向链表"""</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""初始化链表"""</span></span><br><span class="line">        self.head = Node(<span class="literal">None</span>)</span><br><span class="line">        self.count = <span class="number">0</span> </span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">is_empty</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""判断链表是否为空"""</span></span><br><span class="line">        <span class="keyword">if</span> self.count &gt; <span class="number">0</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">length</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""链表长度"""</span></span><br><span class="line">        <span class="keyword">return</span> self.count</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">travel</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""遍历链表"""</span></span><br><span class="line">        cur_node = self.head.next</span><br><span class="line">        print(<span class="string">"The LinkList is: "</span>, end=<span class="string">''</span>)</span><br><span class="line">        <span class="keyword">while</span> cur_node != <span class="literal">None</span>:</span><br><span class="line">            print(<span class="string">"%s "</span> %cur_node.item, end=<span class="string">''</span>)</span><br><span class="line">            cur_node = cur_node.next</span><br><span class="line">        print()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">add</span><span class="params">(self, item)</span>:</span></span><br><span class="line">        <span class="string">"""从链表头部添加元素"""</span></span><br><span class="line">        new_node = Node(item)</span><br><span class="line">        new_node.next = self.head.next</span><br><span class="line">        self.head.next = new_node</span><br><span class="line">        self.count += <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">append</span><span class="params">(self, item)</span>:</span></span><br><span class="line">        <span class="string">"""从尾部添加元素"""</span></span><br><span class="line">        new_node = Node(item)</span><br><span class="line">        cur_node = self.head.next</span><br><span class="line">        <span class="keyword">while</span> cur_node.next != <span class="literal">None</span>:</span><br><span class="line">            cur_node = cur_node.next</span><br><span class="line">        cur_node.next = new_node</span><br><span class="line">        self.count += <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">insert</span><span class="params">(self, pos, item)</span>:</span></span><br><span class="line">        <span class="string">"""指定位置插入元素"""</span></span><br><span class="line">        cur_node = self.head</span><br><span class="line">        new_node = Node(item)</span><br><span class="line">        i = <span class="number">0</span></span><br><span class="line">        <span class="keyword">while</span> i&lt;pos:</span><br><span class="line">            cur_node = cur_node.next</span><br><span class="line">            i+=<span class="number">1</span></span><br><span class="line">        new_node.next = cur_node.next</span><br><span class="line">        cur_node.next = new_node</span><br><span class="line">        self.count += <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">remove</span><span class="params">(self, item)</span>:</span></span><br><span class="line">        <span class="string">"""删除指定节点"""</span></span><br><span class="line">        cur_node = self.head.next</span><br><span class="line">        <span class="keyword">while</span> cur_node.next.item != item:</span><br><span class="line">            cur_node = cur_node.next</span><br><span class="line">        cur_node.next = cur_node.next.next</span><br><span class="line">        self.count -= <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">search</span><span class="params">(self, item)</span>:</span></span><br><span class="line">        <span class="string">"""查找节点是否存在"""</span></span><br><span class="line">        cur_node = self.head.next</span><br><span class="line">        <span class="keyword">while</span> cur_node != <span class="literal">None</span>:</span><br><span class="line">            <span class="keyword">if</span> cur_node.item == item:</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                cur_node = cur_node.next</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    ll = LinkList()</span><br><span class="line">    ll.add(<span class="number">8</span>)</span><br><span class="line">    ll.add(<span class="number">7</span>)</span><br><span class="line">    ll.add(<span class="number">6</span>)</span><br><span class="line">    ll.add(<span class="number">5</span>)</span><br><span class="line">    ll.add(<span class="number">4</span>)</span><br><span class="line">    ll.add(<span class="number">3</span>)</span><br><span class="line">    ll.add(<span class="number">2</span>)</span><br><span class="line">    ll.add(<span class="number">1</span>)</span><br><span class="line">    ll.append(<span class="number">9</span>)</span><br><span class="line">    ll.travel()</span><br><span class="line">    print(<span class="string">"LinkList's length is %s"</span>%ll.length())</span><br><span class="line">    ll.remove(<span class="number">5</span>)</span><br><span class="line">    ll.travel()</span><br><span class="line">    print(<span class="string">"LinkList's length is %s"</span>%ll.length())</span><br><span class="line">    print(ll.search(<span class="number">1</span>))</span><br><span class="line">    print(ll.search(<span class="number">5</span>))</span><br><span class="line">    ll.insert(<span class="number">4</span>, <span class="number">5</span>)</span><br><span class="line">    ll.travel()</span><br><span class="line">    print(<span class="string">"LinkList's length is %s"</span>%ll.length())</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">The LinkList is: 1 2 3 4 5 6 7 8 9 </span><br><span class="line">LinkList&#39;s length is 9</span><br><span class="line">The LinkList is: 1 2 3 4 6 7 8 9 </span><br><span class="line">LinkList&#39;s length is 8</span><br><span class="line">True</span><br><span class="line">False</span><br><span class="line">The LinkList is: 1 2 3 4 5 6 7 8 9 </span><br><span class="line">LinkList&#39;s length is 9</span><br></pre></td></tr></table></figure></li><li><p><strong>单向循环链表</strong></p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Node</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="string">"""节点"""</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, item)</span>:</span></span><br><span class="line">        self.item = item  <span class="comment">#item用于储存数据</span></span><br><span class="line">        self.next = <span class="literal">None</span>  <span class="comment">#next用于指向下一个节点</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">CycLinkList</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="string">"""单向循环链表"""</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""初始化链表"""</span></span><br><span class="line">        self.head = Node(<span class="literal">None</span>)</span><br><span class="line">        <span class="comment"># 循环链接</span></span><br><span class="line">        self.head.next = self.head</span><br><span class="line">        self.count = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">is_empty</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""判断链表是否为空"""</span></span><br><span class="line">        <span class="keyword">if</span> self.count &gt; <span class="number">0</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">length</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""链表长度"""</span></span><br><span class="line">        <span class="keyword">return</span> self.count</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">travel</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""遍历链表"""</span></span><br><span class="line">        cur_node = self.head.next</span><br><span class="line">        print(<span class="string">"The LinkList is: "</span>, end=<span class="string">''</span>)</span><br><span class="line">        <span class="keyword">while</span> cur_node != self.head:</span><br><span class="line">            print(<span class="string">"%s "</span> %cur_node.item, end=<span class="string">''</span>)</span><br><span class="line">            cur_node = cur_node.next</span><br><span class="line">        print()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">add</span><span class="params">(self, item)</span>:</span></span><br><span class="line">        <span class="string">"""从链表头部添加元素"""</span></span><br><span class="line">        new_node = Node(item)</span><br><span class="line">        new_node.next = self.head.next</span><br><span class="line">        self.head.next = new_node</span><br><span class="line">        self.count += <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">append</span><span class="params">(self, item)</span>:</span></span><br><span class="line">        <span class="string">"""从尾部添加元素"""</span></span><br><span class="line">        new_node = Node(item)</span><br><span class="line">        new_node.next = self.head</span><br><span class="line">        cur_node = self.head.next</span><br><span class="line">        <span class="keyword">while</span> cur_node.next != self.head:</span><br><span class="line">            cur_node = cur_node.next</span><br><span class="line">        cur_node.next = new_node</span><br><span class="line">        self.count += <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">insert</span><span class="params">(self, pos, item)</span>:</span></span><br><span class="line">        <span class="string">"""指定位置插入元素"""</span></span><br><span class="line">        cur_node = self.head</span><br><span class="line">        new_node = Node(item)</span><br><span class="line">        i = <span class="number">0</span></span><br><span class="line">        <span class="keyword">while</span> i&lt;pos:</span><br><span class="line">            cur_node = cur_node.next</span><br><span class="line">            i+=<span class="number">1</span></span><br><span class="line">        new_node.next = cur_node.next</span><br><span class="line">        cur_node.next = new_node</span><br><span class="line">        self.count += <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">remove</span><span class="params">(self, item)</span>:</span></span><br><span class="line">        <span class="string">"""删除指定节点"""</span></span><br><span class="line">        cur_node = self.head.next</span><br><span class="line">        <span class="keyword">while</span> cur_node.next.item != item:</span><br><span class="line">            cur_node = cur_node.next</span><br><span class="line">        cur_node.next = cur_node.next.next</span><br><span class="line">        self.count -= <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">search</span><span class="params">(self, item)</span>:</span></span><br><span class="line">        <span class="string">"""查找节点是否存在"""</span></span><br><span class="line">        cur_node = self.head.next</span><br><span class="line">        <span class="keyword">while</span> cur_node != self.head:</span><br><span class="line">            <span class="keyword">if</span> cur_node.item == item:</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                cur_node = cur_node.next</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    ll = CycLinkList()</span><br><span class="line">    ll.add(<span class="number">8</span>)</span><br><span class="line">    ll.add(<span class="number">7</span>)</span><br><span class="line">    ll.add(<span class="number">6</span>)</span><br><span class="line">    ll.add(<span class="number">5</span>)</span><br><span class="line">    ll.add(<span class="number">4</span>)</span><br><span class="line">    ll.add(<span class="number">3</span>)</span><br><span class="line">    ll.add(<span class="number">2</span>)</span><br><span class="line">    ll.add(<span class="number">1</span>)</span><br><span class="line">    ll.append(<span class="number">9</span>)</span><br><span class="line">    ll.travel()</span><br><span class="line">    print(<span class="string">"LinkList's length is %s"</span>%ll.length())</span><br><span class="line">    ll.remove(<span class="number">5</span>)</span><br><span class="line">    ll.travel()</span><br><span class="line">    print(<span class="string">"LinkList's length is %s"</span>%ll.length())</span><br><span class="line">    print(ll.search(<span class="number">1</span>))</span><br><span class="line">    print(ll.search(<span class="number">5</span>))</span><br><span class="line">    ll.insert(<span class="number">4</span>, <span class="number">5</span>)</span><br><span class="line">    ll.travel()</span><br><span class="line">    print(<span class="string">"LinkList's length is %s"</span>%ll.length())</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure><p>执行结果和上面一样</p></li><li><p><strong>双向链表</strong></p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Node</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="string">"""双向节点"""</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, item)</span>:</span></span><br><span class="line">        self.item = item  <span class="comment">#item用于储存数据</span></span><br><span class="line">        self.next = <span class="literal">None</span>  <span class="comment">#next用于指向下一个节点</span></span><br><span class="line">        self.prev = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DLinkList</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="string">"""双向链表"""</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""初始化链表"""</span></span><br><span class="line">        self.head = Node(<span class="literal">None</span>)</span><br><span class="line">        self.count = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">is_empty</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""判断链表是否为空"""</span></span><br><span class="line">        <span class="keyword">if</span> self.count &gt; <span class="number">0</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">length</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""链表长度"""</span></span><br><span class="line">        <span class="keyword">return</span> self.count</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">travel</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""遍历链表"""</span></span><br><span class="line">        cur_node = self.head.next</span><br><span class="line">        print(<span class="string">"The LinkList is: "</span>, end=<span class="string">''</span>)</span><br><span class="line">        <span class="keyword">while</span> cur_node != <span class="literal">None</span>:</span><br><span class="line">            print(<span class="string">"%s "</span> %cur_node.item, end=<span class="string">''</span>)</span><br><span class="line">            cur_node = cur_node.next</span><br><span class="line">        print()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">add</span><span class="params">(self, item)</span>:</span></span><br><span class="line">        <span class="string">"""从链表头部添加元素"""</span></span><br><span class="line">        new_node = Node(item)</span><br><span class="line">        new_node.prev = self.head</span><br><span class="line">        new_node.next = self.head.next</span><br><span class="line">        <span class="keyword">if</span> self.count &gt; <span class="number">0</span>:</span><br><span class="line">            self.head.next.prev = new_node</span><br><span class="line"></span><br><span class="line">        self.head.next = new_node</span><br><span class="line">        self.count += <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">append</span><span class="params">(self, item)</span>:</span></span><br><span class="line">        <span class="string">"""从尾部添加元素"""</span></span><br><span class="line">        new_node = Node(item)</span><br><span class="line">        cur_node = self.head.next</span><br><span class="line">        <span class="keyword">while</span> cur_node.next != <span class="literal">None</span>:</span><br><span class="line">            cur_node = cur_node.next</span><br><span class="line">        new_node.prev = cur_node</span><br><span class="line">        cur_node.next = new_node</span><br><span class="line">        self.count += <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">insert</span><span class="params">(self, pos, item)</span>:</span></span><br><span class="line">        <span class="string">"""指定位置插入元素"""</span></span><br><span class="line">        cur_node = self.head</span><br><span class="line">        new_node = Node(item)</span><br><span class="line">        i = <span class="number">0</span></span><br><span class="line">        <span class="keyword">while</span> i&lt;pos:</span><br><span class="line">            cur_node = cur_node.next</span><br><span class="line">            i+=<span class="number">1</span></span><br><span class="line">        new_node.next = cur_node.next</span><br><span class="line">        new_node.prev = cur_node</span><br><span class="line">        cur_node.next.prev = new_node</span><br><span class="line">        cur_node.next = new_node</span><br><span class="line">        self.count += <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">remove</span><span class="params">(self, item)</span>:</span></span><br><span class="line">        <span class="string">"""删除指定节点"""</span></span><br><span class="line">        cur_node = self.head.next</span><br><span class="line">        <span class="keyword">while</span> cur_node.next.item != item:</span><br><span class="line">            cur_node = cur_node.next</span><br><span class="line">        cur_node.next.next.prev = cur_node</span><br><span class="line">        cur_node.next = cur_node.next.next</span><br><span class="line">        self.count -= <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">search</span><span class="params">(self, item)</span>:</span></span><br><span class="line">        <span class="string">"""查找节点是否存在"""</span></span><br><span class="line">        cur_node = self.head.next</span><br><span class="line">        <span class="keyword">while</span> cur_node != <span class="literal">None</span>:</span><br><span class="line">            <span class="keyword">if</span> cur_node.item == item:</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                cur_node = cur_node.next</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    ll = DLinkList()</span><br><span class="line">    ll.add(<span class="number">8</span>)</span><br><span class="line">    ll.add(<span class="number">7</span>)</span><br><span class="line">    ll.add(<span class="number">6</span>)</span><br><span class="line">    ll.add(<span class="number">5</span>)</span><br><span class="line">    ll.add(<span class="number">4</span>)</span><br><span class="line">    ll.add(<span class="number">3</span>)</span><br><span class="line">    ll.add(<span class="number">2</span>)</span><br><span class="line">    ll.add(<span class="number">1</span>)</span><br><span class="line">    ll.append(<span class="number">9</span>)</span><br><span class="line">    ll.travel()</span><br><span class="line">    print(<span class="string">"LinkList's length is %s"</span>%ll.length())</span><br><span class="line">    ll.remove(<span class="number">5</span>)</span><br><span class="line">    ll.travel()</span><br><span class="line">    print(<span class="string">"LinkList's length is %s"</span>%ll.length())</span><br><span class="line">    print(ll.search(<span class="number">1</span>))</span><br><span class="line">    print(ll.search(<span class="number">5</span>))</span><br><span class="line">    ll.insert(<span class="number">4</span>, <span class="number">5</span>)</span><br><span class="line">    ll.travel()</span><br><span class="line">    print(<span class="string">"LinkList's length is %s"</span>%ll.length())</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure><p>执行结果和上面一样</p></li></ul><h3 id="栈"><a href="#栈" class="headerlink" title="栈"></a>栈</h3><blockquote><p>栈(stack)，有些地方称为堆栈，是一种容器，可存入数据元素、访问元素、删除元素<br>它的特点在于只能允许在容器的一端(称为栈顶端)进行加入数据和输出数据的运算。<br>它没有位置概念，保证任何时候可以访问、删除的元素都是此前最后存入的那个元素，确定了一种默认的访问顺序。<br>由于栈数据结构只允许在一端进行操作，因而按照后进先出(LIFO, Last In First Out)的原理运作。</p></blockquote><p>我们以顺序表来构建一个栈，其功能如下：</p><ul><li>Stack() 创建一个新的空栈</li><li>push(item) 添加一个新的元素item到栈顶</li><li>pop() 弹出栈顶元素</li><li>peek() 返回栈顶元素</li><li>is_empty() 判断栈是否为空</li><li>size() 返回栈的元素个数</li></ul><p>代码如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyStack</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="string">"""顺序栈"""</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.items = []</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">push</span><span class="params">(self, item)</span>:</span></span><br><span class="line">        <span class="string">"""放入元素到栈中"""</span></span><br><span class="line">        self.items.append(item)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">pop</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""弹出栈顶元素"""</span></span><br><span class="line">        <span class="keyword">return</span> self.items.pop()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">peek</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""返回栈顶元素"""</span></span><br><span class="line">        <span class="keyword">return</span> self.items[self.size()<span class="number">-1</span>]</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">is_empty</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""判断栈是否为空"""</span></span><br><span class="line">        <span class="keyword">if</span> self.size() == <span class="number">0</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">size</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""返回栈元素个数"""</span></span><br><span class="line">        <span class="keyword">return</span> len(self.items)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    stack = MyStack()</span><br><span class="line">    stack.push(<span class="string">"hello"</span>)</span><br><span class="line">    stack.push(<span class="string">"world"</span>)</span><br><span class="line">    stack.push(<span class="string">"itcast"</span>)</span><br><span class="line">    print(stack.size())</span><br><span class="line">    print(stack.peek())</span><br><span class="line">    print(stack.pop())</span><br><span class="line">    print(stack.pop())</span><br><span class="line">    print(stack.pop())</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">3</span><br><span class="line">itcast</span><br><span class="line">itcast</span><br><span class="line">world</span><br><span class="line">hello</span><br></pre></td></tr></table></figure><h3 id="队列"><a href="#队列" class="headerlink" title="队列"></a>队列</h3><blockquote><p>队列(queue)是只允许在一端进行插入操作，而在另一端进行删除操作的线性表。<br>队列是一种先进先出的(First In First Out)的线性表，简称FIFO。<br>允许插入的一端为队尾，允许删除的一端为队头。<br><strong>队列不允许在中间部位进行操作</strong><br>假设队列是q=（a1，a2，……，an），那么a1就是队头元素，而an是队尾元素。<br>这样我们就可以删除时，总是从a1开始，而插入时，总是在队列最后。这也比较符合我们通常生活中的习惯，排在第一个的优先出列，最后来的当然排在队伍最后。</p></blockquote><p>我们用顺序表来实现队列，其功能如下：</p><ul><li>Queue() 创建一个空的队列</li><li>enqueue(item) 往队列中添加一个item元素</li><li>dequeue() 从队列头部删除一个元素</li><li>is_empty() 判断一个队列是否为空</li><li>size() 返回队列的大小</li></ul><p>代码如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyQueue</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="string">"""队列"""</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.items = []</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">enqueue</span><span class="params">(self, item)</span>:</span></span><br><span class="line">        <span class="string">"""往队列中添加元素"""</span></span><br><span class="line">        self.items.append(item)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">dequeue</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""删除元素"""</span></span><br><span class="line">        ret = self.items[<span class="number">0</span>]</span><br><span class="line">        <span class="keyword">del</span> self.items[<span class="number">0</span>]</span><br><span class="line">        <span class="keyword">return</span> ret </span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">is_empty</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""判断队列是否为空"""</span></span><br><span class="line">        <span class="keyword">if</span> self.size() == <span class="number">0</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">size</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="string">"""返回队列中元素个数"""</span></span><br><span class="line">        <span class="keyword">return</span> len(self.items)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    q = MyQueue()</span><br><span class="line">    q.enqueue(<span class="string">"hello"</span>)</span><br><span class="line">    q.enqueue(<span class="string">"world"</span>)</span><br><span class="line">    q.enqueue(<span class="string">"itcast"</span>)</span><br><span class="line">    print(q.size())</span><br><span class="line">    print(q.dequeue())</span><br><span class="line">    print(q.dequeue())</span><br><span class="line">    print(q.dequeue())</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">3</span><br><span class="line">hello</span><br><span class="line">world</span><br><span class="line">itcast</span><br></pre></td></tr></table></figure><h2 id="排序"><a href="#排序" class="headerlink" title="排序"></a>排序</h2><blockquote><p>排序算法(Sorting algorithm)是一种能将一串数据依照特定顺序进行排列的一种算法。<br>稳定性：稳定排序算法会让原本有相等键值的纪录维持相对次序。<br>也就是如果一个排序算法是稳定的，当有两个相等键值的纪录R和S，且在原本的列表中R出现在S之前，在排序过的列表中R也将会是在S之前。<br>不稳定排序算法可能会在相等的键值中改变纪录的相对次序，但是稳定排序算法从来不会如此。<br>不稳定排序算法可以被特别地实现为稳定。<br>作这件事情的一个方式是人工扩充键值的比较，如此在其他方面相同键值的两个对象间之比较，(比如上面的比较中加入第二个标准：第二个键值的大小)就会被决定使用在原先数据次序中的条目，当作一个同分决赛。然而，要记住这种次序通常牵涉到额外的空间负担。</p></blockquote><h3 id="冒泡排序"><a href="#冒泡排序" class="headerlink" title="冒泡排序"></a>冒泡排序</h3><blockquote><p>冒泡排序(Bubble Sort)是一种简单的排序算法。<br>它重复地遍历要排序的数列，一次比较两个元素，如果他们的顺序错误就把他们交换过来。<br>遍历数列的工作是重复地进行直到没有再需要交换，也就是说该数列已经排序完成。<br>这个算法的名字由来是因为越小的元素会经由交换慢慢”浮”到数列的顶端。</p></blockquote><p>冒泡排序算法的运作如下：</p><ol><li>比较相邻的元素。如果第一个比第二个大（升序），就交换他们两个。</li><li>对每一对相邻元素作同样的工作，从开始第一对到结尾的最后一对。这步做完后，最后的元素会是最大的数。</li><li>针对所有的元素重复以上的步骤，除了最后一个。</li><li>持续每次对越来越少的元素重复上面的步骤，直到没有任何一对数字需要比较。</li></ol><p>代码如下(升序)：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">bubble_sork</span><span class="params">(array)</span>:</span></span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> range(len(array)<span class="number">-1</span>, <span class="number">0</span>, <span class="number">-1</span>):</span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> range(j):</span><br><span class="line">            <span class="keyword">if</span> array[i] &gt; array[i+<span class="number">1</span>]:</span><br><span class="line">                array[i],array[i+<span class="number">1</span>] = array[i+<span class="number">1</span>],array[i]</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    array = [<span class="number">54</span>,<span class="number">26</span>,<span class="number">93</span>,<span class="number">17</span>,<span class="number">77</span>,<span class="number">31</span>,<span class="number">44</span>,<span class="number">55</span>,<span class="number">20</span>]</span><br><span class="line">    print(<span class="string">"Before sort: %s"</span>%array)</span><br><span class="line">    bubble_sork(array)</span><br><span class="line">    print(<span class="string">"After sort: %s"</span>%array)</span><br></pre></td></tr></table></figure><p>执行结果如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Before sort: [54, 26, 93, 17, 77, 31, 44, 55, 20]</span><br><span class="line">After sort: [17, 20, 26, 31, 44, 54, 55, 77, 93]</span><br></pre></td></tr></table></figure><h3 id="选择排序"><a href="#选择排序" class="headerlink" title="选择排序"></a>选择排序</h3><blockquote><p>选择排序(Selection sort)是一种简单直观的排序算法。<br>它的工作原理如下:<br>首先在未排序序列中找到最小(大)元素，存放到排序序列的起始位置<br>然后，再从剩余未排序元素中继续寻找最小(大)元素，然后放到已排序序列的末尾。<br>以此类推，直到所有元素均排序完毕。</p><p>选择排序的主要优点与数据移动有关。<br>如果某个元素位于正确的最终位置上，则它不会被移动。<br>选择排序每次交换一对元素，它们当中至少有一个将被移到其最终位置上，因此对n个元素的表进行排序总共进行至多n-1次交换。<br>在所有的完全依靠交换去移动元素的排序方法中，选择排序属于非常好的一种。</p></blockquote><p>代码如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">select_sort</span><span class="params">(array)</span>:</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(len(array), <span class="number">0</span>, <span class="number">-1</span>):</span><br><span class="line">        max_index = <span class="number">0</span> </span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> range(i):</span><br><span class="line">            <span class="keyword">if</span> array[max_index] &lt; array[j]:</span><br><span class="line">                max_index = j </span><br><span class="line">        array[max_index],array[j] = array[j],array[max_index]</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    array = [<span class="number">54</span>,<span class="number">26</span>,<span class="number">93</span>,<span class="number">17</span>,<span class="number">77</span>,<span class="number">31</span>,<span class="number">44</span>,<span class="number">55</span>,<span class="number">20</span>]</span><br><span class="line">    print(<span class="string">"Before sort: %s"</span>%array)</span><br><span class="line">    select_sort(array)</span><br><span class="line">    print(<span class="string">"After sort: %s"</span>%array)</span><br></pre></td></tr></table></figure><p>执行结果和上面一样</p><h3 id="插入排序"><a href="#插入排序" class="headerlink" title="插入排序"></a>插入排序</h3><blockquote><p>插入排序(Insertion Sort)是一种简单直观的排序算法。<br>它的工作原理是通过构建有序序列，对于未排序数据，在已排序序列中从后向前扫描，找到相应位置并插入。<br>插入排序在实现上，在从后向前扫描过程中，需要反复把已排序元素逐步向后挪位，为最新元素提供插入空间。</p></blockquote><p>代码如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">insert_sort</span><span class="params">(array)</span>:</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, len(array)):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> range(i, <span class="number">0</span>, <span class="number">-1</span>):</span><br><span class="line">            <span class="keyword">if</span> array[j] &lt; array[j<span class="number">-1</span>]:</span><br><span class="line">                array[j],array[j<span class="number">-1</span>] = array[j<span class="number">-1</span>],array[j]</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    array = [<span class="number">54</span>,<span class="number">26</span>,<span class="number">93</span>,<span class="number">17</span>,<span class="number">77</span>,<span class="number">31</span>,<span class="number">44</span>,<span class="number">55</span>,<span class="number">20</span>]</span><br><span class="line">    print(<span class="string">"Before sort: %s"</span>%array)</span><br><span class="line">    insert_sort(array)</span><br><span class="line">    print(<span class="string">"After sort: %s"</span>%array)</span><br></pre></td></tr></table></figure><p>执行结果和上面一样</p><h3 id="希尔排序"><a href="#希尔排序" class="headerlink" title="希尔排序"></a>希尔排序</h3><blockquote><p>希尔排序(Shell Sort)是插入排序的一种。<br>也称缩小增量排序，是直接插入排序算法的一种更高效的改进版本。<br>希尔排序是非稳定排序算法。该方法因DL．Shell于1959年提出而得名。<br>希尔排序是把记录按下标的一定增量分组，对每组使用直接插入排序算法排序；随着增量逐渐减少，每组包含的关键词越来越多，当增量减至1时，整个文件恰被分成一组，算法便终止。</p><p>希尔排序的基本思想是：将数组列在一个表中并对列分别进行插入排序，重复这过程，不过每次用更长的列(步长更长了，列数更少了)来进行。<br>最后整个表就只有一列了。将数组转换至表是为了更好地理解这算法，算法本身还是使用数组进行排序。</p></blockquote><p>代码如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">shell_sort</span><span class="params">(array)</span>:</span></span><br><span class="line">    n = len(array)</span><br><span class="line">    gap = n // <span class="number">2</span></span><br><span class="line">    <span class="keyword">while</span> gap &gt; <span class="number">0</span>:</span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> range(gap, n): </span><br><span class="line">            <span class="comment"># 插入排序</span></span><br><span class="line">            j = i </span><br><span class="line">            <span class="keyword">while</span> j&gt;=gap:</span><br><span class="line">                <span class="keyword">if</span> array[j-gap] &gt; array[j]:</span><br><span class="line">                    array[j-gap],array[j] = array[j],array[j-gap]</span><br><span class="line">                j -= gap </span><br><span class="line">        gap = gap // <span class="number">2</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    array = [<span class="number">54</span>,<span class="number">26</span>,<span class="number">93</span>,<span class="number">17</span>,<span class="number">77</span>,<span class="number">31</span>,<span class="number">44</span>,<span class="number">55</span>,<span class="number">20</span>]</span><br><span class="line">    print(<span class="string">"Before sort: %s"</span>%array)</span><br><span class="line">    shell_sort(array)</span><br><span class="line">    print(<span class="string">"After sort: %s"</span>%array)</span><br></pre></td></tr></table></figure><p>执行结果和上面相同</p><h3 id="快速排序"><a href="#快速排序" class="headerlink" title="快速排序"></a>快速排序</h3><blockquote><p>快速排序(Quicksort)，又称划分交换排序(partition-exchange sort)<br>通过一趟排序将要排序的数据分割成独立的两部分，其中一部分的所有数据都比另外一部分的所有数据都要小，然后再按此方法对这两部分数据分别进行快速排序，整个排序过程可以递归进行，以此达到整个数据变成有序序列。</p></blockquote><p>步骤为：</p><ol><li>从数列中挑出一个元素，称为”基准”(pivot)</li><li>重新排序数列，所有元素比基准值小的摆放在基准前面，所有元素比基准值大的摆在基准的后面(相同的数可以到任一边)。在这个分区结束之后，该基准就处于数列的中间位置。这个称为分区(partition)操作。</li><li>递归地(recursive)把小于基准值元素的子数列和大于基准值元素的子数列排序。</li></ol><p>递归的最底部情形，是数列的大小是零或一，也就是永远都已经被排序好了。<br>虽然一直递归下去，但是这个算法总会结束，因为在每次的迭代(iteration)中，它至少会把一个元素摆到它最后的位置去。</p><p>代码如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">quick_sort</span><span class="params">(array, start, end)</span>:</span></span><br><span class="line">    <span class="comment"># 退出递归</span></span><br><span class="line">    <span class="keyword">if</span> start &gt;= end:</span><br><span class="line">        <span class="keyword">return</span> array[start]</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 设置基准值</span></span><br><span class="line">    key = array[start]</span><br><span class="line">    low = start</span><br><span class="line">    high = end </span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> low &lt; high:</span><br><span class="line">        <span class="keyword">while</span> low &lt; high <span class="keyword">and</span> array[high]&gt;key:</span><br><span class="line">            high -= <span class="number">1</span></span><br><span class="line">        array[low] = array[high]</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span> low &lt; high <span class="keyword">and</span> array[low]&lt;key:</span><br><span class="line">            low += <span class="number">1</span></span><br><span class="line">        array[high] = array[low]</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 跳出循环后将基准值放在low和high相等的地方</span></span><br><span class="line">    array[low] = key </span><br><span class="line"></span><br><span class="line">    <span class="comment"># 对左字序列进行排序</span></span><br><span class="line">    quick_sort(array, start, low<span class="number">-1</span>)</span><br><span class="line">    <span class="comment"># 对右子序列进行排序</span></span><br><span class="line">    quick_sort(array, low+<span class="number">1</span>, end)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    array = [<span class="number">54</span>,<span class="number">26</span>,<span class="number">93</span>,<span class="number">17</span>,<span class="number">77</span>,<span class="number">31</span>,<span class="number">44</span>,<span class="number">55</span>,<span class="number">20</span>]</span><br><span class="line">    print(<span class="string">"Before sort: %s"</span>%array)</span><br><span class="line">    quick_sort(array,<span class="number">0</span>,len(array)<span class="number">-1</span>)</span><br><span class="line">    print(<span class="string">"After sort: %s"</span>%array)</span><br></pre></td></tr></table></figure><p>执行结果和上面相同。</p><h3 id="归并排序"><a href="#归并排序" class="headerlink" title="归并排序"></a>归并排序</h3><blockquote><p>归并排序是采用分治法的一个非常典型的应用。<br>归并排序的思想就是先递归分解数组，再合并数组。<br>将数组分解最小之后，然后合并两个有序数组，基本思路是比较两个数组的最前面的数，谁小就先取谁，取了后相应的指针就往后移一位。然后再比较，直至一个数组为空，最后把另一个数组的剩余部分复制过来即可。</p></blockquote><p>代码如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">merge_sort</span><span class="params">(array)</span>:</span></span><br><span class="line">    <span class="comment"># 结束递归条件</span></span><br><span class="line">    <span class="keyword">if</span> len(array) &lt;= <span class="number">1</span>:</span><br><span class="line">        <span class="keyword">return</span> array</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 二分分解</span></span><br><span class="line">    num = len(array) // <span class="number">2</span></span><br><span class="line"></span><br><span class="line">    left = merge_sort(array[:num])</span><br><span class="line">    right = merge_sort(array[num:])</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 合并</span></span><br><span class="line">    <span class="keyword">return</span> merge(left, right)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">merge</span><span class="params">(left, right)</span>:</span></span><br><span class="line">    <span class="string">"""合并操作，将两个有序数组合并为一个大的有序数组"""</span></span><br><span class="line">    l_index,r_index = <span class="number">0</span>,<span class="number">0</span> </span><br><span class="line">    result = []</span><br><span class="line">    <span class="keyword">while</span> l_index&lt;len(left) <span class="keyword">and</span> r_index&lt;len(right):</span><br><span class="line">        <span class="keyword">if</span> left[l_index] &lt; right[r_index]:</span><br><span class="line">            result.append(left[l_index])</span><br><span class="line">            l_index += <span class="number">1</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            result.append(right[r_index])</span><br><span class="line">            r_index += <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    result += left[l_index:]</span><br><span class="line">    result += right[r_index:]</span><br><span class="line">    <span class="keyword">return</span> result</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    array = [<span class="number">54</span>,<span class="number">26</span>,<span class="number">93</span>,<span class="number">17</span>,<span class="number">77</span>,<span class="number">31</span>,<span class="number">44</span>,<span class="number">55</span>,<span class="number">20</span>]</span><br><span class="line">    print(<span class="string">"Before sort: %s"</span>%array)</span><br><span class="line">    array = merge_sort(array)</span><br><span class="line">    print(<span class="string">"After sort: %s"</span>%array)</span><br></pre></td></tr></table></figure><p>执行结果和上面相同。</p>]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>Ubuntu 常用设置</title>
    <url>/ubuntu/3bd23daf-2dec-409d-91bd-921eb3c755eb.html</url>
    <content><![CDATA[<p>本文介绍 Ubuntu 的一些常用设置。</p>
<a id="more"></a>

<h2 id="更换-Ali-源"><a href="#更换-Ali-源" class="headerlink" title="更换 Ali 源"></a>更换 Ali 源</h2><p>编辑文件 <code>/etc/apt/sources.list</code>，注意对应的版本号，例如 ubuntu 20 为 <code>focal</code></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">deb http:&#x2F;&#x2F;mirrors.aliyun.com&#x2F;ubuntu&#x2F; focal main restricted universe multiverse</span><br><span class="line">deb-src http:&#x2F;&#x2F;mirrors.aliyun.com&#x2F;ubuntu&#x2F; focal main restricted universe multiverse</span><br><span class="line">deb http:&#x2F;&#x2F;mirrors.aliyun.com&#x2F;ubuntu&#x2F; focal-security main restricted universe multiverse</span><br><span class="line">deb-src http:&#x2F;&#x2F;mirrors.aliyun.com&#x2F;ubuntu&#x2F; focal-security main restricted universe multiverse</span><br><span class="line">deb http:&#x2F;&#x2F;mirrors.aliyun.com&#x2F;ubuntu&#x2F; focal-updates main restricted universe multiverse</span><br><span class="line">deb-src http:&#x2F;&#x2F;mirrors.aliyun.com&#x2F;ubuntu&#x2F; focal-updates main restricted universe multiverse</span><br><span class="line">deb http:&#x2F;&#x2F;mirrors.aliyun.com&#x2F;ubuntu&#x2F; focal-proposed main restricted universe multiverse</span><br><span class="line">deb-src http:&#x2F;&#x2F;mirrors.aliyun.com&#x2F;ubuntu&#x2F; focal-proposed main restricted universe multiverse</span><br><span class="line">deb http:&#x2F;&#x2F;mirrors.aliyun.com&#x2F;ubuntu&#x2F; focal-backports main restricted universe multiverse</span><br><span class="line">deb-src http:&#x2F;&#x2F;mirrors.aliyun.com&#x2F;ubuntu&#x2F; focal-backports main restricted universe multiverse</span><br></pre></td></tr></table></figure>

<br/>

<h2 id="Ubuntu-取消开机动画"><a href="#Ubuntu-取消开机动画" class="headerlink" title="Ubuntu 取消开机动画"></a>Ubuntu 取消开机动画</h2><p>修改文件：<code>/etc/default/grub</code></p>
<p>找到下面这句： <code>GRUB_CMDLINE_LINUX_DEFAULT=&quot;quiet splash&quot;</code></p>
<p>把 <code>quiet splash</code> 去掉或者改成 <code>text</code> 即可</p>
<p>另：<code>GRUB_TIMEOUT</code> 为引导时间，改成 0 可跳过引导界面</p>
<p>然后update开机方式：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo update-grub</span><br></pre></td></tr></table></figure>

<br/>

<h2 id="关闭图形桌面"><a href="#关闭图形桌面" class="headerlink" title="关闭图形桌面"></a>关闭图形桌面</h2><p>关闭用户图形界面，使用tty登录。</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">sudo systemctl <span class="built_in">set</span>-default multi-user.target</span><br><span class="line"><span class="comment"># 可用 init 5 来开启图形化界面</span></span><br></pre></td></tr></table></figure>
<p>如想开启用户图形界面：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo systemctl set-default graphical.target</span><br></pre></td></tr></table></figure>
<p>重启后生效：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo reboot</span><br></pre></td></tr></table></figure>

<br/>


<h2 id="tty-设置英文"><a href="#tty-设置英文" class="headerlink" title="tty 设置英文"></a>tty 设置英文</h2><p>如果关闭了图形界面，在 tty 登录以后可能会发现中文字符变成乱码，这时可以让 tty 自动设置为英文。在 <code>~/.bashrc</code> 中添加如下内容：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> [ <span class="string">"<span class="variable">$TERM</span>"</span> = <span class="string">"linux"</span> ]; <span class="keyword">then</span> </span><br><span class="line">    <span class="built_in">export</span> LANG=C</span><br><span class="line"><span class="keyword">fi</span></span><br></pre></td></tr></table></figure>

<br/>

<h2 id="Ubuntu-时区修改"><a href="#Ubuntu-时区修改" class="headerlink" title="Ubuntu 时区修改"></a>Ubuntu 时区修改</h2><p>Ubuntu 18 以上：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">timedatectl set-timezone Asia&#x2F;Shanghai</span><br></pre></td></tr></table></figure>
<p>Ubuntu 18 以下：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">tzselect</span><br><span class="line">cp /usr/share/zoneinfo/Asia/Shanghai /etc/localtime</span><br></pre></td></tr></table></figure>

<p>之后执行 <code>systemctl networking restart</code> 使其生效</p>
<br/>

<h2 id="Ubuntu-设置静态-ip"><a href="#Ubuntu-设置静态-ip" class="headerlink" title="Ubuntu 设置静态 ip"></a>Ubuntu 设置静态 ip</h2><p><strong>注意</strong>：这里配置的网关可用 <code>route -n</code> 命令来查看</p>
<h3 id="20-版本"><a href="#20-版本" class="headerlink" title="20 版本"></a>20 版本</h3><p>编辑 <code>/etc/netplan/**.yaml</code> 文件，如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">network:</span><br><span class="line">  ethernets:</span><br><span class="line">    ens32:</span><br><span class="line">      dhcp4: false</span><br><span class="line">      addresses: [192.168.100.128&#x2F;24]</span><br><span class="line">      gateway4: 192.168.100.254</span><br><span class="line">      nameservers:</span><br><span class="line">        addresses: [192.168.100.254, 8.8.8.8]</span><br><span class="line">  version: 2</span><br></pre></td></tr></table></figure>
<p>然后使用 <code>netplan apply</code> 命令使其生效</p>
<h3 id="20-以下版本"><a href="#20-以下版本" class="headerlink" title="20 以下版本"></a>20 以下版本</h3><p>编辑 <code>/etc/network/interfaces</code> 文件，追加如下内容：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">auto ens33</span><br><span class="line">iface ens33 inet static</span><br><span class="line">address 192.168.100.128</span><br><span class="line">netmask 255.255.255.0</span><br><span class="line">gateway 192.168.100.254</span><br></pre></td></tr></table></figure>

<p>修改完之后，使用 <code>service networking restart</code> 重启网络。</p>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>Ubuntu</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux SSH</title>
    <url>/linux/ac6a581b-3788-4820-89c0-c55fff84aa06.html</url>
    <content><![CDATA[<p>本文介绍 Linux SSH 相关操作。</p>
<a id="more"></a>
<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>ssh: Secure SHell，22/tcp</p>
<p>其特点是：</p>
<ul>
<li>应用层协议</li>
<li>通信过程及认证过程是加密的，主机认证</li>
<li>用户认证过程加密</li>
<li>数据传输过程加密</li>
</ul>
<p>目前 ssh 有 v1 和 v2 两个版本，但是 v1 版本避免不了中间人攻击，因此 v1 版本也不是那么的安全</p>
<p>ssh 认证过程分为 2 种：基于口令认证（输密码），基于密钥认证（不用输密码，服务器上的私钥对上客户端的私钥即可完成解密）</p>
<p>Linux 上使用 ssh 的软件通常是 openSSH，也是一种 C/S 架构的软件</p>
<p>服务器端：sshd, 配置文件 <code>/etc/ssh/sshd_config</code></p>
<ul>
<li>文件中以#号开头的行后面跟上空格的是纯注释，以#号开头后面直接跟文字的是可修改的默认参数，注意：设定参数时前面不能有任何空格</li>
<li>常用设置：</li>
<li>Port：默认端口</li>
<li>ListenAddress：监听端口</li>
<li>Protocol：协议版本</li>
<li>AllowUsers：限制可登陆用户，后面可跟多个用户</li>
</ul>
<br/>

<h2 id="ssh命令使用"><a href="#ssh命令使用" class="headerlink" title="ssh命令使用"></a>ssh命令使用</h2><p>ssh命令使用方法：</p>
<ul>
<li><code>ssh USERNAME@HOST</code>：以指定用户登陆到目标HOST服务器上去，<code>USERNAME@</code> 省略的话就是以默认用户登陆到服务器</li>
<li><code>ssh -l USERNAME HOST</code>：以指定用户登陆到目标HOST服务器上去，用户同样可以省略</li>
<li><code>ssh USERNAME@HOST &#39;COMMAND&#39;</code>：以指定用户登陆到主机上，然后执行’COMMAND’命令后退出</li>
</ul>
<br/>

<h2 id="ssh-keygen"><a href="#ssh-keygen" class="headerlink" title="ssh-keygen"></a>ssh-keygen</h2><p>ssh-keygen使用方法：</p>
<ul>
<li><code>-t rsa|dsa</code>：指定加密方法，默认为 rsa</li>
<li><code>-f FILENAME</code>：指定文件名，会生成2个文件，私钥文件为FILENAME，公钥文件为FILENAME_pub</li>
<li><code>-P &#39;&#39;</code>：指定加密私钥密码</li>
</ul>
<p>公钥生成完毕之后如果将公钥内容（id_rsa.pub）<strong>追加</strong>至目标主机下的 <code>~/.ssh/authorized_keys</code> 文件中（.ssh目录权限为700），然后即可免密登陆目标主机</p>
<br/>

<h2 id="ssh-copy-id"><a href="#ssh-copy-id" class="headerlink" title="ssh-copy-id"></a>ssh-copy-id</h2><p>ssh-copy-id使用方法：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ssh-copy-id -i ~&#x2F;.ssh&#x2F;id_rsa.pub USERNAME@HOST</span><br></pre></td></tr></table></figure>
<ul>
<li><code>-i</code> 指定公钥存放位置，如：<br><code>ssh-copy-id -i ~/.ssh/id_rsa.pub USERNAME@HOST</code> 就会将公钥内容自动追加到目标主机中的.ssh/authorized_keys文件中</li>
</ul>
<p>上传到服务器上：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ssh-copy-id -i ~&#x2F;.ssh&#x2F;id_rsa.pub USERNAME@HOST</span><br></pre></td></tr></table></figure>
<p>上面这条命令实际上是将 <code>id_rsa.pub</code> 写到目标主机上的 <code>~/.ssh/autorized_keys</code> 文件中，可查看：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cat ~&#x2F;.ssh&#x2F;authorized_keys</span><br></pre></td></tr></table></figure>
<p>可以看到客户端写入到服务器的 <code>id_rsa.pub</code> （公钥）内容，之后客户端通过 <code>ssh</code> 连接远程服务器，就可以免密登录了。</p>
<br/>

<h2 id="scp"><a href="#scp" class="headerlink" title="scp"></a>scp</h2><p>Linux scp命令</p>
<p>Linux scp 命令用于 Linux 之间复制文件和目录。</p>
<p>scp 是 secure copy 的缩写, scp 是 linux 系统下基于 ssh 登陆进行安全的远程文件拷贝命令。</p>
<p>scp 是加密的，rcp 是不加密的，scp 是 rcp 的加强版。</p>
<p>语法</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">scp [-1246BCpqrv] [-c cipher] [-F ssh_config] [-i identity_file] [-l limit] [-o ssh_option] [-P port] [-S program] [[user@]host1:]file1 [...] [[user@]host2:]file2</span><br></pre></td></tr></table></figure>

<p>简易用法:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">scp [可选参数] file_source file_target</span><br></pre></td></tr></table></figure>

<p>参数说明：</p>
<ul>
<li>-1： 强制scp命令使用协议ssh1</li>
<li>-2： 强制scp命令使用协议ssh2</li>
<li>-4： 强制scp命令只使用IPv4寻址</li>
<li>-6： 强制scp命令只使用IPv6寻址</li>
<li>-B： 使用批处理模式（传输过程中不询问传输口令或短语）</li>
<li>-C： 允许压缩。（将-C标志传递给ssh，从而打开压缩功能）</li>
<li>-p：保留原文件的修改时间，访问时间和访问权限。</li>
<li>-q： 不显示传输进度条。</li>
<li>-r： 递归复制整个目录。</li>
<li>-v：详细方式显示输出。scp和ssh(1)会显示出整个过程的调试信息。这些信息用于调试连接，验证和配置问题。</li>
<li>-c cipher： 以cipher将数据传输进行加密，这个选项将直接传递给ssh。</li>
<li>-F ssh_config： 指定一个替代的ssh配置文件，此参数直接传递给ssh。</li>
<li>-i identity_file： 从指定文件中读取传输时使用的密钥文件，此参数直接传递给ssh。</li>
<li>-l limit： 限定用户所能使用的带宽，以Kbit/s为单位。</li>
<li>-o ssh_option： 如果习惯于使用ssh_config(5)中的参数传递方式，</li>
<li>-P port：注意是大写的P, port是指定数据传输用到的端口号</li>
<li>-S program： 指定加密传输时所使用的程序。此程序必须能够理解ssh(1)的选项。</li>
</ul>
<br/>

<h3 id="从本地复制到远程"><a href="#从本地复制到远程" class="headerlink" title="从本地复制到远程"></a>从本地复制到远程</h3><p>命令格式：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">scp local_file remote_username@remote_ip:remote_folder </span><br><span class="line">或者 </span><br><span class="line">scp local_file remote_username@remote_ip:remote_file </span><br><span class="line">或者 </span><br><span class="line">scp local_file remote_ip:remote_folder </span><br><span class="line">或者 </span><br><span class="line">scp local_file remote_ip:remote_file</span><br></pre></td></tr></table></figure>
<p>第1,2个指定了用户名，命令执行后需要再输入密码，第1个仅指定了远程的目录，文件名字不变，第2个指定了文件名；<br>第3,4个没有指定用户名，命令执行后需要输入用户名和密码，第3个仅指定了远程的目录，文件名字不变，第4个指定了文件名；</p>
<p>应用实例：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">scp &#x2F;home&#x2F;space&#x2F;music&#x2F;1.mp3 root@www.runoob.com:&#x2F;home&#x2F;root&#x2F;others&#x2F;music </span><br><span class="line">scp &#x2F;home&#x2F;space&#x2F;music&#x2F;1.mp3 root@www.runoob.com:&#x2F;home&#x2F;root&#x2F;others&#x2F;music&#x2F;001.mp3 </span><br><span class="line">scp &#x2F;home&#x2F;space&#x2F;music&#x2F;1.mp3 www.runoob.com:&#x2F;home&#x2F;root&#x2F;others&#x2F;music </span><br><span class="line">scp &#x2F;home&#x2F;space&#x2F;music&#x2F;1.mp3 www.runoob.com:&#x2F;home&#x2F;root&#x2F;others&#x2F;music&#x2F;001.mp3</span><br></pre></td></tr></table></figure>

<p>复制目录命令格式：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">scp -r local_folder remote_username@remote_ip:remote_folder </span><br><span class="line">或者 </span><br><span class="line">scp -r local_folder remote_ip:remote_folder</span><br></pre></td></tr></table></figure>
<p>第1个指定了用户名，命令执行后需要再输入密码；<br>第2个没有指定用户名，命令执行后需要输入用户名和密码；</p>
<p>应用实例：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">scp -r &#x2F;home&#x2F;space&#x2F;music&#x2F; root@www.runoob.com:&#x2F;home&#x2F;root&#x2F;others&#x2F; </span><br><span class="line">scp -r &#x2F;home&#x2F;space&#x2F;music&#x2F; www.runoob.com:&#x2F;home&#x2F;root&#x2F;others&#x2F;</span><br></pre></td></tr></table></figure>
<p>上面命令将本地 music 目录复制到远程 others 目录下。</p>
<br/>

<h3 id="从远程复制到本地"><a href="#从远程复制到本地" class="headerlink" title="从远程复制到本地"></a>从远程复制到本地</h3><p>从远程复制到本地，只要将从本地复制到远程的命令的后2个参数调换顺序即可，如下实例</p>
<p>应用实例：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">scp root@www.runoob.com:&#x2F;home&#x2F;root&#x2F;others&#x2F;music &#x2F;home&#x2F;space&#x2F;music&#x2F;1.mp3 </span><br><span class="line">scp -r www.runoob.com:&#x2F;home&#x2F;root&#x2F;others&#x2F; &#x2F;home&#x2F;space&#x2F;music&#x2F;</span><br></pre></td></tr></table></figure>
<p>说明</p>
<ul>
<li>1.如果远程服务器防火墙有为 scp 命令设置了指定的端口，我们需要使用 -P 参数来设置命令的端口号，命令格式如下：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#scp 命令使用端口号 4588</span><br><span class="line">scp -P 4588 remote@www.runoob.com:&#x2F;usr&#x2F;local&#x2F;sin.sh &#x2F;home&#x2F;administrator</span><br></pre></td></tr></table></figure></li>
<li>2.使用scp命令要确保使用的用户具有可读取远程服务器相应文件的权限，否则scp命令是无法起作用的。</li>
</ul>
<br/>

<h2 id="sftp"><a href="#sftp" class="headerlink" title="sftp"></a>sftp</h2><p>sftp 是 Secure File Transfer Protocol 的缩写，安全文件传送协议。可以为传输文件提供一种安全的网络的加密方法。sftp 与 ftp 有着几乎一样的语法和功能。SFTP 为 SSH 的其中一部分，是一种传输档案至 Blogger 伺服器的安全方式。其实在 SSH 软件包中，已经包含了一个叫作 SFTP(Secure File Transfer Protocol) 的安全文件信息传输子系统，SFTP 本身没有单独的守护进程，它必须使用 sshd 守护进程（端口号默认是 22）来完成相应的连接和答复操作，所以从某种意义上来说，SFTP 并不像一个服务器程序，而更像是一个客户端程序。SFTP 同样是使用加密传输认证信息和传输的数据，所以，使用 SFTP 是非常安全的。但是，由于这种传输方式使用了加密/解密技术，所以传输效率比普通的 FTP 要低得多，如果您对网络安全性要求更高时，可以使用 SFTP 代替 FTP。</p>
<p>用法；</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 连接远程服务器</span></span><br><span class="line">sftp root@192.168.25.140</span><br><span class="line"><span class="comment"># 查看当前服务器路径</span></span><br><span class="line">lpwd</span><br><span class="line"><span class="comment"># 查看远程服务器路径[默认用户家目录]</span></span><br><span class="line"><span class="built_in">pwd</span></span><br><span class="line"><span class="comment"># 上传文件</span></span><br><span class="line">put 当前路径 远程连接</span><br><span class="line"><span class="comment"># 下载文件</span></span><br><span class="line">get 远程路径  当前路径</span><br><span class="line"></span><br><span class="line">sftp user@ip: 登录，和ssh一样</span><br><span class="line"><span class="built_in">help</span>:建立连接后， linux shell中的$编程了sftp&gt;, 这也对。 现在执行以下<span class="built_in">help</span>, 可以看看sftp支持哪些命令。</span><br><span class="line"><span class="built_in">pwd</span>和lpwd:<span class="built_in">pwd</span>是看远端服务器的目录， 即sftp服务器默认的当前目录。 lpwd是看linux本地目录</span><br><span class="line">ls和lls:ls是看sftp服务器下当前目录下的东东， lls是看linux当前目录下的东东。</span><br><span class="line">put a.txt :这个是把linux当前目录下的a.txt文件上传到sftp服务器的当前目录下。</span><br><span class="line">get b.txt:这个是把sftp服务器当前目录下的b.txt文件下载到linux当前目录下。</span><br><span class="line">mget</span><br><span class="line">!<span class="built_in">command</span> 这个是指在linux上执行<span class="built_in">command</span>这个命令， 比如!ls是列举linux当前目录下的东东， !rm a.txt是删除linux当前目录下的a.txt文件。</span><br><span class="line"><span class="built_in">exit</span>、quit、<span class="built_in">bye</span>: 退出。</span><br><span class="line"><span class="built_in">help</span>：再强调一下<span class="built_in">help</span>，登录以后，一个<span class="built_in">help</span>，基本可以搞定所有命令</span><br></pre></td></tr></table></figure>

<br/>

<h2 id="dropbear"><a href="#dropbear" class="headerlink" title="dropbear"></a>dropbear</h2><p>由于 openSSH 太过庞大，因此有一个针对于嵌入式系统的小型 ssh：dropbear</p>
<p>dropbear 服务器端为 dropbear，客户端为 dbclient</p>
<p>注意：dropbear 默认使用 nsswitch 实现名称解析，并且会在用户登陆时检查其默认 shell 是否为当前系统的安全 shell</p>
<p>主机密钥默认位置：</p>
<ul>
<li>/etc/dropbear/<ul>
<li>RSA: dropbear_rsa_host_key<br>  长度可变, 只要是8的整数倍，默认为1024</li>
<li>DSS: dropbear_dss_host_key<br>  长度固定，默认为1024</li>
</ul>
</li>
</ul>
<br/>

<h2 id="ssh-服务的最佳实践："><a href="#ssh-服务的最佳实践：" class="headerlink" title="ssh 服务的最佳实践："></a>ssh 服务的最佳实践：</h2><ul>
<li>不使用默认端口</li>
<li>禁止使用 Protocol version 1</li>
<li>限制可登陆用户</li>
<li>设定空闲会话超时时长</li>
<li>利用防火墙设置 ssh 访问策略</li>
<li>仅监听特定 IP 地址</li>
<li>基于口令认证时使用强密码策略，如用 <code>tr -dc A-Za-z0-9_ &lt; /dev/urandom | head -c 30 | xargs</code> 来生成 30 位随机数密码</li>
<li>使用基于密钥认证</li>
<li>禁止使用空密码</li>
<li>禁止 root 用户直接登陆</li>
<li>限制 ssh 的访问频度和并发在线数</li>
<li>做好日志，经常分析</li>
</ul>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux 常用命令</title>
    <url>/linux/c643ccd9-8a5f-4264-bb56-2d13cb4f386c.html</url>
    <content><![CDATA[<p>Linux 是一种自由和开放源码的类 UNIX 操作系统，使用 Linux 内核。目前存在着许多不同的 Linux 发行版，可安装在各种各样的电脑硬件设备。各版本的 Linux 使用方式大致相同，本文介绍 Linux 下的常用命令。</p>
<a id="more"></a>

<p>Linux 的命令就是提醒 shell 程序找到键入命令所对应的可执行程序或代码，并由其分析后提交给内核分配资源将其运行起来，表现为一个或多个进程</p>
<p>使用方法如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">命令 [选项] [参数]</span><br></pre></td></tr></table></figure>

<p>其中选项可分为短选项和长选项，用于增加命令功能</p>
<ul>
<li>短选项：-char ，一般是用一个字符表示，多个短选项可以组合</li>
<li>长选项：–word ，用完整单词或单词缩写来表示</li>
</ul>
<p>参数：命令作用的对象（<strong>注：有些选项是可以带参数的</strong>）</p>
<p>获得命令的使用帮助：</p>
<ul>
<li>内部命令：help + 命令</li>
<li>外部命令：命令 + –help</li>
</ul>
<h2 id="基本命令"><a href="#基本命令" class="headerlink" title="基本命令"></a>基本命令</h2><h3 id="pwd"><a href="#pwd" class="headerlink" title="pwd"></a>pwd</h3><p>pwd（Printing Working Directory），输出工作目录（当前目录），用的是 <strong>绝对路径</strong> 表示</p>
<h3 id="ls"><a href="#ls" class="headerlink" title="ls"></a>ls</h3><p>ls（list）：显示当前目录下的所有文件，命令格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ls [OPTION]... [FILE]...</span><br></pre></td></tr></table></figure>

<p>各选项的作用：</p>
<ul>
<li><code>-l</code>：以长格式显示文件信息，文件信息具体内容如下：<br>首先是文件类型：<br>-：普通文件（f）<br>d：目录文件<br>b：块设备文件<br>c：字符设备文件<br>l：符号链接文件（软链接）<br>p：命令管道文件<br>s：套接字文件<br>然后的9位字符是文件权限，r（读）、w（写）、x（执行），每3位一组，第一组表示文件属主对其的权限，第二组表示文件属组中的成员对其的权限，第三组表示其他成员对其的权限<br>后一位数字表示文件硬链接次数<br>然后是文件属主（owner）和文件属组（group）<br>之后是文件大小和改变时间</li>
<li><code>-h</code>：单位转换，将字节单位转换为K或者M等，一般与-l配合使用</li>
<li><code>-a</code>：显示包括隐藏文件在内的所有文件，其中，“.”表示当前目录，“..”表示上级目录</li>
<li><code>-A</code>：显示所有文件，不包括.和..</li>
<li><code>-d</code>：显示目录自身，一般与-l配合使用</li>
<li><code>-i</code>：显示目录下每个文件的索引结点号（index node）</li>
<li><code>-r</code>：逆序显示文件（默认升序）</li>
<li><code>-R</code>：递归显示文件</li>
</ul>
<p>参数：ls 后可跟参数，默认参数为 <strong>当前工作目录</strong></p>
<h3 id="cd"><a href="#cd" class="headerlink" title="cd"></a>cd</h3><p>cd（change directory）：切换工作目录</p>
<ul>
<li><code>cd</code> 后不跟参数是回到当前用户的 home 目录</li>
<li><code>cd ~[USERNAME]</code>：进入指定用户的 home 目录，默认为当前用户</li>
<li><code>cd -</code>：在最近使用的 2 个目录之间来回切换</li>
</ul>
<p><strong>注：cd 后的参数只能是目录文件</strong></p>
<h3 id="type"><a href="#type" class="headerlink" title="type"></a>type</h3><p>显示命令属于哪种类型（命令类型：内置命令和外部命令）</p>
<p>内置命令由 shell 内建，外部命令是在文件系统中的某个路径下有一个与命令名相同的可执行文件</p>
<p>type 后面的参数跟的是某个命令</p>
<h3 id="which"><a href="#which" class="headerlink" title="which"></a>which</h3><p><code>which COMMAND</code>：此命令可显示命令所在路径</p>
<h3 id="date"><a href="#date" class="headerlink" title="date"></a>date</h3><p>date 命令用于显示当前时间（系统时间）</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">date [OPTION]... [+FORMAT]</span><br></pre></td></tr></table></figure>

<p><code>-s</code> 选项用于设置日期和时间</p>
<ul>
<li><code>date +%F</code> 可按「年-月-日」显示时间</li>
<li><code>date +%F-%H-%M-%S</code> 可按「年-月-日-时-分-秒」显示时间</li>
</ul>
<h3 id="man"><a href="#man" class="headerlink" title="man"></a>man</h3><p><code>man COMMAND</code>：显示命令手册</p>
<p><strong>注意</strong>：man 是分章节的，可用 <code>whatis COMMAND</code> 来查看该命令出现在哪些章节</p>
<p>man 后可跟章节名，如：<code>man 2 read</code></p>
<p>章节：</p>
<ol>
<li>可执行程序或者 shell 命令（/bin, /usr/bin, /usr/local/bin）</li>
<li>系统调用（内核提供的函数)</li>
<li>库调用（程序库中的函数）</li>
<li>特殊文件（设备文件,通常位于/dev）</li>
<li>文件格式和规范（配置文件语法,如/etc/passwd）</li>
<li>游戏</li>
<li>杂项（包括宏包和规范）</li>
<li>系统管理命令（/sbin, /usr/sbin, /usr/local/sbin）</li>
</ol>
<p>命令手册（man）中</p>
<ul>
<li>NANE: 命令名称及功能简要说明</li>
<li>SYNOPSIS: 用法说明，包括可用选项</li>
<li>DESCRIPTION: 功能详细说明</li>
<li>OPTIONS: 各选项含义</li>
<li>FILES: 相关配置文件</li>
<li>EXAMPLES: 使用示例</li>
</ul>
<p>在命令手册中翻屏：</p>
<ul>
<li>向后翻一屏：SPACE（空格键）</li>
<li>向前翻一屏：b</li>
<li>向后翻一行：ENTER（回车键）</li>
<li>向前翻一行：k</li>
</ul>
<p>按 <code>q</code> 键退出命令手册</p>
<h3 id="echo"><a href="#echo" class="headerlink" title="echo"></a>echo</h3><p>echo 命令将一个字符串打印到屏幕上（标准输出），命令格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">echo [OPTION]... [STRING]...</span><br></pre></td></tr></table></figure>

<p>用 echo 输出信息时，还可以指定字体样式、前景色、字体颜色（配合 -e 选项使用）</p>
<p>如 <code>echo -e &quot;\033[31mHello\033[0m World!&quot;</code> 就是将 Hello 显示为红色</p>
<p><code>\033[#;#;#mSTRING\033[0m</code> 中的第一个数字#代表字体样式，第二个数字#代表前景色，第三个数字#代表字体颜色，默认从右边字体颜色开始读取，最后的 <code>\033[0m</code> 就是调整字体后的结束符</p>
<h3 id="tee"><a href="#tee" class="headerlink" title="tee"></a>tee</h3><p>tee 命令将输入的信息打印到屏幕上然后在保存在 file 文件中，可以和管道配合使用，命令格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">tee [OPTION]... [FILE]...</span><br></pre></td></tr></table></figure>

<h3 id="whoami"><a href="#whoami" class="headerlink" title="whoami"></a>whoami</h3><p>该命令用于查看当前登陆到本终端的用户，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">whoami [OPTION]...</span><br></pre></td></tr></table></figure>

<h3 id="who"><a href="#who" class="headerlink" title="who"></a>who</h3><p>该命令用于查看当前所有登陆到此计算机的终端，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">who [OPTION]... [ FILE | ARG1 ARG2 ]</span><br></pre></td></tr></table></figure>

<p><code>-r</code>：显示当前运行级别</p>
<p>终端类型：</p>
<ul>
<li>console：控制台</li>
<li>pty：物理终端</li>
<li>tty#：虚拟终端</li>
<li>ttyS#：串行终端</li>
<li>pts/#：伪终端</li>
</ul>
<h3 id="w"><a href="#w" class="headerlink" title="w"></a>w</h3><p>与 who 命令相似息<br>与 who 命令相似，可以显示更为详细的信</p>
<h3 id="basename"><a href="#basename" class="headerlink" title="basename"></a>basename</h3><p>该命令用于显示文件的基名，如 <code>basename /etc/inittab</code> 则显示<code>inittab</code></p>
<p>在 <code>bash</code> 编程中，通常将用 <code>basename $0</code> 来表示脚本文件本身</p>
<h3 id="hostname"><a href="#hostname" class="headerlink" title="hostname"></a>hostname</h3><p>该命令用于显示主机名，后跟上名称也可以修改主机名，但是环境变量 <code>$HOSTNAME</code> 里的主机名不会发生改变</p>
<h3 id="tty"><a href="#tty" class="headerlink" title="tty"></a>tty</h3><p>此命令用于查看当前使用的终端设备</p>
<h3 id="watch"><a href="#watch" class="headerlink" title="watch"></a>watch</h3><p>用于动态刷新某个命令的执行结果</p>
<p><code>-n#</code>：执行刷新间隔</p>
<p><code>watch -n# &#39;COMMAND&#39;</code></p>
<h3 id="grep"><a href="#grep" class="headerlink" title="grep"></a>grep</h3><p>该命令用于搜索文件中匹配模式的行，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">grep [OPTION] PATTERN [file...]</span><br></pre></td></tr></table></figure>
<p>其中模式(PATTERN)可以是字符串，也可以是正则表达式</p>
<p>选项如下：</p>
<ul>
<li><code>-i</code>：忽略字符大小写</li>
<li><code>--color</code>：将搜索到的字符加颜色</li>
<li><code>-v</code>：反向查找，被模式匹配到的行不显示，只显示没有匹配到的行</li>
<li><code>-o</code>：只显示被模式匹配到的字符串（每个串显示为一行）</li>
<li><code>-E</code>：使用扩展正则表达式</li>
<li><code>-A NUM</code>：表示匹配到某一行之后，此行的下NUM行也显示出来</li>
<li><code>-B NUM</code>：表示匹配到某一行后，此行的前面NUM行也显示出来</li>
<li><code>-C NUM</code>：表示匹配到某一行后，此行的前后各NUM行也显示出来</li>
</ul>
<p>正则表达式（贪婪模式，尽可能长的匹配）：</p>
<ul>
<li><code>.</code>：匹配任意单个字符</li>
<li><code>[]</code>:匹配指定范围内的任意单个字符（对 [] 字符集同样有效）</li>
<li><code>[^]</code>:匹配指定范围外的任意单个字符</li>
<li><code>*</code>：匹配其前面的字符任意次</li>
<li><code>.*</code>：任意长度的任意字符</li>
<li><code>?</code>：匹配其前面的字符1或0次</li>
<li><code>\{m,n\}</code>:匹配其前面的字符至少 m 次，至多 n 次<br>如：<code>\{1,\}</code> 最少一次，多了不限</li>
<li><code>^</code>：其后的所有内容必须出现在行首</li>
<li><code>$</code>：此字符的前面的所有内容必须出现在行尾</li>
<li><code>^$</code>：空白行</li>
<li><code>\&lt;(或者\b)</code>:其后面的所有字符必须作为单词首部出现</li>
<li><code>\&gt;(或者\b)</code>:其前面的所有字符必须作为单词尾部出现<br><code>这两个可以结合使用：如</code>&lt;root&gt;` 就是匹配单个 root 单词（此单词只能为 root，注意单词前后不能有其他字母）</li>
<li><code>\(\)</code>:分组，<code>\</code> 为转义字符，将 <code>()</code> 转为它本来的意义。如：<code>\(ab\)*</code> 就是匹配含有任意个连续 ab 的行。关于分组还有一个后向引用，在分组 <code>()</code> 后用 <code>\1</code> 表示第一个括号匹配的内容， <code>\2</code> 表示第二个括号匹配的内容，<code>\3</code> 表示第三个括号匹配的内容，以此类推。</li>
</ul>
<p>扩展正则表达式：<br>相对于正则表达式增加了 <code>+</code>，匹配其前面的字符至少 1 次；同时花括号 <code>{}</code> 不需要用转义字符 <code>\</code>；分组 <code>()</code> 也不需要使用转义字符 <code>\</code>；增加了或运算符 <code>|</code>，匹配其左边的所有字符或者其右边的所有字符，通常和分组配合使用</p>
<p><code>grep -E</code> 和 <code>egrep</code> 的效果是一样的，都是使用扩展正则表达式。</p>
<p><strong>注意：对于以 CRLF 结尾的文件，grep 无法正常匹配行尾，可使用 dos2unix 命令：</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cat text.txt | dos2unix | grep &#39;^$&#39;</span><br></pre></td></tr></table></figure>


<h2 id="文件管理命令"><a href="#文件管理命令" class="headerlink" title="文件管理命令"></a>文件管理命令</h2><h3 id="mkdir"><a href="#mkdir" class="headerlink" title="mkdir"></a>mkdir</h3><p>此命令用于创建空目录，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mkdir [OPTION]... DIRECTORY...</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-p</code>：递归创建目录</li>
<li><code>-v</code>：显示信息</li>
</ul>
<p>可用 <code>,</code> 隔开创建多个目录，也可以跟多个参数，如 <code>mkdir ./x,y,z</code> 可在当前目录下创建 x、y、z 三个目录</p>
<p>用 <code>{}</code> 可以创建多个目录：如 <code>mkdir ./{a,d}_{b,c}</code> 可在当前目录下创建 a_b、a_c、d_b、d_c 四个目录</p>
<h3 id="tree"><a href="#tree" class="headerlink" title="tree"></a>tree</h3><p>查看目录树，后可跟目录，默认为当前目录</p>
<h3 id="rmdir"><a href="#rmdir" class="headerlink" title="rmdir"></a>rmdir</h3><p>该命令用于删除目录（只能删除空目录）</p>
<p><code>-p</code>：递归删除空目录</p>
<h3 id="touch"><a href="#touch" class="headerlink" title="touch"></a>touch</h3><p>该命令用于修改文件的时间戳（3 个时间都改），常用此命令创建文件</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">touch [OPTION]... FILE...</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-c</code>：不创建新文件，改变3个时间</li>
<li><code>-a</code>：仅改变文件访问（access）时间</li>
<li><code>-m</code>：仅改变文件更改时间（modification）</li>
<li><code>-t</code>：使用过去的时间，格式为 年月日时分.秒</li>
</ul>
<p>文体时间如下：</p>
<ul>
<li>access time：访问访问时间，读取文件内容就会改变</li>
<li>modify time：修改时间，该变文件内容的时候该时间就会改变</li>
<li>change time：改变时间，文件元数据发生改变的时候该时间就会改变</li>
</ul>
<p><strong>注</strong>：每一次使用 <code>touch</code> ，最近改动时间均会改变</p>
<h3 id="stat"><a href="#stat" class="headerlink" title="stat"></a>stat</h3><p>查看文件具体信息</p>
<h3 id="rm"><a href="#rm" class="headerlink" title="rm"></a>rm</h3><p>该命令用于删除文件，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">rm [OPTION]... FILE...</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-f</code>：强行删除，不带提示</li>
<li><code>-r</code>：删除目录以及目录下的所有文件</li>
<li><code>-i</code>：删除前给提示</li>
</ul>
<h3 id="cp"><a href="#cp" class="headerlink" title="cp"></a>cp</h3><p>该命令用于复制文件，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cp [OPTION]... FILE...</span><br></pre></td></tr></table></figure>

<p>通常的用法为：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cp SRC DST</span><br></pre></td></tr></table></figure>
<p>其中：</p>
<ul>
<li>SRC：源文件</li>
<li>DST：目标文件</li>
</ul>
<p><strong>可以有多个源文件，但是只能有一个目标文件。如果目标文件为目录，则保存为原文件名，也可以指定文件名</strong></p>
<p>选项：</p>
<ul>
<li><code>-R</code>，<code>-r</code>：递归复制目录以及目录下的所有子文件</li>
<li><code>-f</code>：强行复制</li>
<li><code>-i</code>：在覆盖时给出提示信息</li>
<li><code>-p</code>：复制后保留原有信息（属主、属组、时间戳等）</li>
<li><code>-a</code>：保留文件的所有信息（比-p保留的信息要多，包括链接等，常用于备份）<br>可用{}复制多个文件</li>
</ul>
<h3 id="mv"><a href="#mv" class="headerlink" title="mv"></a>mv</h3><p>该命令用于移动文件（不能覆盖目录），格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mv [OPTION]... FILE...</span><br><span class="line">mv SRC DEST</span><br></pre></td></tr></table></figure>
<p>可以多个源文件，一个目标文件，也可以用 mv 命令对文件进行重命名</p>
<p>选项：</p>
<ul>
<li><code>-i</code>：给出提示信息（覆盖时）</li>
<li><code>-f</code>：强制移动</li>
<li><code>-t</code>：先指定目标文件，然后指定源文件，如 <code>mv -t DEST SRC</code></li>
</ul>
<h3 id="cat"><a href="#cat" class="headerlink" title="cat"></a>cat</h3><p>该命令用于连接并显示文件，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cat [OPTION]... FILE...</span><br></pre></td></tr></table></figure>

<p>选项：</p>
<ul>
<li><code>-n</code>：显示行号</li>
<li><code>-E</code>：显示行结束符</li>
</ul>
<p>cat 反过来写 <code>tac</code> 命令：从尾部到首部显示，和 cat 命令使用方法一样</p>
<h3 id="more-less"><a href="#more-less" class="headerlink" title="more/less"></a>more/less</h3><p>分屏显示文件，more 翻到最后不能往前翻，less 和 man 的使用方法一样</p>
<h3 id="head-tail"><a href="#head-tail" class="headerlink" title="head/tail"></a>head/tail</h3><p><code>head</code>：查看前 n 行<br><code>tail</code>：查看后 n 行</p>
<p><code>head/tail -n + num</code>：显示 <code>前/后num</code> 行，或者直接 <code>-num</code>，默认num为10行</p>
<p><code>tail -f</code>：查看文件尾部，不退出，如果有其他进程追加后续内容就立即予以显示</p>
<h3 id="cut"><a href="#cut" class="headerlink" title="cut"></a>cut</h3><p>该命令用于显示文件内容，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cut [OPTION]... FILE...</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-d</code>：指定字段分隔符，后面直接跟分隔符，默认分隔符为空格</li>
<li><code>-f</code>：指定要显示的字段，后直接跟字段符，如 <code>-f 1,3</code>、<code>-f 1-3</code></li>
</ul>
<h3 id="sort"><a href="#sort" class="headerlink" title="sort"></a>sort</h3><p>文本排序，自左向右按照 ASCLL 码进行排序，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sort [OPTION]... FILE...</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-n</code>：按数值排序</li>
<li><code>-r</code>：降序（默认升序）</li>
<li><code>-t</code>：指定字段分隔符，后跟分隔符</li>
<li><code>-k</code>：以哪个字段为关键字进行排序，后跟字段</li>
<li><code>-u</code>：排序后相同的行只显示一次</li>
<li><code>-f</code>：排序时忽略大小写</li>
</ul>
<h3 id="uniq"><a href="#uniq" class="headerlink" title="uniq"></a>uniq</h3><p>略过相邻重复的行</p>
<p>选项：</p>
<ul>
<li><code>-c</code>：显示文件中行重复的次数</li>
<li><code>-d</code>：只显示重复的行</li>
</ul>
<h3 id="wc"><a href="#wc" class="headerlink" title="wc"></a>wc</h3><p>该命令用于显示一个文件的行数、单词数、字节数，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">wc [OPTION]... FILE...</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-l</code>：只显示行数</li>
<li><code>-w</code>：只显示单词数</li>
<li><code>-c</code>：只显示字节数</li>
<li><code>-m</code>：只显示字符数</li>
<li><code>-L</code>：显示最长的一行包含多少个字符数</li>
</ul>
<h3 id="tr"><a href="#tr" class="headerlink" title="tr"></a>tr</h3><p>该命令用于转换或删除字符，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">tr [OPTION]... SET1 [SET2]</span><br></pre></td></tr></table></figure>

<p>如：<code>tr &#39;ab&#39; &#39;AB&#39;</code> 是将所有 a 转换成 A ，将所有 b 转换成 B</p>
<p>还能用 <code>tr &#39;a-z&#39; &#39;A-Z&#39;</code> 将所有小写字符转换成大写字符</p>
<p>选项：</p>
<ul>
<li><code>-d</code>：删除字符集中的所有字符</li>
</ul>
<p>生成随机字符串： <code>tr -dc A-Za-z0-9_ &lt; /dev/urandom | head -c 30 | xargs</code></p>
<h3 id="locate"><a href="#locate" class="headerlink" title="locate"></a>locate</h3><p>该命令用于查找某个文件的具体位置</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">locate FILE</span><br></pre></td></tr></table></figure>

<p>查找方式是非实时的，模糊匹配文件名，根据全系统文件数据库进行查找<br>updatedb 命令可以手动生成文件数据库</p>
<h3 id="find"><a href="#find" class="headerlink" title="find"></a>find</h3><p>find 命令用于在某个路径下对某文件名进行查找，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">find [-H] [-L] [-P] [-D debugopts] [-Olevel] [path...] [expression]</span><br></pre></td></tr></table></figure>
<p>查找方式是实时查找，能够精确匹配文件名，支持众多查找标准。通过遍历指定目录中的所有文件完成查找，查找速度较慢</p>
<p>find 命令格式：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">find 查找路径 查找标准 找到后的处理</span><br></pre></td></tr></table></figure>
<p>其中：</p>
<ul>
<li>查找路径：默认为当前路径</li>
<li>查找标准：默认为指定路径下的所有文件</li>
<li>处理操作：默认为显示（输出到屏幕）</li>
</ul>
<p>find 的匹配标准：</p>
<ul>
<li><code>-name &#39;FILENAME&#39;</code>：对文件名进行精确匹配<br><code>FILENAME</code> 中可以使用文件名通配，如 <code>*</code>（任意长度任意字符）、<code>?</code>、<code>[]</code> 等</li>
<li><code>-iname &#39;FILENAME&#39;</code>：文件名匹配时忽略大小写</li>
<li><code>-regex PATTERN</code>：基于正则表达式进行文件名匹配</li>
<li><code>-user USERNAME</code>: 根据属主查找</li>
<li><code>-group GROUPNAME</code>: 根据属组查找</li>
<li><code>-uid UID</code>: 根据UID查找</li>
<li><code>-gid GID</code>: 根据GID查找</li>
<li><code>-nouser</code>：查找没有属主的文件</li>
<li><code>-nogroup</code>: 查找没有属组的文件</li>
<li><code>-type TYPENAME</code>：根据文件类型查找<br>f：普通文件<br>d：目录文件<br>b：块设备文件<br>c：字符设备文件<br>l：符号链接文件（软链接）<br>p：命令管道文件<br>s：套接字文件</li>
<li><code>-size [+|-]SIZE</code>：根据文件大小来查找<br>如果SIZE前不跟加号（+）或者减号（-）就表示精确匹配文件大小（注意是近似精确值），如果跟加号就表示匹配大于 SIZE 的文件，跟减号就表示是匹配小于 SIZE 的文件，SIZE 后可跟单位（k、M、G），默认为字节</li>
</ul>
<p>根据时间匹配：</p>
<ul>
<li><code>-mtime [+|-]TIME</code>：根据更改时间进行匹配</li>
<li><code>-atime [+|-]TIME</code>：根据访问时间进行匹配</li>
<li><code>-ctime [+|-]TIME</code>：根据修改时间进行匹配</li>
</ul>
<p><strong>注意</strong>：以上是最近根据天数进行匹配<br><code>-mmin、-amin、-cmin</code> 是根据最近分钟数进行匹配</p>
<p>根据权限匹配：</p>
<ul>
<li><code>-perm MODE</code>：精确根据权限匹配</li>
<li><code>/MODE</code>：只要 3 位中任何一位匹配就能匹配到</li>
<li><code>-MODE</code>：文件权限能够完全包含此 MODE 即可匹配，如 755(rwxr-xr-x) 完全包含 644(rw-r–r–)</li>
</ul>
<p>组合条件：</p>
<ul>
<li><code>-a</code>：与</li>
<li><code>-o</code>：或</li>
<li><code>-not</code>：非</li>
</ul>
<p>如要在当前目录下查找文件大小大于 1M 并且属主是 root 的文件：<code>find ./ -size +1M -a -user root</code></p>
<p>找到后的操作</p>
<ul>
<li><code>-print</code>：显示</li>
<li><code>-ls</code>：类似ls -l显示找到后的每个文件</li>
</ul>
<h3 id="gzip"><a href="#gzip" class="headerlink" title="gzip"></a>gzip</h3><p>用于压缩 <code>.gz</code> 类型的文件，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">gzip [option] &#x2F;SOMEFILE</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-d</code>：解压缩，还可以使用 gunzip 命令来进行解压缩</li>
<li><code>-#</code>：指定压缩比，默认为 6</li>
</ul>
<p><strong>注意</strong>：无论是压缩还是解压缩都会删除原文件</p>
<p><code>zcat</code>：对 .gz 文件不进行解压的情况下查看文本文件的内容</p>
<h3 id="bzip2"><a href="#bzip2" class="headerlink" title="bzip2"></a>bzip2</h3><p>用于压缩 .bz2 类型的文件，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bzip2 [option] &#x2F;SOMEFILE</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-d</code>：解压缩，还可以使用 bunzip2 命令进行解压缩</li>
<li><code>-#</code>：指定压缩比，默认为 6</li>
<li><code>-k</code>：压缩时保留原文件</li>
</ul>
<p><strong>注意</strong>：会删除原文件</p>
<p><code>bzcat</code>：对 .bz2 文件不进行解压的情况下查看文本文件的内容</p>
<h3 id="xz"><a href="#xz" class="headerlink" title="xz"></a>xz</h3><p>用于压缩 .xz 类型的文件，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">xz [option] &#x2F;SOMEFILE</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-d</code>：解压缩，还可以使用 unxz 进行解压</li>
<li><code>-#</code>：指定压缩比，默认为 6</li>
<li><code>-k</code>：保留原文件</li>
</ul>
<p><code>xzcat</code>：对 .xz 文件不进行解压的情况下查看文本文件的内容</p>
<h3 id="zip"><a href="#zip" class="headerlink" title="zip"></a>zip</h3><p>既归档又压缩的工具，压缩后不删除原文件，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">zip FILENAME.zip FILE1 FILE2 ...</span><br></pre></td></tr></table></figure>
<p><code>unzip</code> 命令用于解压缩</p>
<h3 id="tar"><a href="#tar" class="headerlink" title="tar"></a>tar</h3><p>归档工具</p>
<p>选项：</p>
<ul>
<li><code>-c</code>: 创建归档文件</li>
<li><code>-f FILE.tar</code>: 操作的归档文件（注意，组合选项时 f 必须在 c 或 x 的后面）</li>
<li><code>-x</code>: 展开归档</li>
<li><code>--xattrs</code>: 归档时，保留文件的扩展属性信息</li>
<li><code>-t</code>: 不展开归档，直接查看归档了哪些文件</li>
<li><code>-v</code>：显示信息</li>
</ul>
<p>组合：</p>
<ul>
<li><code>-cfz</code>: 归档并调用 gzip 压缩（-z 可以省略，命令通过判断文件名后缀来执行操作）</li>
<li><code>-xfz</code>: 调用 gzip 解压缩并展开归档，-z 选项可省略</li>
<li><code>-cfj</code>: bzip2</li>
<li><code>-xfj</code>:</li>
<li><code>-cfJ</code>: xz</li>
<li><code>-xfJ</code>:</li>
</ul>
<p>使用如下：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># tar 打包压缩</span></span><br><span class="line">tar cvf target.tar <span class="built_in">source</span></span><br><span class="line">tar cvfz target.tar.gz <span class="built_in">source</span></span><br><span class="line">tar cvfj target.tar.bz2 <span class="built_in">source</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># tar 解包</span></span><br><span class="line">tar xvf file.tar</span><br><span class="line">tar xvfz file.tar.gz</span><br><span class="line">tar xvfj file.tar.bz2</span><br></pre></td></tr></table></figure>

<h3 id="sed"><a href="#sed" class="headerlink" title="sed"></a>sed</h3><p>sed(Stream Editor) 是一种行编辑器</p>
<p>sed默认不编辑原文件，仅对模式空间中的数据做处理，处理结束后，将模式空间内容打印至屏幕</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sed [OPTIONS] &#39;AddressCommand&#39; file...</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-n</code>：静默模式（不再默认显示模式空间中的内容）</li>
<li><code>-i</code>：直接修改原文件</li>
<li><code>-e</code>：后面直接跟上 ‘AddressCommand’，可多次使用 -e 操作</li>
<li><code>-r</code>：使用扩展正则表达式</li>
</ul>
<p>如果AddressCommand中需要进行变量替换，那就使用双引号<br>Address:（如果Address不写就表示全文所有行）</p>
<ol>
<li>StartLine,EndLine<br>从 StartLine 到 EndLine（如1,100就是从第一行到第100行）<br>$：最后一行</li>
<li>/RegExp(正则表达式)/<br>匹配符合正则表达式的所有行</li>
<li>/Pattern1/,/Pattern2/<br>从第一次被Pattern1匹配到的行开始到第一次被Pattern2匹配到的行结束这中间的所有行</li>
<li>LineNumber<br>指定行</li>
<li>StartLine + N<br>从StartLine开始向后的N行</li>
</ol>
<p>Command：</p>
<ul>
<li><code>d</code>：删除符合条件的行</li>
<li><code>p</code>：显示符合条件的行</li>
<li><code>a \string</code>：在指定的行后面追加新行，内容为string(string内可使用\n进行换行)</li>
<li><code>i \string</code>：在指定的行前面添加新行，内容为string</li>
<li><code>r FILE</code>：将指定文件的内容添加至符合条件的行处</li>
<li><code>w FILE</code>：将符合条件的行另存至指定的文件中</li>
<li><code>s/Pattern/string/修饰符</code>：查找并替换（默认替换的是每一行中的第一次被模式匹配到的串），常用的修饰符有：g（全局替换，替换的不止是每行的第一个）、i（进行模式匹配时忽略大小写）。注意分割符不一定是斜线，还可以使用别的符号，如s###、s@@@等。string中可以用’&amp;’符号来引用模式匹配中的整个串，同时也支持正则表达式的后向引用</li>
</ul>
<h2 id="用户及权限管理命令"><a href="#用户及权限管理命令" class="headerlink" title="用户及权限管理命令"></a>用户及权限管理命令</h2><p><strong>注</strong>：其中大部分命令需要 <strong>管理员权限</strong></p>
<h3 id="useradd"><a href="#useradd" class="headerlink" title="useradd"></a>useradd</h3><p>此命令用于添加用户，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">useradd [options] USERNAME</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-u</code>：指定用户的 UID，默认为 passwd 下最后一个 UID 的下一个 ID，有一个最大限度，定义在 <code>/etc/login.defs</code> 中（centos）</li>
<li><code>-g</code>：指定用户的基本组（可以是组 ID，也可以是组名，但是这个组一定要存在）</li>
<li><code>-G</code>：指定用户的附加组（可以是多个组，但组一定要存在）</li>
<li><code>-c &quot;COMMENT&quot;</code>：指定用户的注释信息</li>
<li><code>-d /path/...</code>：指定用户的家目录，默认在 home 下建立与用户名相同的目录</li>
<li><code>-s SHELL</code>：指定用户 shell 的路径（在 <code>/etc/shells</code> 下可查看当前系统可用的安全 shell）</li>
<li><code>-m</code>：强制为用户创建家目录</li>
<li><code>-M</code>：不给用户创建家目录</li>
<li><code>-r</code>：指定此用户为系统用户</li>
</ul>
<h3 id="userdel"><a href="#userdel" class="headerlink" title="userdel"></a>userdel</h3><p>该命令用于删除用户，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">userdel [OPTION] [USERNAME]</span><br></pre></td></tr></table></figure>

<p>选项：</p>
<ul>
<li><code>-r</code>：删除用户的同时删除用户的家目录</li>
</ul>
<h3 id="usermod"><a href="#usermod" class="headerlink" title="usermod"></a>usermod</h3><p>该命令用于修改用户属性</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">usermod [OPTION] [USERNAME]</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-u</code>：修改用户 uid</li>
<li><code>-g</code>：修改用户 gid（改到的基本组必须是事先存在的组）</li>
<li><code>-G</code>：修改用户的附加组（注意：会覆盖原有的附加组）</li>
<li><code>-aG</code>：为用户添加附加组（在原有附加组的基础上添加）</li>
<li><code>-c</code>：修改注释信息</li>
<li><code>-d</code>：为用户指定新的家目录（原有家目录此用户无法访问）</li>
<li><code>-dm</code>：指定新的家目录并且将原有家目录的文件复制到新的家目录</li>
<li><code>-l</code>：修改用户的登陆名</li>
<li><code>-L</code>：锁定账号</li>
<li><code>-U</code>：解锁账号</li>
</ul>
<h3 id="id"><a href="#id" class="headerlink" title="id"></a>id</h3><p>该命令用于查看用户信息</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">id [OPTION] [USERNAME]</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-u</code>：显示用户的 uid</li>
<li><code>-g</code>：显示用户的 gid</li>
<li><code>-G</code>：显示用户所有组（基本组和附加组）的 id</li>
<li><code>-n</code>：显示名称而非 id</li>
</ul>
<h3 id="passwd"><a href="#passwd" class="headerlink" title="passwd"></a>passwd</h3><p>该命令用于修改用户密码</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">passwd [USERNAME]</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>--stdin</code>：能从管道读取密码，如 <code>echo &quot;password&quot; | passwd --stdin user1</code></li>
<li><code>-l</code>：锁定用户账号</li>
<li><code>-u</code>：解锁用户账号</li>
<li><code>-d</code>：删除用户密码（用户无法登陆）</li>
</ul>
<h3 id="chage"><a href="#chage" class="headerlink" title="chage"></a>chage</h3><p>该命令用于修改用户时间</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">chage [option] [username]</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-d</code>：用户最近一次的修改时间</li>
<li><code>-E</code>：用户的过期时间</li>
<li><code>-I</code>：用户的非活动时间</li>
<li><code>-m</code>：用户的最短使用期限</li>
<li><code>-M</code>：用户的最长使用期限</li>
<li><code>-W</code>：警告时间</li>
</ul>
<h3 id="chsh"><a href="#chsh" class="headerlink" title="chsh"></a>chsh</h3><p><code>chsh [USERNAME]</code>：修改用户的默认 shell</p>
<h3 id="chfn"><a href="#chfn" class="headerlink" title="chfn"></a>chfn</h3><p><code>chfn [USERNAME]</code>：修改用户的注释信息</p>
<h3 id="groupadd"><a href="#groupadd" class="headerlink" title="groupadd"></a>groupadd</h3><p><code>groupadd [OPTION] [GROUP]</code>：添加一个新的组<br>选项：</p>
<ul>
<li><code>-g</code>：为新创建的组指定 gid（默认为上一个 gid 大于 500 的后一个 id）</li>
<li><code>-r</code>：指定此组为系统组</li>
</ul>
<h3 id="groupmod"><a href="#groupmod" class="headerlink" title="groupmod"></a>groupmod</h3><p><code>group [OPTION] [GROUP]</code>：修改组的属性<br>选项：</p>
<ul>
<li><code>-g</code>：修改 gid</li>
<li><code>-n</code>：修改组名</li>
</ul>
<h3 id="groupdel"><a href="#groupdel" class="headerlink" title="groupdel"></a>groupdel</h3><p><code>groupdel [GROUP]</code>：删除一个组</p>
<h3 id="gpasswd"><a href="#gpasswd" class="headerlink" title="gpasswd"></a>gpasswd</h3><p><code>gpasswd [GROUP]</code>：为组设定密码</p>
<h3 id="chown"><a href="#chown" class="headerlink" title="chown"></a>chown</h3><ul>
<li><code>chown [option] username file...</code>：修改文件属主</li>
<li><code>chown [option] user:group file...</code>：修改文件的属主和属组</li>
<li><code>chown [option] user.group file...</code>：修改文件的属主和属组</li>
</ul>
<p>选项：</p>
<ul>
<li><code>-R</code>:递归修改目录及其内部文件的属主</li>
<li><code>--reference=/path/to/somefile file...</code>：将 file 文件的属主改为和 somefile 的属主一样</li>
</ul>
<h3 id="chgrp"><a href="#chgrp" class="headerlink" title="chgrp"></a>chgrp</h3><p><code>chgrp [option] group file...</code>：修改文件属组<br>选项：</p>
<ul>
<li><code>-R</code>：递归修改目录及其内部文件的属组</li>
<li><code>--reference=/path/to/somefile file...</code>：和 chown 用法一样</li>
</ul>
<h3 id="chmod"><a href="#chmod" class="headerlink" title="chmod"></a>chmod</h3><p>此命令用于修改文件权限，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">chmod [OPTION]... MODE[,MODE]... FILE...</span><br><span class="line">chmod [OPTION]... OCTAL-MODE FILE...</span><br><span class="line">chmod [OPTION]... --reference&#x3D;RFILE FILE...</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-R</code>：递归修改目录及其内部文件的权限</li>
<li><code>--reference</code>：和以上命令用法一样</li>
</ul>
<p>主要用法：</p>
<ul>
<li><strong>修改三类用户权限</strong>：<code>chmod MODE(八进制数，如755) file...</code>（<strong>注意</strong>：如果给的位数不够，会自动从前面补零）</li>
<li><strong>修改某类用户的权限</strong>：<code>chmod 用户类别=MODE file...</code>（用户类别有4种，分别为：属主用户u、属主用户g、其他用户o、所有用户a(a可以省略)。MODE只能用rwx，不能用八进制数,’-‘可以省略，如果修改多类用户权限一样，可以合并，如修改file的属主和属组用户权限为x：<code>chmod ug=x file</code>）</li>
<li><strong>修改某类用户的某位或者某些位权限</strong>：<code>chmod 用户类别+/-MODE file...</code></li>
<li><strong>修改特殊权限</strong>：<ul>
<li>设置 SUID：<code>chmod u+s FILE</code>（如果文件有执行权限就显示为s，否则显示S）</li>
<li>设置 SGID：<code>chmod g+s FILE</code></li>
<li>设置 Sticky：<code>chmod o+t FILE</code></li>
<li>同样的可以使用 8 进制数来表示<br>如 <code>chmod 3755 FILE</code> 就表示设置 SGID 和 Sticky 位</li>
</ul>
</li>
</ul>
<h3 id="su"><a href="#su" class="headerlink" title="su"></a>su</h3><p>此命令用于切换用户，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">su [OPTION]... [-] [USER [ARG]...]</span><br></pre></td></tr></table></figure>
<p>如果用户为 root 的话，用户名可以省略，如 <code>su -</code> 就是直接登陆到 root 上<br>还可以使用 <code>su -l USER -c &#39;COMMAND&#39;</code> 来以某个用户执行某条命令</p>
<h3 id="sudo"><a href="#sudo" class="headerlink" title="sudo"></a>sudo</h3><p>此命令可以让一个用户执行其他用户的权限而不用频繁的输入密码，sudo 的配置文件：<code>/etc/sudoers</code>，关键行：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root		ALL&#x3D;(ALL)		ALL</span><br><span class="line">%wheel		ALL&#x3D;(ALL)		ALL</span><br></pre></td></tr></table></figure>

<p>配置项：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">user			host&#x3D;(runas)		command</span><br></pre></td></tr></table></figure>

<p>解释：</p>
<ul>
<li>第一字段(who)：运行命令者的身份</li>
<li>第二字段(where)：通过哪些主机</li>
<li>第三字段(whom)：以哪个用户的身份</li>
<li>第四字段(which)：运行哪些命令，command前还可以加NOPASSWD表示这些命令不需要使用密码</li>
</ul>
<p>其中的 user 可以是用户名（USERNAME），可以是 #UID ，可以是用户别名，可以是 %组名，可以是 %#GID，前面还可以加 ! 来取反<br>host 可以是 ip，可以是 hostname，可以是 netaddr，也可以用 ! 取反<br>command 可以是 command name，可以是 directory，可以是 sudoedit<br>别名：可以将一系列的名称用一个别名来代替，每个字段都能取别名，格式：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">User_Alias NAME &#x3D; item1, item2, ...</span><br><span class="line">Host_Alias NAME &#x3D; item1, item2, ...</span><br><span class="line">Runas_Alias NAME &#x3D; item1, item2, ...</span><br><span class="line">Cmnd_Alias NAME &#x3D; item1, item2, ...</span><br></pre></td></tr></table></figure>
<p><strong>注意</strong>：这里的别名必须使用全大写<br>如：<code>Cmnd_Alias USERADMINCOMNDS = /usr/sbin/useradd, /usr/sbin/usermod, /usr/sbin/passwd [a-z]*, !/usr/sbin/passwd root</code>（后面的表示不允许更改root密码）</p>
<p>因此第一行的意思就是root用户可以通过任意主机以任意身份运行所有命令</p>
<p>第二行意思就是在wheel组中的用户可以通过任意主机以任意身份运行所有命令</p>
<p>选项：</p>
<ul>
<li><code>-V</code>：显示版本号</li>
<li><code>-l</code>：显示自己（执行 sudo 的使用者）的权限</li>
<li><code>-k</code>：强迫使用者在下一次执行 sudo 时访问密码（不论有没有超时）</li>
<li><code>-b</code>：将要执行的命令放在后台执行</li>
<li><code>-u USERNAME/#UID</code>：指定用户执行命令（默认为 root）</li>
</ul>
<h3 id="umask"><a href="#umask" class="headerlink" title="umask"></a>umask</h3><p>显示当前用户的遮罩码（第一个0可忽略）</p>
<p><code>umask MODE</code>：修改当前用户的遮罩码</p>
<p>遮罩码：用户创建文件的权限掩码</p>
<p>例如用户的遮罩码为002，则用这个用户创建文件时，文件的权限为666-002 = 664，创建目录时，文件的权限为777-002 = 775</p>
<p><strong>注意</strong>：所有文件默认不具有执行权限，如果算得的结果中有执行权限，则将其权限加 1<br>想要永久配置 umask 可以配置 <code>.bash_profile</code> (CentOS) 文件</p>
<h2 id="磁盘管理命令"><a href="#磁盘管理命令" class="headerlink" title="磁盘管理命令"></a>磁盘管理命令</h2><h3 id="ln"><a href="#ln" class="headerlink" title="ln"></a>ln</h3><p>该命令用于创建某个文件的 <strong>硬/软链接</strong>，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ln [option] SRC DEST</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-s</code>：对文件创建软链接，不加s就是创建硬链接</li>
<li><code>-v</code>：显示过程</li>
</ul>
<h3 id="du"><a href="#du" class="headerlink" title="du"></a>du</h3><p>该命令用于查看目录及其子目录所占用空间大小</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">du [OPTION]... [FILE]...</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-s</code>：仅查看参数目录</li>
<li><code>-h</code>：换算单位</li>
</ul>
<h3 id="df"><a href="#df" class="headerlink" title="df"></a>df</h3><p>查看磁盘分区（默认显示磁盘块使用情况）</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">df [OPTION]... [FILE]...</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-h</code>：单位换算</li>
<li><code>-i</code>：显示 iNode 使用情况</li>
</ul>
<h3 id="dd"><a href="#dd" class="headerlink" title="dd"></a>dd</h3><p>用于粘贴磁盘数据（与 cp 不同的是 dd 命令是直接粘贴磁盘的数据流）</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">dd [OPERAND]...</span><br></pre></td></tr></table></figure>
<p>用法：</p>
<ul>
<li>if=数据来源</li>
<li>of=数据存储目标</li>
<li>bs=数据块大小（可以用M表示兆）</li>
<li>count=数据块个数</li>
<li>seek=跳过空间大小</li>
</ul>
<p>磁盘拷贝：<code>dd if=/dev/sda of=/dev/sdb</code><br>备份 MBR：<code>dd if=/dev/sda of=/tmp/mbr.bak bs=512 count=1</code></p>
<h3 id="mknod"><a href="#mknod" class="headerlink" title="mknod"></a>mknod</h3><p>mknod 用于创建设备文件</p>
<p>用法：mknod [option]… NAME TYPE [MAJOR MINOR]</p>
<p><code>-m MODE</code>：指定权限</p>
<h3 id="fdisk"><a href="#fdisk" class="headerlink" title="fdisk"></a>fdisk</h3><p>查看当前系统识别了几块硬盘：<code>fdisk -l</code></p>
<p>管理磁盘分区：<br><code>fdisk /dev/SOMEFILE</code></p>
<ul>
<li>p: 显示当前硬件的分区，包括没保存的改动</li>
<li>n: 创建新分区<ul>
<li>e：扩展分区</li>
<li>p：主分区</li>
</ul>
</li>
<li>d：删除一个分区</li>
<li>w：保存退出</li>
<li>q：不保存退出</li>
<li>t：修改分区类型</li>
<li>l：显示所支持的所有类型</li>
</ul>
<h3 id="partx"><a href="#partx" class="headerlink" title="partx"></a>partx</h3><p>通知内核重读硬盘分区表</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">partx [-a|-d|-l] [--type TYPE] [--nr M-N] [partition] disk</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-a</code>：添加分区表（一般使用 <code>partx -a /dev/DEVICE</code>）</li>
<li><code>-n M:N</code>：指定读取 M-N 分区</li>
</ul>
<h3 id="partprobe"><a href="#partprobe" class="headerlink" title="partprobe"></a>partprobe</h3><p>更新分区表</p>
<h3 id="mkfs"><a href="#mkfs" class="headerlink" title="mkfs"></a>mkfs</h3><p>创建文件系统（注意：重新创建文件系统会损坏原有文件）</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mkfs [-V] [-t fstype] [fs-options] filesys [blocks]</span><br></pre></td></tr></table></figure>
<p><code>-t</code> 后直接跟上文件系统名称就可以创建文件系统</p>
<ul>
<li><code>mkfs -t ext2 = mkfs.ext2</code></li>
<li><code>mkfs -t ext3 = mkfs.ext3</code></li>
</ul>
<h3 id="mkfs-btrfs"><a href="#mkfs-btrfs" class="headerlink" title="mkfs.btrfs"></a>mkfs.btrfs</h3><p>专门用于创建和管理 btrfs 文件系统，此文件系统可以用多个磁盘来创建单个文件系统</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mkfs.btrfs [options] &lt;device&gt; [&lt;device&gt;...]</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-L LABEL</code>：指定卷标</li>
<li><code>-d type</code>：指定存储数据的机制，支持RAID0、RAID1、RAID5、RAID6、RAID10、single(只存单份数据，默认)</li>
<li><code>-m profile</code>：指定元数据存储类型，支持RAID0、RAID1、RAID5、RAID6、RAID10、single、dup</li>
</ul>
<p>查看文件系统的属性可以使用 <code>btrfs filesystem show &lt;device&gt;</code>来进行查看</p>
<h3 id="mke2fs"><a href="#mke2fs" class="headerlink" title="mke2fs"></a>mke2fs</h3><p>专门管理 ext 系列文件系统</p>
<ul>
<li><code>-t ext2|ext3|ext4</code>：指定文件系统</li>
<li><code>-j</code>：相当于 <code>-t ext3</code>，创建 ext3 的文件系统</li>
<li><code>-b BLOCK_SIZE</code>：指定块大小，默认为 4096，可取值为 1024、2048、4096</li>
<li><code>-L Label</code>：指定分区卷标</li>
<li><code>-m #</code>：指定预留给超级用户的块数百分比</li>
<li><code>-i #</code>：用于指定为多少字节的空间创建一个 iNode，默认为 8192，不能小于块大小</li>
<li><code>-N #</code>：用于指定 iNode 个数</li>
<li><code>-F</code>：强制创建文件系统</li>
<li><code>-E</code>：用户指定额外文件系统属性</li>
</ul>
<h3 id="e2label"><a href="#e2label" class="headerlink" title="e2label"></a>e2label</h3><p>用于查看或者定义 ext 系列文件系统的卷标</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">e2label [DEVICE] [LABEL]</span><br></pre></td></tr></table></figure>

<h3 id="tune2fs"><a href="#tune2fs" class="headerlink" title="tune2fs"></a>tune2fs</h3><p>调整 ext 系列文件系统的相关属性</p>
<ul>
<li><code>-j</code>：不损害原有数据，将ext2升级为ext3</li>
<li><code>-L Label</code>：设定或修改卷标</li>
<li><code>-m #</code>：调整预留百分比</li>
<li><code>-r #</code>：指定预留块数</li>
<li><code>-o</code>：设定默认挂载选项</li>
<li><code>-c #</code>：指定挂载次数达到#次之后进行自检，0或-1表关闭此功能</li>
<li><code>-i #</code>：每挂载使用多少天后进行自检；0或-1表示关闭此功能</li>
<li><code>-l</code>：显示超级块信息</li>
</ul>
<h3 id="dumpe2fs"><a href="#dumpe2fs" class="headerlink" title="dumpe2fs"></a>dumpe2fs</h3><p>显示文件系统属性信息</p>
<p><code>-h</code>：只显示超级块中的信息</p>
<h3 id="fsck"><a href="#fsck" class="headerlink" title="fsck"></a>fsck</h3><p>检查并修复 Linux 文件系统<br>也可使用 <code>fsck.FSTYPE</code></p>
<ul>
<li><code>-t FSTYPE</code>：指定文件系统类型</li>
<li><code>-a</code>：自动修复</li>
</ul>
<h3 id="e2fsck"><a href="#e2fsck" class="headerlink" title="e2fsck"></a>e2fsck</h3><p>专用于修复 ext2/ext3 文件系统</p>
<ul>
<li><code>-f</code>：强制检查</li>
<li><code>-p</code>：自动修复</li>
</ul>
<h3 id="blkid"><a href="#blkid" class="headerlink" title="blkid"></a>blkid</h3><p>查询或查看磁盘设备的相关属性，可以查看 UUID、LABEL 和文件系统类型</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">blkid [option] [&#x2F;dev&#x2F;DEVICE]</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-U UUID</code>：根据指定的 UUID 来查找对应的设备</li>
<li><code>-L LABEL</code>：根据指定的 LABEL 来查找对应的设备</li>
</ul>
<h3 id="mount"><a href="#mount" class="headerlink" title="mount"></a>mount</h3><p>可以用于查看当前系统上已挂载设备及挂载点，也可以进行挂载，格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mount 设备 挂载点</span><br></pre></td></tr></table></figure>
<p>设备可以是设备文件，也可以是卷标、UUID<br>挂载点则是一个目录，要求：1、此目录没有被其他进程所使用；2、目录得事先存在；3、目录中的原有文件将会暂时隐藏；<br>mount如果不接任何选项和参数的话还可以用于显示当前系统已挂载的设备及挂载点</p>
<p>选项：</p>
<ul>
<li><code>-a</code>：表示挂载/etc/fstab文件中定义的所有文件系统</li>
<li><code>-n</code>：默认情况下，mount命令没挂载一个设备，都会把挂载的设备信息保存到/etc/mtab文件中，使用-n选项时，不把信息写入此文件中</li>
<li><code>-t</code> FSTYPE：指定正在挂载设备上的文件系统的类型，不使用此选项时，mount会调用blkid命令获取对应文件系统的类型</li>
<li><code>-r</code>：只读挂载光盘时常用此选项</li>
<li><code>-w</code>：读写挂载</li>
<li><code>-o</code>：指定额外的挂载选项，即指定文件系统启用的属性<ul>
<li>remount：重新挂载当前文件系统</li>
<li>ro：挂载为只读</li>
<li>rw：挂载为读写</li>
<li>loop：挂载本地回环设备</li>
</ul>
</li>
</ul>
<p>挂载完成后，要通过挂载点访问对应文件系统上的文件<br>挂载光盘设备：<code>mount -r /dev/cdrom /media/cdrom</code></p>
<h3 id="umount"><a href="#umount" class="headerlink" title="umount"></a>umount</h3><p>卸载某文件系统<br>用法：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">umount 设备</span><br><span class="line">umount 挂载点</span><br></pre></td></tr></table></figure>
<p>注意：卸载时要确保挂载的设备没有进程使用</p>
<h3 id="free"><a href="#free" class="headerlink" title="free"></a>free</h3><p>显示系统中空闲空间和内存的使用量</p>
<ul>
<li><code>-m</code>：以兆为单位显示</li>
</ul>
<h3 id="mkswap"><a href="#mkswap" class="headerlink" title="mkswap"></a>mkswap</h3><p>创建交换分区，后直接跟设备名称</p>
<ul>
<li><code>-L Label</code>：指定卷标</li>
</ul>
<h3 id="swapon"><a href="#swapon" class="headerlink" title="swapon"></a>swapon</h3><p>开启交换设备，后直接跟设备名</p>
<ul>
<li><code>-a</code>：启用所有定义在 /etc/fstab 文件中的交换设备</li>
</ul>
<h3 id="swapoff"><a href="#swapoff" class="headerlink" title="swapoff"></a>swapoff</h3><p>关闭交换设备，后直接跟设备名</p>
<h3 id="fuser"><a href="#fuser" class="headerlink" title="fuser"></a>fuser</h3><p>验证进程正在使用的文件或套接字文件</p>
<ul>
<li><code>-v</code>：查看某文件上正在运行的进程</li>
</ul>
<p><code>fuser -km MOUNT_POINT</code>: 终止正在访问此挂载点的所有进程</p>
<h3 id="mdadm"><a href="#mdadm" class="headerlink" title="mdadm"></a>mdadm</h3><p>这是一个模式化的工具，用于对软 RAID 进行管理</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mdadm [MODE raiddevice] [OPTIONS] DEVICE..</span><br></pre></td></tr></table></figure>
<p>模式MODE：</p>
<ul>
<li>创建：-C</li>
<li>装配：-A</li>
<li>监控：-F</li>
<li>管理：-f，-r，-a</li>
</ul>
<p>raiddevice：<code>/dev/md#</code><br>DEVICE：任意块设备</p>
<h2 id="进程管理命令"><a href="#进程管理命令" class="headerlink" title="进程管理命令"></a>进程管理命令</h2><h3 id="ps"><a href="#ps" class="headerlink" title="ps"></a>ps</h3><p>此命令用于显示进程</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ps [options]</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>a</code>：显示所有与终端有关的进程</li>
<li><code>x</code>：显示所有与终端无关的进程</li>
<li><code>u</code>：以用户为中心组织进程状态信息显示</li>
<li><code>-e</code>：显示所有进程，相当于 ax</li>
<li><code>-l</code>：显示更为详细的信息</li>
<li><code>-f/F</code>：显示完整格式程序信息</li>
<li><code>-o PROPERTY1,PROPERTY2...</code>：显示指定的信息，如 <code>ps -o pid,comm,ni</code> 即为显示进程号、命令、nice值</li>
</ul>
<p>常用组合：ps aux，显示的信息格式如下：</p>
<p><code>USER PID %CPU %MEM VSZ RSS TTY STAT START   TIME COMMAND</code></p>
<p>其中：VSZ表示虚拟内存集、RSS表示常驻内存集、STAT为进程状态</p>
<h3 id="pstree"><a href="#pstree" class="headerlink" title="pstree"></a>pstree</h3><p>此命令用于显示当前系统上的进程树</p>
<h3 id="pgrep"><a href="#pgrep" class="headerlink" title="pgrep"></a>pgrep</h3><p>用于过滤进程信息</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">pgrep [option] pattern</span><br></pre></td></tr></table></figure>
<ul>
<li><code>-u uid</code>：effective user</li>
<li><code>-U uid</code>：real user</li>
<li><code>-t terminal</code>：与指定终端相关的进程</li>
<li><code>-l</code>：显示进程名</li>
<li><code>-a</code>：显示完整格式的进程名</li>
<li><code>-p pid</code>：显示其父进程为此处指定的进程的进程列表</li>
</ul>
<h3 id="pkill"><a href="#pkill" class="headerlink" title="pkill"></a>pkill</h3><p>用于杀死进程，用法和 pgrep 一样</p>
<h3 id="pidof"><a href="#pidof" class="headerlink" title="pidof"></a>pidof</h3><p>根据程序名，查找其相关进程的 ID 号</p>
<h3 id="top"><a href="#top" class="headerlink" title="top"></a>top</h3><p>动态显示当前系统上所有进程的信息</p>
<ul>
<li><code>-d</code>：指定延迟时长，单位为秒</li>
<li><code>-b</code>：批模式，通常用 -n 来配合使用</li>
<li><code>-n #</code>：在批模式下，共显示 # 批</li>
</ul>
<p>在 top 中，还有一些常用命令：</p>
<ul>
<li>M：根据驻留内存大小进行排序</li>
<li>P：根据CPU使用百分比进行排序</li>
<li>T：根据累计时间进行排序</li>
<li>l：是否显示    平均负载和启动时间</li>
<li>t：是否显示进程和CPU状态相关信息</li>
<li>m：是否显示内存相关信息</li>
<li>c：是否显示完整的命令行信息</li>
<li>q：退出top</li>
<li>k：终止某个进程</li>
</ul>
<h3 id="vmstat"><a href="#vmstat" class="headerlink" title="vmstat"></a>vmstat</h3><p>此命令用于查看虚拟内存状态</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vmstat [options] [delay [count]]</span><br></pre></td></tr></table></figure>
<p>procs：</p>
<ul>
<li>r：等待运行的进程的个数；</li>
<li>b：处于不可中断睡眠状态的进程的个数（被阻塞的队列长度）</li>
<li>emory：</li>
<li>swpd：交换内存的使用总量</li>
<li>free：空间物理内存总量</li>
<li>buffer：用于buffer的内存总量</li>
<li>cache：用于cache的内存总量</li>
</ul>
<p>swap：</p>
<ul>
<li>si：数据进入swap中的数据速率</li>
<li>so：数据离开swap的数据速率</li>
</ul>
<p>io：</p>
<ul>
<li>bi：从块设备读入数据到系统的速率</li>
<li>bo：保存数据至块设备的速率</li>
</ul>
<p>system：</p>
<ul>
<li>in：终端速率</li>
<li>cs：进程切换速率</li>
</ul>
<p>cpu：</p>
<ul>
<li>us：用户空间占据比例</li>
<li>sy：内核空间占据CPU比例</li>
<li>id：空闲比例</li>
<li>wa：等待io完成所消耗的时间比例</li>
<li>st：被虚拟化”偷走”的时间比例</li>
</ul>
<h3 id="pmap"><a href="#pmap" class="headerlink" title="pmap"></a>pmap</h3><p>显示进程相关的映射信息</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">pmap [option] pid</span><br></pre></td></tr></table></figure>
<ul>
<li><code>-x</code>：显示详细格式的信息</li>
</ul>
<h3 id="kill"><a href="#kill" class="headerlink" title="kill"></a>kill</h3><p>此命令用于杀死某个进程，也可用于进程间的信号通信</p>
<p>使用 <code>kill -l</code> 可以查看所有的信号，其中常用的信号有：1 SIGHUP（让一个进程不用重启就可以读取配置文件，并让新的配置文件生效）；2 SIGINT（此信号相当于Ctrl+c，中断一个进程）；9 SIGKILL（杀死一个进程）；15 SIGTERM（终止一个进程，此信号为终止进程的默认信号）</p>
<p>kill 用法：<code>kill [-signal] PID</code><br>其中 <code>-signal</code> 可以指定信号号码，如-1即指定SIGHUP，也可指定信号名称，如-SIGHUP，还可以指定名称简写，如-KILL即指定SIGKILL</p>
<p>如果要结束一个命令的所有进程，还可以使用 <code>killall COMMAND</code></p>
<p>kill 命令还可以终止某作业：<code>kill %JOBID</code></p>
<h3 id="renice"><a href="#renice" class="headerlink" title="renice"></a>renice</h3><p>此命令用于调整进程的 nice 值，使用格式如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">renice NI PID</span><br></pre></td></tr></table></figure>

<h3 id="bg"><a href="#bg" class="headerlink" title="bg"></a>bg</h3><p>此命令可以让停止的作业继续运行，格式：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bg [[%]JOBID]</span><br></pre></td></tr></table></figure>

<h3 id="jobs"><a href="#jobs" class="headerlink" title="jobs"></a>jobs</h3><p>此命令用于查看后台的所有作业</p>
<p>可以显示作业号，后面的+表示命令将默认操作的作业，-表示命令将第二个默认操作的作业</p>
<h3 id="fg"><a href="#fg" class="headerlink" title="fg"></a>fg</h3><p>此命令将后台的作业调回前台<br>用法：<code>fg [[%]JOBID]</code></p>
<h3 id="nice"><a href="#nice" class="headerlink" title="nice"></a>nice</h3><p>调整进程启动时的nice值</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">nice [option] [COMMAND [AGR]...]</span><br></pre></td></tr></table></figure>

<h3 id="exec"><a href="#exec" class="headerlink" title="exec"></a>exec</h3><p>启动一个进程，并让这个进程直接替换父进程来执行，而不是以父进程的子进程执行</p>
<h3 id="trap"><a href="#trap" class="headerlink" title="trap"></a>trap</h3><p>此命令用于捕捉信号</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">trap &#39;COMMAND&#39; 信号列表</span><br></pre></td></tr></table></figure>
<p>就是当看见信号的时候执行COMMAND命令然后就捕捉掉这个信号<br>注意：9号信号和15号信号不能捕捉；COMMAND命令中可以有多条命令，用分号隔开。</p>
<p>同样的，COMMAND命令也可以是一个函数，这样就可以将许多命令放在一个函数中执行了</p>
<h3 id="at"><a href="#at" class="headerlink" title="at"></a>at</h3><p>此命令用于在未来的某个时间执行某个作业<br>用法 <code>at TIME</code>，然后会出现&gt;提示符，提示输入命令，输完命令后按Ctrl+d键结束<br>其中，时间可用绝对时间，如HH:MM, DD.MM.YY, MM/DD/YY<br>也可使用相对时间：now+#，单位有minutes，hours，days，weeks</p>
<ul>
<li><code>-l</code>：显示将运行的作业</li>
<li><code>-d JOBID</code>：删除某个作业</li>
</ul>
<p>作业执行完毕后是以邮件发给用户的</p>
<h3 id="batch"><a href="#batch" class="headerlink" title="batch"></a>batch</h3><p>此命令和 at 命令类似，也是在某个时间执行某个任务，和at不同的是，batch不能指定时间，而是让系统自行选择空闲时间去执行某个指定的任务</p>
<h2 id="网络配置命令"><a href="#网络配置命令" class="headerlink" title="网络配置命令"></a>网络配置命令</h2><h3 id="ifconfig"><a href="#ifconfig" class="headerlink" title="ifconfig"></a>ifconfig</h3><p>此命令用于查看或配置网卡</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ifconfig [interface]</span><br><span class="line">ifconfig interface [aftype] options | address ...</span><br><span class="line">ifconfig -a：查看所有网卡，默认为查看已启用网卡</span><br><span class="line">ifconfig INTERFACE [up|down]</span><br><span class="line">ifconfig INTERFACE IP&#x2F;mask [up]</span><br></pre></td></tr></table></figure>

<p>这里的配置会立即生效，但不会永久有效</p>
<h3 id="route"><a href="#route" class="headerlink" title="route"></a>route</h3><p>此命令用于显示或者管理路由表</p>
<ul>
<li>查看路由：<code>route -n</code></li>
<li>添加：<code>route add</code></li>
</ul>
<p><code>route add [-net|-host] target [netmask Nm] [gw Gw] [[dev] If]</code></p>
<p>如添加一条路由：目标192.168.1.3 网关：172.16.0.1，只允许eth0设备通过：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># route add -net 192.168.1.3 netmask 255.255.255.0 gw 172.16.0.1 dev eth0</span><br><span class="line"># route add -net 192.168.1.3&#x2F;24 gw 172.16.0.1 dev eth0</span><br></pre></td></tr></table></figure>

<p>如设置默认路由，网关为172.16.0.1：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># route add -net 0.0.0.0 netmask 0.0.0.0 gw 172.16.0.1</span><br><span class="line"># route add default gw 172.16.0.1</span><br></pre></td></tr></table></figure>

<p>删除：<code>route del</code></p>
<p><code>route del [-net|-host] target [netmask Nm] [gw Gw] [[dev] If]</code></p>
<p>删除路由只指明目标即可，没必要指明下一跳网关</p>
<p>这里配置路由也会立即生效，但不会永久有效</p>
<h3 id="netstat"><a href="#netstat" class="headerlink" title="netstat"></a>netstat</h3><p>此命令用于显示网络相关状态</p>
<ul>
<li><p>显示网络连接</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">netstat [--tcp|-t] [--udp|-u] [--raw|-w] [--listening|-l] [--all|-a] [--extend|-e[--extend|-e]] [--program|-p]</span><br></pre></td></tr></table></figure>
<ul>
<li>-t：tcp协议相关</li>
<li>-u：udp协议相关</li>
<li>-w：raw socket相关</li>
<li>-l：处于监听状态</li>
<li>-a：所有状态</li>
<li>-n：以数字显示IP和端口</li>
<li>-e：扩展格式</li>
<li>-p：显示相关进程及PID</li>
</ul>
<p>常用组合：<br>-tan, -uan, -tnl, -unl</p>
</li>
<li><p>显示路由表</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">netstat &#123;--route|-r&#125; [--numeric|-n]</span><br></pre></td></tr></table></figure>
<ul>
<li>-r：显示内核路由表</li>
<li>-n：以数字显示</li>
</ul>
</li>
<li><p>显示接口统计数据</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">netstat &#123;--interfaces|-I|-i&#125; [INTERFACE] [--all|-a] [--extend|-e] [--program|-p] [--numeric|-n]</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h3 id="ip"><a href="#ip" class="headerlink" title="ip"></a>ip</h3><p>此命令用于配置网络</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ip [ OPTIONS ] OBJECT &#123; COMMAND | help &#125;</span><br></pre></td></tr></table></figure>
<p>OBJECT：={ link | addr | route }</p>
<ul>
<li>link：网络接口属性相关<ul>
<li>show：查看接口属性，如 <code>ip -s link show</code></li>
<li>set：配置接口，如 <code>ip link set DEV up|down</code></li>
</ul>
</li>
<li>addr：协议地址相关<ul>
<li>add：添加地址，如 <code>ip addr add ADDRESS dev DEV</code>，<code>ip addr add 192.168.0.1/24 dev eth0</code>，此命令中还可以通过 label 指定别名</li>
<li>del：删除地址，<code>ip addr del ADDRESS dev DEV</code></li>
<li>show：显示信息，如：<code>ip addr show dev DEV to PREFIX</code></li>
<li>flush：清空，如：<code>ip addr flush dev DEV to PREFIX</code></li>
</ul>
</li>
<li>route：路由相关</li>
</ul>
<p>含有各种子命令，通过man ip查看，不一一列举。如：<code>ip route add to 10.0.1.0/24 dev eth1 via 192.168.100.6</code>。常用子命令有：add, change, show, flush, replace。</p>
<h3 id="ss"><a href="#ss" class="headerlink" title="ss"></a>ss</h3><p>网络状态查看工具</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ss [OPTION]... [FILTER]</span><br></pre></td></tr></table></figure>
<p>选项：</p>
<ul>
<li><code>-t</code>：tcp协议相关</li>
<li><code>-u</code>：udp协议相关</li>
<li><code>-w</code>：裸套接字相关</li>
<li><code>-x</code>：unix sock相关</li>
<li><code>-l</code>：listen状态的连接</li>
<li><code>-a</code>：所有</li>
<li><code>-n</code>：数字格式</li>
<li><code>-p</code>：相关的程序及PID</li>
<li><code>-e</code>：扩展的信息</li>
<li><code>-m</code>：内存用量</li>
<li><code>-o</code>：计时器信息</li>
</ul>
<p>FILTER：=[ state TCP-STATE ] [ EXPRESSION ]</p>
<p>TCP常见的状态：</p>
<ul>
<li>LISTEN：监听</li>
<li>ESTABLISHED：已建立的连接</li>
<li>CLOSED：关闭</li>
</ul>
<p>常用组合：-tan, -tanl, -tanlp, -uan, -tunlp</p>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux 启动设置</title>
    <url>/linux/f2df8a80-8428-4044-8432-437966b910c5.html</url>
    <content><![CDATA[<p>本文介绍 Linux 的启动脚本加载顺序。</p>
<a id="more"></a>

<h2 id="linux-启动顺序"><a href="#linux-启动顺序" class="headerlink" title="linux 启动顺序"></a>linux 启动顺序</h2><ol>
<li>图形模式登录时，顺序读取：<code>/etc/profile</code> 和 <code>~/.profile</code></li>
<li>图形模式登录后，打开终端时，顺序读取：<code>/etc/bash.bashrc</code> 和 <code>~/.bashrc</code></li>
<li>文本模式登录时，顺序读取：<code>/etc/bash.bashrc</code>，<code>/etc/profile</code> 和 <code>~/.bash_profile</code></li>
<li>从其它用户 <code>su</code> 到该用户，则分两种情况：<ol>
<li>如果带 <code>-l</code> 参数（或 <code>-</code> 参数，<code>-login</code> 参数），如：<code>su -l username</code>，则 bash 是 lonin 的，它将顺序读取以下配置文件：<code>/etc/bash.bashrc</code>，<code>/etc/profile</code> 和 <code>~ /.bash_profile</code>。</li>
<li>如果没有带 <code>-l</code> 参数，则 bash 是 non-login 的，它将顺序读取：<code>/etc/bash.bashrc</code> 和 <code>~/.bashrc</code></li>
</ol>
</li>
<li>注销时，或退出 <code>su</code> 登录的用户，如果是 longin 方式，那么 bash 会读取：<code>~/.bash_logout</code></li>
<li>执行自定义的 shell 文件时，若使用 <code>bash -l a.sh</code> 的方式，则 bash 会读取行：<code>/etc/profile</code> 和 <code>~/.bash_profile</code>，若使用其它方式，如：<code>bash a.sh</code>， <code>./a.sh</code>，<code>sh a.sh</code>（这个不属于bash shell），则不会读取上面的任何文件。</li>
</ol>
<h2 id="linux-启动配置"><a href="#linux-启动配置" class="headerlink" title="linux 启动配置"></a>linux 启动配置</h2><p><code>/etc/bash.bashrc</code> 后加入：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> [ -d /etc/bash.d ]; <span class="keyword">then</span></span><br><span class="line">  <span class="keyword">for</span> i <span class="keyword">in</span> /etc/bash.d/*; <span class="keyword">do</span></span><br><span class="line">    <span class="keyword">if</span> [ -r <span class="variable">$i</span> ]; <span class="keyword">then</span></span><br><span class="line">      . <span class="variable">$i</span></span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">  <span class="keyword">done</span></span><br><span class="line">  <span class="built_in">unset</span> i</span><br><span class="line"><span class="keyword">fi</span></span><br></pre></td></tr></table></figure>

<p>创建 <code>/etc/bash.d</code> 目录，其中创建需要配置的文件：</p>
<p><code>/etc/bash.d/setalias.sh</code>：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">alias</span> rm=<span class="string">'rm'</span></span><br><span class="line"><span class="built_in">alias</span> cp=<span class="string">'cp'</span></span><br><span class="line"><span class="built_in">alias</span> mv=<span class="string">'mv'</span></span><br><span class="line"><span class="built_in">alias</span> cls=<span class="string">'clear'</span></span><br><span class="line"><span class="built_in">alias</span> ll=<span class="string">'ls -lh'</span></span><br><span class="line"><span class="built_in">alias</span> la=<span class="string">'ls -A'</span></span><br><span class="line"><span class="built_in">alias</span> vi=<span class="string">'vim'</span></span><br><span class="line"><span class="built_in">alias</span> mysql=<span class="string">'mysql -h 127.0.0.1'</span></span><br><span class="line"><span class="built_in">alias</span> vimhosts=<span class="string">'vim /mnt/c/Windows/System32/drivers/etc/hosts'</span></span><br><span class="line"><span class="built_in">alias</span> redis-cli=<span class="string">"docker run -it --network app_net --rm redis redis-cli -h redis"</span></span><br><span class="line"><span class="built_in">alias</span> mysql=<span class="string">"docker run -it --network app_net --rm mysql:5.7 mysql -uroot -p123456 -h mysql"</span></span><br><span class="line"></span><br><span class="line"><span class="comment">##################################</span></span><br><span class="line"><span class="comment"># Color alias</span></span><br><span class="line"><span class="comment">##################################</span></span><br><span class="line"><span class="keyword">if</span> [ -x /usr/bin/dircolors ]; <span class="keyword">then</span></span><br><span class="line">  <span class="built_in">test</span> -r ~/.dircolors &amp;&amp; <span class="built_in">eval</span> <span class="string">"<span class="variable">$(dircolors -b ~/.dircolors)</span>"</span> || <span class="built_in">eval</span> <span class="string">"<span class="variable">$(dircolors -b)</span>"</span></span><br><span class="line">  <span class="built_in">alias</span> ls=<span class="string">'ls --color=auto'</span></span><br><span class="line">  <span class="built_in">alias</span> grep=<span class="string">'grep --color=auto'</span></span><br><span class="line">  <span class="built_in">alias</span> fgrep=<span class="string">'fgrep --color=auto'</span></span><br><span class="line">  <span class="built_in">alias</span> egrep=<span class="string">'egrep --color=auto'</span></span><br><span class="line"><span class="keyword">fi</span></span><br></pre></td></tr></table></figure>

<p><code>/etc/bash.d/setpath.sh</code>：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">PATH=<span class="string">"/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/snap/bin"</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">export</span> JAVA_HOME=<span class="string">'/usr/local/jdk1.8.0_91'</span></span><br><span class="line"><span class="built_in">export</span> MAVEN_HOME=<span class="string">'/usr/local/apache-maven-3.6.3'</span></span><br><span class="line"><span class="built_in">export</span> M2_HOME=<span class="string">'/usr/local/apache-maven-3.6.3'</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$PATH</span>:<span class="variable">$JAVA_HOME</span>/bin:<span class="variable">$MAVEN_HOME</span>/bin</span><br></pre></td></tr></table></figure>

<p><code>/etc/bash.d/setothers.sh</code>：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> [ <span class="string">"<span class="variable">$TERM</span>"</span> == <span class="string">"xterm"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">export</span> TERM=xterm-256color</span><br><span class="line"><span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># set a fancy prompt (non-color, unless we know we "want" color)</span></span><br><span class="line"><span class="keyword">case</span> <span class="string">"<span class="variable">$TERM</span>"</span> <span class="keyword">in</span></span><br><span class="line">    xterm-color|*-256color) color_prompt=yes;;</span><br><span class="line"><span class="keyword">esac</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># uncomment for a colored prompt, if the terminal has the capability; turned</span></span><br><span class="line"><span class="comment"># off by default to not distract the user: the focus in a terminal window</span></span><br><span class="line"><span class="comment"># should be on the output of commands, not on the prompt</span></span><br><span class="line"><span class="comment">#force_color_prompt=yes</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> [ -n <span class="string">"<span class="variable">$force_color_prompt</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">  <span class="keyword">if</span> [ -x /usr/bin/tput ] &amp;&amp; tput setaf 1 &gt;&amp;/dev/null; <span class="keyword">then</span></span><br><span class="line">    <span class="comment"># We have color support; assume it's compliant with Ecma-48</span></span><br><span class="line">    <span class="comment"># (ISO/IEC-6429). (Lack of such support is extremely rare, and such</span></span><br><span class="line">    <span class="comment"># a case would tend to support setf rather than setaf.)</span></span><br><span class="line">    color_prompt=yes</span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">    color_prompt=</span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line"><span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> [ <span class="string">"<span class="variable">$color_prompt</span>"</span> = yes ]; <span class="keyword">then</span></span><br><span class="line">  <span class="comment"># "\e[F;B;Tm]...\e[0m\]用于设置颜色，其中F表示前景色(30~37)，B表示背景色(40~47)</span></span><br><span class="line">  <span class="comment"># T可设置0 1 4 5 7 8分别表示：OFF,高亮,underline,闪烁,反白,不可见</span></span><br><span class="line">  PS1=<span class="string">'\[\033[35;1m\]\u@WSL\[\033[0m\] \[\033[34m\]\W\[\033[0m\]\$ '</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">#PS1='$&#123;debian_chroot:+($debian_chroot)&#125;\[\033[01;32m\]\u@\h\[\033[00m\]:\[\033[01;34m\]\w\[\033[00m\]\$ '</span></span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">  PS1=<span class="string">'$&#123;debian_chroot:+($debian_chroot)&#125;\u@\h:\w\$ '</span></span><br><span class="line"><span class="keyword">fi</span></span><br><span class="line"><span class="built_in">unset</span> color_prompt force_color_prompt</span><br><span class="line"></span><br><span class="line"><span class="comment"># If this is an xterm set the title to user@host:dir</span></span><br><span class="line"><span class="keyword">case</span> <span class="string">"<span class="variable">$TERM</span>"</span> <span class="keyword">in</span></span><br><span class="line">xterm*|rxvt*)</span><br><span class="line">  PS1=<span class="string">"\[\e]0;<span class="variable">$&#123;debian_chroot:+($debian_chroot)&#125;</span>\u@\h: \w\a\]<span class="variable">$PS1</span>"</span></span><br><span class="line">  ;;</span><br><span class="line">*)</span><br><span class="line">  ;;</span><br><span class="line"><span class="keyword">esac</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 解决 screen 在非 root 用户下无法使用的问题</span></span><br><span class="line"><span class="built_in">export</span> SCREENDIR=<span class="variable">$HOME</span>/.screen</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>bash</tag>
      </tags>
  </entry>
</search>
